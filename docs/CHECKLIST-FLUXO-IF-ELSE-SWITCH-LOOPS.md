# Checklist das ocorrências de fluxo — OTTHI World Edu V644

Inventário lexical de todas as ocorrências encontradas de `if`, `else`, `switch`, `case`, `for`, `while`, `catch`, `return` e `throw`. Um trecho pode conter mais de uma ocorrência.


## `00-runtime-foundation.js`

- [x] `if` — linha 22, coluna 79 — `const lerpAngle = (a, b, t) => { let d=((b-a+Math.PI)%(Math.PI*2))-Math.PI; if(d<-Math.PI)d+=Math.PI*2; return a+d*t; };`
- [x] `return` — linha 22, coluna 107 — `const lerpAngle = (a, b, t) => { let d=((b-a+Math.PI)%(Math.PI*2))-Math.PI; if(d<-Math.PI)d+=Math.PI*2; return a+d*t; };`
- [x] `if` — linha 25, coluna 54 — `const safePointerCapture=(element,pointerId)=>{try{if(element?.setPointerCapture&&Number.isInteger(pointerId))element.setPointerCapture(pointerId);return true;}catch{return false;}};`
- [x] `return` — linha 25, coluna 150 — `const safePointerCapture=(element,pointerId)=>{try{if(element?.setPointerCapture&&Number.isInteger(pointerId))element.setPointerCapture(pointerId);return true;}catch{return false;}};`
- [x] `catch` — linha 25, coluna 163 — `const safePointerCapture=(element,pointerId)=>{try{if(element?.setPointerCapture&&Number.isInteger(pointerId))element.setPointerCapture(pointerId);return true;}catch{return false;}};`
- [x] `return` — linha 25, coluna 169 — `const safePointerCapture=(element,pointerId)=>{try{if(element?.setPointerCapture&&Number.isInteger(pointerId))element.setPointerCapture(pointerId);return true;}catch{return false;}};`
- [x] `if` — linha 26, coluna 54 — `const safePointerRelease=(element,pointerId)=>{try{if(element?.hasPointerCapture?.(pointerId))element.releasePointerCapture(pointerId);return true;}catch{return false;}};`
- [x] `return` — linha 26, coluna 138 — `const safePointerRelease=(element,pointerId)=>{try{if(element?.hasPointerCapture?.(pointerId))element.releasePointerCapture(pointerId);return true;}catch{return false;}};`
- [x] `catch` — linha 26, coluna 151 — `const safePointerRelease=(element,pointerId)=>{try{if(element?.hasPointerCapture?.(pointerId))element.releasePointerCapture(pointerId);return true;}catch{return false;}};`
- [x] `return` — linha 26, coluna 157 — `const safePointerRelease=(element,pointerId)=>{try{if(element?.hasPointerCapture?.(pointerId))element.releasePointerCapture(pointerId);return true;}catch{return false;}};`
- [x] `return` — linha 30, coluna 39 — `const safeLocalGet = key => { try { return window.localStorage?.getItem(key) ?? null; } catch { return null; } };`
- [x] `catch` — linha 30, coluna 91 — `const safeLocalGet = key => { try { return window.localStorage?.getItem(key) ?? null; } catch { return null; } };`
- [x] `return` — linha 30, coluna 99 — `const safeLocalGet = key => { try { return window.localStorage?.getItem(key) ?? null; } catch { return null; } };`
- [x] `return` — linha 31, coluna 90 — `const safeLocalSet = (key, value) => { try { window.localStorage?.setItem(key, value); return true; } catch { return false; } };`
- [x] `catch` — linha 31, coluna 105 — `const safeLocalSet = (key, value) => { try { window.localStorage?.setItem(key, value); return true; } catch { return false; } };`
- [x] `return` — linha 31, coluna 113 — `const safeLocalSet = (key, value) => { try { window.localStorage?.setItem(key, value); return true; } catch { return false; } };`
- [x] `return` — linha 32, coluna 80 — `const safeLocalRemove = key => { try { window.localStorage?.removeItem(key); return true; } catch { return false; } };`
- [x] `catch` — linha 32, coluna 95 — `const safeLocalRemove = key => { try { window.localStorage?.removeItem(key); return true; } catch { return false; } };`
- [x] `return` — linha 32, coluna 103 — `const safeLocalRemove = key => { try { window.localStorage?.removeItem(key); return true; } catch { return false; } };`
- [x] `return` — linha 38, coluna 5 — `return allowed.includes(candidate)`
- [x] `return` — linha 44, coluna 5 — `return window.OTTHI_CONFIG?.rooms?.find(item => item.id === id)?.name || 'Bairro Central';`

## `00a-visual-foundation-avatar-v2.js`

- [x] `return` — linha 118, coluna 5 em `safeAvatarId()` — `return OTTHI_AVATAR_SAFE_ID.test(candidate)?candidate:fallback;`
- [x] `return` — linha 122, coluna 5 em `safeAvatarColor()` — `return OTTHI_AVATAR_SAFE_COLOR.test(candidate)?candidate.toLowerCase():fallback;`
- [x] `return` — linha 126, coluna 5 em `safeLegacyAvatarChoice()` — `return allowed.includes(value)?value:fallback;`
- [x] `return` — linha 129, coluna 5 em `defaultAvatarV2()` — `return {...OTTHI_AVATAR_V2_DEFAULTS};`
- [x] `return` — linha 139, coluna 5 em `normalizeAvatarV2()` — `return {`
- [x] `if` — linha 163, coluna 5 em `updateAvatarV2LegacyChoice()` — `if(!Object.prototype.hasOwnProperty.call(OTTHI_AVATAR_LEGACY_CHOICES,type))return current;`
- [x] `return` — linha 163, coluna 80 em `updateAvatarV2LegacyChoice()` — `if(!Object.prototype.hasOwnProperty.call(OTTHI_AVATAR_LEGACY_CHOICES,type))return current;`
- [x] `if` — linha 167, coluna 5 em `updateAvatarV2LegacyChoice()` — `if(type==='outfit')next.torso=\`legacy-outfit-${selected}\`;`
- [x] `if` — linha 168, coluna 5 em `updateAvatarV2LegacyChoice()` — `if(type==='accessory')next.back=selected==='backpack'?'legacy-backpack':'none';`
- [x] `return` — linha 169, coluna 5 em `updateAvatarV2LegacyChoice()` — `return normalizeAvatarV2(next);`
- [x] `if` — linha 194, coluna 5 em `registerVisualAsset()` — `if(!key)return null;`
- [x] `return` — linha 194, coluna 13 em `registerVisualAsset()` — `if(!key)return null;`
- [x] `return` — linha 198, coluna 5 em `registerVisualAsset()` — `return record;`
- [x] `return` — linha 201, coluna 5 em `updateVisualAsset()` — `return registerVisualAsset(id,null,metadata);`
- [x] `for` — linha 207, coluna 5 em `visualAssetStatus()` — `for(const record of records){`
- [x] `return` — linha 212, coluna 5 em `visualAssetStatus()` — `return{total:records.length,byType,byStatus};`
- [x] `return` — linha 215, coluna 5 em `visualQualityProfile()` — `return OTTHI_VISUAL_QUALITY_PROFILES[tier]||OTTHI_VISUAL_QUALITY_PROFILES.balanced;`
- [x] `if` — linha 218, coluna 5 em `createManagedLOD()` — `if(!highObject)return null;`
- [x] `return` — linha 218, coluna 20 em `createManagedLOD()` — `if(!highObject)return null;`
- [x] `if` — linha 220, coluna 5 em `createManagedLOD()` — `if(!Three?.LOD||!lowObject){`
- [x] `return` — linha 222, coluna 7 em `createManagedLOD()` — `return highObject;`
- [x] `return` — linha 235, coluna 5 em `createManagedLOD()` — `return lod;`
- [x] `for` — linha 241, coluna 5 em `applyVisualQualityBudget()` — `for(const record of visualFoundation.lods){`
- [x] `if` — linha 243, coluna 7 em `applyVisualQualityBudget()` — `if(level)level.distance=Math.max(8,record.baseDistance*profile.lodDistanceScale);`
- [x] `return` — linha 245, coluna 5 em `applyVisualQualityBudget()` — `return profile;`
- [x] `if` — linha 248, coluna 5 em `updateManagedVisualLODs()` — `if(!activeCamera)return;`
- [x] `return` — linha 248, coluna 22 em `updateManagedVisualLODs()` — `if(!activeCamera)return;`
- [x] `for` — linha 249, coluna 5 em `updateManagedVisualLODs()` — `for(const record of visualFoundation.lods){`
- [x] `catch` — linha 253, coluna 8 em `updateManagedVisualLODs()` — `}catch(error){`
- [x] `while` — linha 260, coluna 5 em `outlineBelongsToPlayer()` — `while(owner){`
- [x] `if` — linha 261, coluna 7 em `outlineBelongsToPlayer()` — `if(owner===playerGroup||owner===playerModel||owner===vehicleVisual||owner.userData?.alwaysOutline===true)return true;`
- [x] `return` — linha 261, coluna 112 em `outlineBelongsToPlayer()` — `if(owner===playerGroup||owner===playerModel||owner===vehicleVisual||owner.userData?.alwaysOutline===true)return true;`
- [x] `return` — linha 264, coluna 5 em `outlineBelongsToPlayer()` — `return false;`
- [x] `if` — linha 267, coluna 5 em `updateManagedOutlineVisibility()` — `if(!world?.outlines||!camera)return;`
- [x] `return` — linha 267, coluna 34 em `updateManagedOutlineVisibility()` — `if(!world?.outlines||!camera)return;`
- [x] `for` — linha 271, coluna 5 em `updateManagedOutlineVisibility()` — `for(const line of world.outlines){`
- [x] `if` — linha 272, coluna 7 em `updateManagedOutlineVisibility()` — `if(!line?.parent)continue;`
- [x] `if` — linha 274, coluna 7 em `updateManagedOutlineVisibility()` — `if(keep){`
- [x] `if` — linha 282, coluna 7 em `updateManagedOutlineVisibility()` — `if(show)visible++;else hidden++;`
- [x] `else` — linha 282, coluna 25 em `updateManagedOutlineVisibility()` — `if(show)visible++;else hidden++;`
- [x] `for` — linha 289, coluna 5 em `visualFoundationDiagnostics()` — `for(const record of visualFoundation.lods){`
- [x] `if` — linha 290, coluna 7 em `visualFoundationDiagnostics()` — `if(record.currentLevel>0)lodLevels.far++;else lodLevels.near++;`
- [x] `else` — linha 290, coluna 48 em `visualFoundationDiagnostics()` — `if(record.currentLevel>0)lodLevels.far++;else lodLevels.near++;`
- [x] `return` — linha 292, coluna 5 em `visualFoundationDiagnostics()` — `return{`
- [x] `if` — linha 315, coluna 5 em `createAvatarRigFoundation()` — `if(!root||!bodyRoot||!parts)return null;`
- [x] `return` — linha 315, coluna 33 em `createAvatarRigFoundation()` — `if(!root||!bodyRoot||!parts)return null;`
- [x] `if` — linha 318, coluna 5 em `createAvatarRigFoundation()` — `if(parts.body)parts.body.name='TORSO';`
- [x] `if` — linha 319, coluna 5 em `createAvatarRigFoundation()` — `if(parts.head)parts.head.name='HEAD';`
- [x] `if` — linha 320, coluna 5 em `createAvatarRigFoundation()` — `if(parts.leftArm)parts.leftArm.name='ARM_LEFT';`
- [x] `if` — linha 321, coluna 5 em `createAvatarRigFoundation()` — `if(parts.rightArm)parts.rightArm.name='ARM_RIGHT';`
- [x] `if` — linha 322, coluna 5 em `createAvatarRigFoundation()` — `if(parts.leftLeg)parts.leftLeg.name='LEG_LEFT';`
- [x] `if` — linha 323, coluna 5 em `createAvatarRigFoundation()` — `if(parts.rightLeg)parts.rightLeg.name='LEG_RIGHT';`
- [x] `return` — linha 329, coluna 7 em `createAvatarRigFoundation()` — `return group;`
- [x] `return` — linha 346, coluna 5 em `createAvatarRigFoundation()` — `return root.userData.avatarRig;`
- [x] `return` — linha 350, coluna 5 em `avatarFoundationDiagnostics()` — `return{`

## `01-build-persistence.js`

- [x] `for` — linha 372, coluna 5 em `stableBuildId()` — `for(let i=0;i<source.length;i++){hash^=source.charCodeAt(i);hash=Math.imul(hash,16777619);}`
- [x] `return` — linha 373, coluna 5 em `stableBuildId()` — `return \`build-${(hash>>>0).toString(36)}\`;`
- [x] `if` — linha 376, coluna 5 em `normalizeBuildRecord()` — `if(!item||typeof item!=='object')return null;`
- [x] `return` — linha 376, coluna 38 em `normalizeBuildRecord()` — `if(!item||typeof item!=='object')return null;`
- [x] `if` — linha 378, coluna 5 em `normalizeBuildRecord()` — `if(!type||!Number.isFinite(x)||!Number.isFinite(z))return null;`
- [x] `return` — linha 378, coluna 56 em `normalizeBuildRecord()` — `if(!type||!Number.isFinite(x)||!Number.isFinite(z))return null;`
- [x] `return` — linha 380, coluna 5 em `normalizeBuildRecord()` — `return {...item,id:String(item.id||stableBuildId(item,index)),type,x,z,rotation:Number.isFinite(rotation)?rotation:0,ownerId:String(item.ownerId||'local'),createdAt,updatedAt};`
- [x] `if` — linha 385, coluna 53 em `normalizeBuildList()` — `const record=normalizeBuildRecord(item,index);if(!record)return;`
- [x] `return` — linha 385, coluna 64 em `normalizeBuildList()` — `const record=normalizeBuildRecord(item,index);if(!record)return;`
- [x] `if` — linha 387, coluna 7 em `normalizeBuildList()` — `if(!previous||Number(record.updatedAt||record.createdAt||0)>=Number(previous.updatedAt||previous.createdAt||0))byId.set(record.id,record);`
- [x] `return` — linha 389, coluna 5 em `normalizeBuildList()` — `return [...byId.values()].sort((a,b)=>Number(a.createdAt||0)-Number(b.createdAt||0));`
- [x] `if` — linha 394, coluna 7 em `normalizeBuildTombstones()` — `if(!item)return;const id=String(typeof item==='string'?item:item.id||'').trim();if(!id)return;`
- [x] `return` — linha 394, coluna 16 em `normalizeBuildTombstones()` — `if(!item)return;const id=String(typeof item==='string'?item:item.id||'').trim();if(!id)return;`
- [x] `if` — linha 394, coluna 87 em `normalizeBuildTombstones()` — `if(!item)return;const id=String(typeof item==='string'?item:item.id||'').trim();if(!id)return;`
- [x] `return` — linha 394, coluna 94 em `normalizeBuildTombstones()` — `if(!item)return;const id=String(typeof item==='string'?item:item.id||'').trim();if(!id)return;`
- [x] `if` — linha 396, coluna 35 em `normalizeBuildTombstones()` — `const previous=byId.get(id);if(!previous||removedAt>=previous.removedAt)byId.set(id,{id,removedAt});`
- [x] `return` — linha 398, coluna 5 em `normalizeBuildTombstones()` — `return [...byId.values()].sort((a,b)=>a.removedAt-b.removedAt).slice(-300);`
- [x] `return` — linha 400, coluna 53 em `mergeBuildTombstones()` — `function mergeBuildTombstones(local=[],remote=[]){return normalizeBuildTombstones([...(local||[]),...(remote||[])]);}`
- [x] `return` — linha 403, coluna 5 em `applyBuildTombstones()` — `return normalizeBuildList(builds).filter(item=>!removed.has(item.id)||Number(item.updatedAt||item.createdAt||0)>Number(removed.get(item.id)||0));`
- [x] `return` — linha 406, coluna 5 em `mergeBuildCollections()` — `return applyBuildTombstones(normalizeBuildList([...(local||[]),...(remote||[])]),tombstones);`
- [x] `if` — linha 411, coluna 7 em `mergeEntityCollections()` — `if(!item||typeof item!=='object')return;const id=String(item.id||\`entity-${index}-${Number(item.createdAt||0)}\`),stamp=Number(item.updatedAt||item.createdAt||0),previous=byId.get(id);`
- [x] `return` — linha 411, coluna 40 em `mergeEntityCollections()` — `if(!item||typeof item!=='object')return;const id=String(item.id||\`entity-${index}-${Number(item.createdAt||0)}\`),stamp=Number(item.updatedAt||item.createdAt||0),previous=byId.get(id);`
- [x] `if` — linha 412, coluna 7 em `mergeEntityCollections()` — `if(!previous||stamp>=Number(previous.updatedAt||previous.createdAt||0))byId.set(id,{...item,id});`
- [x] `return` — linha 414, coluna 5 em `mergeEntityCollections()` — `return [...byId.values()];`

## `02-state-save-cloud-account.js`

- [x] `if` — linha 494, coluna 5 em `normalizeState()` — `if (oldFriendship.otto !== undefined && oldFriendship.nino === undefined) friendship.nino = oldFriendship.otto;`
- [x] `if` — linha 497, coluna 5 em `normalizeState()` — `if (flags.talkedOtto && !flags.talkedNeighbor) flags.talkedNeighbor = true;`
- [x] `return` — linha 500, coluna 5 em `normalizeState()` — `return {`
- [x] `if` — linha 552, coluna 7 em `loadState()` — `if (!raw) {`
- [x] `for` — linha 553, coluna 9 em `loadState()` — `for (const key of LEGACY_STORAGE_KEYS) {`
- [x] `if` — linha 555, coluna 11 em `loadState()` — `if (raw) break;`
- [x] `if` — linha 558, coluna 7 em `loadState()` — `if (!raw) return fresh;`
- [x] `return` — linha 558, coluna 17 em `loadState()` — `if (!raw) return fresh;`
- [x] `return` — linha 560, coluna 7 em `loadState()` — `return normalizeState(saved);`
- [x] `catch` — linha 561, coluna 7 em `loadState()` — `} catch (error) {`
- [x] `return` — linha 563, coluna 7 em `loadState()` — `return fresh;`
- [x] `if` — linha 568, coluna 3 em `loadState()` — `if(state.career?.activeJob){const legacyJob=state.career.activeJob;legacyJob.instanceId=legacyJob.instanceId||\`legacy-${legacyJob.id||'job'}-${Number(legacyJob.startedAt||Date.now())}\`;legacyJob.missionState=legacyJob.missionState||'TRAVELLING';legacyJob.rewardClaimed=!!legacyJob.rewardClaimed;legacyJob.previousUniform=legacyJob.previousUniform||'none';legacyJob.previousPackage=Number(legacyJob.previousPackage||0);}`
- [x] `if` — linha 570, coluna 3 em `loadState()` — `if(accountSession){`
- [x] `if` — linha 573, coluna 3 em `loadState()` — `if(!state.profile.nameConfirmed){`
- [x] `if` — linha 575, coluna 5 em `loadState()` — `if(legacyName&&legacyName.toLowerCase()!=='otthos'){state.profile.name=legacyName;state.profile.nameConfirmed=true;}`
- [x] `else` — linha 576, coluna 5 em `loadState()` — `else{state.profile.name='';state.profile.nameConfirmed=false;}`
- [x] `if` — linha 580, coluna 3 em `loadState()` — `if (window.OTTHOS_DB) {`
- [x] `if` — linha 582, coluna 7 em `loadState()` — `if (saved && saved.profile) {`
- [x] `if` — linha 584, coluna 9 em `loadState()` — `if(accountSession)state.account={...(state.account||{}),linked:true,accountId:accountSession.accountId,username:accountSession.username||state.account?.username||''};`
- [x] `else` — linha 586, coluna 9 em `loadState()` — `} else {`
- [x] `catch` — linha 587, coluna 38 em `loadState()` — `window.OTTHOS_DB.save(state).catch(console.warn);`
- [x] `return` — linha 591, coluna 7 em `loadState()` — `return state;`
- [x] `catch` — linha 592, coluna 8 em `loadState()` — `}).catch(error => { console.warn('IndexedDB indisponível; usando armazenamento local.', error); return state; });`
- [x] `return` — linha 592, coluna 101 em `loadState()` — `}).catch(error => { console.warn('IndexedDB indisponível; usando armazenamento local.', error); return state; });`
- [x] `catch` — linha 593, coluna 49 em `loadState()` — `window.OTTHOS_DB.requestPersistentStorage().catch(() => false);`
- [x] `catch` — linha 605, coluna 25 em `commitState()` — `? lastSavePromise.catch(()=>true).then(()=>window.OTTHOS_DB.save(snapshot)).catch(error => {`
- [x] `catch` — linha 605, coluna 83 em `commitState()` — `? lastSavePromise.catch(()=>true).then(()=>window.OTTHOS_DB.save(snapshot)).catch(error => {`
- [x] `return` — linha 607, coluna 11 em `commitState()` — `return false;`
- [x] `return` — linha 612, coluna 5 em `commitState()` — `return lastSavePromise;`
- [x] `if` — linha 617, coluna 5 em `saveState()` — `if (immediate) return commitState();`
- [x] `return` — linha 617, coluna 20 em `saveState()` — `if (immediate) return commitState();`
- [x] `return` — linha 619, coluna 5 em `saveState()` — `return lastSavePromise;`
- [x] `return` — linha 623, coluna 5 em `cloudProgressPayload()` — `return {`
- [x] `if` — linha 632, coluna 5 em `syncCloudProgress()` — `if(!hasValidPlayerName())return false;`
- [x] `return` — linha 632, coluna 30 em `syncCloudProgress()` — `if(!hasValidPlayerName())return false;`
- [x] `return` — linha 633, coluna 5 em `syncCloudProgress()` — `return window.OTTHOS_RTDB?.syncProgress?.(cloudProgressPayload(),force)||false;`
- [x] `if` — linha 636, coluna 5 em `mergeCloudProgress()` — `if(!remote||typeof remote!=='object')return false;`
- [x] `return` — linha 636, coluna 42 em `mergeCloudProgress()` — `if(!remote||typeof remote!=='object')return false;`
- [x] `if` — linha 638, coluna 5 em `mergeCloudProgress()` — `if(remoteSaved<=localSaved+2500){syncCloudProgress(true);return false;}`
- [x] `return` — linha 638, coluna 62 em `mergeCloudProgress()` — `if(remoteSaved<=localSaved+2500){syncCloudProgress(true);return false;}`
- [x] `catch` — linha 653, coluna 85 em `mergeCloudProgress()` — `safeLocalSet(STORAGE_KEY,JSON.stringify(state));window.OTTHOS_DB?.save?.(state).catch(()=>{});if(worldGroup&&world?.builds)reconcileWorldBuilds();updatePlayerNameUI();updateHUD();updateLobbyStats();toast('Progresso recuperado do Firebase sem apagar construções locais.','good',2300);return true;`
- [x] `if` — linha 653, coluna 99 em `mergeCloudProgress()` — `safeLocalSet(STORAGE_KEY,JSON.stringify(state));window.OTTHOS_DB?.save?.(state).catch(()=>{});if(worldGroup&&world?.builds)reconcileWorldBuilds();updatePlayerNameUI();updateHUD();updateLobbyStats();toast('Progresso recuperado do Firebase sem apagar construções locais.','good',2300);return true;`
- [x] `return` — linha 653, coluna 288 em `mergeCloudProgress()` — `safeLocalSet(STORAGE_KEY,JSON.stringify(state));window.OTTHOS_DB?.save?.(state).catch(()=>{});if(worldGroup&&world?.builds)reconcileWorldBuilds();updatePlayerNameUI();updateHUD();updateLobbyStats();toast('Progresso recuperado do Firebase sem apagar construções locais.','good',2300);return true;`
- [x] `return` — linha 657, coluna 28 em `accountLinked()` — `function accountLinked(){return!!(accountSession&&state.account?.linked&&state.account.accountId===accountSession.accountId);}`
- [x] `return` — linha 658, coluna 38 em `accountPromptWasHandled()` — `function accountPromptWasHandled(){return Object.entries(state.flags||{}).some(([key,value])=>/^accountPromptedV\d+$/.test(key)&&value===true);}`
- [x] `return` — linha 659, coluna 32 em `accountStatusText()` — `function accountStatusText(){return accountLinked()?\`Conta: ${state.account.username||accountSession.username}\`:state.account?.linked?'Entrar novamente':'Conta local';}`
- [x] `if` — linha 661, coluna 5 em `accountStatusText()` — `if(window.OTTHOS_RTDB)return window.OTTHOS_RTDB;`
- [x] `return` — linha 661, coluna 27 em `accountStatusText()` — `if(window.OTTHOS_RTDB)return window.OTTHOS_RTDB;`
- [x] `return` — linha 662, coluna 5 em `accountStatusText()` — `return new Promise(resolve=>{`
- [x] `if` — linha 664, coluna 28 em `accountStatusText()` — `const finish=value=>{if(done)return;done=true;clearTimeout(timer);window.removeEventListener('otthos:rtdb-ready',ready);resolve(value);};`
- [x] `return` — linha 664, coluna 36 em `accountStatusText()` — `const finish=value=>{if(done)return;done=true;clearTimeout(timer);window.removeEventListener('otthos:rtdb-ready',ready);resolve(value);};`
- [x] `if` — linha 671, coluna 5 em `accountStatusText()` — `if(!backend?.configured)return{ok:false,error:'A nuvem do jogo ainda não está configurada.'};`
- [x] `return` — linha 671, coluna 29 em `accountStatusText()` — `if(!backend?.configured)return{ok:false,error:'A nuvem do jogo ainda não está configurada.'};`
- [x] `if` — linha 672, coluna 5 em `accountStatusText()` — `if(backend.connected?.())return{ok:true,backend};`
- [x] `return` — linha 672, coluna 30 em `accountStatusText()` — `if(backend.connected?.())return{ok:true,backend};`
- [x] `return` — linha 674, coluna 5 em `accountStatusText()` — `return ok?{ok:true,backend}:{ok:false,error:'Sem conexão. O progresso local continua protegido neste aparelho.'};`
- [x] `if` — linha 678, coluna 5 em `syncGameAccount()` — `if(!accountLinked()||accountSyncing)return false;`
- [x] `return` — linha 678, coluna 41 em `syncGameAccount()` — `if(!accountLinked()||accountSyncing)return false;`
- [x] `if` — linha 680, coluna 7 em `syncGameAccount()` — `if(!accountLinked()||accountSyncing)return false;`
- [x] `return` — linha 680, coluna 43 em `syncGameAccount()` — `if(!accountLinked()||accountSyncing)return false;`
- [x] `if` — linha 681, coluna 56 em `syncGameAccount()` — `const connection=await ensureAccountConnection();if(!connection.ok)return false;`
- [x] `return` — linha 681, coluna 74 em `syncGameAccount()` — `const connection=await ensureAccountConnection();if(!connection.ok)return false;`
- [x] `if` — linha 683, coluna 7 em `syncGameAccount()` — `if(!cloudAccount||cloudAccount.anonymous||cloudAccount.username!==accountSession.username)return false;`
- [x] `return` — linha 683, coluna 97 em `syncGameAccount()` — `if(!cloudAccount||cloudAccount.anonymous||cloudAccount.username!==accountSession.username)return false;`
- [x] `if` — linha 690, coluna 9 em `syncGameAccount()` — `if(result?.ok)state.account.lastCloudSync=Date.now();`
- [x] `return` — linha 691, coluna 9 em `syncGameAccount()` — `return!!result?.ok;`
- [x] `catch` — linha 692, coluna 8 em `syncGameAccount()` — `}catch(error){console.warn('Conta do jogo:',error);return false}`
- [x] `return` — linha 692, coluna 58 em `syncGameAccount()` — `}catch(error){console.warn('Conta do jogo:',error);return false}`
- [x] `if` — linha 695, coluna 5 em `syncGameAccount()` — `if(force)return run();`
- [x] `return` — linha 695, coluna 14 em `syncGameAccount()` — `if(force)return run();`
- [x] `return` — linha 696, coluna 43 em `syncGameAccount()` — `accountSaveTimer=setTimeout(run,2400);return true;`
- [x] `if` — linha 699, coluna 5 em `syncGameAccount()` — `if(!window.OTTHOS_ACCOUNT)throw new Error('Proteção de conta indisponível neste navegador.');`
- [x] `throw` — linha 699, coluna 31 em `syncGameAccount()` — `if(!window.OTTHOS_ACCOUNT)throw new Error('Proteção de conta indisponível neste navegador.');`
- [x] `if` — linha 700, coluna 53 em `syncGameAccount()` — `const cleanName=sanitizePlayerName(displayName);if(cleanName.length<3)throw new Error('Escolha um nome de jogador com pelo menos 3 caracteres.');`
- [x] `throw` — linha 700, coluna 75 em `syncGameAccount()` — `const cleanName=sanitizePlayerName(displayName);if(cleanName.length<3)throw new Error('Escolha um nome de jogador com pelo menos 3 caracteres.');`
- [x] `if` — linha 702, coluna 5 em `syncGameAccount()` — `if(!connection.ok)throw new Error(connection.error);`
- [x] `throw` — linha 702, coluna 23 em `syncGameAccount()` — `if(!connection.ok)throw new Error(connection.error);`
- [x] `if` — linha 704, coluna 5 em `syncGameAccount()` — `if(!authResult?.ok)throw new Error(authResult?.error||'Não foi possível criar a conta protegida.');`
- [x] `throw` — linha 704, coluna 24 em `syncGameAccount()` — `if(!authResult?.ok)throw new Error(authResult?.error||'Não foi possível criar a conta protegida.');`
- [x] `if` — linha 707, coluna 65 em `syncGameAccount()` — `await commitState();const saved=await syncGameAccount(true);if(!saved)throw new Error('A conta foi criada, mas a primeira cópia online não terminou. Tente sincronizar novamente.');`
- [x] `throw` — linha 707, coluna 75 em `syncGameAccount()` — `await commitState();const saved=await syncGameAccount(true);if(!saved)throw new Error('A conta foi criada, mas a primeira cópia online não terminou. Tente sincronizar novamente.');`
- [x] `return` — linha 708, coluna 26 em `syncGameAccount()` — `updatePlayerNameUI();return true;`
- [x] `if` — linha 711, coluna 5 em `syncGameAccount()` — `if(!window.OTTHOS_ACCOUNT)throw new Error('Proteção de conta indisponível neste navegador.');`
- [x] `throw` — linha 711, coluna 31 em `syncGameAccount()` — `if(!window.OTTHOS_ACCOUNT)throw new Error('Proteção de conta indisponível neste navegador.');`
- [x] `if` — linha 713, coluna 5 em `syncGameAccount()` — `if(!connection.ok)throw new Error(connection.error);`
- [x] `throw` — linha 713, coluna 23 em `syncGameAccount()` — `if(!connection.ok)throw new Error(connection.error);`
- [x] `if` — linha 715, coluna 5 em `syncGameAccount()` — `if(!authResult?.ok)throw new Error(authResult?.error||'Nome ou senha incorretos.');`
- [x] `throw` — linha 715, coluna 24 em `syncGameAccount()` — `if(!authResult?.ok)throw new Error(authResult?.error||'Nome ou senha incorretos.');`
- [x] `if` — linha 717, coluna 5 em `syncGameAccount()` — `if(!result?.ok)throw new Error(result?.error||'Não foi possível consultar a conta.');`
- [x] `throw` — linha 717, coluna 20 em `syncGameAccount()` — `if(!result?.ok)throw new Error(result?.error||'Não foi possível consultar a conta.');`
- [x] `if` — linha 718, coluna 5 em `syncGameAccount()` — `if(!result.exists)throw new Error('A conta existe, mas ainda não possui uma cópia de progresso. Entre no aparelho onde ela foi criada e toque em “Sincronizar agora”.');`
- [x] `throw` — linha 718, coluna 23 em `syncGameAccount()` — `if(!result.exists)throw new Error('A conta existe, mas ainda não possui uma cópia de progresso. Entre no aparelho onde ela foi criada e toque em “Sincronizar agora”.');`
- [x] `return` — linha 724, coluna 57 em `syncGameAccount()` — `updatePlayerNameUI();updateHUD();updateLobbyStats();return true;`
- [x] `if` — linha 727, coluna 77 em `syncGameAccount()` — `const result=await window.OTTHOS_RTDB?.signOutPlayerAccount?.(password);if(!result?.ok)return result||{ok:false,error:'Não foi possível confirmar a saída.'};`
- [x] `return` — linha 727, coluna 92 em `syncGameAccount()` — `const result=await window.OTTHOS_RTDB?.signOutPlayerAccount?.(password);if(!result?.ok)return result||{ok:false,error:'Não foi possível confirmar a saída.'};`
- [x] `return` — linha 729, coluna 5 em `syncGameAccount()` — `return{ok:true};`
- [x] `if` — linha 736, coluna 76 em `openAccountForm()` — `submit.onclick=async()=>{error.hidden=true;const pass=password.value;if(creating&&pass!==$('[data-account-confirm]',root).value){showError('As duas senhas precisam ser iguais.');return;}submit.disabled=true;submit.textContent=creating?'Criando conta...':'Recuperando...';try{if(creating)await createGameAccount($('[data-account-display]',root).value,user.value,pass);else await loginGameAccount(user.value,pass);closeModal();toast(creating?'Conta criada e progresso protegido.':'Progresso recuperado com sucesso.','good',2600);if(typeof onReady==='function')onReady();else if(!creating)setTimeout(()=>location.reload(),550);}catch(e){showError(e.message||'Não foi possível concluir.');}};`
- [x] `return` — linha 736, coluna 185 em `openAccountForm()` — `submit.onclick=async()=>{error.hidden=true;const pass=password.value;if(creating&&pass!==$('[data-account-confirm]',root).value){showError('As duas senhas precisam ser iguais.');return;}submit.disabled=true;submit.textContent=creating?'Criando conta...':'Recuperando...';try{if(creating)await createGameAccount($('[data-account-display]',root).value,user.value,pass);else await loginGameAccount(user.value,pass);closeModal();toast(creating?'Conta criada e progresso protegido.':'Progresso recuperado com sucesso.','good',2600);if(typeof onReady==='function')onReady();else if(!creating)setTimeout(()=>location.reload(),550);}catch(e){showError(e.message||'Não foi possível concluir.');}};`
- [x] `if` — linha 736, coluna 282 em `openAccountForm()` — `submit.onclick=async()=>{error.hidden=true;const pass=password.value;if(creating&&pass!==$('[data-account-confirm]',root).value){showError('As duas senhas precisam ser iguais.');return;}submit.disabled=true;submit.textContent=creating?'Criando conta...':'Recuperando...';try{if(creating)await createGameAccount($('[data-account-display]',root).value,user.value,pass);else await loginGameAccount(user.value,pass);closeModal();toast(creating?'Conta criada e progresso protegido.':'Progresso recuperado com sucesso.','good',2600);if(typeof onReady==='function')onReady();else if(!creating)setTimeout(()=>location.reload(),550);}catch(e){showError(e.message||'Não foi possível concluir.');}};`
- [x] `else` — linha 736, coluna 374 em `openAccountForm()` — `submit.onclick=async()=>{error.hidden=true;const pass=password.value;if(creating&&pass!==$('[data-account-confirm]',root).value){showError('As duas senhas precisam ser iguais.');return;}submit.disabled=true;submit.textContent=creating?'Criando conta...':'Recuperando...';try{if(creating)await createGameAccount($('[data-account-display]',root).value,user.value,pass);else await loginGameAccount(user.value,pass);closeModal();toast(creating?'Conta criada e progresso protegido.':'Progresso recuperado com sucesso.','good',2600);if(typeof onReady==='function')onReady();else if(!creating)setTimeout(()=>location.reload(),550);}catch(e){showError(e.message||'Não foi possível concluir.');}};`
- [x] `if` — linha 736, coluna 534 em `openAccountForm()` — `submit.onclick=async()=>{error.hidden=true;const pass=password.value;if(creating&&pass!==$('[data-account-confirm]',root).value){showError('As duas senhas precisam ser iguais.');return;}submit.disabled=true;submit.textContent=creating?'Criando conta...':'Recuperando...';try{if(creating)await createGameAccount($('[data-account-display]',root).value,user.value,pass);else await loginGameAccount(user.value,pass);closeModal();toast(creating?'Conta criada e progresso protegido.':'Progresso recuperado com sucesso.','good',2600);if(typeof onReady==='function')onReady();else if(!creating)setTimeout(()=>location.reload(),550);}catch(e){showError(e.message||'Não foi possível concluir.');}};`
- [x] `else` — linha 736, coluna 575 em `openAccountForm()` — `submit.onclick=async()=>{error.hidden=true;const pass=password.value;if(creating&&pass!==$('[data-account-confirm]',root).value){showError('As duas senhas precisam ser iguais.');return;}submit.disabled=true;submit.textContent=creating?'Criando conta...':'Recuperando...';try{if(creating)await createGameAccount($('[data-account-display]',root).value,user.value,pass);else await loginGameAccount(user.value,pass);closeModal();toast(creating?'Conta criada e progresso protegido.':'Progresso recuperado com sucesso.','good',2600);if(typeof onReady==='function')onReady();else if(!creating)setTimeout(()=>location.reload(),550);}catch(e){showError(e.message||'Não foi possível concluir.');}};`
- [x] `if` — linha 736, coluna 580 em `openAccountForm()` — `submit.onclick=async()=>{error.hidden=true;const pass=password.value;if(creating&&pass!==$('[data-account-confirm]',root).value){showError('As duas senhas precisam ser iguais.');return;}submit.disabled=true;submit.textContent=creating?'Criando conta...':'Recuperando...';try{if(creating)await createGameAccount($('[data-account-display]',root).value,user.value,pass);else await loginGameAccount(user.value,pass);closeModal();toast(creating?'Conta criada e progresso protegido.':'Progresso recuperado com sucesso.','good',2600);if(typeof onReady==='function')onReady();else if(!creating)setTimeout(()=>location.reload(),550);}catch(e){showError(e.message||'Não foi possível concluir.');}};`
- [x] `catch` — linha 736, coluna 632 em `openAccountForm()` — `submit.onclick=async()=>{error.hidden=true;const pass=password.value;if(creating&&pass!==$('[data-account-confirm]',root).value){showError('As duas senhas precisam ser iguais.');return;}submit.disabled=true;submit.textContent=creating?'Criando conta...':'Recuperando...';try{if(creating)await createGameAccount($('[data-account-display]',root).value,user.value,pass);else await loginGameAccount(user.value,pass);closeModal();toast(creating?'Conta criada e progresso protegido.':'Progresso recuperado com sucesso.','good',2600);if(typeof onReady==='function')onReady();else if(!creating)setTimeout(()=>location.reload(),550);}catch(e){showError(e.message||'Não foi possível concluir.');}};`
- [x] `if` — linha 746, coluna 153 em `openAccountCenter()` — `$('[data-account-offline]',root)?.addEventListener('click',()=>openPlayerNameModal(true,()=>{state.flags.accountPromptedV635=true;saveState(true);if(typeof onReady==='function')onReady();}));`
- [x] `if` — linha 752, coluna 155 em `openAccountLogoutGate()` — `const submit=async()=>{error.hidden=true;button.disabled=true;button.textContent='Confirmando...';const result=await unlinkGameAccount(input.value);if(!result?.ok){error.textContent=result?.error||'Senha incorreta.';error.hidden=false;button.disabled=false;button.textContent='Sair desta conta';input.select();return;}closeModal();toast('Conta desconectada. As restrições de segurança foram preservadas.','good',2800);};`
- [x] `return` — linha 752, coluna 317 em `openAccountLogoutGate()` — `const submit=async()=>{error.hidden=true;button.disabled=true;button.textContent='Confirmando...';const result=await unlinkGameAccount(input.value);if(!result?.ok){error.textContent=result?.error||'Senha incorreta.';error.hidden=false;button.disabled=false;button.textContent='Sair desta conta';input.select();return;}closeModal();toast('Conta desconectada. As restrições de segurança foram preservadas.','good',2800);};`
- [x] `if` — linha 753, coluna 134 em `openAccountLogoutGate()` — `button.onclick=submit;$('[data-account-logout-back]',root).onclick=()=>openAccountCenter(required,onReady);input.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();submit();}};setTimeout(()=>input.focus(),80);`

## `03-ui-modal-install-pwa.js`

- [x] `if` — linha 761, coluna 5 em `addXP()` — `if (nextLevel > state.profile.level) {`
- [x] `if` — linha 778, coluna 5 em `awardMedal()` — `if (state.medals.includes(name)) return;`
- [x] `return` — linha 778, coluna 38 em `awardMedal()` — `if (state.medals.includes(name)) return;`
- [x] `if` — linha 784, coluna 5 em `setFlag()` — `if (state.flags[flag] === value) return;`
- [x] `return` — linha 784, coluna 38 em `setFlag()` — `if (state.flags[flag] === value) return;`
- [x] `if` — linha 802, coluna 5 em `vibrate()` — `if (state.settings.vibration && navigator.vibrate) navigator.vibrate(pattern);`
- [x] `if` — linha 805, coluna 5 em `beep()` — `if (!state.settings.sound) return;`
- [x] `return` — linha 805, coluna 32 em `beep()` — `if (!state.settings.sound) return;`
- [x] `if` — linha 808, coluna 7 em `beep()` — `if (!Ctx) return;`
- [x] `return` — linha 808, coluna 17 em `beep()` — `if (!Ctx) return;`
- [x] `catch` — linha 816, coluna 7 em `beep()` — `} catch {}`
- [x] `if` — linha 821, coluna 5 em `openModal()` — `if (els.quickBar) els.quickBar.hidden = true;`
- [x] `if` — linha 825, coluna 5 em `openModal()` — `if (typeof player !== 'undefined' && player.vehicle) player.car.speed *= .7;`
- [x] `if` — linha 831, coluna 5 em `openModal()` — `if (onReady) onReady(els.modalBody);`
- [x] `if` — linha 836, coluna 5 em `closeModal()` — `if (typeof fishingSession !== 'undefined' && fishingSession) cancelFishingSession();`
- [x] `else` — linha 837, coluna 5 em `closeModal()` — `else if (typeof fishingVisual !== 'undefined' && fishingVisual?.active) stopFishingVisual();`
- [x] `if` — linha 837, coluna 10 em `closeModal()` — `else if (typeof fishingVisual !== 'undefined' && fishingVisual?.active) stopFishingVisual();`
- [x] `if` — linha 846, coluna 5 em `closeModal()` — `if (resumePausedGame) {`
- [x] `if` — linha 849, coluna 7 em `closeModal()` — `if (running && player.vehicle) startEngineSound();`
- [x] `return` — linha 853, coluna 5 em `confirmModal()` — `return new Promise(resolve => {`
- [x] `if` — linha 861, coluna 52 em `confirmModal()` — `els.modal.addEventListener('pointerdown', e => { if (e.target === els.modal) closeModal(); });`
- [x] `if` — linha 871, coluna 5 em `updateInstallUI()` — `if (els.installBtn) {`
- [x] `if` — linha 876, coluna 5 em `updateInstallUI()` — `if (els.installHint) {`
- [x] `if` — linha 895, coluna 5 em `updateInstallUI()` — `if (isStandalone()) { updateInstallUI(); return; }`
- [x] `return` — linha 895, coluna 46 em `updateInstallUI()` — `if (isStandalone()) { updateInstallUI(); return; }`
- [x] `if` — linha 896, coluna 5 em `updateInstallUI()` — `if (deferredInstallPrompt) {`
- [x] `return` — linha 901, coluna 7 em `updateInstallUI()` — `return;`
- [x] `if` — linha 908, coluna 3 em `updateInstallUI()` — `if (els.installBtn) els.installBtn.onclick = installApp;`
- [x] `return` — linha 918, coluna 7 em `isOfficialOtthiGameAddress()` — `return current.origin === live.origin`
- [x] `catch` — linha 920, coluna 7 em `isOfficialOtthiGameAddress()` — `} catch {`
- [x] `return` — linha 921, coluna 7 em `isOfficialOtthiGameAddress()` — `return false;`
- [x] `catch` — linha 926, coluna 36 em `isOfficialOtthiGameAddress()` — `try { await saveState(true); } catch {}`
- [x] `catch` — linha 927, coluna 36 em `isOfficialOtthiGameAddress()` — `try { await lastSavePromise; } catch {}`
- [x] `if` — linha 931, coluna 5 em `isOfficialOtthiGameAddress()` — `if (otthiUpdateReloading) return false;`
- [x] `return` — linha 931, coluna 31 em `isOfficialOtthiGameAddress()` — `if (otthiUpdateReloading) return false;`
- [x] `return` — linha 937, coluna 5 em `isOfficialOtthiGameAddress()` — `return true;`
- [x] `if` — linha 941, coluna 5 em `isOfficialOtthiGameAddress()` — `if (!isOfficialOtthiGameAddress() || !navigator.onLine || otthiUpdateReloading) return false;`
- [x] `return` — linha 941, coluna 85 em `isOfficialOtthiGameAddress()` — `if (!isOfficialOtthiGameAddress() || !navigator.onLine || otthiUpdateReloading) return false;`
- [x] `if` — linha 943, coluna 5 em `isOfficialOtthiGameAddress()` — `if (!force && now - otthiLastUpdateCheck < OTTHI_UPDATE_INTERVAL_MS) return false;`
- [x] `return` — linha 943, coluna 74 em `isOfficialOtthiGameAddress()` — `if (!force && now - otthiLastUpdateCheck < OTTHI_UPDATE_INTERVAL_MS) return false;`
- [x] `if` — linha 949, coluna 7 em `isOfficialOtthiGameAddress()` — `if (!response.ok) throw new Error(\`HTTP ${response.status}\`);`
- [x] `throw` — linha 949, coluna 25 em `isOfficialOtthiGameAddress()` — `if (!response.ok) throw new Error(\`HTTP ${response.status}\`);`
- [x] `if` — linha 953, coluna 7 em `isOfficialOtthiGameAddress()` — `if ((remoteBuild && remoteBuild !== OTTHI_GAME_WEB_BUILD)`
- [x] `if` — linha 955, coluna 9 em `isOfficialOtthiGameAddress()` — `if (!otthiServiceWorkerRegistration) {`
- [x] `return` — linha 956, coluna 11 em `isOfficialOtthiGameAddress()` — `return reloadForOtthiUpdate(remoteRevision || remoteBuild);`
- [x] `catch` — linha 958, coluna 58 em `isOfficialOtthiGameAddress()` — `await otthiServiceWorkerRegistration?.update?.().catch(() => {});`
- [x] `if` — linha 959, coluna 9 em `isOfficialOtthiGameAddress()` — `if (otthiServiceWorkerRegistration?.waiting) {`
- [x] `return` — linha 963, coluna 9 em `isOfficialOtthiGameAddress()` — `return true;`
- [x] `catch` — linha 965, coluna 7 em `isOfficialOtthiGameAddress()` — `} catch (error) {`
- [x] `return` — linha 968, coluna 5 em `isOfficialOtthiGameAddress()` — `return false;`
- [x] `if` — linha 972, coluna 5 em `isOfficialOtthiGameAddress()` — `if (!('serviceWorker' in navigator) || !isOfficialOtthiGameAddress()) return;`
- [x] `return` — linha 972, coluna 75 em `isOfficialOtthiGameAddress()` — `if (!('serviceWorker' in navigator) || !isOfficialOtthiGameAddress()) return;`
- [x] `if` — linha 976, coluna 7 em `isOfficialOtthiGameAddress()` — `if (controllerHandled) return;`
- [x] `return` — linha 976, coluna 30 em `isOfficialOtthiGameAddress()` — `if (controllerHandled) return;`
- [x] `if` — linha 978, coluna 7 em `isOfficialOtthiGameAddress()` — `if (hadController) reloadForOtthiUpdate(\`sw-${OTTHI_GAME_WEB_BUILD}\`);`
- [x] `if` — linha 986, coluna 7 em `isOfficialOtthiGameAddress()` — `if (registration.waiting) registration.waiting.postMessage({ type:'SKIP_WAITING' });`
- [x] `if` — linha 989, coluna 9 em `isOfficialOtthiGameAddress()` — `if (!worker) return;`
- [x] `return` — linha 989, coluna 22 em `isOfficialOtthiGameAddress()` — `if (!worker) return;`
- [x] `if` — linha 991, coluna 11 em `isOfficialOtthiGameAddress()` — `if (worker.state === 'installed' && navigator.serviceWorker.controller) {`
- [x] `catch` — linha 996, coluna 35 em `isOfficialOtthiGameAddress()` — `await registration.update().catch(() => {});`
- [x] `catch` — linha 999, coluna 31 em `isOfficialOtthiGameAddress()` — `registration.update().catch(() => {});`
- [x] `catch` — linha 1002, coluna 7 em `isOfficialOtthiGameAddress()` — `} catch (error) {`
- [x] `if` — linha 1007, coluna 3 em `isOfficialOtthiGameAddress()` — `if ('serviceWorker' in navigator) {`
- [x] `if` — linha 1017, coluna 5 em `isOfficialOtthiGameAddress()` — `if (!document.hidden) probeOtthiGameUpdate(false);`
- [x] `if` — linha 1020, coluna 3 em `isOfficialOtthiGameAddress()` — `if (!window.OTTHI_RELEASE_COHERENT) {`
- [x] `else` — linha 1024, coluna 5 em `isOfficialOtthiGameAddress()` — `} else if (!navigator.onLine) {`
- [x] `if` — linha 1024, coluna 10 em `isOfficialOtthiGameAddress()` — `} else if (!navigator.onLine) {`

## `04-education-daily-quiz.js`

- [x] `return` — linha 1043, coluna 46 em `localDateKey()` — `function localDateKey(){const d=new Date();return\`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}\`}`
- [x] `if` — linha 1044, coluna 29 em `daysBetween()` — `function daysBetween(a,b){if(!a||!b)return 99;return Math.round((new Date(\`${b}T12:00:00\`)-new Date(\`${a}T12:00:00\`))/86400000)}`
- [x] `return` — linha 1044, coluna 39 em `daysBetween()` — `function daysBetween(a,b){if(!a||!b)return 99;return Math.round((new Date(\`${b}T12:00:00\`)-new Date(\`${a}T12:00:00\`))/86400000)}`
- [x] `return` — linha 1044, coluna 49 em `daysBetween()` — `function daysBetween(a,b){if(!a||!b)return 99;return Math.round((new Date(\`${b}T12:00:00\`)-new Date(\`${a}T12:00:00\`))/86400000)}`
- [x] `if` — linha 1045, coluna 61 em `ensureDailyChallenges()` — `function ensureDailyChallenges(){const key=localDateKey();if(state.daily.date===key&&state.daily.quests?.length)return;const gap=daysBetween(state.daily.lastDate,key);state.daily.streak=gap===1?(state.daily.streak||0)+1:gap===0?(state.daily.streak||1):1;let seed=Number(key.replaceAll('-',''));const pool=[...DAILY_QUEST_POOL],picked=[];while(picked.length<3&&pool.length){seed=(seed*9301+49297)%233280;const q=pool.splice(Math.floor(seed/233280*pool.length),1)[0];picked.push({id:q.id,progress:0,target:q.target,reward:q.reward,xp:q.xp,claimed:false});}state.daily={date:key,lastDate:key,streak:state.daily.streak||1,quests:picked};saveState();}`
- [x] `return` — linha 1045, coluna 115 em `ensureDailyChallenges()` — `function ensureDailyChallenges(){const key=localDateKey();if(state.daily.date===key&&state.daily.quests?.length)return;const gap=daysBetween(state.daily.lastDate,key);state.daily.streak=gap===1?(state.daily.streak||0)+1:gap===0?(state.daily.streak||1):1;let seed=Number(key.replaceAll('-',''));const pool=[...DAILY_QUEST_POOL],picked=[];while(picked.length<3&&pool.length){seed=(seed*9301+49297)%233280;const q=pool.splice(Math.floor(seed/233280*pool.length),1)[0];picked.push({id:q.id,progress:0,target:q.target,reward:q.reward,xp:q.xp,claimed:false});}state.daily={date:key,lastDate:key,streak:state.daily.streak||1,quests:picked};saveState();}`
- [x] `while` — linha 1045, coluna 340 em `ensureDailyChallenges()` — `function ensureDailyChallenges(){const key=localDateKey();if(state.daily.date===key&&state.daily.quests?.length)return;const gap=daysBetween(state.daily.lastDate,key);state.daily.streak=gap===1?(state.daily.streak||0)+1:gap===0?(state.daily.streak||1):1;let seed=Number(key.replaceAll('-',''));const pool=[...DAILY_QUEST_POOL],picked=[];while(picked.length<3&&pool.length){seed=(seed*9301+49297)%233280;const q=pool.splice(Math.floor(seed/233280*pool.length),1)[0];picked.push({id:q.id,progress:0,target:q.target,reward:q.reward,xp:q.xp,claimed:false});}state.daily={date:key,lastDate:key,streak:state.daily.streak||1,quests:picked};saveState();}`
- [x] `return` — linha 1046, coluna 32 em `dailyDefinition()` — `function dailyDefinition(id){return DAILY_QUEST_POOL.find(q=>q.id===id)}`
- [x] `for` — linha 1049, coluna 5 em `trackDaily()` — `for(const q of state.daily.quests){if(q.id!==type||q.claimed)continue;const before=q.progress;q.progress=clamp((q.progress||0)+amount,0,q.target);if(q.progress!==before){changed=true;if(before<q.target&&q.progress>=q.target)completedNow=true;}}`
- [x] `if` — linha 1049, coluna 40 em `trackDaily()` — `for(const q of state.daily.quests){if(q.id!==type||q.claimed)continue;const before=q.progress;q.progress=clamp((q.progress||0)+amount,0,q.target);if(q.progress!==before){changed=true;if(before<q.target&&q.progress>=q.target)completedNow=true;}}`
- [x] `if` — linha 1049, coluna 151 em `trackDaily()` — `for(const q of state.daily.quests){if(q.id!==type||q.claimed)continue;const before=q.progress;q.progress=clamp((q.progress||0)+amount,0,q.target);if(q.progress!==before){changed=true;if(before<q.target&&q.progress>=q.target)completedNow=true;}}`
- [x] `if` — linha 1049, coluna 188 em `trackDaily()` — `for(const q of state.daily.quests){if(q.id!==type||q.claimed)continue;const before=q.progress;q.progress=clamp((q.progress||0)+amount,0,q.target);if(q.progress!==before){changed=true;if(before<q.target&&q.progress>=q.target)completedNow=true;}}`
- [x] `if` — linha 1050, coluna 5 em `trackDaily()` — `if(!changed)return;updateDailyBadge();const now=performance.now();`
- [x] `return` — linha 1050, coluna 17 em `trackDaily()` — `if(!changed)return;updateDailyBadge();const now=performance.now();`
- [x] `if` — linha 1051, coluna 5 em `trackDaily()` — `if(completedNow||!trackDaily.lastSave||now-trackDaily.lastSave>6000){trackDaily.lastSave=now;saveState();}`
- [x] `if` — linha 1053, coluna 31 em `updateDailyBadge()` — `function updateDailyBadge(){if(!els.dailyBtn)return;ensureDailyChallenges();const ready=state.daily.quests.filter(q=>!q.claimed&&q.progress>=q.target).length;els.dailyBtn.classList.toggle('reward-ready',ready>0);const span=$('span',els.dailyBtn);if(span)span.textContent=ready?\`Prêmio ${ready}\`:'Desafios';}`
- [x] `return` — linha 1053, coluna 48 em `updateDailyBadge()` — `function updateDailyBadge(){if(!els.dailyBtn)return;ensureDailyChallenges();const ready=state.daily.quests.filter(q=>!q.claimed&&q.progress>=q.target).length;els.dailyBtn.classList.toggle('reward-ready',ready>0);const span=$('span',els.dailyBtn);if(span)span.textContent=ready?\`Prêmio ${ready}\`:'Desafios';}`
- [x] `if` — linha 1053, coluna 249 em `updateDailyBadge()` — `function updateDailyBadge(){if(!els.dailyBtn)return;ensureDailyChallenges();const ready=state.daily.quests.filter(q=>!q.claimed&&q.progress>=q.target).length;els.dailyBtn.classList.toggle('reward-ready',ready>0);const span=$('span',els.dailyBtn);if(span)span.textContent=ready?\`Prêmio ${ready}\`:'Desafios';}`
- [x] `if` — linha 1054, coluna 96 em `claimDailyQuest()` — `function claimDailyQuest(index){const q=state.daily.quests[index],def=dailyDefinition(q?.id);if(!q||!def||q.claimed||q.progress<q.target)return;q.claimed=true;addCoins(q.reward);addXP(q.xp);addReputation(3);beep(980,90);vibrate(30);toast(\`Desafio concluído! +${q.reward} moedas\`,'good',2200);saveState(true);openDailyChallenges();}`
- [x] `return` — linha 1054, coluna 140 em `claimDailyQuest()` — `function claimDailyQuest(index){const q=state.daily.quests[index],def=dailyDefinition(q?.id);if(!q||!def||q.claimed||q.progress<q.target)return;q.claimed=true;addCoins(q.reward);addXP(q.xp);addReputation(3);beep(980,90);vibrate(30);toast(\`Desafio concluído! +${q.reward} moedas\`,'good',2200);saveState(true);openDailyChallenges();}`
- [x] `return` — linha 1068, coluna 53 em `seeded()` — `function seeded(seed){let s=(Number(seed)||1)>>>0;return()=>((s=(s*1664525+1013904223)>>>0)/4294967296);}`
- [x] `return` — linha 1069, coluna 46 em `shuffled()` — `function shuffled(values,rand=Math.random){return [...values].sort(()=>rand()-.5);}`
- [x] `return` — linha 1070, coluna 146 em `choiceSet()` — `function choiceSet(answer,candidates,rand){const pool=shuffled([...new Set(candidates.filter(x=>String(x)!==String(answer)))],rand).slice(0,3);return shuffled([answer,...pool],rand);}`
- [x] `if` — linha 1073, coluna 5 em `mathRound()` — `if(level===1){const n=1+Math.floor(rand()*9);return{kind:'choice',visual:emoji.repeat(n),prompt:'Quantos objetos aparecem?',answer:String(n),options:choiceSet(String(n),[n-2,n-1,n+1,n+2].filter(x=>x>0).map(String),rand)};}`
- [x] `return` — linha 1073, coluna 50 em `mathRound()` — `if(level===1){const n=1+Math.floor(rand()*9);return{kind:'choice',visual:emoji.repeat(n),prompt:'Quantos objetos aparecem?',answer:String(n),options:choiceSet(String(n),[n-2,n-1,n+1,n+2].filter(x=>x>0).map(String),rand)};}`
- [x] `if` — linha 1074, coluna 5 em `mathRound()` — `if(level===2){const a=1+Math.floor(rand()*8),b=1+Math.floor(rand()*7),ans=a+b;return{kind:'choice',visual:\`${emoji.repeat(a)} + ${emoji.repeat(b)}\`,prompt:\`Quanto é ${a} + ${b}?\`,answer:String(ans),options:choiceSet(String(ans),[ans-2,ans-1,ans+1,ans+2].map(String),rand)};}`
- [x] `return` — linha 1074, coluna 83 em `mathRound()` — `if(level===2){const a=1+Math.floor(rand()*8),b=1+Math.floor(rand()*7),ans=a+b;return{kind:'choice',visual:\`${emoji.repeat(a)} + ${emoji.repeat(b)}\`,prompt:\`Quanto é ${a} + ${b}?\`,answer:String(ans),options:choiceSet(String(ans),[ans-2,ans-1,ans+1,ans+2].map(String),rand)};}`
- [x] `if` — linha 1075, coluna 5 em `mathRound()` — `if(level===3){const a=5+Math.floor(rand()*10),b=1+Math.floor(rand()*Math.min(8,a)),ans=a-b;return{kind:'choice',visual:\`${a} − ${b}\`,prompt:'Quanto sobrou?',answer:String(ans),options:choiceSet(String(ans),[ans-2,ans-1,ans+1,ans+2].filter(x=>x>=0).map(String),rand)};}`
- [x] `return` — linha 1075, coluna 96 em `mathRound()` — `if(level===3){const a=5+Math.floor(rand()*10),b=1+Math.floor(rand()*Math.min(8,a)),ans=a-b;return{kind:'choice',visual:\`${a} − ${b}\`,prompt:'Quanto sobrou?',answer:String(ans),options:choiceSet(String(ans),[ans-2,ans-1,ans+1,ans+2].filter(x=>x>=0).map(String),rand)};}`
- [x] `if` — linha 1076, coluna 5 em `mathRound()` — `if(level===4){const start=1+Math.floor(rand()*5),step=1+Math.floor(rand()*3),seq=[start,start+step,'?',start+step*3],ans=start+step*2;return{kind:'choice',visual:seq.join('  •  '),prompt:'Qual número completa a sequência?',answer:String(ans),options:choiceSet(String(ans),[ans-step,ans+step,ans+2,ans-1].map(String),rand)};}`
- [x] `return` — linha 1076, coluna 139 em `mathRound()` — `if(level===4){const start=1+Math.floor(rand()*5),step=1+Math.floor(rand()*3),seq=[start,start+step,'?',start+step*3],ans=start+step*2;return{kind:'choice',visual:seq.join('  •  '),prompt:'Qual número completa a sequência?',answer:String(ans),options:choiceSet(String(ans),[ans-step,ans+step,ans+2,ans-1].map(String),rand)};}`
- [x] `if` — linha 1077, coluna 5 em `mathRound()` — `if(level===5){const a=1+Math.floor(rand()*20),b=1+Math.floor(rand()*20),ans=a===b?'=':a>b?'>':'<';return{kind:'choice',visual:\`${a}  ?  ${b}\`,prompt:'Escolha o sinal correto.',answer:ans,options:['>','<','=']};}`
- [x] `return` — linha 1077, coluna 103 em `mathRound()` — `if(level===5){const a=1+Math.floor(rand()*20),b=1+Math.floor(rand()*20),ans=a===b?'=':a>b?'>':'<';return{kind:'choice',visual:\`${a}  ?  ${b}\`,prompt:'Escolha o sinal correto.',answer:ans,options:['>','<','=']};}`
- [x] `return` — linha 1078, coluna 5 em `mathRound()` — `return mathRound(1+Math.floor(rand()*5),rand,index);`
- [x] `if` — linha 1082, coluna 5 em `portugueseRound()` — `if(level===1){const ans=word.pt[0];return{kind:'choice',visual:word.emoji,prompt:\`Com qual letra começa ${word.pt}?\`,answer:ans,options:choiceSet(ans,['A','B','C','G','L','M','P','S','T'],rand)};}`
- [x] `return` — linha 1082, coluna 40 em `portugueseRound()` — `if(level===1){const ans=word.pt[0];return{kind:'choice',visual:word.emoji,prompt:\`Com qual letra começa ${word.pt}?\`,answer:ans,options:choiceSet(ans,['A','B','C','G','L','M','P','S','T'],rand)};}`
- [x] `if` — linha 1083, coluna 5 em `portugueseRound()` — `if(level===2){return{kind:'choice',visual:word.emoji,prompt:'Qual é o nome desta imagem?',answer:word.pt,options:choiceSet(word.pt,WORD_BANK.map(w=>w.pt),rand)};}`
- [x] `return` — linha 1083, coluna 19 em `portugueseRound()` — `if(level===2){return{kind:'choice',visual:word.emoji,prompt:'Qual é o nome desta imagem?',answer:word.pt,options:choiceSet(word.pt,WORD_BANK.map(w=>w.pt),rand)};}`
- [x] `if` — linha 1084, coluna 5 em `portugueseRound()` — `if(level===3){const positions=[...word.pt].map((c,i)=>/[AEIOUÁÉÍÓÚÃÕ]/.test(c)?i:-1).filter(i=>i>=0),pos=positions[Math.floor(rand()*positions.length)]??1,ans=word.pt[pos],masked=[...word.pt];masked[pos]='_';return{kind:'choice',visual:word.emoji,prompt:\`Complete: ${masked.join('')}\`,answer:ans,options:choiceSet(ans,['A','E','I','O','U'],rand)};}`
- [x] `return` — linha 1084, coluna 213 em `portugueseRound()` — `if(level===3){const positions=[...word.pt].map((c,i)=>/[AEIOUÁÉÍÓÚÃÕ]/.test(c)?i:-1).filter(i=>i>=0),pos=positions[Math.floor(rand()*positions.length)]??1,ans=word.pt[pos],masked=[...word.pt];masked[pos]='_';return{kind:'choice',visual:word.emoji,prompt:\`Complete: ${masked.join('')}\`,answer:ans,options:choiceSet(ans,['A','E','I','O','U'],rand)};}`
- [x] `if` — linha 1085, coluna 5 em `portugueseRound()` — `if(level===4){return{kind:'sequence',visual:word.emoji,prompt:'Toque nas sílabas para montar a palavra.',answer:word.syllables.join(''),tokens:shuffled(word.syllables,rand),displayAnswer:word.pt};}`
- [x] `return` — linha 1085, coluna 19 em `portugueseRound()` — `if(level===4){return{kind:'sequence',visual:word.emoji,prompt:'Toque nas sílabas para montar a palavra.',answer:word.syllables.join(''),tokens:shuffled(word.syllables,rand),displayAnswer:word.pt};}`
- [x] `if` — linha 1086, coluna 5 em `portugueseRound()` — `if(level===5){const correct=\`O ${word.pt.toLowerCase()} aparece na imagem.\`;return{kind:'choice',visual:word.emoji,prompt:'Qual frase está escrita corretamente?',answer:correct,options:shuffled([correct,\`A ${word.pt.toLowerCase()} aparecem na imagem.\`,\`Os ${word.pt.toLowerCase()} aparece na imagem.\`],rand)};}`
- [x] `return` — linha 1086, coluna 81 em `portugueseRound()` — `if(level===5){const correct=\`O ${word.pt.toLowerCase()} aparece na imagem.\`;return{kind:'choice',visual:word.emoji,prompt:'Qual frase está escrita corretamente?',answer:correct,options:shuffled([correct,\`A ${word.pt.toLowerCase()} aparecem na imagem.\`,\`Os ${word.pt.toLowerCase()} aparece na imagem.\`],rand)};}`
- [x] `return` — linha 1087, coluna 5 em `portugueseRound()` — `return portugueseRound(1+Math.floor(rand()*5),rand,index);`
- [x] `if` — linha 1091, coluna 5 em `englishRound()` — `if(level===1){return{kind:'choice',visual:word.emoji,prompt:'Choose the English word.',answer:word.en,options:choiceSet(word.en,WORD_BANK.map(w=>w.en),rand),speak:word.en};}`
- [x] `return` — linha 1091, coluna 19 em `englishRound()` — `if(level===1){return{kind:'choice',visual:word.emoji,prompt:'Choose the English word.',answer:word.en,options:choiceSet(word.en,WORD_BANK.map(w=>w.en),rand),speak:word.en};}`
- [x] `if` — linha 1092, coluna 5 em `englishRound()` — `if(level===2){return{kind:'choice',visual:word.pt,prompt:\`Como se diz “${word.pt.toLowerCase()}” em inglês?\`,answer:word.en,options:choiceSet(word.en,WORD_BANK.map(w=>w.en),rand),speak:word.en};}`
- [x] `return` — linha 1092, coluna 19 em `englishRound()` — `if(level===2){return{kind:'choice',visual:word.pt,prompt:\`Como se diz “${word.pt.toLowerCase()}” em inglês?\`,answer:word.en,options:choiceSet(word.en,WORD_BANK.map(w=>w.en),rand),speak:word.en};}`
- [x] `if` — linha 1093, coluna 5 em `englishRound()` — `if(level===3){return{kind:'choice',visual:'🔊',prompt:'Ouça e escolha a palavra.',answer:word.en,options:choiceSet(word.en,WORD_BANK.map(w=>w.en),rand),speak:word.en,autoSpeak:true};}`
- [x] `return` — linha 1093, coluna 19 em `englishRound()` — `if(level===3){return{kind:'choice',visual:'🔊',prompt:'Ouça e escolha a palavra.',answer:word.en,options:choiceSet(word.en,WORD_BANK.map(w=>w.en),rand),speak:word.en,autoSpeak:true};}`
- [x] `if` — linha 1094, coluna 5 em `englishRound()` — `if(level===4){const pos=Math.floor(rand()*word.en.length),ans=word.en[pos],masked=[...word.en];masked[pos]='_';return{kind:'choice',visual:word.emoji,prompt:\`Complete: ${masked.join('')}\`,answer:ans,options:choiceSet(ans,'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split(''),rand),speak:word.en};}`
- [x] `return` — linha 1094, coluna 116 em `englishRound()` — `if(level===4){const pos=Math.floor(rand()*word.en.length),ans=word.en[pos],masked=[...word.en];masked[pos]='_';return{kind:'choice',visual:word.emoji,prompt:\`Complete: ${masked.join('')}\`,answer:ans,options:choiceSet(ans,'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split(''),rand),speak:word.en};}`
- [x] `if` — linha 1095, coluna 5 em `englishRound()` — `if(level===5){const phrases=[['HELLO','Olá'],['THANK YOU','Obrigado'],['GOOD MORNING','Bom dia'],['PLEASE','Por favor'],['GOODBYE','Tchau']],pair=phrases[Math.floor(rand()*phrases.length)];return{kind:'choice',visual:'💬',prompt:\`Qual é o significado de “${pair[0]}”?\`,answer:pair[1],options:choiceSet(pair[1],phrases.map(p=>p[1]),rand),speak:pair[0]};}`
- [x] `return` — linha 1095, coluna 194 em `englishRound()` — `if(level===5){const phrases=[['HELLO','Olá'],['THANK YOU','Obrigado'],['GOOD MORNING','Bom dia'],['PLEASE','Por favor'],['GOODBYE','Tchau']],pair=phrases[Math.floor(rand()*phrases.length)];return{kind:'choice',visual:'💬',prompt:\`Qual é o significado de “${pair[0]}”?\`,answer:pair[1],options:choiceSet(pair[1],phrases.map(p=>p[1]),rand),speak:pair[0]};}`
- [x] `return` — linha 1096, coluna 5 em `englishRound()` — `return englishRound(1+Math.floor(rand()*5),rand,index);`
- [x] `return` — linha 1098, coluna 196 em `generateEducationRounds()` — `function generateEducationRounds(subject,level=1,seed=Date.now(),count=5){const rand=seeded(seed+level*997),maker=subject==='math'?mathRound:subject==='portuguese'?portugueseRound:englishRound;return Array.from({length:count},(_,i)=>maker(level,rand,i));}`
- [x] `return` — linha 1099, coluna 46 em `subjectLevelRecord()` — `function subjectLevelRecord(subject,level){return state.learning.lessons[\`${subject}-${level}\`]||{completed:false,stars:0,best:0,attempts:0};}`
- [x] `return` — linha 1100, coluna 43 em `subjectUnlocked()` — `function subjectUnlocked(subject,level){return level===1||subjectLevelRecord(subject,level-1).completed;}`
- [x] `for` — linha 1101, coluna 50 em `educationSummary()` — `function educationSummary(){let done=0,total=0;for(const id of Object.keys(EDUCATION_SUBJECTS))for(let l=1;l<=6;l++){total++;if(subjectLevelRecord(id,l).completed)done++;}return{done,total,pct:Math.round(done/total*100)};}`
- [x] `for` — linha 1101, coluna 98 em `educationSummary()` — `function educationSummary(){let done=0,total=0;for(const id of Object.keys(EDUCATION_SUBJECTS))for(let l=1;l<=6;l++){total++;if(subjectLevelRecord(id,l).completed)done++;}return{done,total,pct:Math.round(done/total*100)};}`
- [x] `if` — linha 1101, coluna 128 em `educationSummary()` — `function educationSummary(){let done=0,total=0;for(const id of Object.keys(EDUCATION_SUBJECTS))for(let l=1;l<=6;l++){total++;if(subjectLevelRecord(id,l).completed)done++;}return{done,total,pct:Math.round(done/total*100)};}`
- [x] `return` — linha 1101, coluna 174 em `educationSummary()` — `function educationSummary(){let done=0,total=0;for(const id of Object.keys(EDUCATION_SUBJECTS))for(let l=1;l<=6;l++){total++;if(subjectLevelRecord(id,l).completed)done++;}return{done,total,pct:Math.round(done/total*100)};}`
- [x] `catch` — linha 1102, coluna 176 em `speakKidWord()` — `function speakKidWord(text){try{speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(String(text));u.lang='en-US';u.rate=.78;u.pitch=1.08;speechSynthesis.speak(u);}catch{}}`
- [x] `return` — linha 1103, coluna 58 em `dailyChallengesHtml()` — `function dailyChallengesHtml(){ensureDailyChallenges();return state.daily.quests.map((q,i)=>{const d=dailyDefinition(q.id),pct=clamp(q.progress/q.target*100,0,100),ready=q.progress>=q.target&&!q.claimed;return\`<article class="daily-card ${ready?'ready':''} ${q.claimed?'claimed':''}"><div class="daily-icon">${d.icon}</div><div><b>${d.title}</b><span>${d.label(q.progress)}</span><div class="daily-progress"><i style="width:${pct}%"></i></div><small>${q.reward} moedas • ${q.xp} XP</small></div><button data-daily-claim="${i}" ${ready?'':'disabled'}>${q.claimed?'✓':ready?'Receber':'Em andamento'}</button></article>\`}).join('');}`
- [x] `return` — linha 1103, coluna 206 em `dailyChallengesHtml()` — `function dailyChallengesHtml(){ensureDailyChallenges();return state.daily.quests.map((q,i)=>{const d=dailyDefinition(q.id),pct=clamp(q.progress/q.target*100,0,100),ready=q.progress>=q.target&&!q.claimed;return\`<article class="daily-card ${ready?'ready':''} ${q.claimed?'claimed':''}"><div class="daily-icon">${d.icon}</div><div><b>${d.title}</b><span>${d.label(q.progress)}</span><div class="daily-progress"><i style="width:${pct}%"></i></div><small>${q.reward} moedas • ${q.xp} XP</small></div><button data-daily-claim="${i}" ${ready?'':'disabled'}>${q.claimed?'✓':ready?'Receber':'Em andamento'}</button></article>\`}).join('');}`
- [x] `return` — linha 1104, coluna 109 em `educationSubjectHtml()` — `function educationSubjectHtml(subject){const s=EDUCATION_SUBJECTS[subject],levels=SUBJECT_LEVELS[subject];return\`<section class="edu-subject" style="--edu:${s.color}"><header><span>${s.icon}</span><div><b>${s.title}</b><small>${s.description}</small></div></header><div class="edu-path">${levels.map((data,i)=>{const level=i+1,rec=subjectLevelRecord(subject,level),unlocked=subjectUnlocked(subject,level);return\`<button class="edu-node ${rec.completed?'done':''} ${unlocked?'':'locked'}" data-edu-play="${subject}" data-edu-level="${level}" ${unlocked?'':'disabled'}><i>${rec.completed?'✓':unlocked?s.icon:'🔒'}</i><span><b>${level}. ${data[0]}</b><small>${rec.completed?\`${'⭐'.repeat(rec.stars||1)} • recorde ${rec.best}/5\`:data[1]}</small></span></button>\`}).join('')}</div></section>\`;}`
- [x] `return` — linha 1104, coluna 408 em `educationSubjectHtml()` — `function educationSubjectHtml(subject){const s=EDUCATION_SUBJECTS[subject],levels=SUBJECT_LEVELS[subject];return\`<section class="edu-subject" style="--edu:${s.color}"><header><span>${s.icon}</span><div><b>${s.title}</b><small>${s.description}</small></div></header><div class="edu-path">${levels.map((data,i)=>{const level=i+1,rec=subjectLevelRecord(subject,level),unlocked=subjectUnlocked(subject,level);return\`<button class="edu-node ${rec.completed?'done':''} ${unlocked?'':'locked'}" data-edu-play="${subject}" data-edu-level="${level}" ${unlocked?'':'disabled'}><i>${rec.completed?'✓':unlocked?s.icon:'🔒'}</i><span><b>${level}. ${data[0]}</b><small>${rec.completed?\`${'⭐'.repeat(rec.stars||1)} • recorde ${rec.best}/5\`:data[1]}</small></span></button>\`}).join('')}</div></section>\`;}`
- [x] `if` — linha 1112, coluna 263 em `openEducationHub()` — `root.onclick=e=>{const tabBtn=e.target.closest('[data-edu-tab]'),playBtn=e.target.closest('[data-edu-play]'),startBtn=e.target.closest('[data-academy-start]'),claimBtn=e.target.closest('[data-daily-claim]'),onlineBtn=e.target.closest('[data-open-online]');if(tabBtn){openEducationHub(tabBtn.dataset.eduTab);return;}if(startBtn){startSoloEducationGame(startBtn.dataset.academyStart,1);return;}if(playBtn){startSoloEducationGame(playBtn.dataset.eduPlay,Number(playBtn.dataset.eduLevel));return;}if(claimBtn){claimDailyQuest(Number(claimBtn.dataset.dailyClaim));return;}if(onlineBtn){openSocialHub();}};`
- [x] `return` — linha 1112, coluna 314 em `openEducationHub()` — `root.onclick=e=>{const tabBtn=e.target.closest('[data-edu-tab]'),playBtn=e.target.closest('[data-edu-play]'),startBtn=e.target.closest('[data-academy-start]'),claimBtn=e.target.closest('[data-daily-claim]'),onlineBtn=e.target.closest('[data-open-online]');if(tabBtn){openEducationHub(tabBtn.dataset.eduTab);return;}if(startBtn){startSoloEducationGame(startBtn.dataset.academyStart,1);return;}if(playBtn){startSoloEducationGame(playBtn.dataset.eduPlay,Number(playBtn.dataset.eduLevel));return;}if(claimBtn){claimDailyQuest(Number(claimBtn.dataset.dailyClaim));return;}if(onlineBtn){openSocialHub();}};`
- [x] `if` — linha 1112, coluna 322 em `openEducationHub()` — `root.onclick=e=>{const tabBtn=e.target.closest('[data-edu-tab]'),playBtn=e.target.closest('[data-edu-play]'),startBtn=e.target.closest('[data-academy-start]'),claimBtn=e.target.closest('[data-daily-claim]'),onlineBtn=e.target.closest('[data-open-online]');if(tabBtn){openEducationHub(tabBtn.dataset.eduTab);return;}if(startBtn){startSoloEducationGame(startBtn.dataset.academyStart,1);return;}if(playBtn){startSoloEducationGame(playBtn.dataset.eduPlay,Number(playBtn.dataset.eduLevel));return;}if(claimBtn){claimDailyQuest(Number(claimBtn.dataset.dailyClaim));return;}if(onlineBtn){openSocialHub();}};`
- [x] `return` — linha 1112, coluna 391 em `openEducationHub()` — `root.onclick=e=>{const tabBtn=e.target.closest('[data-edu-tab]'),playBtn=e.target.closest('[data-edu-play]'),startBtn=e.target.closest('[data-academy-start]'),claimBtn=e.target.closest('[data-daily-claim]'),onlineBtn=e.target.closest('[data-open-online]');if(tabBtn){openEducationHub(tabBtn.dataset.eduTab);return;}if(startBtn){startSoloEducationGame(startBtn.dataset.academyStart,1);return;}if(playBtn){startSoloEducationGame(playBtn.dataset.eduPlay,Number(playBtn.dataset.eduLevel));return;}if(claimBtn){claimDailyQuest(Number(claimBtn.dataset.dailyClaim));return;}if(onlineBtn){openSocialHub();}};`
- [x] `if` — linha 1112, coluna 399 em `openEducationHub()` — `root.onclick=e=>{const tabBtn=e.target.closest('[data-edu-tab]'),playBtn=e.target.closest('[data-edu-play]'),startBtn=e.target.closest('[data-academy-start]'),claimBtn=e.target.closest('[data-daily-claim]'),onlineBtn=e.target.closest('[data-open-online]');if(tabBtn){openEducationHub(tabBtn.dataset.eduTab);return;}if(startBtn){startSoloEducationGame(startBtn.dataset.academyStart,1);return;}if(playBtn){startSoloEducationGame(playBtn.dataset.eduPlay,Number(playBtn.dataset.eduLevel));return;}if(claimBtn){claimDailyQuest(Number(claimBtn.dataset.dailyClaim));return;}if(onlineBtn){openSocialHub();}};`
- [x] `return` — linha 1112, coluna 492 em `openEducationHub()` — `root.onclick=e=>{const tabBtn=e.target.closest('[data-edu-tab]'),playBtn=e.target.closest('[data-edu-play]'),startBtn=e.target.closest('[data-academy-start]'),claimBtn=e.target.closest('[data-daily-claim]'),onlineBtn=e.target.closest('[data-open-online]');if(tabBtn){openEducationHub(tabBtn.dataset.eduTab);return;}if(startBtn){startSoloEducationGame(startBtn.dataset.academyStart,1);return;}if(playBtn){startSoloEducationGame(playBtn.dataset.eduPlay,Number(playBtn.dataset.eduLevel));return;}if(claimBtn){claimDailyQuest(Number(claimBtn.dataset.dailyClaim));return;}if(onlineBtn){openSocialHub();}};`
- [x] `if` — linha 1112, coluna 500 em `openEducationHub()` — `root.onclick=e=>{const tabBtn=e.target.closest('[data-edu-tab]'),playBtn=e.target.closest('[data-edu-play]'),startBtn=e.target.closest('[data-academy-start]'),claimBtn=e.target.closest('[data-daily-claim]'),onlineBtn=e.target.closest('[data-open-online]');if(tabBtn){openEducationHub(tabBtn.dataset.eduTab);return;}if(startBtn){startSoloEducationGame(startBtn.dataset.academyStart,1);return;}if(playBtn){startSoloEducationGame(playBtn.dataset.eduPlay,Number(playBtn.dataset.eduLevel));return;}if(claimBtn){claimDailyQuest(Number(claimBtn.dataset.dailyClaim));return;}if(onlineBtn){openSocialHub();}};`
- [x] `return` — linha 1112, coluna 566 em `openEducationHub()` — `root.onclick=e=>{const tabBtn=e.target.closest('[data-edu-tab]'),playBtn=e.target.closest('[data-edu-play]'),startBtn=e.target.closest('[data-academy-start]'),claimBtn=e.target.closest('[data-daily-claim]'),onlineBtn=e.target.closest('[data-open-online]');if(tabBtn){openEducationHub(tabBtn.dataset.eduTab);return;}if(startBtn){startSoloEducationGame(startBtn.dataset.academyStart,1);return;}if(playBtn){startSoloEducationGame(playBtn.dataset.eduPlay,Number(playBtn.dataset.eduLevel));return;}if(claimBtn){claimDailyQuest(Number(claimBtn.dataset.dailyClaim));return;}if(onlineBtn){openSocialHub();}};`
- [x] `if` — linha 1112, coluna 574 em `openEducationHub()` — `root.onclick=e=>{const tabBtn=e.target.closest('[data-edu-tab]'),playBtn=e.target.closest('[data-edu-play]'),startBtn=e.target.closest('[data-academy-start]'),claimBtn=e.target.closest('[data-daily-claim]'),onlineBtn=e.target.closest('[data-open-online]');if(tabBtn){openEducationHub(tabBtn.dataset.eduTab);return;}if(startBtn){startSoloEducationGame(startBtn.dataset.academyStart,1);return;}if(playBtn){startSoloEducationGame(playBtn.dataset.eduPlay,Number(playBtn.dataset.eduLevel));return;}if(claimBtn){claimDailyQuest(Number(claimBtn.dataset.dailyClaim));return;}if(onlineBtn){openSocialHub();}};`
- [x] `if` — linha 1119, coluna 133 em `runEducationGame()` — `const complete=()=>{const elapsed=Math.round(performance.now()-started),result={score,total:items.length,elapsed,subject,level};if(onFinish)return onFinish(result);finishSoloEducationGame(result);};`
- [x] `return` — linha 1119, coluna 145 em `runEducationGame()` — `const complete=()=>{const elapsed=Math.round(performance.now()-started),result={score,total:items.length,elapsed,subject,level};if(onFinish)return onFinish(result);finishSoloEducationGame(result);};`
- [x] `if` — linha 1120, coluna 23 em `runEducationGame()` — `const render=()=>{if(step>=items.length||hearts<=0)return complete();const q=items[step],progress=Math.round(step/items.length*100),speak=q.speak?\`<button class="edu-speak" data-edu-speak>🔊 Ouvir</button>\`:'';`
- [x] `return` — linha 1120, coluna 56 em `runEducationGame()` — `const render=()=>{if(step>=items.length||hearts<=0)return complete();const q=items[step],progress=Math.round(step/items.length*100),speak=q.speak?\`<button class="edu-speak" data-edu-speak>🔊 Ouvir</button>\`:'';`
- [x] `if` — linha 1123, coluna 9 em `runEducationGame()` — `if(q.autoSpeak)setTimeout(()=>speakKidWord(q.speak),180);$('[data-edu-speak]',root)?.addEventListener('click',()=>speakKidWord(q.speak));`
- [x] `if` — linha 1124, coluna 52 em `runEducationGame()` — `const resolveAnswer=(answer,button=null)=>{if(locked)return;locked=true;const correct=String(answer).toUpperCase()===String(q.answer).toUpperCase(),feedback=$('#eduFeedback',root);if(correct){score++;state.learning.totalCorrect++;button?.classList.add('correct');feedback.hidden=false;feedback.className='lesson-feedback good';feedback.innerHTML='<b>Correto!</b><span>Continue assim.</span>';beep(780,80);addXP(5);}else{hearts--;button?.classList.add('wrong');feedback.hidden=false;feedback.className='lesson-feedback bad';feedback.innerHTML=\`<b>Quase!</b><span>Resposta: ${escapeHtml(q.displayAnswer||q.answer)}</span>\`;beep(180,110,'sawtooth');}setTimeout(()=>{step++;sequence=[];locked=false;render();},780);};`
- [x] `return` — linha 1124, coluna 62 em `runEducationGame()` — `const resolveAnswer=(answer,button=null)=>{if(locked)return;locked=true;const correct=String(answer).toUpperCase()===String(q.answer).toUpperCase(),feedback=$('#eduFeedback',root);if(correct){score++;state.learning.totalCorrect++;button?.classList.add('correct');feedback.hidden=false;feedback.className='lesson-feedback good';feedback.innerHTML='<b>Correto!</b><span>Continue assim.</span>';beep(780,80);addXP(5);}else{hearts--;button?.classList.add('wrong');feedback.hidden=false;feedback.className='lesson-feedback bad';feedback.innerHTML=\`<b>Quase!</b><span>Resposta: ${escapeHtml(q.displayAnswer||q.answer)}</span>\`;beep(180,110,'sawtooth');}setTimeout(()=>{step++;sequence=[];locked=false;render();},780);};`
- [x] `if` — linha 1124, coluna 189 em `runEducationGame()` — `const resolveAnswer=(answer,button=null)=>{if(locked)return;locked=true;const correct=String(answer).toUpperCase()===String(q.answer).toUpperCase(),feedback=$('#eduFeedback',root);if(correct){score++;state.learning.totalCorrect++;button?.classList.add('correct');feedback.hidden=false;feedback.className='lesson-feedback good';feedback.innerHTML='<b>Correto!</b><span>Continue assim.</span>';beep(780,80);addXP(5);}else{hearts--;button?.classList.add('wrong');feedback.hidden=false;feedback.className='lesson-feedback bad';feedback.innerHTML=\`<b>Quase!</b><span>Resposta: ${escapeHtml(q.displayAnswer||q.answer)}</span>\`;beep(180,110,'sawtooth');}setTimeout(()=>{step++;sequence=[];locked=false;render();},780);};`
- [x] `else` — linha 1124, coluna 424 em `runEducationGame()` — `const resolveAnswer=(answer,button=null)=>{if(locked)return;locked=true;const correct=String(answer).toUpperCase()===String(q.answer).toUpperCase(),feedback=$('#eduFeedback',root);if(correct){score++;state.learning.totalCorrect++;button?.classList.add('correct');feedback.hidden=false;feedback.className='lesson-feedback good';feedback.innerHTML='<b>Correto!</b><span>Continue assim.</span>';beep(780,80);addXP(5);}else{hearts--;button?.classList.add('wrong');feedback.hidden=false;feedback.className='lesson-feedback bad';feedback.innerHTML=\`<b>Quase!</b><span>Resposta: ${escapeHtml(q.displayAnswer||q.answer)}</span>\`;beep(180,110,'sawtooth');}setTimeout(()=>{step++;sequence=[];locked=false;render();},780);};`
- [x] `if` — linha 1126, coluna 72 em `runEducationGame()` — `$$('[data-sequence-token]',root).forEach(btn=>btn.onclick=()=>{if(locked||btn.disabled)return;sequence.push(q.tokens[Number(btn.dataset.sequenceToken)]);btn.disabled=true;$('#sequenceBuilt',root).textContent=sequence.join('');});`
- [x] `return` — linha 1126, coluna 96 em `runEducationGame()` — `$$('[data-sequence-token]',root).forEach(btn=>btn.onclick=()=>{if(locked||btn.disabled)return;sequence.push(q.tokens[Number(btn.dataset.sequenceToken)]);btn.disabled=true;$('#sequenceBuilt',root).textContent=sequence.join('');});`
- [x] `catch` — linha 1131, coluna 145 em `startSoloEducationGame()` — `function startSoloEducationGame(subject,level){try{closeChallengePrompt();runEducationGame({subject,level:Number(level)||1,seed:Date.now()});}catch(error){console.error('Academia Kids:',error);toast('Não foi possível abrir o jogo. Atualize a página.','bad',3000);}}`
- [x] `if` — linha 1132, coluna 531 em `finishSoloEducationGame()` — `function finishSoloEducationGame(result){const passed=result.score>=3,stars=passed?(result.score===result.total?3:result.score>=4?2:1):0,key=\`${result.subject}-${result.level}\`,old=subjectLevelRecord(result.subject,result.level);state.learning.lessons[key]={completed:old.completed||passed,stars:Math.max(old.stars||0,stars),best:Math.max(old.best||0,result.score),attempts:(old.attempts||0)+1};state.learning.lastLesson=key;state.learning.subjectXP[result.subject]=(state.learning.subjectXP[result.subject]||0)+result.score*10;if(passed){state.learning.crowns+=(old.completed?0:1);if(result.score===result.total)state.learning.perfectLessons++;const coins=30+stars*18;addCoins(coins);addXP(25+stars*12);awardMedal(result.score===result.total?\`${EDUCATION_SUBJECTS[result.subject].title} Perfeito\`:\`Aluno ${EDUCATION_SUBJECTS[result.subject].title}\`);saveState(true);openModal('Fase concluída!',\`<div class="lesson-result"><div>${result.score===result.total?'🏆':'🎉'}</div><h3>${result.score}/${result`
- [x] `if` — linha 1132, coluna 585 em `finishSoloEducationGame()` — `function finishSoloEducationGame(result){const passed=result.score>=3,stars=passed?(result.score===result.total?3:result.score>=4?2:1):0,key=\`${result.subject}-${result.level}\`,old=subjectLevelRecord(result.subject,result.level);state.learning.lessons[key]={completed:old.completed||passed,stars:Math.max(old.stars||0,stars),best:Math.max(old.best||0,result.score),attempts:(old.attempts||0)+1};state.learning.lastLesson=key;state.learning.subjectXP[result.subject]=(state.learning.subjectXP[result.subject]||0)+result.score*10;if(passed){state.learning.crowns+=(old.completed?0:1);if(result.score===result.total)state.learning.perfectLessons++;const coins=30+stars*18;addCoins(coins);addXP(25+stars*12);awardMedal(result.score===result.total?\`${EDUCATION_SUBJECTS[result.subject].title} Perfeito\`:\`Aluno ${EDUCATION_SUBJECTS[result.subject].title}\`);saveState(true);openModal('Fase concluída!',\`<div class="lesson-result"><div>${result.score===result.total?'🏆':'🎉'}</div><h3>${result.score}/${result`
- [x] `else` — linha 1132, coluna 1226 em `finishSoloEducationGame()` — `function finishSoloEducationGame(result){const passed=result.score>=3,stars=passed?(result.score===result.total?3:result.score>=4?2:1):0,key=\`${result.subject}-${result.level}\`,old=subjectLevelRecord(result.subject,result.level);state.learning.lessons[key]={completed:old.completed||passed,stars:Math.max(old.stars||0,stars),best:Math.max(old.best||0,result.score),attempts:(old.attempts||0)+1};state.learning.lastLesson=key;state.learning.subjectXP[result.subject]=(state.learning.subjectXP[result.subject]||0)+result.score*10;if(passed){state.learning.crowns+=(old.completed?0:1);if(result.score===result.total)state.learning.perfectLessons++;const coins=30+stars*18;addCoins(coins);addXP(25+stars*12);awardMedal(result.score===result.total?\`${EDUCATION_SUBJECTS[result.subject].title} Perfeito\`:\`Aluno ${EDUCATION_SUBJECTS[result.subject].title}\`);saveState(true);openModal('Fase concluída!',\`<div class="lesson-result"><div>${result.score===result.total?'🏆':'🎉'}</div><h3>${result.score}/${result`
- [x] `if` — linha 1136, coluna 5 em `startCinematicEmote()` — `if(!['dance','play','selfie','highfive','hug'].includes(type))return;clearTimeout(cinematicEmoteTimer);document.body.classList.add('cinematic-emote');cinematicEmoteTimer=setTimeout(()=>document.body.classList.remove('cinematic-emote'),Math.max(900,duration));`
- [x] `return` — linha 1136, coluna 67 em `startCinematicEmote()` — `if(!['dance','play','selfie','highfive','hug'].includes(type))return;clearTimeout(cinematicEmoteTimer);document.body.classList.add('cinematic-emote');cinematicEmoteTimer=setTimeout(()=>document.body.classList.remove('cinematic-emote'),Math.max(900,duration));`
- [x] `if` — linha 1140, coluna 5 em `triggerEmote()` — `if(npc){npc.emoteType=type;npc.emoteUntil=performance.now()+duration;}`
- [x] `if` — linha 1141, coluna 5 em `triggerEmote()` — `if(['dance','play','selfie','highfive','hug'].includes(type)){`
- [x] `if` — linha 1143, coluna 23 em `triggerEmote()` — `setTimeout(()=>{if(triggerEmote.cinemaToken===token)document.body.classList.remove('social-moment');},duration+180);`
- [x] `if` — linha 1145, coluna 5 em `triggerEmote()` — `if(type==='play')state.needs.fun=clamp(state.needs.fun+8,0,100);`

## `05-avatar-life-customization.js`

- [x] `if` — linha 1175, coluna 5 em `effectiveAvatarUniform()` — `if(job&&!terminal.includes(String(job.missionState||''))){`
- [x] `return` — linha 1177, coluna 7 em `effectiveAvatarUniform()` — `return uniforms[job.id]||job.uniform||state.avatar?.uniform||'none';`
- [x] `return` — linha 1179, coluna 5 em `effectiveAvatarUniform()` — `return state.avatar?.uniform||'none';`
- [x] `return` — linha 1184, coluna 5 em `avatarChoiceGroup()` — `return \`<section class="avatar-section"><h3>${title}${locked?' <small>• definido pela missão</small>':''}</h3><div class="avatar-grid">${avatarCatalog[type].map(([id,name,icon]) => \`<button class="avatar-option ${selected===id?'selected':''}" data-avatar-type="${type}" data-avatar-id="${id}" ${locked?'disabled aria-disabled="true"':''}><b>${icon}</b><span>${name}</span></button>\`).join('')}</div></section>\`;`
- [x] `if` — linha 1205, coluna 60 em `openLifePanel()` — `const cancel=$('[data-life-cancel-companions]',root);if(cancel)cancel.onclick=()=>{for(const npc of world.npcs||[]){npc.following=false;npc.pendingRide=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;}closeModal();toast('Passeio encerrado com segurança.','good');};`
- [x] `for` — linha 1205, coluna 90 em `openLifePanel()` — `const cancel=$('[data-life-cancel-companions]',root);if(cancel)cancel.onclick=()=>{for(const npc of world.npcs||[]){npc.following=false;npc.pendingRide=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;}closeModal();toast('Passeio encerrado com segurança.','good');};`
- [x] `if` — linha 1205, coluna 188 em `openLifePanel()` — `const cancel=$('[data-life-cancel-companions]',root);if(cancel)cancel.onclick=()=>{for(const npc of world.npcs||[]){npc.following=false;npc.pendingRide=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;}closeModal();toast('Passeio encerrado com segurança.','good');};`

## `06-missions-profile-hud-inventory-tools.js`

- [x] `for` — linha 1330, coluna 5 em `evaluateMissions()` — `for (const chapter of missionChapters) {`
- [x] `if` — linha 1332, coluna 7 em `evaluateMissions()` — `if (complete && !state.completedChapters.includes(chapter.id)) {`
- [x] `if` — linha 1340, coluna 5 em `evaluateMissions()` — `if(state.objectives?.pinnedChapterId&&!pinned)state.objectives.pinnedChapterId='';`
- [x] `if` — linha 1347, coluna 93 em `updateMissionHUD()` — `const coop=typeof coopMissionProgressLabel==='function'?coopMissionProgressLabel():null;if(coop){els.missionChapter.textContent='MISSÃO COOPERATIVA';els.missionTitle.textContent=coop.title;els.missionStep.textContent=coop.label;els.missionFill.style.width=\`${coop.percent}%\`;els.missionCard.title='Toque para abrir os detalhes da missão cooperativa';els.missionCard.setAttribute('aria-label',\`Abrir missão cooperativa. ${coop.label}\`);return;}`
- [x] `return` — linha 1347, coluna 440 em `updateMissionHUD()` — `const coop=typeof coopMissionProgressLabel==='function'?coopMissionProgressLabel():null;if(coop){els.missionChapter.textContent='MISSÃO COOPERATIVA';els.missionTitle.textContent=coop.title;els.missionStep.textContent=coop.label;els.missionFill.style.width=\`${coop.percent}%\`;els.missionCard.title='Toque para abrir os detalhes da missão cooperativa';els.missionCard.setAttribute('aria-label',\`Abrir missão cooperativa. ${coop.label}\`);return;}`
- [x] `if` — linha 1349, coluna 5 em `updateMissionHUD()` — `if (job) {`
- [x] `return` — linha 1356, coluna 7 em `updateMissionHUD()` — `return;`
- [x] `if` — linha 1358, coluna 5 em `updateMissionHUD()` — `if (!activeMission) return;`
- [x] `return` — linha 1358, coluna 25 em `updateMissionHUD()` — `if (!activeMission) return;`
- [x] `return` — linha 1367, coluna 39 em `objectiveStatusLabel()` — `function objectiveStatusLabel(flag){return state.flags[flag]?'Concluído':'Pendente';}`
- [x] `return` — linha 1368, coluna 204 em `skillMasterySummary()` — `function skillMasterySummary(){const names={miniDash:'Dash Mini',superJump:'Super Pulo',giantSlam:'Impacto Gigante',stealth:'Escudo Furtivo',magnetSpin:'Giro Ímã'},mastery=state.abilities?.mastery||{};return Object.entries(names).map(([id,name])=>\`<div class="skill-mastery"><b>${name}</b><span>Nível ${Number(mastery[id]||0)}</span></div>\`).join('');}`
- [x] `return` — linha 1372, coluna 184 em `openObjectivesPanel()` — `const chapters=missionChapters.map(ch=>{const done=ch.steps.filter(([f])=>!!state.flags[f]).length,complete=done===ch.steps.length,pinned=state.objectives.pinnedChapterId===ch.id;return \`<article class="objective-chapter ${complete?'complete':''} ${pinned?'pinned':''}"><header><b>${complete?'✅':'◆'} ${ch.title}</b><span>${done}/${ch.steps.length}</span></header>${ch.steps.map(([f,t])=>\`<p class="${state.flags[f]?'done':''}">${state.flags[f]?'✓':'○'} ${playerText(t)}</p>\`).join('')}<button class="btn objective-pin" data-pin-chapter="${ch.id}" ${complete?'disabled':''}>${pinned?'Destacado':'Destacar objetivo'}</button></article>\`;}).join('');`
- [x] `return` — linha 1378, coluna 38 em `sanitizePlayerName()` — `function sanitizePlayerName(value){return String(value||'').replace(/[^\p{L}\p{N} _.-]/gu,'').replace(/\s+/g,' ').trim().slice(0,18);}`
- [x] `return` — linha 1379, coluna 83 em `hasValidPlayerName()` — `function hasValidPlayerName(){const name=sanitizePlayerName(state.profile.name);return !!(state.profile.nameConfirmed&&name.length>=3);}`
- [x] `return` — linha 1380, coluna 32 em `playerDisplayName()` — `function playerDisplayName(){return hasValidPlayerName()?sanitizePlayerName(state.profile.name):'Jogador';}`
- [x] `return` — linha 1381, coluna 33 em `playerText()` — `function playerText(value=''){return String(value).replaceAll('Casa do Otthos',\`Casa de ${playerDisplayName()}\`).replaceAll('do Otthos',\`de ${playerDisplayName()}\`).replaceAll('o Otthos',playerDisplayName()).replaceAll('Otthos',playerDisplayName());}`
- [x] `if` — linha 1382, coluna 100 em `updatePlayerNameUI()` — `function updatePlayerNameUI(){const name=hasValidPlayerName()?state.profile.name:'Escolher nome';if(els.lobbyPlayerName)els.lobbyPlayerName.textContent=name;if(els.hudPlayerName)els.hudPlayerName.textContent=hasValidPlayerName()?state.profile.name:'Jogador';if(els.accountStatusLabel)els.accountStatusLabel.textContent=accountStatusText();const menu=$('#avatarMenuName'),quick=$('#avatarQuickName');if(menu)menu.textContent=\`Meu ${playerDisplayName()}\`;if(quick)quick.textContent=playerDisplayName();const homeLoc=MAP_LOCATIONS?.find?.(x=>x.id==='home');if(homeLoc)homeLoc.name=\`Casa de ${playerDisplayName()}\`;const homeHouse=world?.houses?.find?.(x=>x.id==='home');if(homeHouse)homeHouse.name=\`Casa de ${playerDisplayName()}\`;updateLocalPlayerNameLabel?.();}`
- [x] `if` — linha 1382, coluna 160 em `updatePlayerNameUI()` — `function updatePlayerNameUI(){const name=hasValidPlayerName()?state.profile.name:'Escolher nome';if(els.lobbyPlayerName)els.lobbyPlayerName.textContent=name;if(els.hudPlayerName)els.hudPlayerName.textContent=hasValidPlayerName()?state.profile.name:'Jogador';if(els.accountStatusLabel)els.accountStatusLabel.textContent=accountStatusText();const menu=$('#avatarMenuName'),quick=$('#avatarQuickName');if(menu)menu.textContent=\`Meu ${playerDisplayName()}\`;if(quick)quick.textContent=playerDisplayName();const homeLoc=MAP_LOCATIONS?.find?.(x=>x.id==='home');if(homeLoc)homeLoc.name=\`Casa de ${playerDisplayName()}\`;const homeHouse=world?.houses?.find?.(x=>x.id==='home');if(homeHouse)homeHouse.name=\`Casa de ${playerDisplayName()}\`;updateLocalPlayerNameLabel?.();}`
- [x] `if` — linha 1382, coluna 261 em `updatePlayerNameUI()` — `function updatePlayerNameUI(){const name=hasValidPlayerName()?state.profile.name:'Escolher nome';if(els.lobbyPlayerName)els.lobbyPlayerName.textContent=name;if(els.hudPlayerName)els.hudPlayerName.textContent=hasValidPlayerName()?state.profile.name:'Jogador';if(els.accountStatusLabel)els.accountStatusLabel.textContent=accountStatusText();const menu=$('#avatarMenuName'),quick=$('#avatarQuickName');if(menu)menu.textContent=\`Meu ${playerDisplayName()}\`;if(quick)quick.textContent=playerDisplayName();const homeLoc=MAP_LOCATIONS?.find?.(x=>x.id==='home');if(homeLoc)homeLoc.name=\`Casa de ${playerDisplayName()}\`;const homeHouse=world?.houses?.find?.(x=>x.id==='home');if(homeHouse)homeHouse.name=\`Casa de ${playerDisplayName()}\`;updateLocalPlayerNameLabel?.();}`
- [x] `if` — linha 1382, coluna 402 em `updatePlayerNameUI()` — `function updatePlayerNameUI(){const name=hasValidPlayerName()?state.profile.name:'Escolher nome';if(els.lobbyPlayerName)els.lobbyPlayerName.textContent=name;if(els.hudPlayerName)els.hudPlayerName.textContent=hasValidPlayerName()?state.profile.name:'Jogador';if(els.accountStatusLabel)els.accountStatusLabel.textContent=accountStatusText();const menu=$('#avatarMenuName'),quick=$('#avatarQuickName');if(menu)menu.textContent=\`Meu ${playerDisplayName()}\`;if(quick)quick.textContent=playerDisplayName();const homeLoc=MAP_LOCATIONS?.find?.(x=>x.id==='home');if(homeLoc)homeLoc.name=\`Casa de ${playerDisplayName()}\`;const homeHouse=world?.houses?.find?.(x=>x.id==='home');if(homeHouse)homeHouse.name=\`Casa de ${playerDisplayName()}\`;updateLocalPlayerNameLabel?.();}`
- [x] `if` — linha 1382, coluna 456 em `updatePlayerNameUI()` — `function updatePlayerNameUI(){const name=hasValidPlayerName()?state.profile.name:'Escolher nome';if(els.lobbyPlayerName)els.lobbyPlayerName.textContent=name;if(els.hudPlayerName)els.hudPlayerName.textContent=hasValidPlayerName()?state.profile.name:'Jogador';if(els.accountStatusLabel)els.accountStatusLabel.textContent=accountStatusText();const menu=$('#avatarMenuName'),quick=$('#avatarQuickName');if(menu)menu.textContent=\`Meu ${playerDisplayName()}\`;if(quick)quick.textContent=playerDisplayName();const homeLoc=MAP_LOCATIONS?.find?.(x=>x.id==='home');if(homeLoc)homeLoc.name=\`Casa de ${playerDisplayName()}\`;const homeHouse=world?.houses?.find?.(x=>x.id==='home');if(homeHouse)homeHouse.name=\`Casa de ${playerDisplayName()}\`;updateLocalPlayerNameLabel?.();}`
- [x] `if` — linha 1382, coluna 557 em `updatePlayerNameUI()` — `function updatePlayerNameUI(){const name=hasValidPlayerName()?state.profile.name:'Escolher nome';if(els.lobbyPlayerName)els.lobbyPlayerName.textContent=name;if(els.hudPlayerName)els.hudPlayerName.textContent=hasValidPlayerName()?state.profile.name:'Jogador';if(els.accountStatusLabel)els.accountStatusLabel.textContent=accountStatusText();const menu=$('#avatarMenuName'),quick=$('#avatarQuickName');if(menu)menu.textContent=\`Meu ${playerDisplayName()}\`;if(quick)quick.textContent=playerDisplayName();const homeLoc=MAP_LOCATIONS?.find?.(x=>x.id==='home');if(homeLoc)homeLoc.name=\`Casa de ${playerDisplayName()}\`;const homeHouse=world?.houses?.find?.(x=>x.id==='home');if(homeHouse)homeHouse.name=\`Casa de ${playerDisplayName()}\`;updateLocalPlayerNameLabel?.();}`
- [x] `if` — linha 1382, coluna 670 em `updatePlayerNameUI()` — `function updatePlayerNameUI(){const name=hasValidPlayerName()?state.profile.name:'Escolher nome';if(els.lobbyPlayerName)els.lobbyPlayerName.textContent=name;if(els.hudPlayerName)els.hudPlayerName.textContent=hasValidPlayerName()?state.profile.name:'Jogador';if(els.accountStatusLabel)els.accountStatusLabel.textContent=accountStatusText();const menu=$('#avatarMenuName'),quick=$('#avatarQuickName');if(menu)menu.textContent=\`Meu ${playerDisplayName()}\`;if(quick)quick.textContent=playerDisplayName();const homeLoc=MAP_LOCATIONS?.find?.(x=>x.id==='home');if(homeLoc)homeLoc.name=\`Casa de ${playerDisplayName()}\`;const homeHouse=world?.houses?.find?.(x=>x.id==='home');if(homeHouse)homeHouse.name=\`Casa de ${playerDisplayName()}\`;updateLocalPlayerNameLabel?.();}`
- [x] `if` — linha 1383, coluna 71 em `applyPlayerName()` — `function applyPlayerName(name){const clean=sanitizePlayerName(name);if(clean.length<3){toast('Digite um apelido com pelo menos 3 caracteres.','warn',2200);return false;}state.profile.name=clean;state.profile.nameConfirmed=true;state.multiplayer.displayName=clean;updatePlayerNameUI();saveState(true);const publicName=publicPlayerName(clean);window.OTTHOS_RTDB?.setDisplayName?.(publicName);if(running)window.OTTHOS_RTDB?.publish?.({name:publicName,x:player.x,y:player.y,z:player.z,r:player.facing,vehicle:!!player.vehicle,vehicleId:player.car.id||'',vehicleRole:player.vehicle?(player.car.passengerOf?'passenger':'driver'):'',vehiclePassengerOf:player.car.passengerOf||'',vehiclePassengerUid:player.car.passengerUid||'',vehiclePassengerBotId:player.car.passengerBotId||'',boating:!!player.boating,boatId:state.boats.activeBoatId||'',boatRole:player.boating?(player.boat.passengerOf?'passenger':'driver'):'',passengerOf:player.boat.passengerOf||'',boatPassengerUid:player.boat.passengerUid||'',boatPas`
- [x] `return` — linha 1383, coluna 158 em `applyPlayerName()` — `function applyPlayerName(name){const clean=sanitizePlayerName(name);if(clean.length<3){toast('Digite um apelido com pelo menos 3 caracteres.','warn',2200);return false;}state.profile.name=clean;state.profile.nameConfirmed=true;state.multiplayer.displayName=clean;updatePlayerNameUI();saveState(true);const publicName=publicPlayerName(clean);window.OTTHOS_RTDB?.setDisplayName?.(publicName);if(running)window.OTTHOS_RTDB?.publish?.({name:publicName,x:player.x,y:player.y,z:player.z,r:player.facing,vehicle:!!player.vehicle,vehicleId:player.car.id||'',vehicleRole:player.vehicle?(player.car.passengerOf?'passenger':'driver'):'',vehiclePassengerOf:player.car.passengerOf||'',vehiclePassengerUid:player.car.passengerUid||'',vehiclePassengerBotId:player.car.passengerBotId||'',boating:!!player.boating,boatId:state.boats.activeBoatId||'',boatRole:player.boating?(player.boat.passengerOf?'passenger':'driver'):'',passengerOf:player.boat.passengerOf||'',boatPassengerUid:player.boat.passengerUid||'',boatPas`
- [x] `if` — linha 1383, coluna 393 em `applyPlayerName()` — `function applyPlayerName(name){const clean=sanitizePlayerName(name);if(clean.length<3){toast('Digite um apelido com pelo menos 3 caracteres.','warn',2200);return false;}state.profile.name=clean;state.profile.nameConfirmed=true;state.multiplayer.displayName=clean;updatePlayerNameUI();saveState(true);const publicName=publicPlayerName(clean);window.OTTHOS_RTDB?.setDisplayName?.(publicName);if(running)window.OTTHOS_RTDB?.publish?.({name:publicName,x:player.x,y:player.y,z:player.z,r:player.facing,vehicle:!!player.vehicle,vehicleId:player.car.id||'',vehicleRole:player.vehicle?(player.car.passengerOf?'passenger':'driver'):'',vehiclePassengerOf:player.car.passengerOf||'',vehiclePassengerUid:player.car.passengerUid||'',vehiclePassengerBotId:player.car.passengerBotId||'',boating:!!player.boating,boatId:state.boats.activeBoatId||'',boatRole:player.boating?(player.boat.passengerOf?'passenger':'driver'):'',passengerOf:player.boat.passengerOf||'',boatPassengerUid:player.boat.passengerUid||'',boatPas`
- [x] `return` — linha 1383, coluna 1122 em `applyPlayerName()` — `function applyPlayerName(name){const clean=sanitizePlayerName(name);if(clean.length<3){toast('Digite um apelido com pelo menos 3 caracteres.','warn',2200);return false;}state.profile.name=clean;state.profile.nameConfirmed=true;state.multiplayer.displayName=clean;updatePlayerNameUI();saveState(true);const publicName=publicPlayerName(clean);window.OTTHOS_RTDB?.setDisplayName?.(publicName);if(running)window.OTTHOS_RTDB?.publish?.({name:publicName,x:player.x,y:player.y,z:player.z,r:player.facing,vehicle:!!player.vehicle,vehicleId:player.car.id||'',vehicleRole:player.vehicle?(player.car.passengerOf?'passenger':'driver'):'',vehiclePassengerOf:player.car.passengerOf||'',vehiclePassengerUid:player.car.passengerUid||'',vehiclePassengerBotId:player.car.passengerBotId||'',boating:!!player.boating,boatId:state.boats.activeBoatId||'',boatRole:player.boating?(player.boat.passengerOf?'passenger':'driver'):'',passengerOf:player.boat.passengerOf||'',boatPassengerUid:player.boat.passengerUid||'',boatPas`
- [x] `if` — linha 1386, coluna 809 em `openPlayerNameModal()` — `openModal(required?'Escolha seu nome de jogador':'Nome do jogador',\`<div class="player-name-modal"><div class="player-name-icon">👤</div><p>${required?'Antes de entrar, escolha o nome que aparecerá para os outros jogadores.':'Este nome aparece sobre seu personagem no multiplayer.'}</p><label class="field"><span>Nome público</span><input id="playerNameInput" maxlength="18" autocomplete="nickname" inputmode="text" value="${current.replace(/"/g,'&quot;')}" placeholder="Ex.: Thiago"></label><small>De 3 a 18 caracteres. Não use telefone, endereço ou informação pessoal sensível.</small><button class="btn primary xl" data-save-player-name>Salvar nome</button></div>\`,root=>{const input=$('#playerNameInput',root),save=$('[data-save-player-name]',root);setTimeout(()=>input?.focus(),80);const submit=()=>{if(!applyPlayerName(input?.value))return;closeModal();toast(\`Nome definido: ${state.profile.name}\`,'good',1600);if(typeof onSaved==='function')onSaved();};save.onclick=submit;input?.addEventListen`
- [x] `return` — linha 1386, coluna 843 em `openPlayerNameModal()` — `openModal(required?'Escolha seu nome de jogador':'Nome do jogador',\`<div class="player-name-modal"><div class="player-name-icon">👤</div><p>${required?'Antes de entrar, escolha o nome que aparecerá para os outros jogadores.':'Este nome aparece sobre seu personagem no multiplayer.'}</p><label class="field"><span>Nome público</span><input id="playerNameInput" maxlength="18" autocomplete="nickname" inputmode="text" value="${current.replace(/"/g,'&quot;')}" placeholder="Ex.: Thiago"></label><small>De 3 a 18 caracteres. Não use telefone, endereço ou informação pessoal sensível.</small><button class="btn primary xl" data-save-player-name>Salvar nome</button></div>\`,root=>{const input=$('#playerNameInput',root),save=$('[data-save-player-name]',root);setTimeout(()=>input?.focus(),80);const submit=()=>{if(!applyPlayerName(input?.value))return;closeModal();toast(\`Nome definido: ${state.profile.name}\`,'good',1600);if(typeof onSaved==='function')onSaved();};save.onclick=submit;input?.addEventListen`
- [x] `if` — linha 1386, coluna 921 em `openPlayerNameModal()` — `openModal(required?'Escolha seu nome de jogador':'Nome do jogador',\`<div class="player-name-modal"><div class="player-name-icon">👤</div><p>${required?'Antes de entrar, escolha o nome que aparecerá para os outros jogadores.':'Este nome aparece sobre seu personagem no multiplayer.'}</p><label class="field"><span>Nome público</span><input id="playerNameInput" maxlength="18" autocomplete="nickname" inputmode="text" value="${current.replace(/"/g,'&quot;')}" placeholder="Ex.: Thiago"></label><small>De 3 a 18 caracteres. Não use telefone, endereço ou informação pessoal sensível.</small><button class="btn primary xl" data-save-player-name>Salvar nome</button></div>\`,root=>{const input=$('#playerNameInput',root),save=$('[data-save-player-name]',root);setTimeout(()=>input?.focus(),80);const submit=()=>{if(!applyPlayerName(input?.value))return;closeModal();toast(\`Nome definido: ${state.profile.name}\`,'good',1600);if(typeof onSaved==='function')onSaved();};save.onclick=submit;input?.addEventListen`
- [x] `if` — linha 1386, coluna 1022 em `openPlayerNameModal()` — `openModal(required?'Escolha seu nome de jogador':'Nome do jogador',\`<div class="player-name-modal"><div class="player-name-icon">👤</div><p>${required?'Antes de entrar, escolha o nome que aparecerá para os outros jogadores.':'Este nome aparece sobre seu personagem no multiplayer.'}</p><label class="field"><span>Nome público</span><input id="playerNameInput" maxlength="18" autocomplete="nickname" inputmode="text" value="${current.replace(/"/g,'&quot;')}" placeholder="Ex.: Thiago"></label><small>De 3 a 18 caracteres. Não use telefone, endereço ou informação pessoal sensível.</small><button class="btn primary xl" data-save-player-name>Salvar nome</button></div>\`,root=>{const input=$('#playerNameInput',root),save=$('[data-save-player-name]',root);setTimeout(()=>input?.focus(),80);const submit=()=>{if(!applyPlayerName(input?.value))return;closeModal();toast(\`Nome definido: ${state.profile.name}\`,'good',1600);if(typeof onSaved==='function')onSaved();};save.onclick=submit;input?.addEventListen`
- [x] `if` — linha 1407, coluna 5 em `updateHUD()` — `if (els.needsToggleBtn) { els.needsToggleBtn.textContent = lowest < 20 ? '⚠️' : lowest < 50 ? '💛' : '❤️'; els.needsToggleBtn.classList.toggle('warning', lowest < 35); }`
- [x] `if` — linha 1416, coluna 332 em `openInventory()` — `</div><div class="modal-actions"><button class="btn primary" data-open-tools>Ferramentas</button>${inv.cookedFish>0?'<button class="btn" data-eat-cooked>Comer peixe assado</button>':''}</div>\`,root=>{$('[data-open-tools]',root)?.addEventListener('click',openToolbelt);$('[data-eat-cooked]',root)?.addEventListener('click',()=>{if((state.inventory.cookedFish||0)<1)return;state.inventory.cookedFish--;state.needs.hunger=clamp(state.needs.hunger+32,0,100);state.needs.energy=clamp(state.needs.energy+6,0,100);saveState(true);updateHUD();toast('Peixe assado consumido.','good');openInventory();});});`
- [x] `return` — linha 1416, coluna 369 em `openInventory()` — `</div><div class="modal-actions"><button class="btn primary" data-open-tools>Ferramentas</button>${inv.cookedFish>0?'<button class="btn" data-eat-cooked>Comer peixe assado</button>':''}</div>\`,root=>{$('[data-open-tools]',root)?.addEventListener('click',openToolbelt);$('[data-eat-cooked]',root)?.addEventListener('click',()=>{if((state.inventory.cookedFish||0)<1)return;state.inventory.cookedFish--;state.needs.hunger=clamp(state.needs.hunger+32,0,100);state.needs.energy=clamp(state.needs.energy+6,0,100);saveState(true);updateHUD();toast('Peixe assado consumido.','good');openInventory();});});`
- [x] `return` — linha 1424, coluna 27 em `equippedTool()` — `function equippedTool(){return TOOL_DEFS[state.tools?.equipped]||TOOL_DEFS.axe;}`
- [x] `if` — linha 1426, coluna 5 em `equipTool()` — `if(!TOOL_DEFS[id]||!state.tools.owned.includes(id))return false;state.tools.equipped=id;saveState(true);refreshEquippedToolVisual();if(els.toolsBtn){els.toolsBtn.firstChild.textContent=TOOL_DEFS[id].icon;$('span',els.toolsBtn).textContent=TOOL_DEFS[id].name;}toast(\`${TOOL_DEFS[id].name} equipado.\`,'good',1200);return true;`
- [x] `return` — linha 1426, coluna 56 em `equipTool()` — `if(!TOOL_DEFS[id]||!state.tools.owned.includes(id))return false;state.tools.equipped=id;saveState(true);refreshEquippedToolVisual();if(els.toolsBtn){els.toolsBtn.firstChild.textContent=TOOL_DEFS[id].icon;$('span',els.toolsBtn).textContent=TOOL_DEFS[id].name;}toast(\`${TOOL_DEFS[id].name} equipado.\`,'good',1200);return true;`
- [x] `if` — linha 1426, coluna 137 em `equipTool()` — `if(!TOOL_DEFS[id]||!state.tools.owned.includes(id))return false;state.tools.equipped=id;saveState(true);refreshEquippedToolVisual();if(els.toolsBtn){els.toolsBtn.firstChild.textContent=TOOL_DEFS[id].icon;$('span',els.toolsBtn).textContent=TOOL_DEFS[id].name;}toast(\`${TOOL_DEFS[id].name} equipado.\`,'good',1200);return true;`
- [x] `return` — linha 1426, coluna 317 em `equipTool()` — `if(!TOOL_DEFS[id]||!state.tools.owned.includes(id))return false;state.tools.equipped=id;saveState(true);refreshEquippedToolVisual();if(els.toolsBtn){els.toolsBtn.firstChild.textContent=TOOL_DEFS[id].icon;$('span',els.toolsBtn).textContent=TOOL_DEFS[id].name;}toast(\`${TOOL_DEFS[id].name} equipado.\`,'good',1200);return true;`
- [x] `if` — linha 1432, coluna 5 em `refreshEquippedToolVisual()` — `if(!playerModel?.userData?.parts?.rightArm)return;`
- [x] `return` — linha 1432, coluna 48 em `refreshEquippedToolVisual()` — `if(!playerModel?.userData?.parts?.rightArm)return;`
- [x] `if` — linha 1433, coluna 51 em `refreshEquippedToolVisual()` — `const arm=playerModel.userData.parts.rightArm;if(toolVisual){arm.remove(toolVisual);toolVisual=null;}`
- [x] `if` — linha 1436, coluna 5 em `refreshEquippedToolVisual()` — `if(type==='bucket'){`
- [x] `else` — linha 1438, coluna 6 em `refreshEquippedToolVisual()` — `}else{`
- [x] `if` — linha 1440, coluna 7 em `refreshEquippedToolVisual()` — `if(type==='axe'){box(.48,.3,.14,metal,.17,.25,0,toolVisual);box(.18,.18,.17,metal,-.13,.25,0,toolVisual);}`
- [x] `else` — linha 1441, coluna 7 em `refreshEquippedToolVisual()` — `else{box(.72,.16,.16,metal,0,.25,0,toolVisual);box(.16,.28,.16,metal,-.3,.14,0,toolVisual);box(.16,.28,.16,metal,.3,.14,0,toolVisual);}`

## `07-navigation-traffic-routes.js`

- [x] `for` — linha 1456, coluna 44 em `routeLength()` — `function routeLength(points){let total=0;for(let i=1;i<points.length;i++)total+=Math.hypot(points[i].x-points[i-1].x,points[i].z-points[i-1].z);return total;}`
- [x] `return` — linha 1456, coluna 147 em `routeLength()` — `function routeLength(points){let total=0;for(let i=1;i<points.length;i++)total+=Math.hypot(points[i].x-points[i-1].x,points[i].z-points[i-1].z);return total;}`
- [x] `for` — linha 1457, coluna 46 em `compactRoute()` — `function compactRoute(points){const out=[];for(const p of points){if(!p)continue;const last=out[out.length-1];if(!last||Math.hypot(last.x-p.x,last.z-p.z)>.25)out.push({x:+p.x,z:+p.z});}return out;}`
- [x] `if` — linha 1457, coluna 69 em `compactRoute()` — `function compactRoute(points){const out=[];for(const p of points){if(!p)continue;const last=out[out.length-1];if(!last||Math.hypot(last.x-p.x,last.z-p.z)>.25)out.push({x:+p.x,z:+p.z});}return out;}`
- [x] `if` — linha 1457, coluna 113 em `compactRoute()` — `function compactRoute(points){const out=[];for(const p of points){if(!p)continue;const last=out[out.length-1];if(!last||Math.hypot(last.x-p.x,last.z-p.z)>.25)out.push({x:+p.x,z:+p.z});}return out;}`
- [x] `return` — linha 1457, coluna 188 em `compactRoute()` — `function compactRoute(points){const out=[];for(const p of points){if(!p)continue;const last=out[out.length-1];if(!last||Math.hypot(last.x-p.x,last.z-p.z)>.25)out.push({x:+p.x,z:+p.z});}return out;}`
- [x] `return` — linha 1458, coluna 148 em `projectPointToSegment()` — `function projectPointToSegment(p,a,b){const dx=b.x-a.x,dz=b.z-a.z,len2=dx*dx+dz*dz||1,t=Math.max(0,Math.min(1,((p.x-a.x)*dx+(p.z-a.z)*dz)/len2));return{x:a.x+dx*t,z:a.z+dz*t,t};}`
- [x] `for` — linha 1459, coluna 28 em `navBlocked()` — `function navBlocked(x,z){for(const h of world.hazards||[]){if(Math.abs(x-h.x)<=h.w/2+.55&&Math.abs(z-h.z)<=h.d/2+.55)return true;}for(const c of world.colliders||[]){if(c.houseId&&currentHouse&&c.houseId===currentHouse.id)continue;if(Math.abs(x-c.x)<=c.w/2+.45&&Math.abs(z-c.z)<=c.d/2+.45)return true;}return false;}`
- [x] `if` — linha 1459, coluna 62 em `navBlocked()` — `function navBlocked(x,z){for(const h of world.hazards||[]){if(Math.abs(x-h.x)<=h.w/2+.55&&Math.abs(z-h.z)<=h.d/2+.55)return true;}for(const c of world.colliders||[]){if(c.houseId&&currentHouse&&c.houseId===currentHouse.id)continue;if(Math.abs(x-c.x)<=c.w/2+.45&&Math.abs(z-c.z)<=c.d/2+.45)return true;}return false;}`
- [x] `return` — linha 1459, coluna 120 em `navBlocked()` — `function navBlocked(x,z){for(const h of world.hazards||[]){if(Math.abs(x-h.x)<=h.w/2+.55&&Math.abs(z-h.z)<=h.d/2+.55)return true;}for(const c of world.colliders||[]){if(c.houseId&&currentHouse&&c.houseId===currentHouse.id)continue;if(Math.abs(x-c.x)<=c.w/2+.45&&Math.abs(z-c.z)<=c.d/2+.45)return true;}return false;}`
- [x] `for` — linha 1459, coluna 133 em `navBlocked()` — `function navBlocked(x,z){for(const h of world.hazards||[]){if(Math.abs(x-h.x)<=h.w/2+.55&&Math.abs(z-h.z)<=h.d/2+.55)return true;}for(const c of world.colliders||[]){if(c.houseId&&currentHouse&&c.houseId===currentHouse.id)continue;if(Math.abs(x-c.x)<=c.w/2+.45&&Math.abs(z-c.z)<=c.d/2+.45)return true;}return false;}`
- [x] `if` — linha 1459, coluna 169 em `navBlocked()` — `function navBlocked(x,z){for(const h of world.hazards||[]){if(Math.abs(x-h.x)<=h.w/2+.55&&Math.abs(z-h.z)<=h.d/2+.55)return true;}for(const c of world.colliders||[]){if(c.houseId&&currentHouse&&c.houseId===currentHouse.id)continue;if(Math.abs(x-c.x)<=c.w/2+.45&&Math.abs(z-c.z)<=c.d/2+.45)return true;}return false;}`
- [x] `if` — linha 1459, coluna 234 em `navBlocked()` — `function navBlocked(x,z){for(const h of world.hazards||[]){if(Math.abs(x-h.x)<=h.w/2+.55&&Math.abs(z-h.z)<=h.d/2+.55)return true;}for(const c of world.colliders||[]){if(c.houseId&&currentHouse&&c.houseId===currentHouse.id)continue;if(Math.abs(x-c.x)<=c.w/2+.45&&Math.abs(z-c.z)<=c.d/2+.45)return true;}return false;}`
- [x] `return` — linha 1459, coluna 292 em `navBlocked()` — `function navBlocked(x,z){for(const h of world.hazards||[]){if(Math.abs(x-h.x)<=h.w/2+.55&&Math.abs(z-h.z)<=h.d/2+.55)return true;}for(const c of world.colliders||[]){if(c.houseId&&currentHouse&&c.houseId===currentHouse.id)continue;if(Math.abs(x-c.x)<=c.w/2+.45&&Math.abs(z-c.z)<=c.d/2+.45)return true;}return false;}`
- [x] `return` — linha 1459, coluna 305 em `navBlocked()` — `function navBlocked(x,z){for(const h of world.hazards||[]){if(Math.abs(x-h.x)<=h.w/2+.55&&Math.abs(z-h.z)<=h.d/2+.55)return true;}for(const c of world.colliders||[]){if(c.houseId&&currentHouse&&c.houseId===currentHouse.id)continue;if(Math.abs(x-c.x)<=c.w/2+.45&&Math.abs(z-c.z)<=c.d/2+.45)return true;}return false;}`
- [x] `for` — linha 1460, coluna 105 em `segmentClear()` — `function segmentClear(a,b){const len=Math.hypot(b.x-a.x,b.z-a.z),steps=Math.max(1,Math.ceil(len/1.8));for(let i=1;i<steps;i++){const t=i/steps;if(navBlocked(a.x+(b.x-a.x)*t,a.z+(b.z-a.z)*t))return false;}return true;}`
- [x] `if` — linha 1460, coluna 146 em `segmentClear()` — `function segmentClear(a,b){const len=Math.hypot(b.x-a.x,b.z-a.z),steps=Math.max(1,Math.ceil(len/1.8));for(let i=1;i<steps;i++){const t=i/steps;if(navBlocked(a.x+(b.x-a.x)*t,a.z+(b.z-a.z)*t))return false;}return true;}`
- [x] `return` — linha 1460, coluna 193 em `segmentClear()` — `function segmentClear(a,b){const len=Math.hypot(b.x-a.x,b.z-a.z),steps=Math.max(1,Math.ceil(len/1.8));for(let i=1;i<steps;i++){const t=i/steps;if(navBlocked(a.x+(b.x-a.x)*t,a.z+(b.z-a.z)*t))return false;}return true;}`
- [x] `return` — linha 1460, coluna 207 em `segmentClear()` — `function segmentClear(a,b){const len=Math.hypot(b.x-a.x,b.z-a.z),steps=Math.max(1,Math.ceil(len/1.8));for(let i=1;i<steps;i++){const t=i/steps;if(navBlocked(a.x+(b.x-a.x)*t,a.z+(b.z-a.z)*t))return false;}return true;}`
- [x] `for` — linha 1461, coluna 53 em `nearestRoadProjection()` — `function nearestRoadProjection(pos){let best=null;for(const [aId,bId] of NAV_BASE_EDGES){const a=NAV_BASE_NODES[aId],b=NAV_BASE_NODES[bId],p=projectPointToSegment(pos,a,b),distance=Math.hypot(pos.x-p.x,pos.z-p.z),clear=segmentClear(pos,p);const score=distance+(clear?0:120);if(!best||score<best.score)best={aId,bId,point:p,distance,clear,score};}return best;}`
- [x] `if` — linha 1461, coluna 277 em `nearestRoadProjection()` — `function nearestRoadProjection(pos){let best=null;for(const [aId,bId] of NAV_BASE_EDGES){const a=NAV_BASE_NODES[aId],b=NAV_BASE_NODES[bId],p=projectPointToSegment(pos,a,b),distance=Math.hypot(pos.x-p.x,pos.z-p.z),clear=segmentClear(pos,p);const score=distance+(clear?0:120);if(!best||score<best.score)best={aId,bId,point:p,distance,clear,score};}return best;}`
- [x] `return` — linha 1461, coluna 349 em `nearestRoadProjection()` — `function nearestRoadProjection(pos){let best=null;for(const [aId,bId] of NAV_BASE_EDGES){const a=NAV_BASE_NODES[aId],b=NAV_BASE_NODES[bId],p=projectPointToSegment(pos,a,b),distance=Math.hypot(pos.x-p.x,pos.z-p.z),clear=segmentClear(pos,p);const score=distance+(clear?0:120);if(!best||score<best.score)best={aId,bId,point:p,distance,clear,score};}return best;}`
- [x] `return` — linha 1462, coluna 39 em `pointOnRoad()` — `function pointOnRoad(x,z,margin=.7){return WORLD_MAP_ROADS.some(r=>Math.abs(x-r.x)<=r.w/2+margin&&Math.abs(z-r.z)<=r.d/2+margin);}`
- [x] `if` — linha 1464, coluna 5 em `projectPointToPolyline()` — `if(!points?.length)return null;let best=null;`
- [x] `return` — linha 1464, coluna 24 em `projectPointToPolyline()` — `if(!points?.length)return null;let best=null;`
- [x] `for` — linha 1465, coluna 5 em `projectPointToPolyline()` — `for(let i=0;i<points.length;i++){const a=points[i],b=points[(i+1)%points.length];if(!a||!b)continue;const point=projectPointToSegment(pos,a,b),distance=Math.hypot(pos.x-point.x,pos.z-point.z);if(!best||distance<best.distance)best={point,distance,index:i,nextIndex:(i+1)%points.length};}`
- [x] `if` — linha 1465, coluna 86 em `projectPointToPolyline()` — `for(let i=0;i<points.length;i++){const a=points[i],b=points[(i+1)%points.length];if(!a||!b)continue;const point=projectPointToSegment(pos,a,b),distance=Math.hypot(pos.x-point.x,pos.z-point.z);if(!best||distance<best.distance)best={point,distance,index:i,nextIndex:(i+1)%points.length};}`
- [x] `if` — linha 1465, coluna 197 em `projectPointToPolyline()` — `for(let i=0;i<points.length;i++){const a=points[i],b=points[(i+1)%points.length];if(!a||!b)continue;const point=projectPointToSegment(pos,a,b),distance=Math.hypot(pos.x-point.x,pos.z-point.z);if(!best||distance<best.distance)best={point,distance,index:i,nextIndex:(i+1)%points.length};}`
- [x] `return` — linha 1466, coluna 5 em `projectPointToPolyline()` — `return best;`
- [x] `if` — linha 1469, coluna 5 em `snapTrafficToRoad()` — `if(!group||pointOnRoad(group.position.x,group.position.z,.15))return true;`
- [x] `return` — linha 1469, coluna 67 em `snapTrafficToRoad()` — `if(!group||pointOnRoad(group.position.x,group.position.z,.15))return true;`
- [x] `if` — linha 1471, coluna 5 em `snapTrafficToRoad()` — `if(projection?.point&&pointOnRoad(projection.point.x,projection.point.z,.05)){group.position.x=projection.point.x;group.position.z=projection.point.z;return true;}`
- [x] `return` — linha 1471, coluna 155 em `snapTrafficToRoad()` — `if(projection?.point&&pointOnRoad(projection.point.x,projection.point.z,.05)){group.position.x=projection.point.x;group.position.z=projection.point.z;return true;}`
- [x] `if` — linha 1472, coluna 5 em `snapTrafficToRoad()` — `if(previous){group.position.x=previous.x;group.position.z=previous.z;}return false;`
- [x] `return` — linha 1472, coluna 75 em `snapTrafficToRoad()` — `if(previous){group.position.x=previous.x;group.position.z=previous.z;}return false;`
- [x] `if` — linha 1476, coluna 5 em `trafficActorList()` — `if(world.trafficSnapshot&&now-(world.trafficSnapshotAt||0)<12)return world.trafficSnapshot;`
- [x] `return` — linha 1476, coluna 67 em `trafficActorList()` — `if(world.trafficSnapshot&&now-(world.trafficSnapshotAt||0)<12)return world.trafficSnapshot;`
- [x] `for` — linha 1478, coluna 5 em `trafficActorList()` — `for(const bus of world.buses||[])if(bus.group?.visible)actors.push({id:\`bus-${bus.id}\`,type:'bus',group:bus.group,radius:3.05,speed:Math.abs(bus.currentSpeed||0),ref:bus});`
- [x] `if` — linha 1478, coluna 38 em `trafficActorList()` — `for(const bus of world.buses||[])if(bus.group?.visible)actors.push({id:\`bus-${bus.id}\`,type:'bus',group:bus.group,radius:3.05,speed:Math.abs(bus.currentSpeed||0),ref:bus});`
- [x] `for` — linha 1479, coluna 5 em `trafficActorList()` — `for(const car of world.policeCars||[])if(car.group?.visible)actors.push({id:\`police-${car.id}\`,type:'police',group:car.group,radius:1.8,speed:Math.abs(car.currentSpeed||car.speed||0),ref:car});`
- [x] `if` — linha 1479, coluna 43 em `trafficActorList()` — `for(const car of world.policeCars||[])if(car.group?.visible)actors.push({id:\`police-${car.id}\`,type:'police',group:car.group,radius:1.8,speed:Math.abs(car.currentSpeed||car.speed||0),ref:car});`
- [x] `for` — linha 1480, coluna 5 em `trafficActorList()` — `for(const truck of world.fireTrucks||[])if(truck.group?.visible)actors.push({id:\`fire-${truck.id}\`,type:'fire',group:truck.group,radius:2.5,speed:Math.abs(truck.currentSpeed||truck.speed||0),ref:truck});`
- [x] `if` — linha 1480, coluna 45 em `trafficActorList()` — `for(const truck of world.fireTrucks||[])if(truck.group?.visible)actors.push({id:\`fire-${truck.id}\`,type:'fire',group:truck.group,radius:2.5,speed:Math.abs(truck.currentSpeed||truck.speed||0),ref:truck});`
- [x] `for` — linha 1481, coluna 5 em `trafficActorList()` — `for(const ambulance of world.ambulances||[])if(ambulance.group?.visible)actors.push({id:\`ambulance-${ambulance.id}\`,type:'ambulance',group:ambulance.group,radius:2.1,speed:Math.abs(ambulance.currentSpeed||ambulance.speed||0),ref:ambulance});`
- [x] `if` — linha 1481, coluna 49 em `trafficActorList()` — `for(const ambulance of world.ambulances||[])if(ambulance.group?.visible)actors.push({id:\`ambulance-${ambulance.id}\`,type:'ambulance',group:ambulance.group,radius:2.1,speed:Math.abs(ambulance.currentSpeed||ambulance.speed||0),ref:ambulance});`
- [x] `for` — linha 1482, coluna 5 em `trafficActorList()` — `for(const npc of world.npcs||[])if(npc.mobility?.group?.visible&&npc.mobility.type!=='walk')actors.push({id:\`npc-${npc.id}\`,type:npc.mobility.type,group:npc.mobility.group,radius:npc.mobility.radius||1.25,speed:Math.abs(npc.mobility.currentSpeed||npc.mobility.speed||0),ref:npc.mobility});`
- [x] `if` — linha 1482, coluna 37 em `trafficActorList()` — `for(const npc of world.npcs||[])if(npc.mobility?.group?.visible&&npc.mobility.type!=='walk')actors.push({id:\`npc-${npc.id}\`,type:npc.mobility.type,group:npc.mobility.group,radius:npc.mobility.radius||1.25,speed:Math.abs(npc.mobility.currentSpeed||npc.mobility.speed||0),ref:npc.mobility});`
- [x] `return` — linha 1483, coluna 62 em `trafficActorList()` — `world.trafficSnapshot=actors;world.trafficSnapshotAt=now;return actors;`
- [x] `if` — linha 1486, coluna 35 em `trafficPriority()` — `function trafficPriority(actor){if(actor?.incidentTargetId||actor?.targetFireId)return 50;let type=actor?.route?.schoolBus?'school':actor?.route?'bus':actor?.type||actor?.kind||actor?.trafficType||'';if(!type&&world.ambulances?.includes(actor))type='ambulance';else if(!type&&world.fireTrucks?.includes(actor))type='fire';else if(!type&&world.policeCars?.includes(actor))type='police';return({ambulance:45,fire:44,police:43,school:28,bus:22,car:16,moto:14,bike:10,skate:8})[type]||18;}`
- [x] `return` — linha 1486, coluna 83 em `trafficPriority()` — `function trafficPriority(actor){if(actor?.incidentTargetId||actor?.targetFireId)return 50;let type=actor?.route?.schoolBus?'school':actor?.route?'bus':actor?.type||actor?.kind||actor?.trafficType||'';if(!type&&world.ambulances?.includes(actor))type='ambulance';else if(!type&&world.fireTrucks?.includes(actor))type='fire';else if(!type&&world.policeCars?.includes(actor))type='police';return({ambulance:45,fire:44,police:43,school:28,bus:22,car:16,moto:14,bike:10,skate:8})[type]||18;}`
- [x] `if` — linha 1486, coluna 203 em `trafficPriority()` — `function trafficPriority(actor){if(actor?.incidentTargetId||actor?.targetFireId)return 50;let type=actor?.route?.schoolBus?'school':actor?.route?'bus':actor?.type||actor?.kind||actor?.trafficType||'';if(!type&&world.ambulances?.includes(actor))type='ambulance';else if(!type&&world.fireTrucks?.includes(actor))type='fire';else if(!type&&world.policeCars?.includes(actor))type='police';return({ambulance:45,fire:44,police:43,school:28,bus:22,car:16,moto:14,bike:10,skate:8})[type]||18;}`
- [x] `else` — linha 1486, coluna 264 em `trafficPriority()` — `function trafficPriority(actor){if(actor?.incidentTargetId||actor?.targetFireId)return 50;let type=actor?.route?.schoolBus?'school':actor?.route?'bus':actor?.type||actor?.kind||actor?.trafficType||'';if(!type&&world.ambulances?.includes(actor))type='ambulance';else if(!type&&world.fireTrucks?.includes(actor))type='fire';else if(!type&&world.policeCars?.includes(actor))type='police';return({ambulance:45,fire:44,police:43,school:28,bus:22,car:16,moto:14,bike:10,skate:8})[type]||18;}`
- [x] `if` — linha 1486, coluna 269 em `trafficPriority()` — `function trafficPriority(actor){if(actor?.incidentTargetId||actor?.targetFireId)return 50;let type=actor?.route?.schoolBus?'school':actor?.route?'bus':actor?.type||actor?.kind||actor?.trafficType||'';if(!type&&world.ambulances?.includes(actor))type='ambulance';else if(!type&&world.fireTrucks?.includes(actor))type='fire';else if(!type&&world.policeCars?.includes(actor))type='police';return({ambulance:45,fire:44,police:43,school:28,bus:22,car:16,moto:14,bike:10,skate:8})[type]||18;}`
- [x] `else` — linha 1486, coluna 325 em `trafficPriority()` — `function trafficPriority(actor){if(actor?.incidentTargetId||actor?.targetFireId)return 50;let type=actor?.route?.schoolBus?'school':actor?.route?'bus':actor?.type||actor?.kind||actor?.trafficType||'';if(!type&&world.ambulances?.includes(actor))type='ambulance';else if(!type&&world.fireTrucks?.includes(actor))type='fire';else if(!type&&world.policeCars?.includes(actor))type='police';return({ambulance:45,fire:44,police:43,school:28,bus:22,car:16,moto:14,bike:10,skate:8})[type]||18;}`
- [x] `if` — linha 1486, coluna 330 em `trafficPriority()` — `function trafficPriority(actor){if(actor?.incidentTargetId||actor?.targetFireId)return 50;let type=actor?.route?.schoolBus?'school':actor?.route?'bus':actor?.type||actor?.kind||actor?.trafficType||'';if(!type&&world.ambulances?.includes(actor))type='ambulance';else if(!type&&world.fireTrucks?.includes(actor))type='fire';else if(!type&&world.policeCars?.includes(actor))type='police';return({ambulance:45,fire:44,police:43,school:28,bus:22,car:16,moto:14,bike:10,skate:8})[type]||18;}`
- [x] `return` — linha 1486, coluna 388 em `trafficPriority()` — `function trafficPriority(actor){if(actor?.incidentTargetId||actor?.targetFireId)return 50;let type=actor?.route?.schoolBus?'school':actor?.route?'bus':actor?.type||actor?.kind||actor?.trafficType||'';if(!type&&world.ambulances?.includes(actor))type='ambulance';else if(!type&&world.fireTrucks?.includes(actor))type='fire';else if(!type&&world.policeCars?.includes(actor))type='police';return({ambulance:45,fire:44,police:43,school:28,bus:22,car:16,moto:14,bike:10,skate:8})[type]||18;}`
- [x] `if` — linha 1488, coluna 5 em `trafficSpeedFactor()` — `if(!actor?.group)return 1;const now=performance.now();if(actor.incidentLocked||now<Number(actor.incidentUntil||0)||now<Number(actor.trafficHoldUntil||0))return 0;`
- [x] `return` — linha 1488, coluna 22 em `trafficSpeedFactor()` — `if(!actor?.group)return 1;const now=performance.now();if(actor.incidentLocked||now<Number(actor.incidentUntil||0)||now<Number(actor.trafficHoldUntil||0))return 0;`
- [x] `if` — linha 1488, coluna 59 em `trafficSpeedFactor()` — `if(!actor?.group)return 1;const now=performance.now();if(actor.incidentLocked||now<Number(actor.incidentUntil||0)||now<Number(actor.trafficHoldUntil||0))return 0;`
- [x] `return` — linha 1488, coluna 158 em `trafficSpeedFactor()` — `if(!actor?.group)return 1;const now=performance.now();if(actor.incidentLocked||now<Number(actor.incidentUntil||0)||now<Number(actor.trafficHoldUntil||0))return 0;`
- [x] `for` — linha 1490, coluna 5 em `trafficSpeedFactor()` — `for(const other of trafficActorList()){if(other.ref===actor||other.id===actor.id)continue;const dx=other.group.position.x-ax,dz=other.group.position.z-az,forward=dx*fx+dz*fz,side=Math.abs(dx*rx+dz*rz),gap=actorRadius+Number(other.radius||1.5)+.75;`
- [x] `if` — linha 1490, coluna 44 em `trafficSpeedFactor()` — `for(const other of trafficActorList()){if(other.ref===actor||other.id===actor.id)continue;const dx=other.group.position.x-ax,dz=other.group.position.z-az,forward=dx*fx+dz*fz,side=Math.abs(dx*rx+dz*rz),gap=actorRadius+Number(other.radius||1.5)+.75;`
- [x] `if` — linha 1491, coluna 7 em `trafficSpeedFactor()` — `if(forward>-.15&&forward<lookAhead+gap&&side<gap*.9){const clearance=forward-gap;if(other.ref?.incidentLocked||other.ref?.incidentUntil===Number.MAX_SAFE_INTEGER||clearance<=.22)factor=0;else factor=Math.min(factor,clamp(clearance/Math.max(1.2,lookAhead*.72),0,1));}`
- [x] `if` — linha 1491, coluna 88 em `trafficSpeedFactor()` — `if(forward>-.15&&forward<lookAhead+gap&&side<gap*.9){const clearance=forward-gap;if(other.ref?.incidentLocked||other.ref?.incidentUntil===Number.MAX_SAFE_INTEGER||clearance<=.22)factor=0;else factor=Math.min(factor,clamp(clearance/Math.max(1.2,lookAhead*.72),0,1));}`
- [x] `else` — linha 1491, coluna 194 em `trafficSpeedFactor()` — `if(forward>-.15&&forward<lookAhead+gap&&side<gap*.9){const clearance=forward-gap;if(other.ref?.incidentLocked||other.ref?.incidentUntil===Number.MAX_SAFE_INTEGER||clearance<=.22)factor=0;else factor=Math.min(factor,clamp(clearance/Math.max(1.2,lookAhead*.72),0,1));}`
- [x] `if` — linha 1493, coluna 7 em `trafficSpeedFactor()` — `if(futureGap<gap*1.28&&Math.hypot(dx,dz)<lookAhead+gap+5){const myPriority=trafficPriority(actor),otherPriority=trafficPriority(other);if(myPriority<=otherPriority)factor=Math.min(factor,clamp((futureGap-gap*.82)/(gap*.65),0,.48));}`
- [x] `if` — linha 1493, coluna 142 em `trafficSpeedFactor()` — `if(futureGap<gap*1.28&&Math.hypot(dx,dz)<lookAhead+gap+5){const myPriority=trafficPriority(actor),otherPriority=trafficPriority(other);if(myPriority<=otherPriority)factor=Math.min(factor,clamp((futureGap-gap*.82)/(gap*.65),0,.48));}`
- [x] `return` — linha 1494, coluna 6 em `trafficSpeedFactor()` — `}return clamp(factor,0,1);`
- [x] `for` — linha 1497, coluna 88 em `captureTrafficPositions()` — `function captureTrafficPositions(){const before=new Map();world.trafficSnapshot=null;for(const actor of trafficActorList())before.set(actor.id,{x:actor.group.position.x,z:actor.group.position.z,heading:Number(actor.group.rotation?.y||0)});return before;}`
- [x] `return` — linha 1497, coluna 242 em `captureTrafficPositions()` — `function captureTrafficPositions(){const before=new Map();world.trafficSnapshot=null;for(const actor of trafficActorList())before.set(actor.id,{x:actor.group.position.x,z:actor.group.position.z,heading:Number(actor.group.rotation?.y||0)});return before;}`
- [x] `for` — linha 1499, coluna 64 em `resolveTrafficOverlaps()` — `world.trafficSnapshot=null;const actors=trafficActorList();for(const actor of actors)if(!pointOnRoad(actor.group.position.x,actor.group.position.z,.5))snapTrafficToRoad(actor.group,before?.get(actor.id));`
- [x] `if` — linha 1499, coluna 90 em `resolveTrafficOverlaps()` — `world.trafficSnapshot=null;const actors=trafficActorList();for(const actor of actors)if(!pointOnRoad(actor.group.position.x,actor.group.position.z,.5))snapTrafficToRoad(actor.group,before?.get(actor.id));`
- [x] `for` — linha 1500, coluna 5 em `resolveTrafficOverlaps()` — `for(let pass=0;pass<2;pass++)for(let i=0;i<actors.length;i++)for(let j=i+1;j<actors.length;j++){`
- [x] `for` — linha 1500, coluna 34 em `resolveTrafficOverlaps()` — `for(let pass=0;pass<2;pass++)for(let i=0;i<actors.length;i++)for(let j=i+1;j<actors.length;j++){`
- [x] `for` — linha 1500, coluna 66 em `resolveTrafficOverlaps()` — `for(let pass=0;pass<2;pass++)for(let i=0;i<actors.length;i++)for(let j=i+1;j<actors.length;j++){`
- [x] `if` — linha 1501, coluna 171 em `resolveTrafficOverlaps()` — `const a=actors[i],b=actors[j],dx=b.group.position.x-a.group.position.x,dz=b.group.position.z-a.group.position.z,d=Math.hypot(dx,dz),gap=(a.radius+b.radius)*.94+.25;if(d>=gap)continue;if(a.ref?.incidentLocked&&b.ref?.incidentLocked)continue;`
- [x] `if` — linha 1501, coluna 190 em `resolveTrafficOverlaps()` — `const a=actors[i],b=actors[j],dx=b.group.position.x-a.group.position.x,dz=b.group.position.z-a.group.position.z,d=Math.hypot(dx,dz),gap=(a.radius+b.radius)*.94+.25;if(d>=gap)continue;if(a.ref?.incidentLocked&&b.ref?.incidentLocked)continue;`
- [x] `if` — linha 1502, coluna 148 em `resolveTrafficOverlaps()` — `const aEmergency=!!a.ref?.incidentTargetId||!!a.ref?.targetFireId,bEmergency=!!b.ref?.incidentTargetId||!!b.ref?.targetFireId;let yieldActor;if(aEmergency!==bEmergency)yieldActor=aEmergency?b:a;else{const pa=trafficPriority(a),pb=trafficPriority(b);yieldActor=pa===pb?(a.id>b.id?a:b):(pa<pb?a:b);}const other=yieldActor===a?b:a,old=before?.get(yieldActor.id),otherOld=before?.get(other.id);`
- [x] `else` — linha 1502, coluna 201 em `resolveTrafficOverlaps()` — `const aEmergency=!!a.ref?.incidentTargetId||!!a.ref?.targetFireId,bEmergency=!!b.ref?.incidentTargetId||!!b.ref?.targetFireId;let yieldActor;if(aEmergency!==bEmergency)yieldActor=aEmergency?b:a;else{const pa=trafficPriority(a),pb=trafficPriority(b);yieldActor=pa===pb?(a.id>b.id?a:b):(pa<pb?a:b);}const other=yieldActor===a?b:a,old=before?.get(yieldActor.id),otherOld=before?.get(other.id);`
- [x] `if` — linha 1503, coluna 7 em `resolveTrafficOverlaps()` — `if(old){yieldActor.group.position.x=old.x;yieldActor.group.position.z=old.z;snapTrafficToRoad(yieldActor.group,old);}yieldActor.ref.currentSpeed=0;yieldActor.ref.trafficHoldUntil=performance.now()+950;`
- [x] `if` — linha 1504, coluna 106 em `resolveTrafficOverlaps()` — `let remain=Math.hypot(b.group.position.x-a.group.position.x,b.group.position.z-a.group.position.z);if(remain<gap*.9&&otherOld&&!other.ref?.incidentLocked){other.group.position.x=otherOld.x;other.group.position.z=otherOld.z;snapTrafficToRoad(other.group,otherOld);other.ref.currentSpeed=0;other.ref.trafficHoldUntil=performance.now()+620;remain=Math.hypot(b.group.position.x-a.group.position.x,b.group.position.z-a.group.position.z);}`
- [x] `if` — linha 1505, coluna 7 em `resolveTrafficOverlaps()` — `if(remain<gap*.82){const nx=(b.group.position.x-a.group.position.x)/(remain||1),nz=(b.group.position.z-a.group.position.z)/(remain||1),push=(gap-remain)*.56;yieldActor.group.position.x-=nx*push;yieldActor.group.position.z-=nz*push;snapTrafficToRoad(yieldActor.group,old);}`
- [x] `if` — linha 1509, coluna 32 em `graphAdd()` — `function graphAdd(adj,a,b,w){if(!adj.has(a))adj.set(a,[]);if(!adj.has(b))adj.set(b,[]);adj.get(a).push({id:b,w});adj.get(b).push({id:a,w});}`
- [x] `if` — linha 1509, coluna 61 em `graphAdd()` — `function graphAdd(adj,a,b,w){if(!adj.has(a))adj.set(a,[]);if(!adj.has(b))adj.set(b,[]);adj.get(a).push({id:b,w});adj.get(b).push({id:a,w});}`
- [x] `while` — linha 1510, coluna 127 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `for` — linha 1510, coluna 175 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `if` — linha 1510, coluna 228 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `if` — linha 1510, coluna 259 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `for` — linha 1510, coluna 321 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `if` — linha 1510, coluna 358 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `if` — linha 1510, coluna 404 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `if` — linha 1510, coluna 482 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `return` — linha 1510, coluna 502 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `while` — linha 1510, coluna 537 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `if` — linha 1510, coluna 560 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `return` — linha 1510, coluna 599 em `graphShortest()` — `function graphShortest(nodes,adj,startId,endId){const dist=new Map([[startId,0]]),prev=new Map(),open=new Set(nodes.keys());while(open.size){let current=null,best=Infinity;for(const id of open){const d=dist.get(id)??Infinity;if(d<best){best=d;current=id;}}if(current===null||current===endId)break;open.delete(current);for(const e of adj.get(current)||[]){if(!open.has(e.id))continue;const nd=best+e.w;if(nd<(dist.get(e.id)??Infinity)){dist.set(e.id,nd);prev.set(e.id,current);}}}if(!dist.has(endId))return[];const ids=[];let id=endId;while(id){ids.push(id);if(id===startId)break;id=prev.get(id);}return ids.reverse().map(id=>nodes.get(id));}`
- [x] `if` — linha 1513, coluna 5 em `buildRoutePoints()` — `if(!startProjection||!targetProjection)return compactRoute([from,target]);`
- [x] `return` — linha 1513, coluna 44 em `buildRoutePoints()` — `if(!startProjection||!targetProjection)return compactRoute([from,target]);`
- [x] `if` — linha 1515, coluna 47 em `buildRoutePoints()` — `const cached=world.navCache.get(cacheKey);if(cached)return compactRoute([{x:from.x,z:from.z},...cached.slice(1,-1),target]);`
- [x] `return` — linha 1515, coluna 57 em `buildRoutePoints()` — `const cached=world.navCache.get(cacheKey);if(cached)return compactRoute([{x:from.x,z:from.z},...cached.slice(1,-1),target]);`
- [x] `for` — linha 1517, coluna 5 em `buildRoutePoints()` — `for(const[aId,bId]of NAV_BASE_EDGES){const a=nodes.get(aId),b=nodes.get(bId);graphAdd(adj,aId,bId,Math.hypot(a.x-b.x,a.z-b.z));}`
- [x] `for` — linha 1519, coluna 5 em `buildRoutePoints()` — `for(const [id,projection] of [['START',startProjection],['TARGET',targetProjection]]){const a=nodes.get(projection.aId),b=nodes.get(projection.bId),p=nodes.get(id);graphAdd(adj,id,projection.aId,Math.hypot(p.x-a.x,p.z-a.z));graphAdd(adj,id,projection.bId,Math.hypot(p.x-b.x,p.z-b.z));}`
- [x] `if` — linha 1520, coluna 5 em `buildRoutePoints()` — `if(startProjection.aId===targetProjection.aId&&startProjection.bId===targetProjection.bId)graphAdd(adj,'START','TARGET',Math.hypot(startProjection.point.x-targetProjection.point.x,startProjection.point.z-targetProjection.point.z));`
- [x] `if` — linha 1521, coluna 150 em `buildRoutePoints()` — `const core=graphShortest(nodes,adj,'START','TARGET'),route=compactRoute([{x:from.x,z:from.z},...core,target]);world.navCache.set(cacheKey,route);if(world.navCache.size>60)world.navCache.delete(world.navCache.keys().next().value);return route;`
- [x] `return` — linha 1521, coluna 234 em `buildRoutePoints()` — `const core=graphShortest(nodes,adj,'START','TARGET'),route=compactRoute([{x:from.x,z:from.z},...core,target]);world.navCache.set(cacheKey,route);if(world.navCache.size>60)world.navCache.delete(world.navCache.keys().next().value);return route;`
- [x] `if` — linha 1523, coluna 41 em `routeProgressInfo()` — `function routeProgressInfo(route,pos){if(!route?.length)return{remaining:0,distance:Infinity,index:0,point:pos,next:pos,instruction:'sem rota'};let total=routeLength(route),before=0,best={distance:Infinity,index:0,t:0,point:route[0],along:0};for(let i=1;i<route.length;i++){const a=route[i-1],b=route[i],p=projectPointToSegment(pos,a,b),d=Math.hypot(pos.x-p.x,pos.z-p.z),seg=Math.hypot(b.x-a.x,b.z-a.z);if(d<best.distance)best={distance:d,index:i-1,t:p.t,point:p,along:before+seg*p.t};before+=seg;}const next=route[Math.min(route.length-1,best.index+1)]||route.at(-1),after=route[Math.min(route.length-1,best.index+2)]||next;const heading=Math.atan2(next.x-best.point.x,next.z-best.point.z),nextHeading=Math.atan2(after.x-next.x,after.z-next.z);let delta=((nextHeading-heading+Math.PI*3)%(Math.PI*2))-Math.PI;const turnDistance=Math.hypot(next.x-best.point.x,next.z-best.point.z);let instruction=turnDistance<4&&after!==next?(delta<-.35?'vire à direita':delta>.35?'vire à esquerda':'siga em frente'):`
- [x] `return` — linha 1523, coluna 59 em `routeProgressInfo()` — `function routeProgressInfo(route,pos){if(!route?.length)return{remaining:0,distance:Infinity,index:0,point:pos,next:pos,instruction:'sem rota'};let total=routeLength(route),before=0,best={distance:Infinity,index:0,t:0,point:route[0],along:0};for(let i=1;i<route.length;i++){const a=route[i-1],b=route[i],p=projectPointToSegment(pos,a,b),d=Math.hypot(pos.x-p.x,pos.z-p.z),seg=Math.hypot(b.x-a.x,b.z-a.z);if(d<best.distance)best={distance:d,index:i-1,t:p.t,point:p,along:before+seg*p.t};before+=seg;}const next=route[Math.min(route.length-1,best.index+1)]||route.at(-1),after=route[Math.min(route.length-1,best.index+2)]||next;const heading=Math.atan2(next.x-best.point.x,next.z-best.point.z),nextHeading=Math.atan2(after.x-next.x,after.z-next.z);let delta=((nextHeading-heading+Math.PI*3)%(Math.PI*2))-Math.PI;const turnDistance=Math.hypot(next.x-best.point.x,next.z-best.point.z);let instruction=turnDistance<4&&after!==next?(delta<-.35?'vire à direita':delta>.35?'vire à esquerda':'siga em frente'):`
- [x] `for` — linha 1523, coluna 245 em `routeProgressInfo()` — `function routeProgressInfo(route,pos){if(!route?.length)return{remaining:0,distance:Infinity,index:0,point:pos,next:pos,instruction:'sem rota'};let total=routeLength(route),before=0,best={distance:Infinity,index:0,t:0,point:route[0],along:0};for(let i=1;i<route.length;i++){const a=route[i-1],b=route[i],p=projectPointToSegment(pos,a,b),d=Math.hypot(pos.x-p.x,pos.z-p.z),seg=Math.hypot(b.x-a.x,b.z-a.z);if(d<best.distance)best={distance:d,index:i-1,t:p.t,point:p,along:before+seg*p.t};before+=seg;}const next=route[Math.min(route.length-1,best.index+1)]||route.at(-1),after=route[Math.min(route.length-1,best.index+2)]||next;const heading=Math.atan2(next.x-best.point.x,next.z-best.point.z),nextHeading=Math.atan2(after.x-next.x,after.z-next.z);let delta=((nextHeading-heading+Math.PI*3)%(Math.PI*2))-Math.PI;const turnDistance=Math.hypot(next.x-best.point.x,next.z-best.point.z);let instruction=turnDistance<4&&after!==next?(delta<-.35?'vire à direita':delta>.35?'vire à esquerda':'siga em frente'):`
- [x] `if` — linha 1523, coluna 406 em `routeProgressInfo()` — `function routeProgressInfo(route,pos){if(!route?.length)return{remaining:0,distance:Infinity,index:0,point:pos,next:pos,instruction:'sem rota'};let total=routeLength(route),before=0,best={distance:Infinity,index:0,t:0,point:route[0],along:0};for(let i=1;i<route.length;i++){const a=route[i-1],b=route[i],p=projectPointToSegment(pos,a,b),d=Math.hypot(pos.x-p.x,pos.z-p.z),seg=Math.hypot(b.x-a.x,b.z-a.z);if(d<best.distance)best={distance:d,index:i-1,t:p.t,point:p,along:before+seg*p.t};before+=seg;}const next=route[Math.min(route.length-1,best.index+1)]||route.at(-1),after=route[Math.min(route.length-1,best.index+2)]||next;const heading=Math.atan2(next.x-best.point.x,next.z-best.point.z),nextHeading=Math.atan2(after.x-next.x,after.z-next.z);let delta=((nextHeading-heading+Math.PI*3)%(Math.PI*2))-Math.PI;const turnDistance=Math.hypot(next.x-best.point.x,next.z-best.point.z);let instruction=turnDistance<4&&after!==next?(delta<-.35?'vire à direita':delta>.35?'vire à esquerda':'siga em frente'):`
- [x] `return` — linha 1523, coluna 1086 em `routeProgressInfo()` — `function routeProgressInfo(route,pos){if(!route?.length)return{remaining:0,distance:Infinity,index:0,point:pos,next:pos,instruction:'sem rota'};let total=routeLength(route),before=0,best={distance:Infinity,index:0,t:0,point:route[0],along:0};for(let i=1;i<route.length;i++){const a=route[i-1],b=route[i],p=projectPointToSegment(pos,a,b),d=Math.hypot(pos.x-p.x,pos.z-p.z),seg=Math.hypot(b.x-a.x,b.z-a.z);if(d<best.distance)best={distance:d,index:i-1,t:p.t,point:p,along:before+seg*p.t};before+=seg;}const next=route[Math.min(route.length-1,best.index+1)]||route.at(-1),after=route[Math.min(route.length-1,best.index+2)]||next;const heading=Math.atan2(next.x-best.point.x,next.z-best.point.z),nextHeading=Math.atan2(after.x-next.x,after.z-next.z);let delta=((nextHeading-heading+Math.PI*3)%(Math.PI*2))-Math.PI;const turnDistance=Math.hypot(next.x-best.point.x,next.z-best.point.z);let instruction=turnDistance<4&&after!==next?(delta<-.35?'vire à direita':delta>.35?'vire à esquerda':'siga em frente'):`
- [x] `return` — linha 1524, coluna 78 em `remainingRoute()` — `function remainingRoute(route,pos){const info=routeProgressInfo(route,pos);return compactRoute([{x:pos.x,z:pos.z},info.point,...route.slice(info.index+1)]);}`
- [x] `for` — linha 1525, coluna 61 em `sampleRoute()` — `function sampleRoute(points,spacing=3.1){const samples=[];for(let i=1;i<points.length;i++){const a=points[i-1],b=points[i],dx=b.x-a.x,dz=b.z-a.z,len=Math.hypot(dx,dz),steps=Math.max(1,Math.floor(len/spacing));for(let s=1;s<=steps;s++){const t=s/steps;samples.push({x:a.x+dx*t,z:a.z+dz*t,angle:Math.atan2(dx,dz)});}}return samples;}`
- [x] `for` — linha 1525, coluna 212 em `sampleRoute()` — `function sampleRoute(points,spacing=3.1){const samples=[];for(let i=1;i<points.length;i++){const a=points[i-1],b=points[i],dx=b.x-a.x,dz=b.z-a.z,len=Math.hypot(dx,dz),steps=Math.max(1,Math.floor(len/spacing));for(let s=1;s<=steps;s++){const t=s/steps;samples.push({x:a.x+dx*t,z:a.z+dz*t,angle:Math.atan2(dx,dz)});}}return samples;}`
- [x] `return` — linha 1525, coluna 318 em `sampleRoute()` — `function sampleRoute(points,spacing=3.1){const samples=[];for(let i=1;i<points.length;i++){const a=points[i-1],b=points[i],dx=b.x-a.x,dz=b.z-a.z,len=Math.hypot(dx,dz),steps=Math.max(1,Math.floor(len/spacing));for(let s=1;s<=steps;s++){const t=s/steps;samples.push({x:a.x+dx*t,z:a.z+dz*t,angle:Math.atan2(dx,dz)});}}return samples;}`
- [x] `if` — linha 1526, coluna 31 em `createRouteGuide()` — `function createRouteGuide(){if(world.routeGuide)return true;if(!worldGroup||!window.THREE)return false;world.routeGuide=new THREE.Group();world.routeGuide.name='OTTHOS_ROUTE_GUIDE';worldGroup.add(world.routeGuide);const material=mat(0x42eaff,{emissive:0x087fa0,emissiveIntensity:1.45,roughness:.2,transparent:true,opacity:.94});for(let i=0;i<46;i++){const arrow=new THREE.Mesh(new THREE.ConeGeometry(.34,.8,4),material);arrow.rotation.x=Math.PI/2;arrow.visible=false;arrow.frustumCulled=false;world.routeGuide.add(arrow);world.routeArrows.push(arrow);}return true;}`
- [x] `return` — linha 1526, coluna 51 em `createRouteGuide()` — `function createRouteGuide(){if(world.routeGuide)return true;if(!worldGroup||!window.THREE)return false;world.routeGuide=new THREE.Group();world.routeGuide.name='OTTHOS_ROUTE_GUIDE';worldGroup.add(world.routeGuide);const material=mat(0x42eaff,{emissive:0x087fa0,emissiveIntensity:1.45,roughness:.2,transparent:true,opacity:.94});for(let i=0;i<46;i++){const arrow=new THREE.Mesh(new THREE.ConeGeometry(.34,.8,4),material);arrow.rotation.x=Math.PI/2;arrow.visible=false;arrow.frustumCulled=false;world.routeGuide.add(arrow);world.routeArrows.push(arrow);}return true;}`
- [x] `if` — linha 1526, coluna 63 em `createRouteGuide()` — `function createRouteGuide(){if(world.routeGuide)return true;if(!worldGroup||!window.THREE)return false;world.routeGuide=new THREE.Group();world.routeGuide.name='OTTHOS_ROUTE_GUIDE';worldGroup.add(world.routeGuide);const material=mat(0x42eaff,{emissive:0x087fa0,emissiveIntensity:1.45,roughness:.2,transparent:true,opacity:.94});for(let i=0;i<46;i++){const arrow=new THREE.Mesh(new THREE.ConeGeometry(.34,.8,4),material);arrow.rotation.x=Math.PI/2;arrow.visible=false;arrow.frustumCulled=false;world.routeGuide.add(arrow);world.routeArrows.push(arrow);}return true;}`
- [x] `return` — linha 1526, coluna 93 em `createRouteGuide()` — `function createRouteGuide(){if(world.routeGuide)return true;if(!worldGroup||!window.THREE)return false;world.routeGuide=new THREE.Group();world.routeGuide.name='OTTHOS_ROUTE_GUIDE';worldGroup.add(world.routeGuide);const material=mat(0x42eaff,{emissive:0x087fa0,emissiveIntensity:1.45,roughness:.2,transparent:true,opacity:.94});for(let i=0;i<46;i++){const arrow=new THREE.Mesh(new THREE.ConeGeometry(.34,.8,4),material);arrow.rotation.x=Math.PI/2;arrow.visible=false;arrow.frustumCulled=false;world.routeGuide.add(arrow);world.routeArrows.push(arrow);}return true;}`
- [x] `for` — linha 1526, coluna 331 em `createRouteGuide()` — `function createRouteGuide(){if(world.routeGuide)return true;if(!worldGroup||!window.THREE)return false;world.routeGuide=new THREE.Group();world.routeGuide.name='OTTHOS_ROUTE_GUIDE';worldGroup.add(world.routeGuide);const material=mat(0x42eaff,{emissive:0x087fa0,emissiveIntensity:1.45,roughness:.2,transparent:true,opacity:.94});for(let i=0;i<46;i++){const arrow=new THREE.Mesh(new THREE.ConeGeometry(.34,.8,4),material);arrow.rotation.x=Math.PI/2;arrow.visible=false;arrow.frustumCulled=false;world.routeGuide.add(arrow);world.routeArrows.push(arrow);}return true;}`
- [x] `return` — linha 1526, coluna 555 em `createRouteGuide()` — `function createRouteGuide(){if(world.routeGuide)return true;if(!worldGroup||!window.THREE)return false;world.routeGuide=new THREE.Group();world.routeGuide.name='OTTHOS_ROUTE_GUIDE';worldGroup.add(world.routeGuide);const material=mat(0x42eaff,{emissive:0x087fa0,emissiveIntensity:1.45,roughness:.2,transparent:true,opacity:.94});for(let i=0;i<46;i++){const arrow=new THREE.Mesh(new THREE.ConeGeometry(.34,.8,4),material);arrow.rotation.x=Math.PI/2;arrow.visible=false;arrow.frustumCulled=false;world.routeGuide.add(arrow);world.routeArrows.push(arrow);}return true;}`
- [x] `if` — linha 1527, coluna 42 em `updateRouteGuide()` — `function updateRouteGuide(force=false){if(!createRouteGuide())return;if(!state.waypoint){world.routeArrows.forEach(a=>a.visible=false);world.routePath=[];return;}const routeInfo=routeProgressInfo(world.routePath,player),offRoute=routeInfo.distance>7;if(force||!world.routePath.length||(offRoute&&performance.now()-world.routeLastBuild>1400)){world.routeLastBuild=performance.now();world.routePath=buildRoutePoints(player,state.waypoint);}const visibleRoute=remainingRoute(world.routePath,player),samples=sampleRoute(visibleRoute,3.15).filter((_,i)=>i>0).slice(0,world.routeArrows.length);world.routeArrows.forEach((arrow,i)=>{const p=samples[i];arrow.visible=!!p;if(!p)return;arrow.position.set(p.x,groundHeightAt(p.x,p.z)+.28,p.z);arrow.rotation.z=-p.angle;arrow.scale.setScalar(i<6?1.2:1);});}`
- [x] `return` — linha 1527, coluna 65 em `updateRouteGuide()` — `function updateRouteGuide(force=false){if(!createRouteGuide())return;if(!state.waypoint){world.routeArrows.forEach(a=>a.visible=false);world.routePath=[];return;}const routeInfo=routeProgressInfo(world.routePath,player),offRoute=routeInfo.distance>7;if(force||!world.routePath.length||(offRoute&&performance.now()-world.routeLastBuild>1400)){world.routeLastBuild=performance.now();world.routePath=buildRoutePoints(player,state.waypoint);}const visibleRoute=remainingRoute(world.routePath,player),samples=sampleRoute(visibleRoute,3.15).filter((_,i)=>i>0).slice(0,world.routeArrows.length);world.routeArrows.forEach((arrow,i)=>{const p=samples[i];arrow.visible=!!p;if(!p)return;arrow.position.set(p.x,groundHeightAt(p.x,p.z)+.28,p.z);arrow.rotation.z=-p.angle;arrow.scale.setScalar(i<6?1.2:1);});}`
- [x] `if` — linha 1527, coluna 72 em `updateRouteGuide()` — `function updateRouteGuide(force=false){if(!createRouteGuide())return;if(!state.waypoint){world.routeArrows.forEach(a=>a.visible=false);world.routePath=[];return;}const routeInfo=routeProgressInfo(world.routePath,player),offRoute=routeInfo.distance>7;if(force||!world.routePath.length||(offRoute&&performance.now()-world.routeLastBuild>1400)){world.routeLastBuild=performance.now();world.routePath=buildRoutePoints(player,state.waypoint);}const visibleRoute=remainingRoute(world.routePath,player),samples=sampleRoute(visibleRoute,3.15).filter((_,i)=>i>0).slice(0,world.routeArrows.length);world.routeArrows.forEach((arrow,i)=>{const p=samples[i];arrow.visible=!!p;if(!p)return;arrow.position.set(p.x,groundHeightAt(p.x,p.z)+.28,p.z);arrow.rotation.z=-p.angle;arrow.scale.setScalar(i<6?1.2:1);});}`
- [x] `return` — linha 1527, coluna 157 em `updateRouteGuide()` — `function updateRouteGuide(force=false){if(!createRouteGuide())return;if(!state.waypoint){world.routeArrows.forEach(a=>a.visible=false);world.routePath=[];return;}const routeInfo=routeProgressInfo(world.routePath,player),offRoute=routeInfo.distance>7;if(force||!world.routePath.length||(offRoute&&performance.now()-world.routeLastBuild>1400)){world.routeLastBuild=performance.now();world.routePath=buildRoutePoints(player,state.waypoint);}const visibleRoute=remainingRoute(world.routePath,player),samples=sampleRoute(visibleRoute,3.15).filter((_,i)=>i>0).slice(0,world.routeArrows.length);world.routeArrows.forEach((arrow,i)=>{const p=samples[i];arrow.visible=!!p;if(!p)return;arrow.position.set(p.x,groundHeightAt(p.x,p.z)+.28,p.z);arrow.rotation.z=-p.angle;arrow.scale.setScalar(i<6?1.2:1);});}`
- [x] `if` — linha 1527, coluna 253 em `updateRouteGuide()` — `function updateRouteGuide(force=false){if(!createRouteGuide())return;if(!state.waypoint){world.routeArrows.forEach(a=>a.visible=false);world.routePath=[];return;}const routeInfo=routeProgressInfo(world.routePath,player),offRoute=routeInfo.distance>7;if(force||!world.routePath.length||(offRoute&&performance.now()-world.routeLastBuild>1400)){world.routeLastBuild=performance.now();world.routePath=buildRoutePoints(player,state.waypoint);}const visibleRoute=remainingRoute(world.routePath,player),samples=sampleRoute(visibleRoute,3.15).filter((_,i)=>i>0).slice(0,world.routeArrows.length);world.routeArrows.forEach((arrow,i)=>{const p=samples[i];arrow.visible=!!p;if(!p)return;arrow.position.set(p.x,groundHeightAt(p.x,p.z)+.28,p.z);arrow.rotation.z=-p.angle;arrow.scale.setScalar(i<6?1.2:1);});}`
- [x] `if` — linha 1527, coluna 666 em `updateRouteGuide()` — `function updateRouteGuide(force=false){if(!createRouteGuide())return;if(!state.waypoint){world.routeArrows.forEach(a=>a.visible=false);world.routePath=[];return;}const routeInfo=routeProgressInfo(world.routePath,player),offRoute=routeInfo.distance>7;if(force||!world.routePath.length||(offRoute&&performance.now()-world.routeLastBuild>1400)){world.routeLastBuild=performance.now();world.routePath=buildRoutePoints(player,state.waypoint);}const visibleRoute=remainingRoute(world.routePath,player),samples=sampleRoute(visibleRoute,3.15).filter((_,i)=>i>0).slice(0,world.routeArrows.length);world.routeArrows.forEach((arrow,i)=>{const p=samples[i];arrow.visible=!!p;if(!p)return;arrow.position.set(p.x,groundHeightAt(p.x,p.z)+.28,p.z);arrow.rotation.z=-p.angle;arrow.scale.setScalar(i<6?1.2:1);});}`
- [x] `return` — linha 1527, coluna 672 em `updateRouteGuide()` — `function updateRouteGuide(force=false){if(!createRouteGuide())return;if(!state.waypoint){world.routeArrows.forEach(a=>a.visible=false);world.routePath=[];return;}const routeInfo=routeProgressInfo(world.routePath,player),offRoute=routeInfo.distance>7;if(force||!world.routePath.length||(offRoute&&performance.now()-world.routeLastBuild>1400)){world.routeLastBuild=performance.now();world.routePath=buildRoutePoints(player,state.waypoint);}const visibleRoute=remainingRoute(world.routePath,player),samples=sampleRoute(visibleRoute,3.15).filter((_,i)=>i>0).slice(0,world.routeArrows.length);world.routeArrows.forEach((arrow,i)=>{const p=samples[i];arrow.visible=!!p;if(!p)return;arrow.position.set(p.x,groundHeightAt(p.x,p.z)+.28,p.z);arrow.rotation.z=-p.angle;arrow.scale.setScalar(i<6?1.2:1);});}`
- [x] `return` — linha 1528, coluna 37 em `miniPoint()` — `function miniPoint(x,z,scale,w,h){return{x:w/2+(x-player.x)*scale,y:h*.64-(z-player.z)*scale};}`
- [x] `if` — linha 1529, coluna 226 em `miniMapLogicalSize()` — `function miniMapLogicalSize(canvas){const rect=canvas.getBoundingClientRect(),ratio=Math.min(2,window.devicePixelRatio||1),width=Math.max(180,Math.round(rect.width*ratio)),height=Math.max(110,Math.round(rect.height*ratio));if(canvas.width!==width||canvas.height!==height){canvas.width=width;canvas.height=height;}return{w:width,h:height,ratio};}`
- [x] `return` — linha 1529, coluna 316 em `miniMapLogicalSize()` — `function miniMapLogicalSize(canvas){const rect=canvas.getBoundingClientRect(),ratio=Math.min(2,window.devicePixelRatio||1),width=Math.max(180,Math.round(rect.width*ratio)),height=Math.max(110,Math.round(rect.height*ratio));if(canvas.width!==width||canvas.height!==height){canvas.width=width;canvas.height=height;}return{w:width,h:height,ratio};}`
- [x] `return` — linha 1530, coluna 30 em `miniMapScale()` — `function miniMapScale(w,h){return clamp(Math.min(w/196,h/122),.72,1.75);}`
- [x] `if` — linha 1532, coluna 36 em `drawMiniMap()` — `const canvas=els.miniMapCanvas;if(!canvas)return;const {w,h}=miniMapLogicalSize(canvas),ctx=canvas.getContext('2d'),scale=miniMapScale(w,h);ctx.clearRect(0,0,w,h);ctx.fillStyle='#67c957';ctx.fillRect(0,0,w,h);`
- [x] `return` — linha 1532, coluna 47 em `drawMiniMap()` — `const canvas=els.miniMapCanvas;if(!canvas)return;const {w,h}=miniMapLogicalSize(canvas),ctx=canvas.getContext('2d'),scale=miniMapScale(w,h);ctx.clearRect(0,0,w,h);ctx.fillStyle='#67c957';ctx.fillRect(0,0,w,h);`
- [x] `if` — linha 1533, coluna 53 em `drawMiniMap()` — `const room=window.OTTHI_ROOM_WORLD?.current?.();if(room?.bounds){const a=miniPoint(room.bounds.xMin,room.bounds.zMax,scale,w,h),b=miniPoint(room.bounds.xMax,room.bounds.zMin,scale,w,h);ctx.fillStyle='rgba(70,220,255,.12)';ctx.strokeStyle=room.accent||'#5ee7ff';ctx.lineWidth=2;ctx.setLineDash([6,5]);ctx.fillRect(a.x,a.y,b.x-a.x,b.y-a.y);ctx.strokeRect(a.x,a.y,b.x-a.x,b.y-a.y);ctx.setLineDash([]);}`
- [x] `for` — linha 1534, coluna 36 em `drawMiniMap()` — `ctx.save();ctx.lineCap='round';for(const road of WORLD_MAP_ROADS){const horizontal=road.w>=road.d,a=horizontal?miniPoint(road.x-road.w/2,road.z,scale,w,h):miniPoint(road.x,road.z-road.d/2,scale,w,h),b=horizontal?miniPoint(road.x+road.w/2,road.z,scale,w,h):miniPoint(road.x,road.z+road.d/2,scale,w,h);ctx.strokeStyle='#dce1e6';ctx.lineWidth=(horizontal?road.d:road.w)*scale+4;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke();ctx.strokeStyle='#424a55';ctx.lineWidth=(horizontal?road.d:road.w)*scale;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke();}`
- [x] `for` — linha 1535, coluna 5 em `drawMiniMap()` — `for(const house of window.OTTHI_ROOM_WORLD?.houseMarkers?.()||[]){const q=miniPoint(house.x,house.z,scale,w,h);if(q.x<4||q.x>w-4||q.y<4||q.y>h-4)continue;ctx.fillStyle=house.mine?'#ffe35b':'#fff';ctx.strokeStyle='#15314b';ctx.lineWidth=2;ctx.fillRect(q.x-4,q.y-4,8,8);ctx.strokeRect(q.x-4,q.y-4,8,8);}`
- [x] `if` — linha 1535, coluna 116 em `drawMiniMap()` — `for(const house of window.OTTHI_ROOM_WORLD?.houseMarkers?.()||[]){const q=miniPoint(house.x,house.z,scale,w,h);if(q.x<4||q.x>w-4||q.y<4||q.y>h-4)continue;ctx.fillStyle=house.mine?'#ffe35b':'#fff';ctx.strokeStyle='#15314b';ctx.lineWidth=2;ctx.fillRect(q.x-4,q.y-4,8,8);ctx.strokeRect(q.x-4,q.y-4,8,8);}`
- [x] `if` — linha 1536, coluna 5 em `drawMiniMap()` — `if(state.waypoint&&world.routePath.length){const route=remainingRoute(world.routePath,player);ctx.strokeStyle='#38e9ff';ctx.lineWidth=6;ctx.setLineDash([10,6]);ctx.beginPath();route.forEach((p,i)=>{const q=miniPoint(p.x,p.z,scale,w,h);i?ctx.lineTo(q.x,q.y):ctx.moveTo(q.x,q.y);});ctx.stroke();ctx.setLineDash([]);const target=miniPoint(state.waypoint.navX??state.waypoint.x,state.waypoint.navZ??state.waypoint.z,scale,w,h);if(target.x>-12&&target.x<w+12&&target.y>-12&&target.y<h+12){ctx.fillStyle='#ffe33b';ctx.strokeStyle='#172738';ctx.lineWidth=2;ctx.beginPath();ctx.arc(target.x,target.y,7,0,Math.PI*2);ctx.fill();ctx.stroke();}}`
- [x] `if` — linha 1536, coluna 428 em `drawMiniMap()` — `if(state.waypoint&&world.routePath.length){const route=remainingRoute(world.routePath,player);ctx.strokeStyle='#38e9ff';ctx.lineWidth=6;ctx.setLineDash([10,6]);ctx.beginPath();route.forEach((p,i)=>{const q=miniPoint(p.x,p.z,scale,w,h);i?ctx.lineTo(q.x,q.y):ctx.moveTo(q.x,q.y);});ctx.stroke();ctx.setLineDash([]);const target=miniPoint(state.waypoint.navX??state.waypoint.x,state.waypoint.navZ??state.waypoint.z,scale,w,h);if(target.x>-12&&target.x<w+12&&target.y>-12&&target.y<h+12){ctx.fillStyle='#ffe33b';ctx.strokeStyle='#172738';ctx.lineWidth=2;ctx.beginPath();ctx.arc(target.x,target.y,7,0,Math.PI*2);ctx.fill();ctx.stroke();}}`
- [x] `if` — linha 1537, coluna 182 em `drawMiniMap()` — `ctx.restore();ctx.fillStyle='rgba(5,20,35,.82)';ctx.fillRect(7,7,25,23);ctx.fillStyle='#fff';ctx.font=\`900 ${Math.max(12,Math.round(h*.1))}px system-ui\`;ctx.fillText('N',13,24);if(room){ctx.fillStyle='rgba(5,20,35,.78)';ctx.font=\`800 ${Math.max(9,Math.round(h*.065))}px system-ui\`;const label=\`${room.icon||''} ${room.shortName||room.name||''}\`;const width=Math.min(w-14,ctx.measureText(label).width+14);ctx.fillRect(7,h-25,width,18);ctx.fillStyle='#fff';ctx.fillText(label,13,h-12);}`
- [x] `if` — linha 1540, coluna 97 em `updateNavigation()` — `function updateNavigation(dt=0,force=false){updateNavigation.acc=(updateNavigation.acc||0)+dt;if(!force&&updateNavigation.acc<.14)return;updateNavigation.acc=0;updateRouteGuide(force);drawMiniMap();if(!els.miniNav)return;if(state.waypoint){const info=routeProgressInfo(world.routePath,player),dx=info.next.x-player.x,dz=info.next.z-player.z,arrival=Math.hypot((state.waypoint.navX??state.waypoint.x)-player.x,(state.waypoint.navZ??state.waypoint.z)-player.z);els.miniNavName.textContent=\`Rota: ${state.waypoint.name}\`;els.miniNavDistance.textContent=arrival<4?'Você chegou!':\`${Math.round(info.remaining)} m • ${info.instruction}\`;els.miniNavArrow.style.transform=\`rotate(${player.facing-Math.atan2(dx,dz)}rad)\`;els.miniNav.classList.add('active');if(arrival<4&&!state.waypoint.arrived){state.waypoint.arrived=true;toast(\`Você chegou: ${state.waypoint.name}\`,'good',1800);beep(850,90);saveState();}}else{els.miniNavName.textContent='GPS da Vila';els.miniNavDistance.textContent='Toque para escolher o`
- [x] `return` — linha 1540, coluna 133 em `updateNavigation()` — `function updateNavigation(dt=0,force=false){updateNavigation.acc=(updateNavigation.acc||0)+dt;if(!force&&updateNavigation.acc<.14)return;updateNavigation.acc=0;updateRouteGuide(force);drawMiniMap();if(!els.miniNav)return;if(state.waypoint){const info=routeProgressInfo(world.routePath,player),dx=info.next.x-player.x,dz=info.next.z-player.z,arrival=Math.hypot((state.waypoint.navX??state.waypoint.x)-player.x,(state.waypoint.navZ??state.waypoint.z)-player.z);els.miniNavName.textContent=\`Rota: ${state.waypoint.name}\`;els.miniNavDistance.textContent=arrival<4?'Você chegou!':\`${Math.round(info.remaining)} m • ${info.instruction}\`;els.miniNavArrow.style.transform=\`rotate(${player.facing-Math.atan2(dx,dz)}rad)\`;els.miniNav.classList.add('active');if(arrival<4&&!state.waypoint.arrived){state.waypoint.arrived=true;toast(\`Você chegou: ${state.waypoint.name}\`,'good',1800);beep(850,90);saveState();}}else{els.miniNavName.textContent='GPS da Vila';els.miniNavDistance.textContent='Toque para escolher o`
- [x] `if` — linha 1540, coluna 201 em `updateNavigation()` — `function updateNavigation(dt=0,force=false){updateNavigation.acc=(updateNavigation.acc||0)+dt;if(!force&&updateNavigation.acc<.14)return;updateNavigation.acc=0;updateRouteGuide(force);drawMiniMap();if(!els.miniNav)return;if(state.waypoint){const info=routeProgressInfo(world.routePath,player),dx=info.next.x-player.x,dz=info.next.z-player.z,arrival=Math.hypot((state.waypoint.navX??state.waypoint.x)-player.x,(state.waypoint.navZ??state.waypoint.z)-player.z);els.miniNavName.textContent=\`Rota: ${state.waypoint.name}\`;els.miniNavDistance.textContent=arrival<4?'Você chegou!':\`${Math.round(info.remaining)} m • ${info.instruction}\`;els.miniNavArrow.style.transform=\`rotate(${player.facing-Math.atan2(dx,dz)}rad)\`;els.miniNav.classList.add('active');if(arrival<4&&!state.waypoint.arrived){state.waypoint.arrived=true;toast(\`Você chegou: ${state.waypoint.name}\`,'good',1800);beep(850,90);saveState();}}else{els.miniNavName.textContent='GPS da Vila';els.miniNavDistance.textContent='Toque para escolher o`
- [x] `return` — linha 1540, coluna 217 em `updateNavigation()` — `function updateNavigation(dt=0,force=false){updateNavigation.acc=(updateNavigation.acc||0)+dt;if(!force&&updateNavigation.acc<.14)return;updateNavigation.acc=0;updateRouteGuide(force);drawMiniMap();if(!els.miniNav)return;if(state.waypoint){const info=routeProgressInfo(world.routePath,player),dx=info.next.x-player.x,dz=info.next.z-player.z,arrival=Math.hypot((state.waypoint.navX??state.waypoint.x)-player.x,(state.waypoint.navZ??state.waypoint.z)-player.z);els.miniNavName.textContent=\`Rota: ${state.waypoint.name}\`;els.miniNavDistance.textContent=arrival<4?'Você chegou!':\`${Math.round(info.remaining)} m • ${info.instruction}\`;els.miniNavArrow.style.transform=\`rotate(${player.facing-Math.atan2(dx,dz)}rad)\`;els.miniNav.classList.add('active');if(arrival<4&&!state.waypoint.arrived){state.waypoint.arrived=true;toast(\`Você chegou: ${state.waypoint.name}\`,'good',1800);beep(850,90);saveState();}}else{els.miniNavName.textContent='GPS da Vila';els.miniNavDistance.textContent='Toque para escolher o`
- [x] `if` — linha 1540, coluna 224 em `updateNavigation()` — `function updateNavigation(dt=0,force=false){updateNavigation.acc=(updateNavigation.acc||0)+dt;if(!force&&updateNavigation.acc<.14)return;updateNavigation.acc=0;updateRouteGuide(force);drawMiniMap();if(!els.miniNav)return;if(state.waypoint){const info=routeProgressInfo(world.routePath,player),dx=info.next.x-player.x,dz=info.next.z-player.z,arrival=Math.hypot((state.waypoint.navX??state.waypoint.x)-player.x,(state.waypoint.navZ??state.waypoint.z)-player.z);els.miniNavName.textContent=\`Rota: ${state.waypoint.name}\`;els.miniNavDistance.textContent=arrival<4?'Você chegou!':\`${Math.round(info.remaining)} m • ${info.instruction}\`;els.miniNavArrow.style.transform=\`rotate(${player.facing-Math.atan2(dx,dz)}rad)\`;els.miniNav.classList.add('active');if(arrival<4&&!state.waypoint.arrived){state.waypoint.arrived=true;toast(\`Você chegou: ${state.waypoint.name}\`,'good',1800);beep(850,90);saveState();}}else{els.miniNavName.textContent='GPS da Vila';els.miniNavDistance.textContent='Toque para escolher o`
- [x] `if` — linha 1540, coluna 751 em `updateNavigation()` — `function updateNavigation(dt=0,force=false){updateNavigation.acc=(updateNavigation.acc||0)+dt;if(!force&&updateNavigation.acc<.14)return;updateNavigation.acc=0;updateRouteGuide(force);drawMiniMap();if(!els.miniNav)return;if(state.waypoint){const info=routeProgressInfo(world.routePath,player),dx=info.next.x-player.x,dz=info.next.z-player.z,arrival=Math.hypot((state.waypoint.navX??state.waypoint.x)-player.x,(state.waypoint.navZ??state.waypoint.z)-player.z);els.miniNavName.textContent=\`Rota: ${state.waypoint.name}\`;els.miniNavDistance.textContent=arrival<4?'Você chegou!':\`${Math.round(info.remaining)} m • ${info.instruction}\`;els.miniNavArrow.style.transform=\`rotate(${player.facing-Math.atan2(dx,dz)}rad)\`;els.miniNav.classList.add('active');if(arrival<4&&!state.waypoint.arrived){state.waypoint.arrived=true;toast(\`Você chegou: ${state.waypoint.name}\`,'good',1800);beep(850,90);saveState();}}else{els.miniNavName.textContent='GPS da Vila';els.miniNavDistance.textContent='Toque para escolher o`
- [x] `else` — linha 1540, coluna 902 em `updateNavigation()` — `function updateNavigation(dt=0,force=false){updateNavigation.acc=(updateNavigation.acc||0)+dt;if(!force&&updateNavigation.acc<.14)return;updateNavigation.acc=0;updateRouteGuide(force);drawMiniMap();if(!els.miniNav)return;if(state.waypoint){const info=routeProgressInfo(world.routePath,player),dx=info.next.x-player.x,dz=info.next.z-player.z,arrival=Math.hypot((state.waypoint.navX??state.waypoint.x)-player.x,(state.waypoint.navZ??state.waypoint.z)-player.z);els.miniNavName.textContent=\`Rota: ${state.waypoint.name}\`;els.miniNavDistance.textContent=arrival<4?'Você chegou!':\`${Math.round(info.remaining)} m • ${info.instruction}\`;els.miniNavArrow.style.transform=\`rotate(${player.facing-Math.atan2(dx,dz)}rad)\`;els.miniNav.classList.add('active');if(arrival<4&&!state.waypoint.arrived){state.waypoint.arrived=true;toast(\`Você chegou: ${state.waypoint.name}\`,'good',1800);beep(850,90);saveState();}}else{els.miniNavName.textContent='GPS da Vila';els.miniNavDistance.textContent='Toque para escolher o`
- [x] `return` — linha 1541, coluna 83 em `routeSvgMarkup()` — `function routeSvgMarkup(points){const mapped=points.map(p=>worldToMap(p.x,p.z));return \`<svg class="map-route" viewBox="0 0 100 100" preserveAspectRatio="none" aria-hidden="true"><polyline points="${mapped.map(p=>\`${p.left},${p.top}\`).join(' ')}"/></svg>\`;}`

## `08-map-parent-settings.js`

- [x] `if` — linha 1606, coluna 36 em `mapLocationDetails()` — `function mapLocationDetails(loc){if(loc?.description||loc?.actions)return{description:loc.description||'Destino relacionado à missão ativa.',actions:Array.isArray(loc.actions)?loc.actions:['Seguir o GPS','Concluir a etapa']};const d=MAP_LOCATION_DETAILS[loc.id]||MAP_LOCATION_DETAILS.default;return{description:d[0],actions:d[1]};}`
- [x] `return` — linha 1606, coluna 70 em `mapLocationDetails()` — `function mapLocationDetails(loc){if(loc?.description||loc?.actions)return{description:loc.description||'Destino relacionado à missão ativa.',actions:Array.isArray(loc.actions)?loc.actions:['Seguir o GPS','Concluir a etapa']};const d=MAP_LOCATION_DETAILS[loc.id]||MAP_LOCATION_DETAILS.default;return{description:d[0],actions:d[1]};}`
- [x] `return` — linha 1606, coluna 295 em `mapLocationDetails()` — `function mapLocationDetails(loc){if(loc?.description||loc?.actions)return{description:loc.description||'Destino relacionado à missão ativa.',actions:Array.isArray(loc.actions)?loc.actions:['Seguir o GPS','Concluir a etapa']};const d=MAP_LOCATION_DETAILS[loc.id]||MAP_LOCATION_DETAILS.default;return{description:d[0],actions:d[1]};}`
- [x] `return` — linha 1607, coluna 29 em `worldToMap()` — `function worldToMap(x,z){ return { left:clamp((x+116)/232*100,2.5,97.5), top:clamp((116-z)/232*100,2.5,97.5) }; }`
- [x] `if` — linha 1610, coluna 5 em `missionMapLocations()` — `if(job&&typeof serviceVehicleMapLocation==='function'){const vehicleTarget=serviceVehicleMapLocation(job);if(vehicleTarget)items.push(vehicleTarget);}`
- [x] `if` — linha 1610, coluna 111 em `missionMapLocations()` — `if(job&&typeof serviceVehicleMapLocation==='function'){const vehicleTarget=serviceVehicleMapLocation(job);if(vehicleTarget)items.push(vehicleTarget);}`
- [x] `if` — linha 1611, coluna 35 em `missionMapLocations()` — `const waypoint=state.waypoint;if(waypoint&&!MAP_LOCATIONS.some(loc=>loc.id===waypoint.id)&&!items.some(loc=>loc.id===waypoint.id)){const mission=!!job||/^(service-|assist-|fire-|delivery-)/.test(String(waypoint.id||''));items.push({id:waypoint.id,name:waypoint.name||'Destino da missão',icon:String(waypoint.icon||(/service-/.test(waypoint.id)?'🚨':/fire-/.test(waypoint.id)?'🔥':/assist-/.test(waypoint.id)?'⚠️':'📍')),group:mission?'Missão ativa':'Destino atual',x:Number(waypoint.x||0),z:Number(waypoint.z||0),navX:Number(waypoint.navX??waypoint.x??0),navZ:Number(waypoint.navZ??waypoint.z??0),missionTarget:mission,description:mission?'Este é o ponto exato exigido pela missão. A rota azul e as setas do mundo levam até ele.':'Destino atualmente marcado no GPS.',actions:['Seguir a rota azul','Observar as setas no chão']});}`
- [x] `return` — linha 1612, coluna 5 em `missionMapLocations()` — `return items;`
- [x] `return` — linha 1614, coluna 94 em `onlinePlayerMapLocations()` — `function onlinePlayerMapLocations(){const presence=window.OTTHOS_RTDB?.getPresence?.()||{};return Object.entries(presence).filter(([,item])=>item&&Number.isFinite(Number(item.x))&&Number.isFinite(Number(item.z))).map(([uid,item])=>({id:\`online-${String(uid).replace(/[^A-Za-z0-9_-]/g,'').slice(0,48)}\`,name:item.name||'Jogador online',icon:'👤',group:'Jogadores online',x:Number(item.x),z:Number(item.z),navX:Number(item.x),navZ:Number(item.z),description:'Posição individual sincronizada deste participante no bairro atual.',actions:['Marcar no GPS','Encontrar o participante voluntariamente']}));}`
- [x] `for` — linha 1615, coluna 275 em `currentMapLocations()` — `function currentMapLocations(){const all=[...MAP_LOCATIONS,...(window.OTTHI_ROOM_WORLD?.mapHouseLocations?.()||[]),...missionMapLocations(),...(typeof coopMissionMapLocations==='function'?coopMissionMapLocations():[]),...onlinePlayerMapLocations()];const unique=new Map();for(const loc of all)if(loc?.id)unique.set(loc.id,loc);return[...unique.values()];}`
- [x] `if` — linha 1615, coluna 296 em `currentMapLocations()` — `function currentMapLocations(){const all=[...MAP_LOCATIONS,...(window.OTTHI_ROOM_WORLD?.mapHouseLocations?.()||[]),...missionMapLocations(),...(typeof coopMissionMapLocations==='function'?coopMissionMapLocations():[]),...onlinePlayerMapLocations()];const unique=new Map();for(const loc of all)if(loc?.id)unique.set(loc.id,loc);return[...unique.values()];}`
- [x] `return` — linha 1615, coluna 330 em `currentMapLocations()` — `function currentMapLocations(){const all=[...MAP_LOCATIONS,...(window.OTTHI_ROOM_WORLD?.mapHouseLocations?.()||[]),...missionMapLocations(),...(typeof coopMissionMapLocations==='function'?coopMissionMapLocations():[]),...onlinePlayerMapLocations()];const unique=new Map();for(const loc of all)if(loc?.id)unique.set(loc.id,loc);return[...unique.values()];}`
- [x] `return` — linha 1616, coluna 32 em `mapDistance()` — `function mapDistance(point){ return Math.round(Math.hypot(player.x-(point.navX??point.x),player.z-(point.navZ??point.z))); }`
- [x] `return` — linha 1629, coluna 100 em `mapMarkerPlacements()` — `const points=locations.map((loc,index)=>{const pos=worldToMap(loc.navX??loc.x,loc.navZ??loc.z);return{loc,index,originLeft:pos.left,originTop:pos.top,left:clamp(pos.left,minLeft,maxLeft),top:clamp(pos.top,minTop,maxTop)};});`
- [x] `if` — linha 1632, coluna 7 em `mapMarkerPlacements()` — `if(d>=1)return false;`
- [x] `return` — linha 1632, coluna 15 em `mapMarkerPlacements()` — `if(d>=1)return false;`
- [x] `if` — linha 1633, coluna 7 em `mapMarkerPlacements()` — `if(d<.001){const angle=((a.index+1)*2.399963229728653+(b.index+1)*.731);nx=Math.cos(angle);ny=Math.sin(angle);d=1;}`
- [x] `else` — linha 1634, coluna 7 em `mapMarkerPlacements()` — `else{nx/=d;ny/=d;}`
- [x] `if` — linha 1636, coluna 7 em `mapMarkerPlacements()` — `if(pushBoth){a.left-=mx*.5;a.top-=my*.5;b.left+=mx*.5;b.top+=my*.5;}`
- [x] `else` — linha 1637, coluna 7 em `mapMarkerPlacements()` — `else{b.left+=mx;b.top+=my;}`
- [x] `return` — linha 1638, coluna 7 em `mapMarkerPlacements()` — `return true;`
- [x] `for` — linha 1641, coluna 5 em `mapMarkerPlacements()` — `for(let pass=0;pass<72;pass++){`
- [x] `for` — linha 1643, coluna 7 em `mapMarkerPlacements()` — `for(let i=0;i<points.length;i++)for(let j=i+1;j<points.length;j++)moved=separate(points[i],points[j],gapX,gapY,true)||moved;`
- [x] `for` — linha 1643, coluna 39 em `mapMarkerPlacements()` — `for(let i=0;i<points.length;i++)for(let j=i+1;j<points.length;j++)moved=separate(points[i],points[j],gapX,gapY,true)||moved;`
- [x] `for` — linha 1644, coluna 7 em `mapMarkerPlacements()` — `for(const point of points){`
- [x] `if` — linha 1647, coluna 9 em `mapMarkerPlacements()` — `if(pass>38){point.left+=(point.originLeft-point.left)*.006;point.top+=(point.originTop-point.top)*.006;}`
- [x] `if` — linha 1650, coluna 7 em `mapMarkerPlacements()` — `if(!moved&&pass>10)break;`
- [x] `return` — linha 1652, coluna 5 em `mapMarkerPlacements()` — `return points;`
- [x] `if` — linha 1656, coluna 60 em `applyMapMarkerPlacements()` — `const marker=$(\`[data-map-marker="${loc.id}"]\`,root);if(!marker)return;`
- [x] `return` — linha 1656, coluna 71 em `applyMapMarkerPlacements()` — `const marker=$(\`[data-map-marker="${loc.id}"]\`,root);if(!marker)return;`
- [x] `for` — linha 1665, coluna 5 em `mapVisualNodes()` — `for(const loc of locations){if(loc.missionTarget||loc.id===activeId||loc.id===selectedId||loc.id==='home'||loc.coopTarget)priority.push(loc);else regular.push(loc);}`
- [x] `if` — linha 1665, coluna 33 em `mapVisualNodes()` — `for(const loc of locations){if(loc.missionTarget||loc.id===activeId||loc.id===selectedId||loc.id==='home'||loc.coopTarget)priority.push(loc);else regular.push(loc);}`
- [x] `else` — linha 1665, coluna 146 em `mapVisualNodes()` — `for(const loc of locations){if(loc.missionTarget||loc.id===activeId||loc.id===selectedId||loc.id==='home'||loc.coopTarget)priority.push(loc);else regular.push(loc);}`
- [x] `for` — linha 1667, coluna 5 em `mapVisualNodes()` — `for(const loc of regular){const pos=worldToMap(loc.navX??loc.x,loc.navZ??loc.z),key=\`${Math.round(pos.left/cellSize)}:${Math.round(pos.top/cellSize)}\`;if(!cells.has(key))cells.set(key,[]);cells.get(key).push(loc);}`
- [x] `if` — linha 1667, coluna 156 em `mapVisualNodes()` — `for(const loc of regular){const pos=worldToMap(loc.navX??loc.x,loc.navZ??loc.z),key=\`${Math.round(pos.left/cellSize)}:${Math.round(pos.top/cellSize)}\`;if(!cells.has(key))cells.set(key,[]);cells.get(key).push(loc);}`
- [x] `for` — linha 1669, coluna 5 em `mapVisualNodes()` — `for(const group of cells.values()){`
- [x] `if` — linha 1670, coluna 7 em `mapVisualNodes()` — `if(group.length===1){nodes.push(group[0]);continue;}`
- [x] `return` — linha 1673, coluna 5 em `mapVisualNodes()` — `return nodes;`
- [x] `return` — linha 1675, coluna 47 em `mapClusterSelectionMarkup()` — `function mapClusterSelectionMarkup(cluster){return\`<div class="map-selection detailed map-cluster-selection"><div class="map-selection-title"><b>📍 ${cluster.clusterItems.length} locais próximos</b><span>Ícones agrupados para manter o mapa legível</span></div><div class="map-cluster-options">${cluster.clusterItems.sort((a,b)=>mapDistance(a)-mapDistance(b)).map(loc=>\`<button data-cluster-location="${loc.id}"><b>${loc.icon} ${loc.name}</b><span>${mapDistance(loc)} m</span></button>\`).join('')}</div></div>\`;}`
- [x] `if` — linha 1677, coluna 40 em `mapSelectionMarkup()` — `const cluster=mapClusterLookup[id];if(cluster)return mapClusterSelectionMarkup(cluster);const loc=currentMapLocations().find(x=>x.id===id);if(!loc)return'<div class="map-selection empty"><b>Toque em um ícone</b><span>O nome aparecerá aqui antes de iniciar a rota.</span></div>';`
- [x] `return` — linha 1677, coluna 51 em `mapSelectionMarkup()` — `const cluster=mapClusterLookup[id];if(cluster)return mapClusterSelectionMarkup(cluster);const loc=currentMapLocations().find(x=>x.id===id);if(!loc)return'<div class="map-selection empty"><b>Toque em um ícone</b><span>O nome aparecerá aqui antes de iniciar a rota.</span></div>';`
- [x] `if` — linha 1677, coluna 144 em `mapSelectionMarkup()` — `const cluster=mapClusterLookup[id];if(cluster)return mapClusterSelectionMarkup(cluster);const loc=currentMapLocations().find(x=>x.id===id);if(!loc)return'<div class="map-selection empty"><b>Toque em um ícone</b><span>O nome aparecerá aqui antes de iniciar a rota.</span></div>';`
- [x] `return` — linha 1677, coluna 152 em `mapSelectionMarkup()` — `const cluster=mapClusterLookup[id];if(cluster)return mapClusterSelectionMarkup(cluster);const loc=currentMapLocations().find(x=>x.id===id);if(!loc)return'<div class="map-selection empty"><b>Toque em um ícone</b><span>O nome aparecerá aqui antes de iniciar a rota.</span></div>';`
- [x] `return` — linha 1678, coluna 90 em `mapSelectionMarkup()` — `const details=mapLocationDetails(loc),mission=loc.missionTarget?' mission-target':'';return\`<div class="map-selection detailed${mission}"><div class="map-selection-title"><b>${loc.icon} ${loc.name}</b><span>${loc.group}</span></div><p>${details.description}</p><div class="map-action-chips">${details.actions.map(action=>\`<i>${action}</i>\`).join('')}</div><footer><span>${mapDistance(loc)} m de distância</span><button class="btn primary compact" data-route-selected="${loc.id}">Ir para este local</button></footer></div>\`;`
- [x] `if` — linha 1681, coluna 58 em `setWaypoint()` — `const point=currentMapLocations().find(p=>p.id===id);if(!point)return;`
- [x] `return` — linha 1681, coluna 68 em `setWaypoint()` — `const point=currentMapLocations().find(p=>p.id===id);if(!point)return;`
- [x] `if` — linha 1688, coluna 46 em `openMap()` — `const mapLocations=currentMapLocations();if(!mapSelectedId||(!mapLocations.some(x=>x.id===mapSelectedId)&&!mapClusterLookup[mapSelectedId]))mapSelectedId=activeId||'home';`
- [x] `return` — linha 1690, coluna 228 em `openMap()` — `const markers=placements.map(({loc,left,top})=>{const active=loc.id===activeId?' active':'',selected=loc.id===mapSelectedId?' selected':'',mission=loc.missionTarget?' mission-target':'',cluster=loc.mapCluster?' cluster':'';return \`<button class="map-marker clean${active}${selected}${mission}${cluster}" style="left:${left.toFixed(2)}%;top:${top.toFixed(2)}%" data-map-marker="${loc.id}" aria-label="${loc.name}" title="${loc.name}"><b>${loc.mapCluster?loc.clusterItems.length:loc.icon}</b><span>${loc.name}</span></button>\`;}).join('');`
- [x] `if` — linha 1692, coluna 89 em `openMap()` — `const grouped=groups.map(group=>{const list=mapLocations.filter(x=>x.group===group);if(!list.length)return'';const items=list.sort((a,b)=>mapDistance(a)-mapDistance(b)).map(loc=>\`<button class="map-destination ${loc.id===activeId?'active':''}" data-map-list="${loc.id}"><b>${loc.icon}<em>${loc.name}</em></b><span>${mapDistance(loc)} m</span></button>\`).join('');return \`<section class="map-destination-group"><h4>${group}</h4><div>${items}</div></section>\`;}).join('');`
- [x] `return` — linha 1692, coluna 105 em `openMap()` — `const grouped=groups.map(group=>{const list=mapLocations.filter(x=>x.group===group);if(!list.length)return'';const items=list.sort((a,b)=>mapDistance(a)-mapDistance(b)).map(loc=>\`<button class="map-destination ${loc.id===activeId?'active':''}" data-map-list="${loc.id}"><b>${loc.icon}<em>${loc.name}</em></b><span>${mapDistance(loc)} m</span></button>\`).join('');return \`<section class="map-destination-group"><h4>${group}</h4><div>${items}</div></section>\`;}).join('');`
- [x] `return` — linha 1692, coluna 368 em `openMap()` — `const grouped=groups.map(group=>{const list=mapLocations.filter(x=>x.group===group);if(!list.length)return'';const items=list.sort((a,b)=>mapDistance(a)-mapDistance(b)).map(loc=>\`<button class="map-destination ${loc.id===activeId?'active':''}" data-map-list="${loc.id}"><b>${loc.icon}<em>${loc.name}</em></b><span>${mapDistance(loc)} m</span></button>\`).join('');return \`<section class="map-destination-group"><h4>${group}</h4><div>${items}</div></section>\`;}).join('');`
- [x] `if` — linha 1696, coluna 35 em `openMap()` — `const fitMapToViewport=()=>{if(!map||!main)return;const portrait=matchMedia?.('(orientation: portrait)')?.matches??innerHeight>=innerWidth,body=$('.modal-body',els.modal),availableWidth=Math.max(220,main.clientWidth),availableHeight=portrait?Math.min(innerHeight*.55,availableWidth):Math.max(220,Math.min(main.clientHeight,body?.clientHeight||innerHeight*.75)),size=Math.floor(Math.min(availableWidth,availableHeight));map.style.setProperty('--map-size',\`${size}px\`);map.style.setProperty('width',\`${size}px\`,'important');map.style.setProperty('height',\`${size}px\`,'important');const rect=map.getBoundingClientRect();applyMapMarkerPlacements(root,mapMarkerPlacements(visualNodes,pp,rect.width,rect.height));};`
- [x] `return` — linha 1696, coluna 50 em `openMap()` — `const fitMapToViewport=()=>{if(!map||!main)return;const portrait=matchMedia?.('(orientation: portrait)')?.matches??innerHeight>=innerWidth,body=$('.modal-body',els.modal),availableWidth=Math.max(220,main.clientWidth),availableHeight=portrait?Math.min(innerHeight*.55,availableWidth):Math.max(220,Math.min(main.clientHeight,body?.clientHeight||innerHeight*.75)),size=Math.floor(Math.min(availableWidth,availableHeight));map.style.setProperty('--map-size',\`${size}px\`);map.style.setProperty('width',\`${size}px\`,'important');map.style.setProperty('height',\`${size}px\`,'important');const rect=map.getBoundingClientRect();applyMapMarkerPlacements(root,mapMarkerPlacements(visualNodes,pp,rect.width,rect.height));};`
- [x] `if` — linha 1698, coluna 298 em `openMap()` — `const selectMapLocation=id=>{mapSelectedId=id;$$('[data-map-marker]',root).forEach(x=>x.classList.toggle('selected',x.dataset.mapMarker===id));$$('[data-map-list]',root).forEach(x=>x.classList.toggle('selected',x.dataset.mapList===id));selection.innerHTML=mapSelectionMarkup(id);bindRoute();if(matchMedia?.('(orientation: portrait)')?.matches)selection.scrollIntoView?.({behavior:'smooth',block:'nearest'});};`
- [x] `if` — linha 1703, coluna 40 em `refreshOpenMapAfterResize()` — `function refreshOpenMapAfterResize(){if(els.modal.hidden||!els.modal.classList.contains('map-modal'))return;clearTimeout(mapResizeTimer);mapResizeTimer=setTimeout(()=>{if(!els.modal.hidden&&els.modal.classList.contains('map-modal'))openMap();},180);}`
- [x] `return` — linha 1703, coluna 104 em `refreshOpenMapAfterResize()` — `function refreshOpenMapAfterResize(){if(els.modal.hidden||!els.modal.classList.contains('map-modal'))return;clearTimeout(mapResizeTimer);mapResizeTimer=setTimeout(()=>{if(!els.modal.hidden&&els.modal.classList.contains('map-modal'))openMap();},180);}`
- [x] `if` — linha 1703, coluna 171 em `refreshOpenMapAfterResize()` — `function refreshOpenMapAfterResize(){if(els.modal.hidden||!els.modal.classList.contains('map-modal'))return;clearTimeout(mapResizeTimer);mapResizeTimer=setTimeout(()=>{if(!els.modal.hidden&&els.modal.classList.contains('map-modal'))openMap();},180);}`
- [x] `return` — linha 1707, coluna 139 em `performLocalReset()` — `window.OTTHOS_ACCOUNT?.clearSession?.();accountSession=null;safeLocalRemove(STORAGE_KEY);LEGACY_STORAGE_KEYS.forEach(safeLocalRemove);return window.OTTHOS_DB?.clear?.();`
- [x] `if` — linha 1711, coluna 65 em `openFinalResetConfirmation()` — `const input=$('[data-reset-word]',root),confirm=async()=>{if(String(input.value||'').trim().toUpperCase()!=='APAGAR'){$('[data-reset-error]',root).hidden=false;input.select();return;}if(!(await confirmModal('Última confirmação','Tem certeza de que deseja reiniciar os dados locais deste aparelho?','Sim, recomeçar','Cancelar')))return;await performLocalReset();state=defaultState();await commitState();location.reload();};`
- [x] `return` — linha 1711, coluna 182 em `openFinalResetConfirmation()` — `const input=$('[data-reset-word]',root),confirm=async()=>{if(String(input.value||'').trim().toUpperCase()!=='APAGAR'){$('[data-reset-error]',root).hidden=false;input.select();return;}if(!(await confirmModal('Última confirmação','Tem certeza de que deseja reiniciar os dados locais deste aparelho?','Sim, recomeçar','Cancelar')))return;await performLocalReset();state=defaultState();await commitState();location.reload();};`
- [x] `if` — linha 1711, coluna 190 em `openFinalResetConfirmation()` — `const input=$('[data-reset-word]',root),confirm=async()=>{if(String(input.value||'').trim().toUpperCase()!=='APAGAR'){$('[data-reset-error]',root).hidden=false;input.select();return;}if(!(await confirmModal('Última confirmação','Tem certeza de que deseja reiniciar os dados locais deste aparelho?','Sim, recomeçar','Cancelar')))return;await performLocalReset();state=defaultState();await commitState();location.reload();};`
- [x] `return` — linha 1711, coluna 335 em `openFinalResetConfirmation()` — `const input=$('[data-reset-word]',root),confirm=async()=>{if(String(input.value||'').trim().toUpperCase()!=='APAGAR'){$('[data-reset-error]',root).hidden=false;input.select();return;}if(!(await confirmModal('Última confirmação','Tem certeza de que deseja reiniciar os dados locais deste aparelho?','Sim, recomeçar','Cancelar')))return;await performLocalReset();state=defaultState();await commitState();location.reload();};`
- [x] `if` — linha 1712, coluna 140 em `openFinalResetConfirmation()` — `$('[data-reset-confirm]',root).onclick=confirm;$('[data-reset-cancel]',root).onclick=()=>openParentTools(inGame);input.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();confirm();}};setTimeout(()=>input.focus(),80);`
- [x] `return` — linha 1715, coluna 143 em `formatPlayTime()` — `function formatPlayTime(seconds=0){const minutes=Math.max(0,Math.floor(Number(seconds||0)/60)),hours=Math.floor(minutes/60),rest=minutes%60;return hours?\`${hours}h ${rest}min\`:\`${rest} min\`;}`
- [x] `if` — linha 1718, coluna 5 em `formatPlayTime()` — `if(accountLinked()){`
- [x] `if` — linha 1719, coluna 74 em `formatPlayTime()` — `try{const remote=await window.OTTHOS_RTDB?.getGuardianSettings?.();if(remote&&typeof remote==='object')guardian={...guardian,...(remote.settings||remote)};}catch{}`
- [x] `catch` — linha 1719, coluna 163 em `formatPlayTime()` — `try{const remote=await window.OTTHOS_RTDB?.getGuardianSettings?.();if(remote&&typeof remote==='object')guardian={...guardian,...(remote.settings||remote)};}catch{}`
- [x] `catch` — linha 1720, coluna 72 em `formatPlayTime()` — `try{blocked=await window.OTTHOS_RTDB?.getBlockedPlayers?.()||{};}catch{}`
- [x] `catch` — linha 1721, coluna 131 em `formatPlayTime()` — `try{const audit=await window.OTTHOS_RTDB?.getActivityAudit?.(120);activityAudit=Array.isArray(audit?.items)?audit.items:[];}catch{}`
- [x] `return` — linha 1730, coluna 455 em `formatPlayTime()` — `const auditHtml=linked?\`<div class="social-tabs"><b>Histórico online</b><small>últimas ${activityAudit.length} ações</small></div>${activityAudit.length?activityAudit.map(item=>{const when=new Date(Number(item.createdAtClient||item.createdAt||Date.now())).toLocaleString('pt-BR',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'}),peer=item.peerName?\` • ${escapeHtml(item.peerName)}\`:'',text=item.text?\` • “${escapeHtml(item.text)}”\`:'';return\`<div class="settings-row"><div><b>${auditDirectionLabel(item)} ${auditCategoryLabel(item)}</b><small>${escapeHtml(item.action||'atividade')}${peer}${text}</small></div><span class="db-status">${when}</span></div>\`}).join(''):'<p class="empty-online">Nenhuma conversa ou interação registrada ainda.</p>'}\`:'';`
- [x] `if` — linha 1734, coluna 50 em `formatPlayTime()` — `const limit=$('[data-session-limit]',root);if(limit)limit.value=String(Math.max(0,Number(guardian.sessionLimitMinutes||0)));`
- [x] `if` — linha 1735, coluna 458 em `formatPlayTime()` — `$('[data-save-guardian]',root)?.addEventListener('click',async e=>{const btn=e.currentTarget;draft.sessionLimitMinutes=Math.max(0,Number(limit?.value||0));draft.multiplayerEnabled=true;draft.communicationEnabled=true;draft.chatEnabled=true;btn.disabled=true;btn.textContent='Salvando...';const saveGuardian=window.OTTHOS_RTDB?.saveGuardianSettings;const result=saveGuardian?await saveGuardian(draft):{ok:false,error:'Firebase ainda não está pronto.'};if(result?.ok===false||result===false){btn.disabled=false;btn.textContent='Salvar limite de tempo';toast(result?.error||'Não foi possível salvar. Confirme a senha novamente.','warn',3000);return;}const saved=result?.settings||result;state.guardian={...draft,...saved,updatedAt:Date.now()};saveState(true);window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()});toast('Limite de tempo salvo. Multiplayer e interações permanecem liberados.','good',2600);openParentTools(inGame);});`
- [x] `return` — linha 1735, coluna 646 em `formatPlayTime()` — `$('[data-save-guardian]',root)?.addEventListener('click',async e=>{const btn=e.currentTarget;draft.sessionLimitMinutes=Math.max(0,Number(limit?.value||0));draft.multiplayerEnabled=true;draft.communicationEnabled=true;draft.chatEnabled=true;btn.disabled=true;btn.textContent='Salvando...';const saveGuardian=window.OTTHOS_RTDB?.saveGuardianSettings;const result=saveGuardian?await saveGuardian(draft):{ok:false,error:'Firebase ainda não está pronto.'};if(result?.ok===false||result===false){btn.disabled=false;btn.textContent='Salvar limite de tempo';toast(result?.error||'Não foi possível salvar. Confirme a senha novamente.','warn',3000);return;}const saved=result?.settings||result;state.guardian={...draft,...saved,updatedAt:Date.now()};saveState(true);window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()});toast('Limite de tempo salvo. Multiplayer e interações permanecem liberados.','good',2600);openParentTools(inGame);});`
- [x] `if` — linha 1736, coluna 157 em `formatPlayTime()` — `$$('[data-unblock-player]',root).forEach(btn=>btn.onclick=async()=>{const result=await window.OTTHOS_RTDB?.unblockPlayer?.(btn.dataset.unblockPlayer);if(result?.ok===false||result===false){toast(result?.error||'Não foi possível desbloquear.','warn');return;}toast('Jogador desbloqueado.','good');openParentTools(inGame);});`
- [x] `return` — linha 1736, coluna 257 em `formatPlayTime()` — `$$('[data-unblock-player]',root).forEach(btn=>btn.onclick=async()=>{const result=await window.OTTHOS_RTDB?.unblockPlayer?.(btn.dataset.unblockPlayer);if(result?.ok===false||result===false){toast(result?.error||'Não foi possível desbloquear.','warn');return;}toast('Jogador desbloqueado.','good');openParentTools(inGame);});`
- [x] `if` — linha 1740, coluna 68 em `formatPlayTime()` — `fileInput.onchange=async()=>{const file=fileInput.files?.[0];if(!file)return;try{const imported=normalizeState(await window.OTTHOS_DB.importFile(file));if(!(await confirmModal('Importar backup','O progresso atual deste aparelho será substituído pelo arquivo escolhido. Continuar?','Importar','Cancelar')))return;state=imported;await window.OTTHOS_DB.save(state);safeLocalSet(STORAGE_KEY,JSON.stringify(state));location.reload();}catch(error){toast(error.message||'Backup inválido.','bad');}};`
- [x] `return` — linha 1740, coluna 77 em `formatPlayTime()` — `fileInput.onchange=async()=>{const file=fileInput.files?.[0];if(!file)return;try{const imported=normalizeState(await window.OTTHOS_DB.importFile(file));if(!(await confirmModal('Importar backup','O progresso atual deste aparelho será substituído pelo arquivo escolhido. Continuar?','Importar','Cancelar')))return;state=imported;await window.OTTHOS_DB.save(state);safeLocalSet(STORAGE_KEY,JSON.stringify(state));location.reload();}catch(error){toast(error.message||'Backup inválido.','bad');}};`
- [x] `if` — linha 1740, coluna 159 em `formatPlayTime()` — `fileInput.onchange=async()=>{const file=fileInput.files?.[0];if(!file)return;try{const imported=normalizeState(await window.OTTHOS_DB.importFile(file));if(!(await confirmModal('Importar backup','O progresso atual deste aparelho será substituído pelo arquivo escolhido. Continuar?','Importar','Cancelar')))return;state=imported;await window.OTTHOS_DB.save(state);safeLocalSet(STORAGE_KEY,JSON.stringify(state));location.reload();}catch(error){toast(error.message||'Backup inválido.','bad');}};`
- [x] `return` — linha 1740, coluna 312 em `formatPlayTime()` — `fileInput.onchange=async()=>{const file=fileInput.files?.[0];if(!file)return;try{const imported=normalizeState(await window.OTTHOS_DB.importFile(file));if(!(await confirmModal('Importar backup','O progresso atual deste aparelho será substituído pelo arquivo escolhido. Continuar?','Importar','Cancelar')))return;state=imported;await window.OTTHOS_DB.save(state);safeLocalSet(STORAGE_KEY,JSON.stringify(state));location.reload();}catch(error){toast(error.message||'Backup inválido.','bad');}};`
- [x] `catch` — linha 1740, coluna 436 em `formatPlayTime()` — `fileInput.onchange=async()=>{const file=fileInput.files?.[0];if(!file)return;try{const imported=normalizeState(await window.OTTHOS_DB.importFile(file));if(!(await confirmModal('Importar backup','O progresso atual deste aparelho será substituído pelo arquivo escolhido. Continuar?','Importar','Cancelar')))return;state=imported;await window.OTTHOS_DB.save(state);safeLocalSet(STORAGE_KEY,JSON.stringify(state));location.reload();}catch(error){toast(error.message||'Backup inválido.','bad');}};`
- [x] `if` — linha 1745, coluna 5 em `openParentGate()` — `if(accountLinked()){`
- [x] `if` — linha 1747, coluna 280 em `openParentGate()` — `const input=$('[data-parent-password]',root),error=$('[data-parent-gate-error]',root),unlock=async()=>{const btn=$('[data-parent-unlock]',root);btn.disabled=true;btn.textContent='Confirmando...';const result=await window.OTTHOS_RTDB?.reauthenticateAccount?.(input.value);if(!result?.ok){error.textContent=result?.error||'Senha incorreta.';error.hidden=false;btn.disabled=false;btn.textContent='Continuar';input.select();return;}openParentTools(inGame);};`
- [x] `return` — linha 1747, coluna 429 em `openParentGate()` — `const input=$('[data-parent-password]',root),error=$('[data-parent-gate-error]',root),unlock=async()=>{const btn=$('[data-parent-unlock]',root);btn.disabled=true;btn.textContent='Confirmando...';const result=await window.OTTHOS_RTDB?.reauthenticateAccount?.(input.value);if(!result?.ok){error.textContent=result?.error||'Senha incorreta.';error.hidden=false;btn.disabled=false;btn.textContent='Continuar';input.select();return;}openParentTools(inGame);};`
- [x] `if` — linha 1748, coluna 139 em `openParentGate()` — `$('[data-parent-unlock]',root).onclick=unlock;$('[data-parent-cancel]',root).onclick=()=>openSettings(inGame);input.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();unlock();}};setTimeout(()=>input.focus(),80);`
- [x] `return` — linha 1749, coluna 10 em `openParentGate()` — `});return;`
- [x] `if` — linha 1752, coluna 701 em `openParentGate()` — `openModal('Acesso de responsável',\`<div class="parent-gate"><span>🛡️</span><h3>Peça ajuda a um adulto</h3><p>Sem uma conta vinculada, esta verificação libera apenas progresso e backup local. Para controles online protegidos por senha, vincule a conta. Responda:</p><label class="field"><span>Quanto é ${a} × ${b}?</span><input data-parent-answer inputmode="numeric" pattern="[0-9]*" maxlength="3" autocomplete="off"></label><p data-parent-gate-error class="account-error" hidden>Resposta incorreta.</p><button class="btn primary xl" data-parent-unlock>Continuar</button><button class="btn" data-parent-cancel>Cancelar</button></div>\`,root=>{const input=$('[data-parent-answer]',root),unlock=()=>{if(Number(input.value)!==answer){$('[data-parent-gate-error]',root).hidden=false;input.select();return;}openParentTools(inGame);};$('[data-parent-unlock]',root).onclick=unlock;$('[data-parent-cancel]',root).onclick=()=>openSettings(inGame);input.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();unloc`
- [x] `return` — linha 1752, coluna 797 em `openParentGate()` — `openModal('Acesso de responsável',\`<div class="parent-gate"><span>🛡️</span><h3>Peça ajuda a um adulto</h3><p>Sem uma conta vinculada, esta verificação libera apenas progresso e backup local. Para controles online protegidos por senha, vincule a conta. Responda:</p><label class="field"><span>Quanto é ${a} × ${b}?</span><input data-parent-answer inputmode="numeric" pattern="[0-9]*" maxlength="3" autocomplete="off"></label><p data-parent-gate-error class="account-error" hidden>Resposta incorreta.</p><button class="btn primary xl" data-parent-unlock>Continuar</button><button class="btn" data-parent-cancel>Cancelar</button></div>\`,root=>{const input=$('[data-parent-answer]',root),unlock=()=>{if(Number(input.value)!==answer){$('[data-parent-gate-error]',root).hidden=false;input.select();return;}openParentTools(inGame);};$('[data-parent-unlock]',root).onclick=unlock;$('[data-parent-cancel]',root).onclick=()=>openSettings(inGame);input.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();unloc`
- [x] `if` — linha 1752, coluna 961 em `openParentGate()` — `openModal('Acesso de responsável',\`<div class="parent-gate"><span>🛡️</span><h3>Peça ajuda a um adulto</h3><p>Sem uma conta vinculada, esta verificação libera apenas progresso e backup local. Para controles online protegidos por senha, vincule a conta. Responda:</p><label class="field"><span>Quanto é ${a} × ${b}?</span><input data-parent-answer inputmode="numeric" pattern="[0-9]*" maxlength="3" autocomplete="off"></label><p data-parent-gate-error class="account-error" hidden>Resposta incorreta.</p><button class="btn primary xl" data-parent-unlock>Continuar</button><button class="btn" data-parent-cancel>Cancelar</button></div>\`,root=>{const input=$('[data-parent-answer]',root),unlock=()=>{if(Number(input.value)!==answer){$('[data-parent-gate-error]',root).hidden=false;input.select();return;}openParentTools(inGame);};$('[data-parent-unlock]',root).onclick=unlock;$('[data-parent-cancel]',root).onclick=()=>openSettings(inGame);input.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();unloc`
- [x] `if` — linha 1777, coluna 9 em `openSettings()` — `if (key === 'quality') state.settings.quality = requestedQuality() === 'auto' ? 'high' : requestedQuality() === 'high' ? 'low' : 'auto';`
- [x] `else` — linha 1778, coluna 9 em `openSettings()` — `else state.settings[key] = !state.settings[key];`
- [x] `if` — linha 1781, coluna 52 em `openSettings()` — `$('[data-save-now]',root).onclick=async()=>{ if(running) savePlayerPosition(true); else await commitState(); toast('Progresso salvo no celular.','good'); closeModal(); };`
- [x] `else` — linha 1781, coluna 90 em `openSettings()` — `$('[data-save-now]',root).onclick=async()=>{ if(running) savePlayerPosition(true); else await commitState(); toast('Progresso salvo no celular.','good'); closeModal(); };`
- [x] `if` — linha 1782, coluna 46 em `openSettings()` — `const install=$('[data-install]',root);if(install)install.onclick=installApp;`
- [x] `if` — linha 1783, coluna 44 em `openSettings()` — `const home = $('[data-home]', root); if (home) home.onclick = () => { closeModal(); returnHome(); };`
- [x] `if` — linha 1784, coluna 44 em `openSettings()` — `const exit = $('[data-exit]', root); if (exit) exit.onclick = () => { closeModal(); stopGame(); };`
- [x] `if` — linha 1791, coluna 42 em `openSettings()` — `els.challengePromptAccept.onclick=()=>{if(promptSocialRequestId)acceptIncomingSocialRequest(promptSocialRequestId);else if(promptChallengeId)acceptIncomingChallenge(promptChallengeId);else if(promptSessionId){const s=gameSessions.get(promptSessionId);if(s)launchSessionWithCountdown(s);}};`
- [x] `else` — linha 1791, coluna 118 em `openSettings()` — `els.challengePromptAccept.onclick=()=>{if(promptSocialRequestId)acceptIncomingSocialRequest(promptSocialRequestId);else if(promptChallengeId)acceptIncomingChallenge(promptChallengeId);else if(promptSessionId){const s=gameSessions.get(promptSessionId);if(s)launchSessionWithCountdown(s);}};`
- [x] `if` — linha 1791, coluna 123 em `openSettings()` — `els.challengePromptAccept.onclick=()=>{if(promptSocialRequestId)acceptIncomingSocialRequest(promptSocialRequestId);else if(promptChallengeId)acceptIncomingChallenge(promptChallengeId);else if(promptSessionId){const s=gameSessions.get(promptSessionId);if(s)launchSessionWithCountdown(s);}};`
- [x] `else` — linha 1791, coluna 187 em `openSettings()` — `els.challengePromptAccept.onclick=()=>{if(promptSocialRequestId)acceptIncomingSocialRequest(promptSocialRequestId);else if(promptChallengeId)acceptIncomingChallenge(promptChallengeId);else if(promptSessionId){const s=gameSessions.get(promptSessionId);if(s)launchSessionWithCountdown(s);}};`
- [x] `if` — linha 1791, coluna 192 em `openSettings()` — `els.challengePromptAccept.onclick=()=>{if(promptSocialRequestId)acceptIncomingSocialRequest(promptSocialRequestId);else if(promptChallengeId)acceptIncomingChallenge(promptChallengeId);else if(promptSessionId){const s=gameSessions.get(promptSessionId);if(s)launchSessionWithCountdown(s);}};`
- [x] `if` — linha 1791, coluna 254 em `openSettings()` — `els.challengePromptAccept.onclick=()=>{if(promptSocialRequestId)acceptIncomingSocialRequest(promptSocialRequestId);else if(promptChallengeId)acceptIncomingChallenge(promptChallengeId);else if(promptSessionId){const s=gameSessions.get(promptSessionId);if(s)launchSessionWithCountdown(s);}};`
- [x] `if` — linha 1792, coluna 43 em `openSettings()` — `els.challengePromptDecline.onclick=()=>{if(promptSocialRequestId)declineIncomingSocialRequest(promptSocialRequestId);else if(promptChallengeId)declineIncomingChallenge(promptChallengeId);else closeChallengePrompt();};`
- [x] `else` — linha 1792, coluna 120 em `openSettings()` — `els.challengePromptDecline.onclick=()=>{if(promptSocialRequestId)declineIncomingSocialRequest(promptSocialRequestId);else if(promptChallengeId)declineIncomingChallenge(promptChallengeId);else closeChallengePrompt();};`
- [x] `if` — linha 1792, coluna 125 em `openSettings()` — `els.challengePromptDecline.onclick=()=>{if(promptSocialRequestId)declineIncomingSocialRequest(promptSocialRequestId);else if(promptChallengeId)declineIncomingChallenge(promptChallengeId);else closeChallengePrompt();};`
- [x] `else` — linha 1792, coluna 190 em `openSettings()` — `els.challengePromptDecline.onclick=()=>{if(promptSocialRequestId)declineIncomingSocialRequest(promptSocialRequestId);else if(promptChallengeId)declineIncomingChallenge(promptChallengeId);else closeChallengePrompt();};`

## `09-responsive-ar-quality-diagnostics.js`

- [x] `if` — linha 1810, coluna 5 em `syncMobilePanels()` — `if(compact&&state.ui.quickOpen&&state.ui.skillsOpen)state.ui.skillsOpen=false;`
- [x] `if` — linha 1811, coluna 5 em `syncMobilePanels()` — `if(document.body.classList.contains('mode-fishing')){state.ui.quickOpen=false;state.ui.skillsOpen=false;state.ui.needsOpen=false;state.ui.missionOpen=false;els.game?.classList.remove('needs-expanded');els.missionCard?.classList.remove('expanded');}`
- [x] `if` — linha 1813, coluna 143 em `syncMobilePanels()` — `els.skillsToggleBtn?.classList.toggle('active',!!state.ui.skillsOpen);els.quickToggleBtn?.classList.toggle('active',!!state.ui.quickOpen);if(els.quickBar)els.quickBar.hidden=!state.ui.quickOpen;`
- [x] `if` — linha 1815, coluna 81 em `syncMobilePanels()` — `els.quickToggleBtn.onclick = () => { state.ui.quickOpen = !state.ui.quickOpen;if(state.ui.quickOpen)state.ui.skillsOpen=false;syncMobilePanels();saveState(); };`
- [x] `if` — linha 1816, coluna 77 em `syncMobilePanels()` — `els.skillsToggleBtn.onclick=()=>{state.ui.skillsOpen=!state.ui.skillsOpen;if(state.ui.skillsOpen)state.ui.quickOpen=false;syncMobilePanels();saveState();};`
- [x] `if` — linha 1820, coluna 38 em `syncMobilePanels()` — `els.missionCard.onkeydown = e => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); toggleMission(); } };`
- [x] `if` — linha 1823, coluna 5 em `syncMobilePanels()` — `if (!els.nativeViewer || !window.loadModelViewerLib) throw new Error('Visualizador indisponível');`
- [x] `throw` — linha 1823, coluna 58 em `syncMobilePanels()` — `if (!els.nativeViewer || !window.loadModelViewerLib) throw new Error('Visualizador indisponível');`
- [x] `if` — linha 1824, coluna 5 em `syncMobilePanels()` — `if (els.viewerStatus) els.viewerStatus.textContent = 'Carregando visualizador 3D…';`
- [x] `if` — linha 1825, coluna 5 em `syncMobilePanels()` — `if (els.viewerLoadBtn) els.viewerLoadBtn.disabled = true;`
- [x] `if` — linha 1833, coluna 7 em `syncMobilePanels()` — `if (!els.nativeViewer.loaded) {`
- [x] `if` — linha 1845, coluna 7 em `syncMobilePanels()` — `if (els.viewerPlaceholder) els.viewerPlaceholder.hidden = true;`
- [x] `if` — linha 1846, coluna 7 em `syncMobilePanels()` — `if (els.viewerShell) els.viewerShell.classList.add('viewer-ready');`
- [x] `if` — linha 1847, coluna 7 em `syncMobilePanels()` — `if (activateAR) await els.nativeViewer.activateAR();`
- [x] `return` — linha 1848, coluna 7 em `syncMobilePanels()` — `return true;`
- [x] `catch` — linha 1849, coluna 7 em `syncMobilePanels()` — `} catch (error) {`
- [x] `if` — linha 1850, coluna 7 em `syncMobilePanels()` — `if (els.viewerPlaceholder) els.viewerPlaceholder.hidden = false;`
- [x] `if` — linha 1851, coluna 7 em `syncMobilePanels()` — `if (els.viewerStatus) els.viewerStatus.textContent = 'Não foi possível carregar. Toque para tentar novamente.';`
- [x] `throw` — linha 1852, coluna 7 em `syncMobilePanels()` — `throw error;`
- [x] `if` — linha 1854, coluna 7 em `syncMobilePanels()` — `if (els.viewerLoadBtn) els.viewerLoadBtn.disabled = false;`
- [x] `return` — linha 1858, coluna 5 em `otthiGameModelUrl()` — `return new URL('athos.glb', OTTHI_GAME_LIVE_BASE).href;`
- [x] `return` — linha 1868, coluna 5 em `androidSceneViewerUrl()` — `return sceneViewer.href;`
- [x] `return` — linha 1872, coluna 5 em `isAndroidDevice()` — `return /android/i.test(navigator.userAgent || '');`
- [x] `if` — linha 1876, coluna 5 em `openAndroidSceneViewer()` — `if (!isAndroidDevice()) return false;`
- [x] `return` — linha 1876, coluna 29 em `openAndroidSceneViewer()` — `if (!isAndroidDevice()) return false;`
- [x] `return` — linha 1878, coluna 5 em `openAndroidSceneViewer()` — `return true;`
- [x] `if` — linha 1881, coluna 3 em `openAndroidSceneViewer()` — `if (els.viewerLoadBtn) els.viewerLoadBtn.onclick = () => ensureModelViewerReady().catch(() => toast('Visualizador 3D indisponível agora.','warn',2400));`
- [x] `catch` — linha 1881, coluna 85 em `openAndroidSceneViewer()` — `if (els.viewerLoadBtn) els.viewerLoadBtn.onclick = () => ensureModelViewerReady().catch(() => toast('Visualizador 3D indisponível agora.','warn',2400));`
- [x] `if` — linha 1882, coluna 3 em `openAndroidSceneViewer()` — `if (els.insideArBtn) els.insideArBtn.addEventListener('click', event => {`
- [x] `if` — linha 1883, coluna 5 em `openAndroidSceneViewer()` — `if (!isAndroidDevice()) return;`
- [x] `return` — linha 1883, coluna 29 em `openAndroidSceneViewer()` — `if (!isAndroidDevice()) return;`
- [x] `if` — linha 1889, coluna 5 em `openAndroidSceneViewer()` — `if (openAndroidSceneViewer()) return;`
- [x] `return` — linha 1889, coluna 35 em `openAndroidSceneViewer()` — `if (openAndroidSceneViewer()) return;`
- [x] `catch` — linha 1891, coluna 5 em `openAndroidSceneViewer()` — `catch { openModal('Realidade aumentada', '<p>Não foi possível iniciar o AR agora. Verifique a internet e o suporte do aparelho e tente novamente.</p>'); }`
- [x] `if` — linha 1919, coluna 5 em `detectStableAutoTier()` — `if(memory<=4||cores<=4)return 'low';`
- [x] `return` — linha 1919, coluna 28 em `detectStableAutoTier()` — `if(memory<=4||cores<=4)return 'low';`
- [x] `if` — linha 1920, coluna 5 em `detectStableAutoTier()` — `if(!mobile&&memory>=8&&cores>=8)return 'high';`
- [x] `return` — linha 1920, coluna 37 em `detectStableAutoTier()` — `if(!mobile&&memory>=8&&cores>=8)return 'high';`
- [x] `return` — linha 1921, coluna 5 em `detectStableAutoTier()` — `return 'balanced';`
- [x] `if` — linha 1939, coluna 5 em `vehiclePlayerMode()` — `if(kind==='motorcycle')return player.car.passengerOf?PLAYER_MODES.MOTORCYCLE_PASSENGER:PLAYER_MODES.MOTORCYCLE_DRIVER;`
- [x] `return` — linha 1939, coluna 28 em `vehiclePlayerMode()` — `if(kind==='motorcycle')return player.car.passengerOf?PLAYER_MODES.MOTORCYCLE_PASSENGER:PLAYER_MODES.MOTORCYCLE_DRIVER;`
- [x] `if` — linha 1940, coluna 5 em `vehiclePlayerMode()` — `if(kind==='bicycle')return PLAYER_MODES.BICYCLE;`
- [x] `return` — linha 1940, coluna 25 em `vehiclePlayerMode()` — `if(kind==='bicycle')return PLAYER_MODES.BICYCLE;`
- [x] `if` — linha 1941, coluna 5 em `vehiclePlayerMode()` — `if(kind==='skate')return PLAYER_MODES.SKATE;`
- [x] `return` — linha 1941, coluna 23 em `vehiclePlayerMode()` — `if(kind==='skate')return PLAYER_MODES.SKATE;`
- [x] `return` — linha 1942, coluna 5 em `vehiclePlayerMode()` — `return player.car.passengerOf?PLAYER_MODES.CAR_PASSENGER:PLAYER_MODES.CAR_DRIVER;`
- [x] `if` — linha 1945, coluna 5 em `derivePlayerMode()` — `if(player.transit.mode==='metro')return PLAYER_MODES.METRO_PASSENGER;`
- [x] `return` — linha 1945, coluna 38 em `derivePlayerMode()` — `if(player.transit.mode==='metro')return PLAYER_MODES.METRO_PASSENGER;`
- [x] `if` — linha 1946, coluna 5 em `derivePlayerMode()` — `if(player.transit.mode==='bus')return PLAYER_MODES.BUS_PASSENGER;`
- [x] `return` — linha 1946, coluna 36 em `derivePlayerMode()` — `if(player.transit.mode==='bus')return PLAYER_MODES.BUS_PASSENGER;`
- [x] `if` — linha 1947, coluna 5 em `derivePlayerMode()` — `if(player.boating)return player.boat.passengerOf?PLAYER_MODES.BOAT_PASSENGER:PLAYER_MODES.BOAT_DRIVER;`
- [x] `return` — linha 1947, coluna 23 em `derivePlayerMode()` — `if(player.boating)return player.boat.passengerOf?PLAYER_MODES.BOAT_PASSENGER:PLAYER_MODES.BOAT_DRIVER;`
- [x] `if` — linha 1948, coluna 5 em `derivePlayerMode()` — `if(player.vehicle)return vehiclePlayerMode();`
- [x] `return` — linha 1948, coluna 23 em `derivePlayerMode()` — `if(player.vehicle)return vehiclePlayerMode();`
- [x] `if` — linha 1949, coluna 5 em `derivePlayerMode()` — `if(currentHouse)return PLAYER_MODES.INTERIOR;`
- [x] `return` — linha 1949, coluna 21 em `derivePlayerMode()` — `if(currentHouse)return PLAYER_MODES.INTERIOR;`
- [x] `if` — linha 1950, coluna 5 em `derivePlayerMode()` — `if(buildMode)return PLAYER_MODES.BUILDING;`
- [x] `return` — linha 1950, coluna 18 em `derivePlayerMode()` — `if(buildMode)return PLAYER_MODES.BUILDING;`
- [x] `if` — linha 1951, coluna 5 em `derivePlayerMode()` — `if(fishingSession||fishingVisual?.active)return PLAYER_MODES.FISHING;`
- [x] `return` — linha 1951, coluna 46 em `derivePlayerMode()` — `if(fishingSession||fishingVisual?.active)return PLAYER_MODES.FISHING;`
- [x] `if` — linha 1952, coluna 5 em `derivePlayerMode()` — `if(performance.now()<Number(player.emoteUntil||0))return PLAYER_MODES.SOCIAL_ACTION;`
- [x] `return` — linha 1952, coluna 55 em `derivePlayerMode()` — `if(performance.now()<Number(player.emoteUntil||0))return PLAYER_MODES.SOCIAL_ACTION;`
- [x] `return` — linha 1953, coluna 5 em `derivePlayerMode()` — `return sprintRequested?.()&&Math.hypot(input.x,input.z)>.12?PLAYER_MODES.RUNNING:PLAYER_MODES.WALKING;`
- [x] `if` — linha 1957, coluna 5 em `auditPlayerMode()` — `if(player.vehicle)exclusive.push('vehicle');`
- [x] `if` — linha 1958, coluna 5 em `auditPlayerMode()` — `if(player.boating)exclusive.push('boat');`
- [x] `if` — linha 1959, coluna 5 em `auditPlayerMode()` — `if(player.transit.mode)exclusive.push(\`transit:${player.transit.mode}\`);`
- [x] `if` — linha 1960, coluna 5 em `auditPlayerMode()` — `if(currentHouse)exclusive.push('interior');`
- [x] `if` — linha 1961, coluna 5 em `auditPlayerMode()` — `if(buildMode)exclusive.push('building');`
- [x] `if` — linha 1962, coluna 5 em `auditPlayerMode()` — `if(fishingSession||fishingVisual?.active)exclusive.push('fishing');`
- [x] `if` — linha 1965, coluna 5 em `auditPlayerMode()` — `if(next!==playerModeMachine.state){`
- [x] `return` — linha 1969, coluna 5 em `auditPlayerMode()` — `return{state:playerModeMachine.state,conflicts:[...playerModeMachine.conflicts],valid:playerModeMachine.conflicts.length===0};`
- [x] `return` — linha 1971, coluna 50 em `isOnFootMode()` — `function isOnFootMode(mode=derivePlayerMode()){return mode===PLAYER_MODES.WALKING||mode===PLAYER_MODES.RUNNING;}`
- [x] `if` — linha 1974, coluna 5 em `canEnterMobility()` — `if(!audit.valid){console.warn('[OTTHOS] Estado de mobilidade conflitante bloqueado',audit);return false;}`
- [x] `return` — linha 1974, coluna 96 em `canEnterMobility()` — `if(!audit.valid){console.warn('[OTTHOS] Estado de mobilidade conflitante bloqueado',audit);return false;}`
- [x] `return` — linha 1975, coluna 5 em `canEnterMobility()` — `return isOnFootMode(audit.state);`
- [x] `return` — linha 1980, coluna 5 em `cameraRelativeVector()` — `return{x:rightX*nx+forwardX*nz,z:rightZ*nx+forwardZ*nz};`
- [x] `if` — linha 1985, coluna 5 em `normalizeControlIntent()` — `if(disabled)return{x:0,z:0,moveX:0,moveZ:0,steer:0,throttle:0,disabled:true,mode};`
- [x] `return` — linha 1985, coluna 17 em `normalizeControlIntent()` — `if(disabled)return{x:0,z:0,moveX:0,moveZ:0,steer:0,throttle:0,disabled:true,mode};`
- [x] `if` — linha 1986, coluna 5 em `normalizeControlIntent()` — `if([PLAYER_MODES.CAR_DRIVER,PLAYER_MODES.MOTORCYCLE_DRIVER,PLAYER_MODES.BICYCLE,PLAYER_MODES.SKATE,PLAYER_MODES.BOAT_DRIVER].includes(mode))return{x,z,moveX:0,moveZ:0,steer:x,throttle:z,disabled:false,mode};`
- [x] `return` — linha 1986, coluna 145 em `normalizeControlIntent()` — `if([PLAYER_MODES.CAR_DRIVER,PLAYER_MODES.MOTORCYCLE_DRIVER,PLAYER_MODES.BICYCLE,PLAYER_MODES.SKATE,PLAYER_MODES.BOAT_DRIVER].includes(mode))return{x,z,moveX:0,moveZ:0,steer:x,throttle:z,disabled:false,mode};`
- [x] `return` — linha 1988, coluna 5 em `normalizeControlIntent()` — `return{x,z,moveX:worldVector.x,moveZ:worldVector.z,steer:0,throttle:0,disabled:false,mode};`
- [x] `return` — linha 1990, coluna 31 em `requestedQuality()` — `function requestedQuality(){return ['high','low','auto'].includes(state.settings.quality)?state.settings.quality:'auto';}`
- [x] `return` — linha 1991, coluna 27 em `qualityLabel()` — `function qualityLabel(){return requestedQuality()==='high'?'Alta':requestedQuality()==='low'?'Econômica':\`Automática fixa nesta partida • ${qualityTier()==='high'?'Alta':qualityTier()==='low'?'Econômica':'Equilibrada'}\`;}`
- [x] `return` — linha 1992, coluna 61 em `qualityTier()` — `function qualityTier(){const requested=requestedQuality();return requested==='high'?'high':requested==='low'?'low':perf.sessionTier;}`
- [x] `if` — linha 1995, coluna 5 em `targetDpr()` — `if(tier==='high') return mobile?1.0:1.35;`
- [x] `return` — linha 1995, coluna 23 em `targetDpr()` — `if(tier==='high') return mobile?1.0:1.35;`
- [x] `if` — linha 1996, coluna 5 em `targetDpr()` — `if(tier==='low') return mobile?.68:.95;`
- [x] `return` — linha 1996, coluna 22 em `targetDpr()` — `if(tier==='low') return mobile?.68:.95;`
- [x] `return` — linha 1997, coluna 5 em `targetDpr()` — `return mobile?.78:1.08;`
- [x] `if` — linha 2000, coluna 5 em `applyAdaptiveRenderSettings()` — `if(!renderer)return;`
- [x] `return` — linha 2000, coluna 18 em `applyAdaptiveRenderSettings()` — `if(!renderer)return;`
- [x] `if` — linha 2002, coluna 5 em `applyAdaptiveRenderSettings()` — `if(force||!running||Math.abs(perf.appliedDpr-dpr)>.08){renderer.setPixelRatio(dpr);perf.appliedDpr=dpr;}`
- [x] `if` — linha 2005, coluna 5 em `applyAdaptiveRenderSettings()` — `if(sunLight){if(!perf.appliedTier){const size=tier==='high'?(perf.mobile?1024:1536):tier==='balanced'?768:512;sunLight.shadow.mapSize.set(size,size);}sunLight.castShadow=tier==='high'&&!perf.mobile;}`
- [x] `if` — linha 2005, coluna 18 em `applyAdaptiveRenderSettings()` — `if(sunLight){if(!perf.appliedTier){const size=tier==='high'?(perf.mobile?1024:1536):tier==='balanced'?768:512;sunLight.shadow.mapSize.set(size,size);}sunLight.castShadow=tier==='high'&&!perf.mobile;}`
- [x] `if` — linha 2011, coluna 5 em `samplePerformance()` — `if(perf.sampleMs<3)return;`
- [x] `return` — linha 2011, coluna 24 em `samplePerformance()` — `if(perf.sampleMs<3)return;`
- [x] `if` — linha 2013, coluna 5 em `samplePerformance()` — `if(requestedQuality()!=='auto')return;`
- [x] `return` — linha 2013, coluna 36 em `samplePerformance()` — `if(requestedQuality()!=='auto')return;`
- [x] `if` — linha 2015, coluna 5 em `samplePerformance()` — `if(recommendation===perf.recommendation)perf.recommendationSamples++;else{perf.recommendation=recommendation;perf.recommendationSamples=1;}`
- [x] `else` — linha 2015, coluna 74 em `samplePerformance()` — `if(recommendation===perf.recommendation)perf.recommendationSamples++;else{perf.recommendation=recommendation;perf.recommendationSamples=1;}`
- [x] `if` — linha 2019, coluna 5 em `samplePerformance()` — `if(perf.lowSamples>=2&&perf.sessionTier!=='low'){`
- [x] `else` — linha 2022, coluna 6 em `samplePerformance()` — `}else if(!perf.mobile&&perf.highSamples>=7&&perf.sessionTier!=='high'){`
- [x] `if` — linha 2022, coluna 11 em `samplePerformance()` — `}else if(!perf.mobile&&perf.highSamples>=7&&perf.sessionTier!=='high'){`
- [x] `if` — linha 2025, coluna 5 em `samplePerformance()` — `if(perf.recommendationSamples>=5&&recommendation!==state.settings.autoTier&&performance.now()-perf.lastRecommendationSaved>30000){`
- [x] `for` — linha 2030, coluna 5 em `lockStableSceneVisibility()` — `for(const mesh of world.criticalSurfaces){if(!mesh)continue;mesh.visible=true;mesh.frustumCulled=false;}`
- [x] `if` — linha 2030, coluna 47 em `lockStableSceneVisibility()` — `for(const mesh of world.criticalSurfaces){if(!mesh)continue;mesh.visible=true;mesh.frustumCulled=false;}`
- [x] `for` — linha 2031, coluna 5 em `lockStableSceneVisibility()` — `for(const line of world.outlines){if(line?.parent)line.visible=true;}`
- [x] `if` — linha 2031, coluna 39 em `lockStableSceneVisibility()` — `for(const line of world.outlines){if(line?.parent)line.visible=true;}`
- [x] `for` — linha 2032, coluna 65 em `lockStableSceneVisibility()` — `const glowVisible=visualQualityProfile(qualityTier()).glows;for(const light of world.glows){if(light?.parent)light.visible=glowVisible;}`
- [x] `if` — linha 2032, coluna 97 em `lockStableSceneVisibility()` — `const glowVisible=visualQualityProfile(qualityTier()).glows;for(const light of world.glows){if(light?.parent)light.visible=glowVisible;}`
- [x] `if` — linha 2033, coluna 5 em `lockStableSceneVisibility()` — `if(world.clouds){const max=qualityTier()==='high'?8:qualityTier()==='balanced'?6:4;world.clouds.forEach((cloud,i)=>cloud.group.visible=i<max);}`
- [x] `if` — linha 2037, coluna 5 em `freezeWorldFrustumCulling()` — `if(!worldGroup)return;`
- [x] `return` — linha 2037, coluna 20 em `freezeWorldFrustumCulling()` — `if(!worldGroup)return;`
- [x] `if` — linha 2040, coluna 7 em `freezeWorldFrustumCulling()` — `if(!(obj.isMesh||obj.isLine||obj.isLineSegments||obj.isPoints||obj.isSprite))return;`
- [x] `return` — linha 2040, coluna 84 em `freezeWorldFrustumCulling()` — `if(!(obj.isMesh||obj.isLine||obj.isLineSegments||obj.isPoints||obj.isSprite))return;`
- [x] `if` — linha 2042, coluna 7 em `freezeWorldFrustumCulling()` — `if(obj.geometry&&!obj.geometry.boundingSphere){try{obj.geometry.computeBoundingSphere();}catch(_){}}`
- [x] `catch` — linha 2042, coluna 96 em `freezeWorldFrustumCulling()` — `if(obj.geometry&&!obj.geometry.boundingSphere){try{obj.geometry.computeBoundingSphere();}catch(_){}}`
- [x] `if` — linha 2048, coluna 7 em `freezeWorldFrustumCulling()` — `if(obj.frustumCulled)enabled++;else bypassed++;`
- [x] `else` — linha 2048, coluna 38 em `freezeWorldFrustumCulling()` — `if(obj.frustumCulled)enabled++;else bypassed++;`
- [x] `for` — linha 2055, coluna 5 em `updateVisualLOD()` — `for(const mesh of world.criticalSurfaces){if(mesh&&!mesh.visible)mesh.visible=true;}`
- [x] `if` — linha 2055, coluna 47 em `updateVisualLOD()` — `for(const mesh of world.criticalSurfaces){if(mesh&&!mesh.visible)mesh.visible=true;}`
- [x] `return` — linha 2059, coluna 27 em `pwaInstalled()` — `function pwaInstalled(){return matchMedia('(display-mode: standalone)').matches||navigator.standalone===true;}`
- [x] `return` — linha 2060, coluna 33 em `activeVehicleCount()` — `function activeVehicleCount(){return world.vehicles.filter(v=>v?.group?.visible!==false).length+world.buses.filter(v=>v?.group?.visible!==false).length+world.policeCars.filter(v=>v?.group?.visible!==false).length+world.fireTrucks.filter(v=>v?.group?.visible!==false).length+world.ambulances.filter(v=>v?.group?.visible!==false).length;}`
- [x] `return` — linha 2063, coluna 5 em `runtimeDiagnostics()` — `return{version:APP_VERSION,running,paused,mode:mode.state,modeValid:mode.valid,modeConflicts:mode.conflicts,fps:+perf.fps.toFixed(1),frameMs:+(1000/Math.max(1,perf.fps)).toFixed(1),drawCalls:Number(render.calls||0),triangles:Number(render.triangles||0),geometries:Number(memory.geometries||0),textures:Number(memory.textures||0),npcs:world.npcs.length,vehicles:activeVehicleCount(),aiTicks:perf.aiTicks,trafficTicks:perf.trafficTicks,culling:{enabled:perf.cullingEnabled,bypassed:perf.cullingBypassed,total:world.staticRenderObjects||0},visual:visualFoundationDiagnostics(),avatar:avatarFoundationDiagnostics(),pwaInstalled:pwaInstalled(),online:navigator.onLine,browser:navigator.userAgent,save:{version:state.version,lastSaved:Number(state.lastSaved||0),database:window.OTTHOS_DB?.name||'',schema:window.OTTHOS_DB?.schema||0},multiplayer:window.OTTHOS_RTDB?.status?.()||{configured:false,connected:false}};`
- [x] `if` — linha 2066, coluna 5 em `ensureTechnicalPanel()` — `if(technicalPanel)return technicalPanel;const style=document.createElement('style');style.id='otthosTechnicalPanelStyle';style.textContent='.otthos-tech-panel{position:fixed;z-index:100000;right:max(8px,env(safe-area-inset-right));top:max(8px,env(safe-area-inset-top));width:min(330px,calc(100vw - 16px));max-height:calc(100vh - 16px);overflow:auto;padding:12px;border:1px solid rgba(116,220,255,.65);border-radius:14px;background:rgba(4,13,25,.94);color:#eaf8ff;font:700 12px/1.45 ui-monospace,SFMono-Regular,Consolas,monospace;box-shadow:0 16px 45px rgba(0,0,0,.45);backdrop-filter:blur(10px)}.otthos-tech-panel[hidden]{display:none!important}.otthos-tech-panel header{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-bottom:8px}.otthos-tech-panel h2{font:900 14px/1.2 system-ui,sans-serif;margin:0}.otthos-tech-panel button{border:0;border-radius:8px;background:#dff7ff;color:#082032;padding:5px 9px;font-weight:900}.otthos-tech-panel pre{white-space:pre-wrap;word-brea`
- [x] `return` — linha 2066, coluna 23 em `ensureTechnicalPanel()` — `if(technicalPanel)return technicalPanel;const style=document.createElement('style');style.id='otthosTechnicalPanelStyle';style.textContent='.otthos-tech-panel{position:fixed;z-index:100000;right:max(8px,env(safe-area-inset-right));top:max(8px,env(safe-area-inset-top));width:min(330px,calc(100vw - 16px));max-height:calc(100vh - 16px);overflow:auto;padding:12px;border:1px solid rgba(116,220,255,.65);border-radius:14px;background:rgba(4,13,25,.94);color:#eaf8ff;font:700 12px/1.45 ui-monospace,SFMono-Regular,Consolas,monospace;box-shadow:0 16px 45px rgba(0,0,0,.45);backdrop-filter:blur(10px)}.otthos-tech-panel[hidden]{display:none!important}.otthos-tech-panel header{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-bottom:8px}.otthos-tech-panel h2{font:900 14px/1.2 system-ui,sans-serif;margin:0}.otthos-tech-panel button{border:0;border-radius:8px;background:#dff7ff;color:#082032;padding:5px 9px;font-weight:900}.otthos-tech-panel pre{white-space:pre-wrap;word-brea`
- [x] `return` — linha 2066, coluna 1550 em `ensureTechnicalPanel()` — `if(technicalPanel)return technicalPanel;const style=document.createElement('style');style.id='otthosTechnicalPanelStyle';style.textContent='.otthos-tech-panel{position:fixed;z-index:100000;right:max(8px,env(safe-area-inset-right));top:max(8px,env(safe-area-inset-top));width:min(330px,calc(100vw - 16px));max-height:calc(100vh - 16px);overflow:auto;padding:12px;border:1px solid rgba(116,220,255,.65);border-radius:14px;background:rgba(4,13,25,.94);color:#eaf8ff;font:700 12px/1.45 ui-monospace,SFMono-Regular,Consolas,monospace;box-shadow:0 16px 45px rgba(0,0,0,.45);backdrop-filter:blur(10px)}.otthos-tech-panel[hidden]{display:none!important}.otthos-tech-panel header{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-bottom:8px}.otthos-tech-panel h2{font:900 14px/1.2 system-ui,sans-serif;margin:0}.otthos-tech-panel button{border:0;border-radius:8px;background:#dff7ff;color:#082032;padding:5px 9px;font-weight:900}.otthos-tech-panel pre{white-space:pre-wrap;word-brea`
- [x] `if` — linha 2068, coluna 36 em `refreshTechnicalPanel()` — `function refreshTechnicalPanel(){if(!technicalPanelVisible)return;const panel=ensureTechnicalPanel(),d=runtimeDiagnostics();panel.querySelector('[data-tech-data]').textContent=[\`FPS: ${d.fps} • frame: ${d.frameMs} ms\`,\`Draw calls: ${d.drawCalls} • triângulos: ${d.triangles}\`,\`Geometrias: ${d.geometries} • texturas: ${d.textures}\`,\`Materiais cache: ${d.visual.materials.immutable} • acertos: ${d.visual.materials.hits}\`,\`LOD: ${d.visual.lod.registered} • perto: ${d.visual.lod.near} • longe: ${d.visual.lod.far}\`,\`Contornos: ${d.visual.outlines.visible} visíveis • ${d.visual.outlines.hidden} distantes\`,\`NPCs: ${d.npcs} • veículos ativos: ${d.vehicles}\`,\`IA: ${d.aiTicks} ticks • trânsito: ${d.trafficTicks} ticks\`,\`Culling: ${d.culling.enabled} ativo • ${d.culling.bypassed} protegido\`,\`Avatar: schema V${d.avatar.stateVersion} • fallback ${d.avatar.fallbackActive?'ativo':'inativo'}\`,\`Modo: ${d.mode}${d.modeValid?'':' • CONFLITO '+d.modeConflicts.join(', ')}\`,\`Qualidade: ${qualityTier()} • DPR:`
- [x] `return` — linha 2068, coluna 62 em `refreshTechnicalPanel()` — `function refreshTechnicalPanel(){if(!technicalPanelVisible)return;const panel=ensureTechnicalPanel(),d=runtimeDiagnostics();panel.querySelector('[data-tech-data]').textContent=[\`FPS: ${d.fps} • frame: ${d.frameMs} ms\`,\`Draw calls: ${d.drawCalls} • triângulos: ${d.triangles}\`,\`Geometrias: ${d.geometries} • texturas: ${d.textures}\`,\`Materiais cache: ${d.visual.materials.immutable} • acertos: ${d.visual.materials.hits}\`,\`LOD: ${d.visual.lod.registered} • perto: ${d.visual.lod.near} • longe: ${d.visual.lod.far}\`,\`Contornos: ${d.visual.outlines.visible} visíveis • ${d.visual.outlines.hidden} distantes\`,\`NPCs: ${d.npcs} • veículos ativos: ${d.vehicles}\`,\`IA: ${d.aiTicks} ticks • trânsito: ${d.trafficTicks} ticks\`,\`Culling: ${d.culling.enabled} ativo • ${d.culling.bypassed} protegido\`,\`Avatar: schema V${d.avatar.stateVersion} • fallback ${d.avatar.fallbackActive?'ativo':'inativo'}\`,\`Modo: ${d.mode}${d.modeValid?'':' • CONFLITO '+d.modeConflicts.join(', ')}\`,\`Qualidade: ${qualityTier()} • DPR:`
- [x] `if` — linha 2069, coluna 187 em `toggleTechnicalPanel()` — `function toggleTechnicalPanel(force){technicalPanelVisible=typeof force==='boolean'?force:!technicalPanelVisible;const panel=ensureTechnicalPanel();panel.hidden=!technicalPanelVisible;if(technicalPanelVisible)refreshTechnicalPanel();}`
- [x] `if` — linha 2071, coluna 47 em `initTechnicalPanel()` — `window.addEventListener('keydown',event=>{if(event.code==='F3'){event.preventDefault();toggleTechnicalPanel();}});`
- [x] `if` — linha 2072, coluna 209 em `initTechnicalPanel()` — `document.querySelector('.brand-card')?.addEventListener('pointerup',()=>{technicalPanelTapCount++;clearTimeout(technicalPanelTapTimer);technicalPanelTapTimer=setTimeout(()=>technicalPanelTapCount=0,1400);if(technicalPanelTapCount>=5){technicalPanelTapCount=0;toggleTechnicalPanel();}});`

## `10-player-skills.js`

- [x] `return` — linha 2076, coluna 56 em `playerScaleValue()` — `function playerScaleValue(mode = player.scaleMode) { return mode === 'mini' ? .58 : mode === 'giant' ? 1.42 : 1; }`
- [x] `if` — linha 2079, coluna 98 em `skillReady()` — `function skillReady(id){const left=Math.max(0,Number(skillCooldowns[id]||0)-performance.now());if(left>0){toast(\`Skill recarregando: ${Math.ceil(left/1000)} s.\`,'warn',1200);return false;}return true;}`
- [x] `return` — linha 2079, coluna 177 em `skillReady()` — `function skillReady(id){const left=Math.max(0,Number(skillCooldowns[id]||0)-performance.now());if(left>0){toast(\`Skill recarregando: ${Math.ceil(left/1000)} s.\`,'warn',1200);return false;}return true;}`
- [x] `return` — linha 2079, coluna 191 em `skillReady()` — `function skillReady(id){const left=Math.max(0,Number(skillCooldowns[id]||0)-performance.now());if(left>0){toast(\`Skill recarregando: ${Math.ceil(left/1000)} s.\`,'warn',1200);return false;}return true;}`
- [x] `for` — linha 2084, coluna 5 em `recordAdvancedSkill()` — `for(let elapsed=1000;elapsed<=cooldownMs+50;elapsed+=1000)setTimeout(updateAbilityUI,elapsed);setTimeout(updateAbilityUI,cooldownMs+60);`
- [x] `if` — linha 2087, coluna 5 em `collectCrystal()` — `if(!c||c.got)return false;c.got=true;c.mesh.visible=false;state.inventory.crystals++;state.stats.collected++;trackDaily('collect',1);addXP(15);addCoins(5);toast(message,'good');beep(880);vibrate(20);evaluateMissions();checkActiveJob();saveState();return true;`
- [x] `return` — linha 2087, coluna 18 em `collectCrystal()` — `if(!c||c.got)return false;c.got=true;c.mesh.visible=false;state.inventory.crystals++;state.stats.collected++;trackDaily('collect',1);addXP(15);addCoins(5);toast(message,'good');beep(880);vibrate(20);evaluateMissions();checkActiveJob();saveState();return true;`
- [x] `return` — linha 2087, coluna 252 em `collectCrystal()` — `if(!c||c.got)return false;c.got=true;c.mesh.visible=false;state.inventory.crystals++;state.stats.collected++;trackDaily('collect',1);addXP(15);addCoins(5);toast(message,'good');beep(880);vibrate(20);evaluateMissions();checkActiveJob();saveState();return true;`
- [x] `if` — linha 2090, coluna 5 em `syncPlayerRootScale()` — `if(!playerGroup)return;`
- [x] `return` — linha 2090, coluna 21 em `syncPlayerRootScale()` — `if(!playerGroup)return;`
- [x] `if` — linha 2091, coluna 5 em `syncPlayerRootScale()` — `if(player.vehicle){`
- [x] `return` — linha 2094, coluna 7 em `syncPlayerRootScale()` — `return;`
- [x] `if` — linha 2100, coluna 5 em `setScaleMode()` — `if(!els.modal.hidden||paused||player.vehicle||player.boating||player.transit.mode)return;`
- [x] `return` — linha 2100, coluna 87 em `setScaleMode()` — `if(!els.modal.hidden||paused||player.vehicle||player.boating||player.transit.mode)return;`
- [x] `if` — linha 2101, coluna 5 em `setScaleMode()` — `if (!['mini','normal','giant'].includes(mode)) return;`
- [x] `return` — linha 2101, coluna 52 em `setScaleMode()` — `if (!['mini','normal','giant'].includes(mode)) return;`
- [x] `if` — linha 2102, coluna 5 em `setScaleMode()` — `if(mode===player.scaleMode){`
- [x] `if` — linha 2103, coluna 7 em `setScaleMode()` — `if(mode==='mini'){`
- [x] `if` — linha 2104, coluna 9 em `setScaleMode()` — `if(!skillReady('miniDash'))return;player.skillDashUntil=performance.now()+1250;recordAdvancedSkill('miniDash',4200);toast('Dash Mini ativado!','good',1400);beep(760,70,'sine');`
- [x] `return` — linha 2104, coluna 36 em `setScaleMode()` — `if(!skillReady('miniDash'))return;player.skillDashUntil=performance.now()+1250;recordAdvancedSkill('miniDash',4200);toast('Dash Mini ativado!','good',1400);beep(760,70,'sine');`
- [x] `else` — linha 2105, coluna 8 em `setScaleMode()` — `}else if(mode==='normal'){`
- [x] `if` — linha 2105, coluna 13 em `setScaleMode()` — `}else if(mode==='normal'){`
- [x] `if` — linha 2106, coluna 9 em `setScaleMode()` — `if(!player.grounded){toast('Pouse antes de usar o Super Pulo.','warn');return;}if(!skillReady('superJump'))return;state.stats.jumps++;trackDaily('jump',1);player.vy=15;player.grounded=false;recordAdvancedSkill('superJump',4600);toast('Super Pulo!','good',1300);beep(820,80,'sine');vibrate(24);`
- [x] `return` — linha 2106, coluna 80 em `setScaleMode()` — `if(!player.grounded){toast('Pouse antes de usar o Super Pulo.','warn');return;}if(!skillReady('superJump'))return;state.stats.jumps++;trackDaily('jump',1);player.vy=15;player.grounded=false;recordAdvancedSkill('superJump',4600);toast('Super Pulo!','good',1300);beep(820,80,'sine');vibrate(24);`
- [x] `if` — linha 2106, coluna 88 em `setScaleMode()` — `if(!player.grounded){toast('Pouse antes de usar o Super Pulo.','warn');return;}if(!skillReady('superJump'))return;state.stats.jumps++;trackDaily('jump',1);player.vy=15;player.grounded=false;recordAdvancedSkill('superJump',4600);toast('Super Pulo!','good',1300);beep(820,80,'sine');vibrate(24);`
- [x] `return` — linha 2106, coluna 116 em `setScaleMode()` — `if(!player.grounded){toast('Pouse antes de usar o Super Pulo.','warn');return;}if(!skillReady('superJump'))return;state.stats.jumps++;trackDaily('jump',1);player.vy=15;player.grounded=false;recordAdvancedSkill('superJump',4600);toast('Super Pulo!','good',1300);beep(820,80,'sine');vibrate(24);`
- [x] `else` — linha 2107, coluna 8 em `setScaleMode()` — `}else{`
- [x] `if` — linha 2108, coluna 9 em `setScaleMode()` — `if(!skillReady('giantSlam'))return;player.damageUntil=performance.now()+900;for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<5.4)damageEnemy(enemy,2);for(let i=0;i<9;i++){const a=i/9*Math.PI*2;spawnDust(player.x+Math.sin(a)*1.8,player.z+Math.cos(a)*1.8);}recordAdvancedSkill('giantSlam',6000);toast('Impacto Gigante!','good',1500);beep(125,130,'sawtooth');vibrate([45,25,60]);`
- [x] `return` — linha 2108, coluna 37 em `setScaleMode()` — `if(!skillReady('giantSlam'))return;player.damageUntil=performance.now()+900;for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<5.4)damageEnemy(enemy,2);for(let i=0;i<9;i++){const a=i/9*Math.PI*2;spawnDust(player.x+Math.sin(a)*1.8,player.z+Math.cos(a)*1.8);}recordAdvancedSkill('giantSlam',6000);toast('Impacto Gigante!','good',1500);beep(125,130,'sawtooth');vibrate([45,25,60]);`
- [x] `for` — linha 2108, coluna 85 em `setScaleMode()` — `if(!skillReady('giantSlam'))return;player.damageUntil=performance.now()+900;for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<5.4)damageEnemy(enemy,2);for(let i=0;i<9;i++){const a=i/9*Math.PI*2;spawnDust(player.x+Math.sin(a)*1.8,player.z+Math.cos(a)*1.8);}recordAdvancedSkill('giantSlam',6000);toast('Impacto Gigante!','good',1500);beep(125,130,'sawtooth');vibrate([45,25,60]);`
- [x] `if` — linha 2108, coluna 118 em `setScaleMode()` — `if(!skillReady('giantSlam'))return;player.damageUntil=performance.now()+900;for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<5.4)damageEnemy(enemy,2);for(let i=0;i<9;i++){const a=i/9*Math.PI*2;spawnDust(player.x+Math.sin(a)*1.8,player.z+Math.cos(a)*1.8);}recordAdvancedSkill('giantSlam',6000);toast('Impacto Gigante!','good',1500);beep(125,130,'sawtooth');vibrate([45,25,60]);`
- [x] `for` — linha 2108, coluna 199 em `setScaleMode()` — `if(!skillReady('giantSlam'))return;player.damageUntil=performance.now()+900;for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<5.4)damageEnemy(enemy,2);for(let i=0;i<9;i++){const a=i/9*Math.PI*2;spawnDust(player.x+Math.sin(a)*1.8,player.z+Math.cos(a)*1.8);}recordAdvancedSkill('giantSlam',6000);toast('Impacto Gigante!','good',1500);beep(125,130,'sawtooth');vibrate([45,25,60]);`
- [x] `return` — linha 2110, coluna 7 em `setScaleMode()` — `return;`
- [x] `if` — linha 2120, coluna 5 em `toggleCrouch()` — `if(!els.modal.hidden||paused||player.vehicle||player.boating||player.transit.mode)return;`
- [x] `return` — linha 2120, coluna 87 em `toggleCrouch()` — `if(!els.modal.hidden||paused||player.vehicle||player.boating||player.transit.mode)return;`
- [x] `if` — linha 2121, coluna 5 em `toggleCrouch()` — `if(typeof force!=='boolean'&&player.crouched){`
- [x] `if` — linha 2122, coluna 7 em `toggleCrouch()` — `if(!skillReady('stealth')){player.crouched=false;state.abilities.crouched=false;updateAbilityUI();saveState();return;}player.crouched=false;state.abilities.crouched=false;player.shieldUntil=performance.now()+5000;recordAdvancedSkill('stealth',8000);toast('Escudo Furtivo: protegido por 5 s.','good',1800);beep(610,90,'sine');return;`
- [x] `return` — linha 2122, coluna 117 em `toggleCrouch()` — `if(!skillReady('stealth')){player.crouched=false;state.abilities.crouched=false;updateAbilityUI();saveState();return;}player.crouched=false;state.abilities.crouched=false;player.shieldUntil=performance.now()+5000;recordAdvancedSkill('stealth',8000);toast('Escudo Furtivo: protegido por 5 s.','good',1800);beep(610,90,'sine');return;`
- [x] `return` — linha 2122, coluna 332 em `toggleCrouch()` — `if(!skillReady('stealth')){player.crouched=false;state.abilities.crouched=false;updateAbilityUI();saveState();return;}player.crouched=false;state.abilities.crouched=false;player.shieldUntil=performance.now()+5000;recordAdvancedSkill('stealth',8000);toast('Escudo Furtivo: protegido por 5 s.','good',1800);beep(610,90,'sine');return;`
- [x] `if` — linha 2130, coluna 5 em `spinPlayer()` — `if(!els.modal.hidden||paused||player.vehicle||player.boating||player.transit.mode||!skillReady('magnetSpin'))return;`
- [x] `return` — linha 2130, coluna 114 em `spinPlayer()` — `if(!els.modal.hidden||paused||player.vehicle||player.boating||player.transit.mode||!skillReady('magnetSpin'))return;`
- [x] `for` — linha 2132, coluna 5 em `spinPlayer()` — `for(const c of world.crystals)if(!c.got&&Math.hypot(player.x-c.x,player.z-c.z)<6){collectCrystal(c,'Cristal atraído pelo Giro Ímã!');pulled++;}`
- [x] `if` — linha 2132, coluna 35 em `spinPlayer()` — `for(const c of world.crystals)if(!c.got&&Math.hypot(player.x-c.x,player.z-c.z)<6){collectCrystal(c,'Cristal atraído pelo Giro Ímã!');pulled++;}`
- [x] `for` — linha 2133, coluna 5 em `spinPlayer()` — `for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<4.6){damageEnemy(enemy,1);const a=Math.atan2(enemy.group.position.x-player.x,enemy.group.position.z-player.z);enemy.group.position.x+=Math.sin(a)*1.4;enemy.group.position.z+=Math.cos(a)*1.4;}`
- [x] `if` — linha 2133, coluna 38 em `spinPlayer()` — `for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<4.6){damageEnemy(enemy,1);const a=Math.atan2(enemy.group.position.x-player.x,enemy.group.position.z-player.z);enemy.group.position.x+=Math.sin(a)*1.4;enemy.group.position.z+=Math.cos(a)*1.4;}`
- [x] `for` — linha 2138, coluna 277 em `updateAbilityUI()` — `const labels=[[els.crouchBtn,player.crouched?'Escudo':'Abaixar'],[els.miniBtn,player.scaleMode==='mini'?'Dash':'Mini'],[els.normalBtn,player.scaleMode==='normal'?'Super pulo':'Normal'],[els.giantBtn,player.scaleMode==='giant'?'Impacto':'Grande'],[els.spinBtn,'Giro ímã']];for(const [btn,label] of labels){const span=$('span',btn);if(span)span.textContent=label;}`
- [x] `if` — linha 2138, coluna 335 em `updateAbilityUI()` — `const labels=[[els.crouchBtn,player.crouched?'Escudo':'Abaixar'],[els.miniBtn,player.scaleMode==='mini'?'Dash':'Mini'],[els.normalBtn,player.scaleMode==='normal'?'Super pulo':'Normal'],[els.giantBtn,player.scaleMode==='giant'?'Impacto':'Grande'],[els.spinBtn,'Giro ímã']];for(const [btn,label] of labels){const span=$('span',btn);if(span)span.textContent=label;}`
- [x] `for` — linha 2139, coluna 5 em `updateAbilityUI()` — `for(const [id,getButton] of Object.entries(skillButtons)){const btn=getButton(),left=Math.max(0,skillCooldowns[id]-performance.now());btn?.classList.toggle('cooldown',left>0);if(btn){btn.dataset.cooldown=left>0?String(Math.ceil(left/1000)):'';btn.disabled=left>0&&id==='magnetSpin';}}`
- [x] `if` — linha 2139, coluna 180 em `updateAbilityUI()` — `for(const [id,getButton] of Object.entries(skillButtons)){const btn=getButton(),left=Math.max(0,skillCooldowns[id]-performance.now());btn?.classList.toggle('cooldown',left>0);if(btn){btn.dataset.cooldown=left>0?String(Math.ceil(left/1000)):'';btn.disabled=left>0&&id==='magnetSpin';}}`

## `11-render-materials-player-model.js`

- [x] `return` — linha 2149, coluna 66 em `canvasTexture()` — `const rand=()=>{seed^=seed<<13;seed^=seed>>>17;seed^=seed<<5;return(seed>>>0)/4294967296;};`
- [x] `if` — linha 2152, coluna 5 em `canvasTexture()` — `if (kind === 'grass') {`
- [x] `for` — linha 2153, coluna 7 em `canvasTexture()` — `for (let i = 0; i < 720; i++) { ctx.fillStyle=pick();const x=rand()*256,y=rand()*256;ctx.fillRect(x,y,2+rand()*7,2+rand()*7);if(i%9===0)ctx.fillRect(x+2,y-5,2,8); }`
- [x] `if` — linha 2153, coluna 132 em `canvasTexture()` — `for (let i = 0; i < 720; i++) { ctx.fillStyle=pick();const x=rand()*256,y=rand()*256;ctx.fillRect(x,y,2+rand()*7,2+rand()*7);if(i%9===0)ctx.fillRect(x+2,y-5,2,8); }`
- [x] `for` — linha 2154, coluna 42 em `canvasTexture()` — `ctx.fillStyle='rgba(15,65,22,.14)';for(let i=0;i<95;i++)ctx.fillRect(rand()*256,rand()*256,12+rand()*26,2);`
- [x] `else` — linha 2155, coluna 7 em `canvasTexture()` — `} else if (kind === 'road') {`
- [x] `if` — linha 2155, coluna 12 em `canvasTexture()` — `} else if (kind === 'road') {`
- [x] `for` — linha 2156, coluna 7 em `canvasTexture()` — `for (let i=0;i<260;i++){ctx.fillStyle=pick();ctx.globalAlpha=.18+rand()*.2;ctx.fillRect(rand()*256,rand()*256,2+rand()*7,1+rand()*4);}ctx.globalAlpha=1;`
- [x] `for` — linha 2157, coluna 60 em `canvasTexture()` — `ctx.strokeStyle='rgba(10,16,23,.22)';ctx.lineWidth=2;for(let i=0;i<8;i++){const x=rand()*256,y=rand()*256;ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x+10+rand()*22,y-5+rand()*10);ctx.lineTo(x+18+rand()*28,y+rand()*20);ctx.stroke();}`
- [x] `else` — linha 2158, coluna 7 em `canvasTexture()` — `} else if (kind === 'wood') {`
- [x] `if` — linha 2158, coluna 12 em `canvasTexture()` — `} else if (kind === 'wood') {`
- [x] `for` — linha 2159, coluna 7 em `canvasTexture()` — `for(let x=0;x<256;x+=48){ctx.fillStyle=x%96?colors[0]:pick();ctx.fillRect(x,0,47,256);ctx.fillStyle='rgba(255,238,190,.12)';ctx.fillRect(x+3,0,3,256);ctx.fillStyle='rgba(50,22,8,.25)';ctx.fillRect(x+45,0,3,256);}`
- [x] `for` — linha 2160, coluna 60 em `canvasTexture()` — `ctx.strokeStyle='rgba(66,31,13,.28)';ctx.lineWidth=2;for(let i=0;i<28;i++){const x=rand()*256;ctx.beginPath();ctx.moveTo(x,0);ctx.bezierCurveTo(x+8,65,x-7,150,x+4,256);ctx.stroke();}`
- [x] `for` — linha 2161, coluna 7 em `canvasTexture()` — `for(let i=0;i<9;i++){ctx.strokeStyle='rgba(58,28,13,.35)';ctx.strokeRect(rand()*245,rand()*245,5+rand()*9,3+rand()*6);}`
- [x] `else` — linha 2162, coluna 7 em `canvasTexture()` — `} else if (kind === 'brick') {`
- [x] `if` — linha 2162, coluna 12 em `canvasTexture()` — `} else if (kind === 'brick') {`
- [x] `for` — linha 2163, coluna 57 em `canvasTexture()` — `ctx.fillStyle=colors[1];ctx.fillRect(0,0,256,256);for(let y=0;y<256;y+=42){const offset=(y/42)%2?32:0;for(let x=-64+offset;x<256;x+=64){ctx.fillStyle=rand()>.5?colors[0]:pick();ctx.fillRect(x+3,y+3,58,36);ctx.fillStyle='rgba(255,255,255,.10)';ctx.fillRect(x+5,y+5,54,3);}}`
- [x] `for` — linha 2163, coluna 109 em `canvasTexture()` — `ctx.fillStyle=colors[1];ctx.fillRect(0,0,256,256);for(let y=0;y<256;y+=42){const offset=(y/42)%2?32:0;for(let x=-64+offset;x<256;x+=64){ctx.fillStyle=rand()>.5?colors[0]:pick();ctx.fillRect(x+3,y+3,58,36);ctx.fillStyle='rgba(255,255,255,.10)';ctx.fillRect(x+5,y+5,54,3);}}`
- [x] `else` — linha 2164, coluna 7 em `canvasTexture()` — `} else if (kind === 'sidewalk') {`
- [x] `if` — linha 2164, coluna 12 em `canvasTexture()` — `} else if (kind === 'sidewalk') {`
- [x] `for` — linha 2165, coluna 60 em `canvasTexture()` — `ctx.strokeStyle='rgba(54,66,80,.22)';ctx.lineWidth=4;for(let y=0;y<=256;y+=48){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(256,y);ctx.stroke();}for(let x=0;x<=256;x+=48){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,256);ctx.stroke();}`
- [x] `for` — linha 2165, coluna 150 em `canvasTexture()` — `ctx.strokeStyle='rgba(54,66,80,.22)';ctx.lineWidth=4;for(let y=0;y<=256;y+=48){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(256,y);ctx.stroke();}for(let x=0;x<=256;x+=48){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,256);ctx.stroke();}`
- [x] `for` — linha 2166, coluna 7 em `canvasTexture()` — `for(let i=0;i<150;i++){ctx.fillStyle=pick();ctx.globalAlpha=.16;ctx.fillRect(rand()*256,rand()*256,2+rand()*5,2+rand()*5);}ctx.globalAlpha=1;`
- [x] `else` — linha 2167, coluna 7 em `canvasTexture()` — `} else if (kind === 'water') {`
- [x] `if` — linha 2167, coluna 12 em `canvasTexture()` — `} else if (kind === 'water') {`
- [x] `for` — linha 2169, coluna 7 em `canvasTexture()` — `for(let y=10;y<256;y+=24){ctx.beginPath();ctx.moveTo(0,y+Math.sin(y)*4);for(let x=0;x<=256;x+=12)ctx.lineTo(x,y+Math.sin((x+y)*.09)*5);ctx.stroke();}`
- [x] `for` — linha 2169, coluna 79 em `canvasTexture()` — `for(let y=10;y<256;y+=24){ctx.beginPath();ctx.moveTo(0,y+Math.sin(y)*4);for(let x=0;x<=256;x+=12)ctx.lineTo(x,y+Math.sin((x+y)*.09)*5);ctx.stroke();}`
- [x] `else` — linha 2171, coluna 7 em `canvasTexture()` — `} else if(kind==='stone'){`
- [x] `if` — linha 2171, coluna 12 em `canvasTexture()` — `} else if(kind==='stone'){`
- [x] `for` — linha 2172, coluna 57 em `canvasTexture()` — `ctx.fillStyle=colors[1];ctx.fillRect(0,0,256,256);for(let y=0;y<256;y+=50){for(let x=((y/50)%2?-28:0);x<256;x+=58){ctx.fillStyle=rand()>.45?colors[0]:pick();ctx.fillRect(x+3,y+3,52,44);ctx.fillStyle='rgba(255,255,255,.09)';ctx.fillRect(x+5,y+5,48,4);}}`
- [x] `for` — linha 2172, coluna 82 em `canvasTexture()` — `ctx.fillStyle=colors[1];ctx.fillRect(0,0,256,256);for(let y=0;y<256;y+=50){for(let x=((y/50)%2?-28:0);x<256;x+=58){ctx.fillStyle=rand()>.45?colors[0]:pick();ctx.fillRect(x+3,y+3,52,44);ctx.fillStyle='rgba(255,255,255,.09)';ctx.fillRect(x+5,y+5,48,4);}}`
- [x] `else` — linha 2173, coluna 7 em `canvasTexture()` — `} else if(kind==='tile'){`
- [x] `if` — linha 2173, coluna 12 em `canvasTexture()` — `} else if(kind==='tile'){`
- [x] `for` — linha 2174, coluna 49 em `canvasTexture()` — `ctx.strokeStyle=colors[1];ctx.lineWidth=7;for(let p=0;p<=256;p+=64){ctx.beginPath();ctx.moveTo(p,0);ctx.lineTo(p,256);ctx.stroke();ctx.beginPath();ctx.moveTo(0,p);ctx.lineTo(256,p);ctx.stroke();}ctx.fillStyle='rgba(255,255,255,.18)';for(let y=7;y<256;y+=64)for(let x=7;x<256;x+=64)ctx.fillRect(x,y,48,5);`
- [x] `for` — linha 2174, coluna 240 em `canvasTexture()` — `ctx.strokeStyle=colors[1];ctx.lineWidth=7;for(let p=0;p<=256;p+=64){ctx.beginPath();ctx.moveTo(p,0);ctx.lineTo(p,256);ctx.stroke();ctx.beginPath();ctx.moveTo(0,p);ctx.lineTo(256,p);ctx.stroke();}ctx.fillStyle='rgba(255,255,255,.18)';for(let y=7;y<256;y+=64)for(let x=7;x<256;x+=64)ctx.fillRect(x,y,48,5);`
- [x] `for` — linha 2174, coluna 264 em `canvasTexture()` — `ctx.strokeStyle=colors[1];ctx.lineWidth=7;for(let p=0;p<=256;p+=64){ctx.beginPath();ctx.moveTo(p,0);ctx.lineTo(p,256);ctx.stroke();ctx.beginPath();ctx.moveTo(0,p);ctx.lineTo(256,p);ctx.stroke();}ctx.fillStyle='rgba(255,255,255,.18)';for(let y=7;y<256;y+=64)for(let x=7;x<256;x+=64)ctx.fillRect(x,y,48,5);`
- [x] `else` — linha 2175, coluna 7 em `canvasTexture()` — `} else if(kind==='fabric'){`
- [x] `if` — linha 2175, coluna 12 em `canvasTexture()` — `} else if(kind==='fabric'){`
- [x] `for` — linha 2176, coluna 7 em `canvasTexture()` — `for(let y=0;y<256;y+=12)for(let x=0;x<256;x+=12){ctx.fillStyle=(x/12+y/12)%2?colors[0]:colors[1];ctx.globalAlpha=.62;ctx.fillRect(x,y,12,12);}ctx.globalAlpha=1;`
- [x] `for` — linha 2176, coluna 31 em `canvasTexture()` — `for(let y=0;y<256;y+=12)for(let x=0;x<256;x+=12){ctx.fillStyle=(x/12+y/12)%2?colors[0]:colors[1];ctx.globalAlpha=.62;ctx.fillRect(x,y,12,12);}ctx.globalAlpha=1;`
- [x] `else` — linha 2177, coluna 7 em `canvasTexture()` — `} else if(kind==='metal'){`
- [x] `if` — linha 2177, coluna 12 em `canvasTexture()` — `} else if(kind==='metal'){`
- [x] `for` — linha 2178, coluna 7 em `canvasTexture()` — `for(let y=0;y<256;y+=32){ctx.fillStyle=y%64?colors[0]:colors[1];ctx.fillRect(0,y,256,32);ctx.fillStyle='rgba(255,255,255,.14)';ctx.fillRect(0,y,256,3);}for(const x of [10,246])for(let y=14;y<256;y+=42){ctx.fillStyle='#6c7b88';ctx.fillRect(x-3,y-3,6,6);}`
- [x] `for` — linha 2178, coluna 159 em `canvasTexture()` — `for(let y=0;y<256;y+=32){ctx.fillStyle=y%64?colors[0]:colors[1];ctx.fillRect(0,y,256,32);ctx.fillStyle='rgba(255,255,255,.14)';ctx.fillRect(0,y,256,3);}for(const x of [10,246])for(let y=14;y<256;y+=42){ctx.fillStyle='#6c7b88';ctx.fillRect(x-3,y-3,6,6);}`
- [x] `for` — linha 2178, coluna 183 em `canvasTexture()` — `for(let y=0;y<256;y+=32){ctx.fillStyle=y%64?colors[0]:colors[1];ctx.fillRect(0,y,256,32);ctx.fillStyle='rgba(255,255,255,.14)';ctx.fillRect(0,y,256,3);}for(const x of [10,246])for(let y=14;y<256;y+=42){ctx.fillStyle='#6c7b88';ctx.fillRect(x-3,y-3,6,6);}`
- [x] `return` — linha 2180, coluna 350 em `canvasTexture()` — `const tex = new THREE.CanvasTexture(c); tex.magFilter = THREE.NearestFilter; tex.minFilter = THREE.LinearMipmapLinearFilter; tex.generateMipmaps = true; tex.anisotropy = (renderer && renderer.capabilities) ? Math.min(12,renderer.capabilities.getMaxAnisotropy()) : 4; tex.wrapS = tex.wrapT = THREE.RepeatWrapping;tex.encoding=THREE.sRGBEncoding; return tex;`
- [x] `if` — linha 2186, coluna 332 em `professionalTexture()` — `const configure=target=>{target.wrapS=target.wrapT=THREE.RepeatWrapping;target.repeat.set(repeatX,repeatY);target.magFilter=THREE.LinearFilter;target.minFilter=THREE.LinearMipmapLinearFilter;target.generateMipmaps=true;target.anisotropy=(renderer&&renderer.capabilities)?Math.min(12,renderer.capabilities.getMaxAnisotropy()):4;if('colorSpace' in target&&THREE.SRGBColorSpace)target.colorSpace=THREE.SRGBColorSpace;else target.encoding=THREE.sRGBEncoding;};configure(texture);`
- [x] `else` — linha 2186, coluna 419 em `professionalTexture()` — `const configure=target=>{target.wrapS=target.wrapT=THREE.RepeatWrapping;target.repeat.set(repeatX,repeatY);target.magFilter=THREE.LinearFilter;target.minFilter=THREE.LinearMipmapLinearFilter;target.generateMipmaps=true;target.anisotropy=(renderer&&renderer.capabilities)?Math.min(12,renderer.capabilities.getMaxAnisotropy()):4;if('colorSpace' in target&&THREE.SRGBColorSpace)target.colorSpace=THREE.SRGBColorSpace;else target.encoding=THREE.sRGBEncoding;};configure(texture);`
- [x] `return` — linha 2188, coluna 5 em `professionalTexture()` — `return texture;`
- [x] `for` — linha 2235, coluna 5 em `initMaterials()` — `for(const [id,material] of Object.entries(materials))registerVisualAsset(\`material:${id}\`,material,{type:'material',status:'ready',shared:true});`
- [x] `return` — linha 2237, coluna 78 em `mat()` — `function mat(color, opts = {}) { visualFoundation.mutableMaterialCreates++;return new THREE.MeshStandardMaterial({ color, roughness: opts.roughness ?? .72, metalness: opts.metalness ?? .03, emissive: opts.emissive ?? 0x000000, emissiveIntensity: opts.emissiveIntensity ?? 0, transparent: !!opts.transparent, opacity: opts.opacity ?? 1, flatShading: opts.flatShading ?? true }); }`
- [x] `return` — linha 2246, coluna 35 em `geometryKey()` — `function geometryKey(...values){return values.map(v=>Number(v).toFixed(3)).join('|');}`
- [x] `if` — linha 2249, coluna 5 em `sharedBoxGeometry()` — `if(sharedGeometryCache.box.has(key))visualFoundation.geometryCacheHits++;`
- [x] `else` — linha 2250, coluna 5 em `sharedBoxGeometry()` — `else{visualFoundation.geometryCacheMisses++;sharedGeometryCache.box.set(key,new THREE.BoxGeometry(w,h,d));}`
- [x] `return` — linha 2251, coluna 5 em `sharedBoxGeometry()` — `return sharedGeometryCache.box.get(key);`
- [x] `if` — linha 2255, coluna 5 em `sharedCylinderGeometry()` — `if(sharedGeometryCache.cylinder.has(key))visualFoundation.geometryCacheHits++;`
- [x] `else` — linha 2256, coluna 5 em `sharedCylinderGeometry()` — `else{visualFoundation.geometryCacheMisses++;sharedGeometryCache.cylinder.set(key,new THREE.CylinderGeometry(r,r,h,sides));}`
- [x] `return` — linha 2257, coluna 5 em `sharedCylinderGeometry()` — `return sharedGeometryCache.cylinder.get(key);`
- [x] `if` — linha 2261, coluna 5 em `renderMat()` — `if(immutableVisualMaterials.has(key))visualFoundation.materialCacheHits++;`
- [x] `else` — linha 2262, coluna 5 em `renderMat()` — `else{visualFoundation.materialCacheMisses++;immutableVisualMaterials.set(key,mat(color,opts));}`
- [x] `return` — linha 2263, coluna 5 em `renderMat()` — `return immutableVisualMaterials.get(key);`
- [x] `if` — linha 2268, coluna 5 em `tintedBrickMaterial()` — `if(!tintedBrickMaterialCache.has(key))tintedBrickMaterialCache.set(key,new THREE.MeshStandardMaterial({map:source,color:new THREE.Color(color).lerp(new THREE.Color(0xffffff),.34),roughness:.8,metalness:0,flatShading:true}));`
- [x] `return` — linha 2269, coluna 5 em `tintedBrickMaterial()` — `return tintedBrickMaterialCache.get(key);`
- [x] `if` — linha 2272, coluna 29 em `texturedRoofMaterial()` — `const key=Number(color);if(!roofMaterialCache.has(key))roofMaterialCache.set(key,new THREE.MeshStandardMaterial({map:textures.roof,color:new THREE.Color(color).lerp(new THREE.Color(0xffffff),.12),roughness:.76,metalness:0,flatShading:true}));return roofMaterialCache.get(key);`
- [x] `return` — linha 2272, coluna 247 em `texturedRoofMaterial()` — `const key=Number(color);if(!roofMaterialCache.has(key))roofMaterialCache.set(key,new THREE.MeshStandardMaterial({map:textures.roof,color:new THREE.Color(color).lerp(new THREE.Color(0xffffff),.12),roughness:.76,metalness:0,flatShading:true}));return roofMaterialCache.get(key);`
- [x] `if` — linha 2275, coluna 5 em `visualCacheHasValue()` — `if(!cache||!value)return false;`
- [x] `return` — linha 2275, coluna 23 em `visualCacheHasValue()` — `if(!cache||!value)return false;`
- [x] `for` — linha 2276, coluna 5 em `visualCacheHasValue()` — `for(const cached of cache.values())if(cached===value)return true;`
- [x] `if` — linha 2276, coluna 40 em `visualCacheHasValue()` — `for(const cached of cache.values())if(cached===value)return true;`
- [x] `return` — linha 2276, coluna 58 em `visualCacheHasValue()` — `for(const cached of cache.values())if(cached===value)return true;`
- [x] `return` — linha 2277, coluna 5 em `visualCacheHasValue()` — `return false;`
- [x] `return` — linha 2280, coluna 5 em `visualCacheOwnsGeometry()` — `return visualCacheHasValue(sharedGeometryCache.box,geometry)||visualCacheHasValue(sharedGeometryCache.cylinder,geometry)||visualCacheHasValue(outlineGeometryCache,geometry);`
- [x] `return` — linha 2283, coluna 5 em `visualCacheOwnsMaterial()` — `return visualCacheHasValue(immutableVisualMaterials,material)||visualCacheHasValue(tintedBrickMaterialCache,material)||visualCacheHasValue(roofMaterialCache,material)||visualCacheHasValue(outlineMaterialCache,material)||Object.values(materials).includes(material);`
- [x] `return` — linha 2285, coluna 44 em `visualCacheOwnsTexture()` — `function visualCacheOwnsTexture(texture){return !!texture&&(Object.values(textures).includes(texture)||Object.values(materials).some(material=>material?.map===texture));}`
- [x] `if` — linha 2287, coluna 5 em `disposeDetachedVisual()` — `if(!root?.traverse)return;`
- [x] `return` — linha 2287, coluna 24 em `disposeDetachedVisual()` — `if(!root?.traverse)return;`
- [x] `if` — linha 2290, coluna 7 em `disposeDetachedVisual()` — `if(object.userData?.v615Outline&&world?.outlines){`
- [x] `if` — linha 2292, coluna 9 em `disposeDetachedVisual()` — `if(outlineIndex>=0)world.outlines.splice(outlineIndex,1);`
- [x] `if` — linha 2295, coluna 7 em `disposeDetachedVisual()` — `if(geometry&&!visualCacheOwnsGeometry(geometry)&&!disposedGeometry.has(geometry)){disposedGeometry.add(geometry);geometry.dispose?.();}`
- [x] `for` — linha 2297, coluna 7 em `disposeDetachedVisual()` — `for(const material of objectMaterials.filter(Boolean)){`
- [x] `if` — linha 2298, coluna 9 em `disposeDetachedVisual()` — `if(visualCacheOwnsMaterial(material)||disposedMaterial.has(material))continue;`
- [x] `if` — linha 2301, coluna 9 em `disposeDetachedVisual()` — `if(map&&!visualCacheOwnsTexture(map)&&!disposedTexture.has(map)){disposedTexture.add(map);map.dispose?.();}`
- [x] `return` — linha 2308, coluna 169 em `addSoftHighlight()` — `const highlight=new THREE.Mesh(sharedBoxGeometry(w,h,d),m);highlight.position.set(x,y,z);highlight.renderOrder=4;highlight.frustumCulled=true;parent.add(highlight);return highlight;`
- [x] `return` — linha 2314, coluna 127 em `box()` — `mesh.position.set(x, y, z); mesh.castShadow = true; mesh.receiveShadow = true; mesh.frustumCulled=true; parent.add(mesh); return mesh;`
- [x] `if` — linha 2316, coluna 49 em `stabilizeSurface()` — `function stabilizeSurface(mesh,renderOrder=0){if(!mesh)return mesh;mesh.frustumCulled=false;mesh.castShadow=false;mesh.receiveShadow=false;mesh.renderOrder=renderOrder;mesh.userData.stableSurface=true;world.criticalSurfaces.push(mesh);return mesh;}`
- [x] `return` — linha 2316, coluna 58 em `stabilizeSurface()` — `function stabilizeSurface(mesh,renderOrder=0){if(!mesh)return mesh;mesh.frustumCulled=false;mesh.castShadow=false;mesh.receiveShadow=false;mesh.renderOrder=renderOrder;mesh.userData.stableSurface=true;world.criticalSurfaces.push(mesh);return mesh;}`
- [x] `return` — linha 2316, coluna 238 em `stabilizeSurface()` — `function stabilizeSurface(mesh,renderOrder=0){if(!mesh)return mesh;mesh.frustumCulled=false;mesh.castShadow=false;mesh.receiveShadow=false;mesh.renderOrder=renderOrder;mesh.userData.stableSurface=true;world.criticalSurfaces.push(mesh);return mesh;}`
- [x] `return` — linha 2317, coluna 89 em `stableBox()` — `function stableBox(w,h,d,materialOrColor,x=0,y=0,z=0,parent=worldGroup,renderOrder=0){return stabilizeSurface(box(w,h,d,materialOrColor,x,y,z,parent),renderOrder);}`
- [x] `return` — linha 2321, coluna 125 em `cylinder()` — `mesh.position.set(x,y,z); mesh.castShadow = true; mesh.receiveShadow = true; mesh.frustumCulled=true; parent.add(mesh); return mesh;`
- [x] `return` — linha 2324, coluna 167 em `addGlow()` — `const light = new THREE.PointLight(color, .5, size * 3); light.position.set(x,y,z); light.userData.v615Glow=true; worldGroup.add(light); world.glows.push(light); return light;`
- [x] `if` — linha 2328, coluna 5 em `addVoxelOutline()` — `if(!mesh?.geometry||mesh.userData?.voxelOutline)return mesh;`
- [x] `return` — linha 2328, coluna 53 em `addVoxelOutline()` — `if(!mesh?.geometry||mesh.userData?.voxelOutline)return mesh;`
- [x] `if` — linha 2330, coluna 5 em `addVoxelOutline()` — `if(!outlineGeometryCache.has(geometryKey))outlineGeometryCache.set(geometryKey,new THREE.EdgesGeometry(mesh.geometry,22));`
- [x] `if` — linha 2332, coluna 5 em `addVoxelOutline()` — `if(!outlineMaterialCache.has(materialKey))outlineMaterialCache.set(materialKey,new THREE.LineBasicMaterial({color,transparent:true,opacity,depthWrite:false}));`
- [x] `return` — linha 2335, coluna 133 em `addVoxelOutline()` — `lines.renderOrder=5;lines.userData.v615Outline=true;mesh.add(lines);mesh.userData.voxelOutline=lines;world.outlines.push(lines);return mesh;`
- [x] `return` — linha 2338, coluna 5 em `premiumBox()` — `return addVoxelOutline(box(w,h,d,materialOrColor,x,y,z,parent),outline,.34);`
- [x] `return` — linha 2341, coluna 5 em `premiumCylinder()` — `return addVoxelOutline(cylinder(r,h,color,x,y,z,parent,sides),0x142033,.3);`
- [x] `return` — linha 2345, coluna 93 em `makeWindow()` — `premiumBox(.08,h,.2,frame,x,y,z+.04,parent);premiumBox(w,.07,.2,frame,x,y,z+.04,parent);return parent;`
- [x] `for` — linha 2348, coluna 52 em `makePlanter()` — `premiumBox(1.35,.34,.48,0x8a522d,x,y,z,parent);for(const ox of [-.42,0,.42]){premiumBox(.08,.4,.08,0x2f9d46,x+ox,y+.32,z,parent);premiumBox(.28,.16,.28,color,x+ox,y+.55,z,parent);}return parent;`
- [x] `return` — linha 2348, coluna 185 em `makePlanter()` — `premiumBox(1.35,.34,.48,0x8a522d,x,y,z,parent);for(const ox of [-.42,0,.42]){premiumBox(.08,.4,.08,0x2f9d46,x+ox,y+.32,z,parent);premiumBox(.28,.16,.28,color,x+ox,y+.55,z,parent);}return parent;`
- [x] `for` — linha 2374, coluna 5 em `createPlayerModel()` — `for(const arm of [parts.leftArm,parts.rightArm]){`
- [x] `for` — linha 2378, coluna 5 em `createPlayerModel()` — `for(const leg of [parts.leftLeg,parts.rightLeg]){`
- [x] `if` — linha 2405, coluna 49 em `createPlayerModel()` — `vehicleVisual.userData.wheels.push(wheel);if(front)vehicleVisual.userData.frontWheels.push(holder);`
- [x] `if` — linha 2407, coluna 30 em `createPlayerModel()` — `playerModel.traverse(o=>{if(o.isMesh)addVoxelOutline(o,0x0a1a2d,.4);});`
- [x] `if` — linha 2408, coluna 32 em `createPlayerModel()` — `vehicleVisual.traverse(o=>{if(o.isMesh)addVoxelOutline(o,0x14243a,.3);});`
- [x] `return` — linha 2417, coluna 5 em `loadFaithfulAthosModel()` — `return false;`
- [x] `if` — linha 2421, coluna 5 em `clearAvatarLayer()` — `if (avatarLayer && playerGroup){playerGroup.remove(avatarLayer);disposeDetachedVisual(avatarLayer);}`
- [x] `for` — linha 2422, coluna 5 em `clearAvatarLayer()` — `for(const attachment of playerModel?.userData?.avatarAttachments||[]){try{attachment.parent?.remove(attachment);disposeDetachedVisual(attachment);}catch{}}`
- [x] `catch` — linha 2422, coluna 152 em `clearAvatarLayer()` — `for(const attachment of playerModel?.userData?.avatarAttachments||[]){try{attachment.parent?.remove(attachment);disposeDetachedVisual(attachment);}catch{}}`
- [x] `if` — linha 2423, coluna 5 em `clearAvatarLayer()` — `if(playerModel)playerModel.userData.avatarAttachments=[];`
- [x] `if` — linha 2430, coluna 5 em `avatarPartLayer()` — `if(playerModel){playerModel.userData.avatarAttachments=playerModel.userData.avatarAttachments||[];playerModel.userData.avatarAttachments.push(layer);}`
- [x] `return` — linha 2431, coluna 5 em `avatarPartLayer()` — `return layer;`
- [x] `return` — linha 2435, coluna 76 em `addUniformPatch()` — `patch.position.set(x,y,z);patch.rotation.y=rotationY;parent.add(patch);return patch;`
- [x] `if` — linha 2438, coluna 35 em `addUniformLimb()` — `box(w,h,d,color,x,y,z,parent);if(reflective)box(w+.025,.09,d+.025,reflective,x,y-h*.18,z,parent);`
- [x] `return` — linha 2451, coluna 7 em `uniformPalette()` — `};return palettes[uniform]||{primary:0x2477d4,secondary:0x26384e,accent:0xf4f6f8,boot:0x151a20};`
- [x] `if` — linha 2454, coluna 46 em `dressAnimatedAvatar()` — `const parts=playerModel?.userData?.parts;if(!parts)return null;`
- [x] `return` — linha 2454, coluna 56 em `dressAnimatedAvatar()` — `const parts=playerModel?.userData?.parts;if(!parts)return null;`
- [x] `for` — linha 2460, coluna 5 em `dressAnimatedAvatar()` — `for(const arm of[leftArm,rightArm]){`
- [x] `if` — linha 2462, coluna 7 em `dressAnimatedAvatar()` — `if(uniform!=='none')box(.385,.085,.385,palette.accent,0,-.48,.005,arm);`
- [x] `for` — linha 2464, coluna 5 em `dressAnimatedAvatar()` — `for(const leg of[leftLeg,rightLeg]){`
- [x] `if` — linha 2466, coluna 7 em `dressAnimatedAvatar()` — `if(uniform==='firefighter'||uniform==='paramedic'||uniform==='builder')box(.435,.085,.46,palette.accent,0,-.74,.02,leg);`
- [x] `if` — linha 2468, coluna 5 em `dressAnimatedAvatar()` — `if(uniform!=='none'){`
- [x] `return` — linha 2472, coluna 5 em `dressAnimatedAvatar()` — `return{parts,palette,body,leftArm,rightArm,leftLeg,rightLeg};`
- [x] `if` — linha 2475, coluna 5 em `applyAvatarCustomization()` — `if (!playerGroup || !window.THREE) return;`
- [x] `return` — linha 2475, coluna 40 em `applyAvatarCustomization()` — `if (!playerGroup || !window.THREE) return;`
- [x] `if` — linha 2479, coluna 5 em `applyAvatarCustomization()` — `if(uniform!=='none'&&dressed){`
- [x] `if` — linha 2481, coluna 7 em `applyAvatarCustomization()` — `if(uniform==='firefighter'){`
- [x] `else` — linha 2483, coluna 8 em `applyAvatarCustomization()` — `}else if(uniform==='police'){`
- [x] `if` — linha 2483, coluna 13 em `applyAvatarCustomization()` — `}else if(uniform==='police'){`
- [x] `else` — linha 2485, coluna 8 em `applyAvatarCustomization()` — `}else if(uniform==='paramedic'){`
- [x] `if` — linha 2485, coluna 13 em `applyAvatarCustomization()` — `}else if(uniform==='paramedic'){`
- [x] `else` — linha 2487, coluna 8 em `applyAvatarCustomization()` — `}else if(uniform==='teacher'){box(.18,.70,.08,0x6e4a2f,.43,.03,.45,body);box(.40,.27,.08,0xf7f1d0,.43,.35,.45,body);addUniformPatch(body,'PROFESSOR','#ffffff','#397a51',0,.15,.43,0,.82,.20);`
- [x] `if` — linha 2487, coluna 13 em `applyAvatarCustomization()` — `}else if(uniform==='teacher'){box(.18,.70,.08,0x6e4a2f,.43,.03,.45,body);box(.40,.27,.08,0xf7f1d0,.43,.35,.45,body);addUniformPatch(body,'PROFESSOR','#ffffff','#397a51',0,.15,.43,0,.82,.20);`
- [x] `else` — linha 2488, coluna 8 em `applyAvatarCustomization()` — `}else if(uniform==='delivery'){box(.82,.95,.42,0x8b5a2b,0,1.62,-.58,avatarLayer);addUniformPatch(body,'ENTREGA','#ffffff','#b86d13',0,.15,.43,0,.75,.20);`
- [x] `if` — linha 2488, coluna 13 em `applyAvatarCustomization()` — `}else if(uniform==='delivery'){box(.82,.95,.42,0x8b5a2b,0,1.62,-.58,avatarLayer);addUniformPatch(body,'ENTREGA','#ffffff','#b86d13',0,.15,.43,0,.75,.20);`
- [x] `else` — linha 2489, coluna 8 em `applyAvatarCustomization()` — `}else if(uniform==='mechanic'){box(.22,.32,.08,0xf3bd37,.34,-.03,.44,body);box(.90,.16,.85,0x26384e,0,3.25,0,avatarLayer);addUniformPatch(body,'OFICINA','#ffffff','#274d75',0,.15,.43,0,.75,.20);`
- [x] `if` — linha 2489, coluna 13 em `applyAvatarCustomization()` — `}else if(uniform==='mechanic'){box(.22,.32,.08,0xf3bd37,.34,-.03,.44,body);box(.90,.16,.85,0x26384e,0,3.25,0,avatarLayer);addUniformPatch(body,'OFICINA','#ffffff','#274d75',0,.15,.43,0,.75,.20);`
- [x] `else` — linha 2490, coluna 8 em `applyAvatarCustomization()` — `}else if(uniform==='miner'){const helm=new THREE.Mesh(new THREE.SphereGeometry(.64,12,8,0,Math.PI*2,0,Math.PI*.62),mat(0xf0bb2d,{metalness:.08}));helm.position.set(0,3.08,0);avatarLayer.add(helm);box(.22,.22,.08,0xf8f4c6,0,3.17,.58,avatarLayer);box(.75,.18,.07,0x3b2c1b,0,.15,.43,body);`
- [x] `if` — linha 2490, coluna 13 em `applyAvatarCustomization()` — `}else if(uniform==='miner'){const helm=new THREE.Mesh(new THREE.SphereGeometry(.64,12,8,0,Math.PI*2,0,Math.PI*.62),mat(0xf0bb2d,{metalness:.08}));helm.position.set(0,3.08,0);avatarLayer.add(helm);box(.22,.22,.08,0xf8f4c6,0,3.17,.58,avatarLayer);box(.75,.18,.07,0x3b2c1b,0,.15,.43,body);`
- [x] `else` — linha 2491, coluna 8 em `applyAvatarCustomization()` — `}else if(uniform==='builder'){box(1.14,.20,.82,0xf8d54a,0,3.24,0,avatarLayer);box(.72,.15,.85,0xf8d54a,0,3.15,.50,avatarLayer);box(.12,1.02,.07,0xfff2a1,-.33,0,.43,body);box(.12,1.02,.07,0xfff2a1,.33,0,.43,body);`
- [x] `if` — linha 2491, coluna 13 em `applyAvatarCustomization()` — `}else if(uniform==='builder'){box(1.14,.20,.82,0xf8d54a,0,3.24,0,avatarLayer);box(.72,.15,.85,0xf8d54a,0,3.15,.50,avatarLayer);box(.12,1.02,.07,0xfff2a1,-.33,0,.43,body);box(.12,1.02,.07,0xfff2a1,.33,0,.43,body);`
- [x] `else` — linha 2492, coluna 8 em `applyAvatarCustomization()` — `}else if(uniform==='sport'){box(.20,.70,.07,0xf5d84d,-.33,.02,.44,body);box(.20,.70,.07,0xf5d84d,.33,.02,.44,body);addUniformPatch(body,'ATLETA','#ffffff','#185fa8',0,.15,.43,0,.72,.20);}`
- [x] `if` — linha 2492, coluna 13 em `applyAvatarCustomization()` — `}else if(uniform==='sport'){box(.20,.70,.07,0xf5d84d,-.33,.02,.44,body);box(.20,.70,.07,0xf5d84d,.33,.02,.44,body);addUniformPatch(body,'ATLETA','#ffffff','#185fa8',0,.15,.43,0,.72,.20);}`
- [x] `if` — linha 2494, coluna 5 em `applyAvatarCustomization()` — `if(uniform==='none'){`
- [x] `if` — linha 2495, coluna 7 em `applyAvatarCustomization()` — `if(hat==='cap'){box(1.0,.22,1.0,0x2477d4,0,3.28,0,avatarLayer);box(.55,.10,.55,0x2477d4,0,3.18,.58,avatarLayer);}`
- [x] `else` — linha 2496, coluna 7 em `applyAvatarCustomization()` — `else if(hat==='crown'){box(.92,.25,.92,0xffd84d,0,3.32,0,avatarLayer);[[-.32,.22],[0,.34],[.32,.22]].forEach(([x,h])=>box(.18,h,.18,0xffd84d,x,3.48+h/2,0,avatarLayer));}`
- [x] `if` — linha 2496, coluna 12 em `applyAvatarCustomization()` — `else if(hat==='crown'){box(.92,.25,.92,0xffd84d,0,3.32,0,avatarLayer);[[-.32,.22],[0,.34],[.32,.22]].forEach(([x,h])=>box(.18,h,.18,0xffd84d,x,3.48+h/2,0,avatarLayer));}`
- [x] `else` — linha 2497, coluna 7 em `applyAvatarCustomization()` — `else if(hat==='helmet'){const helm=new THREE.Mesh(new THREE.SphereGeometry(.62,12,8,0,Math.PI*2,0,Math.PI*.62),mat(0xf97316,{metalness:.08}));helm.position.set(0,3.08,0);avatarLayer.add(helm);}`
- [x] `if` — linha 2497, coluna 12 em `applyAvatarCustomization()` — `else if(hat==='helmet'){const helm=new THREE.Mesh(new THREE.SphereGeometry(.62,12,8,0,Math.PI*2,0,Math.PI*.62),mat(0xf97316,{metalness:.08}));helm.position.set(0,3.08,0);avatarLayer.add(helm);}`
- [x] `if` — linha 2498, coluna 7 em `applyAvatarCustomization()` — `if(accessory==='backpack'){box(.78,1.05,.42,0x9a5b2b,0,1.65,-.58,avatarLayer);}`
- [x] `else` — linha 2499, coluna 7 em `applyAvatarCustomization()` — `else if(accessory==='glasses'){box(.38,.18,.08,0x111827,-.26,2.78,.59,avatarLayer);box(.38,.18,.08,0x111827,.26,2.78,.59,avatarLayer);box(.18,.06,.08,0x111827,0,2.78,.59,avatarLayer);}`
- [x] `if` — linha 2499, coluna 12 em `applyAvatarCustomization()` — `else if(accessory==='glasses'){box(.38,.18,.08,0x111827,-.26,2.78,.59,avatarLayer);box(.38,.18,.08,0x111827,.26,2.78,.59,avatarLayer);box(.18,.06,.08,0x111827,0,2.78,.59,avatarLayer);}`
- [x] `else` — linha 2500, coluna 7 em `applyAvatarCustomization()` — `else if(accessory==='cape'){const cape=box(.92,1.35,.08,0x8b5cf6,0,1.58,-.60,avatarLayer);cape.rotation.x=-.08;}`
- [x] `if` — linha 2500, coluna 12 em `applyAvatarCustomization()` — `else if(accessory==='cape'){const cape=box(.92,1.35,.08,0x8b5cf6,0,1.58,-.60,avatarLayer);cape.rotation.x=-.08;}`
- [x] `if` — linha 2502, coluna 50 em `applyAvatarCustomization()` — `const markShadows=layer=>layer?.traverse(o=>{if(o.isMesh){o.castShadow=true;o.receiveShadow=true;}});markShadows(avatarLayer);for(const attachment of playerModel.userData.avatarAttachments||[])markShadows(attachment);`
- [x] `for` — linha 2502, coluna 131 em `applyAvatarCustomization()` — `const markShadows=layer=>layer?.traverse(o=>{if(o.isMesh){o.castShadow=true;o.receiveShadow=true;}});markShadows(avatarLayer);for(const attachment of playerModel.userData.avatarAttachments||[])markShadows(attachment);`
- [x] `return` — linha 2507, coluna 78 em `registerInteractable()` — `function registerInteractable(data) { world.interactables.push({...data}); return data; }`
- [x] `if` — linha 2509, coluna 5 em `worldPos()` — `if (entry.getPos) return entry.getPos();`
- [x] `return` — linha 2509, coluna 23 em `worldPos()` — `if (entry.getPos) return entry.getPos();`
- [x] `return` — linha 2510, coluna 5 em `worldPos()` — `return {x:entry.x,z:entry.z,y:entry.y||0};`

## `12-world-resources-nature.js`

- [x] `if` — linha 2515, coluna 5 em `isInteractionAvailable()` — `if (entry.disabled) return false;`
- [x] `return` — linha 2515, coluna 25 em `isInteractionAvailable()` — `if (entry.disabled) return false;`
- [x] `if` — linha 2516, coluna 5 em `isInteractionAvailable()` — `if (currentHouse) return entry.houseId === currentHouse.id || entry.globalInside;`
- [x] `return` — linha 2516, coluna 23 em `isInteractionAvailable()` — `if (currentHouse) return entry.houseId === currentHouse.id || entry.globalInside;`
- [x] `return` — linha 2517, coluna 5 em `isInteractionAvailable()` — `return !entry.houseId;`
- [x] `if` — linha 2524, coluna 5 em `ensureFlowerBatch()` — `if(!batch.stem){`
- [x] `if` — linha 2528, coluna 5 em `ensureFlowerBatch()` — `if(!batch.petals.has(color)){`
- [x] `return` — linha 2532, coluna 5 em `ensureFlowerBatch()` — `return batch;`
- [x] `if` — linha 2546, coluna 5 em `createTree()` — `if(!resource&&scale>.8){box(.34*scale,.18*scale,.34*scale,0xffe26a,.7*scale,3.95*scale,.65*scale,high);}`
- [x] `if` — linha 2551, coluna 5 em `createTree()` — `if(resource){`
- [x] `return` — linha 2555, coluna 5 em `createTree()` — `return group;`
- [x] `if` — linha 2561, coluna 5 em `createRock()` — `if(resource){world.resources.push({id,type:'stone',x,z,mesh,collected:false,hits:0,hitsNeeded:2});registerInteractable({id,type:'resource',icon:'⛏️',label:'Extrair pedra com picareta',x,z,radius:2.2,priority:135,action:()=>collectResource(id)});} return mesh;`
- [x] `return` — linha 2561, coluna 252 em `createRock()` — `if(resource){world.resources.push({id,type:'stone',x,z,mesh,collected:false,hits:0,hitsNeeded:2});registerInteractable({id,type:'resource',icon:'⛏️',label:'Extrair pedra com picareta',x,z,radius:2.2,priority:135,action:()=>collectResource(id)});} return mesh;`
- [x] `return` — linha 2569, coluna 231 em `createGoldFoundry()` — `registerCollider(x,z,7.4,5.8,{landmark:'foundry'});registerInteractable({id:'gold-foundry',type:'workshop',icon:'🏅',label:'Usar fundição de ouro',x,z:z+3.7,radius:3.1,priority:190,action:openWorkshop});world.landmarks.push(g);return g;`
- [x] `for` — linha 2574, coluna 5 em `createGoldMine()` — `for(const [ox,oz,s] of [[-7,-4,2.4],[7,-4,2.4],[-7,4,2.6],[7,4,2.6],[-4,-5,2],[4,-5,2]]){const rock=new THREE.Mesh(new THREE.DodecahedronGeometry(s,0),materials.stone);rock.position.set(ox,s*.48,oz);rock.scale.y=1.35;g.add(rock);}`
- [x] `for` — linha 2576, coluna 5 em `createGoldMine()` — `for(const ox of [-2.5,2.5]){premiumBox(.35,4.7,.35,materials.wood,ox,2.4,-4.3,g);premiumBox(3.1,.35,.35,materials.wood,ox/2,4.65,-4.3,g);}`
- [x] `return` — linha 2583, coluna 95 em `createGoldMine()` — `createSignpost(x+7,z-6,'Mina Dourada',Math.PI*.1);createLamp(x-6,z-3);createLamp(x+6,z-3);return g;`
- [x] `for` — linha 2588, coluna 5 em `createVillageWell()` — `for(const ox of [-1.65,1.65])premiumBox(.28,3.6,.28,materials.wood,ox,2.0,0,g);`
- [x] `return` — linha 2592, coluna 205 em `createVillageWell()` — `registerInteractable({id:'village-well',type:'well',icon:'🪣',label:'Retirar água com o balde',x,z,radius:3.2,priority:170,action:drawWaterFromWell});createSignpost(x+3,z+1.5,'Poço da Vila',Math.PI/2);return g;`
- [x] `if` — linha 2595, coluna 5 em `drawWaterFromWell()` — `if(state.tools.equipped!=='bucket'){toast('Equipe o balde no menu Ferramentas.','warn',1800);return;}`
- [x] `return` — linha 2595, coluna 98 em `drawWaterFromWell()` — `if(state.tools.equipped!=='bucket'){toast('Equipe o balde no menu Ferramentas.','warn',1800);return;}`
- [x] `if` — linha 2596, coluna 33 em `drawWaterFromWell()` — `const now=performance.now();if(world.well&&now-world.well.lastDrawAt<2500){toast('O balde ainda está subindo.','warn',1100);return;}if(world.well)world.well.lastDrawAt=now;`
- [x] `return` — linha 2596, coluna 129 em `drawWaterFromWell()` — `const now=performance.now();if(world.well&&now-world.well.lastDrawAt<2500){toast('O balde ainda está subindo.','warn',1100);return;}if(world.well)world.well.lastDrawAt=now;`
- [x] `if` — linha 2596, coluna 137 em `drawWaterFromWell()` — `const now=performance.now();if(world.well&&now-world.well.lastDrawAt<2500){toast('O balde ainda está subindo.','warn',1100);return;}if(world.well)world.well.lastDrawAt=now;`
- [x] `if` — linha 2601, coluna 5 em `createFlower()` — `if(batch.stemCount<256){`
- [x] `if` — linha 2605, coluna 5 em `createFlower()` — `if(petals.count<128){`
- [x] `return` — linha 2615, coluna 34 em `createLamp()` — `addGlow(x,2.82,z,0xffd56b,4);return g;`
- [x] `return` — linha 2621, coluna 5 em `createSignpost()` — `return post;`
- [x] `for` — linha 2624, coluna 5 em `createFenceLine()` — `for(let i=0;i<=segments;i++){const t=i/segments;box(.18,1.0,.18,materials.wood,lerp(x1,x2,t),.5,lerp(z1,z2,t));}`
- [x] `if` — linha 2631, coluna 5 em `createRoad()` — `if(horizontal){`
- [x] `for` — linha 2635, coluna 7 em `createRoad()` — `for(let lx=-w/2+3;lx<w/2-1.5;lx+=6)stableBox(2.35,.022,.34,lineMaterial,x+lx,.132,z,worldGroup,3);`
- [x] `else` — linha 2637, coluna 6 em `createRoad()` — `}else{`
- [x] `for` — linha 2641, coluna 7 em `createRoad()` — `for(let lz=-d/2+3;lz<d/2-1.5;lz+=6)stableBox(.34,.022,2.35,lineMaterial,x,.132,z+lz,worldGroup,3);`
- [x] `for` — linha 2645, coluna 133 em `createWater()` — `function createWater(x,z,w,d){const water=stableBox(w,.12,d,materials.water,x,.04,z,worldGroup,1);water.material.depthWrite=false;for(let px=x-w/2+2;px<x+w/2;px+=4){stableBox(3.2,.18,.7,0x9fadb8,px,.09,z-d/2-.35,worldGroup,2);stableBox(3.2,.18,.7,0x9fadb8,px,.09,z+d/2+.35,worldGroup,2);}world.hazards.push({type:'water',x,z,w,d});}`
- [x] `return` — linha 2646, coluna 177 em `createLava()` — `function createLava(x,z,w,d){const m=stableBox(w,.12,d,mat(0xff3a00,{emissive:0xff2200,emissiveIntensity:.9}),x,.04,z,worldGroup,1);world.hazards.push({type:'lava',x,z,w,d});return m;}`
- [x] `if` — linha 2651, coluna 5 em `createFurniture()` — `if(type==='bed'){`
- [x] `if` — linha 2655, coluna 5 em `createFurniture()` — `if(type==='sofa'){`
- [x] `if` — linha 2660, coluna 5 em `createFurniture()` — `if(type==='tv'){`
- [x] `if` — linha 2665, coluna 5 em `createFurniture()` — `if(type==='fridge'){`
- [x] `if` — linha 2669, coluna 5 em `createFurniture()` — `if(type==='stove'){`
- [x] `for` — linha 2671, coluna 7 em `createFurniture()` — `for(const ox of [-.28,.28])for(const oz of [-.2,.2])cylinder(.13,.045,0x111827,ox,.95,oz,group,12);`
- [x] `for` — linha 2671, coluna 34 em `createFurniture()` — `for(const ox of [-.28,.28])for(const oz of [-.2,.2])cylinder(.13,.045,0x111827,ox,.95,oz,group,12);`
- [x] `for` — linha 2672, coluna 7 em `createFurniture()` — `for(const ox of [-.3,0,.3])cylinder(.04,.07,0xe8edf2,ox,.8,.46,group,8);`
- [x] `if` — linha 2674, coluna 5 em `createFurniture()` — `if(type==='sink'){`
- [x] `if` — linha 2678, coluna 5 em `createFurniture()` — `if(type==='shower'){`
- [x] `if` — linha 2682, coluna 5 em `createFurniture()` — `if(type==='chest'){box(1.25,.72,.82,wood,0,.36,0,group);box(1.3,.2,.87,0xffc629,0,.82,0,group);box(.18,.34,.08,metal,0,.55,.44,group);}`
- [x] `if` — linha 2683, coluna 5 em `createFurniture()` — `if(type==='table'){box(1.6,.17,1.05,wood,0,.9,0,group);for(const ox of [-.58,.58])for(const oz of [-.35,.35])box(.13,.82,.13,wood,ox,.42,oz,group);box(.55,.08,.55,0x56c5ff,0,.99,0,group);}`
- [x] `for` — linha 2683, coluna 60 em `createFurniture()` — `if(type==='table'){box(1.6,.17,1.05,wood,0,.9,0,group);for(const ox of [-.58,.58])for(const oz of [-.35,.35])box(.13,.82,.13,wood,ox,.42,oz,group);box(.55,.08,.55,0x56c5ff,0,.99,0,group);}`
- [x] `for` — linha 2683, coluna 87 em `createFurniture()` — `if(type==='table'){box(1.6,.17,1.05,wood,0,.9,0,group);for(const ox of [-.58,.58])for(const oz of [-.35,.35])box(.13,.82,.13,wood,ox,.42,oz,group);box(.55,.08,.55,0x56c5ff,0,.99,0,group);}`
- [x] `if` — linha 2684, coluna 5 em `createFurniture()` — `if(type==='wardrobe'){box(1.55,2.14,.68,renderMat(0x89502b,{roughness:.8}),0,1.07,0,group);box(.06,1.9,.7,0x5e351c,0,1.07,0,group);box(.08,.12,.08,0xffd84d,-.13,1.08,.37,group);box(.08,.12,.08,0xffd84d,.13,1.08,.37,group);}`
- [x] `if` — linha 2685, coluna 5 em `createFurniture()` — `if(type==='desk'){`
- [x] `for` — linha 2686, coluna 44 em `createFurniture()` — `box(1.75,.16,.88,wood,0,.83,0,group);for(const ox of [-.65,.65])for(const oz of [-.28,.28])box(.12,.76,.12,wood,ox,.4,oz,group);`
- [x] `for` — linha 2686, coluna 71 em `createFurniture()` — `box(1.75,.16,.88,wood,0,.83,0,group);for(const ox of [-.65,.65])for(const oz of [-.28,.28])box(.12,.76,.12,wood,ox,.4,oz,group);`
- [x] `for` — linha 2688, coluna 108 em `createFurniture()` — `box(1.05,.14,.34,materials.fabric,0,.5,.72,group);box(1.05,.72,.14,materials.fabric,0,.83,.86,group);for(const ox of [-.42,.42])box(.1,.47,.1,metal,ox,.24,.72,group);`
- [x] `if` — linha 2690, coluna 5 em `createFurniture()` — `if(type==='bookshelf'){`
- [x] `for` — linha 2691, coluna 45 em `createFurniture()` — `box(1.72,2.25,.4,wood,0,1.12,0,group);for(const y of [.2,.75,1.3,1.85,2.22])box(1.68,.1,.46,shadeColor(0x89502b,-16),0,y,0,group);`
- [x] `for` — linha 2692, coluna 67 em `createFurniture()` — `const colors=[0xe34e52,0x3f8fd5,0xf0c441,0x55ad67,0x8d65c5];for(let row=0;row<4;row++)for(let i=0;i<7;i++)box(.16,.38,.3,colors[(i+row)%colors.length],-.62+i*.205,.43+row*.55,.04,group);`
- [x] `for` — linha 2692, coluna 93 em `createFurniture()` — `const colors=[0xe34e52,0x3f8fd5,0xf0c441,0x55ad67,0x8d65c5];for(let row=0;row<4;row++)for(let i=0;i<7;i++)box(.16,.38,.3,colors[(i+row)%colors.length],-.62+i*.205,.43+row*.55,.04,group);`
- [x] `if` — linha 2694, coluna 5 em `createFurniture()` — `if(type==='board'){`
- [x] `for` — linha 2696, coluna 7 em `createFurniture()` — `for(const ox of [-1.2,-.45,.35,1.05])box(.42,.06,.025,0xf1f5d5,ox,1.6,.075,group);`
- [x] `if` — linha 2698, coluna 5 em `createFurniture()` — `if(type==='bench'){box(2.25,.22,.62,materials.fabric,0,.62,0,group);box(2.25,.74,.18,materials.fabric,0,1.0,-.24,group);for(const ox of [-.82,.82])box(.14,.55,.14,metal,ox,.28,0,group);}`
- [x] `for` — linha 2698, coluna 125 em `createFurniture()` — `if(type==='bench'){box(2.25,.22,.62,materials.fabric,0,.62,0,group);box(2.25,.74,.18,materials.fabric,0,1.0,-.24,group);for(const ox of [-.82,.82])box(.14,.55,.14,metal,ox,.28,0,group);}`
- [x] `if` — linha 2699, coluna 5 em `createFurniture()` — `if(type==='radio'){`
- [x] `for` — linha 2700, coluna 95 em `createFurniture()` — `box(1.35,.88,.65,materials.metal,0,.7,0,group);box(.72,.42,.05,dark,-.18,.72,.35,group);for(let i=0;i<4;i++)box(.06,.32,.04,0x9aaaba,-.46+i*.14,.72,.39,group);`
- [x] `if` — linha 2703, coluna 5 em `createFurniture()` — `if(type==='plant'){`
- [x] `for` — linha 2705, coluna 7 em `createFurniture()` — `for(const [ox,oy,oz] of [[-.34,1.25,0],[.34,1.08,.08],[0,1.48,-.15],[-.15,1.05,.28]])premiumBox(.62,.22,.4,0x55b94b,ox,oy,oz,group);`
- [x] `return` — linha 2707, coluna 39 em `createFurniture()` — `house.interiorObjects.push(group);return {group,x,z,type,label};`
- [x] `for` — linha 2718, coluna 5 em `signTexture()` — `for(const word of words){`
- [x] `if` — linha 2721, coluna 7 em `signTexture()` — `if(ctx.measureText(candidate).width<=maxWidth||!lines[lines.length-1]) lines[lines.length-1]=candidate;`
- [x] `else` — linha 2722, coluna 7 em `signTexture()` — `else if(lines.length<2) lines.push(word);`
- [x] `if` — linha 2722, coluna 12 em `signTexture()` — `else if(lines.length<2) lines.push(word);`
- [x] `else` — linha 2723, coluna 7 em `signTexture()` — `else lines[1]+=' '+word;`
- [x] `while` — linha 2726, coluna 5 em `signTexture()` — `while(fontSize>30){`
- [x] `if` — linha 2728, coluna 7 em `signTexture()` — `if(lines.every(line=>ctx.measureText(line).width<=maxWidth)) break;`
- [x] `return` — linha 2739, coluna 5 em `signTexture()` — `return tex;`

## `13-houses-npcs-vehicles-base.js`

- [x] `return` — linha 2747, coluna 5 em `shadeColor()` — `return (r<<16)|(g<<8)|b;`
- [x] `if` — linha 2753, coluna 5 em `decorateHouseCommercial()` — `if(publicBuilding){`
- [x] `for` — linha 2755, coluna 7 em `decorateHouseCommercial()` — `for(let i=-4;i<=4;i++)box(.58,.18,1.25,i%2?0xfff5df:awningColor,x+i*.58,2.44,z+3.92,house.front);`
- [x] `else` — linha 2758, coluna 6 em `decorateHouseCommercial()` — `}else{`
- [x] `if` — linha 2762, coluna 29 em `decorateHouseCommercial()` — `house.roof.traverse(o=>{if(o.isMesh&&o.geometry?.type==='BoxGeometry')addVoxelOutline(o,0x182238,.24)});`
- [x] `if` — linha 2763, coluna 30 em `decorateHouseCommercial()` — `house.front.traverse(o=>{if(o.isMesh&&o.geometry?.type==='BoxGeometry')addVoxelOutline(o,0x182238,.25)});`
- [x] `for` — linha 2774, coluna 5 em `createHouse()` — `for(const cx of [-4.12,4.12]){box(.24,2.82,.4,corner,x+cx,1.5,z-3.28);box(.24,2.82,.4,corner,x+cx,1.5,z+3.28,house.front);}`
- [x] `if` — linha 2785, coluna 5 em `createHouse()` — `if(!publicBuilding){createFlower(x-3.1,z+4.7,shadeColor(0xff70c8,(id.charCodeAt(0)%3)*20-20));createFlower(x+3.1,z+4.7,0xffdf55);}`
- [x] `return` — linha 2790, coluna 43 em `createHouse()` — `decorateHouseCommercial(house,config);return house;`
- [x] `if` — linha 2794, coluna 5 em `addHouseInterior()` — `if(type==='home'){`
- [x] `else` — linha 2810, coluna 7 em `addHouseInterior()` — `} else if(type==='shop'){`
- [x] `if` — linha 2810, coluna 12 em `addHouseInterior()` — `} else if(type==='shop'){`
- [x] `for` — linha 2813, coluna 7 em `addHouseInterior()` — `for(const x of [-2.7,0,2.7]){createFurniture(house,'bookshelf',x,-1.35,0,'Prateleira de produtos');for(const z of [-.95,-1.65])premiumBox(.55,.32,.42,[0xffd84d,0x5bd18b,0xff7b72][Math.abs(Math.round(x))%3],house.x+x,.95,house.z+z);}`
- [x] `for` — linha 2813, coluna 106 em `addHouseInterior()` — `for(const x of [-2.7,0,2.7]){createFurniture(house,'bookshelf',x,-1.35,0,'Prateleira de produtos');for(const z of [-.95,-1.65])premiumBox(.55,.32,.42,[0xffd84d,0x5bd18b,0xff7b72][Math.abs(Math.round(x))%3],house.x+x,.95,house.z+z);}`
- [x] `for` — linha 2815, coluna 284 em `addHouseInterior()` — `const lampA=addGlow(house.x-2.3,2.35,house.z,0xffffff,5),lampB=addGlow(house.x+2.3,2.35,house.z,0xffffff,5);house.interiorObjects.push(lampA,lampB);premiumBox(2.4,.12,.82,0x4b7a5d,house.x-1.45,1.18,house.z+2.45);premiumBox(1.35,.42,.12,0xfff1b8,house.x-1.45,1.52,house.z+2.02);for(const px of [-2.8,-1.8,-.8,.8,1.8,2.8])premiumBox(.58,.08,.58,0xf7e9cb,house.x+px,2.55,house.z);`
- [x] `else` — linha 2816, coluna 7 em `addHouseInterior()` — `} else if(type==='workshop'){`
- [x] `if` — linha 2816, coluna 12 em `addHouseInterior()` — `} else if(type==='workshop'){`
- [x] `else` — linha 2819, coluna 7 em `addHouseInterior()` — `} else if(type==='school'){`
- [x] `if` — linha 2819, coluna 12 em `addHouseInterior()` — `} else if(type==='school'){`
- [x] `for` — linha 2822, coluna 7 em `addHouseInterior()` — `for(const [x,z] of [[-2.6,-1.2],[0,-1.2],[2.6,-1.2],[-2.6,.65],[0,.65],[2.6,.65]])createFurniture(house,'desk',x,z,0,'Carteira escolar');`
- [x] `for` — linha 2823, coluna 331 em `addHouseInterior()` — `createFurniture(house,'bookshelf',3.25,2.15,0,'Biblioteca');createFurniture(house,'plant',-3.3,2.1,0,'Horta da turma');createFurniture(house,'desk',0,2.15,0,'Mesa do professor');const teacherWardrobe=createFurniture(house,'wardrobe',-3.25,1.15,0,'Uniformes profissionais');registerActivity(house,teacherWardrobe,'wardrobe');for(const x of [-2.6,0,2.6])premiumBox(.7,.85,.28,0x4a90c4,house.x+x,.62,house.z+2.78);for(const [px,c] of [[-2.5,0xffd34d],[0,0x64d986],[2.5,0xff728e]])premiumBox(1.45,.85,.08,c,house.x+px,1.75,house.z-3.0);for(const px of [-2.7,0,2.7])premiumBox(.72,.08,.72,0xfff4d8,house.x+px,2.58,house.z);const lampA=addGlow(house.x-2.4,2.35,house.z,0xfff6cf,5),lampB=addGlow(house.x+2.4,2.35,house.z,0xfff6cf,5);house.interiorObjects.push(lampA,lampB);`
- [x] `for` — linha 2823, coluna 418 em `addHouseInterior()` — `createFurniture(house,'bookshelf',3.25,2.15,0,'Biblioteca');createFurniture(house,'plant',-3.3,2.1,0,'Horta da turma');createFurniture(house,'desk',0,2.15,0,'Mesa do professor');const teacherWardrobe=createFurniture(house,'wardrobe',-3.25,1.15,0,'Uniformes profissionais');registerActivity(house,teacherWardrobe,'wardrobe');for(const x of [-2.6,0,2.6])premiumBox(.7,.85,.28,0x4a90c4,house.x+x,.62,house.z+2.78);for(const [px,c] of [[-2.5,0xffd34d],[0,0x64d986],[2.5,0xff728e]])premiumBox(1.45,.85,.08,c,house.x+px,1.75,house.z-3.0);for(const px of [-2.7,0,2.7])premiumBox(.72,.08,.72,0xfff4d8,house.x+px,2.58,house.z);const lampA=addGlow(house.x-2.4,2.35,house.z,0xfff6cf,5),lampB=addGlow(house.x+2.4,2.35,house.z,0xfff6cf,5);house.interiorObjects.push(lampA,lampB);`
- [x] `for` — linha 2823, coluna 539 em `addHouseInterior()` — `createFurniture(house,'bookshelf',3.25,2.15,0,'Biblioteca');createFurniture(house,'plant',-3.3,2.1,0,'Horta da turma');createFurniture(house,'desk',0,2.15,0,'Mesa do professor');const teacherWardrobe=createFurniture(house,'wardrobe',-3.25,1.15,0,'Uniformes profissionais');registerActivity(house,teacherWardrobe,'wardrobe');for(const x of [-2.6,0,2.6])premiumBox(.7,.85,.28,0x4a90c4,house.x+x,.62,house.z+2.78);for(const [px,c] of [[-2.5,0xffd34d],[0,0x64d986],[2.5,0xff728e]])premiumBox(1.45,.85,.08,c,house.x+px,1.75,house.z-3.0);for(const px of [-2.7,0,2.7])premiumBox(.72,.08,.72,0xfff4d8,house.x+px,2.58,house.z);const lampA=addGlow(house.x-2.4,2.35,house.z,0xfff6cf,5),lampB=addGlow(house.x+2.4,2.35,house.z,0xfff6cf,5);house.interiorObjects.push(lampA,lampB);`
- [x] `else` — linha 2824, coluna 7 em `addHouseInterior()` — `} else if(type==='police'){`
- [x] `if` — linha 2824, coluna 12 em `addHouseInterior()` — `} else if(type==='police'){`
- [x] `for` — linha 2828, coluna 333 em `addHouseInterior()` — `createFurniture(house,'board',0,2.7,0,'Regras de trânsito');createFurniture(house,'bookshelf',-3.25,1.7,0,'Guias de segurança');const policeWardrobe=createFurniture(house,'wardrobe',3.15,-1.85,0,'Armário de uniformes');registerActivity(house,policeWardrobe,'wardrobe');premiumBox(8.0,.22,.1,0x245da8,house.x,1.08,house.z-3.0);for(const px of [-1.0,0,1.0])premiumBox(.72,.42,.08,0x7edfff,house.x+px,1.48,house.z-2.94);premiumBox(1.65,.08,1.0,0x2a4058,house.x,2.55,house.z);`
- [x] `else` — linha 2829, coluna 7 em `addHouseInterior()` — `} else if(type==='firestation'){`
- [x] `if` — linha 2829, coluna 12 em `addHouseInterior()` — `} else if(type==='firestation'){`
- [x] `for` — linha 2833, coluna 7 em `addHouseInterior()` — `for(const x of [1.75,2.45,3.15])premiumCylinder(.18,1.7,0xe24b3f,house.x+x,.9,house.z+2.45,worldGroup,12);for(let line=-3;line<=3;line+=1.2)premiumBox(.48,.025,5.6,line<0?0xf1c83b:0x222b34,house.x+line,.205,house.z);for(const px of [-2.9,-2.15,-1.4]){premiumBox(.62,1.65,.55,0xc43d38,house.x+px,.88,house.z+2.35);premiumBox(.5,.18,.62,0xf1cc3d,house.x+px,1.8,house.z+2.35);}premiumBox(2.2,.08,1.0,0xfff0cf,house.x,2.55,house.z);`
- [x] `for` — linha 2833, coluna 113 em `addHouseInterior()` — `for(const x of [1.75,2.45,3.15])premiumCylinder(.18,1.7,0xe24b3f,house.x+x,.9,house.z+2.45,worldGroup,12);for(let line=-3;line<=3;line+=1.2)premiumBox(.48,.025,5.6,line<0?0xf1c83b:0x222b34,house.x+line,.205,house.z);for(const px of [-2.9,-2.15,-1.4]){premiumBox(.62,1.65,.55,0xc43d38,house.x+px,.88,house.z+2.35);premiumBox(.5,.18,.62,0xf1cc3d,house.x+px,1.8,house.z+2.35);}premiumBox(2.2,.08,1.0,0xfff0cf,house.x,2.55,house.z);`
- [x] `for` — linha 2833, coluna 223 em `addHouseInterior()` — `for(const x of [1.75,2.45,3.15])premiumCylinder(.18,1.7,0xe24b3f,house.x+x,.9,house.z+2.45,worldGroup,12);for(let line=-3;line<=3;line+=1.2)premiumBox(.48,.025,5.6,line<0?0xf1c83b:0x222b34,house.x+line,.205,house.z);for(const px of [-2.9,-2.15,-1.4]){premiumBox(.62,1.65,.55,0xc43d38,house.x+px,.88,house.z+2.35);premiumBox(.5,.18,.62,0xf1cc3d,house.x+px,1.8,house.z+2.35);}premiumBox(2.2,.08,1.0,0xfff0cf,house.x,2.55,house.z);`
- [x] `else` — linha 2834, coluna 7 em `addHouseInterior()` — `} else if(type==='neighbor'){`
- [x] `if` — linha 2834, coluna 12 em `addHouseInterior()` — `} else if(type==='neighbor'){`
- [x] `return` — linha 2845, coluna 31 em `activityIcon()` — `function activityIcon(type){return ({bed:'🛏',sofa:'🛋',tv:'📺',fridge:'🍎',stove:'🍳',sink:'💧',shower:'🚿',chest:'🎁',shop:'🛒',workshop:'🛠',wardrobe:'👕',school:'🏫',police:'🛡️',firestation:'🚒'})[type]||'✋';}`
- [x] `if` — linha 2853, coluna 47 em `createNPC()` — `box(.76,.18,.64,hairColor,0,2.38,0,group);if(hash%3===0){box(.18,.42,.58,hairColor,-.34,2.15,0,group);box(.18,.42,.58,hairColor,.34,2.15,0,group);}else if(hash%3===1){box(.72,.12,.12,hairColor,0,2.14,.34,group);}`
- [x] `else` — linha 2853, coluna 152 em `createNPC()` — `box(.76,.18,.64,hairColor,0,2.38,0,group);if(hash%3===0){box(.18,.42,.58,hairColor,-.34,2.15,0,group);box(.18,.42,.58,hairColor,.34,2.15,0,group);}else if(hash%3===1){box(.72,.12,.12,hairColor,0,2.14,.34,group);}`
- [x] `if` — linha 2853, coluna 157 em `createNPC()` — `box(.76,.18,.64,hairColor,0,2.38,0,group);if(hash%3===0){box(.18,.42,.58,hairColor,-.34,2.15,0,group);box(.18,.42,.58,hairColor,.34,2.15,0,group);}else if(hash%3===1){box(.72,.12,.12,hairColor,0,2.14,.34,group);}`
- [x] `for` — linha 2858, coluna 5 em `createNPC()` — `for(const arm of [leftArm,rightArm]){box(.24,.48,.24,shirt,0,-.22,0,arm);box(.22,.25,.22,skin,0,-.58,0,arm);}`
- [x] `for` — linha 2859, coluna 5 em `createNPC()` — `for(const leg of [leftLeg,rightLeg]){box(.24,.58,.25,pants,0,-.28,0,leg);box(.27,.18,.34,shoe,0,-.65,.06,leg);}`
- [x] `if` — linha 2860, coluna 5 em `createNPC()` — `if(hash%4===0)box(.9,.18,.7,0x2f7ed6,0,2.48,0,group);`
- [x] `if` — linha 2862, coluna 24 em `createNPC()` — `group.traverse(o=>{if(o.isMesh)addVoxelOutline(o,0x172033,.3);});`
- [x] `return` — linha 2864, coluna 226 em `createNPC()` — `world.npcs.push(npc);registerInteractable({id:\`npc-${id}\`,type:'npc',icon:'💬',label:\`Conversar com ${name}\`,radius:2.7,priority:160,getPos:()=>({x:npc.group.position.x,z:npc.group.position.z}),action:()=>talkToNPC(npc)});return npc;`
- [x] `if` — linha 2867, coluna 5 em `createNpcMobility()` — `if(!npc||!Array.isArray(route)||route.length<2)return npc;const ride=new THREE.Group();npc.group.add(ride);const wheels=[];`
- [x] `return` — linha 2867, coluna 52 em `createNpcMobility()` — `if(!npc||!Array.isArray(route)||route.length<2)return npc;const ride=new THREE.Group();npc.group.add(ride);const wheels=[];`
- [x] `if` — linha 2868, coluna 5 em `createNpcMobility()` — `if(type==='car'){`
- [x] `for` — linha 2870, coluna 7 em `createNpcMobility()` — `for(const p of [[-.78,.28,-.72],[.78,.28,-.72],[-.78,.28,.72],[.78,.28,.72]]){const wheel=premiumCylinder(.28,.24,0x121821,p[0],p[1],p[2],ride,10);wheel.rotation.z=Math.PI/2;wheels.push(wheel);}`
- [x] `else` — linha 2871, coluna 6 em `createNpcMobility()` — `}else if(type==='moto'){`
- [x] `if` — linha 2871, coluna 11 em `createNpcMobility()` — `}else if(type==='moto'){`
- [x] `for` — linha 2872, coluna 7 em `createNpcMobility()` — `for(const z of [-.72,.72]){const wheel=premiumCylinder(.34,.13,0x111822,0,.36,z,ride,12);wheel.rotation.z=Math.PI/2;wheels.push(wheel);}premiumBox(.28,.26,1.15,npc.color,0,.58,0,ride);premiumBox(.56,.15,.42,0x202c3b,0,.76,-.12,ride);premiumBox(.7,.06,.08,0xd7e2eb,0,1.05,.53,ride);`
- [x] `else` — linha 2873, coluna 6 em `createNpcMobility()` — `}else if(type==='bike'){`
- [x] `if` — linha 2873, coluna 11 em `createNpcMobility()` — `}else if(type==='bike'){`
- [x] `for` — linha 2874, coluna 7 em `createNpcMobility()` — `for(const z of [-.72,.72]){const wheel=premiumCylinder(.34,.07,0x17202b,0,.36,z,ride,14);wheel.rotation.z=Math.PI/2;wheels.push(wheel);}premiumBox(.08,.65,1.2,npc.color,0,.62,0,ride);premiumBox(.58,.07,.08,0xe8eef3,0,1.05,.62,ride);premiumBox(.45,.12,.34,0x25364a,0,.84,-.2,ride);`
- [x] `else` — linha 2875, coluna 6 em `createNpcMobility()` — `}else{`
- [x] `for` — linha 2876, coluna 53 em `createNpcMobility()` — `premiumBox(.62,.1,1.2,npc.color,0,.12,0,ride);for(const p of [[-.25,.08,-.42],[.25,.08,-.42],[-.25,.08,.42],[.25,.08,.42]]){const wheel=premiumCylinder(.09,.08,0x1c2633,p[0],p[1],p[2],ride,8);wheel.rotation.z=Math.PI/2;wheels.push(wheel);}`
- [x] `if` — linha 2878, coluna 23 em `createNpcMobility()` — `ride.traverse(o=>{if(o.isMesh)addVoxelOutline(o,0x132238,.22);});const projected=route.map(p=>{const q=nearestRoadProjection({x:p[0],z:p[1]})?.point;return q?{x:q.x,z:q.z}:{x:p[0],z:p[1]};});npc.group.position.set(projected[0].x,0,projected[0].z);npc.baseX=projected[0].x;npc.baseZ=projected[0].z;npc.mobility={id:\`mobility-${npc.id}\`,type,route:projected,index:1,speed:speed||({car:4.4,moto:4.8,bike:3.2,skate:2.8})[type]||3,currentSpeed:0,radius:type==='car'?1.45:type==='moto'?.9:.7,group:npc.group,ride,wheels};return npc;`
- [x] `return` — linha 2878, coluna 154 em `createNpcMobility()` — `ride.traverse(o=>{if(o.isMesh)addVoxelOutline(o,0x132238,.22);});const projected=route.map(p=>{const q=nearestRoadProjection({x:p[0],z:p[1]})?.point;return q?{x:q.x,z:q.z}:{x:p[0],z:p[1]};});npc.group.position.set(projected[0].x,0,projected[0].z);npc.baseX=projected[0].x;npc.baseZ=projected[0].z;npc.mobility={id:\`mobility-${npc.id}\`,type,route:projected,index:1,speed:speed||({car:4.4,moto:4.8,bike:3.2,skate:2.8})[type]||3,currentSpeed:0,radius:type==='car'?1.45:type==='moto'?.9:.7,group:npc.group,ride,wheels};return npc;`
- [x] `return` — linha 2878, coluna 520 em `createNpcMobility()` — `ride.traverse(o=>{if(o.isMesh)addVoxelOutline(o,0x132238,.22);});const projected=route.map(p=>{const q=nearestRoadProjection({x:p[0],z:p[1]})?.point;return q?{x:q.x,z:q.z}:{x:p[0],z:p[1]};});npc.group.position.set(projected[0].x,0,projected[0].z);npc.baseX=projected[0].x;npc.baseZ=projected[0].z;npc.mobility={id:\`mobility-${npc.id}\`,type,route:projected,index:1,speed:speed||({car:4.4,moto:4.8,bike:3.2,skate:2.8})[type]||3,currentSpeed:0,radius:type==='car'?1.45:type==='moto'?.9:.7,group:npc.group,ride,wheels};return npc;`
- [x] `if` — linha 2882, coluna 5 em `createEnemy()` — `if(type==='slime'){box(1.35,.85,1.35,0x31c65b,0,.45,0,group);box(.18,.12,.05,0xff2441,-.28,.55,.7,group);box(.18,.12,.05,0xff2441,.28,.55,.7,group);}`
- [x] `else` — linha 2883, coluna 5 em `createEnemy()` — `else if(type==='bat'){box(1.0,.75,1.0,0x35165e,0,1.4,0,group);box(.8,.18,.45,0x8c4ddb,-.8,1.4,0,group);box(.8,.18,.45,0x8c4ddb,.8,1.4,0,group);box(.12,.1,.05,0xff31f5,-.22,1.48,.54,group);box(.12,.1,.05,0xff31f5,.22,1.48,.54,group);}`
- [x] `if` — linha 2883, coluna 10 em `createEnemy()` — `else if(type==='bat'){box(1.0,.75,1.0,0x35165e,0,1.4,0,group);box(.8,.18,.45,0x8c4ddb,-.8,1.4,0,group);box(.8,.18,.45,0x8c4ddb,.8,1.4,0,group);box(.12,.1,.05,0xff31f5,-.22,1.48,.54,group);box(.12,.1,.05,0xff31f5,.22,1.48,.54,group);}`
- [x] `else` — linha 2884, coluna 5 em `createEnemy()` — `else {box(1.5,1.8,1.2,0x788495,0,1.0,0,group);box(1.0,.8,1.0,0x647080,0,2.2,0,group);box(.14,.1,.05,0xff293f,-.22,2.25,.52,group);box(.14,.1,.05,0xff293f,.22,2.25,.52,group);}`
- [x] `return` — linha 2885, coluna 187 em `createEnemy()` — `const enemy={id:\`enemy-${type}-${world.enemies.length}\`,type,x,z,baseX:x,baseZ:z,group,hp:type==='golem'?3:1,phase:Math.random()*6.28,dead:false,lastHit:0};world.enemies.push(enemy);return enemy;`
- [x] `if` — linha 2892, coluna 269 em `createChest()` — `const group=new THREE.Group();group.position.set(x,0,z);worldGroup.add(group);box(1.2,.72,.9,materials.wood,0,.36,0,group);const lid=box(1.25,.22,.95,secret?0xa855f7:0xffd84d,0,.84,0,group);const chest={id,x,z,group,lid,opened:!!state.flags[\`chest_${id}\`],secret};if(chest.opened)lid.rotation.x=-.6;registerInteractable({id:\`chest-${id}\`,type:'chest',icon:'🎁',label:secret?'Pegar presente secreto':'Abrir presente/baú',x,z,radius:2,priority:200,action:()=>openChest(chest)});return chest;`
- [x] `return` — linha 2892, coluna 480 em `createChest()` — `const group=new THREE.Group();group.position.set(x,0,z);worldGroup.add(group);box(1.2,.72,.9,materials.wood,0,.36,0,group);const lid=box(1.25,.22,.95,secret?0xa855f7:0xffd84d,0,.84,0,group);const chest={id,x,z,group,lid,opened:!!state.flags[\`chest_${id}\`],secret};if(chest.opened)lid.rotation.x=-.6;registerInteractable({id:\`chest-${id}\`,type:'chest',icon:'🎁',label:secret?'Pegar presente secreto':'Abrir presente/baú',x,z,radius:2,priority:200,action:()=>openChest(chest)});return chest;`
- [x] `return` — linha 2895, coluna 28 em `vehicleById()` — `function vehicleById(id){return (world.vehicles||[]).find(v=>v.id===id)||null;}`
- [x] `if` — linha 2896, coluna 32 em `currentVehicleRef()` — `function currentVehicleRef(){if(activeVehicleRef)return activeVehicleRef;const byId=vehicleById(player.car.id);if(byId)return byId;if(world.activeVehicle)return world.activeVehicle;return player.car.id?null:(world.vehicle||null);}`
- [x] `return` — linha 2896, coluna 52 em `currentVehicleRef()` — `function currentVehicleRef(){if(activeVehicleRef)return activeVehicleRef;const byId=vehicleById(player.car.id);if(byId)return byId;if(world.activeVehicle)return world.activeVehicle;return player.car.id?null:(world.vehicle||null);}`
- [x] `if` — linha 2896, coluna 114 em `currentVehicleRef()` — `function currentVehicleRef(){if(activeVehicleRef)return activeVehicleRef;const byId=vehicleById(player.car.id);if(byId)return byId;if(world.activeVehicle)return world.activeVehicle;return player.car.id?null:(world.vehicle||null);}`
- [x] `return` — linha 2896, coluna 122 em `currentVehicleRef()` — `function currentVehicleRef(){if(activeVehicleRef)return activeVehicleRef;const byId=vehicleById(player.car.id);if(byId)return byId;if(world.activeVehicle)return world.activeVehicle;return player.car.id?null:(world.vehicle||null);}`
- [x] `if` — linha 2896, coluna 134 em `currentVehicleRef()` — `function currentVehicleRef(){if(activeVehicleRef)return activeVehicleRef;const byId=vehicleById(player.car.id);if(byId)return byId;if(world.activeVehicle)return world.activeVehicle;return player.car.id?null:(world.vehicle||null);}`
- [x] `return` — linha 2896, coluna 157 em `currentVehicleRef()` — `function currentVehicleRef(){if(activeVehicleRef)return activeVehicleRef;const byId=vehicleById(player.car.id);if(byId)return byId;if(world.activeVehicle)return world.activeVehicle;return player.car.id?null:(world.vehicle||null);}`
- [x] `return` — linha 2896, coluna 184 em `currentVehicleRef()` — `function currentVehicleRef(){if(activeVehicleRef)return activeVehicleRef;const byId=vehicleById(player.car.id);if(byId)return byId;if(world.activeVehicle)return world.activeVehicle;return player.car.id?null:(world.vehicle||null);}`
- [x] `if` — linha 2898, coluna 84 em `applyVehicleAppearance()` — `const a=vehicle?.appearance||{},m=vehicleVisual?.userData?.appearanceMaterials;if(!m)return;`
- [x] `return` — linha 2898, coluna 90 em `applyVehicleAppearance()` — `const a=vehicle?.appearance||{},m=vehicleVisual?.userData?.appearanceMaterials;if(!m)return;`
- [x] `if` — linha 2900, coluna 5 em `applyVehicleAppearance()` — `if(typeof applyServiceVehicleVisual==='function')applyServiceVehicleVisual(vehicle);`
- [x] `if` — linha 2903, coluna 5 em `persistParkedVehicle()` — `if(!vehicle||!state.vehicles)return;state.vehicles.parked[vehicle.id]={x:+vehicle.group.position.x.toFixed(2),z:+vehicle.group.position.z.toFixed(2),heading:+vehicle.group.rotation.y.toFixed(3)};state.vehicles.lastUsedId=vehicle.id;`
- [x] `return` — linha 2903, coluna 34 em `persistParkedVehicle()` — `if(!vehicle||!state.vehicles)return;state.vehicles.parked[vehicle.id]={x:+vehicle.group.position.x.toFixed(2),z:+vehicle.group.position.z.toFixed(2),heading:+vehicle.group.rotation.y.toFixed(3)};state.vehicles.lastUsedId=vehicle.id;`
- [x] `for` — linha 2912, coluna 5 em `createToyCar()` — `for(const p of [[-.84,.24,-.79],[.84,.24,-.79],[-.84,.24,.79],[.84,.24,.79]]){const wheel=cylinder(.34,.28,0x10151d,p[0],p[1],p[2],group,14);wheel.rotation.z=Math.PI/2;const hub=cylinder(.12,.3,0xf5a623,p[0],p[1],p[2],group,10);hub.rotation.z=Math.PI/2;}`
- [x] `if` — linha 2913, coluna 24 em `createToyCar()` — `group.traverse(o=>{if(o.isMesh)addVoxelOutline(o,0x14243a,.28);});`
- [x] `if` — linha 2914, coluna 267 em `createToyCar()` — `const vehicle={id,x:group.position.x,z:group.position.z,heading,group,label:options.label||'Carro da cidade',kind:options.kind||'car',serviceType:options.serviceType||'',appearance,occupied:false,radius:Number(options.radius||1.55)};world.vehicles.push(vehicle);if(!world.vehicle)world.vehicle=vehicle;`
- [x] `return` — linha 2915, coluna 286 em `createToyCar()` — `registerInteractable({id:\`vehicle-${id}\`,type:'vehicle',icon:'🚗',label:\`Entrar: ${vehicle.label}\`,radius:2.5,priority:155,getPos:()=>({x:vehicle.group.position.x,z:vehicle.group.position.z}),available:()=>!vehicle.occupied&&vehicle.group.visible,action:()=>enterVehicle(vehicle)});return vehicle;`
- [x] `if` — linha 2925, coluna 5 em `updateWaypointMarker()` — `if(!world.waypointMarker)return;`
- [x] `return` — linha 2925, coluna 30 em `updateWaypointMarker()` — `if(!world.waypointMarker)return;`
- [x] `if` — linha 2927, coluna 5 em `updateWaypointMarker()` — `if(wp)world.waypointMarker.position.set(wp.x,0,wp.z);`
- [x] `for` — linha 2934, coluna 5 em `createAthleticsGym()` — `for(let i=0;i<segments;i++){`
- [x] `for` — linha 2936, coluna 7 em `createAthleticsGym()` — `for(const offset of [0,2.15,-2.15]){`
- [x] `for` — linha 2943, coluna 5 em `createAthleticsGym()` — `for(let stripe=-4;stripe<=4;stripe++)premiumBox(43,0.035,1.35,stripe%2?0x3b9b4d:0x46aa57,gym.centerX,.19,gym.centerZ+stripe*1.35,worldGroup);`
- [x] `for` — linha 2944, coluna 5 em `createAthleticsGym()` — `for(let j=-2;j<=2;j++)premiumBox(.18,.07,.72,j%2?0x111827:0xffffff,gym.finishX,.24,gym.centerZ+j*.72,worldGroup);`
- [x] `for` — linha 2947, coluna 5 em `createAthleticsGym()` — `for(let row=0;row<3;row++)premiumBox(16-row*1.1,.42,1.25,row%2?0x6f98b8:0x5d83a1,51,.45+row*.48,87.2+row*.72,worldGroup);`
- [x] `for` — linha 2948, coluna 5 em `createAthleticsGym()` — `for(const x of [27,39,51,63,75])premiumBox(.42,.09,.42,0xf5d84d,x,.25,66.5,worldGroup);`
- [x] `if` — linha 2956, coluna 7 em `createSizeChallenges()` — `if(player.scaleMode!=='mini'){toast('Use o botão MINI para passar.','warn',2200);return;}player.z=39.5;setFlag('miniPassage');addXP(25);toast('Passagem mini concluída!','good');`
- [x] `return` — linha 2956, coluna 88 em `createSizeChallenges()` — `if(player.scaleMode!=='mini'){toast('Use o botão MINI para passar.','warn',2200);return;}player.z=39.5;setFlag('miniPassage');addXP(25);toast('Passagem mini concluída!','good');`
- [x] `if` — linha 2961, coluna 7 em `createSizeChallenges()` — `if(!player.crouched){toast('Use ABAIXAR para entrar.','warn',2200);return;}player.z=21.5;setFlag('crouchPassage');addXP(25);toast('Túnel baixo concluído!','good');`
- [x] `return` — linha 2961, coluna 74 em `createSizeChallenges()` — `if(!player.crouched){toast('Use ABAIXAR para entrar.','warn',2200);return;}player.z=21.5;setFlag('crouchPassage');addXP(25);toast('Túnel baixo concluído!','good');`
- [x] `if` — linha 2966, coluna 7 em `createSizeChallenges()` — `if(player.scaleMode!=='giant'){toast('Use GRANDE para abrir o portão.','warn',2200);return;}setFlag('giantGate');addXP(35);toast('Portão pesado aberto!','good');`
- [x] `return` — linha 2966, coluna 91 em `createSizeChallenges()` — `if(player.scaleMode!=='giant'){toast('Use GRANDE para abrir o portão.','warn',2200);return;}setFlag('giantGate');addXP(35);toast('Portão pesado aberto!','good');`
- [x] `for` — linha 2980, coluna 5 em `createSkyDome()` — `for(let i=0;i<7;i++){`
- [x] `if` — linha 2987, coluna 5 em `updateClouds()` — `if(textures.water){textures.water.offset.x=(textures.water.offset.x+dt*.012)%1;textures.water.offset.y=(textures.water.offset.y+dt*.007)%1;}`
- [x] `if` — linha 2988, coluna 5 em `updateClouds()` — `if(!world.clouds)return;`
- [x] `return` — linha 2988, coluna 22 em `updateClouds()` — `if(!world.clouds)return;`
- [x] `for` — linha 2989, coluna 5 em `updateClouds()` — `for(const c of world.clouds){c.group.position.x+=c.speed*dt;if(c.group.position.x>210)c.group.position.x=-210;}`
- [x] `if` — linha 2989, coluna 65 em `updateClouds()` — `for(const c of world.clouds){c.group.position.x+=c.speed*dt;if(c.group.position.x>210)c.group.position.x=-210;}`

## `14-world-district-decoration.js`

- [x] `if` — linha 3000, coluna 5 em `createVoxelMushroom()` — `if(scale>.95)addVoxelOutline(g.children[0],0x5d3b24,.24);world.landmarks.push(g);return g;`
- [x] `return` — linha 3000, coluna 86 em `createVoxelMushroom()` — `if(scale>.95)addVoxelOutline(g.children[0],0x5d3b24,.24);world.landmarks.push(g);return g;`
- [x] `return` — linha 3003, coluna 468 em `iconTexture()` — `const c=document.createElement('canvas');c.width=c.height=256;const ctx=c.getContext('2d');ctx.fillStyle=bg;ctx.fillRect(0,0,256,256);ctx.strokeStyle='rgba(255,255,255,.75)';ctx.lineWidth=18;ctx.strokeRect(12,12,232,232);ctx.fillStyle=fg;ctx.font='900 150px system-ui';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(symbol,128,137);const tex=new THREE.CanvasTexture(c);tex.magFilter=THREE.NearestFilter;tex.minFilter=THREE.LinearMipmapLinearFilter;return tex;`
- [x] `return` — linha 3005, coluna 297 em `createChallengeCube()` — `function createChallengeCube(x,y,z,symbol='◆',color='#ffd33f'){const material=new THREE.MeshStandardMaterial({map:iconTexture(symbol,color),roughness:.5,emissive:0x5c3f00,emissiveIntensity:.16});const cube=box(1.75,1.75,1.75,material,x,y,z);cube.userData.floatBase=y;world.landmarks.push(cube);return cube;}`
- [x] `for` — linha 3009, coluna 5 em `createPortalArch()` — `for(let i=0;i<9;i++){const ang=Math.PI*i/8;const p=new THREE.Mesh(new THREE.BoxGeometry(.55,.55,.35),glow);p.position.set(Math.cos(ang)*2.35,3.3+Math.sin(ang)*2.35,.25);p.rotation.z=-ang;g.add(p);}`
- [x] `return` — linha 3010, coluna 58 em `createPortalArch()` — `addGlow(x,3.4,z,0x2de8ff,14);world.landmarks.push(g);return g;`
- [x] `for` — linha 3014, coluna 80 em `createPlayground()` — `const slide=box(1.35,.22,4.2,0xfacc15,1.7,1.1,1.5,g);slide.rotation.x=-.42;for(const sx of [-2.4,2.4]){box(.18,3.4,.18,materials.wood,sx,1.7,-2,g);box(.18,3.4,.18,materials.wood,sx+1.1,1.7,-2,g);}box(5.3,.18,.18,0xef4444,.55,3.25,-2,g);world.landmarks.push(g);return g;`
- [x] `return` — linha 3014, coluna 265 em `createPlayground()` — `const slide=box(1.35,.22,4.2,0xfacc15,1.7,1.1,1.5,g);slide.rotation.x=-.42;for(const sx of [-2.4,2.4]){box(.18,3.4,.18,materials.wood,sx,1.7,-2,g);box(.18,3.4,.18,materials.wood,sx+1.1,1.7,-2,g);}box(5.3,.18,.18,0xef4444,.55,3.25,-2,g);world.landmarks.push(g);return g;`
- [x] `return` — linha 3016, coluna 431 em `createFountain()` — `function createFountain(x,z){const g=new THREE.Group();g.position.set(x,0,z);worldGroup.add(g);cylinder(3,.32,0x9aa9b8,0,.16,0,g,16);cylinder(2.35,.22,0x31b7e8,0,.36,0,g,16);cylinder(.45,2.3,0xb8c4cf,0,1.35,0,g,12);const orb=new THREE.Mesh(new THREE.OctahedronGeometry(.55,0),mat(0x42e7ff,{emissive:0x05a8cc,emissiveIntensity:1.2,roughness:.2}));orb.position.y=2.8;g.add(orb);addGlow(x,2.8,z,0x42e7ff,7);world.landmarks.push(g);return g;}`
- [x] `for` — linha 3017, coluna 144 em `createAwning()` — `function createAwning(x,z,color=0xef4444,rotation=0){const g=new THREE.Group();g.position.set(x,0,z);g.rotation.y=rotation;worldGroup.add(g);for(let i=-3;i<=3;i++)box(.55,.18,1.25,i%2?0xfff7e8:color,i*.55,2.15,0,g);world.landmarks.push(g);return g;}`
- [x] `return` — linha 3017, coluna 242 em `createAwning()` — `function createAwning(x,z,color=0xef4444,rotation=0){const g=new THREE.Group();g.position.set(x,0,z);g.rotation.y=rotation;worldGroup.add(g);for(let i=-3;i<=3;i++)box(.55,.18,1.25,i%2?0xfff7e8:color,i*.55,2.15,0,g);world.landmarks.push(g);return g;}`
- [x] `return` — linha 3024, coluna 142 em `createStreetTree()` — `const g=createManagedLOD(\`street-tree-${x}-${z}\`,high,low,{distance:34,category:'urban-nature'});g.position.set(x,0,z);worldGroup.add(g);return g;`
- [x] `for` — linha 3031, coluna 5 em `createBackdropBuilding()` — `for(let yy=1.25;yy<h-1;yy+=1.65)for(let xx=-w/2+.85;xx<w/2-.35;xx+=1.5)box(.76,.7,.08,windowMat,xx,yy,front,high);`
- [x] `for` — linha 3031, coluna 37 em `createBackdropBuilding()` — `for(let yy=1.25;yy<h-1;yy+=1.65)for(let xx=-w/2+.85;xx<w/2-.35;xx+=1.5)box(.76,.7,.08,windowMat,xx,yy,front,high);`
- [x] `return` — linha 3035, coluna 159 em `createBackdropBuilding()` — `const g=createManagedLOD(\`backdrop-${x}-${z}\`,high,low,{distance:68,category:'backdrop'});g.position.set(x,0,z);worldGroup.add(g);world.landmarks.push(g);return g;`
- [x] `for` — linha 3039, coluna 5 em `createFloatingIsland()` — `for(let i=0;i<4;i++){const rock=new THREE.Mesh(new THREE.ConeGeometry((2.7-i*.45)*scale,2.1*scale,4),materials.stone);rock.position.y=-1.3*scale-i*.65*scale;rock.rotation.y=Math.PI/4;g.add(rock);}const crystal=new THREE.Mesh(new THREE.OctahedronGeometry(.65*scale,0),mat(0x4de5ff,{emissive:0x009ac8,emissiveIntensity:1.3,roughness:.15}));crystal.position.y=1.9*scale;g.add(crystal);addVoxelOutline(crystal,0x15334c,.35);world.landmarks.push(g);return g;`
- [x] `return` — linha 3039, coluna 449 em `createFloatingIsland()` — `for(let i=0;i<4;i++){const rock=new THREE.Mesh(new THREE.ConeGeometry((2.7-i*.45)*scale,2.1*scale,4),materials.stone);rock.position.y=-1.3*scale-i*.65*scale;rock.rotation.y=Math.PI/4;g.add(rock);}const crystal=new THREE.Mesh(new THREE.OctahedronGeometry(.65*scale,0),mat(0x4de5ff,{emissive:0x009ac8,emissiveIntensity:1.3,roughness:.15}));crystal.position.y=1.9*scale;g.add(crystal);addVoxelOutline(crystal,0x15334c,.35);world.landmarks.push(g);return g;`

## `15-transit-bus-metro.js`

- [x] `for` — linha 3088, coluna 18 em `compactBusPath()` — `const out=[];for(const item of points||[]){if(!item||!Number.isFinite(Number(item.x))||!Number.isFinite(Number(item.z)))continue;const p={...item,x:+item.x,z:+item.z},last=out[out.length-1];if(last&&Math.hypot(last.x-p.x,last.z-p.z)<.12){if(p.stopId)Object.assign(last,p);continue;}out.push(p);}return out;`
- [x] `if` — linha 3088, coluna 48 em `compactBusPath()` — `const out=[];for(const item of points||[]){if(!item||!Number.isFinite(Number(item.x))||!Number.isFinite(Number(item.z)))continue;const p={...item,x:+item.x,z:+item.z},last=out[out.length-1];if(last&&Math.hypot(last.x-p.x,last.z-p.z)<.12){if(p.stopId)Object.assign(last,p);continue;}out.push(p);}return out;`
- [x] `if` — linha 3088, coluna 195 em `compactBusPath()` — `const out=[];for(const item of points||[]){if(!item||!Number.isFinite(Number(item.x))||!Number.isFinite(Number(item.z)))continue;const p={...item,x:+item.x,z:+item.z},last=out[out.length-1];if(last&&Math.hypot(last.x-p.x,last.z-p.z)<.12){if(p.stopId)Object.assign(last,p);continue;}out.push(p);}return out;`
- [x] `if` — linha 3088, coluna 243 em `compactBusPath()` — `const out=[];for(const item of points||[]){if(!item||!Number.isFinite(Number(item.x))||!Number.isFinite(Number(item.z)))continue;const p={...item,x:+item.x,z:+item.z},last=out[out.length-1];if(last&&Math.hypot(last.x-p.x,last.z-p.z)<.12){if(p.stopId)Object.assign(last,p);continue;}out.push(p);}return out;`
- [x] `return` — linha 3088, coluna 300 em `compactBusPath()` — `const out=[];for(const item of points||[]){if(!item||!Number.isFinite(Number(item.x))||!Number.isFinite(Number(item.z)))continue;const p={...item,x:+item.x,z:+item.z},last=out[out.length-1];if(last&&Math.hypot(last.x-p.x,last.z-p.z)<.12){if(p.stopId)Object.assign(last,p);continue;}out.push(p);}return out;`
- [x] `return` — linha 3090, coluna 83 em `projectedBusPoint()` — `function projectedBusPoint(point){const projection=nearestRoadProjection(point);return projection?.point?{...point,x:projection.point.x,z:projection.point.z}:{...point};}`
- [x] `if` — linha 3092, coluna 41 em `offsetBusPath()` — `const source=compactBusPath(points);if(source.length<2)return source;`
- [x] `return` — linha 3092, coluna 60 em `offsetBusPath()` — `const source=compactBusPath(points);if(source.length<2)return source;`
- [x] `return` — linha 3093, coluna 5 em `offsetBusPath()` — `return source.map((point,index)=>{`
- [x] `for` — linha 3095, coluna 7 em `offsetBusPath()` — `for(const amount of [offset,offset*.72,offset*.45,0]){const x=point.x+rightX*amount,z=point.z+rightZ*amount;if(pointOnRoad(x,z,.02))return{...point,x,z};}`
- [x] `if` — linha 3095, coluna 115 em `offsetBusPath()` — `for(const amount of [offset,offset*.72,offset*.45,0]){const x=point.x+rightX*amount,z=point.z+rightZ*amount;if(pointOnRoad(x,z,.02))return{...point,x,z};}`
- [x] `return` — linha 3095, coluna 139 em `offsetBusPath()` — `for(const amount of [offset,offset*.72,offset*.45,0]){const x=point.x+rightX*amount,z=point.z+rightZ*amount;if(pointOnRoad(x,z,.02))return{...point,x,z};}`
- [x] `return` — linha 3096, coluna 7 em `offsetBusPath()` — `return{...point};`
- [x] `if` — linha 3100, coluna 36 em `buildBusRoadPath()` — `const source=route?.points||[];if(source.length<2)return source.map(projectedBusPoint);`
- [x] `return` — linha 3100, coluna 55 em `buildBusRoadPath()` — `const source=route?.points||[];if(source.length<2)return source.map(projectedBusPoint);`
- [x] `for` — linha 3102, coluna 5 em `buildBusRoadPath()` — `for(let i=0;i<source.length;i++){`
- [x] `return` — linha 3104, coluna 114 em `buildBusRoadPath()` — `let leg=compactBusPath(buildRoutePoints(from,to).map(point=>{const projection=nearestRoadProjection(point);return projection?.point?{x:projection.point.x,z:projection.point.z}:{x:point.x,z:point.z};}));`
- [x] `if` — linha 3105, coluna 7 em `buildBusRoadPath()` — `if(leg.length<2)leg=[from,to];`
- [x] `for` — linha 3106, coluna 7 em `buildBusRoadPath()` — `for(let j=1;j<leg.length;j++){`
- [x] `if` — linha 3107, coluna 38 em `buildBusRoadPath()` — `const final=j===leg.length-1;if(lastLeg&&final)continue;`
- [x] `if` — linha 3111, coluna 96 em `buildBusRoadPath()` — `const lane=offsetBusPath(compactBusPath(center),Number(route.laneOffset||BUS_LANE_OFFSET));if(lane.length>1&&Math.hypot(lane[0].x-lane[lane.length-1].x,lane[0].z-lane[lane.length-1].z)<.12)lane.pop();return lane;`
- [x] `return` — linha 3111, coluna 205 em `buildBusRoadPath()` — `const lane=offsetBusPath(compactBusPath(center),Number(route.laneOffset||BUS_LANE_OFFSET));if(lane.length>1&&Math.hypot(lane[0].x-lane[lane.length-1].x,lane[0].z-lane[lane.length-1].z)<.12)lane.pop();return lane;`
- [x] `return` — linha 3113, coluna 32 em `busRoutePoints()` — `function busRoutePoints(bus){return bus?.runtimePoints?.length?bus.runtimePoints:(bus?.route?.points||[]);}`
- [x] `for` — linha 3114, coluna 62 em `busNextStop()` — `function busNextStop(bus){const points=busRoutePoints(bus);for(let step=0;step<points.length;step++){const point=points[(bus.pointIndex+step)%points.length];if(point?.stopId)return point;}return null;}`
- [x] `if` — linha 3114, coluna 160 em `busNextStop()` — `function busNextStop(bus){const points=busRoutePoints(bus);for(let step=0;step<points.length;step++){const point=points[(bus.pointIndex+step)%points.length];if(point?.stopId)return point;}return null;}`
- [x] `return` — linha 3114, coluna 177 em `busNextStop()` — `function busNextStop(bus){const points=busRoutePoints(bus);for(let step=0;step<points.length;step++){const point=points[(bus.pointIndex+step)%points.length];if(point?.stopId)return point;}return null;}`
- [x] `return` — linha 3114, coluna 191 em `busNextStop()` — `function busNextStop(bus){const points=busRoutePoints(bus);for(let step=0;step<points.length;step++){const point=points[(bus.pointIndex+step)%points.length];if(point?.stopId)return point;}return null;}`
- [x] `if` — linha 3116, coluna 5 em `nearestForwardBusSegment()` — `if(!points?.length)return null;let best=null;const start=(bus.pointIndex-1+points.length)%points.length,limit=Math.min(points.length,18);`
- [x] `return` — linha 3116, coluna 24 em `nearestForwardBusSegment()` — `if(!points?.length)return null;let best=null;const start=(bus.pointIndex-1+points.length)%points.length,limit=Math.min(points.length,18);`
- [x] `for` — linha 3117, coluna 5 em `nearestForwardBusSegment()` — `for(let step=0;step<limit;step++){const index=(start+step)%points.length,a=points[index],b=points[(index+1)%points.length],point=projectPointToSegment(bus.group.position,a,b),distance=Math.hypot(bus.group.position.x-point.x,bus.group.position.z-point.z),score=distance+step*.045;if(!best||score<best.score)best={index,nextIndex:(index+1)%points.length,point,distance,score};}`
- [x] `if` — linha 3117, coluna 284 em `nearestForwardBusSegment()` — `for(let step=0;step<limit;step++){const index=(start+step)%points.length,a=points[index],b=points[(index+1)%points.length],point=projectPointToSegment(bus.group.position,a,b),distance=Math.hypot(bus.group.position.x-point.x,bus.group.position.z-point.z),score=distance+step*.045;if(!best||score<best.score)best={index,nextIndex:(index+1)%points.length,point,distance,score};}`
- [x] `return` — linha 3118, coluna 5 em `nearestForwardBusSegment()` — `return best;`
- [x] `if` — linha 3121, coluna 153 em `recoverBusRoute()` — `const points=busRoutePoints(bus);let projection=nearestForwardBusSegment(bus,points);const global=projectPointToPolyline(bus.group.position,points);if(global&&(!projection||global.distance+1.2<projection.distance))projection=global;if(!projection)return false;`
- [x] `if` — linha 3121, coluna 237 em `recoverBusRoute()` — `const points=busRoutePoints(bus);let projection=nearestForwardBusSegment(bus,points);const global=projectPointToPolyline(bus.group.position,points);if(global&&(!projection||global.distance+1.2<projection.distance))projection=global;if(!projection)return false;`
- [x] `return` — linha 3121, coluna 252 em `recoverBusRoute()` — `const points=busRoutePoints(bus);let projection=nearestForwardBusSegment(bus,points);const global=projectPointToPolyline(bus.group.position,points);if(global&&(!projection||global.distance+1.2<projection.distance))projection=global;if(!projection)return false;`
- [x] `return` — linha 3122, coluna 400 em `recoverBusRoute()` — `bus.group.position.x=projection.point.x;bus.group.position.z=projection.point.z;bus.pointIndex=projection.nextIndex;bus.currentSpeed=0;bus.targetSpeed=0;bus.trafficHoldUntil=performance.now()+650;bus.lastProgressAt=performance.now();bus.lastProgressX=bus.group.position.x;bus.lastProgressZ=bus.group.position.z;bus.lastRecoveryAt=bus.lastProgressAt;setBusState(bus,BUS_STATES.RECOVERING,reason);return true;`
- [x] `if` — linha 3125, coluna 5 em `setBusState()` — `if(!bus||bus.state===next)return false;bus.stateHistory=bus.stateHistory||[];bus.stateHistory.push({from:bus.state||'',to:next,at:Date.now(),reason});bus.stateHistory=bus.stateHistory.slice(-24);bus.state=next;bus.stateSince=performance.now();bus.lastStateReason=reason;return true;`
- [x] `return` — linha 3125, coluna 31 em `setBusState()` — `if(!bus||bus.state===next)return false;bus.stateHistory=bus.stateHistory||[];bus.stateHistory.push({from:bus.state||'',to:next,at:Date.now(),reason});bus.stateHistory=bus.stateHistory.slice(-24);bus.state=next;bus.stateSince=performance.now();bus.lastStateReason=reason;return true;`
- [x] `return` — linha 3125, coluna 275 em `setBusState()` — `if(!bus||bus.state===next)return false;bus.stateHistory=bus.stateHistory||[];bus.stateHistory.push({from:bus.state||'',to:next,at:Date.now(),reason});bus.stateHistory=bus.stateHistory.slice(-24);bus.state=next;bus.stateSince=performance.now();bus.lastStateReason=reason;return true;`
- [x] `return` — linha 3128, coluna 174 em `setBusWaiting()` — `const value={stopId:String(stopId||''),routeId:String(routeId||''),startedAt:Date.now()};world.waitingForBus=value;state.transport.busWaiting={...value};saveState(true);return value;`
- [x] `if` — linha 3130, coluna 96 em `clearBusWaiting()` — `function clearBusWaiting(save=true){world.waitingForBus=null;state.transport.busWaiting=null;if(save)saveState(true);}`
- [x] `if` — linha 3131, coluna 74 em `restoreBusWaiting()` — `function restoreBusWaiting(){const waiting=state.transport?.busWaiting;if(!waiting?.stopId)return false;const stop=world.busStops.find(s=>s.id===waiting.stopId);if(!stop){state.transport.busWaiting=null;return false;}world.waitingForBus={stopId:waiting.stopId,routeId:waiting.routeId||'',startedAt:Number(waiting.startedAt||Date.now())};return true;}`
- [x] `return` — linha 3131, coluna 94 em `restoreBusWaiting()` — `function restoreBusWaiting(){const waiting=state.transport?.busWaiting;if(!waiting?.stopId)return false;const stop=world.busStops.find(s=>s.id===waiting.stopId);if(!stop){state.transport.busWaiting=null;return false;}world.waitingForBus={stopId:waiting.stopId,routeId:waiting.routeId||'',startedAt:Number(waiting.startedAt||Date.now())};return true;}`
- [x] `if` — linha 3131, coluna 164 em `restoreBusWaiting()` — `function restoreBusWaiting(){const waiting=state.transport?.busWaiting;if(!waiting?.stopId)return false;const stop=world.busStops.find(s=>s.id===waiting.stopId);if(!stop){state.transport.busWaiting=null;return false;}world.waitingForBus={stopId:waiting.stopId,routeId:waiting.routeId||'',startedAt:Number(waiting.startedAt||Date.now())};return true;}`
- [x] `return` — linha 3131, coluna 206 em `restoreBusWaiting()` — `function restoreBusWaiting(){const waiting=state.transport?.busWaiting;if(!waiting?.stopId)return false;const stop=world.busStops.find(s=>s.id===waiting.stopId);if(!stop){state.transport.busWaiting=null;return false;}world.waitingForBus={stopId:waiting.stopId,routeId:waiting.routeId||'',startedAt:Number(waiting.startedAt||Date.now())};return true;}`
- [x] `return` — linha 3131, coluna 340 em `restoreBusWaiting()` — `function restoreBusWaiting(){const waiting=state.transport?.busWaiting;if(!waiting?.stopId)return false;const stop=world.busStops.find(s=>s.id===waiting.stopId);if(!stop){state.transport.busWaiting=null;return false;}world.waitingForBus={stopId:waiting.stopId,routeId:waiting.routeId||'',startedAt:Number(waiting.startedAt||Date.now())};return true;}`
- [x] `return` — linha 3132, coluna 80 em `busStopRecord()` — `function busStopRecord(stopLike){const id=stopLike?.stopId||stopLike?.id||'';return world.busStops.find(stop=>stop.id===id)||null;}`
- [x] `if` — linha 3135, coluna 5 em `safeBusExitPoint()` — `if(stop&&!positionBlockedForPlayer(stop.x,stop.z,.45,{ignoreActor:bus,ignoreTraffic:false}))return{x:stop.x,z:stop.z,y:groundHeightAt(stop.x,stop.z)};`
- [x] `return` — linha 3135, coluna 97 em `safeBusExitPoint()` — `if(stop&&!positionBlockedForPlayer(stop.x,stop.z,.45,{ignoreActor:bus,ignoreTraffic:false}))return{x:stop.x,z:stop.z,y:groundHeightAt(stop.x,stop.z)};`
- [x] `return` — linha 3136, coluna 5 em `safeBusExitPoint()` — `return safePointNear(bus.group.position.x,bus.group.position.z,{heading,radius:.45,ignoreActor:bus,angles:[Math.PI/2,-Math.PI/2,Math.PI],distances:[3.4,4.0,4.8]});`
- [x] `for` — linha 3142, coluna 5 em `createMetroEntrance()` — `for(let i=0;i<5;i++)premiumBox(2.8,.16,.58,0x718291,0,.35-i*.02,.6+i*.5,g);`
- [x] `for` — linha 3145, coluna 53 em `createMetroEntrance()` — `premiumBox(3.45,2.5,.12,0x13283d,0,1.45,1.82,g);for(const x of [-1.18,0,1.18])premiumBox(.08,2.35,.14,0x64d8ff,x,1.45,1.75,g);`
- [x] `return` — linha 3146, coluna 250 em `createMetroEntrance()` — `station.group=g;world.metroStations.push(station);registerInteractable({id:\`metro-entry-${station.id}\`,type:'metro',icon:'Ⓜ️',label:\`Entrar: ${station.name}\`,x:station.x,z:station.z,radius:3.5,priority:205,action:()=>openMetroStation(station)});return station;`
- [x] `if` — linha 3149, coluna 5 em `ensureMetroOverlay()` — `if(metroOverlay)return metroOverlay;metroOverlay=document.createElement('div');metroOverlay.id='metroTravelOverlay';metroOverlay.className='metro-travel-overlay';metroOverlay.hidden=true;metroOverlay.innerHTML='<div class="metro-tunnel"><i></i><i></i><i></i></div><div class="metro-train"><b>M</b><span></span><span></span><span></span></div><strong data-metro-trip>Próxima estação</strong>';document.body.appendChild(metroOverlay);return metroOverlay;`
- [x] `return` — linha 3149, coluna 21 em `ensureMetroOverlay()` — `if(metroOverlay)return metroOverlay;metroOverlay=document.createElement('div');metroOverlay.id='metroTravelOverlay';metroOverlay.className='metro-travel-overlay';metroOverlay.hidden=true;metroOverlay.innerHTML='<div class="metro-tunnel"><i></i><i></i><i></i></div><div class="metro-train"><b>M</b><span></span><span></span><span></span></div><strong data-metro-trip>Próxima estação</strong>';document.body.appendChild(metroOverlay);return metroOverlay;`
- [x] `return` — linha 3149, coluna 437 em `ensureMetroOverlay()` — `if(metroOverlay)return metroOverlay;metroOverlay=document.createElement('div');metroOverlay.id='metroTravelOverlay';metroOverlay.className='metro-travel-overlay';metroOverlay.hidden=true;metroOverlay.innerHTML='<div class="metro-tunnel"><i></i><i></i><i></i></div><div class="metro-train"><b>M</b><span></span><span></span><span></span></div><strong data-metro-trip>Próxima estação</strong>';document.body.appendChild(metroOverlay);return metroOverlay;`
- [x] `if` — linha 3152, coluna 5 em `openMetroStation()` — `if(player.vehicle||player.boating||player.transit.mode){toast('Desembarque antes de entrar no metrô.','warn');return;}`
- [x] `return` — linha 3152, coluna 115 em `openMetroStation()` — `if(player.vehicle||player.boating||player.transit.mode){toast('Desembarque antes de entrar no metrô.','warn');return;}`
- [x] `if` — linha 3154, coluna 346 em `openMetroStation()` — `openModal(station.name,\`<div class="transit-heading"><b>Ⓜ️ ${station.line}</b><span>Escolha qualquer ponto conhecido do mapa.</span></div><div class="metro-destination-list">${groups}</div>\`,root=>{$$('[data-metro-destination]',root).forEach(btn=>btn.onclick=()=>{const destination=MAP_LOCATIONS.find(x=>x.id===btn.dataset.metroDestination);if(destination)rideMetroTo(station,destination);});});`
- [x] `if` — linha 3157, coluna 5 em `rideMetroTo()` — `if(!destination||player.transit.mode||!canEnterMobility(PLAYER_MODES.METRO_PASSENGER))return false;`
- [x] `return` — linha 3157, coluna 91 em `rideMetroTo()` — `if(!destination||player.transit.mode||!canEnterMobility(PLAYER_MODES.METRO_PASSENGER))return false;`
- [x] `if` — linha 3159, coluna 5 em `rideMetroTo()` — `if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;if(contactShadow)contactShadow.visible=false;`
- [x] `if` — linha 3159, coluna 46 em `rideMetroTo()` — `if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;if(contactShadow)contactShadow.visible=false;`
- [x] `if` — linha 3159, coluna 87 em `rideMetroTo()` — `if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;if(contactShadow)contactShadow.visible=false;`
- [x] `if` — linha 3162, coluna 7 em `rideMetroTo()` — `if(player.transit.mode!=='metro'||player.transit.metroUntil!==token)return;`
- [x] `return` — linha 3162, coluna 75 em `rideMetroTo()` — `if(player.transit.mode!=='metro'||player.transit.metroUntil!==token)return;`
- [x] `if` — linha 3165, coluna 121 em `rideMetroTo()` — `state.transport.metroTrips=(state.transport.metroTrips||0)+1;state.stats.metroTrips=(state.stats.metroTrips||0)+1;if(!state.transport.metroDestinations.includes(destination.id))state.transport.metroDestinations.push(destination.id);state.transport.metroDestinations=state.transport.metroDestinations.slice(-60);trackDaily('metro',1);advanceAdventure('metro',destination.id);setFlag('usedMetro');state.waypoint=null;world.routePath=[];`
- [x] `if` — linha 3166, coluna 62 em `rideMetroTo()` — `player.transit.mode='';player.transit.destinationId='';if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;playerGroup?.position?.set(player.x,player.y,player.z);rememberSafePlayerPosition(true);auditPlayerMode('exit-metro');updateNavigation(0,true);updateContext(true);saveState(true);`
- [x] `if` — linha 3166, coluna 102 em `rideMetroTo()` — `player.transit.mode='';player.transit.destinationId='';if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;playerGroup?.position?.set(player.x,player.y,player.z);rememberSafePlayerPosition(true);auditPlayerMode('exit-metro');updateNavigation(0,true);updateContext(true);saveState(true);`
- [x] `if` — linha 3166, coluna 142 em `rideMetroTo()` — `player.transit.mode='';player.transit.destinationId='';if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;playerGroup?.position?.set(player.x,player.y,player.z);rememberSafePlayerPosition(true);auditPlayerMode('exit-metro');updateNavigation(0,true);updateContext(true);saveState(true);`
- [x] `return` — linha 3168, coluna 13 em `rideMetroTo()` — `},1900);return true;`
- [x] `if` — linha 3171, coluna 58 em `ensureBusStop()` — `let stop=world.busStops.find(s=>s.id===point.stopId);if(stop){if(!stop.routes.includes(route.id))stop.routes.push(route.id);return stop;}`
- [x] `if` — linha 3171, coluna 67 em `ensureBusStop()` — `let stop=world.busStops.find(s=>s.id===point.stopId);if(stop){if(!stop.routes.includes(route.id))stop.routes.push(route.id);return stop;}`
- [x] `return` — linha 3171, coluna 129 em `ensureBusStop()` — `let stop=world.busStops.find(s=>s.id===point.stopId);if(stop){if(!stop.routes.includes(route.id))stop.routes.push(route.id);return stop;}`
- [x] `return` — linha 3172, coluna 834 em `ensureBusStop()` — `stop={id:point.stopId,name:point.stopName,x:point.x,z:point.z,roadX:point.x,roadZ:point.z,routes:[route.id]};world.busStops.push(stop);const g=new THREE.Group();const sideX=Math.abs(point.x)<9?4.8:point.x<0?4.8:-4.8;g.position.set(point.x+sideX,0,point.z);worldGroup.add(g);premiumBox(.18,2.7,.18,0x23364b,0,1.35,0,g);premiumBox(1.5,.92,.16,route.color,0,2.35,0,g);const sign=new THREE.Mesh(new THREE.PlaneGeometry(.72,.72),new THREE.MeshStandardMaterial({map:iconTexture('🚌','#ffffff','#17324d'),transparent:true,side:THREE.DoubleSide}));sign.position.set(0,2.38,.1);g.add(sign);premiumBox(2.8,.18,.82,0xd3dce3,0,.1,0,g);stop.sign=g;stop.x=point.x+sideX;registerInteractable({id:\`bus-stop-${stop.id}\`,type:'bus-stop',icon:'🚌',label:\`Parada: ${stop.name}\`,x:stop.x,z:stop.z,radius:3.3,priority:190,action:()=>openBusStop(stop)});return stop;`
- [x] `if` — linha 3175, coluna 5 em `busSpawnIndex()` — `if(!runtimePoints.length)return 0;let best=preferredOffset,bestScore=-Infinity;for(let i=0;i<runtimePoints.length;i++){const point=runtimePoints[i],roadOk=pointOnRoad(point.x,point.z,.03),distanceToTraffic=trafficActorList().reduce((min,actor)=>Math.min(min,Math.hypot(actor.group.position.x-point.x,actor.group.position.z-point.z)-(actor.radius||1.5)),999),distanceToBus=(world.buses||[]).reduce((min,bus)=>Math.min(min,Math.hypot(bus.group.position.x-point.x,bus.group.position.z-point.z)-5.5),999),cycle=Math.min(Math.abs(i-preferredOffset),runtimePoints.length-Math.abs(i-preferredOffset)),score=(roadOk?0:-500)+Math.min(distanceToTraffic,distanceToBus)-cycle*.045+(point.stopId?-1.2:0);if(score>bestScore){bestScore=score;best=i;}}return best;`
- [x] `return` — linha 3175, coluna 30 em `busSpawnIndex()` — `if(!runtimePoints.length)return 0;let best=preferredOffset,bestScore=-Infinity;for(let i=0;i<runtimePoints.length;i++){const point=runtimePoints[i],roadOk=pointOnRoad(point.x,point.z,.03),distanceToTraffic=trafficActorList().reduce((min,actor)=>Math.min(min,Math.hypot(actor.group.position.x-point.x,actor.group.position.z-point.z)-(actor.radius||1.5)),999),distanceToBus=(world.buses||[]).reduce((min,bus)=>Math.min(min,Math.hypot(bus.group.position.x-point.x,bus.group.position.z-point.z)-5.5),999),cycle=Math.min(Math.abs(i-preferredOffset),runtimePoints.length-Math.abs(i-preferredOffset)),score=(roadOk?0:-500)+Math.min(distanceToTraffic,distanceToBus)-cycle*.045+(point.stopId?-1.2:0);if(score>bestScore){bestScore=score;best=i;}}return best;`
- [x] `for` — linha 3175, coluna 84 em `busSpawnIndex()` — `if(!runtimePoints.length)return 0;let best=preferredOffset,bestScore=-Infinity;for(let i=0;i<runtimePoints.length;i++){const point=runtimePoints[i],roadOk=pointOnRoad(point.x,point.z,.03),distanceToTraffic=trafficActorList().reduce((min,actor)=>Math.min(min,Math.hypot(actor.group.position.x-point.x,actor.group.position.z-point.z)-(actor.radius||1.5)),999),distanceToBus=(world.buses||[]).reduce((min,bus)=>Math.min(min,Math.hypot(bus.group.position.x-point.x,bus.group.position.z-point.z)-5.5),999),cycle=Math.min(Math.abs(i-preferredOffset),runtimePoints.length-Math.abs(i-preferredOffset)),score=(roadOk?0:-500)+Math.min(distanceToTraffic,distanceToBus)-cycle*.045+(point.stopId?-1.2:0);if(score>bestScore){bestScore=score;best=i;}}return best;`
- [x] `if` — linha 3175, coluna 696 em `busSpawnIndex()` — `if(!runtimePoints.length)return 0;let best=preferredOffset,bestScore=-Infinity;for(let i=0;i<runtimePoints.length;i++){const point=runtimePoints[i],roadOk=pointOnRoad(point.x,point.z,.03),distanceToTraffic=trafficActorList().reduce((min,actor)=>Math.min(min,Math.hypot(actor.group.position.x-point.x,actor.group.position.z-point.z)-(actor.radius||1.5)),999),distanceToBus=(world.buses||[]).reduce((min,bus)=>Math.min(min,Math.hypot(bus.group.position.x-point.x,bus.group.position.z-point.z)-5.5),999),cycle=Math.min(Math.abs(i-preferredOffset),runtimePoints.length-Math.abs(i-preferredOffset)),score=(roadOk?0:-500)+Math.min(distanceToTraffic,distanceToBus)-cycle*.045+(point.stopId?-1.2:0);if(score>bestScore){bestScore=score;best=i;}}return best;`
- [x] `return` — linha 3175, coluna 741 em `busSpawnIndex()` — `if(!runtimePoints.length)return 0;let best=preferredOffset,bestScore=-Infinity;for(let i=0;i<runtimePoints.length;i++){const point=runtimePoints[i],roadOk=pointOnRoad(point.x,point.z,.03),distanceToTraffic=trafficActorList().reduce((min,actor)=>Math.min(min,Math.hypot(actor.group.position.x-point.x,actor.group.position.z-point.z)-(actor.radius||1.5)),999),distanceToBus=(world.buses||[]).reduce((min,bus)=>Math.min(min,Math.hypot(bus.group.position.x-point.x,bus.group.position.z-point.z)-5.5),999),cycle=Math.min(Math.abs(i-preferredOffset),runtimePoints.length-Math.abs(i-preferredOffset)),score=(roadOk?0:-500)+Math.min(distanceToTraffic,distanceToBus)-cycle*.045+(point.stopId?-1.2:0);if(score>bestScore){bestScore=score;best=i;}}return best;`
- [x] `for` — linha 3181, coluna 5 em `createBusModel()` — `for(const side of [-1,1]){`
- [x] `for` — linha 3183, coluna 7 em `createBusModel()` — `for(const z of [-2.72,-1.38,0,1.38,2.72])premiumBox(.22,2.0,.18,light,side*1.51,2.03,z,g);`
- [x] `for` — linha 3184, coluna 7 em `createBusModel()` — `for(const z of [-2.08,-.68,.68,2.08])premiumBox(.035,1.25,1.08,dark,side*1.595,2.1,z,g);`
- [x] `if` — linha 3190, coluna 318 em `createBusModel()` — `const routeBoard=new THREE.Mesh(new THREE.PlaneGeometry(1.95,.46),new THREE.MeshStandardMaterial({map:signTexture(\`${route.number} ${route.name}\`,route.schoolBus?'#f0b62d':'#12273b',route.schoolBus?'#1b2835':'#ffffff'),roughness:.5,side:THREE.DoubleSide}));routeBoard.position.set(0,2.92,3.535);g.add(routeBoard);if(route.schoolBus){const schoolSign=new THREE.Mesh(new THREE.PlaneGeometry(1.15,.45),new THREE.MeshStandardMaterial({map:signTexture('ESCOLAR','#fff4c8','#24364a'),side:THREE.DoubleSide,roughness:.6}));schoolSign.position.set(0,2.35,-3.54);schoolSign.rotation.y=Math.PI;g.add(schoolSign);}`
- [x] `for` — linha 3191, coluna 5 em `createBusModel()` — `for(const z of [-2.25,-.85,.55,1.95])for(const x of [-.72,.72]){premiumBox(.72,.18,.62,seat,x,1.08,z,g);premiumBox(.72,.74,.16,seat,x,1.42,z-.26,g);premiumBox(.08,.48,.08,rail,x-.26,1.0,z,g);premiumBox(.08,.48,.08,rail,x+.26,1.0,z,g);}`
- [x] `for` — linha 3191, coluna 42 em `createBusModel()` — `for(const z of [-2.25,-.85,.55,1.95])for(const x of [-.72,.72]){premiumBox(.72,.18,.62,seat,x,1.08,z,g);premiumBox(.72,.74,.16,seat,x,1.42,z-.26,g);premiumBox(.08,.48,.08,rail,x-.26,1.0,z,g);premiumBox(.08,.48,.08,rail,x+.26,1.0,z,g);}`
- [x] `for` — linha 3192, coluna 5 em `createBusModel()` — `for(const x of [-1.15,1.15])premiumBox(.07,2.2,.07,rail,x,1.82,2.55,g);premiumBox(2.35,.07,.07,rail,0,2.72,2.55,g);`
- [x] `for` — linha 3194, coluna 21 em `createBusModel()` — `const wheels=[];for(const p of [[-1.48,.47,-2.3],[1.48,.47,-2.3],[-1.48,.47,2.25],[1.48,.47,2.25]]){const wheel=premiumCylinder(.48,.28,0x111720,p[0],p[1],p[2],g,14);wheel.rotation.z=Math.PI/2;wheels.push(wheel);}`
- [x] `for` — linha 3195, coluna 56 em `createBusModel()` — `const passengerColors=[0xffd84d,0xff72b6,0x54c7ff];for(let i=0;i<3;i++){const pg=new THREE.Group();pg.position.set(i%2?-.72:.72,1.2,-1.6+i*1.35);g.add(pg);premiumBox(.42,.52,.38,passengerColors[i],0,.25,0,pg);premiumBox(.36,.36,.36,0xffc997,0,.68,0,pg);}`
- [x] `for` — linha 3198, coluna 5 em `createBusModel()` — `for(const point of runtimePoints)if(point.stopId)ensureBusStop(route,point);return bus;`
- [x] `if` — linha 3198, coluna 38 em `createBusModel()` — `for(const point of runtimePoints)if(point.stopId)ensureBusStop(route,point);return bus;`
- [x] `return` — linha 3198, coluna 81 em `createBusModel()` — `for(const point of runtimePoints)if(point.stopId)ensureBusStop(route,point);return bus;`
- [x] `for` — linha 3200, coluna 117 em `createTransitWorld()` — `function createTransitWorld(){METRO_STATIONS.forEach(createMetroEntrance);BUS_ROUTES.forEach((route,routeOrder)=>{for(let copy=0;copy<Math.max(1,Number(route.copies||2));copy++)createBusModel(route,copy,routeOrder);});validateBusCoverage();restoreBusWaiting();}`
- [x] `return` — linha 3201, coluna 27 em `busAtStop()` — `function busAtStop(bus){return!!(bus&&bus.doorsOpen&&bus.lastStopId&&Math.abs(Number(bus.currentSpeed||0))<.18&&[BUS_STATES.DOORS_OPEN,BUS_STATES.OFFERING_BOARDING,BUS_STATES.BOARDING].includes(bus.state));}`
- [x] `if` — linha 3202, coluna 38 em `busEtaSeconds()` — `function busEtaSeconds(bus,stopId){if(!bus||!stopId)return Infinity;if(busAtStop(bus)&&bus.lastStopId===stopId)return 0;const points=busRoutePoints(bus);let distance=0,previous={x:bus.group.position.x,z:bus.group.position.z};for(let step=0;step<points.length;step++){const idx=(bus.pointIndex+step)%points.length,point=points[idx];distance+=Math.hypot(point.x-previous.x,point.z-previous.z);if(point.stopId===stopId)return Math.max(1,Math.round(distance/Math.max(1,bus.speed)+(step*.8)));previous=point;}return Infinity;}`
- [x] `return` — linha 3202, coluna 55 em `busEtaSeconds()` — `function busEtaSeconds(bus,stopId){if(!bus||!stopId)return Infinity;if(busAtStop(bus)&&bus.lastStopId===stopId)return 0;const points=busRoutePoints(bus);let distance=0,previous={x:bus.group.position.x,z:bus.group.position.z};for(let step=0;step<points.length;step++){const idx=(bus.pointIndex+step)%points.length,point=points[idx];distance+=Math.hypot(point.x-previous.x,point.z-previous.z);if(point.stopId===stopId)return Math.max(1,Math.round(distance/Math.max(1,bus.speed)+(step*.8)));previous=point;}return Infinity;}`
- [x] `if` — linha 3202, coluna 71 em `busEtaSeconds()` — `function busEtaSeconds(bus,stopId){if(!bus||!stopId)return Infinity;if(busAtStop(bus)&&bus.lastStopId===stopId)return 0;const points=busRoutePoints(bus);let distance=0,previous={x:bus.group.position.x,z:bus.group.position.z};for(let step=0;step<points.length;step++){const idx=(bus.pointIndex+step)%points.length,point=points[idx];distance+=Math.hypot(point.x-previous.x,point.z-previous.z);if(point.stopId===stopId)return Math.max(1,Math.round(distance/Math.max(1,bus.speed)+(step*.8)));previous=point;}return Infinity;}`
- [x] `return` — linha 3202, coluna 114 em `busEtaSeconds()` — `function busEtaSeconds(bus,stopId){if(!bus||!stopId)return Infinity;if(busAtStop(bus)&&bus.lastStopId===stopId)return 0;const points=busRoutePoints(bus);let distance=0,previous={x:bus.group.position.x,z:bus.group.position.z};for(let step=0;step<points.length;step++){const idx=(bus.pointIndex+step)%points.length,point=points[idx];distance+=Math.hypot(point.x-previous.x,point.z-previous.z);if(point.stopId===stopId)return Math.max(1,Math.round(distance/Math.max(1,bus.speed)+(step*.8)));previous=point;}return Infinity;}`
- [x] `for` — linha 3202, coluna 228 em `busEtaSeconds()` — `function busEtaSeconds(bus,stopId){if(!bus||!stopId)return Infinity;if(busAtStop(bus)&&bus.lastStopId===stopId)return 0;const points=busRoutePoints(bus);let distance=0,previous={x:bus.group.position.x,z:bus.group.position.z};for(let step=0;step<points.length;step++){const idx=(bus.pointIndex+step)%points.length,point=points[idx];distance+=Math.hypot(point.x-previous.x,point.z-previous.z);if(point.stopId===stopId)return Math.max(1,Math.round(distance/Math.max(1,bus.speed)+(step*.8)));previous=point;}return Infinity;}`
- [x] `if` — linha 3202, coluna 394 em `busEtaSeconds()` — `function busEtaSeconds(bus,stopId){if(!bus||!stopId)return Infinity;if(busAtStop(bus)&&bus.lastStopId===stopId)return 0;const points=busRoutePoints(bus);let distance=0,previous={x:bus.group.position.x,z:bus.group.position.z};for(let step=0;step<points.length;step++){const idx=(bus.pointIndex+step)%points.length,point=points[idx];distance+=Math.hypot(point.x-previous.x,point.z-previous.z);if(point.stopId===stopId)return Math.max(1,Math.round(distance/Math.max(1,bus.speed)+(step*.8)));previous=point;}return Infinity;}`
- [x] `return` — linha 3202, coluna 419 em `busEtaSeconds()` — `function busEtaSeconds(bus,stopId){if(!bus||!stopId)return Infinity;if(busAtStop(bus)&&bus.lastStopId===stopId)return 0;const points=busRoutePoints(bus);let distance=0,previous={x:bus.group.position.x,z:bus.group.position.z};for(let step=0;step<points.length;step++){const idx=(bus.pointIndex+step)%points.length,point=points[idx];distance+=Math.hypot(point.x-previous.x,point.z-previous.z);if(point.stopId===stopId)return Math.max(1,Math.round(distance/Math.max(1,bus.speed)+(step*.8)));previous=point;}return Infinity;}`
- [x] `return` — linha 3202, coluna 507 em `busEtaSeconds()` — `function busEtaSeconds(bus,stopId){if(!bus||!stopId)return Infinity;if(busAtStop(bus)&&bus.lastStopId===stopId)return 0;const points=busRoutePoints(bus);let distance=0,previous={x:bus.group.position.x,z:bus.group.position.z};for(let step=0;step<points.length;step++){const idx=(bus.pointIndex+step)%points.length,point=points[idx];distance+=Math.hypot(point.x-previous.x,point.z-previous.z);if(point.stopId===stopId)return Math.max(1,Math.round(distance/Math.max(1,bus.speed)+(step*.8)));previous=point;}return Infinity;}`
- [x] `for` — linha 3203, coluna 34 em `validateBusCoverage()` — `function validateBusCoverage(){for(const stop of world.busStops){const active=world.buses.some(bus=>stop.routes.includes(bus.route.id));stop.covered=active;if(!active)console.warn('Parada sem ônibus ativo:',stop.id);}}`
- [x] `if` — linha 3203, coluna 159 em `validateBusCoverage()` — `function validateBusCoverage(){for(const stop of world.busStops){const active=world.buses.some(bus=>stop.routes.includes(bus.route.id));stop.covered=active;if(!active)console.warn('Parada sem ônibus ativo:',stop.id);}}`
- [x] `for` — linha 3205, coluna 107 em `busDestinationsAfter()` — `const points=bus?.route?.points||[],start=Math.max(0,points.findIndex(p=>p.stopId===stopId)),names=[];for(let step=1;step<=points.length;step++){const p=points[(start+step)%points.length];if(p?.stopName&&!names.includes(p.stopName))names.push(p.stopName);}return names.slice(0,8);`
- [x] `if` — linha 3205, coluna 193 em `busDestinationsAfter()` — `const points=bus?.route?.points||[],start=Math.max(0,points.findIndex(p=>p.stopId===stopId)),names=[];for(let step=1;step<=points.length;step++){const p=points[(start+step)%points.length];if(p?.stopName&&!names.includes(p.stopName))names.push(p.stopName);}return names.slice(0,8);`
- [x] `return` — linha 3205, coluna 261 em `busDestinationsAfter()` — `const points=bus?.route?.points||[],start=Math.max(0,points.findIndex(p=>p.stopId===stopId)),names=[];for(let step=1;step<=points.length;step++){const p=points[(start+step)%points.length];if(p?.stopName&&!names.includes(p.stopName))names.push(p.stopName);}return names.slice(0,8);`
- [x] `if` — linha 3208, coluna 5 em `openBusArrivalOffer()` — `if(!bus||!stop||player.transit.mode||player.vehicle||player.boating||!busAtStop(bus))return false;`
- [x] `return` — linha 3208, coluna 90 em `openBusArrivalOffer()` — `if(!bus||!stop||player.transit.mode||player.vehicle||player.boating||!busAtStop(bus))return false;`
- [x] `if` — linha 3209, coluna 5 em `openBusArrivalOffer()` — `if(Math.hypot(player.x-stop.x,player.z-stop.z)>6.2)return false;`
- [x] `return` — linha 3209, coluna 56 em `openBusArrivalOffer()` — `if(Math.hypot(player.x-stop.x,player.z-stop.z)>6.2)return false;`
- [x] `return` — linha 3215, coluna 8 em `openBusArrivalOffer()` — `});return true;`
- [x] `if` — linha 3218, coluna 5 em `offerBusAtStop()` — `if(!bus||!stop)return;const waiting=world.waitingForBus,match=!waiting||(waiting.stopId===stop.id&&(!waiting.routeId||waiting.routeId===bus.route.id));if(!match)return;`
- [x] `return` — linha 3218, coluna 20 em `offerBusAtStop()` — `if(!bus||!stop)return;const waiting=world.waitingForBus,match=!waiting||(waiting.stopId===stop.id&&(!waiting.routeId||waiting.routeId===bus.route.id));if(!match)return;`
- [x] `if` — linha 3218, coluna 156 em `offerBusAtStop()` — `if(!bus||!stop)return;const waiting=world.waitingForBus,match=!waiting||(waiting.stopId===stop.id&&(!waiting.routeId||waiting.routeId===bus.route.id));if(!match)return;`
- [x] `return` — linha 3218, coluna 166 em `offerBusAtStop()` — `if(!bus||!stop)return;const waiting=world.waitingForBus,match=!waiting||(waiting.stopId===stop.id&&(!waiting.routeId||waiting.routeId===bus.route.id));if(!match)return;`
- [x] `if` — linha 3219, coluna 65 em `offerBusAtStop()` — `const near=Math.hypot(player.x-stop.x,player.z-stop.z)<=6.2;if(!near)return;`
- [x] `return` — linha 3219, coluna 74 em `offerBusAtStop()` — `const near=Math.hypot(player.x-stop.x,player.z-stop.z)<=6.2;if(!near)return;`
- [x] `if` — linha 3220, coluna 25 em `offerBusAtStop()` — `const tryOffer=()=>{if(!busAtStop(bus)||player.transit.mode)return;if(els.modal.hidden)openBusArrivalOffer(bus,stop);else if(performance.now()<bus.stopUntil-450)setTimeout(tryOffer,420);};`
- [x] `return` — linha 3220, coluna 65 em `offerBusAtStop()` — `const tryOffer=()=>{if(!busAtStop(bus)||player.transit.mode)return;if(els.modal.hidden)openBusArrivalOffer(bus,stop);else if(performance.now()<bus.stopUntil-450)setTimeout(tryOffer,420);};`
- [x] `if` — linha 3220, coluna 72 em `offerBusAtStop()` — `const tryOffer=()=>{if(!busAtStop(bus)||player.transit.mode)return;if(els.modal.hidden)openBusArrivalOffer(bus,stop);else if(performance.now()<bus.stopUntil-450)setTimeout(tryOffer,420);};`
- [x] `else` — linha 3220, coluna 122 em `offerBusAtStop()` — `const tryOffer=()=>{if(!busAtStop(bus)||player.transit.mode)return;if(els.modal.hidden)openBusArrivalOffer(bus,stop);else if(performance.now()<bus.stopUntil-450)setTimeout(tryOffer,420);};`
- [x] `if` — linha 3220, coluna 127 em `offerBusAtStop()` — `const tryOffer=()=>{if(!busAtStop(bus)||player.transit.mode)return;if(els.modal.hidden)openBusArrivalOffer(bus,stop);else if(performance.now()<bus.stopUntil-450)setTimeout(tryOffer,420);};`
- [x] `return` — linha 3228, coluna 356 em `openBusStop()` — `openModal(\`Parada ${stop.name}\`,\`<div class="transit-heading"><b>🚌 ${uniqueRoutes.map(r=>r.number).join(' • ')}</b><span>${ready?'Há um ônibus parado com portas abertas.':waiting?'Você já está esperando nesta parada.':'Escolha a linha que deseja esperar.'}</span></div><div class="bus-live-list">${buses.map(bus=>{const eta=busEtaSeconds(bus,stop.id);return\`<article class="${eta===0?'arrived':''}"><b>${bus.route.number} • ${bus.route.name}</b><span>${eta===0?'Na parada • portas abertas':\`Chega em aproximadamente ${eta}s\`} • ${bus.state||BUS_STATES.MOVING}</span></article>\`;}).join('')}</div><div class="bus-line-choice">${uniqueRoutes.map(route=>\`<button class="choice" data-wait-route="${route.id}"><b>${route.number} • ${route.name}</b><span>${[...new Set(route.points.filter(p=>p.stopName).map(p=>p.stopName))].join(' → ')}</span></button>\`).join('')}</div><div class="modal-actions">${ready?\`<button class="btn primary" data-board-ready="${ready.id}">Ver destinos e embarcar</button>\`:''}<bu`
- [x] `if` — linha 3237, coluna 5 em `ensureTransitPanel()` — `if(transitPanel)return transitPanel;transitPanel=document.createElement('div');transitPanel.id='transitActivityPanel';transitPanel.className='transit-activity-panel';transitPanel.hidden=true;transitPanel.innerHTML='<div><b data-bus-line>Ônibus</b><span data-bus-next>Próxima parada</span></div><button type="button" data-bus-request>🔔<span>Pedir parada</span></button>';document.body.appendChild(transitPanel);transitPanel.querySelector('[data-bus-request]').onclick=()=>{if(player.transit.mode!=='bus')return;player.transit.requestStop=true;updateTransitPanel();toast('Parada solicitada.','good',1200);};return transitPanel;`
- [x] `return` — linha 3237, coluna 21 em `ensureTransitPanel()` — `if(transitPanel)return transitPanel;transitPanel=document.createElement('div');transitPanel.id='transitActivityPanel';transitPanel.className='transit-activity-panel';transitPanel.hidden=true;transitPanel.innerHTML='<div><b data-bus-line>Ônibus</b><span data-bus-next>Próxima parada</span></div><button type="button" data-bus-request>🔔<span>Pedir parada</span></button>';document.body.appendChild(transitPanel);transitPanel.querySelector('[data-bus-request]').onclick=()=>{if(player.transit.mode!=='bus')return;player.transit.requestStop=true;updateTransitPanel();toast('Parada solicitada.','good',1200);};return transitPanel;`
- [x] `if` — linha 3237, coluna 477 em `ensureTransitPanel()` — `if(transitPanel)return transitPanel;transitPanel=document.createElement('div');transitPanel.id='transitActivityPanel';transitPanel.className='transit-activity-panel';transitPanel.hidden=true;transitPanel.innerHTML='<div><b data-bus-line>Ônibus</b><span data-bus-next>Próxima parada</span></div><button type="button" data-bus-request>🔔<span>Pedir parada</span></button>';document.body.appendChild(transitPanel);transitPanel.querySelector('[data-bus-request]').onclick=()=>{if(player.transit.mode!=='bus')return;player.transit.requestStop=true;updateTransitPanel();toast('Parada solicitada.','good',1200);};return transitPanel;`
- [x] `return` — linha 3237, coluna 508 em `ensureTransitPanel()` — `if(transitPanel)return transitPanel;transitPanel=document.createElement('div');transitPanel.id='transitActivityPanel';transitPanel.className='transit-activity-panel';transitPanel.hidden=true;transitPanel.innerHTML='<div><b data-bus-line>Ônibus</b><span data-bus-next>Próxima parada</span></div><button type="button" data-bus-request>🔔<span>Pedir parada</span></button>';document.body.appendChild(transitPanel);transitPanel.querySelector('[data-bus-request]').onclick=()=>{if(player.transit.mode!=='bus')return;player.transit.requestStop=true;updateTransitPanel();toast('Parada solicitada.','good',1200);};return transitPanel;`
- [x] `return` — linha 3237, coluna 610 em `ensureTransitPanel()` — `if(transitPanel)return transitPanel;transitPanel=document.createElement('div');transitPanel.id='transitActivityPanel';transitPanel.className='transit-activity-panel';transitPanel.hidden=true;transitPanel.innerHTML='<div><b data-bus-line>Ônibus</b><span data-bus-next>Próxima parada</span></div><button type="button" data-bus-request>🔔<span>Pedir parada</span></button>';document.body.appendChild(transitPanel);transitPanel.querySelector('[data-bus-request]').onclick=()=>{if(player.transit.mode!=='bus')return;player.transit.requestStop=true;updateTransitPanel();toast('Parada solicitada.','good',1200);};return transitPanel;`
- [x] `if` — linha 3240, coluna 221 em `updateTransitPanel()` — `const panel=ensureTransitPanel(),bus=world.buses.find(b=>b.id===player.transit.busId);document.body.classList.toggle('bus-passenger',player.transit.mode==='bus'&&!!bus);panel.hidden=player.transit.mode!=='bus'||!bus;if(!bus)return;const nextStop=busNextStop(bus);panel.querySelector('[data-bus-line]').textContent=\`${bus.route.number} • ${bus.route.name}\`;panel.querySelector('[data-bus-next]').textContent=\`Próxima: ${nextStop?.stopName||'Terminal'}\`;const button=panel.querySelector('[data-bus-request]');button.disabled=player.transit.requestStop;button.querySelector('span').textContent=player.transit.requestStop?'Solicitada':'Pedir parada';`
- [x] `return` — linha 3240, coluna 229 em `updateTransitPanel()` — `const panel=ensureTransitPanel(),bus=world.buses.find(b=>b.id===player.transit.busId);document.body.classList.toggle('bus-passenger',player.transit.mode==='bus'&&!!bus);panel.hidden=player.transit.mode!=='bus'||!bus;if(!bus)return;const nextStop=busNextStop(bus);panel.querySelector('[data-bus-line]').textContent=\`${bus.route.number} • ${bus.route.name}\`;panel.querySelector('[data-bus-next]').textContent=\`Próxima: ${nextStop?.stopName||'Terminal'}\`;const button=panel.querySelector('[data-bus-request]');button.disabled=player.transit.requestStop;button.querySelector('span').textContent=player.transit.requestStop?'Solicitada':'Pedir parada';`
- [x] `if` — linha 3243, coluna 5 em `enterBus()` — `if(!bus||player.transit.mode||player.vehicle||player.boating||!canEnterMobility(PLAYER_MODES.BUS_PASSENGER)){toast('Desembarque do transporte atual primeiro.','warn');return false;}`
- [x] `return` — linha 3243, coluna 172 em `enterBus()` — `if(!bus||player.transit.mode||player.vehicle||player.boating||!canEnterMobility(PLAYER_MODES.BUS_PASSENGER)){toast('Desembarque do transporte atual primeiro.','warn');return false;}`
- [x] `if` — linha 3244, coluna 5 em `enterBus()` — `if(!busAtStop(bus)){toast('Embarque somente com o ônibus parado e as portas abertas.','warn');return false;}`
- [x] `return` — linha 3244, coluna 99 em `enterBus()` — `if(!busAtStop(bus)){toast('Embarque somente com o ônibus parado e as portas abertas.','warn');return false;}`
- [x] `if` — linha 3245, coluna 5 em `enterBus()` — `if(Math.hypot(player.x-bus.group.position.x,player.z-bus.group.position.z)>6.2){toast('Chegue mais perto da porta do ônibus.','warn');return false;}`
- [x] `return` — linha 3245, coluna 139 em `enterBus()` — `if(Math.hypot(player.x-bus.group.position.x,player.z-bus.group.position.z)>6.2){toast('Chegue mais perto da porta do ônibus.','warn');return false;}`
- [x] `if` — linha 3248, coluna 5 em `enterBus()` — `if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;if(contactShadow)contactShadow.visible=false;if(toolVisual)toolVisual.visible=false;`
- [x] `if` — linha 3248, coluna 46 em `enterBus()` — `if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;if(contactShadow)contactShadow.visible=false;if(toolVisual)toolVisual.visible=false;`
- [x] `if` — linha 3248, coluna 87 em `enterBus()` — `if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;if(contactShadow)contactShadow.visible=false;if(toolVisual)toolVisual.visible=false;`
- [x] `if` — linha 3248, coluna 132 em `enterBus()` — `if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;if(contactShadow)contactShadow.visible=false;if(toolVisual)toolVisual.visible=false;`
- [x] `if` — linha 3251, coluna 21 em `enterBus()` — `setTimeout(()=>{if(busAtStop(bus)&&bus.state===BUS_STATES.BOARDING)setBusState(bus,BUS_STATES.DOORS_OPEN,'boarding-complete');},420);return true;`
- [x] `return` — linha 3251, coluna 138 em `enterBus()` — `setTimeout(()=>{if(busAtStop(bus)&&bus.state===BUS_STATES.BOARDING)setBusState(bus,BUS_STATES.DOORS_OPEN,'boarding-complete');},420);return true;`
- [x] `if` — linha 3254, coluna 5 em `exitBusAtStop()` — `if(player.transit.mode!=='bus'||!bus||!busAtStop(bus))return false;`
- [x] `return` — linha 3254, coluna 59 em `exitBusAtStop()` — `if(player.transit.mode!=='bus'||!bus||!busAtStop(bus))return false;`
- [x] `if` — linha 3257, coluna 5 em `exitBusAtStop()` — `if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;if(toolVisual)toolVisual.visible=true;`
- [x] `if` — linha 3257, coluna 45 em `exitBusAtStop()` — `if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;if(toolVisual)toolVisual.visible=true;`
- [x] `if` — linha 3257, coluna 85 em `exitBusAtStop()` — `if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;if(toolVisual)toolVisual.visible=true;`
- [x] `if` — linha 3257, coluna 129 em `exitBusAtStop()` — `if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;if(toolVisual)toolVisual.visible=true;`
- [x] `if` — linha 3258, coluna 73 em `exitBusAtStop()` — `const stopId=stop?.id||stopLike?.stopId||bus.lastStopId||'terminal';if(!state.transport.busStops.includes(stopId))state.transport.busStops.push(stopId);state.transport.busStops=state.transport.busStops.slice(-40);state.stats.busStops=(state.stats.busStops||0)+1;trackDaily('bus',1);advanceAdventure('bus',stopId);setFlag('rodeBus');`
- [x] `if` — linha 3260, coluna 21 em `exitBusAtStop()` — `setTimeout(()=>{if(bus.doorsOpen&&bus.state===BUS_STATES.BOARDING)setBusState(bus,BUS_STATES.DOORS_OPEN,'exit-complete');},350);return true;`
- [x] `return` — linha 3260, coluna 133 em `exitBusAtStop()` — `setTimeout(()=>{if(bus.doorsOpen&&bus.state===BUS_STATES.BOARDING)setBusState(bus,BUS_STATES.DOORS_OPEN,'exit-complete');},350);return true;`
- [x] `for` — linha 3264, coluna 5 em `updateTransitWorld()` — `for(const bus of world.buses){`
- [x] `if` — linha 3265, coluna 40 em `updateTransitWorld()` — `const points=busRoutePoints(bus);if(!points.length)continue;`
- [x] `if` — linha 3266, coluna 7 em `updateTransitWorld()` — `if(bus.state===BUS_STATES.SPAWNING)setBusState(bus,BUS_STATES.MOVING,'spawn-complete');`
- [x] `if` — linha 3267, coluna 7 em `updateTransitWorld()` — `if(now<Number(bus.trafficHoldUntil||0)){bus.currentSpeed=lerp(Number(bus.currentSpeed||0),0,Math.min(1,dt*10));continue;}`
- [x] `if` — linha 3268, coluna 7 em `updateTransitWorld()` — `if(bus.state===BUS_STATES.ACCIDENT){bus.currentSpeed=lerp(Number(bus.currentSpeed||0),0,Math.min(1,dt*8));continue;}`
- [x] `if` — linha 3269, coluna 7 em `updateTransitWorld()` — `if(bus.state===BUS_STATES.TURNING_AROUND){`
- [x] `if` — linha 3270, coluna 242 em `updateTransitWorld()` — `bus.currentSpeed=0;const targetHeading=Number(bus.turnTargetHeading||0),delta=((targetHeading-bus.group.rotation.y+Math.PI*3)%(Math.PI*2))-Math.PI;bus.group.rotation.y=lerpAngle(bus.group.rotation.y,targetHeading,Math.min(1,dt*4.8));if(Math.abs(delta)<.045||now>=Number(bus.turnUntil||0)){bus.group.rotation.y=targetHeading;bus.turnTargetHeading=0;bus.turnUntil=0;setBusState(bus,BUS_STATES.MOVING,'turn-complete');}`
- [x] `else` — linha 3271, coluna 8 em `updateTransitWorld()` — `}else if(bus.state===BUS_STATES.OPENING_DOORS){`
- [x] `if` — linha 3271, coluna 13 em `updateTransitWorld()` — `}else if(bus.state===BUS_STATES.OPENING_DOORS){`
- [x] `if` — linha 3272, coluna 28 em `updateTransitWorld()` — `bus.currentSpeed=0;if(now>=Number(bus.openAt||0)){bus.doorsOpen=true;bus.stopUntil=Math.max(Number(bus.stopUntil||0),now+Number(bus.route.dwell||3600));setBusState(bus,BUS_STATES.DOORS_OPEN,'doors-open');const stop=world.busStops.find(s=>s.id===bus.lastStopId);if(stop&&bus.offerStopId!==stop.id){bus.offerStopId=stop.id;offerBusAtStop(bus,stop);}}`
- [x] `if` — linha 3272, coluna 270 em `updateTransitWorld()` — `bus.currentSpeed=0;if(now>=Number(bus.openAt||0)){bus.doorsOpen=true;bus.stopUntil=Math.max(Number(bus.stopUntil||0),now+Number(bus.route.dwell||3600));setBusState(bus,BUS_STATES.DOORS_OPEN,'doors-open');const stop=world.busStops.find(s=>s.id===bus.lastStopId);if(stop&&bus.offerStopId!==stop.id){bus.offerStopId=stop.id;offerBusAtStop(bus,stop);}}`
- [x] `else` — linha 3273, coluna 8 em `updateTransitWorld()` — `}else if([BUS_STATES.DOORS_OPEN,BUS_STATES.OFFERING_BOARDING,BUS_STATES.BOARDING].includes(bus.state)){`
- [x] `if` — linha 3273, coluna 13 em `updateTransitWorld()` — `}else if([BUS_STATES.DOORS_OPEN,BUS_STATES.OFFERING_BOARDING,BUS_STATES.BOARDING].includes(bus.state)){`
- [x] `if` — linha 3275, coluna 9 em `updateTransitWorld()` — `if(player.transit.mode==='bus'&&player.transit.busId===bus.id&&player.transit.requestStop&&bus.lastStopId!==player.transit.originStopId)exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});`
- [x] `if` — linha 3276, coluna 9 em `updateTransitWorld()` — `if(now>=Number(bus.stopUntil||0)&&bus.state!==BUS_STATES.BOARDING){bus.doorsOpen=false;bus.closeUntil=now+420;setBusState(bus,BUS_STATES.CLOSING_DOORS,'dwell-complete');}`
- [x] `else` — linha 3277, coluna 8 em `updateTransitWorld()` — `}else if(bus.state===BUS_STATES.CLOSING_DOORS){`
- [x] `if` — linha 3277, coluna 13 em `updateTransitWorld()` — `}else if(bus.state===BUS_STATES.CLOSING_DOORS){`
- [x] `if` — linha 3278, coluna 28 em `updateTransitWorld()` — `bus.currentSpeed=0;if(now>=Number(bus.closeUntil||0)){bus.offerStopId='';setBusState(bus,BUS_STATES.LEAVING_STOP,'doors-closed');}`
- [x] `else` — linha 3279, coluna 8 em `updateTransitWorld()` — `}else{`
- [x] `if` — linha 3280, coluna 45 em `updateTransitWorld()` — `const target=points[bus.pointIndex];if(!target){bus.pointIndex=0;setBusState(bus,BUS_STATES.REROUTING,'missing-waypoint');continue;}`
- [x] `if` — linha 3282, coluna 9 em `updateTransitWorld()` — `if(d>.35&&turnDelta>2.45&&Math.abs(Number(bus.currentSpeed||0))<.3){bus.turnTargetHeading=heading;bus.turnUntil=now+1050;bus.currentSpeed=0;setBusState(bus,BUS_STATES.TURNING_AROUND,'terminal-u-turn');}`
- [x] `if` — linha 3283, coluna 9 em `updateTransitWorld()` — `if(bus.state===BUS_STATES.TURNING_AROUND)continue;`
- [x] `if` — linha 3286, coluna 9 em `updateTransitWorld()` — `if(trafficFactor<.075)setBusState(bus,BUS_STATES.WAITING_TRAFFIC,'vehicle-ahead');`
- [x] `else` — linha 3287, coluna 9 em `updateTransitWorld()` — `else if(approaching&&d<2.2)setBusState(bus,BUS_STATES.ALIGNING_TO_STOP,'near-stop');`
- [x] `if` — linha 3287, coluna 14 em `updateTransitWorld()` — `else if(approaching&&d<2.2)setBusState(bus,BUS_STATES.ALIGNING_TO_STOP,'near-stop');`
- [x] `else` — linha 3288, coluna 9 em `updateTransitWorld()` — `else if(approaching&&d<6)setBusState(bus,BUS_STATES.BRAKING,'approach-stop');`
- [x] `if` — linha 3288, coluna 14 em `updateTransitWorld()` — `else if(approaching&&d<6)setBusState(bus,BUS_STATES.BRAKING,'approach-stop');`
- [x] `else` — linha 3289, coluna 9 em `updateTransitWorld()` — `else if(approaching)setBusState(bus,BUS_STATES.APPROACHING_STOP,'approach-stop');`
- [x] `if` — linha 3289, coluna 14 em `updateTransitWorld()` — `else if(approaching)setBusState(bus,BUS_STATES.APPROACHING_STOP,'approach-stop');`
- [x] `else` — linha 3290, coluna 9 em `updateTransitWorld()` — `else if(bus.state!==BUS_STATES.LEAVING_STOP)setBusState(bus,BUS_STATES.MOVING,'route');`
- [x] `if` — linha 3290, coluna 14 em `updateTransitWorld()` — `else if(bus.state!==BUS_STATES.LEAVING_STOP)setBusState(bus,BUS_STATES.MOVING,'route');`
- [x] `if` — linha 3291, coluna 9 em `updateTransitWorld()` — `if(trafficFactor<.035){bus.currentSpeed=0;bus.trafficHoldUntil=Math.max(Number(bus.trafficHoldUntil||0),now+420);}else bus.currentSpeed=lerp(Number(bus.currentSpeed||0),bus.targetSpeed,Math.min(1,dt*(trafficFactor<.1?10:4.4)));`
- [x] `else` — linha 3291, coluna 123 em `updateTransitWorld()` — `if(trafficFactor<.035){bus.currentSpeed=0;bus.trafficHoldUntil=Math.max(Number(bus.trafficHoldUntil||0),now+420);}else bus.currentSpeed=lerp(Number(bus.currentSpeed||0),bus.targetSpeed,Math.min(1,dt*(trafficFactor<.1?10:4.4)));`
- [x] `if` — linha 3293, coluna 9 em `updateTransitWorld()` — `if(move>.0001){bus.group.position.x+=dx/d*move;bus.group.position.z+=dz/d*move;if(!pointOnRoad(bus.group.position.x,bus.group.position.z,.03)){bus.group.position.x=previous.x;bus.group.position.z=previous.z;recoverBusRoute(bus,'left-road');}bus.group.rotation.y=lerpAngle(bus.group.rotation.y,heading,Math.min(1,dt*(approaching?3.6:4.4)));for(const wheel of bus.wheels)wheel.rotation.x-=move*2.2;}`
- [x] `if` — linha 3293, coluna 88 em `updateTransitWorld()` — `if(move>.0001){bus.group.position.x+=dx/d*move;bus.group.position.z+=dz/d*move;if(!pointOnRoad(bus.group.position.x,bus.group.position.z,.03)){bus.group.position.x=previous.x;bus.group.position.z=previous.z;recoverBusRoute(bus,'left-road');}bus.group.rotation.y=lerpAngle(bus.group.rotation.y,heading,Math.min(1,dt*(approaching?3.6:4.4)));for(const wheel of bus.wheels)wheel.rotation.x-=move*2.2;}`
- [x] `for` — linha 3293, coluna 348 em `updateTransitWorld()` — `if(move>.0001){bus.group.position.x+=dx/d*move;bus.group.position.z+=dz/d*move;if(!pointOnRoad(bus.group.position.x,bus.group.position.z,.03)){bus.group.position.x=previous.x;bus.group.position.z=previous.z;recoverBusRoute(bus,'left-road');}bus.group.rotation.y=lerpAngle(bus.group.rotation.y,heading,Math.min(1,dt*(approaching?3.6:4.4)));for(const wheel of bus.wheels)wheel.rotation.x-=move*2.2;}`
- [x] `if` — linha 3295, coluna 9 em `updateTransitWorld()` — `if(movedFromProgress>.45){bus.lastProgressX=bus.group.position.x;bus.lastProgressZ=bus.group.position.z;bus.lastProgressAt=now;}`
- [x] `if` — linha 3296, coluna 9 em `updateTransitWorld()` — `if(d<=Math.max(.22,move+.1)){`
- [x] `if` — linha 3298, coluna 11 em `updateTransitWorld()` — `if(target.stopId){bus.lastStopId=target.stopId;bus.lastStopName=target.stopName||target.stopId;bus.doorsOpen=false;bus.openAt=now+260;bus.stopUntil=now+260+Number(bus.route.dwell||3600);setBusState(bus,BUS_STATES.OPENING_DOORS,'aligned-at-stop');}`
- [x] `else` — linha 3299, coluna 11 em `updateTransitWorld()` — `else setBusState(bus,BUS_STATES.MOVING,'waypoint-reached');`
- [x] `else` — linha 3300, coluna 10 em `updateTransitWorld()` — `}else if(now-Number(bus.lastProgressAt||now)>8500&&now-Number(bus.lastRecoveryAt||0)>12000&&trafficFactor>.2){`
- [x] `if` — linha 3300, coluna 15 em `updateTransitWorld()` — `}else if(now-Number(bus.lastProgressAt||now)>8500&&now-Number(bus.lastRecoveryAt||0)>12000&&trafficFactor>.2){`
- [x] `if` — linha 3301, coluna 62 em `updateTransitWorld()` — `recoverBusRoute(bus,'stuck-route');setTimeout(()=>{if(bus.state===BUS_STATES.RECOVERING)setBusState(bus,BUS_STATES.REROUTING,'recovery-cooldown');},450);`
- [x] `if` — linha 3304, coluna 7 em `updateTransitWorld()` — `if(player.transit.mode==='bus'&&player.transit.busId===bus.id){bus.group.updateMatrixWorld?.(true);const seat=bus.group.localToWorld(bus.seatOffset.clone());player.x=seat.x;player.z=seat.z;player.y=seat.y;player.facing=bus.group.rotation.y;player.vx=player.vz=0;}`

## `16-emergency-services.js`

- [x] `if` — linha 3312, coluna 5 em `decorateCityServices()` — `if(fire){for(const x of [-2.35,0,2.35]){premiumBox(1.9,2.35,.12,materials.cityGlass,fire.x+x,1.42,fire.z+3.56,fire.front);for(let y=.45;y<2.5;y+=.48)premiumBox(1.75,.06,.14,0xf4f5f2,fire.x+x,y,fire.z+3.62,fire.front);}premiumBox(7.2,.36,.24,materials.emergencyMetal,fire.x,3.02,fire.z+3.66,fire.front);const badge=new THREE.Mesh(new THREE.PlaneGeometry(1.05,1.05),new THREE.MeshStandardMaterial({map:iconTexture('🚒','#ffffff','#c83232'),transparent:true,side:THREE.DoubleSide}));badge.position.set(fire.x,3.85,fire.z+3.75);fire.front.add(badge);for(const z of [-64,-72]){premiumCylinder(.17,1.35,0xf2c23c,60.5,.68,z,worldGroup,10);premiumBox(.5,.18,.5,0xe8483f,60.5,1.45,z);}}`
- [x] `for` — linha 3312, coluna 14 em `decorateCityServices()` — `if(fire){for(const x of [-2.35,0,2.35]){premiumBox(1.9,2.35,.12,materials.cityGlass,fire.x+x,1.42,fire.z+3.56,fire.front);for(let y=.45;y<2.5;y+=.48)premiumBox(1.75,.06,.14,0xf4f5f2,fire.x+x,y,fire.z+3.62,fire.front);}premiumBox(7.2,.36,.24,materials.emergencyMetal,fire.x,3.02,fire.z+3.66,fire.front);const badge=new THREE.Mesh(new THREE.PlaneGeometry(1.05,1.05),new THREE.MeshStandardMaterial({map:iconTexture('🚒','#ffffff','#c83232'),transparent:true,side:THREE.DoubleSide}));badge.position.set(fire.x,3.85,fire.z+3.75);fire.front.add(badge);for(const z of [-64,-72]){premiumCylinder(.17,1.35,0xf2c23c,60.5,.68,z,worldGroup,10);premiumBox(.5,.18,.5,0xe8483f,60.5,1.45,z);}}`
- [x] `for` — linha 3312, coluna 127 em `decorateCityServices()` — `if(fire){for(const x of [-2.35,0,2.35]){premiumBox(1.9,2.35,.12,materials.cityGlass,fire.x+x,1.42,fire.z+3.56,fire.front);for(let y=.45;y<2.5;y+=.48)premiumBox(1.75,.06,.14,0xf4f5f2,fire.x+x,y,fire.z+3.62,fire.front);}premiumBox(7.2,.36,.24,materials.emergencyMetal,fire.x,3.02,fire.z+3.66,fire.front);const badge=new THREE.Mesh(new THREE.PlaneGeometry(1.05,1.05),new THREE.MeshStandardMaterial({map:iconTexture('🚒','#ffffff','#c83232'),transparent:true,side:THREE.DoubleSide}));badge.position.set(fire.x,3.85,fire.z+3.75);fire.front.add(badge);for(const z of [-64,-72]){premiumCylinder(.17,1.35,0xf2c23c,60.5,.68,z,worldGroup,10);premiumBox(.5,.18,.5,0xe8483f,60.5,1.45,z);}}`
- [x] `for` — linha 3312, coluna 550 em `decorateCityServices()` — `if(fire){for(const x of [-2.35,0,2.35]){premiumBox(1.9,2.35,.12,materials.cityGlass,fire.x+x,1.42,fire.z+3.56,fire.front);for(let y=.45;y<2.5;y+=.48)premiumBox(1.75,.06,.14,0xf4f5f2,fire.x+x,y,fire.z+3.62,fire.front);}premiumBox(7.2,.36,.24,materials.emergencyMetal,fire.x,3.02,fire.z+3.66,fire.front);const badge=new THREE.Mesh(new THREE.PlaneGeometry(1.05,1.05),new THREE.MeshStandardMaterial({map:iconTexture('🚒','#ffffff','#c83232'),transparent:true,side:THREE.DoubleSide}));badge.position.set(fire.x,3.85,fire.z+3.75);fire.front.add(badge);for(const z of [-64,-72]){premiumCylinder(.17,1.35,0xf2c23c,60.5,.68,z,worldGroup,10);premiumBox(.5,.18,.5,0xe8483f,60.5,1.45,z);}}`
- [x] `for` — linha 3313, coluna 5 em `decorateCityServices()` — `for(const station of [police,west].filter(Boolean)){premiumBox(6.4,.28,.2,0x245da8,station.x,2.95,station.z+3.65,station.front);const badge=new THREE.Mesh(new THREE.PlaneGeometry(.9,.9),new THREE.MeshStandardMaterial({map:iconTexture('★','#ffffff','#245da8'),transparent:true,side:THREE.DoubleSide}));badge.position.set(station.x,3.7,station.z+3.72);station.front.add(badge);premiumBox(2.6,.1,1.1,materials.cityGlass,station.x,2.38,station.z+3.86,station.front);}`
- [x] `for` — linha 3314, coluna 5 em `decorateCityServices()` — `for(const p of [[55,-45],[55,-75],[-55,12],[-55,32]]){premiumBox(.18,2.2,.18,0x2e3c4f,p[0],1.1,p[1]);premiumBox(.62,.62,.18,0x111827,p[0],2.08,p[1]);premiumBox(.16,.16,.08,0x46d66c,p[0],2.22,p[1]+.1);premiumBox(.16,.16,.08,0xf0c33c,p[0],2.05,p[1]+.1);premiumBox(.16,.16,.08,0xe74b4b,p[0],1.88,p[1]+.1);}`
- [x] `return` — linha 3331, coluna 94 em `serviceKindForJob()` — `function serviceKindForJob(jobOrId){const id=typeof jobOrId==='string'?jobOrId:jobOrId?.id;return SERVICE_JOB_KIND[id]||'';}`
- [x] `if` — linha 3332, coluna 41 em `serviceVehiclePools()` — `function serviceVehiclePools(kind=''){if(kind==='police')return world.policeCars||[];if(kind==='firefighter')return world.fireTrucks||[];if(kind==='paramedic')return world.ambulances||[];return[...(world.policeCars||[]),...(world.fireTrucks||[]),...(world.ambulances||[])];}`
- [x] `return` — linha 3332, coluna 60 em `serviceVehiclePools()` — `function serviceVehiclePools(kind=''){if(kind==='police')return world.policeCars||[];if(kind==='firefighter')return world.fireTrucks||[];if(kind==='paramedic')return world.ambulances||[];return[...(world.policeCars||[]),...(world.fireTrucks||[]),...(world.ambulances||[])];}`
- [x] `if` — linha 3332, coluna 88 em `serviceVehiclePools()` — `function serviceVehiclePools(kind=''){if(kind==='police')return world.policeCars||[];if(kind==='firefighter')return world.fireTrucks||[];if(kind==='paramedic')return world.ambulances||[];return[...(world.policeCars||[]),...(world.fireTrucks||[]),...(world.ambulances||[])];}`
- [x] `return` — linha 3332, coluna 112 em `serviceVehiclePools()` — `function serviceVehiclePools(kind=''){if(kind==='police')return world.policeCars||[];if(kind==='firefighter')return world.fireTrucks||[];if(kind==='paramedic')return world.ambulances||[];return[...(world.policeCars||[]),...(world.fireTrucks||[]),...(world.ambulances||[])];}`
- [x] `if` — linha 3332, coluna 140 em `serviceVehiclePools()` — `function serviceVehiclePools(kind=''){if(kind==='police')return world.policeCars||[];if(kind==='firefighter')return world.fireTrucks||[];if(kind==='paramedic')return world.ambulances||[];return[...(world.policeCars||[]),...(world.fireTrucks||[]),...(world.ambulances||[])];}`
- [x] `return` — linha 3332, coluna 162 em `serviceVehiclePools()` — `function serviceVehiclePools(kind=''){if(kind==='police')return world.policeCars||[];if(kind==='firefighter')return world.fireTrucks||[];if(kind==='paramedic')return world.ambulances||[];return[...(world.policeCars||[]),...(world.fireTrucks||[]),...(world.ambulances||[])];}`
- [x] `return` — linha 3332, coluna 190 em `serviceVehiclePools()` — `function serviceVehiclePools(kind=''){if(kind==='police')return world.policeCars||[];if(kind==='firefighter')return world.fireTrucks||[];if(kind==='paramedic')return world.ambulances||[];return[...(world.policeCars||[]),...(world.fireTrucks||[]),...(world.ambulances||[])];}`
- [x] `return` — linha 3333, coluna 38 em `serviceVehicleById()` — `function serviceVehicleById(id=''){return serviceVehiclePools().find(vehicle=>vehicle.id===id)||null;}`
- [x] `return` — linha 3334, coluna 39 em `currentServiceMissionJob()` — `function currentServiceMissionJob(){return state.career?.activeJob||(typeof activeCoopServiceJob==='function'?activeCoopServiceJob():null);}`
- [x] `if` — linha 3335, coluna 54 em `serviceVehicleAvailable()` — `function serviceVehicleAvailable(vehicle,job=null){if(!vehicle||vehicle.occupied||vehicle.incidentTargetId||vehicle.targetFireId||vehicle.group?.visible===false)return false;const token=job?.instanceId||currentServiceMissionJob()?.instanceId||'';return!vehicle.missionReservedFor||vehicle.missionReservedFor===token;}`
- [x] `return` — linha 3335, coluna 164 em `serviceVehicleAvailable()` — `function serviceVehicleAvailable(vehicle,job=null){if(!vehicle||vehicle.occupied||vehicle.incidentTargetId||vehicle.targetFireId||vehicle.group?.visible===false)return false;const token=job?.instanceId||currentServiceMissionJob()?.instanceId||'';return!vehicle.missionReservedFor||vehicle.missionReservedFor===token;}`
- [x] `return` — linha 3335, coluna 249 em `serviceVehicleAvailable()` — `function serviceVehicleAvailable(vehicle,job=null){if(!vehicle||vehicle.occupied||vehicle.incidentTargetId||vehicle.targetFireId||vehicle.group?.visible===false)return false;const token=job?.instanceId||currentServiceMissionJob()?.instanceId||'';return!vehicle.missionReservedFor||vehicle.missionReservedFor===token;}`
- [x] `if` — linha 3336, coluna 43 em `serviceVehicleAppearance()` — `function serviceVehicleAppearance(kind){if(kind==='police')return{chassis:0x26384e,primary:0xf4f7fb,primaryDark:0x183d70,secondary:0x215ea8,glass:0x13253a};if(kind==='firefighter')return{chassis:0x263746,primary:0xc93431,primaryDark:0x982622,secondary:0xf2d84d,glass:0x183448};return{chassis:0x263746,primary:0xf6f8fa,primaryDark:0xd83e42,secondary:0xe34848,glass:0x164055};}`
- [x] `return` — linha 3336, coluna 62 em `serviceVehicleAppearance()` — `function serviceVehicleAppearance(kind){if(kind==='police')return{chassis:0x26384e,primary:0xf4f7fb,primaryDark:0x183d70,secondary:0x215ea8,glass:0x13253a};if(kind==='firefighter')return{chassis:0x263746,primary:0xc93431,primaryDark:0x982622,secondary:0xf2d84d,glass:0x183448};return{chassis:0x263746,primary:0xf6f8fa,primaryDark:0xd83e42,secondary:0xe34848,glass:0x164055};}`
- [x] `if` — linha 3336, coluna 159 em `serviceVehicleAppearance()` — `function serviceVehicleAppearance(kind){if(kind==='police')return{chassis:0x26384e,primary:0xf4f7fb,primaryDark:0x183d70,secondary:0x215ea8,glass:0x13253a};if(kind==='firefighter')return{chassis:0x263746,primary:0xc93431,primaryDark:0x982622,secondary:0xf2d84d,glass:0x183448};return{chassis:0x263746,primary:0xf6f8fa,primaryDark:0xd83e42,secondary:0xe34848,glass:0x164055};}`
- [x] `return` — linha 3336, coluna 183 em `serviceVehicleAppearance()` — `function serviceVehicleAppearance(kind){if(kind==='police')return{chassis:0x26384e,primary:0xf4f7fb,primaryDark:0x183d70,secondary:0x215ea8,glass:0x13253a};if(kind==='firefighter')return{chassis:0x263746,primary:0xc93431,primaryDark:0x982622,secondary:0xf2d84d,glass:0x183448};return{chassis:0x263746,primary:0xf6f8fa,primaryDark:0xd83e42,secondary:0xe34848,glass:0x164055};}`
- [x] `return` — linha 3336, coluna 280 em `serviceVehicleAppearance()` — `function serviceVehicleAppearance(kind){if(kind==='police')return{chassis:0x26384e,primary:0xf4f7fb,primaryDark:0x183d70,secondary:0x215ea8,glass:0x13253a};if(kind==='firefighter')return{chassis:0x263746,primary:0xc93431,primaryDark:0x982622,secondary:0xf2d84d,glass:0x183448};return{chassis:0x263746,primary:0xf6f8fa,primaryDark:0xd83e42,secondary:0xe34848,glass:0x164055};}`
- [x] `for` — linha 3337, coluna 265 em `safeServiceDepot()` — `function safeServiceDepot(kind,index=0){const list=SERVICE_DEPOTS[kind]||SERVICE_DEPOTS.police,base=list[index%list.length]||list[0],projection=nearestRoadProjection(base),point=projection?.point||base;let candidate={x:point.x,z:point.z,heading:base.heading||0};for(let step=0;step<8;step++){world.trafficSnapshot=null;const conflict=trafficActorList().some(actor=>actor.group&&Math.hypot(actor.group.position.x-candidate.x,actor.group.position.z-candidate.z)<Number(actor.radius||1.5)+3.2);if(!conflict)break;candidate={...candidate,z:clamp(candidate.z+(step%2?1:-1)*(4+step),-108,108)};const snapped=nearestRoadProjection(candidate)?.point;if(snapped)candidate={...candidate,x:snapped.x,z:snapped.z};}return candidate;}`
- [x] `if` — linha 3337, coluna 494 em `safeServiceDepot()` — `function safeServiceDepot(kind,index=0){const list=SERVICE_DEPOTS[kind]||SERVICE_DEPOTS.police,base=list[index%list.length]||list[0],projection=nearestRoadProjection(base),point=projection?.point||base;let candidate={x:point.x,z:point.z,heading:base.heading||0};for(let step=0;step<8;step++){world.trafficSnapshot=null;const conflict=trafficActorList().some(actor=>actor.group&&Math.hypot(actor.group.position.x-candidate.x,actor.group.position.z-candidate.z)<Number(actor.radius||1.5)+3.2);if(!conflict)break;candidate={...candidate,z:clamp(candidate.z+(step%2?1:-1)*(4+step),-108,108)};const snapped=nearestRoadProjection(candidate)?.point;if(snapped)candidate={...candidate,x:snapped.x,z:snapped.z};}return candidate;}`
- [x] `if` — linha 3337, coluna 645 em `safeServiceDepot()` — `function safeServiceDepot(kind,index=0){const list=SERVICE_DEPOTS[kind]||SERVICE_DEPOTS.police,base=list[index%list.length]||list[0],projection=nearestRoadProjection(base),point=projection?.point||base;let candidate={x:point.x,z:point.z,heading:base.heading||0};for(let step=0;step<8;step++){world.trafficSnapshot=null;const conflict=trafficActorList().some(actor=>actor.group&&Math.hypot(actor.group.position.x-candidate.x,actor.group.position.z-candidate.z)<Number(actor.radius||1.5)+3.2);if(!conflict)break;candidate={...candidate,z:clamp(candidate.z+(step%2?1:-1)*(4+step),-108,108)};const snapped=nearestRoadProjection(candidate)?.point;if(snapped)candidate={...candidate,x:snapped.x,z:snapped.z};}return candidate;}`
- [x] `return` — linha 3337, coluna 706 em `safeServiceDepot()` — `function safeServiceDepot(kind,index=0){const list=SERVICE_DEPOTS[kind]||SERVICE_DEPOTS.police,base=list[index%list.length]||list[0],projection=nearestRoadProjection(base),point=projection?.point||base;let candidate={x:point.x,z:point.z,heading:base.heading||0};for(let step=0;step<8;step++){world.trafficSnapshot=null;const conflict=trafficActorList().some(actor=>actor.group&&Math.hypot(actor.group.position.x-candidate.x,actor.group.position.z-candidate.z)<Number(actor.radius||1.5)+3.2);if(!conflict)break;candidate={...candidate,z:clamp(candidate.z+(step%2?1:-1)*(4+step),-108,108)};const snapped=nearestRoadProjection(candidate)?.point;if(snapped)candidate={...candidate,x:snapped.x,z:snapped.z};}return candidate;}`
- [x] `return` — linha 3340, coluna 405 em `registerServiceVehicle()` — `registerInteractable({id:\`service-vehicle-${vehicle.id}\`,type:'serviceVehicle',icon:kind==='police'?'🚓':kind==='firefighter'?'🚒':'🚑',label:\`Entrar: ${vehicle.label}\`,radius:kind==='firefighter'?3.5:3.0,priority:238,getPos:()=>({x:vehicle.group.position.x,z:vehicle.group.position.z}),available:()=>serviceVehicleAvailable(vehicle,currentServiceMissionJob()),action:()=>enterServiceVehicle(vehicle)});return vehicle;`
- [x] `if` — linha 3343, coluna 112 em `enterServiceVehicle()` — `const job=currentServiceMissionJob(),required=serviceKindForJob(job),kind=String(vehicle?.serviceType||'');if(required&&required!==kind){toast(\`Esta missão exige ${required==='police'?'uma viatura':required==='firefighter'?'um caminhão dos bombeiros':'uma ambulância'}.\`,'warn',2400);return false;}if(job?.serviceVehicleId&&job.serviceVehicleId!==vehicle?.id){const assigned=serviceMissionVehicle(job);toast(\`Use ${assigned?.label||'o veículo indicado'} marcado no GPS para esta missão.\`,'warn',2500);return false;}if(!serviceVehicleAvailable(vehicle,job)){toast('Este veículo está atendendo outra ocorrência.','warn');return false;}return enterVehicle(vehicle);`
- [x] `return` — linha 3343, coluna 289 em `enterServiceVehicle()` — `const job=currentServiceMissionJob(),required=serviceKindForJob(job),kind=String(vehicle?.serviceType||'');if(required&&required!==kind){toast(\`Esta missão exige ${required==='police'?'uma viatura':required==='firefighter'?'um caminhão dos bombeiros':'uma ambulância'}.\`,'warn',2400);return false;}if(job?.serviceVehicleId&&job.serviceVehicleId!==vehicle?.id){const assigned=serviceMissionVehicle(job);toast(\`Use ${assigned?.label||'o veículo indicado'} marcado no GPS para esta missão.\`,'warn',2500);return false;}if(!serviceVehicleAvailable(vehicle,job)){toast('Este veículo está atendendo outra ocorrência.','warn');return false;}return enterVehicle(vehicle);`
- [x] `if` — linha 3343, coluna 303 em `enterServiceVehicle()` — `const job=currentServiceMissionJob(),required=serviceKindForJob(job),kind=String(vehicle?.serviceType||'');if(required&&required!==kind){toast(\`Esta missão exige ${required==='police'?'uma viatura':required==='firefighter'?'um caminhão dos bombeiros':'uma ambulância'}.\`,'warn',2400);return false;}if(job?.serviceVehicleId&&job.serviceVehicleId!==vehicle?.id){const assigned=serviceMissionVehicle(job);toast(\`Use ${assigned?.label||'o veículo indicado'} marcado no GPS para esta missão.\`,'warn',2500);return false;}if(!serviceVehicleAvailable(vehicle,job)){toast('Este veículo está atendendo outra ocorrência.','warn');return false;}return enterVehicle(vehicle);`
- [x] `return` — linha 3343, coluna 506 em `enterServiceVehicle()` — `const job=currentServiceMissionJob(),required=serviceKindForJob(job),kind=String(vehicle?.serviceType||'');if(required&&required!==kind){toast(\`Esta missão exige ${required==='police'?'uma viatura':required==='firefighter'?'um caminhão dos bombeiros':'uma ambulância'}.\`,'warn',2400);return false;}if(job?.serviceVehicleId&&job.serviceVehicleId!==vehicle?.id){const assigned=serviceMissionVehicle(job);toast(\`Use ${assigned?.label||'o veículo indicado'} marcado no GPS para esta missão.\`,'warn',2500);return false;}if(!serviceVehicleAvailable(vehicle,job)){toast('Este veículo está atendendo outra ocorrência.','warn');return false;}return enterVehicle(vehicle);`
- [x] `if` — linha 3343, coluna 520 em `enterServiceVehicle()` — `const job=currentServiceMissionJob(),required=serviceKindForJob(job),kind=String(vehicle?.serviceType||'');if(required&&required!==kind){toast(\`Esta missão exige ${required==='police'?'uma viatura':required==='firefighter'?'um caminhão dos bombeiros':'uma ambulância'}.\`,'warn',2400);return false;}if(job?.serviceVehicleId&&job.serviceVehicleId!==vehicle?.id){const assigned=serviceMissionVehicle(job);toast(\`Use ${assigned?.label||'o veículo indicado'} marcado no GPS para esta missão.\`,'warn',2500);return false;}if(!serviceVehicleAvailable(vehicle,job)){toast('Este veículo está atendendo outra ocorrência.','warn');return false;}return enterVehicle(vehicle);`
- [x] `return` — linha 3343, coluna 624 em `enterServiceVehicle()` — `const job=currentServiceMissionJob(),required=serviceKindForJob(job),kind=String(vehicle?.serviceType||'');if(required&&required!==kind){toast(\`Esta missão exige ${required==='police'?'uma viatura':required==='firefighter'?'um caminhão dos bombeiros':'uma ambulância'}.\`,'warn',2400);return false;}if(job?.serviceVehicleId&&job.serviceVehicleId!==vehicle?.id){const assigned=serviceMissionVehicle(job);toast(\`Use ${assigned?.label||'o veículo indicado'} marcado no GPS para esta missão.\`,'warn',2500);return false;}if(!serviceVehicleAvailable(vehicle,job)){toast('Este veículo está atendendo outra ocorrência.','warn');return false;}return enterVehicle(vehicle);`
- [x] `return` — linha 3343, coluna 638 em `enterServiceVehicle()` — `const job=currentServiceMissionJob(),required=serviceKindForJob(job),kind=String(vehicle?.serviceType||'');if(required&&required!==kind){toast(\`Esta missão exige ${required==='police'?'uma viatura':required==='firefighter'?'um caminhão dos bombeiros':'uma ambulância'}.\`,'warn',2400);return false;}if(job?.serviceVehicleId&&job.serviceVehicleId!==vehicle?.id){const assigned=serviceMissionVehicle(job);toast(\`Use ${assigned?.label||'o veículo indicado'} marcado no GPS para esta missão.\`,'warn',2500);return false;}if(!serviceVehicleAvailable(vehicle,job)){toast('Este veículo está atendendo outra ocorrência.','warn');return false;}return enterVehicle(vehicle);`
- [x] `return` — linha 3345, coluna 66 em `serviceMissionVehicle()` — `function serviceMissionVehicle(job=currentServiceMissionJob()){return job?.serviceVehicleId?serviceVehicleById(job.serviceVehicleId):null;}`
- [x] `if` — linha 3346, coluna 139 em `serviceVehicleMapLocation()` — `function serviceVehicleMapLocation(job=currentServiceMissionJob()){const vehicle=serviceMissionVehicle(job),kind=serviceKindForJob(job);if(!vehicle||job?.serviceVehicleBoarded)return null;const icon=kind==='police'?'🚓':kind==='firefighter'?'🚒':'🚑',x=Number(vehicle.group?.position?.x||vehicle.home?.x||0),z=Number(vehicle.group?.position?.z||vehicle.home?.z||0);return{id:\`service-${vehicle.id}\`,name:\`${vehicle.label} da missão\`,icon,group:'Missão ativa',x,z,navX:x,navZ:z,missionTarget:true,description:\`Este é o ${vehicle.label.toLowerCase()} reservado exclusivamente para a missão atual.\`,actions:['Vista o uniforme','Siga a rota azul','Chegue perto e use AÇÃO para entrar']};}`
- [x] `return` — linha 3346, coluna 179 em `serviceVehicleMapLocation()` — `function serviceVehicleMapLocation(job=currentServiceMissionJob()){const vehicle=serviceMissionVehicle(job),kind=serviceKindForJob(job);if(!vehicle||job?.serviceVehicleBoarded)return null;const icon=kind==='police'?'🚓':kind==='firefighter'?'🚒':'🚑',x=Number(vehicle.group?.position?.x||vehicle.home?.x||0),z=Number(vehicle.group?.position?.z||vehicle.home?.z||0);return{id:\`service-${vehicle.id}\`,name:\`${vehicle.label} da missão\`,icon,group:'Missão ativa',x,z,navX:x,navZ:z,missionTarget:true,description:\`Este é o ${vehicle.label.toLowerCase()} reservado exclusivamente para a missão atual.\`,actions:['Vista o uniforme','Siga a rota azul','Chegue perto e use AÇÃO para entrar']};}`
- [x] `return` — linha 3346, coluna 365 em `serviceVehicleMapLocation()` — `function serviceVehicleMapLocation(job=currentServiceMissionJob()){const vehicle=serviceMissionVehicle(job),kind=serviceKindForJob(job);if(!vehicle||job?.serviceVehicleBoarded)return null;const icon=kind==='police'?'🚓':kind==='firefighter'?'🚒':'🚑',x=Number(vehicle.group?.position?.x||vehicle.home?.x||0),z=Number(vehicle.group?.position?.z||vehicle.home?.z||0);return{id:\`service-${vehicle.id}\`,name:\`${vehicle.label} da missão\`,icon,group:'Missão ativa',x,z,navX:x,navZ:z,missionTarget:true,description:\`Este é o ${vehicle.label.toLowerCase()} reservado exclusivamente para a missão atual.\`,actions:['Vista o uniforme','Siga a rota azul','Chegue perto e use AÇÃO para entrar']};}`
- [x] `if` — linha 3348, coluna 39 em `reserveMissionServiceVehicle()` — `const kind=serviceKindForJob(job);if(!job||!kind)return null;let vehicle=serviceMissionVehicle(job);if(!vehicle||(!serviceVehicleAvailable(vehicle,job)&&activeVehicleRef!==vehicle))vehicle=serviceVehiclePools(kind).find(v=>serviceVehicleAvailable(v,job));if(!vehicle){toast('Todos os veículos desta equipe estão em atendimento. Aguarde a liberação.','warn',2600);return null;}vehicle.missionReservedFor=job.instanceId;job.serviceVehicleId=vehicle.id;job.serviceVehicleKind=kind;job.serviceVehicleBoarded=!!job.serviceVehicleBoarded;job.serviceVehicleArrived=!!job.serviceVehicleArrived;if(options.waypoint!==false&&!job.serviceVehicleBoarded){state.waypoint={id:\`service-${vehicle.id}\`,name:vehicle.label,x:vehicle.group.position.x,z:vehicle.group.position.z,navX:vehicle.group.position.x,navZ:vehicle.group.position.z,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);setMissionState(job,MISSION_STATES.PREPARING,'board-service-ve`
- [x] `return` — linha 3348, coluna 54 em `reserveMissionServiceVehicle()` — `const kind=serviceKindForJob(job);if(!job||!kind)return null;let vehicle=serviceMissionVehicle(job);if(!vehicle||(!serviceVehicleAvailable(vehicle,job)&&activeVehicleRef!==vehicle))vehicle=serviceVehiclePools(kind).find(v=>serviceVehicleAvailable(v,job));if(!vehicle){toast('Todos os veículos desta equipe estão em atendimento. Aguarde a liberação.','warn',2600);return null;}vehicle.missionReservedFor=job.instanceId;job.serviceVehicleId=vehicle.id;job.serviceVehicleKind=kind;job.serviceVehicleBoarded=!!job.serviceVehicleBoarded;job.serviceVehicleArrived=!!job.serviceVehicleArrived;if(options.waypoint!==false&&!job.serviceVehicleBoarded){state.waypoint={id:\`service-${vehicle.id}\`,name:vehicle.label,x:vehicle.group.position.x,z:vehicle.group.position.z,navX:vehicle.group.position.x,navZ:vehicle.group.position.z,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);setMissionState(job,MISSION_STATES.PREPARING,'board-service-ve`
- [x] `if` — linha 3348, coluna 105 em `reserveMissionServiceVehicle()` — `const kind=serviceKindForJob(job);if(!job||!kind)return null;let vehicle=serviceMissionVehicle(job);if(!vehicle||(!serviceVehicleAvailable(vehicle,job)&&activeVehicleRef!==vehicle))vehicle=serviceVehiclePools(kind).find(v=>serviceVehicleAvailable(v,job));if(!vehicle){toast('Todos os veículos desta equipe estão em atendimento. Aguarde a liberação.','warn',2600);return null;}vehicle.missionReservedFor=job.instanceId;job.serviceVehicleId=vehicle.id;job.serviceVehicleKind=kind;job.serviceVehicleBoarded=!!job.serviceVehicleBoarded;job.serviceVehicleArrived=!!job.serviceVehicleArrived;if(options.waypoint!==false&&!job.serviceVehicleBoarded){state.waypoint={id:\`service-${vehicle.id}\`,name:vehicle.label,x:vehicle.group.position.x,z:vehicle.group.position.z,navX:vehicle.group.position.x,navZ:vehicle.group.position.z,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);setMissionState(job,MISSION_STATES.PREPARING,'board-service-ve`
- [x] `if` — linha 3348, coluna 260 em `reserveMissionServiceVehicle()` — `const kind=serviceKindForJob(job);if(!job||!kind)return null;let vehicle=serviceMissionVehicle(job);if(!vehicle||(!serviceVehicleAvailable(vehicle,job)&&activeVehicleRef!==vehicle))vehicle=serviceVehiclePools(kind).find(v=>serviceVehicleAvailable(v,job));if(!vehicle){toast('Todos os veículos desta equipe estão em atendimento. Aguarde a liberação.','warn',2600);return null;}vehicle.missionReservedFor=job.instanceId;job.serviceVehicleId=vehicle.id;job.serviceVehicleKind=kind;job.serviceVehicleBoarded=!!job.serviceVehicleBoarded;job.serviceVehicleArrived=!!job.serviceVehicleArrived;if(options.waypoint!==false&&!job.serviceVehicleBoarded){state.waypoint={id:\`service-${vehicle.id}\`,name:vehicle.label,x:vehicle.group.position.x,z:vehicle.group.position.z,navX:vehicle.group.position.x,navZ:vehicle.group.position.z,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);setMissionState(job,MISSION_STATES.PREPARING,'board-service-ve`
- [x] `return` — linha 3348, coluna 368 em `reserveMissionServiceVehicle()` — `const kind=serviceKindForJob(job);if(!job||!kind)return null;let vehicle=serviceMissionVehicle(job);if(!vehicle||(!serviceVehicleAvailable(vehicle,job)&&activeVehicleRef!==vehicle))vehicle=serviceVehiclePools(kind).find(v=>serviceVehicleAvailable(v,job));if(!vehicle){toast('Todos os veículos desta equipe estão em atendimento. Aguarde a liberação.','warn',2600);return null;}vehicle.missionReservedFor=job.instanceId;job.serviceVehicleId=vehicle.id;job.serviceVehicleKind=kind;job.serviceVehicleBoarded=!!job.serviceVehicleBoarded;job.serviceVehicleArrived=!!job.serviceVehicleArrived;if(options.waypoint!==false&&!job.serviceVehicleBoarded){state.waypoint={id:\`service-${vehicle.id}\`,name:vehicle.label,x:vehicle.group.position.x,z:vehicle.group.position.z,navX:vehicle.group.position.x,navZ:vehicle.group.position.z,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);setMissionState(job,MISSION_STATES.PREPARING,'board-service-ve`
- [x] `if` — linha 3348, coluna 591 em `reserveMissionServiceVehicle()` — `const kind=serviceKindForJob(job);if(!job||!kind)return null;let vehicle=serviceMissionVehicle(job);if(!vehicle||(!serviceVehicleAvailable(vehicle,job)&&activeVehicleRef!==vehicle))vehicle=serviceVehiclePools(kind).find(v=>serviceVehicleAvailable(v,job));if(!vehicle){toast('Todos os veículos desta equipe estão em atendimento. Aguarde a liberação.','warn',2600);return null;}vehicle.missionReservedFor=job.instanceId;job.serviceVehicleId=vehicle.id;job.serviceVehicleKind=kind;job.serviceVehicleBoarded=!!job.serviceVehicleBoarded;job.serviceVehicleArrived=!!job.serviceVehicleArrived;if(options.waypoint!==false&&!job.serviceVehicleBoarded){state.waypoint={id:\`service-${vehicle.id}\`,name:vehicle.label,x:vehicle.group.position.x,z:vehicle.group.position.z,navX:vehicle.group.position.x,navZ:vehicle.group.position.z,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);setMissionState(job,MISSION_STATES.PREPARING,'board-service-ve`
- [x] `return` — linha 3348, coluna 1014 em `reserveMissionServiceVehicle()` — `const kind=serviceKindForJob(job);if(!job||!kind)return null;let vehicle=serviceMissionVehicle(job);if(!vehicle||(!serviceVehicleAvailable(vehicle,job)&&activeVehicleRef!==vehicle))vehicle=serviceVehiclePools(kind).find(v=>serviceVehicleAvailable(v,job));if(!vehicle){toast('Todos os veículos desta equipe estão em atendimento. Aguarde a liberação.','warn',2600);return null;}vehicle.missionReservedFor=job.instanceId;job.serviceVehicleId=vehicle.id;job.serviceVehicleKind=kind;job.serviceVehicleBoarded=!!job.serviceVehicleBoarded;job.serviceVehicleArrived=!!job.serviceVehicleArrived;if(options.waypoint!==false&&!job.serviceVehicleBoarded){state.waypoint={id:\`service-${vehicle.id}\`,name:vehicle.label,x:vehicle.group.position.x,z:vehicle.group.position.z,navX:vehicle.group.position.x,navZ:vehicle.group.position.z,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);setMissionState(job,MISSION_STATES.PREPARING,'board-service-ve`
- [x] `if` — linha 3350, coluna 87 em `releaseMissionServiceVehicle()` — `function releaseMissionServiceVehicle(job){const vehicle=serviceMissionVehicle(job);if(vehicle&&vehicle.missionReservedFor===job?.instanceId)vehicle.missionReservedFor='';if(job){job.serviceVehicleId='';job.serviceVehicleKind='';}}`
- [x] `if` — linha 3350, coluna 174 em `releaseMissionServiceVehicle()` — `function releaseMissionServiceVehicle(job){const vehicle=serviceMissionVehicle(job);if(vehicle&&vehicle.missionReservedFor===job?.instanceId)vehicle.missionReservedFor='';if(job){job.serviceVehicleId='';job.serviceVehicleKind='';}}`
- [x] `return` — linha 3351, coluna 42 em `isDrivingServiceVehicle()` — `function isDrivingServiceVehicle(kind){return!!(player.vehicle&&!player.car.passengerOf&&String(player.car.kind||'')===kind);}`
- [x] `if` — linha 3352, coluna 120 em `onServiceVehicleBoarded()` — `function onServiceVehicleBoarded(vehicle){const job=currentServiceMissionJob(),kind=String(vehicle?.serviceType||'');if(!job||serviceKindForJob(job)!==kind)return;job.serviceVehicleId=vehicle.id;job.serviceVehicleBoarded=true;job.serviceVehicleArrived=false;if(job.cooperative){if(typeof onCoopServiceVehicleBoarded==='function')onCoopServiceVehicleBoarded(vehicle,job);}else{setMissionState(job,MISSION_STATES.TRAVELLING,'service-vehicle-boarded');focusActiveJob();}saveState(true);}`
- [x] `return` — linha 3352, coluna 159 em `onServiceVehicleBoarded()` — `function onServiceVehicleBoarded(vehicle){const job=currentServiceMissionJob(),kind=String(vehicle?.serviceType||'');if(!job||serviceKindForJob(job)!==kind)return;job.serviceVehicleId=vehicle.id;job.serviceVehicleBoarded=true;job.serviceVehicleArrived=false;if(job.cooperative){if(typeof onCoopServiceVehicleBoarded==='function')onCoopServiceVehicleBoarded(vehicle,job);}else{setMissionState(job,MISSION_STATES.TRAVELLING,'service-vehicle-boarded');focusActiveJob();}saveState(true);}`
- [x] `if` — linha 3352, coluna 261 em `onServiceVehicleBoarded()` — `function onServiceVehicleBoarded(vehicle){const job=currentServiceMissionJob(),kind=String(vehicle?.serviceType||'');if(!job||serviceKindForJob(job)!==kind)return;job.serviceVehicleId=vehicle.id;job.serviceVehicleBoarded=true;job.serviceVehicleArrived=false;if(job.cooperative){if(typeof onCoopServiceVehicleBoarded==='function')onCoopServiceVehicleBoarded(vehicle,job);}else{setMissionState(job,MISSION_STATES.TRAVELLING,'service-vehicle-boarded');focusActiveJob();}saveState(true);}`
- [x] `if` — linha 3352, coluna 281 em `onServiceVehicleBoarded()` — `function onServiceVehicleBoarded(vehicle){const job=currentServiceMissionJob(),kind=String(vehicle?.serviceType||'');if(!job||serviceKindForJob(job)!==kind)return;job.serviceVehicleId=vehicle.id;job.serviceVehicleBoarded=true;job.serviceVehicleArrived=false;if(job.cooperative){if(typeof onCoopServiceVehicleBoarded==='function')onCoopServiceVehicleBoarded(vehicle,job);}else{setMissionState(job,MISSION_STATES.TRAVELLING,'service-vehicle-boarded');focusActiveJob();}saveState(true);}`
- [x] `else` — linha 3352, coluna 374 em `onServiceVehicleBoarded()` — `function onServiceVehicleBoarded(vehicle){const job=currentServiceMissionJob(),kind=String(vehicle?.serviceType||'');if(!job||serviceKindForJob(job)!==kind)return;job.serviceVehicleId=vehicle.id;job.serviceVehicleBoarded=true;job.serviceVehicleArrived=false;if(job.cooperative){if(typeof onCoopServiceVehicleBoarded==='function')onCoopServiceVehicleBoarded(vehicle,job);}else{setMissionState(job,MISSION_STATES.TRAVELLING,'service-vehicle-boarded');focusActiveJob();}saveState(true);}`
- [x] `if` — linha 3353, coluna 81 em `onServiceVehicleExited()` — `function onServiceVehicleExited(vehicle){const job=currentServiceMissionJob();if(!job||job.serviceVehicleId!==vehicle?.id)return;job.lastVehicleExitAt=Date.now();saveState();}`
- [x] `return` — linha 3353, coluna 125 em `onServiceVehicleExited()` — `function onServiceVehicleExited(vehicle){const job=currentServiceMissionJob();if(!job||job.serviceVehicleId!==vehicle?.id)return;job.lastVehicleExitAt=Date.now();saveState();}`
- [x] `if` — linha 3354, coluna 49 em `returnServiceVehicleHome()` — `function returnServiceVehicleHome(vehicle,dt){if(!vehicle?.home||vehicle.occupied)return;const distance=moveServiceVehicle(vehicle,vehicle.home,dt);if(distance<.8){vehicle.group.position.set(vehicle.home.x,.02,vehicle.home.z);vehicle.group.rotation.y=vehicle.home.heading||vehicle.group.rotation.y;vehicle.currentSpeed=0;vehicle.serviceRoute=[];vehicle.serviceRouteIndex=0;}}`
- [x] `return` — linha 3354, coluna 85 em `returnServiceVehicleHome()` — `function returnServiceVehicleHome(vehicle,dt){if(!vehicle?.home||vehicle.occupied)return;const distance=moveServiceVehicle(vehicle,vehicle.home,dt);if(distance<.8){vehicle.group.position.set(vehicle.home.x,.02,vehicle.home.z);vehicle.group.rotation.y=vehicle.home.heading||vehicle.group.rotation.y;vehicle.currentSpeed=0;vehicle.serviceRoute=[];vehicle.serviceRouteIndex=0;}}`
- [x] `if` — linha 3354, coluna 151 em `returnServiceVehicleHome()` — `function returnServiceVehicleHome(vehicle,dt){if(!vehicle?.home||vehicle.occupied)return;const distance=moveServiceVehicle(vehicle,vehicle.home,dt);if(distance<.8){vehicle.group.position.set(vehicle.home.x,.02,vehicle.home.z);vehicle.group.rotation.y=vehicle.home.heading||vehicle.group.rotation.y;vehicle.currentSpeed=0;vehicle.serviceRoute=[];vehicle.serviceRouteIndex=0;}}`
- [x] `for` — linha 3357, coluna 5 em `createFireIncidentSite()` — `for(let i=0;i<6;i++){const flame=new THREE.Mesh(new THREE.ConeGeometry(.32+i%2*.1,.8+i%3*.18,7),renderMat(i%2?0xffcf3d:0xff5b35,{emissive:i%2?0xff8a00:0xc31b08,emissiveIntensity:1.5,roughness:.18}));flame.position.set((i%3-1)*.38,.45+Math.floor(i/3)*.2,(i<3?-.22:.22));group.add(flame);flames.push(flame);}const smoke=[];for(let i=0;i<4;i++){const puff=new THREE.Mesh(new THREE.DodecahedronGeometry(.32+i*.07,0),mat(0x66717d,{transparent:true,opacity:.34,roughness:1}));puff.position.set((i%2?-.18:.18),1.2+i*.35,0);group.add(puff);smoke.push(puff);}const fire={...site,group,flames,smoke,active:false,startedAt:0,playerHelping:false,truckHelping:false};world.fires.push(fire);registerInteractable({id:\`fire-${site.id}\`,type:'fire',icon:'🧯',label:\`Ajudar: ${site.name}\`,radius:4.2,priority:245,getPos:()=>({x:site.x,z:site.z}),action:()=>helpExtinguishFire(fire)});return fire;`
- [x] `for` — linha 3357, coluna 326 em `createFireIncidentSite()` — `for(let i=0;i<6;i++){const flame=new THREE.Mesh(new THREE.ConeGeometry(.32+i%2*.1,.8+i%3*.18,7),renderMat(i%2?0xffcf3d:0xff5b35,{emissive:i%2?0xff8a00:0xc31b08,emissiveIntensity:1.5,roughness:.18}));flame.position.set((i%3-1)*.38,.45+Math.floor(i/3)*.2,(i<3?-.22:.22));group.add(flame);flames.push(flame);}const smoke=[];for(let i=0;i<4;i++){const puff=new THREE.Mesh(new THREE.DodecahedronGeometry(.32+i*.07,0),mat(0x66717d,{transparent:true,opacity:.34,roughness:1}));puff.position.set((i%2?-.18:.18),1.2+i*.35,0);group.add(puff);smoke.push(puff);}const fire={...site,group,flames,smoke,active:false,startedAt:0,playerHelping:false,truckHelping:false};world.fires.push(fire);registerInteractable({id:\`fire-${site.id}\`,type:'fire',icon:'🧯',label:\`Ajudar: ${site.name}\`,radius:4.2,priority:245,getPos:()=>({x:site.x,z:site.z}),action:()=>helpExtinguishFire(fire)});return fire;`
- [x] `return` — linha 3357, coluna 870 em `createFireIncidentSite()` — `for(let i=0;i<6;i++){const flame=new THREE.Mesh(new THREE.ConeGeometry(.32+i%2*.1,.8+i%3*.18,7),renderMat(i%2?0xffcf3d:0xff5b35,{emissive:i%2?0xff8a00:0xc31b08,emissiveIntensity:1.5,roughness:.18}));flame.position.set((i%3-1)*.38,.45+Math.floor(i/3)*.2,(i<3?-.22:.22));group.add(flame);flames.push(flame);}const smoke=[];for(let i=0;i<4;i++){const puff=new THREE.Mesh(new THREE.DodecahedronGeometry(.32+i*.07,0),mat(0x66717d,{transparent:true,opacity:.34,roughness:1}));puff.position.set((i%2?-.18:.18),1.2+i*.35,0);group.add(puff);smoke.push(puff);}const fire={...site,group,flames,smoke,active:false,startedAt:0,playerHelping:false,truckHelping:false};world.fires.push(fire);registerInteractable({id:\`fire-${site.id}\`,type:'fire',icon:'🧯',label:\`Ajudar: ${site.name}\`,radius:4.2,priority:245,getPos:()=>({x:site.x,z:site.z}),action:()=>helpExtinguishFire(fire)});return fire;`
- [x] `for` — linha 3362, coluna 5 em `createFireTruck()` — `for(const z of [-1.7,-.75,.2])premiumBox(1.85,.12,.12,0xe8edf0,0,1.45,z,g);premiumCylinder(.58,.95,0xcfd8df,0,1.22,-.2,g,16);premiumBox(.22,.22,3.75,0xe8edf0,0,2.35,-.15,g);for(let z=-1.7;z<=1.4;z+=.52)premiumBox(1.25,.08,.08,0xe8edf0,0,2.36,z,g);`
- [x] `for` — linha 3362, coluna 178 em `createFireTruck()` — `for(const z of [-1.7,-.75,.2])premiumBox(1.85,.12,.12,0xe8edf0,0,1.45,z,g);premiumCylinder(.58,.95,0xcfd8df,0,1.22,-.2,g,16);premiumBox(.22,.22,3.75,0xe8edf0,0,2.35,-.15,g);for(let z=-1.7;z<=1.4;z+=.52)premiumBox(1.25,.08,.08,0xe8edf0,0,2.36,z,g);`
- [x] `for` — linha 3363, coluna 246 em `createFireTruck()` — `const lightA=premiumBox(.58,.18,.32,renderMat(0xff3a42,{emissive:0xb80011,emissiveIntensity:1.2}),-.34,2.28,1.45,g),lightB=premiumBox(.58,.18,.32,renderMat(0x42bfff,{emissive:0x087db4,emissiveIntensity:1.2}),.34,2.28,1.45,g);const wheels=[];for(const p of [[-1.1,.38,-1.45],[1.1,.38,-1.45],[-1.1,.38,1.45],[1.1,.38,1.45]]){const wheel=premiumCylinder(.46,.28,0x111722,p[0],p[1],p[2],g,14);wheel.rotation.z=Math.PI/2;wheels.push(wheel);}worldGroup.add(g);const truck={id,group:g,wheels,lightA,lightB,speed:8.8,currentSpeed:0,targetFireId:'',route:[],routeIndex:0,arrivedAt:0,incidentTargetId:'',responseRoute:[],responseIndex:0};world.fireTrucks.push(truck);return registerServiceVehicle(truck,'firefighter',copy);`
- [x] `return` — linha 3363, coluna 662 em `createFireTruck()` — `const lightA=premiumBox(.58,.18,.32,renderMat(0xff3a42,{emissive:0xb80011,emissiveIntensity:1.2}),-.34,2.28,1.45,g),lightB=premiumBox(.58,.18,.32,renderMat(0x42bfff,{emissive:0x087db4,emissiveIntensity:1.2}),.34,2.28,1.45,g);const wheels=[];for(const p of [[-1.1,.38,-1.45],[1.1,.38,-1.45],[-1.1,.38,1.45],[1.1,.38,1.45]]){const wheel=premiumCylinder(.46,.28,0x111722,p[0],p[1],p[2],g,14);wheel.rotation.z=Math.PI/2;wheels.push(wheel);}worldGroup.add(g);const truck={id,group:g,wheels,lightA,lightB,speed:8.8,currentSpeed:0,targetFireId:'',route:[],routeIndex:0,arrivedAt:0,incidentTargetId:'',responseRoute:[],responseIndex:0};world.fireTrucks.push(truck);return registerServiceVehicle(truck,'firefighter',copy);`
- [x] `if` — linha 3366, coluna 57 em `activateFireIncident()` — `function activateFireIncident(siteId='',forced=false){if(world.fires.some(f=>f.active))return world.fires.find(f=>f.active);const available=world.fires.filter(f=>!f.active),fire=available.find(f=>f.id===siteId)||available[Math.floor(Math.random()*available.length)];if(!fire)return null;fire.active=true;fire.group.visible=true;fire.startedAt=performance.now();fire.playerHelping=false;fire.truckHelping=false;fire.coopIntensity=1;state.cityServices.emergencyCalls=(state.cityServices.emergencyCalls||0)+1;state.cityServices.lastFireAt=Date.now();world.nextFireAt=performance.now()+(forced?90000:65000+Math.random()*45000);toast(\`🚨 Chamado dos bombeiros: ${fire.name}\`,'warn',2600);saveState();return fire;}`
- [x] `return` — linha 3366, coluna 90 em `activateFireIncident()` — `function activateFireIncident(siteId='',forced=false){if(world.fires.some(f=>f.active))return world.fires.find(f=>f.active);const available=world.fires.filter(f=>!f.active),fire=available.find(f=>f.id===siteId)||available[Math.floor(Math.random()*available.length)];if(!fire)return null;fire.active=true;fire.group.visible=true;fire.startedAt=performance.now();fire.playerHelping=false;fire.truckHelping=false;fire.coopIntensity=1;state.cityServices.emergencyCalls=(state.cityServices.emergencyCalls||0)+1;state.cityServices.lastFireAt=Date.now();world.nextFireAt=performance.now()+(forced?90000:65000+Math.random()*45000);toast(\`🚨 Chamado dos bombeiros: ${fire.name}\`,'warn',2600);saveState();return fire;}`
- [x] `if` — linha 3366, coluna 269 em `activateFireIncident()` — `function activateFireIncident(siteId='',forced=false){if(world.fires.some(f=>f.active))return world.fires.find(f=>f.active);const available=world.fires.filter(f=>!f.active),fire=available.find(f=>f.id===siteId)||available[Math.floor(Math.random()*available.length)];if(!fire)return null;fire.active=true;fire.group.visible=true;fire.startedAt=performance.now();fire.playerHelping=false;fire.truckHelping=false;fire.coopIntensity=1;state.cityServices.emergencyCalls=(state.cityServices.emergencyCalls||0)+1;state.cityServices.lastFireAt=Date.now();world.nextFireAt=performance.now()+(forced?90000:65000+Math.random()*45000);toast(\`🚨 Chamado dos bombeiros: ${fire.name}\`,'warn',2600);saveState();return fire;}`
- [x] `return` — linha 3366, coluna 278 em `activateFireIncident()` — `function activateFireIncident(siteId='',forced=false){if(world.fires.some(f=>f.active))return world.fires.find(f=>f.active);const available=world.fires.filter(f=>!f.active),fire=available.find(f=>f.id===siteId)||available[Math.floor(Math.random()*available.length)];if(!fire)return null;fire.active=true;fire.group.visible=true;fire.startedAt=performance.now();fire.playerHelping=false;fire.truckHelping=false;fire.coopIntensity=1;state.cityServices.emergencyCalls=(state.cityServices.emergencyCalls||0)+1;state.cityServices.lastFireAt=Date.now();world.nextFireAt=performance.now()+(forced?90000:65000+Math.random()*45000);toast(\`🚨 Chamado dos bombeiros: ${fire.name}\`,'warn',2600);saveState();return fire;}`
- [x] `return` — linha 3366, coluna 697 em `activateFireIncident()` — `function activateFireIncident(siteId='',forced=false){if(world.fires.some(f=>f.active))return world.fires.find(f=>f.active);const available=world.fires.filter(f=>!f.active),fire=available.find(f=>f.id===siteId)||available[Math.floor(Math.random()*available.length)];if(!fire)return null;fire.active=true;fire.group.visible=true;fire.startedAt=performance.now();fire.playerHelping=false;fire.truckHelping=false;fire.coopIntensity=1;state.cityServices.emergencyCalls=(state.cityServices.emergencyCalls||0)+1;state.cityServices.lastFireAt=Date.now();world.nextFireAt=performance.now()+(forced?90000:65000+Math.random()*45000);toast(\`🚨 Chamado dos bombeiros: ${fire.name}\`,'warn',2600);saveState();return fire;}`
- [x] `return` — linha 3367, coluna 42 em `ensureActiveFire()` — `function ensureActiveFire(force=false){return world.fires.find(f=>f.active)||(force?activateFireIncident('',true):null);}`
- [x] `if` — linha 3368, coluna 37 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `return` — linha 3368, coluna 102 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `if` — linha 3368, coluna 144 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `return` — linha 3368, coluna 297 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `if` — linha 3368, coluna 305 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `return` — linha 3368, coluna 456 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `if` — linha 3368, coluna 464 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `return` — linha 3368, coluna 563 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `if` — linha 3368, coluna 571 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `return` — linha 3368, coluna 681 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `if` — linha 3368, coluna 689 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `for` — linha 3368, coluna 942 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `if` — linha 3368, coluna 1079 em `helpExtinguishFire()` — `function helpExtinguishFire(fire){if(!fire?.active){toast('A ocorrência já foi resolvida.','good');return;}const job=state.career?.activeJob;if(job?.id==='firefighter'&&!job.serviceVehicleBoarded){toast('Primeiro vista o uniforme e leve o caminhão dos bombeiros até a ocorrência.','warn',2600);return;}if(job?.id==='firefighter'&&!job.serviceVehicleArrived){toast('A missão só libera a mangueira depois que o caminhão chegar à ocorrência.','warn',2600);return;}if(player.vehicle){toast('Estacione o caminhão, saia e use AÇÃO junto à área segura.','warn',2200);return;}if(Math.hypot(player.x-fire.x,player.z-fire.z)>5){toast('Chegue mais perto da área segura demarcada.','warn');return;}if(job?.id==='firefighter'){job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'hose-active');}fire.playerHelping=true;player.sitUntil=performance.now()+2400;toast('Mangueira ativada. Mantenha distância segura!','good',1800);for(let i=0;i<8;i++)setTimeout(()=>spawnDust(lerp(player.x,fi`
- [x] `if` — linha 3369, coluna 56 em `extinguishFireIncident()` — `function extinguishFireIncident(fire,byPlayer=false){if(!fire?.active)return;fire.active=false;fire.group.visible=false;fire.playerHelping=false;fire.truckHelping=false;state.cityServices.firesExtinguished=(state.cityServices.firesExtinguished||0)+1;if(byPlayer)state.stats.firesHelped=(state.stats.firesHelped||0)+1;for(const truck of world.fireTrucks)if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;truck.arrivedAt=0;}if(state.career.activeJob?.id==='firefighter'){state.career.activeJob.completed=true;completeActiveJob();}if(state.waypoint?.id===\`fire-${fire.id}\`){state.waypoint=null;updateWaypointMarker();updateNavigation(0,true);}addXP(byPlayer?65:20);toast(byPlayer?'Emergência resolvida em equipe!':'Os bombeiros controlaram a ocorrência.','good',2400);saveState(true);}`
- [x] `return` — linha 3369, coluna 73 em `extinguishFireIncident()` — `function extinguishFireIncident(fire,byPlayer=false){if(!fire?.active)return;fire.active=false;fire.group.visible=false;fire.playerHelping=false;fire.truckHelping=false;state.cityServices.firesExtinguished=(state.cityServices.firesExtinguished||0)+1;if(byPlayer)state.stats.firesHelped=(state.stats.firesHelped||0)+1;for(const truck of world.fireTrucks)if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;truck.arrivedAt=0;}if(state.career.activeJob?.id==='firefighter'){state.career.activeJob.completed=true;completeActiveJob();}if(state.waypoint?.id===\`fire-${fire.id}\`){state.waypoint=null;updateWaypointMarker();updateNavigation(0,true);}addXP(byPlayer?65:20);toast(byPlayer?'Emergência resolvida em equipe!':'Os bombeiros controlaram a ocorrência.','good',2400);saveState(true);}`
- [x] `if` — linha 3369, coluna 253 em `extinguishFireIncident()` — `function extinguishFireIncident(fire,byPlayer=false){if(!fire?.active)return;fire.active=false;fire.group.visible=false;fire.playerHelping=false;fire.truckHelping=false;state.cityServices.firesExtinguished=(state.cityServices.firesExtinguished||0)+1;if(byPlayer)state.stats.firesHelped=(state.stats.firesHelped||0)+1;for(const truck of world.fireTrucks)if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;truck.arrivedAt=0;}if(state.career.activeJob?.id==='firefighter'){state.career.activeJob.completed=true;completeActiveJob();}if(state.waypoint?.id===\`fire-${fire.id}\`){state.waypoint=null;updateWaypointMarker();updateNavigation(0,true);}addXP(byPlayer?65:20);toast(byPlayer?'Emergência resolvida em equipe!':'Os bombeiros controlaram a ocorrência.','good',2400);saveState(true);}`
- [x] `for` — linha 3369, coluna 320 em `extinguishFireIncident()` — `function extinguishFireIncident(fire,byPlayer=false){if(!fire?.active)return;fire.active=false;fire.group.visible=false;fire.playerHelping=false;fire.truckHelping=false;state.cityServices.firesExtinguished=(state.cityServices.firesExtinguished||0)+1;if(byPlayer)state.stats.firesHelped=(state.stats.firesHelped||0)+1;for(const truck of world.fireTrucks)if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;truck.arrivedAt=0;}if(state.career.activeJob?.id==='firefighter'){state.career.activeJob.completed=true;completeActiveJob();}if(state.waypoint?.id===\`fire-${fire.id}\`){state.waypoint=null;updateWaypointMarker();updateNavigation(0,true);}addXP(byPlayer?65:20);toast(byPlayer?'Emergência resolvida em equipe!':'Os bombeiros controlaram a ocorrência.','good',2400);saveState(true);}`
- [x] `if` — linha 3369, coluna 356 em `extinguishFireIncident()` — `function extinguishFireIncident(fire,byPlayer=false){if(!fire?.active)return;fire.active=false;fire.group.visible=false;fire.playerHelping=false;fire.truckHelping=false;state.cityServices.firesExtinguished=(state.cityServices.firesExtinguished||0)+1;if(byPlayer)state.stats.firesHelped=(state.stats.firesHelped||0)+1;for(const truck of world.fireTrucks)if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;truck.arrivedAt=0;}if(state.career.activeJob?.id==='firefighter'){state.career.activeJob.completed=true;completeActiveJob();}if(state.waypoint?.id===\`fire-${fire.id}\`){state.waypoint=null;updateWaypointMarker();updateNavigation(0,true);}addXP(byPlayer?65:20);toast(byPlayer?'Emergência resolvida em equipe!':'Os bombeiros controlaram a ocorrência.','good',2400);saveState(true);}`
- [x] `if` — linha 3369, coluna 478 em `extinguishFireIncident()` — `function extinguishFireIncident(fire,byPlayer=false){if(!fire?.active)return;fire.active=false;fire.group.visible=false;fire.playerHelping=false;fire.truckHelping=false;state.cityServices.firesExtinguished=(state.cityServices.firesExtinguished||0)+1;if(byPlayer)state.stats.firesHelped=(state.stats.firesHelped||0)+1;for(const truck of world.fireTrucks)if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;truck.arrivedAt=0;}if(state.career.activeJob?.id==='firefighter'){state.career.activeJob.completed=true;completeActiveJob();}if(state.waypoint?.id===\`fire-${fire.id}\`){state.waypoint=null;updateWaypointMarker();updateNavigation(0,true);}addXP(byPlayer?65:20);toast(byPlayer?'Emergência resolvida em equipe!':'Os bombeiros controlaram a ocorrência.','good',2400);saveState(true);}`
- [x] `if` — linha 3369, coluna 584 em `extinguishFireIncident()` — `function extinguishFireIncident(fire,byPlayer=false){if(!fire?.active)return;fire.active=false;fire.group.visible=false;fire.playerHelping=false;fire.truckHelping=false;state.cityServices.firesExtinguished=(state.cityServices.firesExtinguished||0)+1;if(byPlayer)state.stats.firesHelped=(state.stats.firesHelped||0)+1;for(const truck of world.fireTrucks)if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;truck.arrivedAt=0;}if(state.career.activeJob?.id==='firefighter'){state.career.activeJob.completed=true;completeActiveJob();}if(state.waypoint?.id===\`fire-${fire.id}\`){state.waypoint=null;updateWaypointMarker();updateNavigation(0,true);}addXP(byPlayer?65:20);toast(byPlayer?'Emergência resolvida em equipe!':'Os bombeiros controlaram a ocorrência.','good',2400);saveState(true);}`
- [x] `if` — linha 3370, coluna 48 em `serviceVehicleRoute()` — `function serviceVehicleRoute(vehicle,target){if(!vehicle.serviceRoute?.length){vehicle.serviceRoute=buildRoutePoints({x:vehicle.group.position.x,z:vehicle.group.position.z},target);vehicle.serviceRouteIndex=1;}return vehicle.serviceRoute;}`
- [x] `return` — linha 3370, coluna 213 em `serviceVehicleRoute()` — `function serviceVehicleRoute(vehicle,target){if(!vehicle.serviceRoute?.length){vehicle.serviceRoute=buildRoutePoints({x:vehicle.group.position.x,z:vehicle.group.position.z},target);vehicle.serviceRouteIndex=1;}return vehicle.serviceRoute;}`
- [x] `if` — linha 3371, coluna 50 em `moveServiceVehicle()` — `function moveServiceVehicle(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}const route=serviceVehicleRoute(vehicle,target),point=route[vehicle.serviceRouteIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.serviceRouteIndex++;if(vehicle.serviceRouteIndex>=route.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveServiceVehicle(vehicle,target,dt);}const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8.5),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=factor<.035?0:lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4.8));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group`
- [x] `return` — linha 3371, coluna 131 em `moveServiceVehicle()` — `function moveServiceVehicle(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}const route=serviceVehicleRoute(vehicle,target),point=route[vehicle.serviceRouteIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.serviceRouteIndex++;if(vehicle.serviceRouteIndex>=route.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveServiceVehicle(vehicle,target,dt);}const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8.5),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=factor<.035?0:lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4.8));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group`
- [x] `if` — linha 3371, coluna 306 em `moveServiceVehicle()` — `function moveServiceVehicle(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}const route=serviceVehicleRoute(vehicle,target),point=route[vehicle.serviceRouteIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.serviceRouteIndex++;if(vehicle.serviceRouteIndex>=route.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveServiceVehicle(vehicle,target,dt);}const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8.5),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=factor<.035?0:lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4.8));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group`
- [x] `return` — linha 3371, coluna 316 em `moveServiceVehicle()` — `function moveServiceVehicle(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}const route=serviceVehicleRoute(vehicle,target),point=route[vehicle.serviceRouteIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.serviceRouteIndex++;if(vehicle.serviceRouteIndex>=route.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveServiceVehicle(vehicle,target,dt);}const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8.5),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=factor<.035?0:lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4.8));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group`
- [x] `if` — linha 3371, coluna 501 em `moveServiceVehicle()` — `function moveServiceVehicle(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}const route=serviceVehicleRoute(vehicle,target),point=route[vehicle.serviceRouteIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.serviceRouteIndex++;if(vehicle.serviceRouteIndex>=route.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveServiceVehicle(vehicle,target,dt);}const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8.5),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=factor<.035?0:lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4.8));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group`
- [x] `if` — linha 3371, coluna 539 em `moveServiceVehicle()` — `function moveServiceVehicle(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}const route=serviceVehicleRoute(vehicle,target),point=route[vehicle.serviceRouteIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.serviceRouteIndex++;if(vehicle.serviceRouteIndex>=route.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveServiceVehicle(vehicle,target,dt);}const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8.5),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=factor<.035?0:lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4.8));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group`
- [x] `return` — linha 3371, coluna 582 em `moveServiceVehicle()` — `function moveServiceVehicle(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}const route=serviceVehicleRoute(vehicle,target),point=route[vehicle.serviceRouteIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.serviceRouteIndex++;if(vehicle.serviceRouteIndex>=route.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveServiceVehicle(vehicle,target,dt);}const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8.5),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=factor<.035?0:lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4.8));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group`
- [x] `return` — linha 3371, coluna 669 em `moveServiceVehicle()` — `function moveServiceVehicle(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}const route=serviceVehicleRoute(vehicle,target),point=route[vehicle.serviceRouteIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.serviceRouteIndex++;if(vehicle.serviceRouteIndex>=route.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveServiceVehicle(vehicle,target,dt);}const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8.5),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=factor<.035?0:lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4.8));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group`
- [x] `if` — linha 3371, coluna 1043 em `moveServiceVehicle()` — `function moveServiceVehicle(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}const route=serviceVehicleRoute(vehicle,target),point=route[vehicle.serviceRouteIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.serviceRouteIndex++;if(vehicle.serviceRouteIndex>=route.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveServiceVehicle(vehicle,target,dt);}const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8.5),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=factor<.035?0:lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4.8));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group`
- [x] `for` — linha 3371, coluna 1258 em `moveServiceVehicle()` — `function moveServiceVehicle(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}const route=serviceVehicleRoute(vehicle,target),point=route[vehicle.serviceRouteIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.serviceRouteIndex++;if(vehicle.serviceRouteIndex>=route.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveServiceVehicle(vehicle,target,dt);}const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8.5),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=factor<.035?0:lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4.8));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group`
- [x] `return` — linha 3371, coluna 1320 em `moveServiceVehicle()` — `function moveServiceVehicle(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}const route=serviceVehicleRoute(vehicle,target),point=route[vehicle.serviceRouteIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.serviceRouteIndex++;if(vehicle.serviceRouteIndex>=route.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveServiceVehicle(vehicle,target,dt);}const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8.5),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=factor<.035?0:lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4.8));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group`
- [x] `if` — linha 3374, coluna 33 em `updateFireService()` — `const now=performance.now();if(!world.fires.length)return;if(!world.fires.some(f=>f.active)&&now>world.nextFireAt&&!currentHouse)activateFireIncident();`
- [x] `return` — linha 3374, coluna 56 em `updateFireService()` — `const now=performance.now();if(!world.fires.length)return;if(!world.fires.some(f=>f.active)&&now>world.nextFireAt&&!currentHouse)activateFireIncident();`
- [x] `if` — linha 3374, coluna 63 em `updateFireService()` — `const now=performance.now();if(!world.fires.length)return;if(!world.fires.some(f=>f.active)&&now>world.nextFireAt&&!currentHouse)activateFireIncident();`
- [x] `for` — linha 3375, coluna 5 em `updateFireService()` — `for(const fire of world.fires){if(!fire.active)continue;const intensity=clamp(Number(fire.coopIntensity??1),.08,1);fire.flames.forEach((flame,i)=>{flame.scale.y=(.82+Math.sin(now*.008+i)*.22)*intensity;flame.scale.x=.55+intensity*.45;flame.scale.z=.55+intensity*.45;flame.rotation.y+=dt*(.7+i*.08);});fire.smoke.forEach((puff,i)=>{puff.position.y=1.25+i*.35+Math.sin(now*.002+i)*.14;puff.material.opacity=.12+intensity*.22;puff.rotation.y+=dt*.35;});}`
- [x] `if` — linha 3375, coluna 36 em `updateFireService()` — `for(const fire of world.fires){if(!fire.active)continue;const intensity=clamp(Number(fire.coopIntensity??1),.08,1);fire.flames.forEach((flame,i)=>{flame.scale.y=(.82+Math.sin(now*.008+i)*.22)*intensity;flame.scale.x=.55+intensity*.45;flame.scale.z=.55+intensity*.45;flame.rotation.y+=dt*(.7+i*.08);});fire.smoke.forEach((puff,i)=>{puff.position.y=1.25+i*.35+Math.sin(now*.002+i)*.14;puff.material.opacity=.12+intensity*.22;puff.rotation.y+=dt*.35;});}`
- [x] `for` — linha 3377, coluna 5 em `updateFireService()` — `for(const truck of world.fireTrucks){`
- [x] `if` — linha 3378, coluna 197 em `updateFireService()` — `const fireResponding=!!(truck.targetFireId||truck.incidentTargetId);truck.lightA.visible=fireResponding&&Math.floor(now/230)%2===0;truck.lightB.visible=fireResponding&&!truck.lightA.visible;if(truck.occupied)continue;if(truck.incidentTargetId)continue;`
- [x] `if` — linha 3378, coluna 224 em `updateFireService()` — `const fireResponding=!!(truck.targetFireId||truck.incidentTargetId);truck.lightA.visible=fireResponding&&Math.floor(now/230)%2===0;truck.lightB.visible=fireResponding&&!truck.lightA.visible;if(truck.occupied)continue;if(truck.incidentTargetId)continue;`
- [x] `if` — linha 3379, coluna 49 em `updateFireService()` — `const reserved=!!truck.missionReservedFor;if(active&&!active.coopMissionId&&!truck.targetFireId&&!reserved){truck.targetFireId=active.id;truck.serviceRoute=[];truck.serviceRouteIndex=0;}`
- [x] `if` — linha 3380, coluna 7 em `updateFireService()` — `if(truck.targetFireId){const fire=world.fires.find(f=>f.id===truck.targetFireId&&f.active);if(!fire){truck.targetFireId='';truck.serviceRoute=[];continue;}const distance=moveServiceVehicle(truck,{x:fire.navX,z:fire.navZ},dt);if(distance<5.5){if(!truck.arrivedAt)truck.arrivedAt=now;fire.truckHelping=true;truck.sprayAcc=(truck.sprayAcc||0)+dt;if(truck.sprayAcc>.1){truck.sprayAcc=0;spawnDust(lerp(truck.group.position.x,fire.x,.7),lerp(truck.group.position.z,fire.z,.7),0x69d8ff);}if(now-truck.arrivedAt>5200&&state.career.activeJob?.id!=='firefighter'&&!fire.coopMissionId)extinguishFireIncident(fire,false);}}`
- [x] `if` — linha 3380, coluna 98 em `updateFireService()` — `if(truck.targetFireId){const fire=world.fires.find(f=>f.id===truck.targetFireId&&f.active);if(!fire){truck.targetFireId='';truck.serviceRoute=[];continue;}const distance=moveServiceVehicle(truck,{x:fire.navX,z:fire.navZ},dt);if(distance<5.5){if(!truck.arrivedAt)truck.arrivedAt=now;fire.truckHelping=true;truck.sprayAcc=(truck.sprayAcc||0)+dt;if(truck.sprayAcc>.1){truck.sprayAcc=0;spawnDust(lerp(truck.group.position.x,fire.x,.7),lerp(truck.group.position.z,fire.z,.7),0x69d8ff);}if(now-truck.arrivedAt>5200&&state.career.activeJob?.id!=='firefighter'&&!fire.coopMissionId)extinguishFireIncident(fire,false);}}`
- [x] `if` — linha 3380, coluna 232 em `updateFireService()` — `if(truck.targetFireId){const fire=world.fires.find(f=>f.id===truck.targetFireId&&f.active);if(!fire){truck.targetFireId='';truck.serviceRoute=[];continue;}const distance=moveServiceVehicle(truck,{x:fire.navX,z:fire.navZ},dt);if(distance<5.5){if(!truck.arrivedAt)truck.arrivedAt=now;fire.truckHelping=true;truck.sprayAcc=(truck.sprayAcc||0)+dt;if(truck.sprayAcc>.1){truck.sprayAcc=0;spawnDust(lerp(truck.group.position.x,fire.x,.7),lerp(truck.group.position.z,fire.z,.7),0x69d8ff);}if(now-truck.arrivedAt>5200&&state.career.activeJob?.id!=='firefighter'&&!fire.coopMissionId)extinguishFireIncident(fire,false);}}`
- [x] `if` — linha 3380, coluna 249 em `updateFireService()` — `if(truck.targetFireId){const fire=world.fires.find(f=>f.id===truck.targetFireId&&f.active);if(!fire){truck.targetFireId='';truck.serviceRoute=[];continue;}const distance=moveServiceVehicle(truck,{x:fire.navX,z:fire.navZ},dt);if(distance<5.5){if(!truck.arrivedAt)truck.arrivedAt=now;fire.truckHelping=true;truck.sprayAcc=(truck.sprayAcc||0)+dt;if(truck.sprayAcc>.1){truck.sprayAcc=0;spawnDust(lerp(truck.group.position.x,fire.x,.7),lerp(truck.group.position.z,fire.z,.7),0x69d8ff);}if(now-truck.arrivedAt>5200&&state.career.activeJob?.id!=='firefighter'&&!fire.coopMissionId)extinguishFireIncident(fire,false);}}`
- [x] `if` — linha 3380, coluna 350 em `updateFireService()` — `if(truck.targetFireId){const fire=world.fires.find(f=>f.id===truck.targetFireId&&f.active);if(!fire){truck.targetFireId='';truck.serviceRoute=[];continue;}const distance=moveServiceVehicle(truck,{x:fire.navX,z:fire.navZ},dt);if(distance<5.5){if(!truck.arrivedAt)truck.arrivedAt=now;fire.truckHelping=true;truck.sprayAcc=(truck.sprayAcc||0)+dt;if(truck.sprayAcc>.1){truck.sprayAcc=0;spawnDust(lerp(truck.group.position.x,fire.x,.7),lerp(truck.group.position.z,fire.z,.7),0x69d8ff);}if(now-truck.arrivedAt>5200&&state.career.activeJob?.id!=='firefighter'&&!fire.coopMissionId)extinguishFireIncident(fire,false);}}`
- [x] `if` — linha 3380, coluna 488 em `updateFireService()` — `if(truck.targetFireId){const fire=world.fires.find(f=>f.id===truck.targetFireId&&f.active);if(!fire){truck.targetFireId='';truck.serviceRoute=[];continue;}const distance=moveServiceVehicle(truck,{x:fire.navX,z:fire.navZ},dt);if(distance<5.5){if(!truck.arrivedAt)truck.arrivedAt=now;fire.truckHelping=true;truck.sprayAcc=(truck.sprayAcc||0)+dt;if(truck.sprayAcc>.1){truck.sprayAcc=0;spawnDust(lerp(truck.group.position.x,fire.x,.7),lerp(truck.group.position.z,fire.z,.7),0x69d8ff);}if(now-truck.arrivedAt>5200&&state.career.activeJob?.id!=='firefighter'&&!fire.coopMissionId)extinguishFireIncident(fire,false);}}`
- [x] `else` — linha 3381, coluna 7 em `updateFireService()` — `else returnServiceVehicleHome(truck,dt);`
- [x] `for` — linha 3391, coluna 21 em `createPoliceCar()` — `const wheels=[];for(const p of [[-.94,.28,-.88],[.94,.28,-.88],[-.94,.28,.88],[.94,.28,.88]]){const wheel=premiumCylinder(.36,.25,0x10151d,p[0],p[1],p[2],g,14);wheel.rotation.z=Math.PI/2;wheels.push(wheel);}`
- [x] `if` — linha 3392, coluna 20 em `createPoliceCar()` — `g.traverse(o=>{if(o.isMesh)addVoxelOutline(o,0x132238,.22);});const car={id,group:g,route,routeIndex:(routeIndex+1)%route.length,speed:8.1,currentSpeed:0,wheels,red,cyan,npcTarget:'',npcChaseUntil:0,incidentTargetId:'',responseRoute:[],responseIndex:0};worldGroup.add(g);world.policeCars.push(car);return registerServiceVehicle(car,'police',world.policeCars.length-1);`
- [x] `return` — linha 3392, coluna 303 em `createPoliceCar()` — `g.traverse(o=>{if(o.isMesh)addVoxelOutline(o,0x132238,.22);});const car={id,group:g,route,routeIndex:(routeIndex+1)%route.length,speed:8.1,currentSpeed:0,wheels,red,cyan,npcTarget:'',npcChaseUntil:0,incidentTargetId:'',responseRoute:[],responseIndex:0};worldGroup.add(g);world.policeCars.push(car);return registerServiceVehicle(car,'police',world.policeCars.length-1);`
- [x] `for` — linha 3398, coluna 5 em `createAmbulance()` — `for(const q of [[-1.0,.38,-1.15],[1.0,.38,-1.15],[-1.0,.38,1.15],[1.0,.38,1.15]]){const wheel=premiumCylinder(.43,.26,0x111722,q[0],q[1],q[2],g,14);wheel.rotation.z=Math.PI/2;wheels.push(wheel);}`
- [x] `return` — linha 3399, coluna 182 em `createAmbulance()` — `worldGroup.add(g);const ambulance={id,group:g,wheels,lightA,lightB,speed:9,currentSpeed:0,incidentTargetId:'',responseRoute:[],responseIndex:0};world.ambulances.push(ambulance);return registerServiceVehicle(ambulance,'paramedic',world.ambulances.length-1);`
- [x] `if` — linha 3407, coluna 5 em `movePoliceToward()` — `if(performance.now()<Number(car.trafficHoldUntil||0)){car.currentSpeed=0;return Math.hypot(car.group.position.x-target.x,car.group.position.z-target.z);}`
- [x] `return` — linha 3407, coluna 78 em `movePoliceToward()` — `if(performance.now()<Number(car.trafficHoldUntil||0)){car.currentSpeed=0;return Math.hypot(car.group.position.x-target.x,car.group.position.z-target.z);}`
- [x] `if` — linha 3408, coluna 97 em `movePoliceToward()` — `const dx=target.x-car.group.position.x,dz=target.z-car.group.position.z,d=Math.hypot(dx,dz);if(d<.08)return d;const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(car,heading,8),targetSpeed=speed*factor;car.currentSpeed=lerp(Number(car.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const move=Math.min(d,car.currentSpeed*dt),previous={x:car.group.position.x,z:car.group.position.z};if(move>.0001){car.group.position.x+=dx/d*move;car.group.position.z+=dz/d*move;snapTrafficToRoad(car.group,previous);car.group.rotation.y=lerpAngle(car.group.rotation.y,heading,Math.min(1,dt*6));for(const wheel of car.wheels)wheel.rotation.x-=move*2.8;}return d;`
- [x] `return` — linha 3408, coluna 106 em `movePoliceToward()` — `const dx=target.x-car.group.position.x,dz=target.z-car.group.position.z,d=Math.hypot(dx,dz);if(d<.08)return d;const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(car,heading,8),targetSpeed=speed*factor;car.currentSpeed=lerp(Number(car.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const move=Math.min(d,car.currentSpeed*dt),previous={x:car.group.position.x,z:car.group.position.z};if(move>.0001){car.group.position.x+=dx/d*move;car.group.position.z+=dz/d*move;snapTrafficToRoad(car.group,previous);car.group.rotation.y=lerpAngle(car.group.rotation.y,heading,Math.min(1,dt*6));for(const wheel of car.wheels)wheel.rotation.x-=move*2.8;}return d;`
- [x] `if` — linha 3408, coluna 393 em `movePoliceToward()` — `const dx=target.x-car.group.position.x,dz=target.z-car.group.position.z,d=Math.hypot(dx,dz);if(d<.08)return d;const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(car,heading,8),targetSpeed=speed*factor;car.currentSpeed=lerp(Number(car.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const move=Math.min(d,car.currentSpeed*dt),previous={x:car.group.position.x,z:car.group.position.z};if(move>.0001){car.group.position.x+=dx/d*move;car.group.position.z+=dz/d*move;snapTrafficToRoad(car.group,previous);car.group.rotation.y=lerpAngle(car.group.rotation.y,heading,Math.min(1,dt*6));for(const wheel of car.wheels)wheel.rotation.x-=move*2.8;}return d;`
- [x] `for` — linha 3408, coluna 588 em `movePoliceToward()` — `const dx=target.x-car.group.position.x,dz=target.z-car.group.position.z,d=Math.hypot(dx,dz);if(d<.08)return d;const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(car,heading,8),targetSpeed=speed*factor;car.currentSpeed=lerp(Number(car.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const move=Math.min(d,car.currentSpeed*dt),previous={x:car.group.position.x,z:car.group.position.z};if(move>.0001){car.group.position.x+=dx/d*move;car.group.position.z+=dz/d*move;snapTrafficToRoad(car.group,previous);car.group.rotation.y=lerpAngle(car.group.rotation.y,heading,Math.min(1,dt*6));for(const wheel of car.wheels)wheel.rotation.x-=move*2.8;}return d;`
- [x] `return` — linha 3408, coluna 646 em `movePoliceToward()` — `const dx=target.x-car.group.position.x,dz=target.z-car.group.position.z,d=Math.hypot(dx,dz);if(d<.08)return d;const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(car,heading,8),targetSpeed=speed*factor;car.currentSpeed=lerp(Number(car.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const move=Math.min(d,car.currentSpeed*dt),previous={x:car.group.position.x,z:car.group.position.z};if(move>.0001){car.group.position.x+=dx/d*move;car.group.position.z+=dz/d*move;snapTrafficToRoad(car.group,previous);car.group.rotation.y=lerpAngle(car.group.rotation.y,heading,Math.min(1,dt*6));for(const wheel of car.wheels)wheel.rotation.x-=move*2.8;}return d;`
- [x] `if` — linha 3411, coluna 85 em `updatePolicePatrol()` — `const target=car.route[car.routeIndex],distance=movePoliceToward(car,target,dt);if(distance<.28)car.routeIndex=(car.routeIndex+1)%car.route.length;`
- [x] `if` — linha 3414, coluna 5 em `updateSafetyPanel()` — `if(!els.safetyPanel)return;els.safetyPanel.hidden=!message;if(els.safetyStatus)els.safetyStatus.textContent=message;`
- [x] `return` — linha 3414, coluna 25 em `updateSafetyPanel()` — `if(!els.safetyPanel)return;els.safetyPanel.hidden=!message;if(els.safetyStatus)els.safetyStatus.textContent=message;`
- [x] `if` — linha 3414, coluna 64 em `updateSafetyPanel()` — `if(!els.safetyPanel)return;els.safetyPanel.hidden=!message;if(els.safetyStatus)els.safetyStatus.textContent=message;`
- [x] `if` — linha 3417, coluna 5 em `startPoliceAlert()` — `if(world.policeAlert||!player.vehicle||player.car.passengerOf)return false;const now=performance.now();`
- [x] `return` — linha 3417, coluna 67 em `startPoliceAlert()` — `if(world.policeAlert||!player.vehicle||player.car.passengerOf)return false;const now=performance.now();`
- [x] `if` — linha 3418, coluna 5 em `startPoliceAlert()` — `if(Date.now()-Number(state.safety.lastIncident||0)<5000)return false;`
- [x] `return` — linha 3418, coluna 61 em `startPoliceAlert()` — `if(Date.now()-Number(state.safety.lastIncident||0)<5000)return false;`
- [x] `return` — linha 3419, coluna 310 em `startPoliceAlert()` — `state.safety.incidents=(state.safety.incidents||0)+1;state.safety.lastIncident=Date.now();world.policeAlert={carId:car.id,startedAt:now,slowSince:0};car.npcTarget='';saveState(true);updateSafetyPanel('Encoste com calma • solte o acelerador');toast('Patrulha de segurança: encoste com calma.','warn',2200);return true;`
- [x] `if` — linha 3422, coluna 35 em `finishSafetyStop()` — `const alert=world.policeAlert;if(!alert)return;world.policeAlert=null;state.safety.safeStops=(state.safety.safeStops||0)+1;`
- [x] `return` — linha 3422, coluna 45 em `finishSafetyStop()` — `const alert=world.policeAlert;if(!alert)return;world.policeAlert=null;state.safety.safeStops=(state.safety.safeStops||0)+1;`
- [x] `if` — linha 3423, coluna 5 em `finishSafetyStop()` — `if(player.vehicle)exitVehicle(true);clearMovementInputs();player.x=68;player.z=-12.2;player.y=groundHeightAt(player.x,player.z);player.vx=player.vy=player.vz=0;player.grounded=true;playerGroup?.position?.set(player.x,player.y,player.z);updateSafetyPanel('');saveState(true);setTimeout(()=>openSafetyLesson('incident'),120);`
- [x] `if` — linha 3432, coluna 191 em `openSafetyLesson()` — `$$('[data-safety-answer]',root).forEach(button=>button.onclick=()=>{const correct=Number(button.dataset.safetyAnswer)===lesson.correct;state.safety.lessons=(state.safety.lessons||0)+1;if(correct){addXP(18);addReputation(3);awardMedal('Direção Segura');}saveState(true);closeModal();toast(correct?'Boa escolha! Segurança vem primeiro.':'Vamos lembrar: reduza, observe e cuide de todos.',correct?'good':'warn',2600);});`
- [x] `for` — linha 3437, coluna 5 em `updatePoliceSystem()` — `for(const car of world.policeCars){`
- [x] `if` — linha 3439, coluna 7 em `updatePoliceSystem()` — `if(car.occupied)continue;if(car.incidentTargetId)continue;if(car.missionReservedFor){returnServiceVehicleHome(car,dt);continue;}`
- [x] `if` — linha 3439, coluna 32 em `updatePoliceSystem()` — `if(car.occupied)continue;if(car.incidentTargetId)continue;if(car.missionReservedFor){returnServiceVehicleHome(car,dt);continue;}`
- [x] `if` — linha 3439, coluna 65 em `updatePoliceSystem()` — `if(car.occupied)continue;if(car.incidentTargetId)continue;if(car.missionReservedFor){returnServiceVehicleHome(car,dt);continue;}`
- [x] `if` — linha 3440, coluna 7 em `updatePoliceSystem()` — `if(active){const distance=movePoliceToward(car,player,dt,12.8),stopped=Math.abs(player.car.speed)<1.15;alert.slowSince=stopped?(alert.slowSince||now):0;updateSafetyPanel(stopped?'Muito bem • aguarde a patrulha':'Encoste com calma • solte o acelerador');if(!player.vehicle||distance<2.15||(alert.slowSince&&now-alert.slowSince>1100)||now-alert.startedAt>15000)finishSafetyStop();}`
- [x] `if` — linha 3440, coluna 260 em `updatePoliceSystem()` — `if(active){const distance=movePoliceToward(car,player,dt,12.8),stopped=Math.abs(player.car.speed)<1.15;alert.slowSince=stopped?(alert.slowSince||now):0;updateSafetyPanel(stopped?'Muito bem • aguarde a patrulha':'Encoste com calma • solte o acelerador');if(!player.vehicle||distance<2.15||(alert.slowSince&&now-alert.slowSince>1100)||now-alert.startedAt>15000)finishSafetyStop();}`
- [x] `else` — linha 3441, coluna 7 em `updatePoliceSystem()` — `else if(car.npcTarget){const npc=world.npcs.find(item=>item.id===car.npcTarget);if(!npc||now>car.npcChaseUntil){if(npc)npcSpeech(npc,'Vou reduzir a velocidade e dirigir com cuidado.');car.npcTarget='';}else if(movePoliceToward(car,npc.group.position,dt,10.2)<2.25){npc.policeCooldown=now+45000;npcSpeech(npc,'Entendi, patrulha! Segurança primeiro.');car.npcTarget='';}}`
- [x] `if` — linha 3441, coluna 12 em `updatePoliceSystem()` — `else if(car.npcTarget){const npc=world.npcs.find(item=>item.id===car.npcTarget);if(!npc||now>car.npcChaseUntil){if(npc)npcSpeech(npc,'Vou reduzir a velocidade e dirigir com cuidado.');car.npcTarget='';}else if(movePoliceToward(car,npc.group.position,dt,10.2)<2.25){npc.policeCooldown=now+45000;npcSpeech(npc,'Entendi, patrulha! Segurança primeiro.');car.npcTarget='';}}`
- [x] `if` — linha 3441, coluna 87 em `updatePoliceSystem()` — `else if(car.npcTarget){const npc=world.npcs.find(item=>item.id===car.npcTarget);if(!npc||now>car.npcChaseUntil){if(npc)npcSpeech(npc,'Vou reduzir a velocidade e dirigir com cuidado.');car.npcTarget='';}else if(movePoliceToward(car,npc.group.position,dt,10.2)<2.25){npc.policeCooldown=now+45000;npcSpeech(npc,'Entendi, patrulha! Segurança primeiro.');car.npcTarget='';}}`
- [x] `if` — linha 3441, coluna 119 em `updatePoliceSystem()` — `else if(car.npcTarget){const npc=world.npcs.find(item=>item.id===car.npcTarget);if(!npc||now>car.npcChaseUntil){if(npc)npcSpeech(npc,'Vou reduzir a velocidade e dirigir com cuidado.');car.npcTarget='';}else if(movePoliceToward(car,npc.group.position,dt,10.2)<2.25){npc.policeCooldown=now+45000;npcSpeech(npc,'Entendi, patrulha! Segurança primeiro.');car.npcTarget='';}}`
- [x] `else` — linha 3441, coluna 209 em `updatePoliceSystem()` — `else if(car.npcTarget){const npc=world.npcs.find(item=>item.id===car.npcTarget);if(!npc||now>car.npcChaseUntil){if(npc)npcSpeech(npc,'Vou reduzir a velocidade e dirigir com cuidado.');car.npcTarget='';}else if(movePoliceToward(car,npc.group.position,dt,10.2)<2.25){npc.policeCooldown=now+45000;npcSpeech(npc,'Entendi, patrulha! Segurança primeiro.');car.npcTarget='';}}`
- [x] `if` — linha 3441, coluna 214 em `updatePoliceSystem()` — `else if(car.npcTarget){const npc=world.npcs.find(item=>item.id===car.npcTarget);if(!npc||now>car.npcChaseUntil){if(npc)npcSpeech(npc,'Vou reduzir a velocidade e dirigir com cuidado.');car.npcTarget='';}else if(movePoliceToward(car,npc.group.position,dt,10.2)<2.25){npc.policeCooldown=now+45000;npcSpeech(npc,'Entendi, patrulha! Segurança primeiro.');car.npcTarget='';}}`
- [x] `else` — linha 3442, coluna 7 em `updatePoliceSystem()` — `else updatePolicePatrol(car,dt);`
- [x] `if` — linha 3444, coluna 5 em `updatePoliceSystem()` — `if(!alert&&player.vehicle&&!player.car.passengerOf&&Math.abs(player.car.speed)>1.4&&!['police','firefighter','paramedic'].includes(player.car.kind)){const hit=world.policeCars.find(car=>!car.occupied&&Math.hypot(player.x-car.group.position.x,player.z-car.group.position.z)<2.45);if(hit)startPoliceAlert(hit);}`
- [x] `if` — linha 3444, coluna 284 em `updatePoliceSystem()` — `if(!alert&&player.vehicle&&!player.car.passengerOf&&Math.abs(player.car.speed)>1.4&&!['police','firefighter','paramedic'].includes(player.car.kind)){const hit=world.policeCars.find(car=>!car.occupied&&Math.hypot(player.x-car.group.position.x,player.z-car.group.position.z)<2.45);if(hit)startPoliceAlert(hit);}`
- [x] `if` — linha 3445, coluna 5 em `updatePoliceSystem()` — `if(!alert){for(const car of world.policeCars){if(car.occupied||car.missionReservedFor||car.npcTarget||car.incidentTargetId)continue;const npc=world.npcs.find(item=>item.mobility&&['car','moto'].includes(item.mobility.type)&&now>Number(item.policeCooldown||0)&&Math.hypot(item.group.position.x-car.group.position.x,item.group.position.z-car.group.position.z)<2.0);if(npc){car.npcTarget=npc.id;car.npcChaseUntil=now+6500;npc.policeCooldown=now+45000;break;}}}`
- [x] `for` — linha 3445, coluna 16 em `updatePoliceSystem()` — `if(!alert){for(const car of world.policeCars){if(car.occupied||car.missionReservedFor||car.npcTarget||car.incidentTargetId)continue;const npc=world.npcs.find(item=>item.mobility&&['car','moto'].includes(item.mobility.type)&&now>Number(item.policeCooldown||0)&&Math.hypot(item.group.position.x-car.group.position.x,item.group.position.z-car.group.position.z)<2.0);if(npc){car.npcTarget=npc.id;car.npcChaseUntil=now+6500;npc.policeCooldown=now+45000;break;}}}`
- [x] `if` — linha 3445, coluna 51 em `updatePoliceSystem()` — `if(!alert){for(const car of world.policeCars){if(car.occupied||car.missionReservedFor||car.npcTarget||car.incidentTargetId)continue;const npc=world.npcs.find(item=>item.mobility&&['car','moto'].includes(item.mobility.type)&&now>Number(item.policeCooldown||0)&&Math.hypot(item.group.position.x-car.group.position.x,item.group.position.z-car.group.position.z)<2.0);if(npc){car.npcTarget=npc.id;car.npcChaseUntil=now+6500;npc.policeCooldown=now+45000;break;}}}`
- [x] `if` — linha 3445, coluna 368 em `updatePoliceSystem()` — `if(!alert){for(const car of world.policeCars){if(car.occupied||car.missionReservedFor||car.npcTarget||car.incidentTargetId)continue;const npc=world.npcs.find(item=>item.mobility&&['car','moto'].includes(item.mobility.type)&&now>Number(item.policeCooldown||0)&&Math.hypot(item.group.position.x-car.group.position.x,item.group.position.z-car.group.position.z)<2.0);if(npc){car.npcTarget=npc.id;car.npcChaseUntil=now+6500;npc.policeCooldown=now+45000;break;}}}`
- [x] `if` — linha 3447, coluna 53 em `moveIncidentResponder()` — `function moveIncidentResponder(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}`
- [x] `return` — linha 3447, coluna 134 em `moveIncidentResponder()` — `function moveIncidentResponder(vehicle,target,dt){if(performance.now()<Number(vehicle.trafficHoldUntil||0)){vehicle.currentSpeed=0;return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);}`
- [x] `if` — linha 3448, coluna 5 em `moveIncidentResponder()` — `if(!vehicle.responseRoute?.length){vehicle.responseRoute=buildRoutePoints({x:vehicle.group.position.x,z:vehicle.group.position.z},target);vehicle.responseIndex=1;}`
- [x] `if` — linha 3449, coluna 62 em `moveIncidentResponder()` — `const point=vehicle.responseRoute[vehicle.responseIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);`
- [x] `return` — linha 3449, coluna 72 em `moveIncidentResponder()` — `const point=vehicle.responseRoute[vehicle.responseIndex];if(!point)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);`
- [x] `if` — linha 3450, coluna 103 em `moveIncidentResponder()` — `const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.responseIndex++;if(vehicle.responseIndex>=vehicle.responseRoute.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveIncidentResponder(vehicle,target,dt);}`
- [x] `if` — linha 3450, coluna 137 em `moveIncidentResponder()` — `const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.responseIndex++;if(vehicle.responseIndex>=vehicle.responseRoute.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveIncidentResponder(vehicle,target,dt);}`
- [x] `return` — linha 3450, coluna 192 em `moveIncidentResponder()` — `const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.responseIndex++;if(vehicle.responseIndex>=vehicle.responseRoute.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveIncidentResponder(vehicle,target,dt);}`
- [x] `return` — linha 3450, coluna 279 em `moveIncidentResponder()` — `const dx=point.x-vehicle.group.position.x,dz=point.z-vehicle.group.position.z,d=Math.hypot(dx,dz);if(d<.45){vehicle.responseIndex++;if(vehicle.responseIndex>=vehicle.responseRoute.length)return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);return moveIncidentResponder(vehicle,target,dt);}`
- [x] `if` — linha 3451, coluna 315 em `moveIncidentResponder()` — `const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group.position.x,z:vehicle.group.position.z};if(move>.0001){vehicle.group.position.x+=dx/d*move;vehicle.group.position.z+=dz/d*move;snapTrafficToRoad(vehicle.group,previous);vehicle.group.rotation.y=lerpAngle(vehicle.group.rotation.y,heading,Math.min(1,dt*6));for(const wheel of vehicle.wheels||[])wheel.rotation.x-=move*2.3;}return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);`
- [x] `for` — linha 3451, coluna 530 em `moveIncidentResponder()` — `const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group.position.x,z:vehicle.group.position.z};if(move>.0001){vehicle.group.position.x+=dx/d*move;vehicle.group.position.z+=dz/d*move;snapTrafficToRoad(vehicle.group,previous);vehicle.group.rotation.y=lerpAngle(vehicle.group.rotation.y,heading,Math.min(1,dt*6));for(const wheel of vehicle.wheels||[])wheel.rotation.x-=move*2.3;}return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);`
- [x] `return` — linha 3451, coluna 596 em `moveIncidentResponder()` — `const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(vehicle,heading,8),targetSpeed=vehicle.speed*factor;vehicle.currentSpeed=lerp(Number(vehicle.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const move=Math.min(d,vehicle.currentSpeed*dt),previous={x:vehicle.group.position.x,z:vehicle.group.position.z};if(move>.0001){vehicle.group.position.x+=dx/d*move;vehicle.group.position.z+=dz/d*move;snapTrafficToRoad(vehicle.group,previous);vehicle.group.rotation.y=lerpAngle(vehicle.group.rotation.y,heading,Math.min(1,dt*6));for(const wheel of vehicle.wheels||[])wheel.rotation.x-=move*2.3;}return Math.hypot(vehicle.group.position.x-target.x,vehicle.group.position.z-target.z);`
- [x] `return` — linha 3453, coluna 39 em `incidentServiceLabel()` — `function incidentServiceLabel(kind){return kind==='police'?'Polícia':kind==='firefighter'?'Bombeiros':'Resgate';}`
- [x] `if` — linha 3454, coluna 69 em `lockIncidentActor()` — `function lockIncidentActor(record,incident){const ref=record?.ref;if(!ref)return;incident.actorStates.push({ref,incidentUntil:ref.incidentUntil||0,currentSpeed:Number(ref.currentSpeed||0),state:ref.state||''});ref.incidentLocked=true;ref.incidentUntil=Number.MAX_SAFE_INTEGER;ref.currentSpeed=0;if(ref===player.car){player.car.speed=0;player.vx=0;player.vz=0;}if(ref.route&&typeof setBusState==='function'&&String(record.type)==='bus')setBusState(ref,BUS_STATES.ACCIDENT,'traffic-incident');}`
- [x] `return` — linha 3454, coluna 77 em `lockIncidentActor()` — `function lockIncidentActor(record,incident){const ref=record?.ref;if(!ref)return;incident.actorStates.push({ref,incidentUntil:ref.incidentUntil||0,currentSpeed:Number(ref.currentSpeed||0),state:ref.state||''});ref.incidentLocked=true;ref.incidentUntil=Number.MAX_SAFE_INTEGER;ref.currentSpeed=0;if(ref===player.car){player.car.speed=0;player.vx=0;player.vz=0;}if(ref.route&&typeof setBusState==='function'&&String(record.type)==='bus')setBusState(ref,BUS_STATES.ACCIDENT,'traffic-incident');}`
- [x] `if` — linha 3454, coluna 298 em `lockIncidentActor()` — `function lockIncidentActor(record,incident){const ref=record?.ref;if(!ref)return;incident.actorStates.push({ref,incidentUntil:ref.incidentUntil||0,currentSpeed:Number(ref.currentSpeed||0),state:ref.state||''});ref.incidentLocked=true;ref.incidentUntil=Number.MAX_SAFE_INTEGER;ref.currentSpeed=0;if(ref===player.car){player.car.speed=0;player.vx=0;player.vz=0;}if(ref.route&&typeof setBusState==='function'&&String(record.type)==='bus')setBusState(ref,BUS_STATES.ACCIDENT,'traffic-incident');}`
- [x] `if` — linha 3454, coluna 363 em `lockIncidentActor()` — `function lockIncidentActor(record,incident){const ref=record?.ref;if(!ref)return;incident.actorStates.push({ref,incidentUntil:ref.incidentUntil||0,currentSpeed:Number(ref.currentSpeed||0),state:ref.state||''});ref.incidentLocked=true;ref.incidentUntil=Number.MAX_SAFE_INTEGER;ref.currentSpeed=0;if(ref===player.car){player.car.speed=0;player.vx=0;player.vz=0;}if(ref.route&&typeof setBusState==='function'&&String(record.type)==='bus')setBusState(ref,BUS_STATES.ACCIDENT,'traffic-incident');}`
- [x] `if` — linha 3455, coluna 40 em `separateIncidentActors()` — `function separateIncidentActors(a,b){if(!a?.group||!b?.group)return;let dx=b.group.position.x-a.group.position.x,dz=b.group.position.z-a.group.position.z,d=Math.hypot(dx,dz);if(d<.05){dx=1;dz=0;d=1;}const gap=(Number(a.radius||1.5)+Number(b.radius||1.5))*.92,push=Math.max(0,(gap-d)/2+.35),nx=dx/d,nz=dz/d;const move=(record,sign)=>{if(record.id==='player-car'){player.x=clamp(player.x+nx*push*sign,-115,115);player.z=clamp(player.z+nz*push*sign,-115,115);return;}record.group.position.x+=nx*push*sign;record.group.position.z+=nz*push*sign;snapTrafficToRoad(record.group);};move(a,-1);move(b,1);}`
- [x] `return` — linha 3455, coluna 64 em `separateIncidentActors()` — `function separateIncidentActors(a,b){if(!a?.group||!b?.group)return;let dx=b.group.position.x-a.group.position.x,dz=b.group.position.z-a.group.position.z,d=Math.hypot(dx,dz);if(d<.05){dx=1;dz=0;d=1;}const gap=(Number(a.radius||1.5)+Number(b.radius||1.5))*.92,push=Math.max(0,(gap-d)/2+.35),nx=dx/d,nz=dz/d;const move=(record,sign)=>{if(record.id==='player-car'){player.x=clamp(player.x+nx*push*sign,-115,115);player.z=clamp(player.z+nz*push*sign,-115,115);return;}record.group.position.x+=nx*push*sign;record.group.position.z+=nz*push*sign;snapTrafficToRoad(record.group);};move(a,-1);move(b,1);}`
- [x] `if` — linha 3455, coluna 177 em `separateIncidentActors()` — `function separateIncidentActors(a,b){if(!a?.group||!b?.group)return;let dx=b.group.position.x-a.group.position.x,dz=b.group.position.z-a.group.position.z,d=Math.hypot(dx,dz);if(d<.05){dx=1;dz=0;d=1;}const gap=(Number(a.radius||1.5)+Number(b.radius||1.5))*.92,push=Math.max(0,(gap-d)/2+.35),nx=dx/d,nz=dz/d;const move=(record,sign)=>{if(record.id==='player-car'){player.x=clamp(player.x+nx*push*sign,-115,115);player.z=clamp(player.z+nz*push*sign,-115,115);return;}record.group.position.x+=nx*push*sign;record.group.position.z+=nz*push*sign;snapTrafficToRoad(record.group);};move(a,-1);move(b,1);}`
- [x] `if` — linha 3455, coluna 336 em `separateIncidentActors()` — `function separateIncidentActors(a,b){if(!a?.group||!b?.group)return;let dx=b.group.position.x-a.group.position.x,dz=b.group.position.z-a.group.position.z,d=Math.hypot(dx,dz);if(d<.05){dx=1;dz=0;d=1;}const gap=(Number(a.radius||1.5)+Number(b.radius||1.5))*.92,push=Math.max(0,(gap-d)/2+.35),nx=dx/d,nz=dz/d;const move=(record,sign)=>{if(record.id==='player-car'){player.x=clamp(player.x+nx*push*sign,-115,115);player.z=clamp(player.z+nz*push*sign,-115,115);return;}record.group.position.x+=nx*push*sign;record.group.position.z+=nz*push*sign;snapTrafficToRoad(record.group);};move(a,-1);move(b,1);}`
- [x] `return` — linha 3455, coluna 459 em `separateIncidentActors()` — `function separateIncidentActors(a,b){if(!a?.group||!b?.group)return;let dx=b.group.position.x-a.group.position.x,dz=b.group.position.z-a.group.position.z,d=Math.hypot(dx,dz);if(d<.05){dx=1;dz=0;d=1;}const gap=(Number(a.radius||1.5)+Number(b.radius||1.5))*.92,push=Math.max(0,(gap-d)/2+.35),nx=dx/d,nz=dz/d;const move=(record,sign)=>{if(record.id==='player-car'){player.x=clamp(player.x+nx*push*sign,-115,115);player.z=clamp(player.z+nz*push*sign,-115,115);return;}record.group.position.x+=nx*push*sign;record.group.position.z+=nz*push*sign;snapTrafficToRoad(record.group);};move(a,-1);move(b,1);}`
- [x] `for` — linha 3456, coluna 127 em `createIncidentVisual()` — `function createIncidentVisual(x,z,fire=false){const group=new THREE.Group();group.position.set(x,0,z);worldGroup.add(group);for(const ox of [-1.55,-.52,.52,1.55]){premiumCylinder(.18,.72,0xf47b20,ox,.36,.95,group,8);premiumBox(.52,.1,.52,0xffffff,ox,.16,.95,group);}premiumBox(3.8,.08,.14,0xf5c542,0,.72,.95,group);if(fire){for(const ox of [-.4,.4]){const flame=new THREE.Mesh(new THREE.ConeGeometry(.3,.85,7),renderMat(0xff6a2f,{emissive:0xd83b12,emissiveIntensity:1.3}));flame.position.set(ox,.48,0);group.add(flame);}}const beacon=new THREE.Mesh(new THREE.TorusGeometry(2.35,.07,8,40),renderMat(0xffc928,{emissive:0xc07800,emissiveIntensity:.55,transparent:true,opacity:.8}));beacon.rotation.x=Math.PI/2;beacon.position.y=.08;group.add(beacon);return group;}`
- [x] `if` — linha 3456, coluna 318 em `createIncidentVisual()` — `function createIncidentVisual(x,z,fire=false){const group=new THREE.Group();group.position.set(x,0,z);worldGroup.add(group);for(const ox of [-1.55,-.52,.52,1.55]){premiumCylinder(.18,.72,0xf47b20,ox,.36,.95,group,8);premiumBox(.52,.1,.52,0xffffff,ox,.16,.95,group);}premiumBox(3.8,.08,.14,0xf5c542,0,.72,.95,group);if(fire){for(const ox of [-.4,.4]){const flame=new THREE.Mesh(new THREE.ConeGeometry(.3,.85,7),renderMat(0xff6a2f,{emissive:0xd83b12,emissiveIntensity:1.3}));flame.position.set(ox,.48,0);group.add(flame);}}const beacon=new THREE.Mesh(new THREE.TorusGeometry(2.35,.07,8,40),renderMat(0xffc928,{emissive:0xc07800,emissiveIntensity:.55,transparent:true,opacity:.8}));beacon.rotation.x=Math.PI/2;beacon.position.y=.08;group.add(beacon);return group;}`
- [x] `for` — linha 3456, coluna 327 em `createIncidentVisual()` — `function createIncidentVisual(x,z,fire=false){const group=new THREE.Group();group.position.set(x,0,z);worldGroup.add(group);for(const ox of [-1.55,-.52,.52,1.55]){premiumCylinder(.18,.72,0xf47b20,ox,.36,.95,group,8);premiumBox(.52,.1,.52,0xffffff,ox,.16,.95,group);}premiumBox(3.8,.08,.14,0xf5c542,0,.72,.95,group);if(fire){for(const ox of [-.4,.4]){const flame=new THREE.Mesh(new THREE.ConeGeometry(.3,.85,7),renderMat(0xff6a2f,{emissive:0xd83b12,emissiveIntensity:1.3}));flame.position.set(ox,.48,0);group.add(flame);}}const beacon=new THREE.Mesh(new THREE.TorusGeometry(2.35,.07,8,40),renderMat(0xffc928,{emissive:0xc07800,emissiveIntensity:.55,transparent:true,opacity:.8}));beacon.rotation.x=Math.PI/2;beacon.position.y=.08;group.add(beacon);return group;}`
- [x] `return` — linha 3456, coluna 750 em `createIncidentVisual()` — `function createIncidentVisual(x,z,fire=false){const group=new THREE.Group();group.position.set(x,0,z);worldGroup.add(group);for(const ox of [-1.55,-.52,.52,1.55]){premiumCylinder(.18,.72,0xf47b20,ox,.36,.95,group,8);premiumBox(.52,.1,.52,0xffffff,ox,.16,.95,group);}premiumBox(3.8,.08,.14,0xf5c542,0,.72,.95,group);if(fire){for(const ox of [-.4,.4]){const flame=new THREE.Mesh(new THREE.ConeGeometry(.3,.85,7),renderMat(0xff6a2f,{emissive:0xd83b12,emissiveIntensity:1.3}));flame.position.set(ox,.48,0);group.add(flame);}}const beacon=new THREE.Mesh(new THREE.TorusGeometry(2.35,.07,8,40),renderMat(0xffc928,{emissive:0xc07800,emissiveIntensity:.55,transparent:true,opacity:.8}));beacon.rotation.x=Math.PI/2;beacon.position.y=.08;group.add(beacon);return group;}`
- [x] `if` — linha 3458, coluna 5 em `createTrafficIncidentAt()` — `if(world.activeIncident||currentHouse)return null;const now=performance.now(),fire=severity==='fire',projection=nearestRoadProjection({x,z})?.point||{x,z},group=createIncidentVisual(x,z,fire),incident={id:\`incident-${++world.emergencySeq}\`,x,z,navX:projection.x,navZ:projection.z,group,fire,startedAt:now,resolved:false,required:{police:true,paramedic:true,firefighter:true},arrived:{police:false,paramedic:false,firefighter:false},tasks:{police:false,paramedic:false,firefighter:false},playerAssigned:{},responders:{},actorStates:[],actors:actors.map(item=>item.ref).filter(Boolean),readyAt:0};world.activeIncident=incident;world.trafficIncidents.push(incident);if(actors.length>=2)separateIncidentActors(actors[0],actors[1]);for(const actor of actors)lockIncidentActor(actor,incident);if(actors.some(actor=>actor.id==='player-car')){player.car.speed=0;state.stats.accidentsHelped=(state.stats.accidentsHelped||0)+1;}`
- [x] `return` — linha 3458, coluna 43 em `createTrafficIncidentAt()` — `if(world.activeIncident||currentHouse)return null;const now=performance.now(),fire=severity==='fire',projection=nearestRoadProjection({x,z})?.point||{x,z},group=createIncidentVisual(x,z,fire),incident={id:\`incident-${++world.emergencySeq}\`,x,z,navX:projection.x,navZ:projection.z,group,fire,startedAt:now,resolved:false,required:{police:true,paramedic:true,firefighter:true},arrived:{police:false,paramedic:false,firefighter:false},tasks:{police:false,paramedic:false,firefighter:false},playerAssigned:{},responders:{},actorStates:[],actors:actors.map(item=>item.ref).filter(Boolean),readyAt:0};world.activeIncident=incident;world.trafficIncidents.push(incident);if(actors.length>=2)separateIncidentActors(actors[0],actors[1]);for(const actor of actors)lockIncidentActor(actor,incident);if(actors.some(actor=>actor.id==='player-car')){player.car.speed=0;state.stats.accidentsHelped=(state.stats.accidentsHelped||0)+1;}`
- [x] `if` — linha 3458, coluna 668 em `createTrafficIncidentAt()` — `if(world.activeIncident||currentHouse)return null;const now=performance.now(),fire=severity==='fire',projection=nearestRoadProjection({x,z})?.point||{x,z},group=createIncidentVisual(x,z,fire),incident={id:\`incident-${++world.emergencySeq}\`,x,z,navX:projection.x,navZ:projection.z,group,fire,startedAt:now,resolved:false,required:{police:true,paramedic:true,firefighter:true},arrived:{police:false,paramedic:false,firefighter:false},tasks:{police:false,paramedic:false,firefighter:false},playerAssigned:{},responders:{},actorStates:[],actors:actors.map(item=>item.ref).filter(Boolean),readyAt:0};world.activeIncident=incident;world.trafficIncidents.push(incident);if(actors.length>=2)separateIncidentActors(actors[0],actors[1]);for(const actor of actors)lockIncidentActor(actor,incident);if(actors.some(actor=>actor.id==='player-car')){player.car.speed=0;state.stats.accidentsHelped=(state.stats.accidentsHelped||0)+1;}`
- [x] `for` — linha 3458, coluna 732 em `createTrafficIncidentAt()` — `if(world.activeIncident||currentHouse)return null;const now=performance.now(),fire=severity==='fire',projection=nearestRoadProjection({x,z})?.point||{x,z},group=createIncidentVisual(x,z,fire),incident={id:\`incident-${++world.emergencySeq}\`,x,z,navX:projection.x,navZ:projection.z,group,fire,startedAt:now,resolved:false,required:{police:true,paramedic:true,firefighter:true},arrived:{police:false,paramedic:false,firefighter:false},tasks:{police:false,paramedic:false,firefighter:false},playerAssigned:{},responders:{},actorStates:[],actors:actors.map(item=>item.ref).filter(Boolean),readyAt:0};world.activeIncident=incident;world.trafficIncidents.push(incident);if(actors.length>=2)separateIncidentActors(actors[0],actors[1]);for(const actor of actors)lockIncidentActor(actor,incident);if(actors.some(actor=>actor.id==='player-car')){player.car.speed=0;state.stats.accidentsHelped=(state.stats.accidentsHelped||0)+1;}`
- [x] `if` — linha 3458, coluna 792 em `createTrafficIncidentAt()` — `if(world.activeIncident||currentHouse)return null;const now=performance.now(),fire=severity==='fire',projection=nearestRoadProjection({x,z})?.point||{x,z},group=createIncidentVisual(x,z,fire),incident={id:\`incident-${++world.emergencySeq}\`,x,z,navX:projection.x,navZ:projection.z,group,fire,startedAt:now,resolved:false,required:{police:true,paramedic:true,firefighter:true},arrived:{police:false,paramedic:false,firefighter:false},tasks:{police:false,paramedic:false,firefighter:false},playerAssigned:{},responders:{},actorStates:[],actors:actors.map(item=>item.ref).filter(Boolean),readyAt:0};world.activeIncident=incident;world.trafficIncidents.push(incident);if(actors.length>=2)separateIncidentActors(actors[0],actors[1]);for(const actor of actors)lockIncidentActor(actor,incident);if(actors.some(actor=>actor.id==='player-car')){player.car.speed=0;state.stats.accidentsHelped=(state.stats.accidentsHelped||0)+1;}`
- [x] `return` — linha 3460, coluna 305 em `createTrafficIncidentAt()` — `dispatchIncidentResponders(incident);state.cityServices.emergencyCalls=(state.cityServices.emergencyCalls||0)+1;toast(fire?'🚨 Acidente com princípio de incêndio: polícia, resgate e bombeiros acionados.':'🚨 Acidente: veículos parados; polícia, resgate e bombeiros acionados.','warn',3400);saveState();return incident;`
- [x] `if` — linha 3462, coluna 56 em `createTrafficIncident()` — `function createTrafficIncident(a,b,severity='minor'){if(!a||!b)return null;const x=(a.group.position.x+b.group.position.x)/2,z=(a.group.position.z+b.group.position.z)/2;return createTrafficIncidentAt(x,z,severity,[a,b]);}`
- [x] `return` — linha 3462, coluna 66 em `createTrafficIncident()` — `function createTrafficIncident(a,b,severity='minor'){if(!a||!b)return null;const x=(a.group.position.x+b.group.position.x)/2,z=(a.group.position.z+b.group.position.z)/2;return createTrafficIncidentAt(x,z,severity,[a,b]);}`
- [x] `return` — linha 3462, coluna 172 em `createTrafficIncident()` — `function createTrafficIncident(a,b,severity='minor'){if(!a||!b)return null;const x=(a.group.position.x+b.group.position.x)/2,z=(a.group.position.z+b.group.position.z)/2;return createTrafficIncidentAt(x,z,severity,[a,b]);}`
- [x] `if` — linha 3463, coluna 53 em `ensureActiveTrafficIncident()` — `function ensureActiveTrafficIncident(force=false){if(world.activeIncident)return world.activeIncident;if(!force)return null;const site=TRAINING_INCIDENT_SITES[world.emergencySeq%TRAINING_INCIDENT_SITES.length],projection=nearestRoadProjection(site)?.point||site;return createTrafficIncidentAt(projection.x,projection.z,'minor',[]);}`
- [x] `return` — linha 3463, coluna 77 em `ensureActiveTrafficIncident()` — `function ensureActiveTrafficIncident(force=false){if(world.activeIncident)return world.activeIncident;if(!force)return null;const site=TRAINING_INCIDENT_SITES[world.emergencySeq%TRAINING_INCIDENT_SITES.length],projection=nearestRoadProjection(site)?.point||site;return createTrafficIncidentAt(projection.x,projection.z,'minor',[]);}`
- [x] `if` — linha 3463, coluna 105 em `ensureActiveTrafficIncident()` — `function ensureActiveTrafficIncident(force=false){if(world.activeIncident)return world.activeIncident;if(!force)return null;const site=TRAINING_INCIDENT_SITES[world.emergencySeq%TRAINING_INCIDENT_SITES.length],projection=nearestRoadProjection(site)?.point||site;return createTrafficIncidentAt(projection.x,projection.z,'minor',[]);}`
- [x] `return` — linha 3463, coluna 115 em `ensureActiveTrafficIncident()` — `function ensureActiveTrafficIncident(force=false){if(world.activeIncident)return world.activeIncident;if(!force)return null;const site=TRAINING_INCIDENT_SITES[world.emergencySeq%TRAINING_INCIDENT_SITES.length],projection=nearestRoadProjection(site)?.point||site;return createTrafficIncidentAt(projection.x,projection.z,'minor',[]);}`
- [x] `return` — linha 3463, coluna 265 em `ensureActiveTrafficIncident()` — `function ensureActiveTrafficIncident(force=false){if(world.activeIncident)return world.activeIncident;if(!force)return null;const site=TRAINING_INCIDENT_SITES[world.emergencySeq%TRAINING_INCIDENT_SITES.length],projection=nearestRoadProjection(site)?.point||site;return createTrafficIncidentAt(projection.x,projection.z,'minor',[]);}`
- [x] `if` — linha 3465, coluna 39 em `dispatchIncidentResponder()` — `const job=state.career?.activeJob;if(job&&serviceKindForJob(job)===kind){incident.playerAssigned[kind]=true;job.emergencyIncidentId=incident.id;job.serviceVehicleArrived=false;job.sceneTaskCompleted=false;reserveMissionServiceVehicle(job,{waypoint:true});return null;}`
- [x] `return` — linha 3465, coluna 260 em `dispatchIncidentResponder()` — `const job=state.career?.activeJob;if(job&&serviceKindForJob(job)===kind){incident.playerAssigned[kind]=true;job.emergencyIncidentId=incident.id;job.serviceVehicleArrived=false;job.sceneTaskCompleted=false;reserveMissionServiceVehicle(job,{waypoint:true});return null;}`
- [x] `if` — linha 3466, coluna 86 em `dispatchIncidentResponder()` — `const vehicle=serviceVehiclePools(kind).find(v=>serviceVehicleAvailable(v,null));if(!vehicle)return null;vehicle.incidentTargetId=incident.id;vehicle.responseRoute=[];vehicle.responseIndex=0;incident.responders[kind]=vehicle.id;return vehicle;`
- [x] `return` — linha 3466, coluna 98 em `dispatchIncidentResponder()` — `const vehicle=serviceVehiclePools(kind).find(v=>serviceVehicleAvailable(v,null));if(!vehicle)return null;vehicle.incidentTargetId=incident.id;vehicle.responseRoute=[];vehicle.responseIndex=0;incident.responders[kind]=vehicle.id;return vehicle;`
- [x] `return` — linha 3466, coluna 233 em `dispatchIncidentResponder()` — `const vehicle=serviceVehiclePools(kind).find(v=>serviceVehicleAvailable(v,null));if(!vehicle)return null;vehicle.incidentTargetId=incident.id;vehicle.responseRoute=[];vehicle.responseIndex=0;incident.responders[kind]=vehicle.id;return vehicle;`
- [x] `if` — linha 3470, coluna 5 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `return` — linha 3470, coluna 118 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `if` — linha 3470, coluna 194 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `return` — linha 3470, coluna 330 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `if` — linha 3470, coluna 344 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `return` — linha 3470, coluna 533 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `if` — linha 3470, coluna 547 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `return` — linha 3470, coluna 645 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `if` — linha 3470, coluna 659 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `return` — linha 3470, coluna 767 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `for` — linha 3470, coluna 936 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `return` — linha 3470, coluna 1381 em `assistTrafficIncident()` — `if(!incident||incident.resolved||world.activeIncident!==incident){toast('A ocorrência já foi encerrada.','good');return false;}const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!kind||job.emergencyIncidentId!==incident.id){toast('A área está isolada. Aceite uma missão de emergência para ajudar.','warn',2400);return false;}if(!job.serviceVehicleArrived){toast(\`Chegue com ${kind==='police'?'a viatura':kind==='firefighter'?'o caminhão dos bombeiros':'a ambulância'} antes de iniciar o atendimento.\`,'warn',2500);return false;}if(player.vehicle){toast('Estacione, saia do veículo e use AÇÃO junto à ocorrência.','warn',2200);return false;}if(Math.hypot(player.x-incident.x,player.z-incident.z)>5.5){toast('Aproxime-se da área sinalizada.','warn');return false;}incident.tasks[kind]=true;job.sceneTaskCompleted=true;setMissionState(job,MISSION_STATES.RETURNING,'scene-secured');player.sitUntil=performance.now()+1800;for(let i=0;i<7;i++)setTimeout(()=>spawnDust(incident.x+(Math.random(`
- [x] `if` — linha 3472, coluna 110 em `markPlayerIncidentArrival()` — `function markPlayerIncidentArrival(incident){const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!job||!kind||job.emergencyIncidentId!==incident.id||!isDrivingServiceVehicle(kind))return false;const distance=Math.hypot(player.x-incident.navX,player.z-incident.navZ);if(distance>5.2)return false;if(!incident.arrived[kind]){incident.arrived[kind]=true;job.serviceVehicleArrived=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'assist-incident');state.waypoint={id:\`assist-${incident.id}\`,name:'Área do acidente',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,arrived:true};updateWaypointMarker();updateNavigation(0,true);toast('Veículo posicionado. Estacione, saia e use AÇÃO na área sinalizada.','good',3000);saveState(true);}return true;}`
- [x] `return` — linha 3472, coluna 196 em `markPlayerIncidentArrival()` — `function markPlayerIncidentArrival(incident){const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!job||!kind||job.emergencyIncidentId!==incident.id||!isDrivingServiceVehicle(kind))return false;const distance=Math.hypot(player.x-incident.navX,player.z-incident.navZ);if(distance>5.2)return false;if(!incident.arrived[kind]){incident.arrived[kind]=true;job.serviceVehicleArrived=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'assist-incident');state.waypoint={id:\`assist-${incident.id}\`,name:'Área do acidente',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,arrived:true};updateWaypointMarker();updateNavigation(0,true);toast('Veículo posicionado. Estacione, saia e use AÇÃO na área sinalizada.','good',3000);saveState(true);}return true;}`
- [x] `if` — linha 3472, coluna 282 em `markPlayerIncidentArrival()` — `function markPlayerIncidentArrival(incident){const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!job||!kind||job.emergencyIncidentId!==incident.id||!isDrivingServiceVehicle(kind))return false;const distance=Math.hypot(player.x-incident.navX,player.z-incident.navZ);if(distance>5.2)return false;if(!incident.arrived[kind]){incident.arrived[kind]=true;job.serviceVehicleArrived=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'assist-incident');state.waypoint={id:\`assist-${incident.id}\`,name:'Área do acidente',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,arrived:true};updateWaypointMarker();updateNavigation(0,true);toast('Veículo posicionado. Estacione, saia e use AÇÃO na área sinalizada.','good',3000);saveState(true);}return true;}`
- [x] `return` — linha 3472, coluna 298 em `markPlayerIncidentArrival()` — `function markPlayerIncidentArrival(incident){const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!job||!kind||job.emergencyIncidentId!==incident.id||!isDrivingServiceVehicle(kind))return false;const distance=Math.hypot(player.x-incident.navX,player.z-incident.navZ);if(distance>5.2)return false;if(!incident.arrived[kind]){incident.arrived[kind]=true;job.serviceVehicleArrived=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'assist-incident');state.waypoint={id:\`assist-${incident.id}\`,name:'Área do acidente',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,arrived:true};updateWaypointMarker();updateNavigation(0,true);toast('Veículo posicionado. Estacione, saia e use AÇÃO na área sinalizada.','good',3000);saveState(true);}return true;}`
- [x] `if` — linha 3472, coluna 311 em `markPlayerIncidentArrival()` — `function markPlayerIncidentArrival(incident){const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!job||!kind||job.emergencyIncidentId!==incident.id||!isDrivingServiceVehicle(kind))return false;const distance=Math.hypot(player.x-incident.navX,player.z-incident.navZ);if(distance>5.2)return false;if(!incident.arrived[kind]){incident.arrived[kind]=true;job.serviceVehicleArrived=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'assist-incident');state.waypoint={id:\`assist-${incident.id}\`,name:'Área do acidente',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,arrived:true};updateWaypointMarker();updateNavigation(0,true);toast('Veículo posicionado. Estacione, saia e use AÇÃO na área sinalizada.','good',3000);saveState(true);}return true;}`
- [x] `return` — linha 3472, coluna 767 em `markPlayerIncidentArrival()` — `function markPlayerIncidentArrival(incident){const job=state.career?.activeJob,kind=serviceKindForJob(job);if(!job||!kind||job.emergencyIncidentId!==incident.id||!isDrivingServiceVehicle(kind))return false;const distance=Math.hypot(player.x-incident.navX,player.z-incident.navZ);if(distance>5.2)return false;if(!incident.arrived[kind]){incident.arrived[kind]=true;job.serviceVehicleArrived=true;setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'assist-incident');state.waypoint={id:\`assist-${incident.id}\`,name:'Área do acidente',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,arrived:true};updateWaypointMarker();updateNavigation(0,true);toast('Veículo posicionado. Estacione, saia e use AÇÃO na área sinalizada.','good',3000);saveState(true);}return true;}`
- [x] `for` — linha 3473, coluna 49 em `releaseIncidentActorStates()` — `function releaseIncidentActorStates(incident){for(const saved of incident.actorStates||[]){const ref=saved.ref;if(!ref)continue;ref.incidentLocked=false;ref.incidentUntil=0;ref.currentSpeed=0;if(saved.state===BUS_STATES.ACCIDENT||ref.state===BUS_STATES.ACCIDENT)setBusState(ref,BUS_STATES.MOVING,'incident-cleared');}}`
- [x] `if` — linha 3473, coluna 114 em `releaseIncidentActorStates()` — `function releaseIncidentActorStates(incident){for(const saved of incident.actorStates||[]){const ref=saved.ref;if(!ref)continue;ref.incidentLocked=false;ref.incidentUntil=0;ref.currentSpeed=0;if(saved.state===BUS_STATES.ACCIDENT||ref.state===BUS_STATES.ACCIDENT)setBusState(ref,BUS_STATES.MOVING,'incident-cleared');}}`
- [x] `if` — linha 3473, coluna 195 em `releaseIncidentActorStates()` — `function releaseIncidentActorStates(incident){for(const saved of incident.actorStates||[]){const ref=saved.ref;if(!ref)continue;ref.incidentLocked=false;ref.incidentUntil=0;ref.currentSpeed=0;if(saved.state===BUS_STATES.ACCIDENT||ref.state===BUS_STATES.ACCIDENT)setBusState(ref,BUS_STATES.MOVING,'incident-cleared');}}`
- [x] `if` — linha 3475, coluna 5 em `resolveTrafficIncident()` — `if(!incident||incident.resolved)return;incident.resolved=true;incident.group.visible=false;releaseIncidentActorStates(incident);for(const car of world.policeCars){if(car.incidentTargetId===incident.id){car.incidentTargetId='';car.responseRoute=[];car.responseIndex=0;}}for(const ambulance of world.ambulances){if(ambulance.incidentTargetId===incident.id){ambulance.incidentTargetId='';ambulance.responseRoute=[];ambulance.responseIndex=0;}}for(const truck of world.fireTrucks){if(truck.incidentTargetId===incident.id){truck.incidentTargetId='';truck.responseRoute=[];truck.responseIndex=0;}}`
- [x] `return` — linha 3475, coluna 37 em `resolveTrafficIncident()` — `if(!incident||incident.resolved)return;incident.resolved=true;incident.group.visible=false;releaseIncidentActorStates(incident);for(const car of world.policeCars){if(car.incidentTargetId===incident.id){car.incidentTargetId='';car.responseRoute=[];car.responseIndex=0;}}for(const ambulance of world.ambulances){if(ambulance.incidentTargetId===incident.id){ambulance.incidentTargetId='';ambulance.responseRoute=[];ambulance.responseIndex=0;}}for(const truck of world.fireTrucks){if(truck.incidentTargetId===incident.id){truck.incidentTargetId='';truck.responseRoute=[];truck.responseIndex=0;}}`
- [x] `for` — linha 3475, coluna 133 em `resolveTrafficIncident()` — `if(!incident||incident.resolved)return;incident.resolved=true;incident.group.visible=false;releaseIncidentActorStates(incident);for(const car of world.policeCars){if(car.incidentTargetId===incident.id){car.incidentTargetId='';car.responseRoute=[];car.responseIndex=0;}}for(const ambulance of world.ambulances){if(ambulance.incidentTargetId===incident.id){ambulance.incidentTargetId='';ambulance.responseRoute=[];ambulance.responseIndex=0;}}for(const truck of world.fireTrucks){if(truck.incidentTargetId===incident.id){truck.incidentTargetId='';truck.responseRoute=[];truck.responseIndex=0;}}`
- [x] `if` — linha 3475, coluna 168 em `resolveTrafficIncident()` — `if(!incident||incident.resolved)return;incident.resolved=true;incident.group.visible=false;releaseIncidentActorStates(incident);for(const car of world.policeCars){if(car.incidentTargetId===incident.id){car.incidentTargetId='';car.responseRoute=[];car.responseIndex=0;}}for(const ambulance of world.ambulances){if(ambulance.incidentTargetId===incident.id){ambulance.incidentTargetId='';ambulance.responseRoute=[];ambulance.responseIndex=0;}}for(const truck of world.fireTrucks){if(truck.incidentTargetId===incident.id){truck.incidentTargetId='';truck.responseRoute=[];truck.responseIndex=0;}}`
- [x] `for` — linha 3475, coluna 274 em `resolveTrafficIncident()` — `if(!incident||incident.resolved)return;incident.resolved=true;incident.group.visible=false;releaseIncidentActorStates(incident);for(const car of world.policeCars){if(car.incidentTargetId===incident.id){car.incidentTargetId='';car.responseRoute=[];car.responseIndex=0;}}for(const ambulance of world.ambulances){if(ambulance.incidentTargetId===incident.id){ambulance.incidentTargetId='';ambulance.responseRoute=[];ambulance.responseIndex=0;}}for(const truck of world.fireTrucks){if(truck.incidentTargetId===incident.id){truck.incidentTargetId='';truck.responseRoute=[];truck.responseIndex=0;}}`
- [x] `if` — linha 3475, coluna 315 em `resolveTrafficIncident()` — `if(!incident||incident.resolved)return;incident.resolved=true;incident.group.visible=false;releaseIncidentActorStates(incident);for(const car of world.policeCars){if(car.incidentTargetId===incident.id){car.incidentTargetId='';car.responseRoute=[];car.responseIndex=0;}}for(const ambulance of world.ambulances){if(ambulance.incidentTargetId===incident.id){ambulance.incidentTargetId='';ambulance.responseRoute=[];ambulance.responseIndex=0;}}for(const truck of world.fireTrucks){if(truck.incidentTargetId===incident.id){truck.incidentTargetId='';truck.responseRoute=[];truck.responseIndex=0;}}`
- [x] `for` — linha 3475, coluna 445 em `resolveTrafficIncident()` — `if(!incident||incident.resolved)return;incident.resolved=true;incident.group.visible=false;releaseIncidentActorStates(incident);for(const car of world.policeCars){if(car.incidentTargetId===incident.id){car.incidentTargetId='';car.responseRoute=[];car.responseIndex=0;}}for(const ambulance of world.ambulances){if(ambulance.incidentTargetId===incident.id){ambulance.incidentTargetId='';ambulance.responseRoute=[];ambulance.responseIndex=0;}}for(const truck of world.fireTrucks){if(truck.incidentTargetId===incident.id){truck.incidentTargetId='';truck.responseRoute=[];truck.responseIndex=0;}}`
- [x] `if` — linha 3475, coluna 482 em `resolveTrafficIncident()` — `if(!incident||incident.resolved)return;incident.resolved=true;incident.group.visible=false;releaseIncidentActorStates(incident);for(const car of world.policeCars){if(car.incidentTargetId===incident.id){car.incidentTargetId='';car.responseRoute=[];car.responseIndex=0;}}for(const ambulance of world.ambulances){if(ambulance.incidentTargetId===incident.id){ambulance.incidentTargetId='';ambulance.responseRoute=[];ambulance.responseIndex=0;}}for(const truck of world.fireTrucks){if(truck.incidentTargetId===incident.id){truck.incidentTargetId='';truck.responseRoute=[];truck.responseIndex=0;}}`
- [x] `if` — linha 3476, coluna 67 em `resolveTrafficIncident()` — `const job=state.career?.activeJob,kind=serviceKindForJob(job);if(job&&job.emergencyIncidentId===incident.id&&incident.tasks[kind])completeActiveJob();`
- [x] `for` — linha 3480, coluna 63 em `updateTrafficIncidents()` — `const now=performance.now(),incident=world.activeIncident;for(const ambulance of world.ambulances){const responding=!!ambulance.incidentTargetId;ambulance.lightA.visible=responding&&Math.floor(now/220)%2===0;ambulance.lightB.visible=responding&&!ambulance.lightA.visible;if(!incident&&!ambulance.occupied&&!ambulance.missionReservedFor)returnServiceVehicleHome(ambulance,dt);}`
- [x] `if` — linha 3480, coluna 276 em `updateTrafficIncidents()` — `const now=performance.now(),incident=world.activeIncident;for(const ambulance of world.ambulances){const responding=!!ambulance.incidentTargetId;ambulance.lightA.visible=responding&&Math.floor(now/220)%2===0;ambulance.lightB.visible=responding&&!ambulance.lightA.visible;if(!incident&&!ambulance.occupied&&!ambulance.missionReservedFor)returnServiceVehicleHome(ambulance,dt);}`
- [x] `if` — linha 3481, coluna 5 em `updateTrafficIncidents()` — `if(incident){`
- [x] `for` — linha 3483, coluna 7 em `updateTrafficIncidents()` — `for(const car of world.policeCars.filter(c=>c.incidentTargetId===incident.id&&!c.occupied)){if(moveIncidentResponder(car,target,dt)<3.7){incident.arrived.police=true;if(!incident.playerAssigned.police&&now-incident.startedAt>1800)incident.tasks.police=true;}}`
- [x] `if` — linha 3483, coluna 99 em `updateTrafficIncidents()` — `for(const car of world.policeCars.filter(c=>c.incidentTargetId===incident.id&&!c.occupied)){if(moveIncidentResponder(car,target,dt)<3.7){incident.arrived.police=true;if(!incident.playerAssigned.police&&now-incident.startedAt>1800)incident.tasks.police=true;}}`
- [x] `if` — linha 3483, coluna 173 em `updateTrafficIncidents()` — `for(const car of world.policeCars.filter(c=>c.incidentTargetId===incident.id&&!c.occupied)){if(moveIncidentResponder(car,target,dt)<3.7){incident.arrived.police=true;if(!incident.playerAssigned.police&&now-incident.startedAt>1800)incident.tasks.police=true;}}`
- [x] `for` — linha 3484, coluna 7 em `updateTrafficIncidents()` — `for(const ambulance of world.ambulances.filter(c=>c.incidentTargetId===incident.id&&!c.occupied)){if(moveIncidentResponder(ambulance,target,dt)<3.9){incident.arrived.paramedic=true;if(!incident.playerAssigned.paramedic&&now-incident.startedAt>1800)incident.tasks.paramedic=true;}}`
- [x] `if` — linha 3484, coluna 105 em `updateTrafficIncidents()` — `for(const ambulance of world.ambulances.filter(c=>c.incidentTargetId===incident.id&&!c.occupied)){if(moveIncidentResponder(ambulance,target,dt)<3.9){incident.arrived.paramedic=true;if(!incident.playerAssigned.paramedic&&now-incident.startedAt>1800)incident.tasks.paramedic=true;}}`
- [x] `if` — linha 3484, coluna 188 em `updateTrafficIncidents()` — `for(const ambulance of world.ambulances.filter(c=>c.incidentTargetId===incident.id&&!c.occupied)){if(moveIncidentResponder(ambulance,target,dt)<3.9){incident.arrived.paramedic=true;if(!incident.playerAssigned.paramedic&&now-incident.startedAt>1800)incident.tasks.paramedic=true;}}`
- [x] `for` — linha 3485, coluna 7 em `updateTrafficIncidents()` — `for(const truck of world.fireTrucks.filter(c=>c.incidentTargetId===incident.id&&!c.occupied)){if(moveIncidentResponder(truck,target,dt)<4.4){incident.arrived.firefighter=true;if(!incident.playerAssigned.firefighter&&now-incident.startedAt>1800)incident.tasks.firefighter=true;}}`
- [x] `if` — linha 3485, coluna 101 em `updateTrafficIncidents()` — `for(const truck of world.fireTrucks.filter(c=>c.incidentTargetId===incident.id&&!c.occupied)){if(moveIncidentResponder(truck,target,dt)<4.4){incident.arrived.firefighter=true;if(!incident.playerAssigned.firefighter&&now-incident.startedAt>1800)incident.tasks.firefighter=true;}}`
- [x] `if` — linha 3485, coluna 182 em `updateTrafficIncidents()` — `for(const truck of world.fireTrucks.filter(c=>c.incidentTargetId===incident.id&&!c.occupied)){if(moveIncidentResponder(truck,target,dt)<4.4){incident.arrived.firefighter=true;if(!incident.playerAssigned.firefighter&&now-incident.startedAt>1800)incident.tasks.firefighter=true;}}`
- [x] `if` — linha 3486, coluna 167 em `updateTrafficIncidents()` — `const required=Object.keys(incident.required).filter(kind=>incident.required[kind]),complete=required.every(kind=>incident.arrived[kind]&&incident.tasks[kind]);if(complete){incident.readyAt=incident.readyAt||now;if(now-incident.readyAt>1900)resolveTrafficIncident(incident);}else incident.readyAt=0;return;`
- [x] `if` — linha 3486, coluna 219 em `updateTrafficIncidents()` — `const required=Object.keys(incident.required).filter(kind=>incident.required[kind]),complete=required.every(kind=>incident.arrived[kind]&&incident.tasks[kind]);if(complete){incident.readyAt=incident.readyAt||now;if(now-incident.readyAt>1900)resolveTrafficIncident(incident);}else incident.readyAt=0;return;`
- [x] `else` — linha 3486, coluna 282 em `updateTrafficIncidents()` — `const required=Object.keys(incident.required).filter(kind=>incident.required[kind]),complete=required.every(kind=>incident.arrived[kind]&&incident.tasks[kind]);if(complete){incident.readyAt=incident.readyAt||now;if(now-incident.readyAt>1900)resolveTrafficIncident(incident);}else incident.readyAt=0;return;`
- [x] `return` — linha 3486, coluna 306 em `updateTrafficIncidents()` — `const required=Object.keys(incident.required).filter(kind=>incident.required[kind]),complete=required.every(kind=>incident.arrived[kind]&&incident.tasks[kind]);if(complete){incident.readyAt=incident.readyAt||now;if(now-incident.readyAt>1900)resolveTrafficIncident(incident);}else incident.readyAt=0;return;`
- [x] `if` — linha 3488, coluna 67 em `updateTrafficIncidents()` — `updateTrafficIncidents.acc=(updateTrafficIncidents.acc||0)+dt;if(updateTrafficIncidents.acc<.18)return;updateTrafficIncidents.acc=0;const actors=trafficActorList().filter(a=>a.speed>1.4&&!a.ref?.incidentUntil&&!a.ref?.incidentLocked);if(player.vehicle&&!player.car.passengerOf&&Math.abs(player.car.speed)>1.4)actors.push({id:'player-car',type:'player',group:{position:{x:player.x,z:player.z}},radius:1.7,speed:Math.abs(player.car.speed),ref:player.car,isPlayer:true});`
- [x] `return` — linha 3488, coluna 101 em `updateTrafficIncidents()` — `updateTrafficIncidents.acc=(updateTrafficIncidents.acc||0)+dt;if(updateTrafficIncidents.acc<.18)return;updateTrafficIncidents.acc=0;const actors=trafficActorList().filter(a=>a.speed>1.4&&!a.ref?.incidentUntil&&!a.ref?.incidentLocked);if(player.vehicle&&!player.car.passengerOf&&Math.abs(player.car.speed)>1.4)actors.push({id:'player-car',type:'player',group:{position:{x:player.x,z:player.z}},radius:1.7,speed:Math.abs(player.car.speed),ref:player.car,isPlayer:true});`
- [x] `if` — linha 3488, coluna 239 em `updateTrafficIncidents()` — `updateTrafficIncidents.acc=(updateTrafficIncidents.acc||0)+dt;if(updateTrafficIncidents.acc<.18)return;updateTrafficIncidents.acc=0;const actors=trafficActorList().filter(a=>a.speed>1.4&&!a.ref?.incidentUntil&&!a.ref?.incidentLocked);if(player.vehicle&&!player.car.passengerOf&&Math.abs(player.car.speed)>1.4)actors.push({id:'player-car',type:'player',group:{position:{x:player.x,z:player.z}},radius:1.7,speed:Math.abs(player.car.speed),ref:player.car,isPlayer:true});`
- [x] `for` — linha 3489, coluna 5 em `updateTrafficIncidents()` — `for(let i=0;i<actors.length;i++)for(let j=i+1;j<actors.length;j++){const a=actors[i],b=actors[j],distance=Math.hypot(a.group.position.x-b.group.position.x,a.group.position.z-b.group.position.z),limit=(a.radius+b.radius)*.62;if(distance<limit&&a.speed+b.speed>5.4){createTrafficIncident(a,b,a.speed+b.speed>13&&Math.random()<.35?'fire':'minor');return;}}`
- [x] `for` — linha 3489, coluna 37 em `updateTrafficIncidents()` — `for(let i=0;i<actors.length;i++)for(let j=i+1;j<actors.length;j++){const a=actors[i],b=actors[j],distance=Math.hypot(a.group.position.x-b.group.position.x,a.group.position.z-b.group.position.z),limit=(a.radius+b.radius)*.62;if(distance<limit&&a.speed+b.speed>5.4){createTrafficIncident(a,b,a.speed+b.speed>13&&Math.random()<.35?'fire':'minor');return;}}`
- [x] `if` — linha 3489, coluna 229 em `updateTrafficIncidents()` — `for(let i=0;i<actors.length;i++)for(let j=i+1;j<actors.length;j++){const a=actors[i],b=actors[j],distance=Math.hypot(a.group.position.x-b.group.position.x,a.group.position.z-b.group.position.z),limit=(a.radius+b.radius)*.62;if(distance<limit&&a.speed+b.speed>5.4){createTrafficIncident(a,b,a.speed+b.speed>13&&Math.random()<.35?'fire':'minor');return;}}`
- [x] `return` — linha 3489, coluna 349 em `updateTrafficIncidents()` — `for(let i=0;i<actors.length;i++)for(let j=i+1;j<actors.length;j++){const a=actors[i],b=actors[j],distance=Math.hypot(a.group.position.x-b.group.position.x,a.group.position.z-b.group.position.z),limit=(a.radius+b.radius)*.62;if(distance<limit&&a.speed+b.speed>5.4){createTrafficIncident(a,b,a.speed+b.speed>13&&Math.random()<.35?'fire':'minor');return;}}`

## `17-adventures-learning-world.js`

- [x] `for` — linha 3499, coluna 197 em `createRoyalCastle()` — `const moatA=premiumBox(8.2,.18,2.5,materials.water,-8.1,.08,-13.8,g),moatB=premiumBox(8.2,.18,2.5,materials.water,8.1,.08,-13.8,g),moatBack=premiumBox(27,.18,2.2,materials.water,0,.08,13.5,g);for(const water of [moatA,moatB,moatBack])water.material.depthWrite=false;`
- [x] `for` — linha 3503, coluna 5 em `createRoyalCastle()` — `for(const [tx,tz] of [[-13,-10],[13,-10],[-13,10],[13,10]]){premiumCylinder(3.6,10,stone,tx,5.5,tz,g,12);premiumCylinder(4,1.1,trim,tx,10.2,tz,g,12);const cone=new THREE.Mesh(new THREE.ConeGeometry(4.25,4.8,12),renderMat(roof,{roughness:.55,metalness:.08}));cone.position.set(tx,13.05,tz);cone.castShadow=true;g.add(cone);premiumBox(.16,2,.16,gold,tx,16.2,tz,g);const flag=premiumBox(1.8,.8,.08,0xe43d4c,tx+.9,16.7,tz,g);flag.rotation.y=.15;}`
- [x] `for` — linha 3504, coluna 45 em `createRoyalCastle()` — `const battlement=(px,pz,alongX,count)=>{for(let i=0;i<count;i++){const offset=(i-(count-1)/2)*2.15;premiumBox(1.25,1.15,1.25,trim,px+(alongX?offset:0),7.75,pz+(alongX?0:offset),g);}};battlement(-8.3,-10,true,4);battlement(8.3,-10,true,4);battlement(0,10,true,11);battlement(-13.5,0,false,7);battlement(13.5,0,false,7);`
- [x] `for` — linha 3505, coluna 5 em `createRoyalCastle()` — `for(const wx of [-4.8,0,4.8]){premiumBox(1.2,1.9,.18,0x5ee7ff,wx,6.7,-1.05,g);premiumBox(.12,1.9,.2,trim,wx,6.7,-1.15,g);}premiumBox(5.2,.35,5.8,materials.wood,0,.34,-12.3,g);`
- [x] `for` — linha 3506, coluna 5 em `createRoyalCastle()` — `for(const px of [-9,-5,5,9]){premiumCylinder(.48,.56,0xc88a45,px,1.1,4.8,g,10);premiumBox(.82,.3,.82,px<0?0xff6fa8:0xffd34d,px,1.55,4.8,g);}`
- [x] `for` — linha 3507, coluna 5 em `createRoyalCastle()` — `for(const a of [-1,1]){const chain=premiumCylinder(.08,5.5,0x333b45,a*2.2,2.8,-11.4,g,8);chain.rotation.x=Math.PI/2;chain.rotation.z=.1*a;}const crown=new THREE.Mesh(new THREE.OctahedronGeometry(.85,0),mat(gold,{emissive:0xb87900,emissiveIntensity:.7}));crown.position.set(0,10.7,2.5);g.add(crown);addGlow(x,10.7,z+2.5,gold,5);`
- [x] `for` — linha 3511, coluna 5 em `createRoyalCastle()` — `for(const px of [-6.6,-3.3,0,3.3,6.6]){premiumBox(1.8,3.0,.12,px===0?0xffd34d:0x315aa8,px,3.15,6.14,hall);premiumBox(.15,3.0,.18,0xe7e9ee,px,3.15,6.05,hall);}`
- [x] `for` — linha 3513, coluna 5 em `createRoyalCastle()` — `for(const side of [-1,1]){premiumBox(5.4,.12,1.3,0xcaa76b,side*4.8,.62,-.45,hall);for(const px of [-1.8,0,1.8])premiumBox(.18,.55,.18,0x70452e,side*4.8+px,.3,-.45,hall);}`
- [x] `for` — linha 3513, coluna 87 em `createRoyalCastle()` — `for(const side of [-1,1]){premiumBox(5.4,.12,1.3,0xcaa76b,side*4.8,.62,-.45,hall);for(const px of [-1.8,0,1.8])premiumBox(.18,.55,.18,0x70452e,side*4.8+px,.3,-.45,hall);}`
- [x] `for` — linha 3514, coluna 5 em `createRoyalCastle()` — `for(const [px,pz,c] of [[-6.4,-4.6,0x315aa8],[6.4,-4.6,0xe43d4c],[-6.4,2.2,0xe43d4c],[6.4,2.2,0x315aa8]]){premiumCylinder(.34,2.6,0xd6b866,px,1.3,pz,hall,12);const flame=new THREE.Mesh(new THREE.ConeGeometry(.38,.85,10),renderMat(c,{emissive:c,emissiveIntensity:.75,roughness:.32}));flame.position.set(px,3,pz);hall.add(flame);}`
- [x] `return` — linha 3521, coluna 240 em `createRoyalCastle()` — `registerInteractable({id:'castle-adventures',type:'adventure',icon:'🏰',label:'Desafios do Castelo',x,z:z-11.5,radius:5.2,priority:195,action:openAdventureHub});world.castle=g;world.castleHouse=castleHouse;createCastleChallengeTokens();return g;`
- [x] `return` — linha 3524, coluna 334 em `createCastleChallengeTokens()` — `const coords=[[-10,-7],[10,-7],[-10,7],[10,7],[0,5],[-6,1]];world.challengeTokens=coords.map(([dx,dz],i)=>{const mesh=new THREE.Mesh(new THREE.OctahedronGeometry(.62,0),mat(0xffd34d,{emissive:0xc68400,emissiveIntensity:1.1,metalness:.22,roughness:.25}));mesh.position.set(88+dx,1.5,62+dz);mesh.visible=false;worldGroup.add(mesh);return{id:\`crown-${i}\`,x:88+dx,z:62+dz,mesh,got:false};});`
- [x] `if` — linha 3530, coluna 36 em `startAdventure()` — `const def=ADVENTURE_DEFS[type];if(!def||world.activeChallenge||activeRace)return false;if(type==='castle'&&Math.hypot(player.x-88,player.z-52)>15){setWaypoint('castle');toast('Siga a rota até o Castelo para começar.','good',2200);return false;}closeModal();const startedAt=Date.now(),endsAt=type==='castle'?startedAt+75000:0;world.activeChallenge={type,target:def.target,progress:new Set(),startedAt:performance.now(),endsAt:type==='castle'?performance.now()+75000:0};state.adventures.active={type,startedAt,endsAt,progress:[]};if(type==='castle')for(const token of world.challengeTokens){token.got=false;token.mesh.visible=true;}els.raceBadge.hidden=false;els.raceTitle.textContent=\`${def.icon} ${def.name}\`;els.raceStatus.textContent=\`0/${def.target}\`;toast(\`${def.name} começou!\`,'good',1900);saveState(true);return true;`
- [x] `return` — linha 3530, coluna 79 em `startAdventure()` — `const def=ADVENTURE_DEFS[type];if(!def||world.activeChallenge||activeRace)return false;if(type==='castle'&&Math.hypot(player.x-88,player.z-52)>15){setWaypoint('castle');toast('Siga a rota até o Castelo para começar.','good',2200);return false;}closeModal();const startedAt=Date.now(),endsAt=type==='castle'?startedAt+75000:0;world.activeChallenge={type,target:def.target,progress:new Set(),startedAt:performance.now(),endsAt:type==='castle'?performance.now()+75000:0};state.adventures.active={type,startedAt,endsAt,progress:[]};if(type==='castle')for(const token of world.challengeTokens){token.got=false;token.mesh.visible=true;}els.raceBadge.hidden=false;els.raceTitle.textContent=\`${def.icon} ${def.name}\`;els.raceStatus.textContent=\`0/${def.target}\`;toast(\`${def.name} começou!\`,'good',1900);saveState(true);return true;`
- [x] `if` — linha 3530, coluna 92 em `startAdventure()` — `const def=ADVENTURE_DEFS[type];if(!def||world.activeChallenge||activeRace)return false;if(type==='castle'&&Math.hypot(player.x-88,player.z-52)>15){setWaypoint('castle');toast('Siga a rota até o Castelo para começar.','good',2200);return false;}closeModal();const startedAt=Date.now(),endsAt=type==='castle'?startedAt+75000:0;world.activeChallenge={type,target:def.target,progress:new Set(),startedAt:performance.now(),endsAt:type==='castle'?performance.now()+75000:0};state.adventures.active={type,startedAt,endsAt,progress:[]};if(type==='castle')for(const token of world.challengeTokens){token.got=false;token.mesh.visible=true;}els.raceBadge.hidden=false;els.raceTitle.textContent=\`${def.icon} ${def.name}\`;els.raceStatus.textContent=\`0/${def.target}\`;toast(\`${def.name} começou!\`,'good',1900);saveState(true);return true;`
- [x] `return` — linha 3530, coluna 235 em `startAdventure()` — `const def=ADVENTURE_DEFS[type];if(!def||world.activeChallenge||activeRace)return false;if(type==='castle'&&Math.hypot(player.x-88,player.z-52)>15){setWaypoint('castle');toast('Siga a rota até o Castelo para começar.','good',2200);return false;}closeModal();const startedAt=Date.now(),endsAt=type==='castle'?startedAt+75000:0;world.activeChallenge={type,target:def.target,progress:new Set(),startedAt:performance.now(),endsAt:type==='castle'?performance.now()+75000:0};state.adventures.active={type,startedAt,endsAt,progress:[]};if(type==='castle')for(const token of world.challengeTokens){token.got=false;token.mesh.visible=true;}els.raceBadge.hidden=false;els.raceTitle.textContent=\`${def.icon} ${def.name}\`;els.raceStatus.textContent=\`0/${def.target}\`;toast(\`${def.name} começou!\`,'good',1900);saveState(true);return true;`
- [x] `if` — linha 3530, coluna 533 em `startAdventure()` — `const def=ADVENTURE_DEFS[type];if(!def||world.activeChallenge||activeRace)return false;if(type==='castle'&&Math.hypot(player.x-88,player.z-52)>15){setWaypoint('castle');toast('Siga a rota até o Castelo para começar.','good',2200);return false;}closeModal();const startedAt=Date.now(),endsAt=type==='castle'?startedAt+75000:0;world.activeChallenge={type,target:def.target,progress:new Set(),startedAt:performance.now(),endsAt:type==='castle'?performance.now()+75000:0};state.adventures.active={type,startedAt,endsAt,progress:[]};if(type==='castle')for(const token of world.challengeTokens){token.got=false;token.mesh.visible=true;}els.raceBadge.hidden=false;els.raceTitle.textContent=\`${def.icon} ${def.name}\`;els.raceStatus.textContent=\`0/${def.target}\`;toast(\`${def.name} começou!\`,'good',1900);saveState(true);return true;`
- [x] `for` — linha 3530, coluna 552 em `startAdventure()` — `const def=ADVENTURE_DEFS[type];if(!def||world.activeChallenge||activeRace)return false;if(type==='castle'&&Math.hypot(player.x-88,player.z-52)>15){setWaypoint('castle');toast('Siga a rota até o Castelo para começar.','good',2200);return false;}closeModal();const startedAt=Date.now(),endsAt=type==='castle'?startedAt+75000:0;world.activeChallenge={type,target:def.target,progress:new Set(),startedAt:performance.now(),endsAt:type==='castle'?performance.now()+75000:0};state.adventures.active={type,startedAt,endsAt,progress:[]};if(type==='castle')for(const token of world.challengeTokens){token.got=false;token.mesh.visible=true;}els.raceBadge.hidden=false;els.raceTitle.textContent=\`${def.icon} ${def.name}\`;els.raceStatus.textContent=\`0/${def.target}\`;toast(\`${def.name} começou!\`,'good',1900);saveState(true);return true;`
- [x] `return` — linha 3530, coluna 817 em `startAdventure()` — `const def=ADVENTURE_DEFS[type];if(!def||world.activeChallenge||activeRace)return false;if(type==='castle'&&Math.hypot(player.x-88,player.z-52)>15){setWaypoint('castle');toast('Siga a rota até o Castelo para começar.','good',2200);return false;}closeModal();const startedAt=Date.now(),endsAt=type==='castle'?startedAt+75000:0;world.activeChallenge={type,target:def.target,progress:new Set(),startedAt:performance.now(),endsAt:type==='castle'?performance.now()+75000:0};state.adventures.active={type,startedAt,endsAt,progress:[]};if(type==='castle')for(const token of world.challengeTokens){token.got=false;token.mesh.visible=true;}els.raceBadge.hidden=false;els.raceTitle.textContent=\`${def.icon} ${def.name}\`;els.raceStatus.textContent=\`0/${def.target}\`;toast(\`${def.name} começou!\`,'good',1900);saveState(true);return true;`
- [x] `if` — linha 3533, coluna 74 em `restoreActiveAdventure()` — `const saved=state.adventures?.active,def=ADVENTURE_DEFS[saved?.type];if(!saved||!def)return false;const progress=new Set(Array.isArray(saved.progress)?saved.progress:[]),elapsed=Math.max(0,Date.now()-Number(saved.startedAt||Date.now())),remaining=saved.type==='castle'?Math.max(5000,Number(saved.endsAt||Date.now()+75000)-Date.now()):0;`
- [x] `return` — linha 3533, coluna 90 em `restoreActiveAdventure()` — `const saved=state.adventures?.active,def=ADVENTURE_DEFS[saved?.type];if(!saved||!def)return false;const progress=new Set(Array.isArray(saved.progress)?saved.progress:[]),elapsed=Math.max(0,Date.now()-Number(saved.startedAt||Date.now())),remaining=saved.type==='castle'?Math.max(5000,Number(saved.endsAt||Date.now()+75000)-Date.now()):0;`
- [x] `if` — linha 3535, coluna 5 em `restoreActiveAdventure()` — `if(saved.type==='castle')for(const token of world.challengeTokens){token.got=progress.has(token.id);token.mesh.visible=!token.got;}`
- [x] `for` — linha 3535, coluna 30 em `restoreActiveAdventure()` — `if(saved.type==='castle')for(const token of world.challengeTokens){token.got=progress.has(token.id);token.mesh.visible=!token.got;}`
- [x] `return` — linha 3536, coluna 144 em `restoreActiveAdventure()` — `els.raceBadge.hidden=false;els.raceTitle.textContent=\`${def.icon} ${def.name}\`;els.raceStatus.textContent=\`${progress.size}/${def.target}\`;return true;`
- [x] `if` — linha 3539, coluna 40 em `advanceAdventure()` — `const active=world.activeChallenge;if(!active||active.type!==type||active.progress.has(key))return false;active.progress.add(key);if(state.adventures.active){state.adventures.active.progress=[...active.progress];state.adventures.active.updatedAt=Date.now();}els.raceStatus.textContent=\`${active.progress.size}/${active.target}\`;beep(780,55,'sine');if(active.progress.size>=active.target)finishAdventure(true);else saveState();return true;`
- [x] `return` — linha 3539, coluna 97 em `advanceAdventure()` — `const active=world.activeChallenge;if(!active||active.type!==type||active.progress.has(key))return false;active.progress.add(key);if(state.adventures.active){state.adventures.active.progress=[...active.progress];state.adventures.active.updatedAt=Date.now();}els.raceStatus.textContent=\`${active.progress.size}/${active.target}\`;beep(780,55,'sine');if(active.progress.size>=active.target)finishAdventure(true);else saveState();return true;`
- [x] `if` — linha 3539, coluna 135 em `advanceAdventure()` — `const active=world.activeChallenge;if(!active||active.type!==type||active.progress.has(key))return false;active.progress.add(key);if(state.adventures.active){state.adventures.active.progress=[...active.progress];state.adventures.active.updatedAt=Date.now();}els.raceStatus.textContent=\`${active.progress.size}/${active.target}\`;beep(780,55,'sine');if(active.progress.size>=active.target)finishAdventure(true);else saveState();return true;`
- [x] `if` — linha 3539, coluna 353 em `advanceAdventure()` — `const active=world.activeChallenge;if(!active||active.type!==type||active.progress.has(key))return false;active.progress.add(key);if(state.adventures.active){state.adventures.active.progress=[...active.progress];state.adventures.active.updatedAt=Date.now();}els.raceStatus.textContent=\`${active.progress.size}/${active.target}\`;beep(780,55,'sine');if(active.progress.size>=active.target)finishAdventure(true);else saveState();return true;`
- [x] `else` — linha 3539, coluna 414 em `advanceAdventure()` — `const active=world.activeChallenge;if(!active||active.type!==type||active.progress.has(key))return false;active.progress.add(key);if(state.adventures.active){state.adventures.active.progress=[...active.progress];state.adventures.active.updatedAt=Date.now();}els.raceStatus.textContent=\`${active.progress.size}/${active.target}\`;beep(780,55,'sine');if(active.progress.size>=active.target)finishAdventure(true);else saveState();return true;`
- [x] `return` — linha 3539, coluna 431 em `advanceAdventure()` — `const active=world.activeChallenge;if(!active||active.type!==type||active.progress.has(key))return false;active.progress.add(key);if(state.adventures.active){state.adventures.active.progress=[...active.progress];state.adventures.active.updatedAt=Date.now();}els.raceStatus.textContent=\`${active.progress.size}/${active.target}\`;beep(780,55,'sine');if(active.progress.size>=active.target)finishAdventure(true);else saveState();return true;`
- [x] `if` — linha 3542, coluna 40 em `finishAdventure()` — `const active=world.activeChallenge;if(!active)return;const def=ADVENTURE_DEFS[active.type];for(const token of world.challengeTokens)token.mesh.visible=false;world.activeChallenge=null;state.adventures.active=null;els.raceBadge.hidden=true;if(success){if(!state.adventures.completed.includes(active.type))state.adventures.completed.push(active.type);const elapsed=(performance.now()-active.startedAt)/1000,stateBest=Number(state.adventures.bestTimes[active.type]||Infinity);state.adventures.bestTimes[active.type]=Math.min(stateBest,elapsed);addCoins(def.reward);addXP(def.xp);addReputation(8);awardMedal(def.name);if(active.type==='castle')setFlag('castleChallenge');toast(\`Desafio concluído! +${def.reward} moedas\`,'good',2600);}else toast('Tempo esgotado. Tente novamente!','warn',2200);saveState(true);`
- [x] `return` — linha 3542, coluna 51 em `finishAdventure()` — `const active=world.activeChallenge;if(!active)return;const def=ADVENTURE_DEFS[active.type];for(const token of world.challengeTokens)token.mesh.visible=false;world.activeChallenge=null;state.adventures.active=null;els.raceBadge.hidden=true;if(success){if(!state.adventures.completed.includes(active.type))state.adventures.completed.push(active.type);const elapsed=(performance.now()-active.startedAt)/1000,stateBest=Number(state.adventures.bestTimes[active.type]||Infinity);state.adventures.bestTimes[active.type]=Math.min(stateBest,elapsed);addCoins(def.reward);addXP(def.xp);addReputation(8);awardMedal(def.name);if(active.type==='castle')setFlag('castleChallenge');toast(\`Desafio concluído! +${def.reward} moedas\`,'good',2600);}else toast('Tempo esgotado. Tente novamente!','warn',2200);saveState(true);`
- [x] `for` — linha 3542, coluna 96 em `finishAdventure()` — `const active=world.activeChallenge;if(!active)return;const def=ADVENTURE_DEFS[active.type];for(const token of world.challengeTokens)token.mesh.visible=false;world.activeChallenge=null;state.adventures.active=null;els.raceBadge.hidden=true;if(success){if(!state.adventures.completed.includes(active.type))state.adventures.completed.push(active.type);const elapsed=(performance.now()-active.startedAt)/1000,stateBest=Number(state.adventures.bestTimes[active.type]||Infinity);state.adventures.bestTimes[active.type]=Math.min(stateBest,elapsed);addCoins(def.reward);addXP(def.xp);addReputation(8);awardMedal(def.name);if(active.type==='castle')setFlag('castleChallenge');toast(\`Desafio concluído! +${def.reward} moedas\`,'good',2600);}else toast('Tempo esgotado. Tente novamente!','warn',2200);saveState(true);`
- [x] `if` — linha 3542, coluna 244 em `finishAdventure()` — `const active=world.activeChallenge;if(!active)return;const def=ADVENTURE_DEFS[active.type];for(const token of world.challengeTokens)token.mesh.visible=false;world.activeChallenge=null;state.adventures.active=null;els.raceBadge.hidden=true;if(success){if(!state.adventures.completed.includes(active.type))state.adventures.completed.push(active.type);const elapsed=(performance.now()-active.startedAt)/1000,stateBest=Number(state.adventures.bestTimes[active.type]||Infinity);state.adventures.bestTimes[active.type]=Math.min(stateBest,elapsed);addCoins(def.reward);addXP(def.xp);addReputation(8);awardMedal(def.name);if(active.type==='castle')setFlag('castleChallenge');toast(\`Desafio concluído! +${def.reward} moedas\`,'good',2600);}else toast('Tempo esgotado. Tente novamente!','warn',2200);saveState(true);`
- [x] `if` — linha 3542, coluna 256 em `finishAdventure()` — `const active=world.activeChallenge;if(!active)return;const def=ADVENTURE_DEFS[active.type];for(const token of world.challengeTokens)token.mesh.visible=false;world.activeChallenge=null;state.adventures.active=null;els.raceBadge.hidden=true;if(success){if(!state.adventures.completed.includes(active.type))state.adventures.completed.push(active.type);const elapsed=(performance.now()-active.startedAt)/1000,stateBest=Number(state.adventures.bestTimes[active.type]||Infinity);state.adventures.bestTimes[active.type]=Math.min(stateBest,elapsed);addCoins(def.reward);addXP(def.xp);addReputation(8);awardMedal(def.name);if(active.type==='castle')setFlag('castleChallenge');toast(\`Desafio concluído! +${def.reward} moedas\`,'good',2600);}else toast('Tempo esgotado. Tente novamente!','warn',2200);saveState(true);`
- [x] `if` — linha 3542, coluna 619 em `finishAdventure()` — `const active=world.activeChallenge;if(!active)return;const def=ADVENTURE_DEFS[active.type];for(const token of world.challengeTokens)token.mesh.visible=false;world.activeChallenge=null;state.adventures.active=null;els.raceBadge.hidden=true;if(success){if(!state.adventures.completed.includes(active.type))state.adventures.completed.push(active.type);const elapsed=(performance.now()-active.startedAt)/1000,stateBest=Number(state.adventures.bestTimes[active.type]||Infinity);state.adventures.bestTimes[active.type]=Math.min(stateBest,elapsed);addCoins(def.reward);addXP(def.xp);addReputation(8);awardMedal(def.name);if(active.type==='castle')setFlag('castleChallenge');toast(\`Desafio concluído! +${def.reward} moedas\`,'good',2600);}else toast('Tempo esgotado. Tente novamente!','warn',2200);saveState(true);`
- [x] `else` — linha 3542, coluna 735 em `finishAdventure()` — `const active=world.activeChallenge;if(!active)return;const def=ADVENTURE_DEFS[active.type];for(const token of world.challengeTokens)token.mesh.visible=false;world.activeChallenge=null;state.adventures.active=null;els.raceBadge.hidden=true;if(success){if(!state.adventures.completed.includes(active.type))state.adventures.completed.push(active.type);const elapsed=(performance.now()-active.startedAt)/1000,stateBest=Number(state.adventures.bestTimes[active.type]||Infinity);state.adventures.bestTimes[active.type]=Math.min(stateBest,elapsed);addCoins(def.reward);addXP(def.xp);addReputation(8);awardMedal(def.name);if(active.type==='castle')setFlag('castleChallenge');toast(\`Desafio concluído! +${def.reward} moedas\`,'good',2600);}else toast('Tempo esgotado. Tente novamente!','warn',2200);saveState(true);`
- [x] `if` — linha 3545, coluna 40 em `updateAdventure()` — `const active=world.activeChallenge;if(!active)return;if(active.type==='castle'){if(performance.now()>=active.endsAt){finishAdventure(false);return;}for(const token of world.challengeTokens){if(token.got)continue;token.mesh.rotation.y+=.08;token.mesh.position.y=1.45+Math.sin(animTime*3+token.x)*.18;if(Math.hypot(player.x-token.x,player.z-token.z)<1.5){token.got=true;token.mesh.visible=false;advanceAdventure('castle',token.id);}}if(world.activeChallenge)els.raceStatus.textContent=\`${active.progress.size}/${active.target} • ${Math.ceil((active.endsAt-performance.now())/1000)} s\`;}}`
- [x] `return` — linha 3545, coluna 51 em `updateAdventure()` — `const active=world.activeChallenge;if(!active)return;if(active.type==='castle'){if(performance.now()>=active.endsAt){finishAdventure(false);return;}for(const token of world.challengeTokens){if(token.got)continue;token.mesh.rotation.y+=.08;token.mesh.position.y=1.45+Math.sin(animTime*3+token.x)*.18;if(Math.hypot(player.x-token.x,player.z-token.z)<1.5){token.got=true;token.mesh.visible=false;advanceAdventure('castle',token.id);}}if(world.activeChallenge)els.raceStatus.textContent=\`${active.progress.size}/${active.target} • ${Math.ceil((active.endsAt-performance.now())/1000)} s\`;}}`
- [x] `if` — linha 3545, coluna 58 em `updateAdventure()` — `const active=world.activeChallenge;if(!active)return;if(active.type==='castle'){if(performance.now()>=active.endsAt){finishAdventure(false);return;}for(const token of world.challengeTokens){if(token.got)continue;token.mesh.rotation.y+=.08;token.mesh.position.y=1.45+Math.sin(animTime*3+token.x)*.18;if(Math.hypot(player.x-token.x,player.z-token.z)<1.5){token.got=true;token.mesh.visible=false;advanceAdventure('castle',token.id);}}if(world.activeChallenge)els.raceStatus.textContent=\`${active.progress.size}/${active.target} • ${Math.ceil((active.endsAt-performance.now())/1000)} s\`;}}`
- [x] `if` — linha 3545, coluna 85 em `updateAdventure()` — `const active=world.activeChallenge;if(!active)return;if(active.type==='castle'){if(performance.now()>=active.endsAt){finishAdventure(false);return;}for(const token of world.challengeTokens){if(token.got)continue;token.mesh.rotation.y+=.08;token.mesh.position.y=1.45+Math.sin(animTime*3+token.x)*.18;if(Math.hypot(player.x-token.x,player.z-token.z)<1.5){token.got=true;token.mesh.visible=false;advanceAdventure('castle',token.id);}}if(world.activeChallenge)els.raceStatus.textContent=\`${active.progress.size}/${active.target} • ${Math.ceil((active.endsAt-performance.now())/1000)} s\`;}}`
- [x] `return` — linha 3545, coluna 145 em `updateAdventure()` — `const active=world.activeChallenge;if(!active)return;if(active.type==='castle'){if(performance.now()>=active.endsAt){finishAdventure(false);return;}for(const token of world.challengeTokens){if(token.got)continue;token.mesh.rotation.y+=.08;token.mesh.position.y=1.45+Math.sin(animTime*3+token.x)*.18;if(Math.hypot(player.x-token.x,player.z-token.z)<1.5){token.got=true;token.mesh.visible=false;advanceAdventure('castle',token.id);}}if(world.activeChallenge)els.raceStatus.textContent=\`${active.progress.size}/${active.target} • ${Math.ceil((active.endsAt-performance.now())/1000)} s\`;}}`
- [x] `for` — linha 3545, coluna 153 em `updateAdventure()` — `const active=world.activeChallenge;if(!active)return;if(active.type==='castle'){if(performance.now()>=active.endsAt){finishAdventure(false);return;}for(const token of world.challengeTokens){if(token.got)continue;token.mesh.rotation.y+=.08;token.mesh.position.y=1.45+Math.sin(animTime*3+token.x)*.18;if(Math.hypot(player.x-token.x,player.z-token.z)<1.5){token.got=true;token.mesh.visible=false;advanceAdventure('castle',token.id);}}if(world.activeChallenge)els.raceStatus.textContent=\`${active.progress.size}/${active.target} • ${Math.ceil((active.endsAt-performance.now())/1000)} s\`;}}`
- [x] `if` — linha 3545, coluna 195 em `updateAdventure()` — `const active=world.activeChallenge;if(!active)return;if(active.type==='castle'){if(performance.now()>=active.endsAt){finishAdventure(false);return;}for(const token of world.challengeTokens){if(token.got)continue;token.mesh.rotation.y+=.08;token.mesh.position.y=1.45+Math.sin(animTime*3+token.x)*.18;if(Math.hypot(player.x-token.x,player.z-token.z)<1.5){token.got=true;token.mesh.visible=false;advanceAdventure('castle',token.id);}}if(world.activeChallenge)els.raceStatus.textContent=\`${active.progress.size}/${active.target} • ${Math.ceil((active.endsAt-performance.now())/1000)} s\`;}}`
- [x] `if` — linha 3545, coluna 304 em `updateAdventure()` — `const active=world.activeChallenge;if(!active)return;if(active.type==='castle'){if(performance.now()>=active.endsAt){finishAdventure(false);return;}for(const token of world.challengeTokens){if(token.got)continue;token.mesh.rotation.y+=.08;token.mesh.position.y=1.45+Math.sin(animTime*3+token.x)*.18;if(Math.hypot(player.x-token.x,player.z-token.z)<1.5){token.got=true;token.mesh.visible=false;advanceAdventure('castle',token.id);}}if(world.activeChallenge)els.raceStatus.textContent=\`${active.progress.size}/${active.target} • ${Math.ceil((active.endsAt-performance.now())/1000)} s\`;}}`
- [x] `if` — linha 3545, coluna 436 em `updateAdventure()` — `const active=world.activeChallenge;if(!active)return;if(active.type==='castle'){if(performance.now()>=active.endsAt){finishAdventure(false);return;}for(const token of world.challengeTokens){if(token.got)continue;token.mesh.rotation.y+=.08;token.mesh.position.y=1.45+Math.sin(animTime*3+token.x)*.18;if(Math.hypot(player.x-token.x,player.z-token.z)<1.5){token.got=true;token.mesh.visible=false;advanceAdventure('castle',token.id);}}if(world.activeChallenge)els.raceStatus.textContent=\`${active.progress.size}/${active.target} • ${Math.ceil((active.endsAt-performance.now())/1000)} s\`;}}`
- [x] `return` — linha 3548, coluna 735 em `createLearningStation()` — `const g=new THREE.Group();g.position.set(x,0,z);worldGroup.add(g);premiumBox(2.5,.35,2.5,0x34485e,0,.18,0,g);premiumBox(1.7,2.1,1.2,color,0,1.35,0,g);const panel=new THREE.Mesh(new THREE.PlaneGeometry(1.25,1.25),new THREE.MeshStandardMaterial({map:iconTexture(icon,color,'#ffffff'),roughness:.35,emissive:new THREE.Color(color),emissiveIntensity:.12,side:THREE.DoubleSide}));panel.position.set(0,1.55,.63);g.add(panel);const beacon=new THREE.Mesh(new THREE.OctahedronGeometry(.34,0),mat(color,{emissive:color,emissiveIntensity:1.1}));beacon.position.y=2.75;g.add(beacon);registerInteractable({id:\`learning-${id}\`,type:'education',icon,label,x,z,radius:3,priority:130,action:()=>openEducationHub(subject)});world.landmarks.push(g);return g;`

## `18-water-fishing-boats.js`

- [x] `return` — linha 3559, coluna 35 em `rectOverlap()` — `function rectOverlap(a,b,pad=0){return Math.abs(a.x-b.x)<(a.w+b.w)/2+pad&&Math.abs(a.z-b.z)<(a.d+b.d)/2+pad;}`
- [x] `return` — linha 3560, coluna 31 em `insideWater()` — `function insideWater(x,z,h){return Math.abs(x-h.x)<=h.w/2&&Math.abs(z-h.z)<=h.d/2;}`
- [x] `return` — linha 3561, coluna 25 em `waterAt()` — `function waterAt(x,z){return(world.hazards||[]).find(h=>h.type==='water'&&insideWater(x,z,h));}`
- [x] `return` — linha 3562, coluna 39 em `isInsideLakeNavigable()` — `function isInsideLakeNavigable(x,z){return (x>=-113&&x<=-29&&z>=44&&z<=60)||(x>=-116&&x<=-82&&z>=55&&z<=84);}`
- [x] `return` — linha 3563, coluna 32 em `isNearFishingArea()` — `function isNearFishingArea(){return player.boating||Math.hypot(player.x+28,player.z-45)<9||Math.hypot(player.x+27,player.z-57)<9;}`
- [x] `if` — linha 3564, coluna 45 em `resolveWaterWalking()` — `function resolveWaterWalking(prevX,prevZ){if(player.boating||player.vehicle||currentHouse)return;const h=waterAt(player.x,player.z);if(!h||groundHeightAt(player.x,player.z)>.24)return;player.x=prevX;player.z=prevZ;player.vx=player.vz=0;if(performance.now()-waterWarningAt>1700){waterWarningAt=performance.now();toast('A água é funda. Use o píer e o barco.','warn',1900);}}`
- [x] `return` — linha 3564, coluna 93 em `resolveWaterWalking()` — `function resolveWaterWalking(prevX,prevZ){if(player.boating||player.vehicle||currentHouse)return;const h=waterAt(player.x,player.z);if(!h||groundHeightAt(player.x,player.z)>.24)return;player.x=prevX;player.z=prevZ;player.vx=player.vz=0;if(performance.now()-waterWarningAt>1700){waterWarningAt=performance.now();toast('A água é funda. Use o píer e o barco.','warn',1900);}}`
- [x] `if` — linha 3564, coluna 135 em `resolveWaterWalking()` — `function resolveWaterWalking(prevX,prevZ){if(player.boating||player.vehicle||currentHouse)return;const h=waterAt(player.x,player.z);if(!h||groundHeightAt(player.x,player.z)>.24)return;player.x=prevX;player.z=prevZ;player.vx=player.vz=0;if(performance.now()-waterWarningAt>1700){waterWarningAt=performance.now();toast('A água é funda. Use o píer e o barco.','warn',1900);}}`
- [x] `return` — linha 3564, coluna 180 em `resolveWaterWalking()` — `function resolveWaterWalking(prevX,prevZ){if(player.boating||player.vehicle||currentHouse)return;const h=waterAt(player.x,player.z);if(!h||groundHeightAt(player.x,player.z)>.24)return;player.x=prevX;player.z=prevZ;player.vx=player.vz=0;if(performance.now()-waterWarningAt>1700){waterWarningAt=performance.now();toast('A água é funda. Use o píer e o barco.','warn',1900);}}`
- [x] `if` — linha 3564, coluna 239 em `resolveWaterWalking()` — `function resolveWaterWalking(prevX,prevZ){if(player.boating||player.vehicle||currentHouse)return;const h=waterAt(player.x,player.z);if(!h||groundHeightAt(player.x,player.z)>.24)return;player.x=prevX;player.z=prevZ;player.vx=player.vz=0;if(performance.now()-waterWarningAt>1700){waterWarningAt=performance.now();toast('A água é funda. Use o píer e o barco.','warn',1900);}}`
- [x] `return` — linha 3567, coluna 144 em `distanceToBoatDock()` — `function distanceToBoatDock(x=player.x,z=player.z){const nx=clamp(x,BOAT_DOCK.minX,BOAT_DOCK.maxX),nz=clamp(z,BOAT_DOCK.minZ,BOAT_DOCK.maxZ);return Math.hypot(x-nx,z-nz);}`
- [x] `return` — linha 3568, coluna 28 em `validBoatExit()` — `function validBoatExit(){return distanceToBoatDock()<=BOAT_DOCK.touchDistance;}`
- [x] `return` — linha 3569, coluna 32 em `safeBoatExitPoint()` — `function safeBoatExitPoint(){return{x:BOAT_DOCK.exitX,z:clamp(player.z,BOAT_DOCK.minZ+.25,BOAT_DOCK.maxZ-.25)};}`
- [x] `if` — linha 3572, coluna 5 em `ensureFishingVisual()` — `if(fishingVisual||!playerGroup||!worldGroup)return fishingVisual;`
- [x] `return` — linha 3572, coluna 49 em `ensureFishingVisual()` — `if(fishingVisual||!playerGroup||!worldGroup)return fishingVisual;`
- [x] `return` — linha 3583, coluna 5 em `ensureFishingVisual()` — `return fishingVisual;`
- [x] `if` — linha 3585, coluna 54 em `setFishingLine()` — `function setFishingLine(a,b){const v=fishingVisual;if(!v)return;const attr=v.line.geometry.getAttribute('position'),arr=attr.array;arr[0]=a.x;arr[1]=a.y;arr[2]=a.z;arr[3]=b.x;arr[4]=b.y;arr[5]=b.z;attr.needsUpdate=true;}`
- [x] `return` — linha 3585, coluna 60 em `setFishingLine()` — `function setFishingLine(a,b){const v=fishingVisual;if(!v)return;const attr=v.line.geometry.getAttribute('position'),arr=attr.array;arr[0]=a.x;arr[1]=a.y;arr[2]=a.z;arr[3]=b.x;arr[4]=b.y;arr[5]=b.z;attr.needsUpdate=true;}`
- [x] `if` — linha 3588, coluna 5 em `fishingCastTarget()` — `if(source==='boat'){const heading=player.boat.heading;dx=Math.sin(heading);dz=Math.cos(heading);}else{dx=-45-player.x;dz=52-player.z;const m=Math.hypot(dx,dz)||1;dx/=m;dz/=m;}`
- [x] `else` — linha 3588, coluna 102 em `fishingCastTarget()` — `if(source==='boat'){const heading=player.boat.heading;dx=Math.sin(heading);dz=Math.cos(heading);}else{dx=-45-player.x;dz=52-player.z;const m=Math.hypot(dx,dz)||1;dx/=m;dz/=m;}`
- [x] `if` — linha 3590, coluna 5 em `fishingCastTarget()` — `if(!isInsideLakeNavigable(x,z)){dx=-72-player.x;dz=52-player.z;const m=Math.hypot(dx,dz)||1;x=player.x+dx/m*dist;z=player.z+dz/m*dist;}`
- [x] `return` — linha 3591, coluna 5 em `fishingCastTarget()` — `return new THREE.Vector3(x,.16,z);`
- [x] `if` — linha 3595, coluna 5 em `beginFishingVisual()` — `if(!v)return;`
- [x] `return` — linha 3595, coluna 11 em `beginFishingVisual()` — `if(!v)return;`
- [x] `if` — linha 3596, coluna 5 em `beginFishingVisual()` — `if(!fishingCameraState)fishingCameraState={yaw:cameraYaw,pitch:cameraPitch,zoom:cameraZoom};`
- [x] `if` — linha 3607, coluna 62 em `castFishingVisual()` — `function castFishingVisual(){const v=ensureFishingVisual();if(!v?.active)return;v.tip.getWorldPosition(v.castStart);v.target.copy(fishingCastTarget(v.source));v.phase='casting';v.phaseAt=performance.now();beep(420,55,'sine');}`
- [x] `return` — linha 3607, coluna 76 em `castFishingVisual()` — `function castFishingVisual(){const v=ensureFishingVisual();if(!v?.active)return;v.tip.getWorldPosition(v.castStart);v.target.copy(fishingCastTarget(v.source));v.phase='casting';v.phaseAt=performance.now();beep(420,55,'sine');}`
- [x] `if` — linha 3608, coluna 54 em `hookFishingVisual()` — `function hookFishingVisual(){const v=fishingVisual;if(!v?.active)return;v.phase='hooked';v.phaseAt=performance.now();v.bobber.scale.setScalar(1.28);}`
- [x] `return` — linha 3608, coluna 68 em `hookFishingVisual()` — `function hookFishingVisual(){const v=fishingVisual;if(!v?.active)return;v.phase='hooked';v.phaseAt=performance.now();v.bobber.scale.setScalar(1.28);}`
- [x] `if` — linha 3609, coluna 70 em `pullFishingVisual()` — `function pullFishingVisual(success,fishData){const v=fishingVisual;if(!v?.active)return;v.phase=success?'pulling':'escaping';v.phaseAt=performance.now();v.fishSize=clamp(.8+Number(fishData?.size||.5)*.08,.82,1.35);v.fish.scale.setScalar(v.fishSize);v.fish.visible=!!success;}`
- [x] `return` — linha 3609, coluna 84 em `pullFishingVisual()` — `function pullFishingVisual(success,fishData){const v=fishingVisual;if(!v?.active)return;v.phase=success?'pulling':'escaping';v.phaseAt=performance.now();v.fishSize=clamp(.8+Number(fishData?.size||.5)*.08,.82,1.35);v.fish.scale.setScalar(v.fishSize);v.fish.visible=!!success;}`
- [x] `if` — linha 3611, coluna 5 em `restoreFishingCamera()` — `if(!fishingCameraState)return;`
- [x] `return` — linha 3611, coluna 28 em `restoreFishingCamera()` — `if(!fishingCameraState)return;`
- [x] `if` — linha 3616, coluna 5 em `stopFishingVisual()` — `if(!v){restoreFishingCamera();return;}`
- [x] `return` — linha 3616, coluna 35 em `stopFishingVisual()` — `if(!v){restoreFishingCamera();return;}`
- [x] `if` — linha 3618, coluna 21 em `stopFishingVisual()` — `const hide=()=>{if(token!==v.hideToken)return;v.active=false;v.phase='idle';v.rodRoot.visible=false;v.line.visible=false;v.bobber.visible=false;v.fish.visible=false;v.bobber.scale.setScalar(1);if(player.emoteType==='fishing'){player.emoteType='';player.emoteUntil=0;player.emoteSeq=(player.emoteSeq||0)+1;}setFishingUiActive(false);restoreFishingCamera();};`
- [x] `return` — linha 3618, coluna 44 em `stopFishingVisual()` — `const hide=()=>{if(token!==v.hideToken)return;v.active=false;v.phase='idle';v.rodRoot.visible=false;v.line.visible=false;v.bobber.visible=false;v.fish.visible=false;v.bobber.scale.setScalar(1);if(player.emoteType==='fishing'){player.emoteType='';player.emoteUntil=0;player.emoteSeq=(player.emoteSeq||0)+1;}setFishingUiActive(false);restoreFishingCamera();};`
- [x] `if` — linha 3618, coluna 198 em `stopFishingVisual()` — `const hide=()=>{if(token!==v.hideToken)return;v.active=false;v.phase='idle';v.rodRoot.visible=false;v.line.visible=false;v.bobber.visible=false;v.fish.visible=false;v.bobber.scale.setScalar(1);if(player.emoteType==='fishing'){player.emoteType='';player.emoteUntil=0;player.emoteSeq=(player.emoteSeq||0)+1;}setFishingUiActive(false);restoreFishingCamera();};`
- [x] `if` — linha 3619, coluna 5 em `stopFishingVisual()` — `if(delay>0)setTimeout(hide,delay);else hide();`
- [x] `else` — linha 3619, coluna 39 em `stopFishingVisual()` — `if(delay>0)setTimeout(hide,delay);else hide();`
- [x] `if` — linha 3621, coluna 55 em `clearFishingTimers()` — `function clearFishingTimers(session=fishingSession){if(!session)return;clearTimeout(session.hookTimer);clearTimeout(session.escapeTimer);clearTimeout(session.finishTimer);}`
- [x] `return` — linha 3621, coluna 67 em `clearFishingTimers()` — `function clearFishingTimers(session=fishingSession){if(!session)return;clearTimeout(session.hookTimer);clearTimeout(session.escapeTimer);clearTimeout(session.finishTimer);}`
- [x] `if` — linha 3624, coluna 5 em `setFishingUiActive()` — `if(active){state.ui.quickOpen=false;state.ui.skillsOpen=false;state.ui.needsOpen=false;state.ui.missionOpen=false;els.game?.classList.remove('needs-expanded');els.missionCard?.classList.remove('expanded');syncMobilePanels();}`
- [x] `if` — linha 3629, coluna 5 em `ensureFishingModalStyle()` — `if(document.getElementById('otthosFishingModalStyle'))return;`
- [x] `return` — linha 3629, coluna 59 em `ensureFishingModalStyle()` — `if(document.getElementById('otthosFishingModalStyle'))return;`
- [x] `if` — linha 3640, coluna 27 em `updateFishingVisual()` — `const v=fishingVisual;if(!v?.active)return;const now=performance.now(),tip=new THREE.Vector3();v.tip.getWorldPosition(tip);let lineEnd=v.bobber.position;`
- [x] `return` — linha 3640, coluna 41 em `updateFishingVisual()` — `const v=fishingVisual;if(!v?.active)return;const now=performance.now(),tip=new THREE.Vector3();v.tip.getWorldPosition(tip);let lineEnd=v.bobber.position;`
- [x] `if` — linha 3641, coluna 5 em `updateFishingVisual()` — `if(v.phase==='ready'){v.bobber.position.copy(tip);v.rodRoot.rotation.x=lerp(v.rodRoot.rotation.x,-.48,.18);}`
- [x] `else` — linha 3642, coluna 5 em `updateFishingVisual()` — `else if(v.phase==='casting'){const t=clamp((now-v.phaseAt)/620,0,1),ease=1-Math.pow(1-t,3);v.bobber.position.lerpVectors(v.castStart,v.target,ease);v.bobber.position.y+=Math.sin(Math.PI*t)*2.35;v.rodRoot.rotation.x=lerp(-.18,-.72,ease);if(t>=1){v.phase='waiting';v.phaseAt=now;v.bobber.position.copy(v.target);}}`
- [x] `if` — linha 3642, coluna 10 em `updateFishingVisual()` — `else if(v.phase==='casting'){const t=clamp((now-v.phaseAt)/620,0,1),ease=1-Math.pow(1-t,3);v.bobber.position.lerpVectors(v.castStart,v.target,ease);v.bobber.position.y+=Math.sin(Math.PI*t)*2.35;v.rodRoot.rotation.x=lerp(-.18,-.72,ease);if(t>=1){v.phase='waiting';v.phaseAt=now;v.bobber.position.copy(v.target);}}`
- [x] `if` — linha 3642, coluna 241 em `updateFishingVisual()` — `else if(v.phase==='casting'){const t=clamp((now-v.phaseAt)/620,0,1),ease=1-Math.pow(1-t,3);v.bobber.position.lerpVectors(v.castStart,v.target,ease);v.bobber.position.y+=Math.sin(Math.PI*t)*2.35;v.rodRoot.rotation.x=lerp(-.18,-.72,ease);if(t>=1){v.phase='waiting';v.phaseAt=now;v.bobber.position.copy(v.target);}}`
- [x] `else` — linha 3643, coluna 5 em `updateFishingVisual()` — `else if(v.phase==='waiting'){v.bobber.position.set(v.target.x,.15+Math.sin(now*.007)*.035,v.target.z);v.rodRoot.rotation.x=lerp(v.rodRoot.rotation.x,-.62,.08);}`
- [x] `if` — linha 3643, coluna 10 em `updateFishingVisual()` — `else if(v.phase==='waiting'){v.bobber.position.set(v.target.x,.15+Math.sin(now*.007)*.035,v.target.z);v.rodRoot.rotation.x=lerp(v.rodRoot.rotation.x,-.62,.08);}`
- [x] `else` — linha 3644, coluna 5 em `updateFishingVisual()` — `else if(v.phase==='hooked'){v.bobber.position.set(v.target.x,.09+Math.sin(now*.026)*.11,v.target.z);v.bobber.scale.setScalar(1.12+Math.sin(now*.03)*.12);v.rodRoot.rotation.x=lerp(v.rodRoot.rotation.x,-.82,.12);}`
- [x] `if` — linha 3644, coluna 10 em `updateFishingVisual()` — `else if(v.phase==='hooked'){v.bobber.position.set(v.target.x,.09+Math.sin(now*.026)*.11,v.target.z);v.bobber.scale.setScalar(1.12+Math.sin(now*.03)*.12);v.rodRoot.rotation.x=lerp(v.rodRoot.rotation.x,-.82,.12);}`
- [x] `else` — linha 3645, coluna 5 em `updateFishingVisual()` — `else if(v.phase==='pulling'){const t=clamp((now-v.phaseAt)/820,0,1),ease=1-Math.pow(1-t,3);v.bobber.position.lerpVectors(v.target,tip,ease);v.bobber.position.y+=Math.sin(Math.PI*t)*1.3;v.fish.position.copy(v.bobber.position);v.fish.position.y-=.18;v.fish.rotation.y+=.16;v.fish.rotation.z=Math.sin(now*.025)*.35;v.rodRoot.rotation.x=lerp(v.rodRoot.rotation.x,-1.02,.15);if(t>=1){v.phase='caught';v.phaseAt=now;v.bobber.visible=false;}}`
- [x] `if` — linha 3645, coluna 10 em `updateFishingVisual()` — `else if(v.phase==='pulling'){const t=clamp((now-v.phaseAt)/820,0,1),ease=1-Math.pow(1-t,3);v.bobber.position.lerpVectors(v.target,tip,ease);v.bobber.position.y+=Math.sin(Math.PI*t)*1.3;v.fish.position.copy(v.bobber.position);v.fish.position.y-=.18;v.fish.rotation.y+=.16;v.fish.rotation.z=Math.sin(now*.025)*.35;v.rodRoot.rotation.x=lerp(v.rodRoot.rotation.x,-1.02,.15);if(t>=1){v.phase='caught';v.phaseAt=now;v.bobber.visible=false;}}`
- [x] `if` — linha 3645, coluna 375 em `updateFishingVisual()` — `else if(v.phase==='pulling'){const t=clamp((now-v.phaseAt)/820,0,1),ease=1-Math.pow(1-t,3);v.bobber.position.lerpVectors(v.target,tip,ease);v.bobber.position.y+=Math.sin(Math.PI*t)*1.3;v.fish.position.copy(v.bobber.position);v.fish.position.y-=.18;v.fish.rotation.y+=.16;v.fish.rotation.z=Math.sin(now*.025)*.35;v.rodRoot.rotation.x=lerp(v.rodRoot.rotation.x,-1.02,.15);if(t>=1){v.phase='caught';v.phaseAt=now;v.bobber.visible=false;}}`
- [x] `else` — linha 3646, coluna 5 em `updateFishingVisual()` — `else if(v.phase==='caught'){v.fish.position.copy(tip);v.fish.position.y-=.35;v.fish.position.x+=Math.sin(now*.02)*.08;v.fish.rotation.z=Math.sin(now*.028)*.42;lineEnd=tip;}`
- [x] `if` — linha 3646, coluna 10 em `updateFishingVisual()` — `else if(v.phase==='caught'){v.fish.position.copy(tip);v.fish.position.y-=.35;v.fish.position.x+=Math.sin(now*.02)*.08;v.fish.rotation.z=Math.sin(now*.028)*.42;lineEnd=tip;}`
- [x] `else` — linha 3647, coluna 5 em `updateFishingVisual()` — `else if(v.phase==='escaping'){const t=clamp((now-v.phaseAt)/760,0,1);v.bobber.position.set(v.target.x,.12-t*.5,v.target.z);v.bobber.scale.setScalar(1-t*.65);if(t>=1)stopFishingVisual();}`
- [x] `if` — linha 3647, coluna 10 em `updateFishingVisual()` — `else if(v.phase==='escaping'){const t=clamp((now-v.phaseAt)/760,0,1);v.bobber.position.set(v.target.x,.12-t*.5,v.target.z);v.bobber.scale.setScalar(1-t*.65);if(t>=1)stopFishingVisual();}`
- [x] `if` — linha 3647, coluna 162 em `updateFishingVisual()` — `else if(v.phase==='escaping'){const t=clamp((now-v.phaseAt)/760,0,1);v.bobber.position.set(v.target.x,.12-t*.5,v.target.z);v.bobber.scale.setScalar(1-t*.65);if(t>=1)stopFishingVisual();}`
- [x] `return` — linha 3654, coluna 442 em `createShoreFisher()` — `const bobber=new THREE.Mesh(new THREE.SphereGeometry(.09,8,6),renderMat(0xff5647,{emissive:0x8c160f,emissiveIntensity:.32,roughness:.5}));worldGroup.add(bobber);const reach=3.8+Math.random()*1.4,dx=Math.sin(heading),dz=Math.cos(heading);bobber.position.set(x+dx*reach,.16,z+dz*reach);npc.fishingActivity.rod=rod;npc.fishingActivity.line=line;npc.fishingActivity.bobber=bobber;npc.fishingActivity.reach=reach;world.shoreFishers.push(npc);return npc;`
- [x] `if` — linha 3657, coluna 5 em `createShoreFishingLife()` — `if(world.shoreFishers.length)return;[`
- [x] `return` — linha 3657, coluna 34 em `createShoreFishingLife()` — `if(world.shoreFishers.length)return;[`
- [x] `if` — linha 3662, coluna 5 em `updateShoreFishers()` — `if(textures.water){textures.water.offset.x=(textures.water.offset.x+dt*.012)%1;textures.water.offset.y=(textures.water.offset.y+dt*.006)%1;}`
- [x] `for` — linha 3663, coluna 33 em `updateShoreFishers()` — `const now=performance.now();for(const npc of world.shoreFishers){const act=npc.fishingActivity;if(!act)continue;npc.group.position.set(npc.baseX,0,npc.baseZ);npc.group.rotation.y=act.heading;const pulse=Math.sin(now*.0021+act.phase),bite=Math.sin(now*.006+act.phase)> .92;act.rod.rotation.x=-.58+pulse*.035-(bite?.1:0);if(npc.limbs){npc.limbs.leftArm.rotation.x=lerp(npc.limbs.leftArm.rotation.x,-1.55+(bite?-.18:0),.18);npc.limbs.rightArm.rotation.x=lerp(npc.limbs.rightArm.rotation.x,-1.72+(bite?-.25:0),.18);npc.limbs.leftLeg.rotation.x=lerp(npc.limbs.leftLeg.rotation.x,.16,.18);npc.limbs.rightLeg.rotation.x=lerp(npc.limbs.rightLeg.rotation.x,-.16,.18);}act.bobber.position.y=.15+Math.sin(now*.004+act.phase)*.035-(bite?.055:0);const tip=new THREE.Vector3();act.rod.localToWorld(tip.set(0,1.08,0));const arr=act.line.geometry.attributes.position.array;arr[0]=tip.x;arr[1]=tip.y;arr[2]=tip.z;arr[3]=act.bobber.position.x;arr[4]=act.bobber.position.y;arr[5]=act.bobber.position.z;act.line.geometry`
- [x] `if` — linha 3663, coluna 100 em `updateShoreFishers()` — `const now=performance.now();for(const npc of world.shoreFishers){const act=npc.fishingActivity;if(!act)continue;npc.group.position.set(npc.baseX,0,npc.baseZ);npc.group.rotation.y=act.heading;const pulse=Math.sin(now*.0021+act.phase),bite=Math.sin(now*.006+act.phase)> .92;act.rod.rotation.x=-.58+pulse*.035-(bite?.1:0);if(npc.limbs){npc.limbs.leftArm.rotation.x=lerp(npc.limbs.leftArm.rotation.x,-1.55+(bite?-.18:0),.18);npc.limbs.rightArm.rotation.x=lerp(npc.limbs.rightArm.rotation.x,-1.72+(bite?-.25:0),.18);npc.limbs.leftLeg.rotation.x=lerp(npc.limbs.leftLeg.rotation.x,.16,.18);npc.limbs.rightLeg.rotation.x=lerp(npc.limbs.rightLeg.rotation.x,-.16,.18);}act.bobber.position.y=.15+Math.sin(now*.004+act.phase)*.035-(bite?.055:0);const tip=new THREE.Vector3();act.rod.localToWorld(tip.set(0,1.08,0));const arr=act.line.geometry.attributes.position.array;arr[0]=tip.x;arr[1]=tip.y;arr[2]=tip.z;arr[3]=act.bobber.position.x;arr[4]=act.bobber.position.y;arr[5]=act.bobber.position.z;act.line.geometry`
- [x] `if` — linha 3663, coluna 324 em `updateShoreFishers()` — `const now=performance.now();for(const npc of world.shoreFishers){const act=npc.fishingActivity;if(!act)continue;npc.group.position.set(npc.baseX,0,npc.baseZ);npc.group.rotation.y=act.heading;const pulse=Math.sin(now*.0021+act.phase),bite=Math.sin(now*.006+act.phase)> .92;act.rod.rotation.x=-.58+pulse*.035-(bite?.1:0);if(npc.limbs){npc.limbs.leftArm.rotation.x=lerp(npc.limbs.leftArm.rotation.x,-1.55+(bite?-.18:0),.18);npc.limbs.rightArm.rotation.x=lerp(npc.limbs.rightArm.rotation.x,-1.72+(bite?-.25:0),.18);npc.limbs.leftLeg.rotation.x=lerp(npc.limbs.leftLeg.rotation.x,.16,.18);npc.limbs.rightLeg.rotation.x=lerp(npc.limbs.rightLeg.rotation.x,-.16,.18);}act.bobber.position.y=.15+Math.sin(now*.004+act.phase)*.035-(bite?.055:0);const tip=new THREE.Vector3();act.rod.localToWorld(tip.set(0,1.08,0));const arr=act.line.geometry.attributes.position.array;arr[0]=tip.x;arr[1]=tip.y;arr[2]=tip.z;arr[3]=act.bobber.position.x;arr[4]=act.bobber.position.y;arr[5]=act.bobber.position.z;act.line.geometry`
- [x] `if` — linha 3671, coluna 5 em `ensureBoatPanel()` — `if(boatPanel)return boatPanel;boatPanel=document.createElement('div');boatPanel.id='boatActivityPanel';boatPanel.className='boat-activity-panel';boatPanel.innerHTML='<button type="button" data-boat-fish>🎣<span>Pescar</span></button><button type="button" data-boat-exit>🏝️<span>Sair</span></button>';document.body.appendChild(boatPanel);boatPanel.querySelector('[data-boat-fish]').onclick=()=>startFishing('boat');boatPanel.querySelector('[data-boat-exit]').onclick=()=>exitBoat();return boatPanel;`
- [x] `return` — linha 3671, coluna 18 em `ensureBoatPanel()` — `if(boatPanel)return boatPanel;boatPanel=document.createElement('div');boatPanel.id='boatActivityPanel';boatPanel.className='boat-activity-panel';boatPanel.innerHTML='<button type="button" data-boat-fish>🎣<span>Pescar</span></button><button type="button" data-boat-exit>🏝️<span>Sair</span></button>';document.body.appendChild(boatPanel);boatPanel.querySelector('[data-boat-fish]').onclick=()=>startFishing('boat');boatPanel.querySelector('[data-boat-exit]').onclick=()=>exitBoat();return boatPanel;`
- [x] `return` — linha 3671, coluna 485 em `ensureBoatPanel()` — `if(boatPanel)return boatPanel;boatPanel=document.createElement('div');boatPanel.id='boatActivityPanel';boatPanel.className='boat-activity-panel';boatPanel.innerHTML='<button type="button" data-boat-fish>🎣<span>Pescar</span></button><button type="button" data-boat-exit>🏝️<span>Sair</span></button>';document.body.appendChild(boatPanel);boatPanel.querySelector('[data-boat-fish]').onclick=()=>startFishing('boat');boatPanel.querySelector('[data-boat-exit]').onclick=()=>exitBoat();return boatPanel;`
- [x] `if` — linha 3673, coluna 89 em `updateBoatPanel()` — `function updateBoatPanel(){const panel=ensureBoatPanel();panel.hidden=!player.boating;if(player.boating){const passenger=!!player.boat.passengerOf;panel.querySelector('[data-boat-fish]').disabled=passenger;panel.querySelector('[data-boat-fish] span').textContent=passenger?'Passageiro':'Pescar';}}`
- [x] `if` — linha 3675, coluna 5 em `updateBoatPanel()` — `if(player.boating)return false;`
- [x] `return` — linha 3675, coluna 23 em `updateBoatPanel()` — `if(player.boating)return false;`
- [x] `if` — linha 3676, coluna 5 em `updateBoatPanel()` — `if(!canEnterMobility(PLAYER_MODES.BOAT_DRIVER)&&!player.vehicle)return false;`
- [x] `return` — linha 3676, coluna 69 em `updateBoatPanel()` — `if(!canEnterMobility(PLAYER_MODES.BOAT_DRIVER)&&!player.vehicle)return false;`
- [x] `if` — linha 3677, coluna 5 em `updateBoatPanel()` — `if(player.vehicle)exitVehicle(true);`
- [x] `if` — linha 3678, coluna 5 em `updateBoatPanel()` — `if(player.transit.mode||!world.boat)return false;`
- [x] `return` — linha 3678, coluna 41 em `updateBoatPanel()` — `if(player.transit.mode||!world.boat)return false;`
- [x] `if` — linha 3679, coluna 39 em `updateBoatPanel()` — `const p=world.boat.group.position;if(Math.hypot(player.x-p.x,player.z-p.z)>3.6){toast('Chegue perto do barco pelo píer.','warn');return false;}`
- [x] `return` — linha 3679, coluna 134 em `updateBoatPanel()` — `const p=world.boat.group.position;if(Math.hypot(player.x-p.x,player.z-p.z)>3.6){toast('Chegue perto do barco pelo píer.','warn');return false;}`
- [x] `if` — linha 3680, coluna 69 em `updateBoatPanel()` — `const lock=await window.OTTHOS_RTDB?.claimBoat?.(world.boat.id);if(lock&&lock.ok===false){toast(lock.error||'O barco já está sendo usado por outro jogador.','warn',2800);return false;}`
- [x] `return` — linha 3680, coluna 175 em `updateBoatPanel()` — `const lock=await window.OTTHOS_RTDB?.claimBoat?.(world.boat.id);if(lock&&lock.ok===false){toast(lock.error||'O barco já está sendo usado por outro jogador.','warn',2800);return false;}`
- [x] `if` — linha 3681, coluna 422 em `updateBoatPanel()` — `input.mobilityAccelerate=false;input.mobilityBrake=false;player.boating=true;player.boat.passengerOf='';player.boat.passengerUid='';player.boat.passengerBotId='';player.boat.heading=world.boat.heading||0;player.boat.speed=0;player.boat.steerVisual=0;player.x=p.x;player.z=p.z;player.y=.18;player.vx=player.vz=0;state.boats.activeBoatId=world.boat.id;state.boats.passengerOf='';const companion=nearestRideCompanion(9);if(companion)boardNpcPassenger(companion,'boat');updateBoatPanel();updateVehicleControlsUI();auditPlayerMode('board-boat-driver');toast('Barco pronto. Use o manche para navegar.','good',2300);saveState(true);return true;`
- [x] `return` — linha 3681, coluna 630 em `updateBoatPanel()` — `input.mobilityAccelerate=false;input.mobilityBrake=false;player.boating=true;player.boat.passengerOf='';player.boat.passengerUid='';player.boat.passengerBotId='';player.boat.heading=world.boat.heading||0;player.boat.speed=0;player.boat.steerVisual=0;player.x=p.x;player.z=p.z;player.y=.18;player.vx=player.vz=0;state.boats.activeBoatId=world.boat.id;state.boats.passengerOf='';const companion=nearestRideCompanion(9);if(companion)boardNpcPassenger(companion,'boat');updateBoatPanel();updateVehicleControlsUI();auditPlayerMode('board-boat-driver');toast('Barco pronto. Use o manche para navegar.','good',2300);saveState(true);return true;`
- [x] `if` — linha 3685, coluna 5 em `enterBoatAsPassenger()` — `if(!ghost||!target?.boating||target.boatRole==='passenger'){toast('O motorista ou o barco não está mais disponível.','warn');return false;}`
- [x] `return` — linha 3685, coluna 130 em `enterBoatAsPassenger()` — `if(!ghost||!target?.boating||target.boatRole==='passenger'){toast('O motorista ou o barco não está mais disponível.','warn');return false;}`
- [x] `if` — linha 3686, coluna 5 em `enterBoatAsPassenger()` — `if(player.boating){toast('Você já está em um barco.','warn');return false;}`
- [x] `return` — linha 3686, coluna 66 em `enterBoatAsPassenger()` — `if(player.boating){toast('Você já está em um barco.','warn');return false;}`
- [x] `if` — linha 3687, coluna 5 em `enterBoatAsPassenger()` — `if(player.vehicle)exitVehicle(true);`
- [x] `if` — linha 3688, coluna 5 em `enterBoatAsPassenger()` — `if(player.transit.mode||!canEnterMobility(PLAYER_MODES.BOAT_PASSENGER))return false;`
- [x] `return` — linha 3688, coluna 76 em `enterBoatAsPassenger()` — `if(player.transit.mode||!canEnterMobility(PLAYER_MODES.BOAT_PASSENGER))return false;`
- [x] `return` — linha 3689, coluna 420 em `enterBoatAsPassenger()` — `player.boating=true;player.boat.passengerOf=hostUid;player.boat.passengerBotId='';player.boat.speed=0;player.x=ghost.position.x;player.z=ghost.position.z;state.boats.activeBoatId=target.boatId||'lake-boat';state.boats.passengerOf=hostUid;updateBoatPanel();updateVehicleControlsUI();auditPlayerMode('board-remote-boat');toast('Você entrou como passageiro. O motorista controla o barco.','good',2600);saveState(true);return true;`
- [x] `if` — linha 3692, coluna 5 em `exitBoat()` — `if(!player.boating)return false;`
- [x] `return` — linha 3692, coluna 24 em `exitBoat()` — `if(!player.boating)return false;`
- [x] `if` — linha 3693, coluna 5 em `exitBoat()` — `if(!silent&&!validBoatExit()){toast('Encoste a lateral do barco no píer para sair com segurança.','warn',2500);return false;}`
- [x] `return` — linha 3693, coluna 116 em `exitBoat()` — `if(!silent&&!validBoatExit()){toast('Encoste a lateral do barco no píer para sair com segurança.','warn',2500);return false;}`
- [x] `if` — linha 3695, coluna 5 em `exitBoat()` — `if(passengerHost)window.OTTHOS_RTDB?.sendInteraction?.(passengerHost,{type:'boatPassengerLeft'});else if(hostedPassenger)window.OTTHOS_RTDB?.sendInteraction?.(hostedPassenger,{type:'boatEnded'});`
- [x] `else` — linha 3695, coluna 102 em `exitBoat()` — `if(passengerHost)window.OTTHOS_RTDB?.sendInteraction?.(passengerHost,{type:'boatPassengerLeft'});else if(hostedPassenger)window.OTTHOS_RTDB?.sendInteraction?.(hostedPassenger,{type:'boatEnded'});`
- [x] `if` — linha 3695, coluna 107 em `exitBoat()` — `if(passengerHost)window.OTTHOS_RTDB?.sendInteraction?.(passengerHost,{type:'boatPassengerLeft'});else if(hostedPassenger)window.OTTHOS_RTDB?.sendInteraction?.(hostedPassenger,{type:'boatEnded'});`
- [x] `if` — linha 3696, coluna 267 em `exitBoat()` — `releaseNpcPassenger('boat');input.mobilityAccelerate=false;input.mobilityBrake=false;player.boating=false;player.boat.passengerOf='';player.boat.passengerUid='';player.boat.speed=0;player.boat.steerVisual=0;state.boats.passengerOf='';state.boats.activeBoatId='';if(!wasPassenger&&world.boat)window.OTTHOS_RTDB?.releaseBoat?.(world.boat.id);if(world.boat&&!wasPassenger){world.boat.group.position.set(player.x,.1,player.z);world.boat.heading=player.boat.heading;world.boat.group.rotation.y=player.boat.heading;}`
- [x] `if` — linha 3696, coluna 345 em `exitBoat()` — `releaseNpcPassenger('boat');input.mobilityAccelerate=false;input.mobilityBrake=false;player.boating=false;player.boat.passengerOf='';player.boat.passengerUid='';player.boat.speed=0;player.boat.steerVisual=0;state.boats.passengerOf='';state.boats.activeBoatId='';if(!wasPassenger&&world.boat)window.OTTHOS_RTDB?.releaseBoat?.(world.boat.id);if(world.boat&&!wasPassenger){world.boat.group.position.set(player.x,.1,player.z);world.boat.heading=player.boat.heading;world.boat.group.rotation.y=player.boat.heading;}`
- [x] `if` — linha 3697, coluna 266 em `exitBoat()` — `const safe=safeBoatExitPoint();player.x=safe.x;player.z=safe.z;player.y=groundHeightAt(safe.x,safe.z);player.vx=player.vy=player.vz=0;player.grounded=true;rememberSafePlayerPosition(true);updateBoatPanel();updateVehicleControlsUI();auditPlayerMode('exit-boat');if(!silent)toast('Você desembarcou no píer.','good');saveState(true);return true;`
- [x] `return` — linha 3697, coluna 335 em `exitBoat()` — `const safe=safeBoatExitPoint();player.x=safe.x;player.z=safe.z;player.y=groundHeightAt(safe.x,safe.z);player.vx=player.vy=player.vz=0;player.grounded=true;rememberSafePlayerPosition(true);updateBoatPanel();updateVehicleControlsUI();auditPlayerMode('exit-boat');if(!silent)toast('Você desembarcou no píer.','good');saveState(true);return true;`
- [x] `if` — linha 3700, coluna 28 em `updateBoatPhysics()` — `const boat=player.boat;if(boat.passengerOf){const ghost=world.ghosts.get(boat.passengerOf),target=ghost?.userData?.target;if(!ghost||!target){boat.hostMissingAt=boat.hostMissingAt||performance.now();if(performance.now()-boat.hostMissingAt>3500){toast('O motorista saiu. Você voltou ao píer.','warn');player.x=-24.7;player.z=52;exitBoat(true);}player.vx=player.vz=0;return;}boat.hostMissingAt=0;const tx=Number(target.x||ghost.position.x),tz=Number(target.z||ghost.position.z);player.vx=clamp((tx-player.x)*8,-18,18);player.vz=clamp((tz-player.z)*8,-18,18);boat.heading=Number(target.r||boat.heading);player.facing=boat.heading;return;}`
- [x] `if` — linha 3700, coluna 127 em `updateBoatPhysics()` — `const boat=player.boat;if(boat.passengerOf){const ghost=world.ghosts.get(boat.passengerOf),target=ghost?.userData?.target;if(!ghost||!target){boat.hostMissingAt=boat.hostMissingAt||performance.now();if(performance.now()-boat.hostMissingAt>3500){toast('O motorista saiu. Você voltou ao píer.','warn');player.x=-24.7;player.z=52;exitBoat(true);}player.vx=player.vz=0;return;}boat.hostMissingAt=0;const tx=Number(target.x||ghost.position.x),tz=Number(target.z||ghost.position.z);player.vx=clamp((tx-player.x)*8,-18,18);player.vz=clamp((tz-player.z)*8,-18,18);boat.heading=Number(target.r||boat.heading);player.facing=boat.heading;return;}`
- [x] `if` — linha 3700, coluna 204 em `updateBoatPhysics()` — `const boat=player.boat;if(boat.passengerOf){const ghost=world.ghosts.get(boat.passengerOf),target=ghost?.userData?.target;if(!ghost||!target){boat.hostMissingAt=boat.hostMissingAt||performance.now();if(performance.now()-boat.hostMissingAt>3500){toast('O motorista saiu. Você voltou ao píer.','warn');player.x=-24.7;player.z=52;exitBoat(true);}player.vx=player.vz=0;return;}boat.hostMissingAt=0;const tx=Number(target.x||ghost.position.x),tz=Number(target.z||ghost.position.z);player.vx=clamp((tx-player.x)*8,-18,18);player.vz=clamp((tz-player.z)*8,-18,18);boat.heading=Number(target.r||boat.heading);player.facing=boat.heading;return;}`
- [x] `return` — linha 3700, coluna 370 em `updateBoatPhysics()` — `const boat=player.boat;if(boat.passengerOf){const ghost=world.ghosts.get(boat.passengerOf),target=ghost?.userData?.target;if(!ghost||!target){boat.hostMissingAt=boat.hostMissingAt||performance.now();if(performance.now()-boat.hostMissingAt>3500){toast('O motorista saiu. Você voltou ao píer.','warn');player.x=-24.7;player.z=52;exitBoat(true);}player.vx=player.vz=0;return;}boat.hostMissingAt=0;const tx=Number(target.x||ghost.position.x),tz=Number(target.z||ghost.position.z);player.vx=clamp((tx-player.x)*8,-18,18);player.vz=clamp((tz-player.z)*8,-18,18);boat.heading=Number(target.r||boat.heading);player.facing=boat.heading;return;}`
- [x] `return` — linha 3700, coluna 632 em `updateBoatPhysics()` — `const boat=player.boat;if(boat.passengerOf){const ghost=world.ghosts.get(boat.passengerOf),target=ghost?.userData?.target;if(!ghost||!target){boat.hostMissingAt=boat.hostMissingAt||performance.now();if(performance.now()-boat.hostMissingAt>3500){toast('O motorista saiu. Você voltou ao píer.','warn');player.x=-24.7;player.z=52;exitBoat(true);}player.vx=player.vz=0;return;}boat.hostMissingAt=0;const tx=Number(target.x||ghost.position.x),tz=Number(target.z||ghost.position.z);player.vx=clamp((tx-player.x)*8,-18,18);player.vz=clamp((tz-player.z)*8,-18,18);boat.heading=Number(target.r||boat.heading);player.facing=boat.heading;return;}`
- [x] `if` — linha 3703, coluna 5 em `updateBoatPhysics()` — `if(command.brake)boat.speed=approachNumber(boat.speed,0,(7.2+Math.abs(boat.speed)*.8)*dt);else{const crossing=boat.speed*throttle<-.08;boat.speed+=throttle*8.5*(crossing?1.8:1)*dt;}`
- [x] `else` — linha 3703, coluna 95 em `updateBoatPhysics()` — `if(command.brake)boat.speed=approachNumber(boat.speed,0,(7.2+Math.abs(boat.speed)*.8)*dt);else{const crossing=boat.speed*throttle<-.08;boat.speed+=throttle*8.5*(crossing?1.8:1)*dt;}`
- [x] `if` — linha 3704, coluna 5 em `updateBoatPhysics()` — `if(!throttle&&!command.brake)boat.speed*=Math.pow(.12,dt);if(Math.abs(boat.speed)<.02&&!throttle)boat.speed=0;boat.speed=clamp(boat.speed,-3.4,8.8);boat.steerVisual=lerp(boat.steerVisual||0,steer,Math.min(1,dt*6.5));const authority=Math.max(clamp(Math.abs(boat.speed)/2.8,0,1),Math.abs(throttle)>.1?.12:0);const turnRate=1.5/(1+Math.abs(boat.speed)*.075);boat.heading+=boat.steerVisual*turnRate*authority*dt*(boat.speed<-.08?-1:1);player.vx=Math.sin(boat.heading)*boat.speed;player.vz=Math.cos(boat.heading)*boat.speed;player.facing=boat.heading;updateMobilityControlLabels();`
- [x] `if` — linha 3704, coluna 63 em `updateBoatPhysics()` — `if(!throttle&&!command.brake)boat.speed*=Math.pow(.12,dt);if(Math.abs(boat.speed)<.02&&!throttle)boat.speed=0;boat.speed=clamp(boat.speed,-3.4,8.8);boat.steerVisual=lerp(boat.steerVisual||0,steer,Math.min(1,dt*6.5));const authority=Math.max(clamp(Math.abs(boat.speed)/2.8,0,1),Math.abs(throttle)>.1?.12:0);const turnRate=1.5/(1+Math.abs(boat.speed)*.075);boat.heading+=boat.steerVisual*turnRate*authority*dt*(boat.speed<-.08?-1:1);player.vx=Math.sin(boat.heading)*boat.speed;player.vz=Math.cos(boat.heading)*boat.speed;player.facing=boat.heading;updateMobilityControlLabels();`
- [x] `if` — linha 3706, coluna 39 em `constrainBoat()` — `function constrainBoat(prevX,prevZ){if(!player.boating)return;if(!isInsideLakeNavigable(player.x,player.z)){player.x=prevX;player.z=prevZ;player.boat.speed*=-.18;player.vx=player.vz=0;}if(world.boat){world.boat.group.position.set(player.x,.1,player.z);world.boat.group.rotation.y=player.boat.heading;world.boat.heading=player.boat.heading;}state.boats.lastPosition={x:+player.x.toFixed(2),z:+player.z.toFixed(2),heading:+player.boat.heading.toFixed(3)};}`
- [x] `return` — linha 3706, coluna 58 em `constrainBoat()` — `function constrainBoat(prevX,prevZ){if(!player.boating)return;if(!isInsideLakeNavigable(player.x,player.z)){player.x=prevX;player.z=prevZ;player.boat.speed*=-.18;player.vx=player.vz=0;}if(world.boat){world.boat.group.position.set(player.x,.1,player.z);world.boat.group.rotation.y=player.boat.heading;world.boat.heading=player.boat.heading;}state.boats.lastPosition={x:+player.x.toFixed(2),z:+player.z.toFixed(2),heading:+player.boat.heading.toFixed(3)};}`
- [x] `if` — linha 3706, coluna 65 em `constrainBoat()` — `function constrainBoat(prevX,prevZ){if(!player.boating)return;if(!isInsideLakeNavigable(player.x,player.z)){player.x=prevX;player.z=prevZ;player.boat.speed*=-.18;player.vx=player.vz=0;}if(world.boat){world.boat.group.position.set(player.x,.1,player.z);world.boat.group.rotation.y=player.boat.heading;world.boat.heading=player.boat.heading;}state.boats.lastPosition={x:+player.x.toFixed(2),z:+player.z.toFixed(2),heading:+player.boat.heading.toFixed(3)};}`
- [x] `if` — linha 3706, coluna 188 em `constrainBoat()` — `function constrainBoat(prevX,prevZ){if(!player.boating)return;if(!isInsideLakeNavigable(player.x,player.z)){player.x=prevX;player.z=prevZ;player.boat.speed*=-.18;player.vx=player.vz=0;}if(world.boat){world.boat.group.position.set(player.x,.1,player.z);world.boat.group.rotation.y=player.boat.heading;world.boat.heading=player.boat.heading;}state.boats.lastPosition={x:+player.x.toFixed(2),z:+player.z.toFixed(2),heading:+player.boat.heading.toFixed(3)};}`
- [x] `for` — linha 3707, coluna 51 em `weightedFish()` — `function weightedFish(){let r=Math.random()*100;for(const fish of FISH_SPECIES){r-=fish.weight;if(r<=0)return fish;}return FISH_SPECIES[0];}`
- [x] `if` — linha 3707, coluna 98 em `weightedFish()` — `function weightedFish(){let r=Math.random()*100;for(const fish of FISH_SPECIES){r-=fish.weight;if(r<=0)return fish;}return FISH_SPECIES[0];}`
- [x] `return` — linha 3707, coluna 106 em `weightedFish()` — `function weightedFish(){let r=Math.random()*100;for(const fish of FISH_SPECIES){r-=fish.weight;if(r<=0)return fish;}return FISH_SPECIES[0];}`
- [x] `return` — linha 3707, coluna 119 em `weightedFish()` — `function weightedFish(){let r=Math.random()*100;for(const fish of FISH_SPECIES){r-=fish.weight;if(r<=0)return fish;}return FISH_SPECIES[0];}`
- [x] `if` — linha 3709, coluna 5 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `return` — linha 3709, coluna 73 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `if` — linha 3709, coluna 81 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `return` — linha 3709, coluna 170 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `if` — linha 3709, coluna 178 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `return` — linha 3709, coluna 247 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `if` — linha 3709, coluna 255 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `return` — linha 3709, coluna 333 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `if` — linha 3709, coluna 341 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `return` — linha 3709, coluna 445 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `if` — linha 3709, coluna 531 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `return` — linha 3709, coluna 615 em `startFishing()` — `if(fishingSession){toast('Finalize a pesca atual primeiro.','warn');return;}if((state.inventory.fishingRod||0)<1){toast('Você precisa de uma vara de pesca.','warn');return;}if((state.inventory.bait||0)<1){toast('Você ficou sem isca.','warn');return;}if(source==='boat'&&!player.boating){toast('Entre no barco primeiro.','warn');return;}if(source!=='boat'&&!isNearFishingArea()){toast('Pesque somente nos pontos marcados da margem.','warn');return;}const wait=Math.max(0,6500-(Date.now()-Number(state.fishing.lastAttempt||0)));if(wait>0){toast(\`Aguarde ${Math.ceil(wait/1000)} s para tentar novamente.\`,'warn');return;}`
- [x] `if` — linha 3713, coluna 25 em `startFishing()` — `cast.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;cast.disabled=true;state.inventory.bait--;state.fishing.lastAttempt=Date.now();saveState(true);status.textContent='🎣 Aguarde…';meter.style.animation='fishingWait 2.4s linear forwards';castFishingVisual();const hookDelay=1400+Math.random()*1700;fishingSession.hookTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token||els.modal.hidden){cancelFishingSession();return;}status.textContent='⚡ Fisgou!';pull.hidden=false;hookFishingVisual();beep(880,100,'sine');vibrate([35,35,55]);fishingSession.hookedAt=performance.now();fishingSession.escapeTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token)return;status.textContent='💨 Escapou';pull.hidden=true;pullFishingVisual(false);fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);},2700);},hookDelay);};`
- [x] `return` — linha 3713, coluna 74 em `startFishing()` — `cast.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;cast.disabled=true;state.inventory.bait--;state.fishing.lastAttempt=Date.now();saveState(true);status.textContent='🎣 Aguarde…';meter.style.animation='fishingWait 2.4s linear forwards';castFishingVisual();const hookDelay=1400+Math.random()*1700;fishingSession.hookTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token||els.modal.hidden){cancelFishingSession();return;}status.textContent='⚡ Fisgou!';pull.hidden=false;hookFishingVisual();beep(880,100,'sine');vibrate([35,35,55]);fishingSession.hookedAt=performance.now();fishingSession.escapeTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token)return;status.textContent='💨 Escapou';pull.hidden=true;pullFishingVisual(false);fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);},2700);},hookDelay);};`
- [x] `if` — linha 3713, coluna 366 em `startFishing()` — `cast.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;cast.disabled=true;state.inventory.bait--;state.fishing.lastAttempt=Date.now();saveState(true);status.textContent='🎣 Aguarde…';meter.style.animation='fishingWait 2.4s linear forwards';castFishingVisual();const hookDelay=1400+Math.random()*1700;fishingSession.hookTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token||els.modal.hidden){cancelFishingSession();return;}status.textContent='⚡ Fisgou!';pull.hidden=false;hookFishingVisual();beep(880,100,'sine');vibrate([35,35,55]);fishingSession.hookedAt=performance.now();fishingSession.escapeTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token)return;status.textContent='💨 Escapou';pull.hidden=true;pullFishingVisual(false);fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);},2700);},hookDelay);};`
- [x] `return` — linha 3713, coluna 457 em `startFishing()` — `cast.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;cast.disabled=true;state.inventory.bait--;state.fishing.lastAttempt=Date.now();saveState(true);status.textContent='🎣 Aguarde…';meter.style.animation='fishingWait 2.4s linear forwards';castFishingVisual();const hookDelay=1400+Math.random()*1700;fishingSession.hookTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token||els.modal.hidden){cancelFishingSession();return;}status.textContent='⚡ Fisgou!';pull.hidden=false;hookFishingVisual();beep(880,100,'sine');vibrate([35,35,55]);fishingSession.hookedAt=performance.now();fishingSession.escapeTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token)return;status.textContent='💨 Escapou';pull.hidden=true;pullFishingVisual(false);fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);},2700);},hookDelay);};`
- [x] `if` — linha 3713, coluna 660 em `startFishing()` — `cast.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;cast.disabled=true;state.inventory.bait--;state.fishing.lastAttempt=Date.now();saveState(true);status.textContent='🎣 Aguarde…';meter.style.animation='fishingWait 2.4s linear forwards';castFishingVisual();const hookDelay=1400+Math.random()*1700;fishingSession.hookTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token||els.modal.hidden){cancelFishingSession();return;}status.textContent='⚡ Fisgou!';pull.hidden=false;hookFishingVisual();beep(880,100,'sine');vibrate([35,35,55]);fishingSession.hookedAt=performance.now();fishingSession.escapeTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token)return;status.textContent='💨 Escapou';pull.hidden=true;pullFishingVisual(false);fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);},2700);},hookDelay);};`
- [x] `return` — linha 3713, coluna 709 em `startFishing()` — `cast.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;cast.disabled=true;state.inventory.bait--;state.fishing.lastAttempt=Date.now();saveState(true);status.textContent='🎣 Aguarde…';meter.style.animation='fishingWait 2.4s linear forwards';castFishingVisual();const hookDelay=1400+Math.random()*1700;fishingSession.hookTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token||els.modal.hidden){cancelFishingSession();return;}status.textContent='⚡ Fisgou!';pull.hidden=false;hookFishingVisual();beep(880,100,'sine');vibrate([35,35,55]);fishingSession.hookedAt=performance.now();fishingSession.escapeTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token)return;status.textContent='💨 Escapou';pull.hidden=true;pullFishingVisual(false);fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);},2700);},hookDelay);};`
- [x] `if` — linha 3713, coluna 832 em `startFishing()` — `cast.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;cast.disabled=true;state.inventory.bait--;state.fishing.lastAttempt=Date.now();saveState(true);status.textContent='🎣 Aguarde…';meter.style.animation='fishingWait 2.4s linear forwards';castFishingVisual();const hookDelay=1400+Math.random()*1700;fishingSession.hookTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token||els.modal.hidden){cancelFishingSession();return;}status.textContent='⚡ Fisgou!';pull.hidden=false;hookFishingVisual();beep(880,100,'sine');vibrate([35,35,55]);fishingSession.hookedAt=performance.now();fishingSession.escapeTimer=setTimeout(()=>{if(!fishingSession||fishingSession.token!==token)return;status.textContent='💨 Escapou';pull.hidden=true;pullFishingVisual(false);fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);},2700);},hookDelay);};`
- [x] `if` — linha 3714, coluna 25 em `startFishing()` — `pull.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;clearTimeout(fishingSession.escapeTimer);const reaction=performance.now()-Number(fishingSession.hookedAt||performance.now()),success=reaction<2400&&Math.random()<.88;pull.hidden=true;if(!success){status.textContent='💨 Escapou';pullFishingVisual(false);saveState();fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);return;}const fish=weightedFish(),size=+(fish.min+Math.random()*(fish.max-fish.min)).toFixed(2),catchId=uid();state.inventory.rawFish=(state.inventory.rawFish||0)+1;state.fishing.catches.push({id:catchId,species:fish.name,size,rarity:fish.rarity,caughtAt:Date.now(),source,cooperative:!!options.cooperative});state.fishing.catches=state.fishing.catches.slice(-200);state.fishing.species[fish.name]=(state.fishing.species[fish.name]||0)+1;let xp=fish.xp,coins=fish.coins;if(options.cooperative&&options.requestId&&!state.fishing.cooperative`
- [x] `return` — linha 3714, coluna 74 em `startFishing()` — `pull.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;clearTimeout(fishingSession.escapeTimer);const reaction=performance.now()-Number(fishingSession.hookedAt||performance.now()),success=reaction<2400&&Math.random()<.88;pull.hidden=true;if(!success){status.textContent='💨 Escapou';pullFishingVisual(false);saveState();fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);return;}const fish=weightedFish(),size=+(fish.min+Math.random()*(fish.max-fish.min)).toFixed(2),catchId=uid();state.inventory.rawFish=(state.inventory.rawFish||0)+1;state.fishing.catches.push({id:catchId,species:fish.name,size,rarity:fish.rarity,caughtAt:Date.now(),source,cooperative:!!options.cooperative});state.fishing.catches=state.fishing.catches.slice(-200);state.fishing.species[fish.name]=(state.fishing.species[fish.name]||0)+1;let xp=fish.xp,coins=fish.coins;if(options.cooperative&&options.requestId&&!state.fishing.cooperative`
- [x] `if` — linha 3714, coluna 264 em `startFishing()` — `pull.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;clearTimeout(fishingSession.escapeTimer);const reaction=performance.now()-Number(fishingSession.hookedAt||performance.now()),success=reaction<2400&&Math.random()<.88;pull.hidden=true;if(!success){status.textContent='💨 Escapou';pullFishingVisual(false);saveState();fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);return;}const fish=weightedFish(),size=+(fish.min+Math.random()*(fish.max-fish.min)).toFixed(2),catchId=uid();state.inventory.rawFish=(state.inventory.rawFish||0)+1;state.fishing.catches.push({id:catchId,species:fish.name,size,rarity:fish.rarity,caughtAt:Date.now(),source,cooperative:!!options.cooperative});state.fishing.catches=state.fishing.catches.slice(-200);state.fishing.species[fish.name]=(state.fishing.species[fish.name]||0)+1;let xp=fish.xp,coins=fish.coins;if(options.cooperative&&options.requestId&&!state.fishing.cooperative`
- [x] `if` — linha 3714, coluna 388 em `startFishing()` — `pull.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;clearTimeout(fishingSession.escapeTimer);const reaction=performance.now()-Number(fishingSession.hookedAt||performance.now()),success=reaction<2400&&Math.random()<.88;pull.hidden=true;if(!success){status.textContent='💨 Escapou';pullFishingVisual(false);saveState();fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);return;}const fish=weightedFish(),size=+(fish.min+Math.random()*(fish.max-fish.min)).toFixed(2),catchId=uid();state.inventory.rawFish=(state.inventory.rawFish||0)+1;state.fishing.catches.push({id:catchId,species:fish.name,size,rarity:fish.rarity,caughtAt:Date.now(),source,cooperative:!!options.cooperative});state.fishing.catches=state.fishing.catches.slice(-200);state.fishing.species[fish.name]=(state.fishing.species[fish.name]||0)+1;let xp=fish.xp,coins=fish.coins;if(options.cooperative&&options.requestId&&!state.fishing.cooperative`
- [x] `return` — linha 3714, coluna 468 em `startFishing()` — `pull.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;clearTimeout(fishingSession.escapeTimer);const reaction=performance.now()-Number(fishingSession.hookedAt||performance.now()),success=reaction<2400&&Math.random()<.88;pull.hidden=true;if(!success){status.textContent='💨 Escapou';pullFishingVisual(false);saveState();fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);return;}const fish=weightedFish(),size=+(fish.min+Math.random()*(fish.max-fish.min)).toFixed(2),catchId=uid();state.inventory.rawFish=(state.inventory.rawFish||0)+1;state.fishing.catches.push({id:catchId,species:fish.name,size,rarity:fish.rarity,caughtAt:Date.now(),source,cooperative:!!options.cooperative});state.fishing.catches=state.fishing.catches.slice(-200);state.fishing.species[fish.name]=(state.fishing.species[fish.name]||0)+1;let xp=fish.xp,coins=fish.coins;if(options.cooperative&&options.requestId&&!state.fishing.cooperative`
- [x] `if` — linha 3714, coluna 938 em `startFishing()` — `pull.onclick=()=>{if(!fishingSession||fishingSession.token!==token)return;clearTimeout(fishingSession.escapeTimer);const reaction=performance.now()-Number(fishingSession.hookedAt||performance.now()),success=reaction<2400&&Math.random()<.88;pull.hidden=true;if(!success){status.textContent='💨 Escapou';pullFishingVisual(false);saveState();fishingSession.finishTimer=setTimeout(()=>{if(fishingSession?.token===token)fishingSession=null;stopFishingVisual();},800);return;}const fish=weightedFish(),size=+(fish.min+Math.random()*(fish.max-fish.min)).toFixed(2),catchId=uid();state.inventory.rawFish=(state.inventory.rawFish||0)+1;state.fishing.catches.push({id:catchId,species:fish.name,size,rarity:fish.rarity,caughtAt:Date.now(),source,cooperative:!!options.cooperative});state.fishing.catches=state.fishing.catches.slice(-200);state.fishing.species[fish.name]=(state.fishing.species[fish.name]||0)+1;let xp=fish.xp,coins=fish.coins;if(options.cooperative&&options.requestId&&!state.fishing.cooperative`

## `19-campfire-hunting-house-extensions.js`

- [x] `if` — linha 3719, coluna 33 em `campfireAllowed()` — `function campfireAllowed(x,z){if(Math.hypot(x+70,z+62)>14)return false;if(waterAt(x,z))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap({x,z,w:3,d:3},r,.5)))return false;if(world.houses.some(h=>Math.hypot(x-h.x,z-h.z)<10))return false;return true;}`
- [x] `return` — linha 3719, coluna 61 em `campfireAllowed()` — `function campfireAllowed(x,z){if(Math.hypot(x+70,z+62)>14)return false;if(waterAt(x,z))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap({x,z,w:3,d:3},r,.5)))return false;if(world.houses.some(h=>Math.hypot(x-h.x,z-h.z)<10))return false;return true;}`
- [x] `if` — linha 3719, coluna 74 em `campfireAllowed()` — `function campfireAllowed(x,z){if(Math.hypot(x+70,z+62)>14)return false;if(waterAt(x,z))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap({x,z,w:3,d:3},r,.5)))return false;if(world.houses.some(h=>Math.hypot(x-h.x,z-h.z)<10))return false;return true;}`
- [x] `return` — linha 3719, coluna 90 em `campfireAllowed()` — `function campfireAllowed(x,z){if(Math.hypot(x+70,z+62)>14)return false;if(waterAt(x,z))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap({x,z,w:3,d:3},r,.5)))return false;if(world.houses.some(h=>Math.hypot(x-h.x,z-h.z)<10))return false;return true;}`
- [x] `if` — linha 3719, coluna 103 em `campfireAllowed()` — `function campfireAllowed(x,z){if(Math.hypot(x+70,z+62)>14)return false;if(waterAt(x,z))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap({x,z,w:3,d:3},r,.5)))return false;if(world.houses.some(h=>Math.hypot(x-h.x,z-h.z)<10))return false;return true;}`
- [x] `return` — linha 3719, coluna 163 em `campfireAllowed()` — `function campfireAllowed(x,z){if(Math.hypot(x+70,z+62)>14)return false;if(waterAt(x,z))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap({x,z,w:3,d:3},r,.5)))return false;if(world.houses.some(h=>Math.hypot(x-h.x,z-h.z)<10))return false;return true;}`
- [x] `if` — linha 3719, coluna 176 em `campfireAllowed()` — `function campfireAllowed(x,z){if(Math.hypot(x+70,z+62)>14)return false;if(waterAt(x,z))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap({x,z,w:3,d:3},r,.5)))return false;if(world.houses.some(h=>Math.hypot(x-h.x,z-h.z)<10))return false;return true;}`
- [x] `return` — linha 3719, coluna 228 em `campfireAllowed()` — `function campfireAllowed(x,z){if(Math.hypot(x+70,z+62)>14)return false;if(waterAt(x,z))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap({x,z,w:3,d:3},r,.5)))return false;if(world.houses.some(h=>Math.hypot(x-h.x,z-h.z)<10))return false;return true;}`
- [x] `return` — linha 3719, coluna 241 em `campfireAllowed()` — `function campfireAllowed(x,z){if(Math.hypot(x+70,z+62)>14)return false;if(waterAt(x,z))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap({x,z,w:3,d:3},r,.5)))return false;if(world.houses.some(h=>Math.hypot(x-h.x,z-h.z)<10))return false;return true;}`
- [x] `if` — linha 3721, coluna 5 em `spawnCampfire()` — `if(!data?.id||world.campfires.some(x=>x.data.id===data.id))return;const g=new THREE.Group();g.position.set(data.x,0,data.z);worldGroup.add(g);for(const a of [-.55,.55]){const log=premiumCylinder(.18,1.6,0x76502d,a,.2,0,g,8);log.rotation.z=Math.PI/2;log.rotation.y=a>0?Math.PI/4:-Math.PI/4;}const flame=new THREE.Mesh(new THREE.ConeGeometry(.42,1.15,7),mat(0xff7a18,{emissive:0xff4a00,emissiveIntensity:1.4,transparent:true,opacity:.9}));flame.position.y=.9;g.add(flame);const record={data:{...data,remote},group:g,flame,interactable:null};record.interactable=registerInteractable({id:\`campfire-${data.id}\`,type:'campfire',icon:'🔥',label:'Usar fogueira',x:data.x,z:data.z,radius:3.2,priority:150,action:()=>openCampfire(data.id)});world.campfires.push(record);`
- [x] `return` — linha 3721, coluna 64 em `spawnCampfire()` — `if(!data?.id||world.campfires.some(x=>x.data.id===data.id))return;const g=new THREE.Group();g.position.set(data.x,0,data.z);worldGroup.add(g);for(const a of [-.55,.55]){const log=premiumCylinder(.18,1.6,0x76502d,a,.2,0,g,8);log.rotation.z=Math.PI/2;log.rotation.y=a>0?Math.PI/4:-Math.PI/4;}const flame=new THREE.Mesh(new THREE.ConeGeometry(.42,1.15,7),mat(0xff7a18,{emissive:0xff4a00,emissiveIntensity:1.4,transparent:true,opacity:.9}));flame.position.y=.9;g.add(flame);const record={data:{...data,remote},group:g,flame,interactable:null};record.interactable=registerInteractable({id:\`campfire-${data.id}\`,type:'campfire',icon:'🔥',label:'Usar fogueira',x:data.x,z:data.z,radius:3.2,priority:150,action:()=>openCampfire(data.id)});world.campfires.push(record);`
- [x] `for` — linha 3721, coluna 147 em `spawnCampfire()` — `if(!data?.id||world.campfires.some(x=>x.data.id===data.id))return;const g=new THREE.Group();g.position.set(data.x,0,data.z);worldGroup.add(g);for(const a of [-.55,.55]){const log=premiumCylinder(.18,1.6,0x76502d,a,.2,0,g,8);log.rotation.z=Math.PI/2;log.rotation.y=a>0?Math.PI/4:-Math.PI/4;}const flame=new THREE.Mesh(new THREE.ConeGeometry(.42,1.15,7),mat(0xff7a18,{emissive:0xff4a00,emissiveIntensity:1.4,transparent:true,opacity:.9}));flame.position.y=.9;g.add(flame);const record={data:{...data,remote},group:g,flame,interactable:null};record.interactable=registerInteractable({id:\`campfire-${data.id}\`,type:'campfire',icon:'🔥',label:'Usar fogueira',x:data.x,z:data.z,radius:3.2,priority:150,action:()=>openCampfire(data.id)});world.campfires.push(record);`
- [x] `return` — linha 3723, coluna 46 em `nearestActiveCampfire()` — `function nearestActiveCampfire(radius=999){return world.campfires.filter(c=>!c.data.expired&&Number(c.data.expiresAt||0)>Date.now()).sort((a,b)=>distance2D(player,a.data)-distance2D(player,b.data)).find(c=>distance2D(player,c.data)<=radius)||null;}`
- [x] `if` — linha 3724, coluna 94 em `nearestActiveCampfire()` — `async function buildCampfire(){const x=Math.round(player.x*2)/2,z=Math.round(player.z*2)/2;if(!campfireAllowed(x,z)){toast('Use a área demarcada do acampamento.','warn');return;}if((state.inventory.wood||0)<3){toast('Você precisa de 3 madeiras.','warn');return;}if(!(await confirmModal('Construir fogueira','Gastar 3 madeiras para montar a fogueira?','Construir','Cancelar')))return;state.inventory.wood-=3;const data={id:uid(),ownerId:state.profile.playerId,x,z,createdAt:Date.now(),expiresAt:Date.now()+10*60*1000,cooking:null};state.campfires.push(data);spawnCampfire(data);saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Fogueira acesa com segurança.','good');}`
- [x] `return` — linha 3724, coluna 173 em `nearestActiveCampfire()` — `async function buildCampfire(){const x=Math.round(player.x*2)/2,z=Math.round(player.z*2)/2;if(!campfireAllowed(x,z)){toast('Use a área demarcada do acampamento.','warn');return;}if((state.inventory.wood||0)<3){toast('Você precisa de 3 madeiras.','warn');return;}if(!(await confirmModal('Construir fogueira','Gastar 3 madeiras para montar a fogueira?','Construir','Cancelar')))return;state.inventory.wood-=3;const data={id:uid(),ownerId:state.profile.playerId,x,z,createdAt:Date.now(),expiresAt:Date.now()+10*60*1000,cooking:null};state.campfires.push(data);spawnCampfire(data);saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Fogueira acesa com segurança.','good');}`
- [x] `if` — linha 3724, coluna 181 em `nearestActiveCampfire()` — `async function buildCampfire(){const x=Math.round(player.x*2)/2,z=Math.round(player.z*2)/2;if(!campfireAllowed(x,z)){toast('Use a área demarcada do acampamento.','warn');return;}if((state.inventory.wood||0)<3){toast('Você precisa de 3 madeiras.','warn');return;}if(!(await confirmModal('Construir fogueira','Gastar 3 madeiras para montar a fogueira?','Construir','Cancelar')))return;state.inventory.wood-=3;const data={id:uid(),ownerId:state.profile.playerId,x,z,createdAt:Date.now(),expiresAt:Date.now()+10*60*1000,cooking:null};state.campfires.push(data);spawnCampfire(data);saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Fogueira acesa com segurança.','good');}`
- [x] `return` — linha 3724, coluna 257 em `nearestActiveCampfire()` — `async function buildCampfire(){const x=Math.round(player.x*2)/2,z=Math.round(player.z*2)/2;if(!campfireAllowed(x,z)){toast('Use a área demarcada do acampamento.','warn');return;}if((state.inventory.wood||0)<3){toast('Você precisa de 3 madeiras.','warn');return;}if(!(await confirmModal('Construir fogueira','Gastar 3 madeiras para montar a fogueira?','Construir','Cancelar')))return;state.inventory.wood-=3;const data={id:uid(),ownerId:state.profile.playerId,x,z,createdAt:Date.now(),expiresAt:Date.now()+10*60*1000,cooking:null};state.campfires.push(data);spawnCampfire(data);saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Fogueira acesa com segurança.','good');}`
- [x] `if` — linha 3724, coluna 265 em `nearestActiveCampfire()` — `async function buildCampfire(){const x=Math.round(player.x*2)/2,z=Math.round(player.z*2)/2;if(!campfireAllowed(x,z)){toast('Use a área demarcada do acampamento.','warn');return;}if((state.inventory.wood||0)<3){toast('Você precisa de 3 madeiras.','warn');return;}if(!(await confirmModal('Construir fogueira','Gastar 3 madeiras para montar a fogueira?','Construir','Cancelar')))return;state.inventory.wood-=3;const data={id:uid(),ownerId:state.profile.playerId,x,z,createdAt:Date.now(),expiresAt:Date.now()+10*60*1000,cooking:null};state.campfires.push(data);spawnCampfire(data);saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Fogueira acesa com segurança.','good');}`
- [x] `return` — linha 3724, coluna 379 em `nearestActiveCampfire()` — `async function buildCampfire(){const x=Math.round(player.x*2)/2,z=Math.round(player.z*2)/2;if(!campfireAllowed(x,z)){toast('Use a área demarcada do acampamento.','warn');return;}if((state.inventory.wood||0)<3){toast('Você precisa de 3 madeiras.','warn');return;}if(!(await confirmModal('Construir fogueira','Gastar 3 madeiras para montar a fogueira?','Construir','Cancelar')))return;state.inventory.wood-=3;const data={id:uid(),ownerId:state.profile.playerId,x,z,createdAt:Date.now(),expiresAt:Date.now()+10*60*1000,cooking:null};state.campfires.push(data);spawnCampfire(data);saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Fogueira acesa com segurança.','good');}`
- [x] `if` — linha 3725, coluna 50 em `finishCampfireCooking()` — `function finishCampfireCooking(data,cookingId){if(!data?.cooking||data.cooking.id!==cookingId||data.cooking.collected)return;data.cooking.collected=true;data.cooking=null;state.inventory.cookedFish=(state.inventory.cookedFish||0)+1;saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('O peixe ficou assado!','good',2200);beep(740,100,'sine');}`
- [x] `return` — linha 3725, coluna 121 em `finishCampfireCooking()` — `function finishCampfireCooking(data,cookingId){if(!data?.cooking||data.cooking.id!==cookingId||data.cooking.collected)return;data.cooking.collected=true;data.cooking=null;state.inventory.cookedFish=(state.inventory.cookedFish||0)+1;saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('O peixe ficou assado!','good',2200);beep(740,100,'sine');}`
- [x] `if` — linha 3726, coluna 100 em `openCampfire()` — `function openCampfire(id){const record=world.campfires.find(c=>c.data.id===id),data=record?.data;if(!data||Number(data.expiresAt||0)<=Date.now()){toast('Esta fogueira já apagou.','warn');return;}const mine=!data.remote;openModal('Fogueira',\`<div class="activity-card"><div class="activity-icon">🔥</div><h3>Fogueira do acampamento</h3><p>${data.cooking?'Peixe assando...':'Sente-se, cozinhe seu peixe ou convide um amigo.'}</p><div class="modal-actions"><button class="btn primary" data-cook ${!mine||data.cooking||state.inventory.rawFish<1?'disabled':''}>Assar 1 peixe cru</button><button class="btn" data-sit>Sentar ao redor</button><button class="btn good" data-eat ${state.inventory.cookedFish<1?'disabled':''}>Comer peixe assado</button></div></div>\`,root=>{`
- [x] `return` — linha 3726, coluna 190 em `openCampfire()` — `function openCampfire(id){const record=world.campfires.find(c=>c.data.id===id),data=record?.data;if(!data||Number(data.expiresAt||0)<=Date.now()){toast('Esta fogueira já apagou.','warn');return;}const mine=!data.remote;openModal('Fogueira',\`<div class="activity-card"><div class="activity-icon">🔥</div><h3>Fogueira do acampamento</h3><p>${data.cooking?'Peixe assando...':'Sente-se, cozinhe seu peixe ou convide um amigo.'}</p><div class="modal-actions"><button class="btn primary" data-cook ${!mine||data.cooking||state.inventory.rawFish<1?'disabled':''}>Assar 1 peixe cru</button><button class="btn" data-sit>Sentar ao redor</button><button class="btn good" data-eat ${state.inventory.cookedFish<1?'disabled':''}>Comer peixe assado</button></div></div>\`,root=>{`
- [x] `if` — linha 3727, coluna 226 em `openCampfire()` — `$('[data-sit]',root).onclick=()=>{player.sitUntil=performance.now()+4500;state.needs.fun=clamp(state.needs.fun+8,0,100);closeModal();toast('Você descansou perto da fogueira.','good');};$('[data-eat]',root).onclick=()=>{if(state.inventory.cookedFish<1)return;state.inventory.cookedFish--;state.needs.hunger=clamp(state.needs.hunger+32,0,100);saveState(true);updateHUD();openCampfire(id);};$('[data-cook]',root).onclick=async()=>{if(!mine||data.cooking||state.inventory.rawFish<1)return;if(!(await confirmModal('Assar peixe','Usar 1 peixe cru nesta fogueira?','Assar','Cancelar')))return;state.inventory.rawFish--;const cookingId=uid();data.cooking={id:cookingId,startedAt:Date.now(),endsAt:Date.now()+5000,collected:false};saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Peixe na grelha. Aguarde alguns segundos.','good');setTimeout(()=>finishCampfireCooking(data,cookingId),5100);};`
- [x] `return` — linha 3727, coluna 258 em `openCampfire()` — `$('[data-sit]',root).onclick=()=>{player.sitUntil=performance.now()+4500;state.needs.fun=clamp(state.needs.fun+8,0,100);closeModal();toast('Você descansou perto da fogueira.','good');};$('[data-eat]',root).onclick=()=>{if(state.inventory.cookedFish<1)return;state.inventory.cookedFish--;state.needs.hunger=clamp(state.needs.hunger+32,0,100);saveState(true);updateHUD();openCampfire(id);};$('[data-cook]',root).onclick=async()=>{if(!mine||data.cooking||state.inventory.rawFish<1)return;if(!(await confirmModal('Assar peixe','Usar 1 peixe cru nesta fogueira?','Assar','Cancelar')))return;state.inventory.rawFish--;const cookingId=uid();data.cooking={id:cookingId,startedAt:Date.now(),endsAt:Date.now()+5000,collected:false};saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Peixe na grelha. Aguarde alguns segundos.','good');setTimeout(()=>finishCampfireCooking(data,cookingId),5100);};`
- [x] `if` — linha 3727, coluna 435 em `openCampfire()` — `$('[data-sit]',root).onclick=()=>{player.sitUntil=performance.now()+4500;state.needs.fun=clamp(state.needs.fun+8,0,100);closeModal();toast('Você descansou perto da fogueira.','good');};$('[data-eat]',root).onclick=()=>{if(state.inventory.cookedFish<1)return;state.inventory.cookedFish--;state.needs.hunger=clamp(state.needs.hunger+32,0,100);saveState(true);updateHUD();openCampfire(id);};$('[data-cook]',root).onclick=async()=>{if(!mine||data.cooking||state.inventory.rawFish<1)return;if(!(await confirmModal('Assar peixe','Usar 1 peixe cru nesta fogueira?','Assar','Cancelar')))return;state.inventory.rawFish--;const cookingId=uid();data.cooking={id:cookingId,startedAt:Date.now(),endsAt:Date.now()+5000,collected:false};saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Peixe na grelha. Aguarde alguns segundos.','good');setTimeout(()=>finishCampfireCooking(data,cookingId),5100);};`
- [x] `return` — linha 3727, coluna 485 em `openCampfire()` — `$('[data-sit]',root).onclick=()=>{player.sitUntil=performance.now()+4500;state.needs.fun=clamp(state.needs.fun+8,0,100);closeModal();toast('Você descansou perto da fogueira.','good');};$('[data-eat]',root).onclick=()=>{if(state.inventory.cookedFish<1)return;state.inventory.cookedFish--;state.needs.hunger=clamp(state.needs.hunger+32,0,100);saveState(true);updateHUD();openCampfire(id);};$('[data-cook]',root).onclick=async()=>{if(!mine||data.cooking||state.inventory.rawFish<1)return;if(!(await confirmModal('Assar peixe','Usar 1 peixe cru nesta fogueira?','Assar','Cancelar')))return;state.inventory.rawFish--;const cookingId=uid();data.cooking={id:cookingId,startedAt:Date.now(),endsAt:Date.now()+5000,collected:false};saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Peixe na grelha. Aguarde alguns segundos.','good');setTimeout(()=>finishCampfireCooking(data,cookingId),5100);};`
- [x] `if` — linha 3727, coluna 492 em `openCampfire()` — `$('[data-sit]',root).onclick=()=>{player.sitUntil=performance.now()+4500;state.needs.fun=clamp(state.needs.fun+8,0,100);closeModal();toast('Você descansou perto da fogueira.','good');};$('[data-eat]',root).onclick=()=>{if(state.inventory.cookedFish<1)return;state.inventory.cookedFish--;state.needs.hunger=clamp(state.needs.hunger+32,0,100);saveState(true);updateHUD();openCampfire(id);};$('[data-cook]',root).onclick=async()=>{if(!mine||data.cooking||state.inventory.rawFish<1)return;if(!(await confirmModal('Assar peixe','Usar 1 peixe cru nesta fogueira?','Assar','Cancelar')))return;state.inventory.rawFish--;const cookingId=uid();data.cooking={id:cookingId,startedAt:Date.now(),endsAt:Date.now()+5000,collected:false};saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Peixe na grelha. Aguarde alguns segundos.','good');setTimeout(()=>finishCampfireCooking(data,cookingId),5100);};`
- [x] `return` — linha 3727, coluna 586 em `openCampfire()` — `$('[data-sit]',root).onclick=()=>{player.sitUntil=performance.now()+4500;state.needs.fun=clamp(state.needs.fun+8,0,100);closeModal();toast('Você descansou perto da fogueira.','good');};$('[data-eat]',root).onclick=()=>{if(state.inventory.cookedFish<1)return;state.inventory.cookedFish--;state.needs.hunger=clamp(state.needs.hunger+32,0,100);saveState(true);updateHUD();openCampfire(id);};$('[data-cook]',root).onclick=async()=>{if(!mine||data.cooking||state.inventory.rawFish<1)return;if(!(await confirmModal('Assar peixe','Usar 1 peixe cru nesta fogueira?','Assar','Cancelar')))return;state.inventory.rawFish--;const cookingId=uid();data.cooking={id:cookingId,startedAt:Date.now(),endsAt:Date.now()+5000,collected:false};saveState(true);window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);toast('Peixe na grelha. Aguarde alguns segundos.','good');setTimeout(()=>finishCampfireCooking(data,cookingId),5100);};`
- [x] `if` — linha 3729, coluna 68 em `openNearestCampfire()` — `function openNearestCampfire(){const c=nearestActiveCampfire(12);if(c)openCampfire(c.data.id);else toast('A fogueira do amigo não está próxima ou já apagou.','warn');}`
- [x] `else` — linha 3729, coluna 97 em `openNearestCampfire()` — `function openNearestCampfire(){const c=nearestActiveCampfire(12);if(c)openCampfire(c.data.id);else toast('A fogueira do amigo não está próxima ou já apagou.','warn');}`
- [x] `if` — linha 3730, coluna 64 em `openCampfireZone()` — `function openCampfireZone(){const c=nearestActiveCampfire(8);if(c)openCampfire(c.data.id);else openModal('Acampamento','<p>Esta é uma área segura para montar fogueira. Ela usa madeira real do inventário e apaga automaticamente.</p><div class="modal-actions"><button class="btn primary" data-build-fire>Construir fogueira</button></div>',root=>$('[data-build-fire]',root).onclick=()=>{closeModal();buildCampfire();});}`
- [x] `else` — linha 3730, coluna 93 em `openCampfireZone()` — `function openCampfireZone(){const c=nearestActiveCampfire(8);if(c)openCampfire(c.data.id);else openModal('Acampamento','<p>Esta é uma área segura para montar fogueira. Ela usa madeira real do inventário e apaga automaticamente.</p><div class="modal-actions"><button class="btn primary" data-build-fire>Construir fogueira</button></div>',root=>$('[data-build-fire]',root).onclick=()=>{closeModal();buildCampfire();});}`
- [x] `for` — linha 3731, coluna 33 em `createCampfireZone()` — `function createCampfireZone(){for(let i=0;i<9;i++){const a=i/9*Math.PI*2;premiumBox(.75,.3,.5,0x8b765e,-70+Math.cos(a)*2,.15,-62+Math.sin(a)*2);}createSignpost(-65,-57,'Acampamento',Math.PI*.25);registerInteractable({id:'campfire-zone',type:'campfire-zone',icon:'🔥',label:'Área de fogueira',x:-70,z:-62,radius:5,priority:145,action:openCampfireZone});}`
- [x] `for` — linha 3732, coluna 199 em `createForestAnimal()` — `function createForestAnimal(id,type,x,z,color){const g=new THREE.Group();g.position.set(x,0,z);worldGroup.add(g);premiumBox(1.25,.72,.55,color,0,.72,0,g);premiumBox(.55,.55,.5,color,0,1.25,.38,g);for(const ox of [-.42,.42])for(const oz of [-.18,.18])premiumBox(.16,.55,.16,0x5e432b,ox,.28,oz,g);if(type==='deer'){premiumBox(.08,.5,.08,0x5e432b,-.18,1.68,.4,g);premiumBox(.08,.5,.08,0x5e432b,.18,1.68,.4,g);}const animal={id,type,group:g,x,z,available:true,respawnAt:0};world.animals.push(animal);return animal;}`
- [x] `for` — linha 3732, coluna 226 em `createForestAnimal()` — `function createForestAnimal(id,type,x,z,color){const g=new THREE.Group();g.position.set(x,0,z);worldGroup.add(g);premiumBox(1.25,.72,.55,color,0,.72,0,g);premiumBox(.55,.55,.5,color,0,1.25,.38,g);for(const ox of [-.42,.42])for(const oz of [-.18,.18])premiumBox(.16,.55,.16,0x5e432b,ox,.28,oz,g);if(type==='deer'){premiumBox(.08,.5,.08,0x5e432b,-.18,1.68,.4,g);premiumBox(.08,.5,.08,0x5e432b,.18,1.68,.4,g);}const animal={id,type,group:g,x,z,available:true,respawnAt:0};world.animals.push(animal);return animal;}`
- [x] `if` — linha 3732, coluna 298 em `createForestAnimal()` — `function createForestAnimal(id,type,x,z,color){const g=new THREE.Group();g.position.set(x,0,z);worldGroup.add(g);premiumBox(1.25,.72,.55,color,0,.72,0,g);premiumBox(.55,.55,.5,color,0,1.25,.38,g);for(const ox of [-.42,.42])for(const oz of [-.18,.18])premiumBox(.16,.55,.16,0x5e432b,ox,.28,oz,g);if(type==='deer'){premiumBox(.08,.5,.08,0x5e432b,-.18,1.68,.4,g);premiumBox(.08,.5,.08,0x5e432b,.18,1.68,.4,g);}const animal={id,type,group:g,x,z,available:true,respawnAt:0};world.animals.push(animal);return animal;}`
- [x] `return` — linha 3732, coluna 499 em `createForestAnimal()` — `function createForestAnimal(id,type,x,z,color){const g=new THREE.Group();g.position.set(x,0,z);worldGroup.add(g);premiumBox(1.25,.72,.55,color,0,.72,0,g);premiumBox(.55,.55,.5,color,0,1.25,.38,g);for(const ox of [-.42,.42])for(const oz of [-.18,.18])premiumBox(.16,.55,.16,0x5e432b,ox,.28,oz,g);if(type==='deer'){premiumBox(.08,.5,.08,0x5e432b,-.18,1.68,.4,g);premiumBox(.08,.5,.08,0x5e432b,.18,1.68,.4,g);}const animal={id,type,group:g,x,z,available:true,respawnAt:0};world.animals.push(animal);return animal;}`
- [x] `if` — linha 3733, coluna 120 em `startHunting()` — `function startHunting(options={}){const cooldown=Math.max(0,20000-(Date.now()-Number(state.hunting.lastAttempt||0)));if(cooldown>0){toast(\`A floresta precisa descansar por ${Math.ceil(cooldown/1000)} s.\`,'warn');return;}if(Math.hypot(player.x+96,player.z+72)>28){toast('Siga as pegadas até a área de rastreamento.','warn');return;}state.hunting.lastAttempt=Date.now();state.hunting.tracksFound++;const trail=['Pegadas perto das pedras','Galhos mexidos à esquerda','Folhas amassadas perto da árvore'],correct=Math.floor(Math.random()*3);openModal(options.cooperative?'Rastreamento com amigo':'Rastreamento na floresta',\`<div class="activity-card hunting-card"><div class="activity-icon">🐾</div><h3>Encontre o caminho do animal</h3><p>${trail[correct]}. Qual direção você seguirá?</p><div class="choice-grid"><button class="choice" data-track="0"><b>⬅️ Esquerda</b><span>Seguir pistas</span></button><button class="choice" data-track="1"><b>⬆️ Em frente</b><span>Seguir pistas</span></button><button cl`
- [x] `return` — linha 3733, coluna 215 em `startHunting()` — `function startHunting(options={}){const cooldown=Math.max(0,20000-(Date.now()-Number(state.hunting.lastAttempt||0)));if(cooldown>0){toast(\`A floresta precisa descansar por ${Math.ceil(cooldown/1000)} s.\`,'warn');return;}if(Math.hypot(player.x+96,player.z+72)>28){toast('Siga as pegadas até a área de rastreamento.','warn');return;}state.hunting.lastAttempt=Date.now();state.hunting.tracksFound++;const trail=['Pegadas perto das pedras','Galhos mexidos à esquerda','Folhas amassadas perto da árvore'],correct=Math.floor(Math.random()*3);openModal(options.cooperative?'Rastreamento com amigo':'Rastreamento na floresta',\`<div class="activity-card hunting-card"><div class="activity-icon">🐾</div><h3>Encontre o caminho do animal</h3><p>${trail[correct]}. Qual direção você seguirá?</p><div class="choice-grid"><button class="choice" data-track="0"><b>⬅️ Esquerda</b><span>Seguir pistas</span></button><button class="choice" data-track="1"><b>⬆️ Em frente</b><span>Seguir pistas</span></button><button cl`
- [x] `if` — linha 3733, coluna 223 em `startHunting()` — `function startHunting(options={}){const cooldown=Math.max(0,20000-(Date.now()-Number(state.hunting.lastAttempt||0)));if(cooldown>0){toast(\`A floresta precisa descansar por ${Math.ceil(cooldown/1000)} s.\`,'warn');return;}if(Math.hypot(player.x+96,player.z+72)>28){toast('Siga as pegadas até a área de rastreamento.','warn');return;}state.hunting.lastAttempt=Date.now();state.hunting.tracksFound++;const trail=['Pegadas perto das pedras','Galhos mexidos à esquerda','Folhas amassadas perto da árvore'],correct=Math.floor(Math.random()*3);openModal(options.cooperative?'Rastreamento com amigo':'Rastreamento na floresta',\`<div class="activity-card hunting-card"><div class="activity-icon">🐾</div><h3>Encontre o caminho do animal</h3><p>${trail[correct]}. Qual direção você seguirá?</p><div class="choice-grid"><button class="choice" data-track="0"><b>⬅️ Esquerda</b><span>Seguir pistas</span></button><button class="choice" data-track="1"><b>⬆️ Em frente</b><span>Seguir pistas</span></button><button cl`
- [x] `return` — linha 3733, coluna 326 em `startHunting()` — `function startHunting(options={}){const cooldown=Math.max(0,20000-(Date.now()-Number(state.hunting.lastAttempt||0)));if(cooldown>0){toast(\`A floresta precisa descansar por ${Math.ceil(cooldown/1000)} s.\`,'warn');return;}if(Math.hypot(player.x+96,player.z+72)>28){toast('Siga as pegadas até a área de rastreamento.','warn');return;}state.hunting.lastAttempt=Date.now();state.hunting.tracksFound++;const trail=['Pegadas perto das pedras','Galhos mexidos à esquerda','Folhas amassadas perto da árvore'],correct=Math.floor(Math.random()*3);openModal(options.cooperative?'Rastreamento com amigo':'Rastreamento na floresta',\`<div class="activity-card hunting-card"><div class="activity-icon">🐾</div><h3>Encontre o caminho do animal</h3><p>${trail[correct]}. Qual direção você seguirá?</p><div class="choice-grid"><button class="choice" data-track="0"><b>⬅️ Esquerda</b><span>Seguir pistas</span></button><button class="choice" data-track="1"><b>⬆️ Em frente</b><span>Seguir pistas</span></button><button cl`
- [x] `if` — linha 3733, coluna 1328 em `startHunting()` — `function startHunting(options={}){const cooldown=Math.max(0,20000-(Date.now()-Number(state.hunting.lastAttempt||0)));if(cooldown>0){toast(\`A floresta precisa descansar por ${Math.ceil(cooldown/1000)} s.\`,'warn');return;}if(Math.hypot(player.x+96,player.z+72)>28){toast('Siga as pegadas até a área de rastreamento.','warn');return;}state.hunting.lastAttempt=Date.now();state.hunting.tracksFound++;const trail=['Pegadas perto das pedras','Galhos mexidos à esquerda','Folhas amassadas perto da árvore'],correct=Math.floor(Math.random()*3);openModal(options.cooperative?'Rastreamento com amigo':'Rastreamento na floresta',\`<div class="activity-card hunting-card"><div class="activity-icon">🐾</div><h3>Encontre o caminho do animal</h3><p>${trail[correct]}. Qual direção você seguirá?</p><div class="choice-grid"><button class="choice" data-track="0"><b>⬅️ Esquerda</b><span>Seguir pistas</span></button><button class="choice" data-track="1"><b>⬆️ Em frente</b><span>Seguir pistas</span></button><button cl`
- [x] `if` — linha 3733, coluna 1497 em `startHunting()` — `function startHunting(options={}){const cooldown=Math.max(0,20000-(Date.now()-Number(state.hunting.lastAttempt||0)));if(cooldown>0){toast(\`A floresta precisa descansar por ${Math.ceil(cooldown/1000)} s.\`,'warn');return;}if(Math.hypot(player.x+96,player.z+72)>28){toast('Siga as pegadas até a área de rastreamento.','warn');return;}state.hunting.lastAttempt=Date.now();state.hunting.tracksFound++;const trail=['Pegadas perto das pedras','Galhos mexidos à esquerda','Folhas amassadas perto da árvore'],correct=Math.floor(Math.random()*3);openModal(options.cooperative?'Rastreamento com amigo':'Rastreamento na floresta',\`<div class="activity-card hunting-card"><div class="activity-icon">🐾</div><h3>Encontre o caminho do animal</h3><p>${trail[correct]}. Qual direção você seguirá?</p><div class="choice-grid"><button class="choice" data-track="0"><b>⬅️ Esquerda</b><span>Seguir pistas</span></button><button class="choice" data-track="1"><b>⬆️ Em frente</b><span>Seguir pistas</span></button><button cl`
- [x] `if` — linha 3733, coluna 1891 em `startHunting()` — `function startHunting(options={}){const cooldown=Math.max(0,20000-(Date.now()-Number(state.hunting.lastAttempt||0)));if(cooldown>0){toast(\`A floresta precisa descansar por ${Math.ceil(cooldown/1000)} s.\`,'warn');return;}if(Math.hypot(player.x+96,player.z+72)>28){toast('Siga as pegadas até a área de rastreamento.','warn');return;}state.hunting.lastAttempt=Date.now();state.hunting.tracksFound++;const trail=['Pegadas perto das pedras','Galhos mexidos à esquerda','Folhas amassadas perto da árvore'],correct=Math.floor(Math.random()*3);openModal(options.cooperative?'Rastreamento com amigo':'Rastreamento na floresta',\`<div class="activity-card hunting-card"><div class="activity-icon">🐾</div><h3>Encontre o caminho do animal</h3><p>${trail[correct]}. Qual direção você seguirá?</p><div class="choice-grid"><button class="choice" data-track="0"><b>⬅️ Esquerda</b><span>Seguir pistas</span></button><button class="choice" data-track="1"><b>⬆️ Em frente</b><span>Seguir pistas</span></button><button cl`
- [x] `else` — linha 3733, coluna 1988 em `startHunting()` — `function startHunting(options={}){const cooldown=Math.max(0,20000-(Date.now()-Number(state.hunting.lastAttempt||0)));if(cooldown>0){toast(\`A floresta precisa descansar por ${Math.ceil(cooldown/1000)} s.\`,'warn');return;}if(Math.hypot(player.x+96,player.z+72)>28){toast('Siga as pegadas até a área de rastreamento.','warn');return;}state.hunting.lastAttempt=Date.now();state.hunting.tracksFound++;const trail=['Pegadas perto das pedras','Galhos mexidos à esquerda','Folhas amassadas perto da árvore'],correct=Math.floor(Math.random()*3);openModal(options.cooperative?'Rastreamento com amigo':'Rastreamento na floresta',\`<div class="activity-card hunting-card"><div class="activity-icon">🐾</div><h3>Encontre o caminho do animal</h3><p>${trail[correct]}. Qual direção você seguirá?</p><div class="choice-grid"><button class="choice" data-track="0"><b>⬅️ Esquerda</b><span>Seguir pistas</span></button><button class="choice" data-track="1"><b>⬆️ Em frente</b><span>Seguir pistas</span></button><button cl`
- [x] `for` — linha 3734, coluna 91 em `createHuntingArea()` — `function createHuntingArea(){createSignpost(-89,-65,'Rastreamento infantil',Math.PI*.3);for(const p of [[-93,-70],[-97,-73],[-101,-75],[-104,-70]]){const mark=premiumBox(.45,.05,.7,0x634b32,p[0],.04,p[1]);mark.rotation.y=Math.random()*Math.PI;}createForestAnimal('deer-1','deer',-105,-78,0xb67945);createForestAnimal('rabbit-1','rabbit',-88,-82,0xc8b7a3);registerInteractable({id:'hunting-trail',type:'hunting',icon:'🐾',label:'Seguir rastros',x:-96,z:-72,radius:6,priority:140,action:()=>startHunting()});}`
- [x] `return` — linha 3735, coluna 44 em `nearestOwnedHouseForExtension()` — `function nearestOwnedHouseForExtension(){return world.houses.filter(h=>state.houses[h.id]?.owned&&!h.publicBuilding).sort((a,b)=>distance2D(player,a)-distance2D(player,b))[0];}`
- [x] `return` — linha 3736, coluna 57 em `extensionPlacement()` — `function extensionPlacement(house,side){const off=7.1;return side===0?{x:house.x+off,z:house.z,rotation:0}:side===1?{x:house.x,z:house.z+off,rotation:Math.PI/2}:side===2?{x:house.x-off,z:house.z,rotation:Math.PI}: {x:house.x,z:house.z-off,rotation:-Math.PI/2};}`
- [x] `if` — linha 3737, coluna 79 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `return` — linha 3737, coluna 127 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `if` — linha 3737, coluna 140 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `return` — linha 3737, coluna 189 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `if` — linha 3737, coluna 202 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `return` — linha 3737, coluna 253 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `if` — linha 3737, coluna 266 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `return` — linha 3737, coluna 358 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `for` — linha 3737, coluna 371 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `if` — linha 3737, coluna 400 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `if` — linha 3737, coluna 433 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `return` — linha 3737, coluna 479 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `return` — linha 3737, coluna 493 em `extensionValid()` — `function extensionValid(draft){const rect={x:draft.x,z:draft.z,w:5.4,d:5.4};if(Math.abs(draft.x)>111||Math.abs(draft.z)>111)return false;if(world.hazards.some(h=>rectOverlap(rect,h,.5)))return false;if(WORLD_MAP_ROADS.some(r=>rectOverlap(rect,r,.4)))return false;if(world.houseExtensions.some(e=>rectOverlap(rect,{x:e.data.x,z:e.data.z,w:5.4,d:5.4},.35)))return false;for(const h of world.houses){if(h.id===draft.houseId)continue;if(rectOverlap(rect,{x:h.x,z:h.z,w:9,d:7},.6))return false;}return true;}`
- [x] `return` — linha 3749, coluna 46 em `furnitureCatalogForRoom()` — `function furnitureCatalogForRoom(roomType){return ROOM_FURNITURE_CATALOG[roomType]||ROOM_FURNITURE_CATALOG.storage;}`
- [x] `return` — linha 3750, coluna 51 em `furnitureSpec()` — `function furnitureSpec(type,roomType='storage'){return furnitureCatalogForRoom(roomType).find(item=>item.id===type)||Object.values(ROOM_FURNITURE_CATALOG).flat().find(item=>item.id===type)||null;}`
- [x] `return` — linha 3751, coluna 47 em `furnitureForExtension()` — `function furnitureForExtension(extensionId){return(state.roomFurniture||[]).filter(item=>item.extensionId===extensionId);}`
- [x] `return` — linha 3752, coluna 173 em `furnitureFootprint()` — `function furnitureFootprint(item,roomType='storage'){const spec=furnitureSpec(item.type,roomType)||{w:.8,d:.8},quarter=Math.round(Number(item.rotation||0)/(Math.PI/2))%2;return{w:quarter?spec.d:spec.w,d:quarter?spec.w:spec.d};}`
- [x] `if` — linha 3753, coluna 228 em `furniturePlacementValid()` — `function furniturePlacementValid(extensionId,candidate,ignoreId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId),roomType=record?.data?.type||'storage',foot=furnitureFootprint(candidate,roomType);if(Math.abs(candidate.x)+foot.w/2>2.28||Math.abs(candidate.z)+foot.d/2>2.22)return false;if(candidate.z>1.25&&Math.abs(candidate.x)<1.05)return false;const rect={x:candidate.x,z:candidate.z,w:foot.w,d:foot.d};return!furnitureForExtension(extensionId).some(item=>item.id!==ignoreId&&rectOverlap(rect,{x:Number(item.x||0),z:Number(item.z||0),...furnitureFootprint(item,roomType)},.12));}`
- [x] `return` — linha 3753, coluna 304 em `furniturePlacementValid()` — `function furniturePlacementValid(extensionId,candidate,ignoreId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId),roomType=record?.data?.type||'storage',foot=furnitureFootprint(candidate,roomType);if(Math.abs(candidate.x)+foot.w/2>2.28||Math.abs(candidate.z)+foot.d/2>2.22)return false;if(candidate.z>1.25&&Math.abs(candidate.x)<1.05)return false;const rect={x:candidate.x,z:candidate.z,w:foot.w,d:foot.d};return!furnitureForExtension(extensionId).some(item=>item.id!==ignoreId&&rectOverlap(rect,{x:Number(item.x||0),z:Number(item.z||0),...furnitureFootprint(item,roomType)},.12));}`
- [x] `if` — linha 3753, coluna 317 em `furniturePlacementValid()` — `function furniturePlacementValid(extensionId,candidate,ignoreId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId),roomType=record?.data?.type||'storage',foot=furnitureFootprint(candidate,roomType);if(Math.abs(candidate.x)+foot.w/2>2.28||Math.abs(candidate.z)+foot.d/2>2.22)return false;if(candidate.z>1.25&&Math.abs(candidate.x)<1.05)return false;const rect={x:candidate.x,z:candidate.z,w:foot.w,d:foot.d};return!furnitureForExtension(extensionId).some(item=>item.id!==ignoreId&&rectOverlap(rect,{x:Number(item.x||0),z:Number(item.z||0),...furnitureFootprint(item,roomType)},.12));}`
- [x] `return` — linha 3753, coluna 365 em `furniturePlacementValid()` — `function furniturePlacementValid(extensionId,candidate,ignoreId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId),roomType=record?.data?.type||'storage',foot=furnitureFootprint(candidate,roomType);if(Math.abs(candidate.x)+foot.w/2>2.28||Math.abs(candidate.z)+foot.d/2>2.22)return false;if(candidate.z>1.25&&Math.abs(candidate.x)<1.05)return false;const rect={x:candidate.x,z:candidate.z,w:foot.w,d:foot.d};return!furnitureForExtension(extensionId).some(item=>item.id!==ignoreId&&rectOverlap(rect,{x:Number(item.x||0),z:Number(item.z||0),...furnitureFootprint(item,roomType)},.12));}`
- [x] `return` — linha 3753, coluna 437 em `furniturePlacementValid()` — `function furniturePlacementValid(extensionId,candidate,ignoreId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId),roomType=record?.data?.type||'storage',foot=furnitureFootprint(candidate,roomType);if(Math.abs(candidate.x)+foot.w/2>2.28||Math.abs(candidate.z)+foot.d/2>2.22)return false;if(candidate.z>1.25&&Math.abs(candidate.x)<1.05)return false;const rect={x:candidate.x,z:candidate.z,w:foot.w,d:foot.d};return!furnitureForExtension(extensionId).some(item=>item.id!==ignoreId&&rectOverlap(rect,{x:Number(item.x||0),z:Number(item.z||0),...furnitureFootprint(item,roomType)},.12));}`
- [x] `if` — linha 3754, coluna 41 em `disposeFurnitureGroup()` — `function disposeFurnitureGroup(group){if(!group)return;group.parent?.remove(group);disposeDetachedVisual(group);}`
- [x] `return` — linha 3754, coluna 51 em `disposeFurnitureGroup()` — `function disposeFurnitureGroup(group){if(!group)return;group.parent?.remove(group);disposeDetachedVisual(group);}`
- [x] `if` — linha 3755, coluna 36 em `clearFurniturePreview()` — `function clearFurniturePreview(){if(furniturePreview)disposeFurnitureGroup(furniturePreview);furniturePreview=null;furnitureDraft=null;}`
- [x] `if` — linha 3756, coluna 36 em `clearExtensionPreview()` — `function clearExtensionPreview(){if(extensionPreview&&worldGroup){worldGroup.remove(extensionPreview);disposeDetachedVisual(extensionPreview);}extensionPreview=null;extensionDraft=null;clearFurniturePreview();}`
- [x] `if` — linha 3757, coluna 37 em `renderExtensionPreview()` — `function renderExtensionPreview(){if(!extensionDraft)return;const d={...extensionDraft};if(extensionPreview&&worldGroup){worldGroup.remove(extensionPreview);disposeDetachedVisual(extensionPreview);}extensionPreview=null;extensionDraft=d;const spec=ROOM_SPECS[d.type]||ROOM_SPECS.storage,g=new THREE.Group();g.position.set(Number(d.x||0),0,Number(d.z||0));g.rotation.y=Number(d.rotation||0);worldGroup.add(g);const m=new THREE.MeshStandardMaterial({color:extensionValid(d)?0x62e58a:0xff5263,transparent:true,opacity:.4,roughness:.5});const floor=new THREE.Mesh(new THREE.BoxGeometry(5.2,.18,5.2),m);floor.position.y=.09;g.add(floor);for(const [x,z,w,dd]of [[0,-2.5,5.2,.16],[-2.5,0,.16,5.2],[2.5,0,.16,5.2]]){const wall=new THREE.Mesh(new THREE.BoxGeometry(w,2.8,dd),m);wall.position.set(x,1.4,z);g.add(wall);}extensionPreview=g;}`
- [x] `return` — linha 3757, coluna 56 em `renderExtensionPreview()` — `function renderExtensionPreview(){if(!extensionDraft)return;const d={...extensionDraft};if(extensionPreview&&worldGroup){worldGroup.remove(extensionPreview);disposeDetachedVisual(extensionPreview);}extensionPreview=null;extensionDraft=d;const spec=ROOM_SPECS[d.type]||ROOM_SPECS.storage,g=new THREE.Group();g.position.set(Number(d.x||0),0,Number(d.z||0));g.rotation.y=Number(d.rotation||0);worldGroup.add(g);const m=new THREE.MeshStandardMaterial({color:extensionValid(d)?0x62e58a:0xff5263,transparent:true,opacity:.4,roughness:.5});const floor=new THREE.Mesh(new THREE.BoxGeometry(5.2,.18,5.2),m);floor.position.y=.09;g.add(floor);for(const [x,z,w,dd]of [[0,-2.5,5.2,.16],[-2.5,0,.16,5.2],[2.5,0,.16,5.2]]){const wall=new THREE.Mesh(new THREE.BoxGeometry(w,2.8,dd),m);wall.position.set(x,1.4,z);g.add(wall);}extensionPreview=g;}`
- [x] `if` — linha 3757, coluna 91 em `renderExtensionPreview()` — `function renderExtensionPreview(){if(!extensionDraft)return;const d={...extensionDraft};if(extensionPreview&&worldGroup){worldGroup.remove(extensionPreview);disposeDetachedVisual(extensionPreview);}extensionPreview=null;extensionDraft=d;const spec=ROOM_SPECS[d.type]||ROOM_SPECS.storage,g=new THREE.Group();g.position.set(Number(d.x||0),0,Number(d.z||0));g.rotation.y=Number(d.rotation||0);worldGroup.add(g);const m=new THREE.MeshStandardMaterial({color:extensionValid(d)?0x62e58a:0xff5263,transparent:true,opacity:.4,roughness:.5});const floor=new THREE.Mesh(new THREE.BoxGeometry(5.2,.18,5.2),m);floor.position.y=.09;g.add(floor);for(const [x,z,w,dd]of [[0,-2.5,5.2,.16],[-2.5,0,.16,5.2],[2.5,0,.16,5.2]]){const wall=new THREE.Mesh(new THREE.BoxGeometry(w,2.8,dd),m);wall.position.set(x,1.4,z);g.add(wall);}extensionPreview=g;}`
- [x] `for` — linha 3757, coluna 635 em `renderExtensionPreview()` — `function renderExtensionPreview(){if(!extensionDraft)return;const d={...extensionDraft};if(extensionPreview&&worldGroup){worldGroup.remove(extensionPreview);disposeDetachedVisual(extensionPreview);}extensionPreview=null;extensionDraft=d;const spec=ROOM_SPECS[d.type]||ROOM_SPECS.storage,g=new THREE.Group();g.position.set(Number(d.x||0),0,Number(d.z||0));g.rotation.y=Number(d.rotation||0);worldGroup.add(g);const m=new THREE.MeshStandardMaterial({color:extensionValid(d)?0x62e58a:0xff5263,transparent:true,opacity:.4,roughness:.5});const floor=new THREE.Mesh(new THREE.BoxGeometry(5.2,.18,5.2),m);floor.position.y=.09;g.add(floor);for(const [x,z,w,dd]of [[0,-2.5,5.2,.16],[-2.5,0,.16,5.2],[2.5,0,.16,5.2]]){const wall=new THREE.Mesh(new THREE.BoxGeometry(w,2.8,dd),m);wall.position.set(x,1.4,z);g.add(wall);}extensionPreview=g;}`
- [x] `if` — linha 3758, coluna 218 em `createFurnitureModel()` — `function createFurnitureModel(item,parent,preview=false,valid=true){const roomType=world.houseExtensions.find(record=>record.data.id===item.extensionId)?.data?.type||'storage',spec=furnitureSpec(item.type,roomType);if(!spec)return null;const g=new THREE.Group();g.position.set(Number(item.x||0),.23,Number(item.z||0));g.rotation.y=Number(item.rotation||0);parent.add(g);if(preview){const material=new THREE.MeshStandardMaterial({color:valid?0x60e88b:0xff5668,transparent:true,opacity:.48,roughness:.5});const mesh=new THREE.Mesh(new THREE.BoxGeometry(spec.w,.55,spec.d),material);mesh.position.y=.3;g.add(mesh);return g;}const wood=0x9b6336,dark=0x283547,light=0xf1eadb,accent=0x2f8fd8,cloth=0x467bb6,metal=0x78899a,green=0x3e9a4d;`
- [x] `return` — linha 3758, coluna 227 em `createFurnitureModel()` — `function createFurnitureModel(item,parent,preview=false,valid=true){const roomType=world.houseExtensions.find(record=>record.data.id===item.extensionId)?.data?.type||'storage',spec=furnitureSpec(item.type,roomType);if(!spec)return null;const g=new THREE.Group();g.position.set(Number(item.x||0),.23,Number(item.z||0));g.rotation.y=Number(item.rotation||0);parent.add(g);if(preview){const material=new THREE.MeshStandardMaterial({color:valid?0x60e88b:0xff5668,transparent:true,opacity:.48,roughness:.5});const mesh=new THREE.Mesh(new THREE.BoxGeometry(spec.w,.55,spec.d),material);mesh.position.y=.3;g.add(mesh);return g;}const wood=0x9b6336,dark=0x283547,light=0xf1eadb,accent=0x2f8fd8,cloth=0x467bb6,metal=0x78899a,green=0x3e9a4d;`
- [x] `if` — linha 3758, coluna 373 em `createFurnitureModel()` — `function createFurnitureModel(item,parent,preview=false,valid=true){const roomType=world.houseExtensions.find(record=>record.data.id===item.extensionId)?.data?.type||'storage',spec=furnitureSpec(item.type,roomType);if(!spec)return null;const g=new THREE.Group();g.position.set(Number(item.x||0),.23,Number(item.z||0));g.rotation.y=Number(item.rotation||0);parent.add(g);if(preview){const material=new THREE.MeshStandardMaterial({color:valid?0x60e88b:0xff5668,transparent:true,opacity:.48,roughness:.5});const mesh=new THREE.Mesh(new THREE.BoxGeometry(spec.w,.55,spec.d),material);mesh.position.y=.3;g.add(mesh);return g;}const wood=0x9b6336,dark=0x283547,light=0xf1eadb,accent=0x2f8fd8,cloth=0x467bb6,metal=0x78899a,green=0x3e9a4d;`
- [x] `return` — linha 3758, coluna 614 em `createFurnitureModel()` — `function createFurnitureModel(item,parent,preview=false,valid=true){const roomType=world.houseExtensions.find(record=>record.data.id===item.extensionId)?.data?.type||'storage',spec=furnitureSpec(item.type,roomType);if(!spec)return null;const g=new THREE.Group();g.position.set(Number(item.x||0),.23,Number(item.z||0));g.rotation.y=Number(item.rotation||0);parent.add(g);if(preview){const material=new THREE.MeshStandardMaterial({color:valid?0x60e88b:0xff5668,transparent:true,opacity:.48,roughness:.5});const mesh=new THREE.Mesh(new THREE.BoxGeometry(spec.w,.55,spec.d),material);mesh.position.y=.3;g.add(mesh);return g;}const wood=0x9b6336,dark=0x283547,light=0xf1eadb,accent=0x2f8fd8,cloth=0x467bb6,metal=0x78899a,green=0x3e9a4d;`
- [x] `if` — linha 3760, coluna 5 em `createFurnitureModel()` — `if(item.type==='bed'){pb(2.05,.28,1.42,wood,0,.18,0);pb(1.95,.28,1.35,0xd8edf8,0,.43,0);pb(1.9,.08,.42,0x4d86bd,0,.61,.42);pb(.72,.17,.42,light,-.48,.62,-.38);pb(2.05,1.0,.16,wood,0,.72,-.7);}`
- [x] `else` — linha 3761, coluna 5 em `createFurnitureModel()` — `else if(item.type==='sofa'){pb(2.0,.42,.78,cloth,0,.4,0);pb(2.0,.78,.22,shadeColor(cloth,-20),0,.73,-.31);pb(.24,.67,.82,shadeColor(cloth,-12),-.9,.56,0);pb(.24,.67,.82,shadeColor(cloth,-12),.9,.56,0);pb(.05,.48,.05,dark,-.78,.1,.25);pb(.05,.48,.05,dark,.78,.1,.25);}`
- [x] `if` — linha 3761, coluna 10 em `createFurnitureModel()` — `else if(item.type==='sofa'){pb(2.0,.42,.78,cloth,0,.4,0);pb(2.0,.78,.22,shadeColor(cloth,-20),0,.73,-.31);pb(.24,.67,.82,shadeColor(cloth,-12),-.9,.56,0);pb(.24,.67,.82,shadeColor(cloth,-12),.9,.56,0);pb(.05,.48,.05,dark,-.78,.1,.25);pb(.05,.48,.05,dark,.78,.1,.25);}`
- [x] `else` — linha 3762, coluna 5 em `createFurnitureModel()` — `else if(item.type==='tv'){pb(1.3,.78,.12,dark,0,.82,0);pb(1.14,.62,.04,0x154d6a,0,.82,.08);pb(.1,.48,.1,metal,0,.3,0);pb(.72,.1,.45,dark,0,.08,0);}`
- [x] `if` — linha 3762, coluna 10 em `createFurnitureModel()` — `else if(item.type==='tv'){pb(1.3,.78,.12,dark,0,.82,0);pb(1.14,.62,.04,0x154d6a,0,.82,.08);pb(.1,.48,.1,metal,0,.3,0);pb(.72,.1,.45,dark,0,.08,0);}`
- [x] `else` — linha 3763, coluna 5 em `createFurnitureModel()` — `else if(['coffee-table','porch-table','work-table'].includes(item.type)){pb(spec.w,.12,spec.d,wood,0,.62,0);for(const x of [-spec.w*.38,spec.w*.38])for(const z of [-spec.d*.32,spec.d*.32])pb(.09,.62,.09,dark,x,.31,z);}`
- [x] `if` — linha 3763, coluna 10 em `createFurnitureModel()` — `else if(['coffee-table','porch-table','work-table'].includes(item.type)){pb(spec.w,.12,spec.d,wood,0,.62,0);for(const x of [-spec.w*.38,spec.w*.38])for(const z of [-spec.d*.32,spec.d*.32])pb(.09,.62,.09,dark,x,.31,z);}`
- [x] `for` — linha 3763, coluna 113 em `createFurnitureModel()` — `else if(['coffee-table','porch-table','work-table'].includes(item.type)){pb(spec.w,.12,spec.d,wood,0,.62,0);for(const x of [-spec.w*.38,spec.w*.38])for(const z of [-spec.d*.32,spec.d*.32])pb(.09,.62,.09,dark,x,.31,z);}`
- [x] `for` — linha 3763, coluna 153 em `createFurnitureModel()` — `else if(['coffee-table','porch-table','work-table'].includes(item.type)){pb(spec.w,.12,spec.d,wood,0,.62,0);for(const x of [-spec.w*.38,spec.w*.38])for(const z of [-spec.d*.32,spec.d*.32])pb(.09,.62,.09,dark,x,.31,z);}`
- [x] `else` — linha 3764, coluna 5 em `createFurnitureModel()` — `else if(['wardrobe','cabinet','bookshelf','shelf','storage-shelf'].includes(item.type)){pb(spec.w,1.65,spec.d,wood,0,.84,0);for(const y of [.38,.82,1.25])pb(spec.w-.12,.06,spec.d+.02,light,0,y,.02);if(item.type==='wardrobe'||item.type==='cabinet'){pb(.06,1.5,.04,dark,0,.86,spec.d/2+.03);pb(.07,.12,.05,metal,-.12,.85,spec.d/2+.06);pb(.07,.12,.05,metal,.12,.85,spec.d/2+.06);}else for(let i=0;i<5;i++)pb(.15,.42,.27,[0xe36d5c,0x4e8bd9,0xf0c84d,0x62a96b][i%4],-.45+i*.22,.58,spec.d/2+.03);}`
- [x] `if` — linha 3764, coluna 10 em `createFurnitureModel()` — `else if(['wardrobe','cabinet','bookshelf','shelf','storage-shelf'].includes(item.type)){pb(spec.w,1.65,spec.d,wood,0,.84,0);for(const y of [.38,.82,1.25])pb(spec.w-.12,.06,spec.d+.02,light,0,y,.02);if(item.type==='wardrobe'||item.type==='cabinet'){pb(.06,1.5,.04,dark,0,.86,spec.d/2+.03);pb(.07,.12,.05,metal,-.12,.85,spec.d/2+.06);pb(.07,.12,.05,metal,.12,.85,spec.d/2+.06);}else for(let i=0;i<5;i++)pb(.15,.42,.27,[0xe36d5c,0x4e8bd9,0xf0c84d,0x62a96b][i%4],-.45+i*.22,.58,spec.d/2+.03);}`
- [x] `for` — linha 3764, coluna 129 em `createFurnitureModel()` — `else if(['wardrobe','cabinet','bookshelf','shelf','storage-shelf'].includes(item.type)){pb(spec.w,1.65,spec.d,wood,0,.84,0);for(const y of [.38,.82,1.25])pb(spec.w-.12,.06,spec.d+.02,light,0,y,.02);if(item.type==='wardrobe'||item.type==='cabinet'){pb(.06,1.5,.04,dark,0,.86,spec.d/2+.03);pb(.07,.12,.05,metal,-.12,.85,spec.d/2+.06);pb(.07,.12,.05,metal,.12,.85,spec.d/2+.06);}else for(let i=0;i<5;i++)pb(.15,.42,.27,[0xe36d5c,0x4e8bd9,0xf0c84d,0x62a96b][i%4],-.45+i*.22,.58,spec.d/2+.03);}`
- [x] `if` — linha 3764, coluna 203 em `createFurnitureModel()` — `else if(['wardrobe','cabinet','bookshelf','shelf','storage-shelf'].includes(item.type)){pb(spec.w,1.65,spec.d,wood,0,.84,0);for(const y of [.38,.82,1.25])pb(spec.w-.12,.06,spec.d+.02,light,0,y,.02);if(item.type==='wardrobe'||item.type==='cabinet'){pb(.06,1.5,.04,dark,0,.86,spec.d/2+.03);pb(.07,.12,.05,metal,-.12,.85,spec.d/2+.06);pb(.07,.12,.05,metal,.12,.85,spec.d/2+.06);}else for(let i=0;i<5;i++)pb(.15,.42,.27,[0xe36d5c,0x4e8bd9,0xf0c84d,0x62a96b][i%4],-.45+i*.22,.58,spec.d/2+.03);}`
- [x] `else` — linha 3764, coluna 381 em `createFurnitureModel()` — `else if(['wardrobe','cabinet','bookshelf','shelf','storage-shelf'].includes(item.type)){pb(spec.w,1.65,spec.d,wood,0,.84,0);for(const y of [.38,.82,1.25])pb(spec.w-.12,.06,spec.d+.02,light,0,y,.02);if(item.type==='wardrobe'||item.type==='cabinet'){pb(.06,1.5,.04,dark,0,.86,spec.d/2+.03);pb(.07,.12,.05,metal,-.12,.85,spec.d/2+.06);pb(.07,.12,.05,metal,.12,.85,spec.d/2+.06);}else for(let i=0;i<5;i++)pb(.15,.42,.27,[0xe36d5c,0x4e8bd9,0xf0c84d,0x62a96b][i%4],-.45+i*.22,.58,spec.d/2+.03);}`
- [x] `for` — linha 3764, coluna 386 em `createFurnitureModel()` — `else if(['wardrobe','cabinet','bookshelf','shelf','storage-shelf'].includes(item.type)){pb(spec.w,1.65,spec.d,wood,0,.84,0);for(const y of [.38,.82,1.25])pb(spec.w-.12,.06,spec.d+.02,light,0,y,.02);if(item.type==='wardrobe'||item.type==='cabinet'){pb(.06,1.5,.04,dark,0,.86,spec.d/2+.03);pb(.07,.12,.05,metal,-.12,.85,spec.d/2+.06);pb(.07,.12,.05,metal,.12,.85,spec.d/2+.06);}else for(let i=0;i<5;i++)pb(.15,.42,.27,[0xe36d5c,0x4e8bd9,0xf0c84d,0x62a96b][i%4],-.45+i*.22,.58,spec.d/2+.03);}`
- [x] `else` — linha 3765, coluna 5 em `createFurnitureModel()` — `else if(item.type==='nightstand'){pb(.6,.58,.5,wood,0,.3,0);pb(.5,.18,.03,light,0,.37,.27);pb(.07,.08,.05,metal,0,.37,.3);}`
- [x] `if` — linha 3765, coluna 10 em `createFurnitureModel()` — `else if(item.type==='nightstand'){pb(.6,.58,.5,wood,0,.3,0);pb(.5,.18,.03,light,0,.37,.27);pb(.07,.08,.05,metal,0,.37,.3);}`
- [x] `else` — linha 3766, coluna 5 em `createFurnitureModel()` — `else if(['lamp','porch-light'].includes(item.type)){pb(.12,.75,.12,metal,0,.4,0);const shade=new THREE.Mesh(new THREE.ConeGeometry(.3,.48,10,1,true),mat(0xffe6a2,{emissive:0xffc94d,emissiveIntensity:.45,side:THREE.DoubleSide}));shade.position.y=.9;g.add(shade);pb(.42,.08,.42,dark,0,.04,0);}`
- [x] `if` — linha 3766, coluna 10 em `createFurnitureModel()` — `else if(['lamp','porch-light'].includes(item.type)){pb(.12,.75,.12,metal,0,.4,0);const shade=new THREE.Mesh(new THREE.ConeGeometry(.3,.48,10,1,true),mat(0xffe6a2,{emissive:0xffc94d,emissiveIntensity:.45,side:THREE.DoubleSide}));shade.position.y=.9;g.add(shade);pb(.42,.08,.42,dark,0,.04,0);}`
- [x] `else` — linha 3767, coluna 5 em `createFurnitureModel()` — `else if(item.type==='dining-table'){pb(1.45,.12,.95,wood,0,.72,0);for(const x of [-.58,.58])for(const z of [-.35,.35])pb(.1,.72,.1,dark,x,.36,z);}`
- [x] `if` — linha 3767, coluna 10 em `createFurnitureModel()` — `else if(item.type==='dining-table'){pb(1.45,.12,.95,wood,0,.72,0);for(const x of [-.58,.58])for(const z of [-.35,.35])pb(.1,.72,.1,dark,x,.36,z);}`
- [x] `for` — linha 3767, coluna 71 em `createFurnitureModel()` — `else if(item.type==='dining-table'){pb(1.45,.12,.95,wood,0,.72,0);for(const x of [-.58,.58])for(const z of [-.35,.35])pb(.1,.72,.1,dark,x,.36,z);}`
- [x] `for` — linha 3767, coluna 97 em `createFurnitureModel()` — `else if(item.type==='dining-table'){pb(1.45,.12,.95,wood,0,.72,0);for(const x of [-.58,.58])for(const z of [-.35,.35])pb(.1,.72,.1,dark,x,.36,z);}`
- [x] `else` — linha 3768, coluna 5 em `createFurnitureModel()` — `else if(item.type==='stove'){pb(.8,.9,.62,0xe8edf2,0,.45,0);pb(.78,.08,.64,dark,0,.93,0);for(const x of [-.22,.22])for(const z of [-.16,.16]){const burner=new THREE.Mesh(new THREE.CylinderGeometry(.11,.11,.025,12),mat(0x202733));burner.position.set(x,.99,z);g.add(burner);}pb(.55,.42,.04,0x27394a,0,.47,.33);}`
- [x] `if` — linha 3768, coluna 10 em `createFurnitureModel()` — `else if(item.type==='stove'){pb(.8,.9,.62,0xe8edf2,0,.45,0);pb(.78,.08,.64,dark,0,.93,0);for(const x of [-.22,.22])for(const z of [-.16,.16]){const burner=new THREE.Mesh(new THREE.CylinderGeometry(.11,.11,.025,12),mat(0x202733));burner.position.set(x,.99,z);g.add(burner);}pb(.55,.42,.04,0x27394a,0,.47,.33);}`
- [x] `for` — linha 3768, coluna 94 em `createFurnitureModel()` — `else if(item.type==='stove'){pb(.8,.9,.62,0xe8edf2,0,.45,0);pb(.78,.08,.64,dark,0,.93,0);for(const x of [-.22,.22])for(const z of [-.16,.16]){const burner=new THREE.Mesh(new THREE.CylinderGeometry(.11,.11,.025,12),mat(0x202733));burner.position.set(x,.99,z);g.add(burner);}pb(.55,.42,.04,0x27394a,0,.47,.33);}`
- [x] `for` — linha 3768, coluna 120 em `createFurnitureModel()` — `else if(item.type==='stove'){pb(.8,.9,.62,0xe8edf2,0,.45,0);pb(.78,.08,.64,dark,0,.93,0);for(const x of [-.22,.22])for(const z of [-.16,.16]){const burner=new THREE.Mesh(new THREE.CylinderGeometry(.11,.11,.025,12),mat(0x202733));burner.position.set(x,.99,z);g.add(burner);}pb(.55,.42,.04,0x27394a,0,.47,.33);}`
- [x] `else` — linha 3769, coluna 5 em `createFurnitureModel()` — `else if(item.type==='fridge'){pb(.8,1.75,.7,0xe7edf2,0,.88,0);pb(.05,1.55,.04,metal,.23,.9,.37);pb(.8,.06,.72,dark,0,.68,0);}`
- [x] `if` — linha 3769, coluna 10 em `createFurnitureModel()` — `else if(item.type==='fridge'){pb(.8,1.75,.7,0xe7edf2,0,.88,0);pb(.05,1.55,.04,metal,.23,.9,.37);pb(.8,.06,.72,dark,0,.68,0);}`
- [x] `else` — linha 3770, coluna 5 em `createFurnitureModel()` — `else if(['sink','bath-sink'].includes(item.type)){pb(spec.w,.78,spec.d,light,0,.39,0);const basin=new THREE.Mesh(new THREE.CylinderGeometry(.25,.22,.12,14),mat(0xb7d6e4));basin.position.set(0,.82,.02);g.add(basin);pb(.06,.34,.06,metal,0,.98,-.12);pb(.26,.05,.05,metal,.1,1.12,-.12);}`
- [x] `if` — linha 3770, coluna 10 em `createFurnitureModel()` — `else if(['sink','bath-sink'].includes(item.type)){pb(spec.w,.78,spec.d,light,0,.39,0);const basin=new THREE.Mesh(new THREE.CylinderGeometry(.25,.22,.12,14),mat(0xb7d6e4));basin.position.set(0,.82,.02);g.add(basin);pb(.06,.34,.06,metal,0,.98,-.12);pb(.26,.05,.05,metal,.1,1.12,-.12);}`
- [x] `else` — linha 3771, coluna 5 em `createFurnitureModel()` — `else if(item.type==='shower'){pb(.95,.05,.95,0x9bdff2,0,.03,0);pb(.06,1.75,.06,metal,-.38,.88,-.38);pb(.62,.05,.05,metal,-.08,1.62,-.38);const head=new THREE.Mesh(new THREE.CylinderGeometry(.12,.12,.08,12),mat(0x9aabb7));head.position.set(.2,1.55,-.38);head.rotation.z=Math.PI/2;g.add(head);}`
- [x] `if` — linha 3771, coluna 10 em `createFurnitureModel()` — `else if(item.type==='shower'){pb(.95,.05,.95,0x9bdff2,0,.03,0);pb(.06,1.75,.06,metal,-.38,.88,-.38);pb(.62,.05,.05,metal,-.08,1.62,-.38);const head=new THREE.Mesh(new THREE.CylinderGeometry(.12,.12,.08,12),mat(0x9aabb7));head.position.set(.2,1.55,-.38);head.rotation.z=Math.PI/2;g.add(head);}`
- [x] `else` — linha 3772, coluna 5 em `createFurnitureModel()` — `else if(item.type==='toilet'){pb(.62,.42,.68,light,0,.25,.12);const seat=new THREE.Mesh(new THREE.TorusGeometry(.24,.07,8,14),mat(0xdfe8ed));seat.position.set(0,.5,.13);seat.rotation.x=Math.PI/2;g.add(seat);pb(.58,.72,.28,light,0,.47,-.3);}`
- [x] `if` — linha 3772, coluna 10 em `createFurnitureModel()` — `else if(item.type==='toilet'){pb(.62,.42,.68,light,0,.25,.12);const seat=new THREE.Mesh(new THREE.TorusGeometry(.24,.07,8,14),mat(0xdfe8ed));seat.position.set(0,.5,.13);seat.rotation.x=Math.PI/2;g.add(seat);pb(.58,.72,.28,light,0,.47,-.3);}`
- [x] `else` — linha 3773, coluna 5 em `createFurnitureModel()` — `else if(item.type==='desk'){pb(1.48,.12,.66,wood,0,.72,0);pb(.12,.72,.12,dark,-.58,.36,0);pb(.12,.72,.12,dark,.58,.36,0);pb(.72,.55,.06,dark,0,1.12,-.14);pb(.62,.43,.04,0x216789,0,1.12,-.1);}`
- [x] `if` — linha 3773, coluna 10 em `createFurnitureModel()` — `else if(item.type==='desk'){pb(1.48,.12,.66,wood,0,.72,0);pb(.12,.72,.12,dark,-.58,.36,0);pb(.12,.72,.12,dark,.58,.36,0);pb(.72,.55,.06,dark,0,1.12,-.14);pb(.62,.43,.04,0x216789,0,1.12,-.1);}`
- [x] `else` — linha 3774, coluna 5 em `createFurnitureModel()` — `else if(item.type==='chair'){pb(.55,.12,.55,wood,0,.55,0);pb(.55,.76,.1,wood,0,.92,-.23);for(const x of [-.2,.2])for(const z of [-.2,.2])pb(.07,.55,.07,dark,x,.28,z);}`
- [x] `if` — linha 3774, coluna 10 em `createFurnitureModel()` — `else if(item.type==='chair'){pb(.55,.12,.55,wood,0,.55,0);pb(.55,.76,.1,wood,0,.92,-.23);for(const x of [-.2,.2])for(const z of [-.2,.2])pb(.07,.55,.07,dark,x,.28,z);}`
- [x] `for` — linha 3774, coluna 94 em `createFurnitureModel()` — `else if(item.type==='chair'){pb(.55,.12,.55,wood,0,.55,0);pb(.55,.76,.1,wood,0,.92,-.23);for(const x of [-.2,.2])for(const z of [-.2,.2])pb(.07,.55,.07,dark,x,.28,z);}`
- [x] `for` — linha 3774, coluna 118 em `createFurnitureModel()` — `else if(item.type==='chair'){pb(.55,.12,.55,wood,0,.55,0);pb(.55,.76,.1,wood,0,.92,-.23);for(const x of [-.2,.2])for(const z of [-.2,.2])pb(.07,.55,.07,dark,x,.28,z);}`
- [x] `else` — linha 3775, coluna 5 em `createFurnitureModel()` — `else if(item.type==='toolbench'){pb(1.52,.16,.62,wood,0,.8,0);for(const x of [-.62,.62])pb(.12,.8,.12,dark,x,.4,0);pb(1.45,.8,.1,0x51677a,0,1.22,-.25);for(const x of [-.45,0,.45])pb(.13,.4,.06,0xf0b33d,x,1.24,.32);}`
- [x] `if` — linha 3775, coluna 10 em `createFurnitureModel()` — `else if(item.type==='toolbench'){pb(1.52,.16,.62,wood,0,.8,0);for(const x of [-.62,.62])pb(.12,.8,.12,dark,x,.4,0);pb(1.45,.8,.1,0x51677a,0,1.22,-.25);for(const x of [-.45,0,.45])pb(.13,.4,.06,0xf0b33d,x,1.24,.32);}`
- [x] `for` — linha 3775, coluna 67 em `createFurnitureModel()` — `else if(item.type==='toolbench'){pb(1.52,.16,.62,wood,0,.8,0);for(const x of [-.62,.62])pb(.12,.8,.12,dark,x,.4,0);pb(1.45,.8,.1,0x51677a,0,1.22,-.25);for(const x of [-.45,0,.45])pb(.13,.4,.06,0xf0b33d,x,1.24,.32);}`
- [x] `for` — linha 3775, coluna 156 em `createFurnitureModel()` — `else if(item.type==='toolbench'){pb(1.52,.16,.62,wood,0,.8,0);for(const x of [-.62,.62])pb(.12,.8,.12,dark,x,.4,0);pb(1.45,.8,.1,0x51677a,0,1.22,-.25);for(const x of [-.45,0,.45])pb(.13,.4,.06,0xf0b33d,x,1.24,.32);}`
- [x] `else` — linha 3776, coluna 5 em `createFurnitureModel()` — `else if(item.type==='bench'){pb(1.58,.16,.52,wood,0,.55,0);pb(1.58,.62,.12,wood,0,.88,-.2);for(const x of [-.58,.58])pb(.1,.55,.1,dark,x,.28,0);}`
- [x] `if` — linha 3776, coluna 10 em `createFurnitureModel()` — `else if(item.type==='bench'){pb(1.58,.16,.52,wood,0,.55,0);pb(1.58,.62,.12,wood,0,.88,-.2);for(const x of [-.58,.58])pb(.1,.55,.1,dark,x,.28,0);}`
- [x] `for` — linha 3776, coluna 96 em `createFurnitureModel()` — `else if(item.type==='bench'){pb(1.58,.16,.52,wood,0,.55,0);pb(1.58,.62,.12,wood,0,.88,-.2);for(const x of [-.58,.58])pb(.1,.55,.1,dark,x,.28,0);}`
- [x] `else` — linha 3777, coluna 5 em `createFurnitureModel()` — `else if(item.type==='plant'){const pot=new THREE.Mesh(new THREE.CylinderGeometry(.28,.22,.48,10),mat(0xb55f34));pot.position.y=.25;g.add(pot);for(const x of [-.18,0,.18]){pb(.06,.72,.06,green,x,.76,0);pb(.28,.16,.18,shadeColor(green,x===0?15:-10),x,.98,0);}}`
- [x] `if` — linha 3777, coluna 10 em `createFurnitureModel()` — `else if(item.type==='plant'){const pot=new THREE.Mesh(new THREE.CylinderGeometry(.28,.22,.48,10),mat(0xb55f34));pot.position.y=.25;g.add(pot);for(const x of [-.18,0,.18]){pb(.06,.72,.06,green,x,.76,0);pb(.28,.16,.18,shadeColor(green,x===0?15:-10),x,.98,0);}}`
- [x] `for` — linha 3777, coluna 147 em `createFurnitureModel()` — `else if(item.type==='plant'){const pot=new THREE.Mesh(new THREE.CylinderGeometry(.28,.22,.48,10),mat(0xb55f34));pot.position.y=.25;g.add(pot);for(const x of [-.18,0,.18]){pb(.06,.72,.06,green,x,.76,0);pb(.28,.16,.18,shadeColor(green,x===0?15:-10),x,.98,0);}}`
- [x] `else` — linha 3778, coluna 5 em `createFurnitureModel()` — `else if(item.type==='chest'){pb(1.05,.58,.65,wood,0,.3,0);pb(1.08,.18,.68,shadeColor(wood,-15),0,.64,0);pb(.1,.5,.04,metal,0,.38,.35);}`
- [x] `if` — linha 3778, coluna 10 em `createFurnitureModel()` — `else if(item.type==='chest'){pb(1.05,.58,.65,wood,0,.3,0);pb(1.08,.18,.68,shadeColor(wood,-15),0,.64,0);pb(.1,.5,.04,metal,0,.38,.35);}`
- [x] `else` — linha 3779, coluna 5 em `createFurnitureModel()` — `else if(item.type==='crate'){pb(.7,.7,.7,wood,0,.35,0);pb(.76,.08,.08,dark,0,.38,.36);pb(.08,.76,.08,dark,-.36,.38,0);pb(.08,.76,.08,dark,.36,.38,0);}`
- [x] `if` — linha 3779, coluna 10 em `createFurnitureModel()` — `else if(item.type==='crate'){pb(.7,.7,.7,wood,0,.35,0);pb(.76,.08,.08,dark,0,.38,.36);pb(.08,.76,.08,dark,-.36,.38,0);pb(.08,.76,.08,dark,.36,.38,0);}`
- [x] `else` — linha 3780, coluna 5 em `createFurnitureModel()` — `else{pb(spec.w,.65,spec.d,accent,0,.34,0);}g.userData.furnitureId=item.id;return g;`
- [x] `return` — linha 3780, coluna 79 em `createFurnitureModel()` — `else{pb(spec.w,.65,spec.d,accent,0,.34,0);}g.userData.furnitureId=item.id;return g;`
- [x] `if` — linha 3782, coluna 45 em `renderExtensionFurniture()` — `function renderExtensionFurniture(record){if(!record)return;if(record.furnitureGroup)disposeFurnitureGroup(record.furnitureGroup);record.furnitureGroup=new THREE.Group();record.furnitureGroup.name=\`OTTHI_FURNITURE_${record.data.id}\`;record.group.add(record.furnitureGroup);for(const item of furnitureForExtension(record.data.id)){const mesh=createFurnitureModel(item,record.furnitureGroup,false,true);if(mesh)world.extensionFurniture.push({item,mesh,extensionId:record.data.id});}}`
- [x] `return` — linha 3782, coluna 56 em `renderExtensionFurniture()` — `function renderExtensionFurniture(record){if(!record)return;if(record.furnitureGroup)disposeFurnitureGroup(record.furnitureGroup);record.furnitureGroup=new THREE.Group();record.furnitureGroup.name=\`OTTHI_FURNITURE_${record.data.id}\`;record.group.add(record.furnitureGroup);for(const item of furnitureForExtension(record.data.id)){const mesh=createFurnitureModel(item,record.furnitureGroup,false,true);if(mesh)world.extensionFurniture.push({item,mesh,extensionId:record.data.id});}}`
- [x] `if` — linha 3782, coluna 63 em `renderExtensionFurniture()` — `function renderExtensionFurniture(record){if(!record)return;if(record.furnitureGroup)disposeFurnitureGroup(record.furnitureGroup);record.furnitureGroup=new THREE.Group();record.furnitureGroup.name=\`OTTHI_FURNITURE_${record.data.id}\`;record.group.add(record.furnitureGroup);for(const item of furnitureForExtension(record.data.id)){const mesh=createFurnitureModel(item,record.furnitureGroup,false,true);if(mesh)world.extensionFurniture.push({item,mesh,extensionId:record.data.id});}}`
- [x] `for` — linha 3782, coluna 276 em `renderExtensionFurniture()` — `function renderExtensionFurniture(record){if(!record)return;if(record.furnitureGroup)disposeFurnitureGroup(record.furnitureGroup);record.furnitureGroup=new THREE.Group();record.furnitureGroup.name=\`OTTHI_FURNITURE_${record.data.id}\`;record.group.add(record.furnitureGroup);for(const item of furnitureForExtension(record.data.id)){const mesh=createFurnitureModel(item,record.furnitureGroup,false,true);if(mesh)world.extensionFurniture.push({item,mesh,extensionId:record.data.id});}}`
- [x] `if` — linha 3782, coluna 404 em `renderExtensionFurniture()` — `function renderExtensionFurniture(record){if(!record)return;if(record.furnitureGroup)disposeFurnitureGroup(record.furnitureGroup);record.furnitureGroup=new THREE.Group();record.furnitureGroup.name=\`OTTHI_FURNITURE_${record.data.id}\`;record.group.add(record.furnitureGroup);for(const item of furnitureForExtension(record.data.id)){const mesh=createFurnitureModel(item,record.furnitureGroup,false,true);if(mesh)world.extensionFurniture.push({item,mesh,extensionId:record.data.id});}}`
- [x] `if` — linha 3783, coluna 67 em `renderFurniturePreview()` — `function renderFurniturePreview(record){clearFurniturePreview();if(!record||!furnitureDraft)return;const valid=furniturePlacementValid(record.data.id,furnitureDraft,furnitureDraft.id||'');furniturePreview=createFurnitureModel(furnitureDraft,record.group,true,valid);return valid;}`
- [x] `return` — linha 3783, coluna 95 em `renderFurniturePreview()` — `function renderFurniturePreview(record){clearFurniturePreview();if(!record||!furnitureDraft)return;const valid=furniturePlacementValid(record.data.id,furnitureDraft,furnitureDraft.id||'');furniturePreview=createFurnitureModel(furnitureDraft,record.group,true,valid);return valid;}`
- [x] `return` — linha 3783, coluna 269 em `renderFurniturePreview()` — `function renderFurniturePreview(record){clearFurniturePreview();if(!record||!furnitureDraft)return;const valid=furniturePlacementValid(record.data.id,furnitureDraft,furnitureDraft.id||'');furniturePreview=createFurnitureModel(furnitureDraft,record.group,true,valid);return valid;}`
- [x] `for` — linha 3784, coluna 93 em `nextFurnitureSlot()` — `function nextFurnitureSlot(record,draft,direction=1){let index=Number(draft.slotIndex||0);for(let attempt=0;attempt<FURNITURE_SLOTS.length;attempt++){index=(index+direction+FURNITURE_SLOTS.length)%FURNITURE_SLOTS.length;const slot=FURNITURE_SLOTS[index],candidate={...draft,slotIndex:index,x:slot.x,z:slot.z};if(furniturePlacementValid(record.data.id,candidate,candidate.id||''))return candidate;}const slot=FURNITURE_SLOTS[index];return{...draft,slotIndex:index,x:slot.x,z:slot.z};}`
- [x] `if` — linha 3784, coluna 312 em `nextFurnitureSlot()` — `function nextFurnitureSlot(record,draft,direction=1){let index=Number(draft.slotIndex||0);for(let attempt=0;attempt<FURNITURE_SLOTS.length;attempt++){index=(index+direction+FURNITURE_SLOTS.length)%FURNITURE_SLOTS.length;const slot=FURNITURE_SLOTS[index],candidate={...draft,slotIndex:index,x:slot.x,z:slot.z};if(furniturePlacementValid(record.data.id,candidate,candidate.id||''))return candidate;}const slot=FURNITURE_SLOTS[index];return{...draft,slotIndex:index,x:slot.x,z:slot.z};}`
- [x] `return` — linha 3784, coluna 382 em `nextFurnitureSlot()` — `function nextFurnitureSlot(record,draft,direction=1){let index=Number(draft.slotIndex||0);for(let attempt=0;attempt<FURNITURE_SLOTS.length;attempt++){index=(index+direction+FURNITURE_SLOTS.length)%FURNITURE_SLOTS.length;const slot=FURNITURE_SLOTS[index],candidate={...draft,slotIndex:index,x:slot.x,z:slot.z};if(furniturePlacementValid(record.data.id,candidate,candidate.id||''))return candidate;}const slot=FURNITURE_SLOTS[index];return{...draft,slotIndex:index,x:slot.x,z:slot.z};}`
- [x] `return` — linha 3784, coluna 434 em `nextFurnitureSlot()` — `function nextFurnitureSlot(record,draft,direction=1){let index=Number(draft.slotIndex||0);for(let attempt=0;attempt<FURNITURE_SLOTS.length;attempt++){index=(index+direction+FURNITURE_SLOTS.length)%FURNITURE_SLOTS.length;const slot=FURNITURE_SLOTS[index],candidate={...draft,slotIndex:index,x:slot.x,z:slot.z};if(furniturePlacementValid(record.data.id,candidate,candidate.id||''))return candidate;}const slot=FURNITURE_SLOTS[index];return{...draft,slotIndex:index,x:slot.x,z:slot.z};}`
- [x] `if` — linha 3785, coluna 141 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `return` — linha 3785, coluna 231 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `if` — linha 3785, coluna 371 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `return` — linha 3785, coluna 380 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `if` — linha 3785, coluna 708 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `if` — linha 3785, coluna 1965 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `return` — linha 3785, coluna 2098 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `if` — linha 3785, coluna 2106 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `return` — linha 3785, coluna 2206 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `if` — linha 3785, coluna 2214 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `if` — linha 3785, coluna 2348 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `else` — linha 3785, coluna 2465 em `openFurniturePlacement()` — `function openFurniturePlacement(extensionId,type,existingId=''){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record||record.data.remote){toast('Somente o dono pode mobiliar este cômodo.','warn');return;}const existing=(state.roomFurniture||[]).find(item=>item.id===existingId),spec=furnitureSpec(type||existing?.type,record.data.type);if(!spec)return;const first=existing?{...existing}:{id:'',extensionId,type:spec.id,slotIndex:-1,x:0,z:0,rotation:0,createdAt:Date.now()};furnitureDraft=existing?first:nextFurnitureSlot(record,first,1);renderFurniturePreview(record);const refresh=root=>{const valid=renderFurniturePreview(record),status=$('[data-furniture-status]',root);if(status){status.textContent=valid?'✓ Posição livre':'⚠ Escolha outra posição';status.className=\`extension-status ${valid?'good':'bad'}\`;}};openModal(existing?'Mover ou girar móvel':\`Adicionar: ${spec.name}\`,\`<div class="furniture-placement-card"><span>${spec.icon}</span><h3>${spec.name}</h3><`
- [x] `if` — linha 3786, coluna 206 em `openFurniturePlacement()` — `async function removeRoomFurniture(extensionId,furnitureId){const item=(state.roomFurniture||[]).find(entry=>entry.id===furnitureId),record=world.houseExtensions.find(entry=>entry.data.id===extensionId);if(!item||!record)return;if(!(await confirmModal('Remover móvel',\`Remover ${furnitureSpec(item.type,record.data.type)?.name||'este móvel'} do cômodo?\`,'Remover','Cancelar'))){openFurnitureManager(extensionId);return;}state.roomFurniture=state.roomFurniture.filter(entry=>entry.id!==furnitureId);renderExtensionFurniture(record);saveState(true);toast('Móvel removido.','good');openFurnitureManager(extensionId);}`
- [x] `return` — linha 3786, coluna 224 em `openFurniturePlacement()` — `async function removeRoomFurniture(extensionId,furnitureId){const item=(state.roomFurniture||[]).find(entry=>entry.id===furnitureId),record=world.houseExtensions.find(entry=>entry.data.id===extensionId);if(!item||!record)return;if(!(await confirmModal('Remover móvel',\`Remover ${furnitureSpec(item.type,record.data.type)?.name||'este móvel'} do cômodo?\`,'Remover','Cancelar'))){openFurnitureManager(extensionId);return;}state.roomFurniture=state.roomFurniture.filter(entry=>entry.id!==furnitureId);renderExtensionFurniture(record);saveState(true);toast('Móvel removido.','good');openFurnitureManager(extensionId);}`
- [x] `if` — linha 3786, coluna 231 em `openFurniturePlacement()` — `async function removeRoomFurniture(extensionId,furnitureId){const item=(state.roomFurniture||[]).find(entry=>entry.id===furnitureId),record=world.houseExtensions.find(entry=>entry.data.id===extensionId);if(!item||!record)return;if(!(await confirmModal('Remover móvel',\`Remover ${furnitureSpec(item.type,record.data.type)?.name||'este móvel'} do cômodo?\`,'Remover','Cancelar'))){openFurnitureManager(extensionId);return;}state.roomFurniture=state.roomFurniture.filter(entry=>entry.id!==furnitureId);renderExtensionFurniture(record);saveState(true);toast('Móvel removido.','good');openFurnitureManager(extensionId);}`
- [x] `return` — linha 3786, coluna 415 em `openFurniturePlacement()` — `async function removeRoomFurniture(extensionId,furnitureId){const item=(state.roomFurniture||[]).find(entry=>entry.id===furnitureId),record=world.houseExtensions.find(entry=>entry.data.id===extensionId);if(!item||!record)return;if(!(await confirmModal('Remover móvel',\`Remover ${furnitureSpec(item.type,record.data.type)?.name||'este móvel'} do cômodo?\`,'Remover','Cancelar'))){openFurnitureManager(extensionId);return;}state.roomFurniture=state.roomFurniture.filter(entry=>entry.id!==furnitureId);renderExtensionFurniture(record);saveState(true);toast('Móvel removido.','good');openFurnitureManager(extensionId);}`
- [x] `if` — linha 3787, coluna 120 em `openFurnitureManager()` — `function openFurnitureManager(extensionId){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record)return;const spec=ROOM_SPECS[record.data.type]||ROOM_SPECS.storage,owned=!record.data.remote,items=furnitureForExtension(extensionId),catalog=furnitureCatalogForRoom(record.data.type);openModal(\`Mobiliar ${spec.name}\`,\`<div class="room-furniture-heading"><span>${spec.icon}</span><div><h3>${spec.name}</h3><p>${owned?'Escolha móveis, mova, gire ou remova sem alterar o cômodo.':'Visite o cômodo mobiliado pelo proprietário.'}</p></div></div>${owned?\`<h3>Adicionar móvel</h3><div class="choice-grid furniture-catalog">${catalog.map(item=>\`<button class="choice" data-add-furniture="${item.id}"><b>${item.icon} ${item.name}</b><span>${item.price} moedas</span></button>\`).join('')}</div>\`:''}<h3>Móveis colocados</h3><div class="furniture-list">${items.length?items.map(item=>{const furniture=furnitureSpec(item.type,record.data.type);return\`<article><span>${furniture?.icon`
- [x] `return` — linha 3787, coluna 131 em `openFurnitureManager()` — `function openFurnitureManager(extensionId){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record)return;const spec=ROOM_SPECS[record.data.type]||ROOM_SPECS.storage,owned=!record.data.remote,items=furnitureForExtension(extensionId),catalog=furnitureCatalogForRoom(record.data.type);openModal(\`Mobiliar ${spec.name}\`,\`<div class="room-furniture-heading"><span>${spec.icon}</span><div><h3>${spec.name}</h3><p>${owned?'Escolha móveis, mova, gire ou remova sem alterar o cômodo.':'Visite o cômodo mobiliado pelo proprietário.'}</p></div></div>${owned?\`<h3>Adicionar móvel</h3><div class="choice-grid furniture-catalog">${catalog.map(item=>\`<button class="choice" data-add-furniture="${item.id}"><b>${item.icon} ${item.name}</b><span>${item.price} moedas</span></button>\`).join('')}</div>\`:''}<h3>Móveis colocados</h3><div class="furniture-list">${items.length?items.map(item=>{const furniture=furnitureSpec(item.type,record.data.type);return\`<article><span>${furniture?.icon`
- [x] `return` — linha 3787, coluna 964 em `openFurnitureManager()` — `function openFurnitureManager(extensionId){const record=world.houseExtensions.find(item=>item.data.id===extensionId);if(!record)return;const spec=ROOM_SPECS[record.data.type]||ROOM_SPECS.storage,owned=!record.data.remote,items=furnitureForExtension(extensionId),catalog=furnitureCatalogForRoom(record.data.type);openModal(\`Mobiliar ${spec.name}\`,\`<div class="room-furniture-heading"><span>${spec.icon}</span><div><h3>${spec.name}</h3><p>${owned?'Escolha móveis, mova, gire ou remova sem alterar o cômodo.':'Visite o cômodo mobiliado pelo proprietário.'}</p></div></div>${owned?\`<h3>Adicionar móvel</h3><div class="choice-grid furniture-catalog">${catalog.map(item=>\`<button class="choice" data-add-furniture="${item.id}"><b>${item.icon} ${item.name}</b><span>${item.price} moedas</span></button>\`).join('')}</div>\`:''}<h3>Móveis colocados</h3><div class="furniture-list">${items.length?items.map(item=>{const furniture=furnitureSpec(item.type,record.data.type);return\`<article><span>${furniture?.icon`
- [x] `if` — linha 3788, coluna 51 em `spawnHouseExtension()` — `function spawnHouseExtension(data,remote=false){if(!data?.id||world.houseExtensions.some(x=>x.data.id===data.id))return;const spec=ROOM_SPECS[data.type]||ROOM_SPECS.storage,g=new THREE.Group();g.position.set(data.x,0,data.z);g.rotation.y=Number(data.rotation||0);worldGroup.add(g);premiumBox(5.2,.22,5.2,spec.color,0,.11,0,g);premiumBox(5.25,.32,.18,0xf1eadb,0,1.55,-2.51,g);premiumBox(.18,3.0,5.2,0xf1eadb,-2.51,1.5,0,g);premiumBox(.18,3.0,5.2,0xf1eadb,2.51,1.5,0,g);premiumBox(5.5,.35,5.5,shadeColor(spec.color,-20),0,3.12,0,g);premiumBox(1.35,2.25,.12,0x684329,0,1.12,2.53,g);registerPlatform(data.x,data.z,5.2,5.2,.23,{extensionId:data.id});const record={data:{...data,remote},group:g,furnitureGroup:null};world.houseExtensions.push(record);renderExtensionFurniture(record);registerInteractable({id:\`extension-${data.id}\`,type:'house-extension',icon:spec.icon,label:remote?\`Visitar ${spec.name}\`:\`Abrir e mobiliar ${spec.name}\`,x:data.x,z:data.z,radius:3.3,priority:125,action:()=>openFurnitureMa`
- [x] `return` — linha 3788, coluna 116 em `spawnHouseExtension()` — `function spawnHouseExtension(data,remote=false){if(!data?.id||world.houseExtensions.some(x=>x.data.id===data.id))return;const spec=ROOM_SPECS[data.type]||ROOM_SPECS.storage,g=new THREE.Group();g.position.set(data.x,0,data.z);g.rotation.y=Number(data.rotation||0);worldGroup.add(g);premiumBox(5.2,.22,5.2,spec.color,0,.11,0,g);premiumBox(5.25,.32,.18,0xf1eadb,0,1.55,-2.51,g);premiumBox(.18,3.0,5.2,0xf1eadb,-2.51,1.5,0,g);premiumBox(.18,3.0,5.2,0xf1eadb,2.51,1.5,0,g);premiumBox(5.5,.35,5.5,shadeColor(spec.color,-20),0,3.12,0,g);premiumBox(1.35,2.25,.12,0x684329,0,1.12,2.53,g);registerPlatform(data.x,data.z,5.2,5.2,.23,{extensionId:data.id});const record={data:{...data,remote},group:g,furnitureGroup:null};world.houseExtensions.push(record);renderExtensionFurniture(record);registerInteractable({id:\`extension-${data.id}\`,type:'house-extension',icon:spec.icon,label:remote?\`Visitar ${spec.name}\`:\`Abrir e mobiliar ${spec.name}\`,x:data.x,z:data.z,radius:3.3,priority:125,action:()=>openFurnitureMa`
- [x] `return` — linha 3789, coluna 34 em `resourcesEnough()` — `function resourcesEnough(cost){return Object.entries(cost).every(([k,v])=>Number(state.inventory[k]||0)>=v);}`
- [x] `return` — linha 3790, coluna 27 em `costText()` — `function costText(cost){return Object.entries(cost).map(([k,v])=>\`${v} ${k==='wood'?'madeiras':k==='stone'?'pedras':'blocos'}\`).join(' + ');}`
- [x] `if` — linha 3791, coluna 88 em `openHouseExtensionPlanner()` — `function openHouseExtensionPlanner(type){const house=nearestOwnedHouseForExtension();if(!house||distance2D(player,house)>16){toast('Chegue perto de uma casa que você possui.','warn');return;}let side=0;const spec=ROOM_SPECS[type];const place=extensionPlacement(house,side);extensionDraft={id:uid(),houseId:house.id,type,side,...place,ownerId:state.profile.playerId,ownerName:playerDisplayName()};renderExtensionPreview();openModal(\`Ampliar: ${spec.name}\`,\`<p>A prévia transparente aparece ao lado da casa. Ajuste o lado e a rotação antes de confirmar.</p><div class="extension-status" data-extension-status></div><div class="modal-actions"><button class="btn" data-side>Próximo lado</button><button class="btn" data-rotate>Girar</button><button class="btn primary" data-confirm-extension>Confirmar • ${costText(spec.cost)}</button><button class="btn danger" data-cancel-extension>Cancelar</button></div>\`,root=>{const status=$('[data-extension-status]',root),update=()=>{const p=extensionPlacement(ho`
- [x] `return` — linha 3791, coluna 186 em `openHouseExtensionPlanner()` — `function openHouseExtensionPlanner(type){const house=nearestOwnedHouseForExtension();if(!house||distance2D(player,house)>16){toast('Chegue perto de uma casa que você possui.','warn');return;}let side=0;const spec=ROOM_SPECS[type];const place=extensionPlacement(house,side);extensionDraft={id:uid(),houseId:house.id,type,side,...place,ownerId:state.profile.playerId,ownerName:playerDisplayName()};renderExtensionPreview();openModal(\`Ampliar: ${spec.name}\`,\`<p>A prévia transparente aparece ao lado da casa. Ajuste o lado e a rotação antes de confirmar.</p><div class="extension-status" data-extension-status></div><div class="modal-actions"><button class="btn" data-side>Próximo lado</button><button class="btn" data-rotate>Girar</button><button class="btn primary" data-confirm-extension>Confirmar • ${costText(spec.cost)}</button><button class="btn danger" data-cancel-extension>Cancelar</button></div>\`,root=>{const status=$('[data-extension-status]',root),update=()=>{const p=extensionPlacement(ho`
- [x] `if` — linha 3791, coluna 1593 em `openHouseExtensionPlanner()` — `function openHouseExtensionPlanner(type){const house=nearestOwnedHouseForExtension();if(!house||distance2D(player,house)>16){toast('Chegue perto de uma casa que você possui.','warn');return;}let side=0;const spec=ROOM_SPECS[type];const place=extensionPlacement(house,side);extensionDraft={id:uid(),houseId:house.id,type,side,...place,ownerId:state.profile.playerId,ownerName:playerDisplayName()};renderExtensionPreview();openModal(\`Ampliar: ${spec.name}\`,\`<p>A prévia transparente aparece ao lado da casa. Ajuste o lado e a rotação antes de confirmar.</p><div class="extension-status" data-extension-status></div><div class="modal-actions"><button class="btn" data-side>Próximo lado</button><button class="btn" data-rotate>Girar</button><button class="btn primary" data-confirm-extension>Confirmar • ${costText(spec.cost)}</button><button class="btn danger" data-cancel-extension>Cancelar</button></div>\`,root=>{const status=$('[data-extension-status]',root),update=()=>{const p=extensionPlacement(ho`
- [x] `return` — linha 3791, coluna 1676 em `openHouseExtensionPlanner()` — `function openHouseExtensionPlanner(type){const house=nearestOwnedHouseForExtension();if(!house||distance2D(player,house)>16){toast('Chegue perto de uma casa que você possui.','warn');return;}let side=0;const spec=ROOM_SPECS[type];const place=extensionPlacement(house,side);extensionDraft={id:uid(),houseId:house.id,type,side,...place,ownerId:state.profile.playerId,ownerName:playerDisplayName()};renderExtensionPreview();openModal(\`Ampliar: ${spec.name}\`,\`<p>A prévia transparente aparece ao lado da casa. Ajuste o lado e a rotação antes de confirmar.</p><div class="extension-status" data-extension-status></div><div class="modal-actions"><button class="btn" data-side>Próximo lado</button><button class="btn" data-rotate>Girar</button><button class="btn primary" data-confirm-extension>Confirmar • ${costText(spec.cost)}</button><button class="btn danger" data-cancel-extension>Cancelar</button></div>\`,root=>{const status=$('[data-extension-status]',root),update=()=>{const p=extensionPlacement(ho`
- [x] `if` — linha 3791, coluna 1684 em `openHouseExtensionPlanner()` — `function openHouseExtensionPlanner(type){const house=nearestOwnedHouseForExtension();if(!house||distance2D(player,house)>16){toast('Chegue perto de uma casa que você possui.','warn');return;}let side=0;const spec=ROOM_SPECS[type];const place=extensionPlacement(house,side);extensionDraft={id:uid(),houseId:house.id,type,side,...place,ownerId:state.profile.playerId,ownerName:playerDisplayName()};renderExtensionPreview();openModal(\`Ampliar: ${spec.name}\`,\`<p>A prévia transparente aparece ao lado da casa. Ajuste o lado e a rotação antes de confirmar.</p><div class="extension-status" data-extension-status></div><div class="modal-actions"><button class="btn" data-side>Próximo lado</button><button class="btn" data-rotate>Girar</button><button class="btn primary" data-confirm-extension>Confirmar • ${costText(spec.cost)}</button><button class="btn danger" data-cancel-extension>Cancelar</button></div>\`,root=>{const status=$('[data-extension-status]',root),update=()=>{const p=extensionPlacement(ho`
- [x] `return` — linha 3791, coluna 1778 em `openHouseExtensionPlanner()` — `function openHouseExtensionPlanner(type){const house=nearestOwnedHouseForExtension();if(!house||distance2D(player,house)>16){toast('Chegue perto de uma casa que você possui.','warn');return;}let side=0;const spec=ROOM_SPECS[type];const place=extensionPlacement(house,side);extensionDraft={id:uid(),houseId:house.id,type,side,...place,ownerId:state.profile.playerId,ownerName:playerDisplayName()};renderExtensionPreview();openModal(\`Ampliar: ${spec.name}\`,\`<p>A prévia transparente aparece ao lado da casa. Ajuste o lado e a rotação antes de confirmar.</p><div class="extension-status" data-extension-status></div><div class="modal-actions"><button class="btn" data-side>Próximo lado</button><button class="btn" data-rotate>Girar</button><button class="btn primary" data-confirm-extension>Confirmar • ${costText(spec.cost)}</button><button class="btn danger" data-cancel-extension>Cancelar</button></div>\`,root=>{const status=$('[data-extension-status]',root),update=()=>{const p=extensionPlacement(ho`
- [x] `for` — linha 3791, coluna 1786 em `openHouseExtensionPlanner()` — `function openHouseExtensionPlanner(type){const house=nearestOwnedHouseForExtension();if(!house||distance2D(player,house)>16){toast('Chegue perto de uma casa que você possui.','warn');return;}let side=0;const spec=ROOM_SPECS[type];const place=extensionPlacement(house,side);extensionDraft={id:uid(),houseId:house.id,type,side,...place,ownerId:state.profile.playerId,ownerName:playerDisplayName()};renderExtensionPreview();openModal(\`Ampliar: ${spec.name}\`,\`<p>A prévia transparente aparece ao lado da casa. Ajuste o lado e a rotação antes de confirmar.</p><div class="extension-status" data-extension-status></div><div class="modal-actions"><button class="btn" data-side>Próximo lado</button><button class="btn" data-rotate>Girar</button><button class="btn primary" data-confirm-extension>Confirmar • ${costText(spec.cost)}</button><button class="btn danger" data-cancel-extension>Cancelar</button></div>\`,root=>{const status=$('[data-extension-status]',root),update=()=>{const p=extensionPlacement(ho`
- [x] `for` — linha 3794, coluna 97 em `createLakeExpansion()` — `premiumBox(12,.32,3,materials.wood,-30,.34,52);registerPlatform(-30,52,12,3,.5,{pier:true});for(const z of [44.8,59.2])premiumBox(2.2,.35,.35,0xd7c7a0,-25.5,.25,z);createSignpost(-22,47,'Píer e Pesca',Math.PI/2);`
- [x] `for` — linha 3795, coluna 5 em `createLakeExpansion()` — `for(const [x,z,s] of [[-23.8,42.9,.75],[-26.5,42.4,.55],[-29.4,42.7,.68],[-32.2,43.1,.52],[-23.9,60.8,.68],[-27.1,61.2,.55],[-30.4,61.0,.72]]){premiumBox(1.1*s,.28*s,.8*s,materials.stone,x,.1,z);for(let i=0;i<3;i++){const reed=premiumCylinder(.035,.85+Math.random()*.35,0x4f8f42,x+(i-1)*.18,.45,z+(Math.random()-.5)*.28,worldGroup,6);reed.rotation.z=(Math.random()-.5)*.12;}}`
- [x] `for` — linha 3795, coluna 200 em `createLakeExpansion()` — `for(const [x,z,s] of [[-23.8,42.9,.75],[-26.5,42.4,.55],[-29.4,42.7,.68],[-32.2,43.1,.52],[-23.9,60.8,.68],[-27.1,61.2,.55],[-30.4,61.0,.72]]){premiumBox(1.1*s,.28*s,.8*s,materials.stone,x,.1,z);for(let i=0;i<3;i++){const reed=premiumCylinder(.035,.85+Math.random()*.35,0x4f8f42,x+(i-1)*.18,.45,z+(Math.random()-.5)*.28,worldGroup,6);reed.rotation.z=(Math.random()-.5)*.12;}}`
- [x] `for` — linha 3798, coluna 35 em `restoreLifeExpansion()` — `function restoreLifeExpansion(){for(const c of state.campfires){if(Number(c.expiresAt||0)>Date.now())spawnCampfire(c);if(c.cooking&&Number(c.cooking.endsAt||0)<=Date.now())finishCampfireCooking(c,c.cooking.id);}for(const e of state.houseExtensions)spawnHouseExtension(e);}`
- [x] `if` — linha 3798, coluna 67 em `restoreLifeExpansion()` — `function restoreLifeExpansion(){for(const c of state.campfires){if(Number(c.expiresAt||0)>Date.now())spawnCampfire(c);if(c.cooking&&Number(c.cooking.endsAt||0)<=Date.now())finishCampfireCooking(c,c.cooking.id);}for(const e of state.houseExtensions)spawnHouseExtension(e);}`
- [x] `if` — linha 3798, coluna 121 em `restoreLifeExpansion()` — `function restoreLifeExpansion(){for(const c of state.campfires){if(Number(c.expiresAt||0)>Date.now())spawnCampfire(c);if(c.cooking&&Number(c.cooking.endsAt||0)<=Date.now())finishCampfireCooking(c,c.cooking.id);}for(const e of state.houseExtensions)spawnHouseExtension(e);}`
- [x] `for` — linha 3798, coluna 214 em `restoreLifeExpansion()` — `function restoreLifeExpansion(){for(const c of state.campfires){if(Number(c.expiresAt||0)>Date.now())spawnCampfire(c);if(c.cooking&&Number(c.cooking.endsAt||0)<=Date.now())finishCampfireCooking(c,c.cooking.id);}for(const e of state.houseExtensions)spawnHouseExtension(e);}`
- [x] `if` — linha 3799, coluna 82 em `updateLifeActivities()` — `function updateLifeActivities(dt){updateShoreFishers(dt);world.activityAcc+=dt;if(world.activityAcc<.5)return;world.activityAcc=0;const now=Date.now();for(const c of [...world.campfires]){if(c.flame)c.flame.scale.y=.85+Math.sin(performance.now()*.009+c.data.x)*.18;if(c.data.cooking&&Number(c.data.cooking.endsAt||0)<=now)finishCampfireCooking(c.data,c.data.cooking.id);if(Number(c.data.expiresAt||0)<=now){c.data.expired=true;c.interactable.disabled=true;worldGroup.remove(c.group);world.campfires=world.campfires.filter(x=>x!==c);if(!c.data.remote)state.campfires=state.campfires.filter(x=>x.id!==c.data.id);}}`
- [x] `return` — linha 3799, coluna 106 em `updateLifeActivities()` — `function updateLifeActivities(dt){updateShoreFishers(dt);world.activityAcc+=dt;if(world.activityAcc<.5)return;world.activityAcc=0;const now=Date.now();for(const c of [...world.campfires]){if(c.flame)c.flame.scale.y=.85+Math.sin(performance.now()*.009+c.data.x)*.18;if(c.data.cooking&&Number(c.data.cooking.endsAt||0)<=now)finishCampfireCooking(c.data,c.data.cooking.id);if(Number(c.data.expiresAt||0)<=now){c.data.expired=true;c.interactable.disabled=true;worldGroup.remove(c.group);world.campfires=world.campfires.filter(x=>x!==c);if(!c.data.remote)state.campfires=state.campfires.filter(x=>x.id!==c.data.id);}}`
- [x] `for` — linha 3799, coluna 154 em `updateLifeActivities()` — `function updateLifeActivities(dt){updateShoreFishers(dt);world.activityAcc+=dt;if(world.activityAcc<.5)return;world.activityAcc=0;const now=Date.now();for(const c of [...world.campfires]){if(c.flame)c.flame.scale.y=.85+Math.sin(performance.now()*.009+c.data.x)*.18;if(c.data.cooking&&Number(c.data.cooking.endsAt||0)<=now)finishCampfireCooking(c.data,c.data.cooking.id);if(Number(c.data.expiresAt||0)<=now){c.data.expired=true;c.interactable.disabled=true;worldGroup.remove(c.group);world.campfires=world.campfires.filter(x=>x!==c);if(!c.data.remote)state.campfires=state.campfires.filter(x=>x.id!==c.data.id);}}`
- [x] `if` — linha 3799, coluna 191 em `updateLifeActivities()` — `function updateLifeActivities(dt){updateShoreFishers(dt);world.activityAcc+=dt;if(world.activityAcc<.5)return;world.activityAcc=0;const now=Date.now();for(const c of [...world.campfires]){if(c.flame)c.flame.scale.y=.85+Math.sin(performance.now()*.009+c.data.x)*.18;if(c.data.cooking&&Number(c.data.cooking.endsAt||0)<=now)finishCampfireCooking(c.data,c.data.cooking.id);if(Number(c.data.expiresAt||0)<=now){c.data.expired=true;c.interactable.disabled=true;worldGroup.remove(c.group);world.campfires=world.campfires.filter(x=>x!==c);if(!c.data.remote)state.campfires=state.campfires.filter(x=>x.id!==c.data.id);}}`
- [x] `if` — linha 3799, coluna 268 em `updateLifeActivities()` — `function updateLifeActivities(dt){updateShoreFishers(dt);world.activityAcc+=dt;if(world.activityAcc<.5)return;world.activityAcc=0;const now=Date.now();for(const c of [...world.campfires]){if(c.flame)c.flame.scale.y=.85+Math.sin(performance.now()*.009+c.data.x)*.18;if(c.data.cooking&&Number(c.data.cooking.endsAt||0)<=now)finishCampfireCooking(c.data,c.data.cooking.id);if(Number(c.data.expiresAt||0)<=now){c.data.expired=true;c.interactable.disabled=true;worldGroup.remove(c.group);world.campfires=world.campfires.filter(x=>x!==c);if(!c.data.remote)state.campfires=state.campfires.filter(x=>x.id!==c.data.id);}}`
- [x] `if` — linha 3799, coluna 373 em `updateLifeActivities()` — `function updateLifeActivities(dt){updateShoreFishers(dt);world.activityAcc+=dt;if(world.activityAcc<.5)return;world.activityAcc=0;const now=Date.now();for(const c of [...world.campfires]){if(c.flame)c.flame.scale.y=.85+Math.sin(performance.now()*.009+c.data.x)*.18;if(c.data.cooking&&Number(c.data.cooking.endsAt||0)<=now)finishCampfireCooking(c.data,c.data.cooking.id);if(Number(c.data.expiresAt||0)<=now){c.data.expired=true;c.interactable.disabled=true;worldGroup.remove(c.group);world.campfires=world.campfires.filter(x=>x!==c);if(!c.data.remote)state.campfires=state.campfires.filter(x=>x.id!==c.data.id);}}`
- [x] `if` — linha 3799, coluna 535 em `updateLifeActivities()` — `function updateLifeActivities(dt){updateShoreFishers(dt);world.activityAcc+=dt;if(world.activityAcc<.5)return;world.activityAcc=0;const now=Date.now();for(const c of [...world.campfires]){if(c.flame)c.flame.scale.y=.85+Math.sin(performance.now()*.009+c.data.x)*.18;if(c.data.cooking&&Number(c.data.cooking.endsAt||0)<=now)finishCampfireCooking(c.data,c.data.cooking.id);if(Number(c.data.expiresAt||0)<=now){c.data.expired=true;c.interactable.disabled=true;worldGroup.remove(c.group);world.campfires=world.campfires.filter(x=>x!==c);if(!c.data.remote)state.campfires=state.campfires.filter(x=>x.id!==c.data.id);}}`
- [x] `for` — linha 3800, coluna 5 em `updateLifeActivities()` — `for(const a of world.animals){if(!a.available&&a.respawnAt<=now){a.available=true;a.group.visible=true;}if(a.available){a.group.rotation.y+=Math.sin(performance.now()*.0005+a.x)*.004;}}`
- [x] `if` — linha 3800, coluna 35 em `updateLifeActivities()` — `for(const a of world.animals){if(!a.available&&a.respawnAt<=now){a.available=true;a.group.visible=true;}if(a.available){a.group.rotation.y+=Math.sin(performance.now()*.0005+a.x)*.004;}}`
- [x] `if` — linha 3800, coluna 109 em `updateLifeActivities()` — `for(const a of world.animals){if(!a.available&&a.respawnAt<=now){a.available=true;a.group.visible=true;}if(a.available){a.group.rotation.y+=Math.sin(performance.now()*.0005+a.x)*.004;}}`
- [x] `if` — linha 3801, coluna 5 em `updateLifeActivities()` — `if(Math.random()<.08&&state.campfires.length!==world.campfires.filter(c=>!c.data.remote).length){saveState();window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);}`

## `20-world-build-cloud-houses.js`

- [x] `for` — linha 3817, coluna 5 em `buildWorld()` — `for(let i=-5;i<=5;i++){const part=box(2.1,.35,5,materials.wood,-12+i*2.15,.25,52);world.bridgeParts.push(part);registerPlatform(-12+i*2.15,52,2.1,5,.43,{bridgePart:i+5});}`
- [x] `for` — linha 3820, coluna 5 em `buildWorld()` — `for(let i=0;i<48;i++){const x=-92+(Math.random()-.5)*68,z=-52+(Math.random()-.5)*84;if(Math.abs(x+68)<10&&Math.abs(z-52)<12)continue;if(Math.abs(x+68)<11&&Math.abs(z+18)<11)continue;if(Math.abs(x+92)<14&&Math.abs(z+92)<13)continue;createTree(x,z,.75+Math.random()*.55,true);}`
- [x] `if` — linha 3820, coluna 89 em `buildWorld()` — `for(let i=0;i<48;i++){const x=-92+(Math.random()-.5)*68,z=-52+(Math.random()-.5)*84;if(Math.abs(x+68)<10&&Math.abs(z-52)<12)continue;if(Math.abs(x+68)<11&&Math.abs(z+18)<11)continue;if(Math.abs(x+92)<14&&Math.abs(z+92)<13)continue;createTree(x,z,.75+Math.random()*.55,true);}`
- [x] `if` — linha 3820, coluna 138 em `buildWorld()` — `for(let i=0;i<48;i++){const x=-92+(Math.random()-.5)*68,z=-52+(Math.random()-.5)*84;if(Math.abs(x+68)<10&&Math.abs(z-52)<12)continue;if(Math.abs(x+68)<11&&Math.abs(z+18)<11)continue;if(Math.abs(x+92)<14&&Math.abs(z+92)<13)continue;createTree(x,z,.75+Math.random()*.55,true);}`
- [x] `if` — linha 3820, coluna 187 em `buildWorld()` — `for(let i=0;i<48;i++){const x=-92+(Math.random()-.5)*68,z=-52+(Math.random()-.5)*84;if(Math.abs(x+68)<10&&Math.abs(z-52)<12)continue;if(Math.abs(x+68)<11&&Math.abs(z+18)<11)continue;if(Math.abs(x+92)<14&&Math.abs(z+92)<13)continue;createTree(x,z,.75+Math.random()*.55,true);}`
- [x] `for` — linha 3821, coluna 5 em `buildWorld()` — `for(let i=0;i<18;i++)createRock(-44+(Math.random()-.5)*60,-95+(Math.random()-.5)*54,.7+Math.random()*.6,true);`
- [x] `for` — linha 3822, coluna 5 em `buildWorld()` — `for(let i=0;i<80;i++)createFlower((Math.random()-.5)*190,(Math.random()-.5)*190,Math.random()>.5?0xff74c9:0xffdf55);`
- [x] `for` — linha 3835, coluna 101 em `buildWorld()` — `createFenceLine(-36,26,-14,26,9);createFenceLine(14,26,36,26,9);createFenceLine(-10,29,10,29,8);for(const p of [[-9,9],[9,9],[-33,8],[33,8],[-10,-7],[10,-7]])createLamp(p[0],p[1]);`
- [x] `for` — linha 3841, coluna 68 em `buildWorld()` — `createFenceLine(38,22,65,22,10);createFenceLine(65,22,65,43,8);for(let x=42;x<62;x+=4)for(let z=27;z<40;z+=4){box(2.8,.12,2.8,0x75451f,x,.06,z);box(.18,.55,.18,0x54c93e,x,.33,z);}`
- [x] `for` — linha 3841, coluna 91 em `buildWorld()` — `createFenceLine(38,22,65,22,10);createFenceLine(65,22,65,43,8);for(let x=42;x<62;x+=4)for(let z=27;z<40;z+=4){box(2.8,.12,2.8,0x75451f,x,.06,z);box(.18,.55,.18,0x54c93e,x,.33,z);}`
- [x] `for` — linha 3856, coluna 5 em `buildWorld()` — `for(const p of [[12,1,-2],[-14,1,-8],[36,1,-15],[-45,1,18],[-63,1,-35],[78,1,15],[95,1,-20]])createCrystal(...p);`
- [x] `for` — linha 3864, coluna 5 em `buildWorld()` — `for(let i=0;i<34;i++){const a=i/34*Math.PI*2,r=118+Math.random()*10,x=Math.cos(a)*r,z=Math.sin(a)*r;box(12,12+Math.random()*16,12,0x6d7d8a,x,6,z);}`
- [x] `if` — linha 3868, coluna 55 em `collectResource()` — `const resource=world.resources.find(r=>r.id===id);if(!resource||resource.collected)return;`
- [x] `return` — linha 3868, coluna 88 em `collectResource()` — `const resource=world.resources.find(r=>r.id===id);if(!resource||resource.collected)return;`
- [x] `if` — linha 3869, coluna 57 em `collectResource()` — `const needed=resource.type==='wood'?'axe':'pickaxe';if(state.tools.equipped!==needed){toast(\`Equipe ${needed==='axe'?'o machado':'a picareta'} em Ferramentas.\`,'warn',1700);return;}`
- [x] `return` — linha 3869, coluna 178 em `collectResource()` — `const needed=resource.type==='wood'?'axe':'pickaxe';if(state.tools.equipped!==needed){toast(\`Equipe ${needed==='axe'?'o machado':'a picareta'} em Ferramentas.\`,'warn',1700);return;}`
- [x] `if` — linha 3871, coluna 5 em `collectResource()` — `if(resource.hits<Number(resource.hitsNeeded||1)){toast(\`${resource.type==='wood'?'Árvore':'Rocha'}: ${resource.hits}/${resource.hitsNeeded}\`,'good',850);return;}`
- [x] `return` — linha 3871, coluna 158 em `collectResource()` — `if(resource.hits<Number(resource.hitsNeeded||1)){toast(\`${resource.type==='wood'?'Árvore':'Rocha'}: ${resource.hits}/${resource.hitsNeeded}\`,'good',850);return;}`
- [x] `if` — linha 3878, coluna 5 em `openChest()` — `if(chest.opened){toast('Este baú já foi aberto.','warn');return;}`
- [x] `return` — linha 3878, coluna 62 em `openChest()` — `if(chest.opened){toast('Este baú já foi aberto.','warn');return;}`
- [x] `if` — linha 3881, coluna 5 em `openChest()` — `if(chest.secret)setFlag('secretChest');toast(chest.secret?'Baú secreto! +3 cristais e 100 moedas':'Baú aberto!','good',2200);evaluateMissions();saveState();`
- [x] `return` — linha 3884, coluna 38 em `cloudHouseRecord()` — `function cloudHouseRecord(houseId){return cloudHouses.get(houseId)||null;}`
- [x] `return` — linha 3885, coluna 35 em `isMyCloudHouse()` — `function isMyCloudHouse(record){return !!record&&record.ownerUid===window.OTTHOS_RTDB?.uid;}`
- [x] `if` — linha 3887, coluna 39 em `reconcileCloudHouses()` — `const uid=window.OTTHOS_RTDB?.uid;if(!uid)return;`
- [x] `return` — linha 3887, coluna 47 em `reconcileCloudHouses()` — `const uid=window.OTTHOS_RTDB?.uid;if(!uid)return;`
- [x] `for` — linha 3888, coluna 5 em `reconcileCloudHouses()` — `for(const h of world.houses||[]){if(h.publicBuilding)continue;const cloud=cloudHouseRecord(h.id);if(cloud){state.houses[h.id]={...(state.houses[h.id]||{}),owned:cloud.ownerUid===uid,locked:!!cloud.locked,ownerUid:cloud.ownerUid,ownerName:cloud.ownerName||'Jogador',price:h.price};}}`
- [x] `if` — linha 3888, coluna 38 em `reconcileCloudHouses()` — `for(const h of world.houses||[]){if(h.publicBuilding)continue;const cloud=cloudHouseRecord(h.id);if(cloud){state.houses[h.id]={...(state.houses[h.id]||{}),owned:cloud.ownerUid===uid,locked:!!cloud.locked,ownerUid:cloud.ownerUid,ownerName:cloud.ownerName||'Jogador',price:h.price};}}`
- [x] `if` — linha 3888, coluna 102 em `reconcileCloudHouses()` — `for(const h of world.houses||[]){if(h.publicBuilding)continue;const cloud=cloudHouseRecord(h.id);if(cloud){state.houses[h.id]={...(state.houses[h.id]||{}),owned:cloud.ownerUid===uid,locked:!!cloud.locked,ownerUid:cloud.ownerUid,ownerName:cloud.ownerName||'Jogador',price:h.price};}}`
- [x] `if` — linha 3892, coluna 5 em `reconcileCloudHouses()` — `if(!window.OTTHOS_RTDB?.connected?.()){toast('Conecte ao Firebase para comprar uma casa exclusiva.','warn',2600);return false;}`
- [x] `return` — linha 3892, coluna 118 em `reconcileCloudHouses()` — `if(!window.OTTHOS_RTDB?.connected?.()){toast('Conecte ao Firebase para comprar uma casa exclusiva.','warn',2600);return false;}`
- [x] `if` — linha 3894, coluna 5 em `reconcileCloudHouses()` — `if(!result?.ok){toast(result?.ownerName?\`Esta casa já pertence a ${result.ownerName}.\`:'Não foi possível comprar a casa.','warn',2600);return false;}`
- [x] `return` — linha 3894, coluna 140 em `reconcileCloudHouses()` — `if(!result?.ok){toast(result?.ownerName?\`Esta casa já pertence a ${result.ownerName}.\`:'Não foi possível comprar a casa.','warn',2600);return false;}`
- [x] `return` — linha 3895, coluna 165 em `reconcileCloudHouses()` — `state.houses[house.id]={...(state.houses[house.id]||{}),owned:true,locked:false,ownerUid:window.OTTHOS_RTDB.uid,ownerName:state.profile.name,price:house.price};return true;`
- [x] `if` — linha 3899, coluna 5 em `reconcileCloudHouses()` — `if(house.publicBuilding){if(await confirmModal(house.name,'Deseja entrar?','Entrar','Cancelar'))enterHouse(house);return;}`
- [x] `if` — linha 3899, coluna 30 em `reconcileCloudHouses()` — `if(house.publicBuilding){if(await confirmModal(house.name,'Deseja entrar?','Entrar','Cancelar'))enterHouse(house);return;}`
- [x] `return` — linha 3899, coluna 119 em `reconcileCloudHouses()` — `if(house.publicBuilding){if(await confirmModal(house.name,'Deseja entrar?','Entrar','Cancelar'))enterHouse(house);return;}`
- [x] `if` — linha 3900, coluna 5 em `reconcileCloudHouses()` — `if(cloud&&!mine){`
- [x] `if` — linha 3901, coluna 7 em `reconcileCloudHouses()` — `if(cloud.locked){toast(\`Casa trancada por ${cloud.ownerName||'outro jogador'}.\`,'warn',2500);return;}`
- [x] `return` — linha 3901, coluna 100 em `reconcileCloudHouses()` — `if(cloud.locked){toast(\`Casa trancada por ${cloud.ownerName||'outro jogador'}.\`,'warn',2500);return;}`
- [x] `if` — linha 3902, coluna 7 em `reconcileCloudHouses()` — `if(await confirmModal(house.name,\`Casa de ${cloud.ownerName||'outro jogador'}. A porta está aberta. Deseja visitar?\`,'Visitar','Cancelar'))enterHouse(house);return;`
- [x] `return` — linha 3902, coluna 164 em `reconcileCloudHouses()` — `if(await confirmModal(house.name,\`Casa de ${cloud.ownerName||'outro jogador'}. A porta está aberta. Deseja visitar?\`,'Visitar','Cancelar'))enterHouse(house);return;`
- [x] `if` — linha 3904, coluna 5 em `reconcileCloudHouses()` — `if(!cloud&&!local.owned){`
- [x] `if` — linha 3906, coluna 54 em `reconcileCloudHouses()` — `$('[data-buy-house]',root).onclick=async()=>{if(state.profile.coins<house.price){toast('Moedas insuficientes.','warn');return;}const ok=await claimHouseOnline(house);if(!ok)return;addCoins(-house.price);setFlag('boughtHouse');awardMedal('Nova Propriedade');saveState(true);closeModal();handleHouseDoor(house);};`
- [x] `return` — linha 3906, coluna 128 em `reconcileCloudHouses()` — `$('[data-buy-house]',root).onclick=async()=>{if(state.profile.coins<house.price){toast('Moedas insuficientes.','warn');return;}const ok=await claimHouseOnline(house);if(!ok)return;addCoins(-house.price);setFlag('boughtHouse');awardMedal('Nova Propriedade');saveState(true);closeModal();handleHouseDoor(house);};`
- [x] `if` — linha 3906, coluna 175 em `reconcileCloudHouses()` — `$('[data-buy-house]',root).onclick=async()=>{if(state.profile.coins<house.price){toast('Moedas insuficientes.','warn');return;}const ok=await claimHouseOnline(house);if(!ok)return;addCoins(-house.price);setFlag('boughtHouse');awardMedal('Nova Propriedade');saveState(true);closeModal();handleHouseDoor(house);};`
- [x] `return` — linha 3906, coluna 182 em `reconcileCloudHouses()` — `$('[data-buy-house]',root).onclick=async()=>{if(state.profile.coins<house.price){toast('Moedas insuficientes.','warn');return;}const ok=await claimHouseOnline(house);if(!ok)return;addCoins(-house.price);setFlag('boughtHouse');awardMedal('Nova Propriedade');saveState(true);closeModal();handleHouseDoor(house);};`
- [x] `return` — linha 3908, coluna 10 em `reconcileCloudHouses()` — `});return;`
- [x] `if` — linha 3910, coluna 35 em `reconcileCloudHouses()` — `const owned=mine||local.owned;if(owned){const locked=cloud?!!cloud.locked:!!local.locked;openModal(house.name,\`<p>Esta casa pertence a <b>${state.profile.name}</b>.</p><div class="modal-actions"><button class="btn primary" data-enter>Entrar</button><button class="btn" data-lock>${locked?'Destrancar':'Trancar'}</button><button class="btn" data-cancel>Cancelar</button></div>\`,root=>{`
- [x] `if` — linha 3911, coluna 194 em `reconcileCloudHouses()` — `$('[data-enter]',root).onclick=()=>{closeModal();enterHouse(house);};$('[data-lock]',root).onclick=async()=>{const next=!locked,ok=await window.OTTHOS_RTDB?.setHouseLock?.(house.id,next);if(ok){state.houses[house.id]={...(state.houses[house.id]||{}),owned:true,locked:next};saveState(true);closeModal();toast(next?'Casa trancada.':'Casa destrancada.','good');}else toast('Não foi possível alterar a fechadura.','warn');};$('[data-cancel]',root).onclick=closeModal;`
- [x] `else` — linha 3911, coluna 367 em `reconcileCloudHouses()` — `$('[data-enter]',root).onclick=()=>{closeModal();enterHouse(house);};$('[data-lock]',root).onclick=async()=>{const next=!locked,ok=await window.OTTHOS_RTDB?.setHouseLock?.(house.id,next);if(ok){state.houses[house.id]={...(state.houses[house.id]||{}),owned:true,locked:next};saveState(true);closeModal();toast(next?'Casa trancada.':'Casa destrancada.','good');}else toast('Não foi possível alterar a fechadura.','warn');};$('[data-cancel]',root).onclick=closeModal;`
- [x] `return` — linha 3912, coluna 8 em `reconcileCloudHouses()` — `});return;}`
- [x] `if` — linha 3917, coluna 5 em `enterHouse()` — `if(!house||!canEnterMobility(PLAYER_MODES.INTERIOR)){toast('Saia do transporte antes de entrar.','warn');return false;}`
- [x] `return` — linha 3917, coluna 110 em `enterHouse()` — `if(!house||!canEnterMobility(PLAYER_MODES.INTERIOR)){toast('Saia do transporte antes de entrar.','warn');return false;}`
- [x] `if` — linha 3920, coluna 5 em `enterHouse()` — `if(house.exteriorGroup){house.exteriorGroup.visible=false;if(house.interiorGroup)house.interiorGroup.visible=true;for(const item of house.interiorObjects||[])if(item?.visible!==undefined)item.visible=true;}`
- [x] `if` — linha 3920, coluna 63 em `enterHouse()` — `if(house.exteriorGroup){house.exteriorGroup.visible=false;if(house.interiorGroup)house.interiorGroup.visible=true;for(const item of house.interiorObjects||[])if(item?.visible!==undefined)item.visible=true;}`
- [x] `for` — linha 3920, coluna 119 em `enterHouse()` — `if(house.exteriorGroup){house.exteriorGroup.visible=false;if(house.interiorGroup)house.interiorGroup.visible=true;for(const item of house.interiorObjects||[])if(item?.visible!==undefined)item.visible=true;}`
- [x] `if` — linha 3920, coluna 163 em `enterHouse()` — `if(house.exteriorGroup){house.exteriorGroup.visible=false;if(house.interiorGroup)house.interiorGroup.visible=true;for(const item of house.interiorObjects||[])if(item?.visible!==undefined)item.visible=true;}`
- [x] `else` — linha 3921, coluna 5 em `enterHouse()` — `else{house.roof.visible=false;house.front.visible=false;house.door.visible=false;}`
- [x] `for` — linha 3922, coluna 5 em `enterHouse()` — `for(const bus of world.buses)bus.group.visible=false;`
- [x] `for` — linha 3923, coluna 5 em `enterHouse()` — `for(const car of world.policeCars)car.group.visible=false;for(const truck of world.fireTrucks)truck.group.visible=false;for(const ambulance of world.ambulances)ambulance.group.visible=false;for(const fire of world.fires)fire.group.visible=false;`
- [x] `for` — linha 3923, coluna 63 em `enterHouse()` — `for(const car of world.policeCars)car.group.visible=false;for(const truck of world.fireTrucks)truck.group.visible=false;for(const ambulance of world.ambulances)ambulance.group.visible=false;for(const fire of world.fires)fire.group.visible=false;`
- [x] `for` — linha 3923, coluna 125 em `enterHouse()` — `for(const car of world.policeCars)car.group.visible=false;for(const truck of world.fireTrucks)truck.group.visible=false;for(const ambulance of world.ambulances)ambulance.group.visible=false;for(const fire of world.fires)fire.group.visible=false;`
- [x] `for` — linha 3923, coluna 195 em `enterHouse()` — `for(const car of world.policeCars)car.group.visible=false;for(const truck of world.fireTrucks)truck.group.visible=false;for(const ambulance of world.ambulances)ambulance.group.visible=false;for(const fire of world.fires)fire.group.visible=false;`
- [x] `if` — linha 3926, coluna 5 em `enterHouse()` — `if(house.id==='home')setFlag('enteredHome');toast(\`Entrou: ${house.name}\`,'good');updateContext(true);savePlayerPosition(true);return true;`
- [x] `return` — linha 3926, coluna 132 em `enterHouse()` — `if(house.id==='home')setFlag('enteredHome');toast(\`Entrou: ${house.name}\`,'good');updateContext(true);savePlayerPosition(true);return true;`
- [x] `if` — linha 3929, coluna 5 em `exitHouse()` — `if(!currentHouse)return false;const h=currentHouse;`
- [x] `return` — linha 3929, coluna 22 em `exitHouse()` — `if(!currentHouse)return false;const h=currentHouse;`
- [x] `if` — linha 3930, coluna 5 em `exitHouse()` — `if(h.exteriorGroup){h.exteriorGroup.visible=true;if(h.interiorGroup)h.interiorGroup.visible=false;for(const item of h.interiorObjects||[])if(item?.visible!==undefined)item.visible=false;}`
- [x] `if` — linha 3930, coluna 54 em `exitHouse()` — `if(h.exteriorGroup){h.exteriorGroup.visible=true;if(h.interiorGroup)h.interiorGroup.visible=false;for(const item of h.interiorObjects||[])if(item?.visible!==undefined)item.visible=false;}`
- [x] `for` — linha 3930, coluna 103 em `exitHouse()` — `if(h.exteriorGroup){h.exteriorGroup.visible=true;if(h.interiorGroup)h.interiorGroup.visible=false;for(const item of h.interiorObjects||[])if(item?.visible!==undefined)item.visible=false;}`
- [x] `if` — linha 3930, coluna 143 em `exitHouse()` — `if(h.exteriorGroup){h.exteriorGroup.visible=true;if(h.interiorGroup)h.interiorGroup.visible=false;for(const item of h.interiorObjects||[])if(item?.visible!==undefined)item.visible=false;}`
- [x] `else` — linha 3931, coluna 5 em `exitHouse()` — `else{h.roof.visible=true;h.front.visible=true;h.door.visible=true;}`
- [x] `for` — linha 3932, coluna 5 em `exitHouse()` — `for(const bus of world.buses)bus.group.visible=true;`
- [x] `for` — linha 3933, coluna 5 em `exitHouse()` — `for(const car of world.policeCars)car.group.visible=true;for(const truck of world.fireTrucks)truck.group.visible=true;for(const ambulance of world.ambulances)ambulance.group.visible=true;for(const fire of world.fires)fire.group.visible=!!fire.active;`
- [x] `for` — linha 3933, coluna 62 em `exitHouse()` — `for(const car of world.policeCars)car.group.visible=true;for(const truck of world.fireTrucks)truck.group.visible=true;for(const ambulance of world.ambulances)ambulance.group.visible=true;for(const fire of world.fires)fire.group.visible=!!fire.active;`
- [x] `for` — linha 3933, coluna 123 em `exitHouse()` — `for(const car of world.policeCars)car.group.visible=true;for(const truck of world.fireTrucks)truck.group.visible=true;for(const ambulance of world.ambulances)ambulance.group.visible=true;for(const fire of world.fires)fire.group.visible=!!fire.active;`
- [x] `for` — linha 3933, coluna 192 em `exitHouse()` — `for(const car of world.policeCars)car.group.visible=true;for(const truck of world.fireTrucks)truck.group.visible=true;for(const ambulance of world.ambulances)ambulance.group.visible=true;for(const fire of world.fires)fire.group.visible=!!fire.active;`
- [x] `return` — linha 3936, coluna 230 em `exitHouse()` — `player.x=safe.x;player.z=safe.z;player.y=safe.y;player.vx=player.vz=player.vy=0;player.grounded=true;rememberSafePlayerPosition(true);auditPlayerMode('exit-interior');toast(\`Saiu: ${h.name}.\`,'good');savePlayerPosition(true);return true;`
- [x] `if` — linha 3944, coluna 89 em `openHomeChest()` — `$$('[data-store]',root).forEach(btn=>btn.onclick=()=>{const key=btn.dataset.store;if((state.inventory[key]||0)<=0){toast('Você não tem esse item.','warn');return;}state.inventory[key]--;state.homeStorage[key]=(state.homeStorage[key]||0)+1;saveState(true);openHomeChest();});`
- [x] `return` — linha 3944, coluna 162 em `openHomeChest()` — `$$('[data-store]',root).forEach(btn=>btn.onclick=()=>{const key=btn.dataset.store;if((state.inventory[key]||0)<=0){toast('Você não tem esse item.','warn');return;}state.inventory[key]--;state.homeStorage[key]=(state.homeStorage[key]||0)+1;saveState(true);openHomeChest();});`
- [x] `if` — linha 3945, coluna 87 em `openHomeChest()` — `$$('[data-take]',root).forEach(btn=>btn.onclick=()=>{const key=btn.dataset.take;if((state.homeStorage[key]||0)<=0){toast('O baú não tem esse item.','warn');return;}state.homeStorage[key]--;state.inventory[key]=(state.inventory[key]||0)+1;saveState(true);openHomeChest();});`
- [x] `return` — linha 3945, coluna 163 em `openHomeChest()` — `$$('[data-take]',root).forEach(btn=>btn.onclick=()=>{const key=btn.dataset.take;if((state.homeStorage[key]||0)<=0){toast('O baú não tem esse item.','warn');return;}state.homeStorage[key]--;state.inventory[key]=(state.inventory[key]||0)+1;saveState(true);openHomeChest();});`

## `21-interactions-shop-social-races.js`

- [x] `if` — linha 3951, coluna 5 em `useActivity()` — `if(type==='bed'){`
- [x] `else` — linha 3953, coluna 6 em `useActivity()` — `}else if(type==='sofa'){`
- [x] `if` — linha 3953, coluna 11 em `useActivity()` — `}else if(type==='sofa'){`
- [x] `else` — linha 3955, coluna 6 em `useActivity()` — `}else if(type==='tv'){`
- [x] `if` — linha 3955, coluna 11 em `useActivity()` — `}else if(type==='tv'){`
- [x] `else` — linha 3957, coluna 6 em `useActivity()` — `}else if(type==='fridge'){`
- [x] `if` — linha 3957, coluna 11 em `useActivity()` — `}else if(type==='fridge'){`
- [x] `if` — linha 3959, coluna 43 em `useActivity()` — `$('[data-eat]',root).onclick=()=>{if(state.inventory.food<=0){toast('A geladeira está vazia.','warn');return;}state.inventory.food--;state.needs.hunger=clamp(state.needs.hunger+32,0,100);setFlag('ateMeal');addXP(12);saveState();closeModal();toast('Lanche delicioso!','good');};$('[data-close]',root).onclick=closeModal;`
- [x] `return` — linha 3959, coluna 111 em `useActivity()` — `$('[data-eat]',root).onclick=()=>{if(state.inventory.food<=0){toast('A geladeira está vazia.','warn');return;}state.inventory.food--;state.needs.hunger=clamp(state.needs.hunger+32,0,100);setFlag('ateMeal');addXP(12);saveState();closeModal();toast('Lanche delicioso!','good');};$('[data-close]',root).onclick=closeModal;`
- [x] `else` — linha 3961, coluna 6 em `useActivity()` — `}else if(type==='stove'){`
- [x] `if` — linha 3961, coluna 11 em `useActivity()` — `}else if(type==='stove'){`
- [x] `if` — linha 3963, coluna 44 em `useActivity()` — `$('[data-cook]',root).onclick=()=>{if(state.inventory.food<=0){toast('Você precisa comprar ou colher comida.','warn');return;}state.inventory.food--;state.stats.cooked++;trackDaily('cook',1);state.needs.hunger=100;state.needs.fun=clamp(state.needs.fun+8,0,100);setFlag('ateMeal');addXP(20);saveState();closeModal();toast('Refeição pronta!','good');};$('[data-close]',root).onclick=closeModal;`
- [x] `return` — linha 3963, coluna 127 em `useActivity()` — `$('[data-cook]',root).onclick=()=>{if(state.inventory.food<=0){toast('Você precisa comprar ou colher comida.','warn');return;}state.inventory.food--;state.stats.cooked++;trackDaily('cook',1);state.needs.hunger=100;state.needs.fun=clamp(state.needs.fun+8,0,100);setFlag('ateMeal');addXP(20);saveState();closeModal();toast('Refeição pronta!','good');};$('[data-close]',root).onclick=closeModal;`
- [x] `else` — linha 3965, coluna 6 em `useActivity()` — `}else if(type==='sink'){`
- [x] `if` — linha 3965, coluna 11 em `useActivity()` — `}else if(type==='sink'){`
- [x] `if` — linha 3966, coluna 7 em `useActivity()` — `if(state.inventory.water>0)state.inventory.water--;state.needs.hunger=clamp(state.needs.hunger+5,0,100);state.needs.hygiene=clamp(state.needs.hygiene+8,0,100);toast('Bebeu água.','good');saveState();`
- [x] `else` — linha 3967, coluna 6 em `useActivity()` — `}else if(type==='shower'){`
- [x] `if` — linha 3967, coluna 11 em `useActivity()` — `}else if(type==='shower'){`
- [x] `else` — linha 3969, coluna 6 em `useActivity()` — `}else if(type==='chest')openHomeChest();`
- [x] `if` — linha 3969, coluna 11 em `useActivity()` — `}else if(type==='chest')openHomeChest();`
- [x] `else` — linha 3970, coluna 5 em `useActivity()` — `else if(type==='shop')openShop();`
- [x] `if` — linha 3970, coluna 10 em `useActivity()` — `else if(type==='shop')openShop();`
- [x] `else` — linha 3971, coluna 5 em `useActivity()` — `else if(type==='workshop')openWorkshop();`
- [x] `if` — linha 3971, coluna 10 em `useActivity()` — `else if(type==='workshop')openWorkshop();`
- [x] `else` — linha 3972, coluna 5 em `useActivity()` — `else if(type==='wardrobe')openAvatarStudio();`
- [x] `if` — linha 3972, coluna 10 em `useActivity()` — `else if(type==='wardrobe')openAvatarStudio();`
- [x] `else` — linha 3973, coluna 5 em `useActivity()` — `else if(type==='school'){if(state.career.activeJob?.id==='teacher')openTeacherJobLesson(house);else openEducationHub(String(state.learning.lastLesson||'math').split('-')[0]);}`
- [x] `if` — linha 3973, coluna 10 em `useActivity()` — `else if(type==='school'){if(state.career.activeJob?.id==='teacher')openTeacherJobLesson(house);else openEducationHub(String(state.learning.lastLesson||'math').split('-')[0]);}`
- [x] `if` — linha 3973, coluna 30 em `useActivity()` — `else if(type==='school'){if(state.career.activeJob?.id==='teacher')openTeacherJobLesson(house);else openEducationHub(String(state.learning.lastLesson||'math').split('-')[0]);}`
- [x] `else` — linha 3973, coluna 100 em `useActivity()` — `else if(type==='school'){if(state.career.activeJob?.id==='teacher')openTeacherJobLesson(house);else openEducationHub(String(state.learning.lastLesson||'math').split('-')[0]);}`
- [x] `else` — linha 3974, coluna 5 em `useActivity()` — `else if(type==='police'){if(state.career.activeJob?.id==='police')toast('Patrulha ativa: siga os pontos marcados no GPS.','good',2200);else openSafetyLesson('station');}`
- [x] `if` — linha 3974, coluna 10 em `useActivity()` — `else if(type==='police'){if(state.career.activeJob?.id==='police')toast('Patrulha ativa: siga os pontos marcados no GPS.','good',2200);else openSafetyLesson('station');}`
- [x] `if` — linha 3974, coluna 30 em `useActivity()` — `else if(type==='police'){if(state.career.activeJob?.id==='police')toast('Patrulha ativa: siga os pontos marcados no GPS.','good',2200);else openSafetyLesson('station');}`
- [x] `else` — linha 3974, coluna 140 em `useActivity()` — `else if(type==='police'){if(state.career.activeJob?.id==='police')toast('Patrulha ativa: siga os pontos marcados no GPS.','good',2200);else openSafetyLesson('station');}`
- [x] `else` — linha 3975, coluna 5 em `useActivity()` — `else if(type==='firestation')openFireStationDesk();`
- [x] `if` — linha 3975, coluna 10 em `useActivity()` — `else if(type==='firestation')openFireStationDesk();`
- [x] `if` — linha 3981, coluna 120 em `openShop()` — `$$('[data-buy]',root).forEach(btn=>btn.onclick=()=>{const [name,price,key,amount]=items[Number(btn.dataset.buy)];if(state.profile.coins<price){toast('Moedas insuficientes.','warn');return;}addCoins(-price);state.inventory[key]+=amount;addXP(5);saveState();closeModal();toast(\`${name} comprado!\`,'good');});`
- [x] `return` — linha 3981, coluna 188 em `openShop()` — `$$('[data-buy]',root).forEach(btn=>btn.onclick=()=>{const [name,price,key,amount]=items[Number(btn.dataset.buy)];if(state.profile.coins<price){toast('Moedas insuficientes.','warn');return;}addCoins(-price);state.inventory[key]+=amount;addXP(5);saveState();closeModal();toast(\`${name} comprado!\`,'good');});`
- [x] `if` — linha 3987, coluna 43 em `openWorkshop()` — `$('[data-sword]',root).onclick=()=>{if(inv.wood<2||inv.stone<2){toast('Faltam materiais.','warn');return;}inv.wood-=2;inv.stone-=2;state.flags.swordUpgrade=(state.flags.swordUpgrade||0)+1;addXP(35);saveState();closeModal();toast('Ferramenta de aventura melhorada!','good');};`
- [x] `return` — linha 3987, coluna 105 em `openWorkshop()` — `$('[data-sword]',root).onclick=()=>{if(inv.wood<2||inv.stone<2){toast('Faltam materiais.','warn');return;}inv.wood-=2;inv.stone-=2;state.flags.swordUpgrade=(state.flags.swordUpgrade||0)+1;addXP(35);saveState();closeModal();toast('Ferramenta de aventura melhorada!','good');};`
- [x] `if` — linha 3988, coluna 44 em `openWorkshop()` — `$('[data-blocks]',root).onclick=()=>{if(inv.wood<1||inv.stone<1){toast('Faltam materiais.','warn');return;}inv.wood--;inv.stone--;inv.blocks+=3;inv.fences+=2;saveState();closeModal();toast('Kit de construção pronto!','good');};`
- [x] `return` — linha 3988, coluna 106 em `openWorkshop()` — `$('[data-blocks]',root).onclick=()=>{if(inv.wood<1||inv.stone<1){toast('Faltam materiais.','warn');return;}inv.wood--;inv.stone--;inv.blocks+=3;inv.fences+=2;saveState();closeModal();toast('Kit de construção pronto!','good');};`
- [x] `if` — linha 3989, coluna 43 em `openWorkshop()` — `$('[data-smelt]',root).onclick=()=>{if((inv.goldOre||0)<3){toast('Você precisa de 3 minérios de ouro.','warn');return;}inv.goldOre-=3;inv.goldBar=(inv.goldBar||0)+1;addCoins(60);addXP(35);saveState(true);closeModal();toast('Barra de ouro criada: +60 moedas.','good',2200);};`
- [x] `return` — linha 3989, coluna 118 em `openWorkshop()` — `$('[data-smelt]',root).onclick=()=>{if((inv.goldOre||0)<3){toast('Você precisa de 3 minérios de ouro.','warn');return;}inv.goldOre-=3;inv.goldBar=(inv.goldBar||0)+1;addCoins(60);addXP(35);saveState(true);closeModal();toast('Barra de ouro criada: +60 moedas.','good',2200);};`
- [x] `return` — linha 3993, coluna 35 em `friendshipTier()` — `function friendshipTier(value){ return value>=60?'Melhor amigo':value>=30?'Amigo':value>=10?'Conhecido':'Vizinho'; }`
- [x] `if` — linha 3996, coluna 5 em `changeFriendship()` — `if(npc.id==='nino')setFlag('talkedNeighbor');`
- [x] `if` — linha 3997, coluna 5 em `changeFriendship()` — `if(message)toast(message,'good');addXP(Math.max(2,amount*2));addReputation(Math.max(1,Math.floor(amount/2)));evaluateMissions();saveState();`
- [x] `if` — linha 4000, coluna 5 em `talkToNPC()` — `if(npc.id==='maya'&&state.flags.deliveryActive&&player.vehicle&&distance2D(player,npc)<3.5){state.flags.deliveryActive=false;state.inventory.package=0;setFlag('deliveryDone');if(state.career.activeJob?.id==='delivery')completeActiveJob();else{addCoins(120);addReputation(30);}toast('Entrega concluída para Maya!','good',2400);}`
- [x] `if` — linha 4000, coluna 180 em `talkToNPC()` — `if(npc.id==='maya'&&state.flags.deliveryActive&&player.vehicle&&distance2D(player,npc)<3.5){state.flags.deliveryActive=false;state.inventory.package=0;setFlag('deliveryDone');if(state.career.activeJob?.id==='delivery')completeActiveJob();else{addCoins(120);addReputation(30);}toast('Entrega concluída para Maya!','good',2400);}`
- [x] `else` — linha 4000, coluna 243 em `talkToNPC()` — `if(npc.id==='maya'&&state.flags.deliveryActive&&player.vehicle&&distance2D(player,npc)<3.5){state.flags.deliveryActive=false;state.inventory.package=0;setFlag('deliveryDone');if(state.career.activeJob?.id==='delivery')completeActiveJob();else{addCoins(120);addReputation(30);}toast('Entrega concluída para Maya!','good',2400);}`
- [x] `if` — linha 4022, coluna 9 em `talkToNPC()` — `if(action==='talk'){state.stats.talks++;trackDaily('talk',1);changeFriendship(npc,2,\`${npc.name} gostou da conversa.\`);closeModal();}`
- [x] `else` — linha 4023, coluna 9 em `talkToNPC()` — `else if(action==='joke'){state.social.jokes++;state.needs.fun=clamp(state.needs.fun+12,0,100);changeFriendship(npc,3,\`${npc.name} riu da piada!\`);closeModal();}`
- [x] `if` — linha 4023, coluna 14 em `talkToNPC()` — `else if(action==='joke'){state.social.jokes++;state.needs.fun=clamp(state.needs.fun+12,0,100);changeFriendship(npc,3,\`${npc.name} riu da piada!\`);closeModal();}`
- [x] `else` — linha 4024, coluna 9 em `talkToNPC()` — `else if(action==='gift'){`
- [x] `if` — linha 4024, coluna 14 em `talkToNPC()` — `else if(action==='gift'){`
- [x] `if` — linha 4025, coluna 11 em `talkToNPC()` — `if(state.inventory.food>0){state.inventory.food--;state.social.gifts++;changeFriendship(npc,7,'Presente entregue!');closeModal();}`
- [x] `else` — linha 4026, coluna 11 em `talkToNPC()` — `else if(state.inventory.crystals>0){state.inventory.crystals--;state.social.gifts++;changeFriendship(npc,10,'Cristal presenteado!');closeModal();}`
- [x] `if` — linha 4026, coluna 16 em `talkToNPC()` — `else if(state.inventory.crystals>0){state.inventory.crystals--;state.social.gifts++;changeFriendship(npc,10,'Cristal presenteado!');closeModal();}`
- [x] `else` — linha 4027, coluna 11 em `talkToNPC()` — `else toast('Você não tem comida nem cristal para presentear.','warn');`
- [x] `else` — linha 4028, coluna 11 em `talkToNPC()` — `} else if(action==='argue'){`
- [x] `if` — linha 4028, coluna 16 em `talkToNPC()` — `} else if(action==='argue'){`
- [x] `else` — linha 4030, coluna 11 em `talkToNPC()` — `} else if(action==='race'){closeModal();startRace('sprint',npc);}`
- [x] `if` — linha 4030, coluna 16 em `talkToNPC()` — `} else if(action==='race'){closeModal();startRace('sprint',npc);}`
- [x] `else` — linha 4031, coluna 9 em `talkToNPC()` — `else if(action==='coinrace'){closeModal();startRace('coins',npc);}`
- [x] `if` — linha 4031, coluna 14 em `talkToNPC()` — `else if(action==='coinrace'){closeModal();startRace('coins',npc);}`
- [x] `else` — linha 4032, coluna 9 em `talkToNPC()` — `else if(action==='house'){closeModal();openHouseChallenge(npc);}`
- [x] `if` — linha 4032, coluna 14 em `talkToNPC()` — `else if(action==='house'){closeModal();openHouseChallenge(npc);}`
- [x] `else` — linha 4033, coluna 9 em `talkToNPC()` — `else if(action==='job'){closeModal();openJobCenter(npc.id);}`
- [x] `if` — linha 4033, coluna 14 em `talkToNPC()` — `else if(action==='job'){closeModal();openJobCenter(npc.id);}`
- [x] `else` — linha 4034, coluna 9 em `talkToNPC()` — `else if(action==='invite'){`
- [x] `if` — linha 4034, coluna 14 em `talkToNPC()` — `else if(action==='invite'){`
- [x] `if` — linha 4035, coluna 11 em `talkToNPC()` — `if((state.friendship[npc.id]||0)<10){toast('A amizade precisa chegar a 10.','warn');return;}`
- [x] `return` — linha 4035, coluna 95 em `talkToNPC()` — `if((state.friendship[npc.id]||0)<10){toast('A amizade precisa chegar a 10.','warn');return;}`
- [x] `if` — linha 4036, coluna 11 em `talkToNPC()` — `if(!state.social.invited.includes(npc.id))state.social.invited.push(npc.id);changeFriendship(npc,2,\`${npc.name} aceitou visitar sua casa!\`);closeModal();`
- [x] `else` — linha 4037, coluna 10 em `talkToNPC()` — `}else if(action==='wave'){triggerEmote('wave',npc);changeFriendship(npc,1);closeModal();}`
- [x] `if` — linha 4037, coluna 15 em `talkToNPC()` — `}else if(action==='wave'){triggerEmote('wave',npc);changeFriendship(npc,1);closeModal();}`
- [x] `else` — linha 4038, coluna 9 em `talkToNPC()` — `else if(action==='dance'){triggerEmote('dance',npc);state.needs.fun=clamp(state.needs.fun+10,0,100);changeFriendship(npc,2);closeModal();}`
- [x] `if` — linha 4038, coluna 14 em `talkToNPC()` — `else if(action==='dance'){triggerEmote('dance',npc);state.needs.fun=clamp(state.needs.fun+10,0,100);changeFriendship(npc,2);closeModal();}`
- [x] `else` — linha 4039, coluna 9 em `talkToNPC()` — `else if(action==='play'){triggerEmote('play',npc);state.needs.fun=clamp(state.needs.fun+14,0,100);changeFriendship(npc,3,\`${npc.name} adorou brincar!\`);closeModal();}`
- [x] `if` — linha 4039, coluna 14 em `talkToNPC()` — `else if(action==='play'){triggerEmote('play',npc);state.needs.fun=clamp(state.needs.fun+14,0,100);changeFriendship(npc,3,\`${npc.name} adorou brincar!\`);closeModal();}`
- [x] `else` — linha 4040, coluna 9 em `talkToNPC()` — `else if(action==='selfie'){triggerEmote('selfie',npc);state.flags.selfies=(state.flags.selfies||0)+1;changeFriendship(npc,2);closeModal();}`
- [x] `if` — linha 4040, coluna 14 em `talkToNPC()` — `else if(action==='selfie'){triggerEmote('selfie',npc);state.flags.selfies=(state.flags.selfies||0)+1;changeFriendship(npc,2);closeModal();}`
- [x] `else` — linha 4041, coluna 9 em `talkToNPC()` — `else if(action==='follow'){const stopping=!!npc.following;npc.following=!stopping;if(stopping){npc.pendingRide=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;}toast(stopping?\`${npc.name} parou de acompanhar você.\`:\`${npc.name} vai acompanhar você.\`,'good');closeModal();}`
- [x] `if` — linha 4041, coluna 14 em `talkToNPC()` — `else if(action==='follow'){const stopping=!!npc.following;npc.following=!stopping;if(stopping){npc.pendingRide=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;}toast(stopping?\`${npc.name} parou de acompanhar você.\`:\`${npc.name} vai acompanhar você.\`,'good');closeModal();}`
- [x] `if` — linha 4041, coluna 91 em `talkToNPC()` — `else if(action==='follow'){const stopping=!!npc.following;npc.following=!stopping;if(stopping){npc.pendingRide=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;}toast(stopping?\`${npc.name} parou de acompanhar você.\`:\`${npc.name} vai acompanhar você.\`,'good');closeModal();}`
- [x] `if` — linha 4041, coluna 149 em `talkToNPC()` — `else if(action==='follow'){const stopping=!!npc.following;npc.following=!stopping;if(stopping){npc.pendingRide=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;}toast(stopping?\`${npc.name} parou de acompanhar você.\`:\`${npc.name} vai acompanhar você.\`,'good');closeModal();}`
- [x] `else` — linha 4042, coluna 9 em `talkToNPC()` — `else if(action==='ride'){if(npc.pendingRide||npc.passengerMode){npc.pendingRide=false;npc.following=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;toast(\`Passeio com ${npc.name} cancelado.\`,'good');}else{npc.pendingRide=true;npc.following=true;toast(\`${npc.name} vai entrar no próximo carro ou barco com você.\`,'good',2400);}closeModal();}`
- [x] `if` — linha 4042, coluna 14 em `talkToNPC()` — `else if(action==='ride'){if(npc.pendingRide||npc.passengerMode){npc.pendingRide=false;npc.following=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;toast(\`Passeio com ${npc.name} cancelado.\`,'good');}else{npc.pendingRide=true;npc.following=true;toast(\`${npc.name} vai entrar no próximo carro ou barco com você.\`,'good',2400);}closeModal();}`
- [x] `if` — linha 4042, coluna 34 em `talkToNPC()` — `else if(action==='ride'){if(npc.pendingRide||npc.passengerMode){npc.pendingRide=false;npc.following=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;toast(\`Passeio com ${npc.name} cancelado.\`,'good');}else{npc.pendingRide=true;npc.following=true;toast(\`${npc.name} vai entrar no próximo carro ou barco com você.\`,'good',2400);}closeModal();}`
- [x] `if` — linha 4042, coluna 138 em `talkToNPC()` — `else if(action==='ride'){if(npc.pendingRide||npc.passengerMode){npc.pendingRide=false;npc.following=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;toast(\`Passeio com ${npc.name} cancelado.\`,'good');}else{npc.pendingRide=true;npc.following=true;toast(\`${npc.name} vai entrar no próximo carro ou barco com você.\`,'good',2400);}closeModal();}`
- [x] `else` — linha 4042, coluna 226 em `talkToNPC()` — `else if(action==='ride'){if(npc.pendingRide||npc.passengerMode){npc.pendingRide=false;npc.following=false;npc.passengerMode=null;if(npc.group)npc.group.visible=true;toast(\`Passeio com ${npc.name} cancelado.\`,'good');}else{npc.pendingRide=true;npc.following=true;toast(\`${npc.name} vai entrar no próximo carro ou barco com você.\`,'good',2400);}closeModal();}`
- [x] `if` — linha 4049, coluna 5 em `openHouseChallenge()` — `if(!options.length){toast('Você já conquistou todas as casas disponíveis.','good');return;}`
- [x] `return` — linha 4049, coluna 88 em `openHouseChallenge()` — `if(!options.length){toast('Você já conquistou todas as casas disponíveis.','good');return;}`
- [x] `return` — linha 4061, coluna 241 em `createRaceOpponent()` — `const group=new THREE.Group();worldGroup.add(group);box(.78,1.12,.55,npc?.color||0xff72b6,0,1.1,0,group);box(.68,.68,.68,0xffd3a0,0,2.0,0,group);box(.08,.08,.04,0x111827,-.15,2.05,.36,group);box(.08,.08,.04,0x111827,.15,2.05,.36,group);return group;`
- [x] `if` — linha 4064, coluna 5 em `clearRaceObjects()` — `if(activeRace?.opponent)worldGroup.remove(activeRace.opponent);`
- [x] `for` — linha 4065, coluna 5 em `clearRaceObjects()` — `for(const coin of world.raceCoins)worldGroup.remove(coin.mesh);`
- [x] `for` — linha 4070, coluna 5 em `spawnRaceCoins()` — `for(let i=0;i<12;i++){`
- [x] `if` — linha 4075, coluna 5 em `startRace()` — `if(activeRace){toast('Termine o desafio atual.','warn');return;}`
- [x] `return` — linha 4075, coluna 61 em `startRace()` — `if(activeRace){toast('Termine o desafio atual.','warn');return;}`
- [x] `if` — linha 4076, coluna 5 em `startRace()` — `if(currentHouse)exitHouse();`
- [x] `if` — linha 4077, coluna 25 em `startRace()` — `const gym=world.gym;if(!gym){toast('Ginásio ainda não carregou.','warn');return;}`
- [x] `return` — linha 4077, coluna 78 em `startRace()` — `const gym=world.gym;if(!gym){toast('Ginásio ainda não carregou.','warn');return;}`
- [x] `if` — linha 4081, coluna 5 em `startRace()` — `if(type==='coins')spawnRaceCoins();`
- [x] `if` — linha 4086, coluna 5 em `finishRace()` — `if(!activeRace)return;const race=activeRace;state.stats.races++;trackDaily('race',1);clearRaceObjects();activeRace=null;els.raceBadge.hidden=true;`
- [x] `return` — linha 4086, coluna 20 em `finishRace()` — `if(!activeRace)return;const race=activeRace;state.stats.races++;trackDaily('race',1);clearRaceObjects();activeRace=null;els.raceBadge.hidden=true;`
- [x] `if` — linha 4087, coluna 5 em `finishRace()` — `if(won){`
- [x] `if` — linha 4088, coluna 26 em `finishRace()` — `state.races.wins++;if(race.type==='coins')state.races.coinWins++;`
- [x] `if` — linha 4090, coluna 7 em `finishRace()` — `if(race.housePrize){const old=state.houses[race.housePrize]||{};state.houses[race.housePrize]={...old,owned:true,locked:false};state.races.houseWins++;setFlag('wonHouseChallenge');setFlag('boughtHouse');awardMedal('Casa Conquistada');}`
- [x] `else` — linha 4092, coluna 6 em `finishRace()` — `}else{state.races.losses++;toast(\`${race.npcName} venceu. Tente novamente!\`,'warn',2400);}`
- [x] `if` — linha 4096, coluna 5 em `updateRace()` — `if(!activeRace)return;const race=activeRace,gym=world.gym,now=performance.now();`
- [x] `return` — linha 4096, coluna 20 em `updateRace()` — `if(!activeRace)return;const race=activeRace,gym=world.gym,now=performance.now();`
- [x] `if` — linha 4097, coluna 5 em `updateRace()` — `if(now<race.startAt){els.raceStatus.textContent=\`${Math.max(1,Math.ceil((race.startAt-now)/1000))}...\`;return;}`
- [x] `return` — linha 4097, coluna 108 em `updateRace()` — `if(now<race.startAt){els.raceStatus.textContent=\`${Math.max(1,Math.ceil((race.startAt-now)/1000))}...\`;return;}`
- [x] `if` — linha 4098, coluna 5 em `updateRace()` — `if(!race.started){race.started=true;race.startedAt=now;els.raceStatus.textContent='VALENDO!';beep(880,100);}`
- [x] `if` — linha 4100, coluna 5 em `updateRace()` — `if(race.type==='sprint'){`
- [x] `if` — linha 4103, coluna 7 em `updateRace()` — `if(player.x>=gym.finishX)finishRace(true);else if(race.opponentX>=gym.finishX||race.timeLeft<=0)finishRace(false);`
- [x] `else` — linha 4103, coluna 49 em `updateRace()` — `if(player.x>=gym.finishX)finishRace(true);else if(race.opponentX>=gym.finishX||race.timeLeft<=0)finishRace(false);`
- [x] `if` — linha 4103, coluna 54 em `updateRace()` — `if(player.x>=gym.finishX)finishRace(true);else if(race.opponentX>=gym.finishX||race.timeLeft<=0)finishRace(false);`
- [x] `else` — linha 4104, coluna 6 em `updateRace()` — `}else{`
- [x] `if` — linha 4106, coluna 7 em `updateRace()` — `if(elapsed-race.lastOpponentCoin>3.2){race.lastOpponentCoin=elapsed;race.opponentScore++;}`
- [x] `for` — linha 4107, coluna 7 em `updateRace()` — `for(const coin of world.raceCoins){if(coin.got)continue;coin.mesh.rotation.y+=dt*5;if(Math.hypot(player.x-coin.x,player.z-coin.z)<1.25){coin.got=true;coin.mesh.visible=false;race.playerScore++;beep(920,45);}}`
- [x] `if` — linha 4107, coluna 42 em `updateRace()` — `for(const coin of world.raceCoins){if(coin.got)continue;coin.mesh.rotation.y+=dt*5;if(Math.hypot(player.x-coin.x,player.z-coin.z)<1.25){coin.got=true;coin.mesh.visible=false;race.playerScore++;beep(920,45);}}`
- [x] `if` — linha 4107, coluna 90 em `updateRace()` — `for(const coin of world.raceCoins){if(coin.got)continue;coin.mesh.rotation.y+=dt*5;if(Math.hypot(player.x-coin.x,player.z-coin.z)<1.25){coin.got=true;coin.mesh.visible=false;race.playerScore++;beep(920,45);}}`
- [x] `if` — linha 4109, coluna 7 em `updateRace()` — `if(race.playerScore>=8)finishRace(true);else if(race.opponentScore>=8)finishRace(false);else if(race.timeLeft<=0)finishRace(race.playerScore>race.opponentScore);`
- [x] `else` — linha 4109, coluna 47 em `updateRace()` — `if(race.playerScore>=8)finishRace(true);else if(race.opponentScore>=8)finishRace(false);else if(race.timeLeft<=0)finishRace(race.playerScore>race.opponentScore);`
- [x] `if` — linha 4109, coluna 52 em `updateRace()` — `if(race.playerScore>=8)finishRace(true);else if(race.opponentScore>=8)finishRace(false);else if(race.timeLeft<=0)finishRace(race.playerScore>race.opponentScore);`
- [x] `else` — linha 4109, coluna 95 em `updateRace()` — `if(race.playerScore>=8)finishRace(true);else if(race.opponentScore>=8)finishRace(false);else if(race.timeLeft<=0)finishRace(race.playerScore>race.opponentScore);`
- [x] `if` — linha 4109, coluna 100 em `updateRace()` — `if(race.playerScore>=8)finishRace(true);else if(race.opponentScore>=8)finishRace(false);else if(race.timeLeft<=0)finishRace(race.playerScore>race.opponentScore);`

## `22-careers-jobs-uniforms.js`

- [x] `if` — linha 4128, coluna 48 em `setMissionState()` — `function setMissionState(job,next,reason=''){if(!job)return false;const changed=job.missionState!==next||job.missionReason!==reason;if(!changed)return false;job.missionState=next;job.missionStateAt=Date.now();job.missionReason=reason;return true;}`
- [x] `return` — linha 4128, coluna 56 em `setMissionState()` — `function setMissionState(job,next,reason=''){if(!job)return false;const changed=job.missionState!==next||job.missionReason!==reason;if(!changed)return false;job.missionState=next;job.missionStateAt=Date.now();job.missionReason=reason;return true;}`
- [x] `if` — linha 4128, coluna 135 em `setMissionState()` — `function setMissionState(job,next,reason=''){if(!job)return false;const changed=job.missionState!==next||job.missionReason!==reason;if(!changed)return false;job.missionState=next;job.missionStateAt=Date.now();job.missionReason=reason;return true;}`
- [x] `return` — linha 4128, coluna 147 em `setMissionState()` — `function setMissionState(job,next,reason=''){if(!job)return false;const changed=job.missionState!==next||job.missionReason!==reason;if(!changed)return false;job.missionState=next;job.missionStateAt=Date.now();job.missionReason=reason;return true;}`
- [x] `return` — linha 4128, coluna 237 em `setMissionState()` — `function setMissionState(job,next,reason=''){if(!job)return false;const changed=job.missionState!==next||job.missionReason!==reason;if(!changed)return false;job.missionState=next;job.missionStateAt=Date.now();job.missionReason=reason;return true;}`
- [x] `return` — linha 4129, coluna 105 em `missionVehicleName()` — `function missionVehicleName(job){const kind=serviceKindForJob(job),vehicle=serviceMissionVehicle(job);return vehicle?.label||(kind==='police'?'Viatura da Polícia':kind==='firefighter'?'Caminhão dos Bombeiros':kind==='paramedic'?'Ambulância de Resgate':'');}`
- [x] `if` — linha 4130, coluna 41 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `return` — linha 4130, coluna 49 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `if` — linha 4130, coluna 124 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `return` — linha 4130, coluna 315 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `if` — linha 4130, coluna 860 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `return` — linha 4130, coluna 883 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `if` — linha 4130, coluna 1019 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `return` — linha 4130, coluna 1041 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `if` — linha 4130, coluna 1195 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `return` — linha 4130, coluna 1216 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `if` — linha 4130, coluna 1380 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `return` — linha 4130, coluna 1403 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `if` — linha 4130, coluna 1503 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `return` — linha 4130, coluna 1525 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `return` — linha 4130, coluna 1631 em `missionInstructionSteps()` — `function missionInstructionSteps(job){if(!job)return[];const kind=serviceKindForJob(job),vehicle=missionVehicleName(job);if(kind){const scene=job.id==='police'&&!job.emergencyIncidentId?'os três pontos da patrulha':job.id==='firefighter'&&!job.emergencyIncidentId?'a ocorrência de incêndio':'a área do acidente';return[{text:\`Vestir o uniforme de ${job.title}\`,done:effectiveAvatarUniform()===JOB_UNIFORMS[job.id]},{text:\`Ir até ${vehicle} marcado no GPS\`,done:!!job.serviceVehicleBoarded},{text:\`Entrar e dirigir ${vehicle}\`,done:!!job.serviceVehicleBoarded},{text:\`Chegar com o veículo até ${scene}\`,done:!!job.serviceVehicleArrived||Number(job.progress||0)>0},{text:job.id==='police'&&!job.emergencyIncidentId?'Concluir todos os pontos sem trocar de veículo':'Estacionar, sair e usar AÇÃO no local',done:!!job.sceneTaskCompleted||job.completed===true}];}if(job.id==='delivery')return[{text:'Ir à garagem e pegar o carrinho',done:!!player.vehicle},{text:'Levar o pacote até Maya',done:!!state.flags`
- [x] `return` — linha 4131, coluna 144 em `missionBriefingMarkup()` — `function missionBriefingMarkup(job){const progress=activeJobProgress(job),steps=missionInstructionSteps(job),vehicle=missionVehicleName(job);return\`<div class="mission-briefing"><div class="mission-briefing-head"><span>${job.icon||'💼'}</span><div><small>${job.missionState||'MISSÃO ATIVA'}</small><h3>${job.title}</h3><p>${job.description}</p></div></div>${vehicle?\`<div class="mission-assigned-vehicle"><b>VEÍCULO RESERVADO</b><span>${vehicle}${job.serviceVehicleBoarded?' • você já entrou':' • marcado no GPS'}</span></div>\`:''}<ol class="mission-step-list">${steps.map((step,index)=>\`<li class="${step.done?'done':''}"><i>${step.done?'✓':index+1}</i><span>${step.text}</span></li>\`).join('')}</ol><div class="job-progress"><i style="width:${progress.percent}%"></i></div><b class="mission-current-instruction">Agora: ${progress.label}</b></div>\`;}`
- [x] `if` — linha 4134, coluna 5 em `equipJobUniform()` — `if(job&&job.id===jobId){job.uniform=uniform;job.uniformLocked=true;}`
- [x] `return` — linha 4135, coluna 77 em `equipJobUniform()` — `state.avatar.uniform=uniform;applyAvatarCustomization();saveState(true);return uniform;`
- [x] `if` — linha 4138, coluna 38 em `focusActiveJob()` — `const job=state.career.activeJob;if(!job){toast('Nenhum trabalho ativo.','warn');return false;}if(currentHouse&&!(job.id==='teacher'&&currentHouse.id.startsWith('school')))exitHouse();const serviceKind=serviceKindForJob(job);`
- [x] `return` — linha 4138, coluna 86 em `focusActiveJob()` — `const job=state.career.activeJob;if(!job){toast('Nenhum trabalho ativo.','warn');return false;}if(currentHouse&&!(job.id==='teacher'&&currentHouse.id.startsWith('school')))exitHouse();const serviceKind=serviceKindForJob(job);`
- [x] `if` — linha 4138, coluna 100 em `focusActiveJob()` — `const job=state.career.activeJob;if(!job){toast('Nenhum trabalho ativo.','warn');return false;}if(currentHouse&&!(job.id==='teacher'&&currentHouse.id.startsWith('school')))exitHouse();const serviceKind=serviceKindForJob(job);`
- [x] `if` — linha 4139, coluna 5 em `focusActiveJob()` — `if(serviceKind&&!job.serviceVehicleBoarded){const vehicle=reserveMissionServiceVehicle(job,{waypoint:true});if(!vehicle)return false;closeModal();updateMissionHUD();toast(\`Primeiro passo: vista o uniforme e entre em ${serviceKind==='police'?'uma viatura':serviceKind==='firefighter'?'um caminhão dos bombeiros':'uma ambulância'}.\`,'good',2600);return true;}`
- [x] `if` — linha 4139, coluna 113 em `focusActiveJob()` — `if(serviceKind&&!job.serviceVehicleBoarded){const vehicle=reserveMissionServiceVehicle(job,{waypoint:true});if(!vehicle)return false;closeModal();updateMissionHUD();toast(\`Primeiro passo: vista o uniforme e entre em ${serviceKind==='police'?'uma viatura':serviceKind==='firefighter'?'um caminhão dos bombeiros':'uma ambulância'}.\`,'good',2600);return true;}`
- [x] `return` — linha 4139, coluna 125 em `focusActiveJob()` — `if(serviceKind&&!job.serviceVehicleBoarded){const vehicle=reserveMissionServiceVehicle(job,{waypoint:true});if(!vehicle)return false;closeModal();updateMissionHUD();toast(\`Primeiro passo: vista o uniforme e entre em ${serviceKind==='police'?'uma viatura':serviceKind==='firefighter'?'um caminhão dos bombeiros':'uma ambulância'}.\`,'good',2600);return true;}`
- [x] `return` — linha 4139, coluna 349 em `focusActiveJob()` — `if(serviceKind&&!job.serviceVehicleBoarded){const vehicle=reserveMissionServiceVehicle(job,{waypoint:true});if(!vehicle)return false;closeModal();updateMissionHUD();toast(\`Primeiro passo: vista o uniforme e entre em ${serviceKind==='police'?'uma viatura':serviceKind==='firefighter'?'um caminhão dos bombeiros':'uma ambulância'}.\`,'good',2600);return true;}`
- [x] `if` — linha 4140, coluna 5 em `focusActiveJob()` — `if(job.emergencyIncidentId){const incident=world.activeIncident?.id===job.emergencyIncidentId?world.activeIncident:null;if(incident){state.waypoint={id:\`assist-${incident.id}\`,name:'Área do acidente',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);}}`
- [x] `if` — linha 4140, coluna 125 em `focusActiveJob()` — `if(job.emergencyIncidentId){const incident=world.activeIncident?.id===job.emergencyIncidentId?world.activeIncident:null;if(incident){state.waypoint={id:\`assist-${incident.id}\`,name:'Área do acidente',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);}}`
- [x] `else` — linha 4141, coluna 5 em `focusActiveJob()` — `else if(job.id==='delivery')setWaypoint('garage');`
- [x] `if` — linha 4141, coluna 10 em `focusActiveJob()` — `else if(job.id==='delivery')setWaypoint('garage');`
- [x] `else` — linha 4142, coluna 5 em `focusActiveJob()` — `else if(job.id==='police'){const id=(job.route||[])[Number(job.progress||0)]||'police';setWaypoint(id);}`
- [x] `if` — linha 4142, coluna 10 em `focusActiveJob()` — `else if(job.id==='police'){const id=(job.route||[])[Number(job.progress||0)]||'police';setWaypoint(id);}`
- [x] `else` — linha 4143, coluna 5 em `focusActiveJob()` — `else if(job.id==='firefighter'){const fire=ensureActiveFire(true);if(fire){state.waypoint={id:\`fire-${fire.id}\`,name:fire.name,x:fire.x,z:fire.z,navX:fire.navX,navZ:fire.navZ,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);}}`
- [x] `if` — linha 4143, coluna 10 em `focusActiveJob()` — `else if(job.id==='firefighter'){const fire=ensureActiveFire(true);if(fire){state.waypoint={id:\`fire-${fire.id}\`,name:fire.name,x:fire.x,z:fire.z,navX:fire.navX,navZ:fire.navZ,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);}}`
- [x] `if` — linha 4143, coluna 71 em `focusActiveJob()` — `else if(job.id==='firefighter'){const fire=ensureActiveFire(true);if(fire){state.waypoint={id:\`fire-${fire.id}\`,name:fire.name,x:fire.x,z:fire.z,navX:fire.navX,navZ:fire.navZ,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);}}`
- [x] `else` — linha 4144, coluna 5 em `focusActiveJob()` — `else if(job.id==='paramedic'){const incident=ensureActiveTrafficIncident(true);if(incident){job.emergencyIncidentId=incident.id;focusActiveJob();return true;}}`
- [x] `if` — linha 4144, coluna 10 em `focusActiveJob()` — `else if(job.id==='paramedic'){const incident=ensureActiveTrafficIncident(true);if(incident){job.emergencyIncidentId=incident.id;focusActiveJob();return true;}}`
- [x] `if` — linha 4144, coluna 84 em `focusActiveJob()` — `else if(job.id==='paramedic'){const incident=ensureActiveTrafficIncident(true);if(incident){job.emergencyIncidentId=incident.id;focusActiveJob();return true;}}`
- [x] `return` — linha 4144, coluna 150 em `focusActiveJob()` — `else if(job.id==='paramedic'){const incident=ensureActiveTrafficIncident(true);if(incident){job.emergencyIncidentId=incident.id;focusActiveJob();return true;}}`
- [x] `else` — linha 4145, coluna 5 em `focusActiveJob()` — `else if(job.id==='teacher')setWaypoint(job.schoolId||'school');else if(job.id==='gather')setWaypoint('forest');else if(job.id==='crystals')setWaypoint('crystal');else if(job.id==='builder')setWaypoint('home-extension');`
- [x] `if` — linha 4145, coluna 10 em `focusActiveJob()` — `else if(job.id==='teacher')setWaypoint(job.schoolId||'school');else if(job.id==='gather')setWaypoint('forest');else if(job.id==='crystals')setWaypoint('crystal');else if(job.id==='builder')setWaypoint('home-extension');`
- [x] `else` — linha 4145, coluna 68 em `focusActiveJob()` — `else if(job.id==='teacher')setWaypoint(job.schoolId||'school');else if(job.id==='gather')setWaypoint('forest');else if(job.id==='crystals')setWaypoint('crystal');else if(job.id==='builder')setWaypoint('home-extension');`
- [x] `if` — linha 4145, coluna 73 em `focusActiveJob()` — `else if(job.id==='teacher')setWaypoint(job.schoolId||'school');else if(job.id==='gather')setWaypoint('forest');else if(job.id==='crystals')setWaypoint('crystal');else if(job.id==='builder')setWaypoint('home-extension');`
- [x] `else` — linha 4145, coluna 116 em `focusActiveJob()` — `else if(job.id==='teacher')setWaypoint(job.schoolId||'school');else if(job.id==='gather')setWaypoint('forest');else if(job.id==='crystals')setWaypoint('crystal');else if(job.id==='builder')setWaypoint('home-extension');`
- [x] `if` — linha 4145, coluna 121 em `focusActiveJob()` — `else if(job.id==='teacher')setWaypoint(job.schoolId||'school');else if(job.id==='gather')setWaypoint('forest');else if(job.id==='crystals')setWaypoint('crystal');else if(job.id==='builder')setWaypoint('home-extension');`
- [x] `else` — linha 4145, coluna 167 em `focusActiveJob()` — `else if(job.id==='teacher')setWaypoint(job.schoolId||'school');else if(job.id==='gather')setWaypoint('forest');else if(job.id==='crystals')setWaypoint('crystal');else if(job.id==='builder')setWaypoint('home-extension');`
- [x] `if` — linha 4145, coluna 172 em `focusActiveJob()` — `else if(job.id==='teacher')setWaypoint(job.schoolId||'school');else if(job.id==='gather')setWaypoint('forest');else if(job.id==='crystals')setWaypoint('crystal');else if(job.id==='builder')setWaypoint('home-extension');`
- [x] `return` — linha 4146, coluna 104 em `focusActiveJob()` — `closeModal();updateMissionHUD();toast(\`Missão ativa: ${job.title}. Siga a rota azul.\`,'good',2300);return true;`
- [x] `if` — linha 4149, coluna 38 em `focusActiveJob()` — `const job=state.career.activeJob;if(!job||job.missionState===MISSION_STATES.COMPLETING)return false;`
- [x] `return` — linha 4149, coluna 92 em `focusActiveJob()` — `const job=state.career.activeJob;if(!job||job.missionState===MISSION_STATES.COMPLETING)return false;`
- [x] `if` — linha 4150, coluna 177 em `focusActiveJob()` — `const ok=await confirmModal('Cancelar trabalho',\`Deseja encerrar "${job.title}"? Suas moedas, itens e conquistas continuam salvos.\`,'Cancelar trabalho','Continuar missão');if(!ok)return false;`
- [x] `return` — linha 4150, coluna 184 em `focusActiveJob()` — `const ok=await confirmModal('Cancelar trabalho',\`Deseja encerrar "${job.title}"? Suas moedas, itens e conquistas continuam salvos.\`,'Cancelar trabalho','Continuar missão');if(!ok)return false;`
- [x] `if` — linha 4152, coluna 5 em `focusActiveJob()` — `if(job.id==='delivery'){state.flags.deliveryActive=false;state.inventory.package=Number(job.previousPackage||0);}`
- [x] `return` — linha 4153, coluna 373 em `focusActiveJob()` — `releaseMissionServiceVehicle(job);state.avatar.uniform=job.previousUniform||'none';state.career.lastMission={...job,endedAt:Date.now()};state.career.activeJob=null;state.waypoint=null;world.routePath=[];updateWaypointMarker();applyAvatarCustomization();updateMissionHUD();saveState(true);closeModal();toast('Trabalho cancelado. Você pode escolher outro.','good',1900);return true;`
- [x] `if` — linha 4157, coluna 5 em `activeJobProgress()` — `if(!job)return{percent:0,label:'0%'};const start=job.start||{},serviceKind=serviceKindForJob(job);`
- [x] `return` — linha 4157, coluna 13 em `activeJobProgress()` — `if(!job)return{percent:0,label:'0%'};const start=job.start||{},serviceKind=serviceKindForJob(job);`
- [x] `if` — linha 4158, coluna 5 em `activeJobProgress()` — `if(serviceKind&&!job.serviceVehicleBoarded)return{percent:18,label:\`Entre ${serviceKind==='police'?'na viatura':serviceKind==='firefighter'?'no caminhão dos bombeiros':'na ambulância'}\`};`
- [x] `return` — linha 4158, coluna 48 em `activeJobProgress()` — `if(serviceKind&&!job.serviceVehicleBoarded)return{percent:18,label:\`Entre ${serviceKind==='police'?'na viatura':serviceKind==='firefighter'?'no caminhão dos bombeiros':'na ambulância'}\`};`
- [x] `if` — linha 4159, coluna 5 em `activeJobProgress()` — `if(job.id==='delivery')return{percent:state.flags.deliveryDone?100:(player.vehicle?55:25),label:state.flags.deliveryDone?'concluído':player.vehicle?'Leve o pacote até Maya':'Pegue o carrinho'};`
- [x] `return` — linha 4159, coluna 28 em `activeJobProgress()` — `if(job.id==='delivery')return{percent:state.flags.deliveryDone?100:(player.vehicle?55:25),label:state.flags.deliveryDone?'concluído':player.vehicle?'Leve o pacote até Maya':'Pegue o carrinho'};`
- [x] `if` — linha 4160, coluna 5 em `activeJobProgress()` — `if(job.id==='police'){if(job.emergencyIncidentId)return{percent:job.sceneTaskCompleted?90:job.serviceVehicleArrived?72:42,label:job.sceneTaskCompleted?'Área protegida; aguarde conclusão':job.serviceVehicleArrived?'Saia da viatura e use AÇÃO':'Dirija a viatura até o acidente'};const done=Number(job.progress||0),total=(job.route||[]).length||3;return{percent:clamp(24+done/total*76,0,100),label:\`${done}/${total} pontos patrulhados com a viatura\`};}`
- [x] `if` — linha 4160, coluna 27 em `activeJobProgress()` — `if(job.id==='police'){if(job.emergencyIncidentId)return{percent:job.sceneTaskCompleted?90:job.serviceVehicleArrived?72:42,label:job.sceneTaskCompleted?'Área protegida; aguarde conclusão':job.serviceVehicleArrived?'Saia da viatura e use AÇÃO':'Dirija a viatura até o acidente'};const done=Number(job.progress||0),total=(job.route||[]).length||3;return{percent:clamp(24+done/total*76,0,100),label:\`${done}/${total} pontos patrulhados com a viatura\`};}`
- [x] `return` — linha 4160, coluna 54 em `activeJobProgress()` — `if(job.id==='police'){if(job.emergencyIncidentId)return{percent:job.sceneTaskCompleted?90:job.serviceVehicleArrived?72:42,label:job.sceneTaskCompleted?'Área protegida; aguarde conclusão':job.serviceVehicleArrived?'Saia da viatura e use AÇÃO':'Dirija a viatura até o acidente'};const done=Number(job.progress||0),total=(job.route||[]).length||3;return{percent:clamp(24+done/total*76,0,100),label:\`${done}/${total} pontos patrulhados com a viatura\`};}`
- [x] `return` — linha 4160, coluna 349 em `activeJobProgress()` — `if(job.id==='police'){if(job.emergencyIncidentId)return{percent:job.sceneTaskCompleted?90:job.serviceVehicleArrived?72:42,label:job.sceneTaskCompleted?'Área protegida; aguarde conclusão':job.serviceVehicleArrived?'Saia da viatura e use AÇÃO':'Dirija a viatura até o acidente'};const done=Number(job.progress||0),total=(job.route||[]).length||3;return{percent:clamp(24+done/total*76,0,100),label:\`${done}/${total} pontos patrulhados com a viatura\`};}`
- [x] `if` — linha 4161, coluna 5 em `activeJobProgress()` — `if(job.id==='firefighter'){if(job.emergencyIncidentId)return{percent:job.sceneTaskCompleted?90:job.serviceVehicleArrived?72:42,label:job.sceneTaskCompleted?'Incêndio controlado':job.serviceVehicleArrived?'Saia do caminhão e use AÇÃO':'Dirija o caminhão até o acidente'};const fire=world.fires.find(f=>f.active);return{percent:job.completed?100:(fire?.playerHelping?88:job.serviceVehicleArrived?68:fire?42:25),label:job.completed?'emergência concluída':fire?.playerHelping?'Mangueira em operação':job.serviceVehicleArrived?'Estacione e ajude com a mangueira':fire?'Dirija até a ocorrência':'Aguardando chamado'};}`
- [x] `if` — linha 4161, coluna 32 em `activeJobProgress()` — `if(job.id==='firefighter'){if(job.emergencyIncidentId)return{percent:job.sceneTaskCompleted?90:job.serviceVehicleArrived?72:42,label:job.sceneTaskCompleted?'Incêndio controlado':job.serviceVehicleArrived?'Saia do caminhão e use AÇÃO':'Dirija o caminhão até o acidente'};const fire=world.fires.find(f=>f.active);return{percent:job.completed?100:(fire?.playerHelping?88:job.serviceVehicleArrived?68:fire?42:25),label:job.completed?'emergência concluída':fire?.playerHelping?'Mangueira em operação':job.serviceVehicleArrived?'Estacione e ajude com a mangueira':fire?'Dirija até a ocorrência':'Aguardando chamado'};}`
- [x] `return` — linha 4161, coluna 59 em `activeJobProgress()` — `if(job.id==='firefighter'){if(job.emergencyIncidentId)return{percent:job.sceneTaskCompleted?90:job.serviceVehicleArrived?72:42,label:job.sceneTaskCompleted?'Incêndio controlado':job.serviceVehicleArrived?'Saia do caminhão e use AÇÃO':'Dirija o caminhão até o acidente'};const fire=world.fires.find(f=>f.active);return{percent:job.completed?100:(fire?.playerHelping?88:job.serviceVehicleArrived?68:fire?42:25),label:job.completed?'emergência concluída':fire?.playerHelping?'Mangueira em operação':job.serviceVehicleArrived?'Estacione e ajude com a mangueira':fire?'Dirija até a ocorrência':'Aguardando chamado'};}`
- [x] `return` — linha 4161, coluna 316 em `activeJobProgress()` — `if(job.id==='firefighter'){if(job.emergencyIncidentId)return{percent:job.sceneTaskCompleted?90:job.serviceVehicleArrived?72:42,label:job.sceneTaskCompleted?'Incêndio controlado':job.serviceVehicleArrived?'Saia do caminhão e use AÇÃO':'Dirija o caminhão até o acidente'};const fire=world.fires.find(f=>f.active);return{percent:job.completed?100:(fire?.playerHelping?88:job.serviceVehicleArrived?68:fire?42:25),label:job.completed?'emergência concluída':fire?.playerHelping?'Mangueira em operação':job.serviceVehicleArrived?'Estacione e ajude com a mangueira':fire?'Dirija até a ocorrência':'Aguardando chamado'};}`
- [x] `if` — linha 4162, coluna 5 em `activeJobProgress()` — `if(job.id==='paramedic')return{percent:job.sceneTaskCompleted?90:job.serviceVehicleArrived?70:42,label:job.sceneTaskCompleted?'Atendimento realizado; aguarde liberação':job.serviceVehicleArrived?'Saia da ambulância e use AÇÃO':'Dirija a ambulância até o acidente'};`
- [x] `return` — linha 4162, coluna 29 em `activeJobProgress()` — `if(job.id==='paramedic')return{percent:job.sceneTaskCompleted?90:job.serviceVehicleArrived?70:42,label:job.sceneTaskCompleted?'Atendimento realizado; aguarde liberação':job.serviceVehicleArrived?'Saia da ambulância e use AÇÃO':'Dirija a ambulância até o acidente'};`
- [x] `if` — linha 4163, coluna 5 em `activeJobProgress()` — `if(job.id==='teacher')return{percent:job.completed?100:(currentHouse?.id?.startsWith('school')?55:20),label:job.completed?'aula concluída':'Vá até uma escola e use o quadro'};`
- [x] `return` — linha 4163, coluna 27 em `activeJobProgress()` — `if(job.id==='teacher')return{percent:job.completed?100:(currentHouse?.id?.startsWith('school')?55:20),label:job.completed?'aula concluída':'Vá até uma escola e use o quadro'};`
- [x] `if` — linha 4164, coluna 5 em `activeJobProgress()` — `if(job.id==='gather'){const w=Math.max(0,state.inventory.wood-(start.wood||0)),r=Math.max(0,state.inventory.stone-(start.stone||0));return{percent:clamp((w/3+r/2)*50,0,100),label:\`${Math.min(w,3)}/3 madeiras • ${Math.min(r,2)}/2 pedras\`};}`
- [x] `return` — linha 4164, coluna 137 em `activeJobProgress()` — `if(job.id==='gather'){const w=Math.max(0,state.inventory.wood-(start.wood||0)),r=Math.max(0,state.inventory.stone-(start.stone||0));return{percent:clamp((w/3+r/2)*50,0,100),label:\`${Math.min(w,3)}/3 madeiras • ${Math.min(r,2)}/2 pedras\`};}`
- [x] `if` — linha 4165, coluna 5 em `activeJobProgress()` — `if(job.id==='crystals'){const n=Math.max(0,state.inventory.crystals-(start.crystals||0));return{percent:clamp(n/3*100,0,100),label:\`${Math.min(n,3)}/3 cristais\`};}if(job.id==='builder'){const n=Math.max(0,state.builds.length-(start.builds||0));return{percent:clamp(n/2*100,0,100),label:\`${Math.min(n,2)}/2 construções\`};}return{percent:0,label:'Em andamento'};`
- [x] `return` — linha 4165, coluna 94 em `activeJobProgress()` — `if(job.id==='crystals'){const n=Math.max(0,state.inventory.crystals-(start.crystals||0));return{percent:clamp(n/3*100,0,100),label:\`${Math.min(n,3)}/3 cristais\`};}if(job.id==='builder'){const n=Math.max(0,state.builds.length-(start.builds||0));return{percent:clamp(n/2*100,0,100),label:\`${Math.min(n,2)}/2 construções\`};}return{percent:0,label:'Em andamento'};`
- [x] `if` — linha 4165, coluna 168 em `activeJobProgress()` — `if(job.id==='crystals'){const n=Math.max(0,state.inventory.crystals-(start.crystals||0));return{percent:clamp(n/3*100,0,100),label:\`${Math.min(n,3)}/3 cristais\`};}if(job.id==='builder'){const n=Math.max(0,state.builds.length-(start.builds||0));return{percent:clamp(n/2*100,0,100),label:\`${Math.min(n,2)}/2 construções\`};}return{percent:0,label:'Em andamento'};`
- [x] `return` — linha 4165, coluna 249 em `activeJobProgress()` — `if(job.id==='crystals'){const n=Math.max(0,state.inventory.crystals-(start.crystals||0));return{percent:clamp(n/3*100,0,100),label:\`${Math.min(n,3)}/3 cristais\`};}if(job.id==='builder'){const n=Math.max(0,state.builds.length-(start.builds||0));return{percent:clamp(n/2*100,0,100),label:\`${Math.min(n,2)}/2 construções\`};}return{percent:0,label:'Em andamento'};`
- [x] `return` — linha 4165, coluna 326 em `activeJobProgress()` — `if(job.id==='crystals'){const n=Math.max(0,state.inventory.crystals-(start.crystals||0));return{percent:clamp(n/3*100,0,100),label:\`${Math.min(n,3)}/3 cristais\`};}if(job.id==='builder'){const n=Math.max(0,state.builds.length-(start.builds||0));return{percent:clamp(n/2*100,0,100),label:\`${Math.min(n,2)}/2 construções\`};}return{percent:0,label:'Em andamento'};`
- [x] `if` — linha 4177, coluna 5 em `startJob()` — `if(!job||state.career.activeJob){toast('Cancele ou conclua o trabalho atual antes de trocar.','warn');return false;}const inv=state.inventory,schoolId=Math.random()>.5?'school':'school-east',instanceId=\`job-${job.id}-${Date.now()}-${Math.random().toString(36).slice(2,7)}\`;`
- [x] `return` — linha 4177, coluna 107 em `startJob()` — `if(!job||state.career.activeJob){toast('Cancele ou conclua o trabalho atual antes de trocar.','warn');return false;}const inv=state.inventory,schoolId=Math.random()>.5?'school':'school-east',instanceId=\`job-${job.id}-${Date.now()}-${Math.random().toString(36).slice(2,7)}\`;`
- [x] `if` — linha 4179, coluna 110 em `startJob()` — `equipJobUniform(job.id);setMissionState(state.career.activeJob,MISSION_STATES.PREPARING,'uniform-ready');if(job.id==='delivery'){state.flags.deliveryActive=true;state.flags.deliveryDone=false;state.inventory.package=Math.max(1,Number(state.inventory.package||0));}`
- [x] `else` — linha 4180, coluna 5 em `startJob()` — `else if(job.id==='police'){state.career.activeJob.route=['village','school','fire-station'];state.career.activeJob.progress=0;reserveMissionServiceVehicle(state.career.activeJob,{waypoint:true});}`
- [x] `if` — linha 4180, coluna 10 em `startJob()` — `else if(job.id==='police'){state.career.activeJob.route=['village','school','fire-station'];state.career.activeJob.progress=0;reserveMissionServiceVehicle(state.career.activeJob,{waypoint:true});}`
- [x] `else` — linha 4181, coluna 5 em `startJob()` — `else if(job.id==='firefighter'){ensureActiveFire(true);reserveMissionServiceVehicle(state.career.activeJob,{waypoint:true});}`
- [x] `if` — linha 4181, coluna 10 em `startJob()` — `else if(job.id==='firefighter'){ensureActiveFire(true);reserveMissionServiceVehicle(state.career.activeJob,{waypoint:true});}`
- [x] `else` — linha 4182, coluna 5 em `startJob()` — `else if(job.id==='paramedic'){const incident=ensureActiveTrafficIncident(true);if(incident)state.career.activeJob.emergencyIncidentId=incident.id;reserveMissionServiceVehicle(state.career.activeJob,{waypoint:true});}`
- [x] `if` — linha 4182, coluna 10 em `startJob()` — `else if(job.id==='paramedic'){const incident=ensureActiveTrafficIncident(true);if(incident)state.career.activeJob.emergencyIncidentId=incident.id;reserveMissionServiceVehicle(state.career.activeJob,{waypoint:true});}`
- [x] `if` — linha 4182, coluna 84 em `startJob()` — `else if(job.id==='paramedic'){const incident=ensureActiveTrafficIncident(true);if(incident)state.career.activeJob.emergencyIncidentId=incident.id;reserveMissionServiceVehicle(state.career.activeJob,{waypoint:true});}`
- [x] `if` — linha 4183, coluna 5 em `startJob()` — `if(currentHouse&&!(job.id==='teacher'&&currentHouse.id.startsWith('school')))exitHouse();saveState(true);updateMissionHUD();if(options.focus!==false)focusActiveJob();toast(\`Missão aceita: ${job.title}. Toque no cartão da missão para ver todas as etapas.\`,'good',3200);return true;`
- [x] `if` — linha 4183, coluna 129 em `startJob()` — `if(currentHouse&&!(job.id==='teacher'&&currentHouse.id.startsWith('school')))exitHouse();saveState(true);updateMissionHUD();if(options.focus!==false)focusActiveJob();toast(\`Missão aceita: ${job.title}. Toque no cartão da missão para ver todas as etapas.\`,'good',3200);return true;`
- [x] `return` — linha 4183, coluna 273 em `startJob()` — `if(currentHouse&&!(job.id==='teacher'&&currentHouse.id.startsWith('school')))exitHouse();saveState(true);updateMissionHUD();if(options.focus!==false)focusActiveJob();toast(\`Missão aceita: ${job.title}. Toque no cartão da missão para ver todas as etapas.\`,'good',3200);return true;`
- [x] `if` — linha 4186, coluna 38 em `completeActiveJob()` — `const job=state.career.activeJob;if(!job||job.missionState===MISSION_STATES.COMPLETING||job.missionState===MISSION_STATES.COMPLETED)return false;`
- [x] `return` — linha 4186, coluna 137 em `completeActiveJob()` — `const job=state.career.activeJob;if(!job||job.missionState===MISSION_STATES.COMPLETING||job.missionState===MISSION_STATES.COMPLETED)return false;`
- [x] `if` — linha 4188, coluna 5 em `completeActiveJob()` — `if(job.rewardClaimed||tokens.includes(token)){console.warn('[OTTHOS] Recompensa duplicada bloqueada:',token);return false;}`
- [x] `return` — linha 4188, coluna 114 em `completeActiveJob()` — `if(job.rewardClaimed||tokens.includes(token)){console.warn('[OTTHOS] Recompensa duplicada bloqueada:',token);return false;}`
- [x] `if` — linha 4189, coluna 115 em `completeActiveJob()` — `setMissionState(job,MISSION_STATES.COMPLETING,'objective-complete');job.rewardClaimed=true;tokens.push(token);if(tokens.length>120)tokens.splice(0,tokens.length-120);`
- [x] `if` — linha 4191, coluna 5 em `completeActiveJob()` — `if(job.id==='delivery'){state.flags.deliveryActive=false;state.inventory.package=Number(job.previousPackage||0);state.flags.deliveryDone=true;state.flags.completedDeliveryJob=true;state.cityServices.deliveries++;}`
- [x] `if` — linha 4192, coluna 5 em `completeActiveJob()` — `if(job.id==='police'){state.flags.completedPoliceJob=true;state.cityServices.policePatrols++;state.stats.patrols++;}`
- [x] `if` — linha 4193, coluna 5 em `completeActiveJob()` — `if(job.id==='firefighter'){state.flags.completedFirefighterJob=true;state.cityServices.rescuesCompleted=(state.cityServices.rescuesCompleted||0)+1;}if(job.id==='paramedic'){state.flags.completedParamedicJob=true;state.cityServices.paramedicRescues=(state.cityServices.paramedicRescues||0)+1;state.stats.paramedicCalls=(state.stats.paramedicCalls||0)+1;}`
- [x] `if` — linha 4193, coluna 153 em `completeActiveJob()` — `if(job.id==='firefighter'){state.flags.completedFirefighterJob=true;state.cityServices.rescuesCompleted=(state.cityServices.rescuesCompleted||0)+1;}if(job.id==='paramedic'){state.flags.completedParamedicJob=true;state.cityServices.paramedicRescues=(state.cityServices.paramedicRescues||0)+1;state.stats.paramedicCalls=(state.stats.paramedicCalls||0)+1;}`
- [x] `if` — linha 4194, coluna 5 em `completeActiveJob()` — `if(job.id==='teacher'){state.flags.completedTeacherJob=true;state.cityServices.lessonsTaught++;state.stats.classesTaught++;}`
- [x] `if` — linha 4196, coluna 400 em `completeActiveJob()` — `job.uniformLocked=false;releaseMissionServiceVehicle(job);state.avatar.uniform=job.previousUniform||'none';setMissionState(job,MISSION_STATES.COMPLETED,'reward-committed');job.completedAt=Date.now();state.career.lastMission={...job};state.objectives.history.push({id:token,type:'job',jobId:job.id,title:job.title,completedAt:job.completedAt,reward:Number(job.reward||0),rep:Number(job.rep||0)});if(state.objectives.history.length>80)state.objectives.history.splice(0,state.objectives.history.length-80);`
- [x] `return` — linha 4197, coluna 275 em `completeActiveJob()` — `state.career.activeJob=null;state.waypoint=null;world.routePath=[];updateWaypointMarker();applyAvatarCustomization();setFlag('completedJob');evaluateMissions();updateMissionHUD();updateHUD();saveState(true);toast(\`Trabalho concluído! +${job.reward} moedas\`,'good',2600);return true;`
- [x] `if` — linha 4200, coluna 38 em `checkActiveJob()` — `const job=state.career.activeJob;if(!job)return;`
- [x] `return` — linha 4200, coluna 46 em `checkActiveJob()` — `const job=state.career.activeJob;if(!job)return;`
- [x] `if` — linha 4202, coluna 5 em `checkActiveJob()` — `if(job.id==='gather'&&state.inventory.wood-(start.wood||0)>=3&&state.inventory.stone-(start.stone||0)>=2)completeActiveJob();`
- [x] `else` — linha 4203, coluna 5 em `checkActiveJob()` — `else if(job.id==='crystals'&&state.inventory.crystals-(start.crystals||0)>=3)completeActiveJob();`
- [x] `if` — linha 4203, coluna 10 em `checkActiveJob()` — `else if(job.id==='crystals'&&state.inventory.crystals-(start.crystals||0)>=3)completeActiveJob();`
- [x] `else` — linha 4204, coluna 5 em `checkActiveJob()` — `else if(job.id==='builder'&&state.builds.length-(start.builds||0)>=2)completeActiveJob();`
- [x] `if` — linha 4204, coluna 10 em `checkActiveJob()` — `else if(job.id==='builder'&&state.builds.length-(start.builds||0)>=2)completeActiveJob();`
- [x] `if` — linha 4208, coluna 39 em `restoreActiveJobRuntime()` — `const job=state.career?.activeJob;if(!job)return;job.uniform=JOB_UNIFORMS[job.id]||job.uniform||'none';job.uniformLocked=true;if(job.id==='delivery'){state.flags.deliveryActive=true;state.inventory.package=Math.max(1,Number(state.inventory.package||0));}if(job.id==='firefighter')ensureActiveFire(true);if(job.id==='paramedic'&&!world.activeIncident){const incident=ensureActiveTrafficIncident(true);if(incident)job.emergencyIncidentId=incident.id;}if(serviceKindForJob(job))reserveMissionServiceVehicle(job,{waypoint:false});applyAvatarCustomization();updateMissionHUD();`
- [x] `return` — linha 4208, coluna 47 em `restoreActiveJobRuntime()` — `const job=state.career?.activeJob;if(!job)return;job.uniform=JOB_UNIFORMS[job.id]||job.uniform||'none';job.uniformLocked=true;if(job.id==='delivery'){state.flags.deliveryActive=true;state.inventory.package=Math.max(1,Number(state.inventory.package||0));}if(job.id==='firefighter')ensureActiveFire(true);if(job.id==='paramedic'&&!world.activeIncident){const incident=ensureActiveTrafficIncident(true);if(incident)job.emergencyIncidentId=incident.id;}if(serviceKindForJob(job))reserveMissionServiceVehicle(job,{waypoint:false});applyAvatarCustomization();updateMissionHUD();`
- [x] `if` — linha 4208, coluna 131 em `restoreActiveJobRuntime()` — `const job=state.career?.activeJob;if(!job)return;job.uniform=JOB_UNIFORMS[job.id]||job.uniform||'none';job.uniformLocked=true;if(job.id==='delivery'){state.flags.deliveryActive=true;state.inventory.package=Math.max(1,Number(state.inventory.package||0));}if(job.id==='firefighter')ensureActiveFire(true);if(job.id==='paramedic'&&!world.activeIncident){const incident=ensureActiveTrafficIncident(true);if(incident)job.emergencyIncidentId=incident.id;}if(serviceKindForJob(job))reserveMissionServiceVehicle(job,{waypoint:false});applyAvatarCustomization();updateMissionHUD();`
- [x] `if` — linha 4208, coluna 259 em `restoreActiveJobRuntime()` — `const job=state.career?.activeJob;if(!job)return;job.uniform=JOB_UNIFORMS[job.id]||job.uniform||'none';job.uniformLocked=true;if(job.id==='delivery'){state.flags.deliveryActive=true;state.inventory.package=Math.max(1,Number(state.inventory.package||0));}if(job.id==='firefighter')ensureActiveFire(true);if(job.id==='paramedic'&&!world.activeIncident){const incident=ensureActiveTrafficIncident(true);if(incident)job.emergencyIncidentId=incident.id;}if(serviceKindForJob(job))reserveMissionServiceVehicle(job,{waypoint:false});applyAvatarCustomization();updateMissionHUD();`
- [x] `if` — linha 4208, coluna 308 em `restoreActiveJobRuntime()` — `const job=state.career?.activeJob;if(!job)return;job.uniform=JOB_UNIFORMS[job.id]||job.uniform||'none';job.uniformLocked=true;if(job.id==='delivery'){state.flags.deliveryActive=true;state.inventory.package=Math.max(1,Number(state.inventory.package||0));}if(job.id==='firefighter')ensureActiveFire(true);if(job.id==='paramedic'&&!world.activeIncident){const incident=ensureActiveTrafficIncident(true);if(incident)job.emergencyIncidentId=incident.id;}if(serviceKindForJob(job))reserveMissionServiceVehicle(job,{waypoint:false});applyAvatarCustomization();updateMissionHUD();`
- [x] `if` — linha 4208, coluna 405 em `restoreActiveJobRuntime()` — `const job=state.career?.activeJob;if(!job)return;job.uniform=JOB_UNIFORMS[job.id]||job.uniform||'none';job.uniformLocked=true;if(job.id==='delivery'){state.flags.deliveryActive=true;state.inventory.package=Math.max(1,Number(state.inventory.package||0));}if(job.id==='firefighter')ensureActiveFire(true);if(job.id==='paramedic'&&!world.activeIncident){const incident=ensureActiveTrafficIncident(true);if(incident)job.emergencyIncidentId=incident.id;}if(serviceKindForJob(job))reserveMissionServiceVehicle(job,{waypoint:false});applyAvatarCustomization();updateMissionHUD();`
- [x] `if` — linha 4208, coluna 454 em `restoreActiveJobRuntime()` — `const job=state.career?.activeJob;if(!job)return;job.uniform=JOB_UNIFORMS[job.id]||job.uniform||'none';job.uniformLocked=true;if(job.id==='delivery'){state.flags.deliveryActive=true;state.inventory.package=Math.max(1,Number(state.inventory.package||0));}if(job.id==='firefighter')ensureActiveFire(true);if(job.id==='paramedic'&&!world.activeIncident){const incident=ensureActiveTrafficIncident(true);if(incident)job.emergencyIncidentId=incident.id;}if(serviceKindForJob(job))reserveMissionServiceVehicle(job,{waypoint:false});applyAvatarCustomization();updateMissionHUD();`
- [x] `if` — linha 4211, coluna 38 em `updateCareerMissions()` — `const job=state.career.activeJob;if(!job)return;const now=performance.now();if(now-Number(updateCareerMissions.lastAt||0)<180)return;updateCareerMissions.lastAt=now;let signature='',serviceKind=serviceKindForJob(job);`
- [x] `return` — linha 4211, coluna 46 em `updateCareerMissions()` — `const job=state.career.activeJob;if(!job)return;const now=performance.now();if(now-Number(updateCareerMissions.lastAt||0)<180)return;updateCareerMissions.lastAt=now;let signature='',serviceKind=serviceKindForJob(job);`
- [x] `if` — linha 4211, coluna 81 em `updateCareerMissions()` — `const job=state.career.activeJob;if(!job)return;const now=performance.now();if(now-Number(updateCareerMissions.lastAt||0)<180)return;updateCareerMissions.lastAt=now;let signature='',serviceKind=serviceKindForJob(job);`
- [x] `return` — linha 4211, coluna 131 em `updateCareerMissions()` — `const job=state.career.activeJob;if(!job)return;const now=performance.now();if(now-Number(updateCareerMissions.lastAt||0)<180)return;updateCareerMissions.lastAt=now;let signature='',serviceKind=serviceKindForJob(job);`
- [x] `if` — linha 4212, coluna 5 em `updateCareerMissions()` — `if(serviceKind&&!job.serviceVehicleBoarded){reserveMissionServiceVehicle(job,{waypoint:false});setMissionState(job,MISSION_STATES.PREPARING,'need-service-vehicle');}`
- [x] `else` — linha 4213, coluna 5 em `updateCareerMissions()` — `else if(job.emergencyIncidentId){const incident=world.activeIncident?.id===job.emergencyIncidentId?world.activeIncident:null;if(!incident&&job.sceneTaskCompleted)completeActiveJob();else if(incident)setMissionState(job,job.sceneTaskCompleted?MISSION_STATES.RETURNING:job.serviceVehicleArrived?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,job.sceneTaskCompleted?'scene-complete':job.serviceVehicleArrived?'assist-scene':'respond-incident');}`
- [x] `if` — linha 4213, coluna 10 em `updateCareerMissions()` — `else if(job.emergencyIncidentId){const incident=world.activeIncident?.id===job.emergencyIncidentId?world.activeIncident:null;if(!incident&&job.sceneTaskCompleted)completeActiveJob();else if(incident)setMissionState(job,job.sceneTaskCompleted?MISSION_STATES.RETURNING:job.serviceVehicleArrived?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,job.sceneTaskCompleted?'scene-complete':job.serviceVehicleArrived?'assist-scene':'respond-incident');}`
- [x] `if` — linha 4213, coluna 130 em `updateCareerMissions()` — `else if(job.emergencyIncidentId){const incident=world.activeIncident?.id===job.emergencyIncidentId?world.activeIncident:null;if(!incident&&job.sceneTaskCompleted)completeActiveJob();else if(incident)setMissionState(job,job.sceneTaskCompleted?MISSION_STATES.RETURNING:job.serviceVehicleArrived?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,job.sceneTaskCompleted?'scene-complete':job.serviceVehicleArrived?'assist-scene':'respond-incident');}`
- [x] `else` — linha 4213, coluna 187 em `updateCareerMissions()` — `else if(job.emergencyIncidentId){const incident=world.activeIncident?.id===job.emergencyIncidentId?world.activeIncident:null;if(!incident&&job.sceneTaskCompleted)completeActiveJob();else if(incident)setMissionState(job,job.sceneTaskCompleted?MISSION_STATES.RETURNING:job.serviceVehicleArrived?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,job.sceneTaskCompleted?'scene-complete':job.serviceVehicleArrived?'assist-scene':'respond-incident');}`
- [x] `if` — linha 4213, coluna 192 em `updateCareerMissions()` — `else if(job.emergencyIncidentId){const incident=world.activeIncident?.id===job.emergencyIncidentId?world.activeIncident:null;if(!incident&&job.sceneTaskCompleted)completeActiveJob();else if(incident)setMissionState(job,job.sceneTaskCompleted?MISSION_STATES.RETURNING:job.serviceVehicleArrived?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,job.sceneTaskCompleted?'scene-complete':job.serviceVehicleArrived?'assist-scene':'respond-incident');}`
- [x] `else` — linha 4214, coluna 5 em `updateCareerMissions()` — `else if(job.id==='police'){`
- [x] `if` — linha 4214, coluna 10 em `updateCareerMissions()` — `else if(job.id==='police'){`
- [x] `if` — linha 4215, coluna 116 em `updateCareerMissions()` — `const route=job.route||[],targetId=route[Number(job.progress||0)],loc=MAP_LOCATIONS.find(x=>x.id===targetId);if(loc){const distance=Math.hypot(player.x-(loc.navX??loc.x),player.z-(loc.navZ??loc.z)),correctVehicle=isDrivingServiceVehicle('police');setMissionState(job,!correctVehicle?MISSION_STATES.PREPARING:distance<5?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,!correctVehicle?'need-police-car':distance<5?'checkpoint':'route');if(correctVehicle&&distance<5&&!job.lastCheckpointAt){job.lastCheckpointAt=Date.now();job.progress=(job.progress||0)+1;toast(\`Patrulha: ${loc.name} verificado com a viatura.\`,'good',1900);beep(720,70);if(job.progress>=route.length){completeActiveJob();state.waypoint=null;updateWaypointMarker();}else setWaypoint(route[job.progress]);saveState(true);}if(job.lastCheckpointAt&&Date.now()-job.lastCheckpointAt>1600)job.lastCheckpointAt=0;}`
- [x] `if` — linha 4215, coluna 450 em `updateCareerMissions()` — `const route=job.route||[],targetId=route[Number(job.progress||0)],loc=MAP_LOCATIONS.find(x=>x.id===targetId);if(loc){const distance=Math.hypot(player.x-(loc.navX??loc.x),player.z-(loc.navZ??loc.z)),correctVehicle=isDrivingServiceVehicle('police');setMissionState(job,!correctVehicle?MISSION_STATES.PREPARING:distance<5?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,!correctVehicle?'need-police-car':distance<5?'checkpoint':'route');if(correctVehicle&&distance<5&&!job.lastCheckpointAt){job.lastCheckpointAt=Date.now();job.progress=(job.progress||0)+1;toast(\`Patrulha: ${loc.name} verificado com a viatura.\`,'good',1900);beep(720,70);if(job.progress>=route.length){completeActiveJob();state.waypoint=null;updateWaypointMarker();}else setWaypoint(route[job.progress]);saveState(true);}if(job.lastCheckpointAt&&Date.now()-job.lastCheckpointAt>1600)job.lastCheckpointAt=0;}`
- [x] `if` — linha 4215, coluna 651 em `updateCareerMissions()` — `const route=job.route||[],targetId=route[Number(job.progress||0)],loc=MAP_LOCATIONS.find(x=>x.id===targetId);if(loc){const distance=Math.hypot(player.x-(loc.navX??loc.x),player.z-(loc.navZ??loc.z)),correctVehicle=isDrivingServiceVehicle('police');setMissionState(job,!correctVehicle?MISSION_STATES.PREPARING:distance<5?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,!correctVehicle?'need-police-car':distance<5?'checkpoint':'route');if(correctVehicle&&distance<5&&!job.lastCheckpointAt){job.lastCheckpointAt=Date.now();job.progress=(job.progress||0)+1;toast(\`Patrulha: ${loc.name} verificado com a viatura.\`,'good',1900);beep(720,70);if(job.progress>=route.length){completeActiveJob();state.waypoint=null;updateWaypointMarker();}else setWaypoint(route[job.progress]);saveState(true);}if(job.lastCheckpointAt&&Date.now()-job.lastCheckpointAt>1600)job.lastCheckpointAt=0;}`
- [x] `else` — linha 4215, coluna 746 em `updateCareerMissions()` — `const route=job.route||[],targetId=route[Number(job.progress||0)],loc=MAP_LOCATIONS.find(x=>x.id===targetId);if(loc){const distance=Math.hypot(player.x-(loc.navX??loc.x),player.z-(loc.navZ??loc.z)),correctVehicle=isDrivingServiceVehicle('police');setMissionState(job,!correctVehicle?MISSION_STATES.PREPARING:distance<5?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,!correctVehicle?'need-police-car':distance<5?'checkpoint':'route');if(correctVehicle&&distance<5&&!job.lastCheckpointAt){job.lastCheckpointAt=Date.now();job.progress=(job.progress||0)+1;toast(\`Patrulha: ${loc.name} verificado com a viatura.\`,'good',1900);beep(720,70);if(job.progress>=route.length){completeActiveJob();state.waypoint=null;updateWaypointMarker();}else setWaypoint(route[job.progress]);saveState(true);}if(job.lastCheckpointAt&&Date.now()-job.lastCheckpointAt>1600)job.lastCheckpointAt=0;}`
- [x] `if` — linha 4215, coluna 801 em `updateCareerMissions()` — `const route=job.route||[],targetId=route[Number(job.progress||0)],loc=MAP_LOCATIONS.find(x=>x.id===targetId);if(loc){const distance=Math.hypot(player.x-(loc.navX??loc.x),player.z-(loc.navZ??loc.z)),correctVehicle=isDrivingServiceVehicle('police');setMissionState(job,!correctVehicle?MISSION_STATES.PREPARING:distance<5?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,!correctVehicle?'need-police-car':distance<5?'checkpoint':'route');if(correctVehicle&&distance<5&&!job.lastCheckpointAt){job.lastCheckpointAt=Date.now();job.progress=(job.progress||0)+1;toast(\`Patrulha: ${loc.name} verificado com a viatura.\`,'good',1900);beep(720,70);if(job.progress>=route.length){completeActiveJob();state.waypoint=null;updateWaypointMarker();}else setWaypoint(route[job.progress]);saveState(true);}if(job.lastCheckpointAt&&Date.now()-job.lastCheckpointAt>1600)job.lastCheckpointAt=0;}`
- [x] `else` — linha 4216, coluna 6 em `updateCareerMissions()` — `}else if(job.id==='delivery'){const d=Math.hypot(player.x-65,player.z-54);setMissionState(job,d<5?MISSION_STATES.ACTION_REQUIRED:player.vehicle?MISSION_STATES.TRAVELLING:MISSION_STATES.PREPARING,d<5?'deliver-to-maya':player.vehicle?'driving':'need-vehicle');}`
- [x] `if` — linha 4216, coluna 11 em `updateCareerMissions()` — `}else if(job.id==='delivery'){const d=Math.hypot(player.x-65,player.z-54);setMissionState(job,d<5?MISSION_STATES.ACTION_REQUIRED:player.vehicle?MISSION_STATES.TRAVELLING:MISSION_STATES.PREPARING,d<5?'deliver-to-maya':player.vehicle?'driving':'need-vehicle');}`
- [x] `else` — linha 4217, coluna 5 em `updateCareerMissions()` — `else if(job.id==='teacher'){const atSchool=!!currentHouse?.id?.startsWith('school');setMissionState(job,atSchool?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,atSchool?'teach':'go-school');}`
- [x] `if` — linha 4217, coluna 10 em `updateCareerMissions()` — `else if(job.id==='teacher'){const atSchool=!!currentHouse?.id?.startsWith('school');setMissionState(job,atSchool?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,atSchool?'teach':'go-school');}`
- [x] `else` — linha 4218, coluna 5 em `updateCareerMissions()` — `else if(job.id==='firefighter'){const fire=world.fires.find(f=>f.active),distance=fire?Math.hypot(player.x-fire.navX,player.z-fire.navZ):Infinity;if(isDrivingServiceVehicle('firefighter')&&distance<6&&!job.serviceVehicleArrived){job.serviceVehicleArrived=true;toast('Caminhão posicionado. Estacione, saia e use a mangueira.','good',2500);saveState(true);}setMissionState(job,job.serviceVehicleArrived?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,fire?'respond':'await-call');}`
- [x] `if` — linha 4218, coluna 10 em `updateCareerMissions()` — `else if(job.id==='firefighter'){const fire=world.fires.find(f=>f.active),distance=fire?Math.hypot(player.x-fire.navX,player.z-fire.navZ):Infinity;if(isDrivingServiceVehicle('firefighter')&&distance<6&&!job.serviceVehicleArrived){job.serviceVehicleArrived=true;toast('Caminhão posicionado. Estacione, saia e use a mangueira.','good',2500);saveState(true);}setMissionState(job,job.serviceVehicleArrived?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,fire?'respond':'await-call');}`
- [x] `if` — linha 4218, coluna 151 em `updateCareerMissions()` — `else if(job.id==='firefighter'){const fire=world.fires.find(f=>f.active),distance=fire?Math.hypot(player.x-fire.navX,player.z-fire.navZ):Infinity;if(isDrivingServiceVehicle('firefighter')&&distance<6&&!job.serviceVehicleArrived){job.serviceVehicleArrived=true;toast('Caminhão posicionado. Estacione, saia e use a mangueira.','good',2500);saveState(true);}setMissionState(job,job.serviceVehicleArrived?MISSION_STATES.ACTION_REQUIRED:MISSION_STATES.TRAVELLING,fire?'respond':'await-call');}`
- [x] `else` — linha 4219, coluna 5 em `updateCareerMissions()` — `else if(job.id==='paramedic'){const incident=ensureActiveTrafficIncident(true);if(incident&&!job.emergencyIncidentId)job.emergencyIncidentId=incident.id;}`
- [x] `if` — linha 4219, coluna 10 em `updateCareerMissions()` — `else if(job.id==='paramedic'){const incident=ensureActiveTrafficIncident(true);if(incident&&!job.emergencyIncidentId)job.emergencyIncidentId=incident.id;}`
- [x] `if` — linha 4219, coluna 84 em `updateCareerMissions()` — `else if(job.id==='paramedic'){const incident=ensureActiveTrafficIncident(true);if(incident&&!job.emergencyIncidentId)job.emergencyIncidentId=incident.id;}`
- [x] `else` — linha 4220, coluna 5 em `updateCareerMissions()` — `else setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'collect-or-build');checkActiveJob();signature=\`${job.id}|${job.missionState}|${activeJobProgress(job).label}\`;if(signature!==updateCareerMissions.signature){updateCareerMissions.signature=signature;updateMissionHUD();}`
- [x] `if` — linha 4220, coluna 172 em `updateCareerMissions()` — `else setMissionState(job,MISSION_STATES.ACTION_REQUIRED,'collect-or-build');checkActiveJob();signature=\`${job.id}|${job.missionState}|${activeJobProgress(job).label}\`;if(signature!==updateCareerMissions.signature){updateCareerMissions.signature=signature;updateMissionHUD();}`
- [x] `if` — linha 4224, coluna 354 em `openTeacherJobLesson()` — `openModal('Missão de professor',\`<div class="teacher-mission"><span>🧑‍🏫</span><h3>Conduza uma atividade segura</h3><p>${q[0]}</p><div class="choice-grid">${q[1].map((answer,i)=>\`<button class="choice" data-teacher-answer="${i}"><b>${answer}</b></button>\`).join('')}</div></div>\`,root=>{$$('[data-teacher-answer]',root).forEach(btn=>btn.onclick=()=>{if(Number(btn.dataset.teacherAnswer)!==q[2]){toast('Vamos pensar de novo com calma.','warn',1400);return;}state.career.activeJob.completed=true;closeModal();completeActiveJob();addXP(55);toast(\`Aula concluída na ${house.name}!\`,'good',2400);});});`
- [x] `return` — linha 4224, coluna 452 em `openTeacherJobLesson()` — `openModal('Missão de professor',\`<div class="teacher-mission"><span>🧑‍🏫</span><h3>Conduza uma atividade segura</h3><p>${q[0]}</p><div class="choice-grid">${q[1].map((answer,i)=>\`<button class="choice" data-teacher-answer="${i}"><b>${answer}</b></button>\`).join('')}</div></div>\`,root=>{$$('[data-teacher-answer]',root).forEach(btn=>btn.onclick=()=>{if(Number(btn.dataset.teacherAnswer)!==q[2]){toast('Vamos pensar de novo com calma.','warn',1400);return;}state.career.activeJob.completed=true;closeModal();completeActiveJob();addXP(55);toast(\`Aula concluída na ${house.name}!\`,'good',2400);});});`
- [x] `if` — linha 4229, coluna 48 em `openFireStationDesk()` — `const startBtn=$('[data-fire-job]',root);if(startBtn)startBtn.onclick=()=>{if(state.career.activeJob){toast('Escolha “Trocar de trabalho” no painel de empregos.','warn');return;}startJob(JOBS.find(j=>j.id==='firefighter'));};`
- [x] `if` — linha 4229, coluna 82 em `openFireStationDesk()` — `const startBtn=$('[data-fire-job]',root);if(startBtn)startBtn.onclick=()=>{if(state.career.activeJob){toast('Escolha “Trocar de trabalho” no painel de empregos.','warn');return;}startJob(JOBS.find(j=>j.id==='firefighter'));};`
- [x] `return` — linha 4229, coluna 177 em `openFireStationDesk()` — `const startBtn=$('[data-fire-job]',root);if(startBtn)startBtn.onclick=()=>{if(state.career.activeJob){toast('Escolha “Trocar de trabalho” no painel de empregos.','warn');return;}startJob(JOBS.find(j=>j.id==='firefighter'));};`
- [x] `if` — linha 4230, coluna 49 em `openFireStationDesk()` — `const cont=$('[data-fire-continue]',root);if(cont)cont.onclick=()=>{closeModal();focusActiveJob();};`
- [x] `if` — linha 4231, coluna 49 em `openFireStationDesk()` — `const cancel=$('[data-fire-cancel]',root);if(cancel)cancel.onclick=async()=>{closeModal();await cancelActiveJob();};`
- [x] `if` — linha 4232, coluna 85 em `openFireStationDesk()` — `$('[data-fire-call]',root).onclick=()=>{const current=ensureActiveFire(false);if(!current){toast('Nenhum chamado ativo agora.','good');return;}state.waypoint={id:\`fire-${current.id}\`,name:current.name,x:current.x,z:current.z,navX:current.navX,navZ:current.navZ};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();closeModal();if(currentHouse)exitHouse();toast(\`Chamado marcado: ${current.name}\`,'good');};`
- [x] `return` — linha 4232, coluna 142 em `openFireStationDesk()` — `$('[data-fire-call]',root).onclick=()=>{const current=ensureActiveFire(false);if(!current){toast('Nenhum chamado ativo agora.','good');return;}state.waypoint={id:\`fire-${current.id}\`,name:current.name,x:current.x,z:current.z,navX:current.navX,navZ:current.navZ};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();closeModal();if(currentHouse)exitHouse();toast(\`Chamado marcado: ${current.name}\`,'good');};`
- [x] `if` — linha 4232, coluna 361 em `openFireStationDesk()` — `$('[data-fire-call]',root).onclick=()=>{const current=ensureActiveFire(false);if(!current){toast('Nenhum chamado ativo agora.','good');return;}state.waypoint={id:\`fire-${current.id}\`,name:current.name,x:current.x,z:current.z,navX:current.navX,navZ:current.navZ};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();closeModal();if(currentHouse)exitHouse();toast(\`Chamado marcado: ${current.name}\`,'good');};`
- [x] `if` — linha 4239, coluna 5 em `startDeliveryJob()` — `if(state.flags.deliveryActive){toast('Você já está fazendo uma entrega.','warn');return;}state.flags.deliveryActive=true;state.inventory.package=1;toast('Pacote recebido. Leve até Maya!','good',2200);saveState();`
- [x] `return` — linha 4239, coluna 86 em `startDeliveryJob()` — `if(state.flags.deliveryActive){toast('Você já está fazendo uma entrega.','warn');return;}state.flags.deliveryActive=true;state.inventory.package=1;toast('Pacote recebido. Leve até Maya!','good',2200);saveState();`

## `23-vehicle-effects-driving.js`

- [x] `if` — linha 4246, coluna 55 em `clearServiceVehicleVisual()` — `const layer=vehicleVisual?.userData?.serviceLayer;if(layer){vehicleVisual.remove(layer);disposeDetachedVisual(layer);}`
- [x] `if` — linha 4247, coluna 5 em `clearServiceVehicleVisual()` — `if(vehicleVisual){vehicleVisual.userData.serviceLayer=null;vehicleVisual.userData.serviceLights=[];vehicleVisual.scale.set(1,1,1);}`
- [x] `return` — linha 4249, coluna 43 em `serviceVehicleIcon()` — `function serviceVehicleIcon(kind='car'){return({police:'🚓',firefighter:'🚒',paramedic:'🚑',ambulance:'🚑',fire:'🚒'})[kind]||'🚗';}`
- [x] `if` — linha 4251, coluna 5 em `applyServiceVehicleVisual()` — `if(!vehicleVisual)return;clearServiceVehicleVisual();const kind=String(vehicle.serviceType||vehicle.kind||'car');if(!['police','firefighter','fire','paramedic','ambulance'].includes(kind))return;`
- [x] `return` — linha 4251, coluna 23 em `applyServiceVehicleVisual()` — `if(!vehicleVisual)return;clearServiceVehicleVisual();const kind=String(vehicle.serviceType||vehicle.kind||'car');if(!['police','firefighter','fire','paramedic','ambulance'].includes(kind))return;`
- [x] `if` — linha 4251, coluna 118 em `applyServiceVehicleVisual()` — `if(!vehicleVisual)return;clearServiceVehicleVisual();const kind=String(vehicle.serviceType||vehicle.kind||'car');if(!['police','firefighter','fire','paramedic','ambulance'].includes(kind))return;`
- [x] `return` — linha 4251, coluna 193 em `applyServiceVehicleVisual()` — `if(!vehicleVisual)return;clearServiceVehicleVisual();const kind=String(vehicle.serviceType||vehicle.kind||'car');if(!['police','firefighter','fire','paramedic','ambulance'].includes(kind))return;`
- [x] `return` — linha 4253, coluna 190 em `applyServiceVehicleVisual()` — `const addLight=(x,color,emissive)=>{const mesh=box(.46,.16,.3,mat(color,{emissive,emissiveIntensity:.95,roughness:.2}),x,1.39,-.1,layer);vehicleVisual.userData.serviceLights.push(mesh);return mesh;};`
- [x] `if` — linha 4254, coluna 5 em `applyServiceVehicleVisual()` — `if(kind==='police'){`
- [x] `else` — linha 4256, coluna 6 em `applyServiceVehicleVisual()` — `}else if(kind==='paramedic'||kind==='ambulance'){`
- [x] `if` — linha 4256, coluna 11 em `applyServiceVehicleVisual()` — `}else if(kind==='paramedic'||kind==='ambulance'){`
- [x] `else` — linha 4258, coluna 6 em `applyServiceVehicleVisual()` — `}else{`
- [x] `for` — linha 4259, coluna 227 em `applyServiceVehicleVisual()` — `vehicleVisual.scale.set(1.18,1.2,1.42);box(1.92,.88,1.65,0xc93431,0,1.02,-.58,layer);box(1.86,.14,2.15,0xf2d84d,0,.76,-.2,layer);box(.22,.22,2.5,0xe8edf0,-.45,1.62,-.2,layer);box(.22,.22,2.5,0xe8edf0,.45,1.62,-.2,layer);for(let z=-1.15;z<=.95;z+=.42)box(.95,.07,.07,0xe8edf0,0,1.64,z,layer);addLight(-.27,0xe53945,0xa50018);addLight(.27,0x42bfff,0x087db4);const patch=new THREE.Mesh(new THREE.PlaneGeometry(.92,.24),new THREE.MeshStandardMaterial({map:signTexture('BOMBEIROS','#b52d2a','#ffffff'),transparent:true,side:THREE.DoubleSide}));patch.position.set(0,1.28,1.31);layer.add(patch);`
- [x] `if` — linha 4261, coluna 24 em `applyServiceVehicleVisual()` — `layer.traverse(o=>{if(o.isMesh){o.castShadow=true;o.receiveShadow=true;addVoxelOutline(o,0x152235,.2);}});`
- [x] `if` — linha 4264, coluna 5 em `spawnDust()` — `if(fxParticles.length>=FX_MAX_PARTICLES){const oldest=fxParticles.shift();worldGroup.remove(oldest.mesh);oldest.mesh.geometry.dispose();oldest.mesh.material.dispose();}`
- [x] `for` — linha 4270, coluna 5 em `updateFX()` — `for(let i=fxParticles.length-1;i>=0;i--){`
- [x] `if` — linha 4275, coluna 7 em `updateFX()` — `if(p.life<=0){worldGroup.remove(p.mesh);p.mesh.geometry.dispose();p.mesh.material.dispose();fxParticles.splice(i,1);}`
- [x] `if` — linha 4280, coluna 5 em `startEngineSound()` — `if(!state.settings.sound||engineAudio)return;`
- [x] `return` — linha 4280, coluna 43 em `startEngineSound()` — `if(!state.settings.sound||engineAudio)return;`
- [x] `if` — linha 4282, coluna 64 em `startEngineSound()` — `const Ctx=window.AudioContext||window.webkitAudioContext;if(!Ctx)return;`
- [x] `return` — linha 4282, coluna 72 em `startEngineSound()` — `const Ctx=window.AudioContext||window.webkitAudioContext;if(!Ctx)return;`
- [x] `catch` — linha 4288, coluna 6 em `startEngineSound()` — `}catch(_){}`
- [x] `if` — linha 4291, coluna 5 em `stopEngineSound()` — `if(!engineAudio)return;`
- [x] `return` — linha 4291, coluna 21 em `stopEngineSound()` — `if(!engineAudio)return;`
- [x] `catch` — linha 4292, coluna 145 em `stopEngineSound()` — `try{engineAudio.gain.gain.linearRampToValueAtTime(0,engineAudio.ctx.currentTime+.12);engineAudio.osc.stop(engineAudio.ctx.currentTime+.16);}catch(_){}`
- [x] `if` — linha 4296, coluna 5 em `updateVehicleFX()` — `if(!player.vehicle){if(engineAudio)stopEngineSound();return;}`
- [x] `if` — linha 4296, coluna 25 em `updateVehicleFX()` — `if(!player.vehicle){if(engineAudio)stopEngineSound();return;}`
- [x] `return` — linha 4296, coluna 58 em `updateVehicleFX()` — `if(!player.vehicle){if(engineAudio)stopEngineSound();return;}`
- [x] `if` — linha 4297, coluna 5 em `updateVehicleFX()` — `if(player.car.passengerOf){if(engineAudio)stopEngineSound();if(els.vehicleBadge)els.vehicleBadge.textContent=\`${serviceVehicleIcon(player.car.kind)} Passageiro — AÇÃO para sair\`;return;}`
- [x] `if` — linha 4297, coluna 32 em `updateVehicleFX()` — `if(player.car.passengerOf){if(engineAudio)stopEngineSound();if(els.vehicleBadge)els.vehicleBadge.textContent=\`${serviceVehicleIcon(player.car.kind)} Passageiro — AÇÃO para sair\`;return;}`
- [x] `if` — linha 4297, coluna 65 em `updateVehicleFX()` — `if(player.car.passengerOf){if(engineAudio)stopEngineSound();if(els.vehicleBadge)els.vehicleBadge.textContent=\`${serviceVehicleIcon(player.car.kind)} Passageiro — AÇÃO para sair\`;return;}`
- [x] `return` — linha 4297, coluna 183 em `updateVehicleFX()` — `if(player.car.passengerOf){if(engineAudio)stopEngineSound();if(els.vehicleBadge)els.vehicleBadge.textContent=\`${serviceVehicleIcon(player.car.kind)} Passageiro — AÇÃO para sair\`;return;}`
- [x] `if` — linha 4306, coluna 5 em `updateVehicleFX()` — `if(car.drift>.4&&Math.abs(car.speed)>3){`
- [x] `if` — linha 4308, coluna 7 em `updateVehicleFX()` — `if(updateVehicleFX.acc>.045){updateVehicleFX.acc=0;spawnDust(player.x-Math.sin(car.heading)*1.05,player.z-Math.cos(car.heading)*1.05);}`
- [x] `if` — linha 4309, coluna 30 em `updateVehicleFX()` — `driftSoundCooldown-=dt;if(driftSoundCooldown<=0){driftSoundCooldown=.5;beep(180,55,'sawtooth');}`
- [x] `for` — linha 4311, coluna 91 em `updateVehicleFX()` — `const serviceKind=String(car.kind||'car'),serviceIcon=serviceVehicleIcon(serviceKind);for(const [index,light] of (vehicleVisual.userData.serviceLights||[]).entries())light.material.emissiveIntensity=.32+(Math.floor(performance.now()/210)+index)%2*.9;if(els.vehicleBadge)els.vehicleBadge.textContent=\`${serviceIcon} ${car.label||'Veículo'} • ${Math.round(Math.abs(car.speed)*6)} km/h${sprintRequested()?' • TURBO':''} — AÇÃO para sair\`;`
- [x] `if` — linha 4311, coluna 255 em `updateVehicleFX()` — `const serviceKind=String(car.kind||'car'),serviceIcon=serviceVehicleIcon(serviceKind);for(const [index,light] of (vehicleVisual.userData.serviceLights||[]).entries())light.material.emissiveIntensity=.32+(Math.floor(performance.now()/210)+index)%2*.9;if(els.vehicleBadge)els.vehicleBadge.textContent=\`${serviceIcon} ${car.label||'Veículo'} • ${Math.round(Math.abs(car.speed)*6)} km/h${sprintRequested()?' • TURBO':''} — AÇÃO para sair\`;`
- [x] `if` — linha 4312, coluna 5 em `updateVehicleFX()` — `if(!state.settings.sound&&engineAudio)stopEngineSound();`
- [x] `else` — linha 4313, coluna 5 em `updateVehicleFX()` — `else if(state.settings.sound&&!engineAudio)startEngineSound();`
- [x] `if` — linha 4313, coluna 10 em `updateVehicleFX()` — `else if(state.settings.sound&&!engineAudio)startEngineSound();`
- [x] `else` — linha 4314, coluna 5 em `updateVehicleFX()` — `else if(state.settings.sound&&engineAudio){`
- [x] `if` — linha 4314, coluna 10 em `updateVehicleFX()` — `else if(state.settings.sound&&engineAudio){`
- [x] `catch` — linha 4316, coluna 92 em `updateVehicleFX()` — `try{engineAudio.osc.frequency.setTargetAtTime(freq,engineAudio.ctx.currentTime,.05);}catch(_){}`
- [x] `return` — linha 4320, coluna 35 em `mobilityDriverActive()` — `function mobilityDriverActive(){return(player.vehicle&&!player.car.passengerOf)||(player.boating&&!player.boat.passengerOf);}`
- [x] `if` — linha 4324, coluna 5 em `updateMobilityControlLabels()` — `if(mobility){if(runIcon)runIcon.textContent='▲';if(runLabel)runLabel.textContent='Acelerar';if(jumpIcon)jumpIcon.textContent=speed>.04?'■':'▼';if(jumpLabel)jumpLabel.textContent=speed>.04?'Freio':'Ré';if(actionIcon)actionIcon.textContent=isBoat?'🛶':'🚗';if(actionLabel)actionLabel.textContent='Sair';}`
- [x] `if` — linha 4324, coluna 18 em `updateMobilityControlLabels()` — `if(mobility){if(runIcon)runIcon.textContent='▲';if(runLabel)runLabel.textContent='Acelerar';if(jumpIcon)jumpIcon.textContent=speed>.04?'■':'▼';if(jumpLabel)jumpLabel.textContent=speed>.04?'Freio':'Ré';if(actionIcon)actionIcon.textContent=isBoat?'🛶':'🚗';if(actionLabel)actionLabel.textContent='Sair';}`
- [x] `if` — linha 4324, coluna 53 em `updateMobilityControlLabels()` — `if(mobility){if(runIcon)runIcon.textContent='▲';if(runLabel)runLabel.textContent='Acelerar';if(jumpIcon)jumpIcon.textContent=speed>.04?'■':'▼';if(jumpLabel)jumpLabel.textContent=speed>.04?'Freio':'Ré';if(actionIcon)actionIcon.textContent=isBoat?'🛶':'🚗';if(actionLabel)actionLabel.textContent='Sair';}`
- [x] `if` — linha 4324, coluna 97 em `updateMobilityControlLabels()` — `if(mobility){if(runIcon)runIcon.textContent='▲';if(runLabel)runLabel.textContent='Acelerar';if(jumpIcon)jumpIcon.textContent=speed>.04?'■':'▼';if(jumpLabel)jumpLabel.textContent=speed>.04?'Freio':'Ré';if(actionIcon)actionIcon.textContent=isBoat?'🛶':'🚗';if(actionLabel)actionLabel.textContent='Sair';}`
- [x] `if` — linha 4324, coluna 148 em `updateMobilityControlLabels()` — `if(mobility){if(runIcon)runIcon.textContent='▲';if(runLabel)runLabel.textContent='Acelerar';if(jumpIcon)jumpIcon.textContent=speed>.04?'■':'▼';if(jumpLabel)jumpLabel.textContent=speed>.04?'Freio':'Ré';if(actionIcon)actionIcon.textContent=isBoat?'🛶':'🚗';if(actionLabel)actionLabel.textContent='Sair';}`
- [x] `if` — linha 4324, coluna 206 em `updateMobilityControlLabels()` — `if(mobility){if(runIcon)runIcon.textContent='▲';if(runLabel)runLabel.textContent='Acelerar';if(jumpIcon)jumpIcon.textContent=speed>.04?'■':'▼';if(jumpLabel)jumpLabel.textContent=speed>.04?'Freio':'Ré';if(actionIcon)actionIcon.textContent=isBoat?'🛶':'🚗';if(actionLabel)actionLabel.textContent='Sair';}`
- [x] `if` — linha 4324, coluna 258 em `updateMobilityControlLabels()` — `if(mobility){if(runIcon)runIcon.textContent='▲';if(runLabel)runLabel.textContent='Acelerar';if(jumpIcon)jumpIcon.textContent=speed>.04?'■':'▼';if(jumpLabel)jumpLabel.textContent=speed>.04?'Freio':'Ré';if(actionIcon)actionIcon.textContent=isBoat?'🛶':'🚗';if(actionLabel)actionLabel.textContent='Sair';}`
- [x] `else` — linha 4325, coluna 5 em `updateMobilityControlLabels()` — `else{if(runIcon)runIcon.textContent='🏃';if(runLabel)runLabel.textContent='Correr';if(jumpIcon)jumpIcon.textContent='⬆';if(jumpLabel)jumpLabel.textContent='Pular';}`
- [x] `if` — linha 4325, coluna 10 em `updateMobilityControlLabels()` — `else{if(runIcon)runIcon.textContent='🏃';if(runLabel)runLabel.textContent='Correr';if(jumpIcon)jumpIcon.textContent='⬆';if(jumpLabel)jumpLabel.textContent='Pular';}`
- [x] `if` — linha 4325, coluna 45 em `updateMobilityControlLabels()` — `else{if(runIcon)runIcon.textContent='🏃';if(runLabel)runLabel.textContent='Correr';if(jumpIcon)jumpIcon.textContent='⬆';if(jumpLabel)jumpLabel.textContent='Pular';}`
- [x] `if` — linha 4325, coluna 87 em `updateMobilityControlLabels()` — `else{if(runIcon)runIcon.textContent='🏃';if(runLabel)runLabel.textContent='Correr';if(jumpIcon)jumpIcon.textContent='⬆';if(jumpLabel)jumpLabel.textContent='Pular';}`
- [x] `if` — linha 4325, coluna 124 em `updateMobilityControlLabels()` — `else{if(runIcon)runIcon.textContent='🏃';if(runLabel)runLabel.textContent='Correr';if(jumpIcon)jumpIcon.textContent='⬆';if(jumpLabel)jumpLabel.textContent='Pular';}`
- [x] `if` — linha 4327, coluna 5 em `updateMobilityControlLabels()` — `if(els.runBtn)els.runBtn.setAttribute('aria-label',mobility?\`Acelerar ${isBoat?'barco':'veículo'}\`:'Correr');if(els.jumpBtn)els.jumpBtn.setAttribute('aria-label',mobility?(speed>.04?'Frear':'Engatar ré'):'Pular');`
- [x] `if` — linha 4327, coluna 114 em `updateMobilityControlLabels()` — `if(els.runBtn)els.runBtn.setAttribute('aria-label',mobility?\`Acelerar ${isBoat?'barco':'veículo'}\`:'Correr');if(els.jumpBtn)els.jumpBtn.setAttribute('aria-label',mobility?(speed>.04?'Frear':'Engatar ré'):'Pular');`
- [x] `if` — linha 4335, coluna 5 em `updateVehicleControlsUI()` — `if(specialIcon)specialIcon.textContent=mobility?'📣':'🔥';if(specialLabel)specialLabel.textContent=mobility?'Buzina':'Poder';if(els.specialBtn)els.specialBtn.setAttribute('aria-label',mobility?\`Buzina do ${player.boating?'barco':'carro'}\`:\`Poder de ${playerDisplayName()}\`);updateMobilityControlLabels();`
- [x] `if` — linha 4335, coluna 61 em `updateVehicleControlsUI()` — `if(specialIcon)specialIcon.textContent=mobility?'📣':'🔥';if(specialLabel)specialLabel.textContent=mobility?'Buzina':'Poder';if(els.specialBtn)els.specialBtn.setAttribute('aria-label',mobility?\`Buzina do ${player.boating?'barco':'carro'}\`:\`Poder de ${playerDisplayName()}\`);updateMobilityControlLabels();`
- [x] `if` — linha 4335, coluna 128 em `updateVehicleControlsUI()` — `if(specialIcon)specialIcon.textContent=mobility?'📣':'🔥';if(specialLabel)specialLabel.textContent=mobility?'Buzina':'Poder';if(els.specialBtn)els.specialBtn.setAttribute('aria-label',mobility?\`Buzina do ${player.boating?'barco':'carro'}\`:\`Poder de ${playerDisplayName()}\`);updateMobilityControlLabels();`
- [x] `if` — linha 4338, coluna 5 em `vehicleHorn()` — `if(!mobilityDriverActive()||paused||!els.modal.hidden)return;const t=performance.now();if(t<player.hornUntil)return;player.hornUntil=t+360;const boat=player.boating;beep(boat?330:410,boat?125:95,boat?'sine':'square');setTimeout(()=>{if(mobilityDriverActive())beep(boat?390:520,boat?100:70,boat?'sine':'square');},105);vibrate(18);if(!boat&&vehicleVisual){vehicleVisual.scale.set(1.015,.99,1.015);setTimeout(()=>vehicleVisual?.scale?.set(1,1,1),120);}`
- [x] `return` — linha 4338, coluna 59 em `vehicleHorn()` — `if(!mobilityDriverActive()||paused||!els.modal.hidden)return;const t=performance.now();if(t<player.hornUntil)return;player.hornUntil=t+360;const boat=player.boating;beep(boat?330:410,boat?125:95,boat?'sine':'square');setTimeout(()=>{if(mobilityDriverActive())beep(boat?390:520,boat?100:70,boat?'sine':'square');},105);vibrate(18);if(!boat&&vehicleVisual){vehicleVisual.scale.set(1.015,.99,1.015);setTimeout(()=>vehicleVisual?.scale?.set(1,1,1),120);}`
- [x] `if` — linha 4338, coluna 92 em `vehicleHorn()` — `if(!mobilityDriverActive()||paused||!els.modal.hidden)return;const t=performance.now();if(t<player.hornUntil)return;player.hornUntil=t+360;const boat=player.boating;beep(boat?330:410,boat?125:95,boat?'sine':'square');setTimeout(()=>{if(mobilityDriverActive())beep(boat?390:520,boat?100:70,boat?'sine':'square');},105);vibrate(18);if(!boat&&vehicleVisual){vehicleVisual.scale.set(1.015,.99,1.015);setTimeout(()=>vehicleVisual?.scale?.set(1,1,1),120);}`
- [x] `return` — linha 4338, coluna 114 em `vehicleHorn()` — `if(!mobilityDriverActive()||paused||!els.modal.hidden)return;const t=performance.now();if(t<player.hornUntil)return;player.hornUntil=t+360;const boat=player.boating;beep(boat?330:410,boat?125:95,boat?'sine':'square');setTimeout(()=>{if(mobilityDriverActive())beep(boat?390:520,boat?100:70,boat?'sine':'square');},105);vibrate(18);if(!boat&&vehicleVisual){vehicleVisual.scale.set(1.015,.99,1.015);setTimeout(()=>vehicleVisual?.scale?.set(1,1,1),120);}`
- [x] `if` — linha 4338, coluna 238 em `vehicleHorn()` — `if(!mobilityDriverActive()||paused||!els.modal.hidden)return;const t=performance.now();if(t<player.hornUntil)return;player.hornUntil=t+360;const boat=player.boating;beep(boat?330:410,boat?125:95,boat?'sine':'square');setTimeout(()=>{if(mobilityDriverActive())beep(boat?390:520,boat?100:70,boat?'sine':'square');},105);vibrate(18);if(!boat&&vehicleVisual){vehicleVisual.scale.set(1.015,.99,1.015);setTimeout(()=>vehicleVisual?.scale?.set(1,1,1),120);}`
- [x] `if` — linha 4338, coluna 335 em `vehicleHorn()` — `if(!mobilityDriverActive()||paused||!els.modal.hidden)return;const t=performance.now();if(t<player.hornUntil)return;player.hornUntil=t+360;const boat=player.boating;beep(boat?330:410,boat?125:95,boat?'sine':'square');setTimeout(()=>{if(mobilityDriverActive())beep(boat?390:520,boat?100:70,boat?'sine':'square');},105);vibrate(18);if(!boat&&vehicleVisual){vehicleVisual.scale.set(1.015,.99,1.015);setTimeout(()=>vehicleVisual?.scale?.set(1,1,1),120);}`
- [x] `if` — linha 4341, coluna 5 em `enterVehicle()` — `if(player.vehicle||player.boating||player.transit.mode||!vehicle||vehicle.occupied||!canEnterMobility(PLAYER_MODES.VEHICLE_DRIVER))return false;`
- [x] `return` — linha 4341, coluna 136 em `enterVehicle()` — `if(player.vehicle||player.boating||player.transit.mode||!vehicle||vehicle.occupied||!canEnterMobility(PLAYER_MODES.VEHICLE_DRIVER))return false;`
- [x] `if` — linha 4344, coluna 93 em `enterVehicle()` — `player.scaleMode='normal';player.crouched=false;syncPlayerRootScale();updateAbilityUI();if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;applyVehicleAppearance(vehicle);vehicleVisual.visible=true;vehicleVisual.scale.set(1,1,1);vehicleVisual.rotation.set(0,0,0);vehicle.group.visible=false;els.vehicleBadge.hidden=false;updateVehicleControlsUI();updateRunUI();setFlag('gotVehicle');state.vehicles.lastUsedId=vehicle.id;if(typeof onServiceVehicleBoarded==='function')onServiceVehicleBoarded(vehicle);`
- [x] `if` — linha 4344, coluna 134 em `enterVehicle()` — `player.scaleMode='normal';player.crouched=false;syncPlayerRootScale();updateAbilityUI();if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;applyVehicleAppearance(vehicle);vehicleVisual.visible=true;vehicleVisual.scale.set(1,1,1);vehicleVisual.rotation.set(0,0,0);vehicle.group.visible=false;els.vehicleBadge.hidden=false;updateVehicleControlsUI();updateRunUI();setFlag('gotVehicle');state.vehicles.lastUsedId=vehicle.id;if(typeof onServiceVehicleBoarded==='function')onServiceVehicleBoarded(vehicle);`
- [x] `if` — linha 4344, coluna 456 em `enterVehicle()` — `player.scaleMode='normal';player.crouched=false;syncPlayerRootScale();updateAbilityUI();if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;applyVehicleAppearance(vehicle);vehicleVisual.visible=true;vehicleVisual.scale.set(1,1,1);vehicleVisual.rotation.set(0,0,0);vehicle.group.visible=false;els.vehicleBadge.hidden=false;updateVehicleControlsUI();updateRunUI();setFlag('gotVehicle');state.vehicles.lastUsedId=vehicle.id;if(typeof onServiceVehicleBoarded==='function')onServiceVehicleBoarded(vehicle);`
- [x] `if` — linha 4345, coluna 5 em `enterVehicle()` — `if(state.career.activeJob?.id==='delivery'){setMissionState(state.career.activeJob,MISSION_STATES.TRAVELLING,'vehicle-boarded');state.waypoint={id:'delivery-maya',name:'Entregar para Maya',x:65,z:54,navX:55,navZ:48,arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();}`
- [x] `if` — linha 4346, coluna 44 em `enterVehicle()` — `const companion=nearestRideCompanion();if(companion)boardNpcPassenger(companion,'car');toast(\`${serviceVehicleIcon(player.car.kind)} ${vehicle.label||'Veículo'} ligado! Use o manche para dirigir.\`,'good');startEngineSound();saveState(true);return true;`
- [x] `return` — linha 4346, coluna 245 em `enterVehicle()` — `const companion=nearestRideCompanion();if(companion)boardNpcPassenger(companion,'car');toast(\`${serviceVehicleIcon(player.car.kind)} ${vehicle.label||'Veículo'} ligado! Use o manche para dirigir.\`,'good');startEngineSound();saveState(true);return true;`
- [x] `if` — linha 4350, coluna 5 em `enterVehicleAsPassenger()` — `if(!ghost||!target?.vehicle||target.vehicleRole==='passenger'){toast('O motorista ou o carro não está mais disponível.','warn');return false;}`
- [x] `return` — linha 4350, coluna 133 em `enterVehicleAsPassenger()` — `if(!ghost||!target?.vehicle||target.vehicleRole==='passenger'){toast('O motorista ou o carro não está mais disponível.','warn');return false;}`
- [x] `if` — linha 4351, coluna 5 em `enterVehicleAsPassenger()` — `if(player.vehicle){toast('Você já está em um veículo.','warn');return false;}`
- [x] `return` — linha 4351, coluna 68 em `enterVehicleAsPassenger()` — `if(player.vehicle){toast('Você já está em um veículo.','warn');return false;}`
- [x] `if` — linha 4352, coluna 5 em `enterVehicleAsPassenger()` — `if(player.boating)exitBoat(true);`
- [x] `if` — linha 4353, coluna 5 em `enterVehicleAsPassenger()` — `if(player.transit.mode||!canEnterMobility(PLAYER_MODES.VEHICLE_PASSENGER))return false;`
- [x] `return` — linha 4353, coluna 79 em `enterVehicleAsPassenger()` — `if(player.transit.mode||!canEnterMobility(PLAYER_MODES.VEHICLE_PASSENGER))return false;`
- [x] `if` — linha 4355, coluna 5 em `enterVehicleAsPassenger()` — `if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;vehicleVisual.visible=false;els.vehicleBadge.hidden=false;updateVehicleControlsUI();updateRunUI();updateAbilityUI();auditPlayerMode('board-remote-car');toast('Você entrou como passageiro. O motorista controla o carro.','good',2500);saveState(true);return true;`
- [x] `if` — linha 4355, coluna 46 em `enterVehicleAsPassenger()` — `if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;vehicleVisual.visible=false;els.vehicleBadge.hidden=false;updateVehicleControlsUI();updateRunUI();updateAbilityUI();auditPlayerMode('board-remote-car');toast('Você entrou como passageiro. O motorista controla o carro.','good',2500);saveState(true);return true;`
- [x] `return` — linha 4355, coluna 335 em `enterVehicleAsPassenger()` — `if(playerModel)playerModel.visible=false;if(avatarLayer)avatarLayer.visible=false;vehicleVisual.visible=false;els.vehicleBadge.hidden=false;updateVehicleControlsUI();updateRunUI();updateAbilityUI();auditPlayerMode('board-remote-car');toast('Você entrou como passageiro. O motorista controla o carro.','good',2500);saveState(true);return true;`
- [x] `if` — linha 4358, coluna 5 em `exitVehicle()` — `if(!player.vehicle)return false;`
- [x] `return` — linha 4358, coluna 24 em `exitVehicle()` — `if(!player.vehicle)return false;`
- [x] `if` — linha 4360, coluna 5 em `exitVehicle()` — `if(passengerHost)window.OTTHOS_RTDB?.sendInteraction?.(passengerHost,{type:'vehiclePassengerLeft'});else if(hostedPassenger)window.OTTHOS_RTDB?.sendInteraction?.(hostedPassenger,{type:'vehicleEnded'});`
- [x] `else` — linha 4360, coluna 105 em `exitVehicle()` — `if(passengerHost)window.OTTHOS_RTDB?.sendInteraction?.(passengerHost,{type:'vehiclePassengerLeft'});else if(hostedPassenger)window.OTTHOS_RTDB?.sendInteraction?.(hostedPassenger,{type:'vehicleEnded'});`
- [x] `if` — linha 4360, coluna 110 em `exitVehicle()` — `if(passengerHost)window.OTTHOS_RTDB?.sendInteraction?.(passengerHost,{type:'vehiclePassengerLeft'});else if(hostedPassenger)window.OTTHOS_RTDB?.sendInteraction?.(hostedPassenger,{type:'vehicleEnded'});`
- [x] `if` — linha 4362, coluna 279 em `exitVehicle()` — `const prior=player.preVehicleAbilities||state.abilities||{scaleMode:'normal',crouched:false};player.scaleMode=['mini','normal','giant'].includes(prior.scaleMode)?prior.scaleMode:'normal';player.crouched=!!prior.crouched;player.preVehicleAbilities=null;syncPlayerRootScale();if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;vehicleVisual.visible=false;vehicleVisual.rotation.set(0,0,0);els.vehicleBadge.hidden=true;updateVehicleControlsUI();updateRunUI();updateAbilityUI();stopEngineSound();`
- [x] `if` — linha 4362, coluna 319 em `exitVehicle()` — `const prior=player.preVehicleAbilities||state.abilities||{scaleMode:'normal',crouched:false};player.scaleMode=['mini','normal','giant'].includes(prior.scaleMode)?prior.scaleMode:'normal';player.crouched=!!prior.crouched;player.preVehicleAbilities=null;syncPlayerRootScale();if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;vehicleVisual.visible=false;vehicleVisual.rotation.set(0,0,0);els.vehicleBadge.hidden=true;updateVehicleControlsUI();updateRunUI();updateAbilityUI();stopEngineSound();`
- [x] `if` — linha 4363, coluna 5 em `exitVehicle()` — `if(drivenVehicle&&!wasPassenger){drivenVehicle.occupied=false;drivenVehicle.group.visible=true;drivenVehicle.group.position.set(parkX,groundHeightAt(parkX,parkZ),parkZ);drivenVehicle.group.rotation.y=parkHeading;drivenVehicle.x=parkX;drivenVehicle.z=parkZ;drivenVehicle.heading=parkHeading;persistParkedVehicle(drivenVehicle);}`
- [x] `if` — linha 4364, coluna 5 em `exitVehicle()` — `if(typeof onServiceVehicleExited==='function')onServiceVehicleExited(drivenVehicle);clearServiceVehicleVisual();activeVehicleRef=null;world.activeVehicle=null;player.x=exitPoint.x;player.z=exitPoint.z;player.y=exitPoint.y;player.vx=player.vy=player.vz=0;player.grounded=true;player.car.id='';player.car.kind='car';rememberSafePlayerPosition(true);auditPlayerMode('exit-vehicle');if(!silent)toast('Saiu do veículo em local seguro.','good');saveState(true);return true;`
- [x] `if` — linha 4364, coluna 384 em `exitVehicle()` — `if(typeof onServiceVehicleExited==='function')onServiceVehicleExited(drivenVehicle);clearServiceVehicleVisual();activeVehicleRef=null;world.activeVehicle=null;player.x=exitPoint.x;player.z=exitPoint.z;player.y=exitPoint.y;player.vx=player.vy=player.vz=0;player.grounded=true;player.car.id='';player.car.kind='car';rememberSafePlayerPosition(true);auditPlayerMode('exit-vehicle');if(!silent)toast('Saiu do veículo em local seguro.','good');saveState(true);return true;`
- [x] `return` — linha 4364, coluna 460 em `exitVehicle()` — `if(typeof onServiceVehicleExited==='function')onServiceVehicleExited(drivenVehicle);clearServiceVehicleVisual();activeVehicleRef=null;world.activeVehicle=null;player.x=exitPoint.x;player.z=exitPoint.z;player.y=exitPoint.y;player.vx=player.vy=player.vz=0;player.grounded=true;player.car.id='';player.car.kind='car';rememberSafePlayerPosition(true);auditPlayerMode('exit-vehicle');if(!silent)toast('Saiu do veículo em local seguro.','good');saveState(true);return true;`
- [x] `if` — linha 4367, coluna 5 em `repairBridge()` — `if(state.flags.bridgeFixed){toast('A ponte já está consertada.','good');return;}`
- [x] `return` — linha 4367, coluna 77 em `repairBridge()` — `if(state.flags.bridgeFixed){toast('A ponte já está consertada.','good');return;}`
- [x] `if` — linha 4368, coluna 5 em `repairBridge()` — `if(state.inventory.wood<3||state.inventory.stone<2){toast('Precisa de 3 madeiras e 2 pedras.','warn');return;}`
- [x] `return` — linha 4368, coluna 107 em `repairBridge()` — `if(state.inventory.wood<3||state.inventory.stone<2){toast('Precisa de 3 madeiras e 2 pedras.','warn');return;}`

## `24-construction-system.js`

- [x] `return` — linha 4383, coluna 105 em `buildCostText()` — `function buildCostText(cost){const names={wood:'madeira',stone:'pedra',blocks:'bloco',fences:'cerca'};return Object.entries(cost).map(([key,value])=>\`${value} ${names[key]||key}${value>1?'s':''}\`).join(' + ');}`
- [x] `return` — linha 4384, coluna 35 em `currentBuildOwnerIds()` — `function currentBuildOwnerIds(){return new Set(['local',state.profile?.playerId,state.account?.accountId,window.OTTHOS_RTDB?.uid].filter(Boolean).map(String));}`
- [x] `return` — linha 4385, coluna 37 em `buildOwnedByPlayer()` — `function buildOwnedByPlayer(data){return !data?.ownerId||currentBuildOwnerIds().has(String(data.ownerId));}`
- [x] `return` — linha 4388, coluna 5 em `buildFootprint()` — `return Math.abs(Math.sin(Number(rotation||0)))>.7?{w:base.d,d:base.w}:{...base};`
- [x] `if` — linha 4391, coluna 5 em `buildPlacementCandidate()` — `if(!buildMode)return null;const distance=['wall','fence','bench','planter'].includes(buildMode)?2.8:2.45;`
- [x] `return` — linha 4391, coluna 19 em `buildPlacementCandidate()` — `if(!buildMode)return null;const distance=['wall','fence','bench','planter'].includes(buildMode)?2.8:2.45;`
- [x] `return` — linha 4394, coluna 5 em `buildPlacementCandidate()` — `return{x,z,y,rotation,valid:canBuildAt(x,z,buildMode,rotation)};`
- [x] `if` — linha 4397, coluna 5 em `canBuildAt()` — `if(!Number.isFinite(x)||!Number.isFinite(z)||!type)return false;const footprint=buildFootprint(type,rotation),rect={x,z,w:footprint.w,d:footprint.d};`
- [x] `return` — linha 4397, coluna 56 em `canBuildAt()` — `if(!Number.isFinite(x)||!Number.isFinite(z)||!type)return false;const footprint=buildFootprint(type,rotation),rect={x,z,w:footprint.w,d:footprint.d};`
- [x] `if` — linha 4399, coluna 5 em `canBuildAt()` — `if(corners.some(([cx,cz])=>waterAt(cx,cz)))return false;`
- [x] `return` — linha 4399, coluna 48 em `canBuildAt()` — `if(corners.some(([cx,cz])=>waterAt(cx,cz)))return false;`
- [x] `if` — linha 4400, coluna 65 em `canBuildAt()` — `const heights=corners.map(([cx,cz])=>groundHeightAt(cx,cz));if(Math.max(...heights)-Math.min(...heights)>.72)return false;`
- [x] `return` — linha 4400, coluna 114 em `canBuildAt()` — `const heights=corners.map(([cx,cz])=>groundHeightAt(cx,cz));if(Math.max(...heights)-Math.min(...heights)>.72)return false;`
- [x] `if` — linha 4401, coluna 139 em `canBuildAt()` — `const permitted=(Math.abs(x)<18&&z>27&&z<48)||world.houses.some(h=>state.houses[h.id]?.owned&&Math.abs(x-h.x)<10&&Math.abs(z-h.z)<10);if(!permitted)return false;`
- [x] `return` — linha 4401, coluna 153 em `canBuildAt()` — `const permitted=(Math.abs(x)<18&&z>27&&z<48)||world.houses.some(h=>state.houses[h.id]?.owned&&Math.abs(x-h.x)<10&&Math.abs(z-h.z)<10);if(!permitted)return false;`
- [x] `if` — linha 4402, coluna 5 em `canBuildAt()` — `if(world.hazards.some(h=>Number.isFinite(h.w)&&Number.isFinite(h.d)&&rectOverlap(rect,h,.2)))return false;`
- [x] `return` — linha 4402, coluna 98 em `canBuildAt()` — `if(world.hazards.some(h=>Number.isFinite(h.w)&&Number.isFinite(h.d)&&rectOverlap(rect,h,.2)))return false;`
- [x] `if` — linha 4403, coluna 5 em `canBuildAt()` — `if(world.colliders.some(c=>rectOverlap(rect,{x:c.x,z:c.z,w:c.w,d:c.d},.22)))return false;`
- [x] `return` — linha 4403, coluna 81 em `canBuildAt()` — `if(world.colliders.some(c=>rectOverlap(rect,{x:c.x,z:c.z,w:c.w,d:c.d},.22)))return false;`
- [x] `if` — linha 4404, coluna 5 em `canBuildAt()` — `if(world.builds.some(b=>{const size=buildFootprint(b.data.type,b.data.rotation);return rectOverlap(rect,{x:b.data.x,z:b.data.z,w:size.w,d:size.d},.18);} ))return false;`
- [x] `return` — linha 4404, coluna 85 em `canBuildAt()` — `if(world.builds.some(b=>{const size=buildFootprint(b.data.type,b.data.rotation);return rectOverlap(rect,{x:b.data.x,z:b.data.z,w:size.w,d:size.d},.18);} ))return false;`
- [x] `return` — linha 4404, coluna 160 em `canBuildAt()` — `if(world.builds.some(b=>{const size=buildFootprint(b.data.type,b.data.rotation);return rectOverlap(rect,{x:b.data.x,z:b.data.z,w:size.w,d:size.d},.18);} ))return false;`
- [x] `return` — linha 4405, coluna 5 em `canBuildAt()` — `return true;`
- [x] `if` — linha 4408, coluna 5 em `createBuildPreviewMesh()` — `if(!window.THREE||!worldGroup)return null;const group=new THREE.Group(),material=new THREE.MeshBasicMaterial({color:0x64ef78,transparent:true,opacity:.42,depthWrite:false,depthTest:true});`
- [x] `return` — linha 4408, coluna 35 em `createBuildPreviewMesh()` — `if(!window.THREE||!worldGroup)return null;const group=new THREE.Group(),material=new THREE.MeshBasicMaterial({color:0x64ef78,transparent:true,opacity:.42,depthWrite:false,depthTest:true});`
- [x] `return` — linha 4409, coluna 180 em `createBuildPreviewMesh()` — `const part=(w,h,d,x,y,z)=>{const mesh=new THREE.Mesh(sharedBoxGeometry(w,h,d),material);mesh.position.set(x,y,z);mesh.renderOrder=990;mesh.frustumCulled=false;group.add(mesh);return mesh;};`
- [x] `if` — linha 4410, coluna 5 em `createBuildPreviewMesh()` — `if(type==='block')part(1.5,1.5,1.5,0,.75,0);`
- [x] `else` — linha 4411, coluna 5 em `createBuildPreviewMesh()` — `else if(type==='wall')part(3,2.4,.32,0,1.2,0);`
- [x] `if` — linha 4411, coluna 10 em `createBuildPreviewMesh()` — `else if(type==='wall')part(3,2.4,.32,0,1.2,0);`
- [x] `else` — linha 4412, coluna 5 em `createBuildPreviewMesh()` — `else if(type==='floor')part(3,.28,3,0,.14,0);`
- [x] `if` — linha 4412, coluna 10 em `createBuildPreviewMesh()` — `else if(type==='floor')part(3,.28,3,0,.14,0);`
- [x] `else` — linha 4413, coluna 5 em `createBuildPreviewMesh()` — `else if(type==='fence'){part(2.4,.18,.22,0,.32,0);for(const x of [-1.0,0,1.0])part(.16,1.05,.16,x,.52,0);}`
- [x] `if` — linha 4413, coluna 10 em `createBuildPreviewMesh()` — `else if(type==='fence'){part(2.4,.18,.22,0,.32,0);for(const x of [-1.0,0,1.0])part(.16,1.05,.16,x,.52,0);}`
- [x] `for` — linha 4413, coluna 55 em `createBuildPreviewMesh()` — `else if(type==='fence'){part(2.4,.18,.22,0,.32,0);for(const x of [-1.0,0,1.0])part(.16,1.05,.16,x,.52,0);}`
- [x] `else` — linha 4414, coluna 5 em `createBuildPreviewMesh()` — `else if(type==='bench'){part(2.2,.22,.65,0,.62,0);part(2.2,.74,.18,0,1.02,-.27);for(const x of [-.8,.8])part(.14,.55,.14,x,.28,0);}`
- [x] `if` — linha 4414, coluna 10 em `createBuildPreviewMesh()` — `else if(type==='bench'){part(2.2,.22,.65,0,.62,0);part(2.2,.74,.18,0,1.02,-.27);for(const x of [-.8,.8])part(.14,.55,.14,x,.28,0);}`
- [x] `for` — linha 4414, coluna 85 em `createBuildPreviewMesh()` — `else if(type==='bench'){part(2.2,.22,.65,0,.62,0);part(2.2,.74,.18,0,1.02,-.27);for(const x of [-.8,.8])part(.14,.55,.14,x,.28,0);}`
- [x] `else` — linha 4415, coluna 5 em `createBuildPreviewMesh()` — `else if(type==='planter'){part(2.2,.55,.86,0,.28,0);part(1.82,.18,.58,0,.58,0);for(const x of [-.65,0,.65]){part(.09,.5,.09,x,.92,0);part(.42,.18,.42,x,1.15,0);}}`
- [x] `if` — linha 4415, coluna 10 em `createBuildPreviewMesh()` — `else if(type==='planter'){part(2.2,.55,.86,0,.28,0);part(1.82,.18,.58,0,.58,0);for(const x of [-.65,0,.65]){part(.09,.5,.09,x,.92,0);part(.42,.18,.42,x,1.15,0);}}`
- [x] `for` — linha 4415, coluna 84 em `createBuildPreviewMesh()` — `else if(type==='planter'){part(2.2,.55,.86,0,.28,0);part(1.82,.18,.58,0,.58,0);for(const x of [-.65,0,.65]){part(.09,.5,.09,x,.92,0);part(.42,.18,.42,x,1.15,0);}}`
- [x] `else` — linha 4416, coluna 5 em `createBuildPreviewMesh()` — `else{part(.22,2.4,.22,0,1.2,0);part(.65,.65,.65,0,2.65,0);}`
- [x] `return` — linha 4417, coluna 99 em `createBuildPreviewMesh()` — `group.userData.previewMaterial=material;group.userData.previewType=type;worldGroup.add(group);return group;`
- [x] `if` — linha 4420, coluna 5 em `disposeBuildPreview()` — `if(!buildPreview)return;worldGroup?.remove(buildPreview);const material=buildPreview.userData?.previewMaterial;material?.dispose?.();buildPreview=null;buildPlacement=null;`
- [x] `return` — linha 4420, coluna 22 em `disposeBuildPreview()` — `if(!buildPreview)return;worldGroup?.remove(buildPreview);const material=buildPreview.userData?.previewMaterial;material?.dispose?.();buildPreview=null;buildPlacement=null;`
- [x] `if` — linha 4423, coluna 5 em `ensureBuildPanel()` — `if(buildPanel)return buildPanel;buildPanel=document.createElement('section');buildPanel.id='buildControlPanel';buildPanel.className='build-control-panel';buildPanel.hidden=true;buildPanel.setAttribute('aria-label','Controles da construção');`
- [x] `return` — linha 4423, coluna 19 em `ensureBuildPanel()` — `if(buildPanel)return buildPanel;buildPanel=document.createElement('section');buildPanel.id='buildControlPanel';buildPanel.className='build-control-panel';buildPanel.hidden=true;buildPanel.setAttribute('aria-label','Controles da construção');`
- [x] `return` — linha 4425, coluna 272 em `ensureBuildPanel()` — `document.body.appendChild(buildPanel);buildPanel.querySelector('[data-build-rotate]').onclick=rotateBuildPreview;buildPanel.querySelector('[data-build-confirm]').onclick=placeBuild;buildPanel.querySelector('[data-build-cancel]').onclick=()=>endBuildMode('cancelled');return buildPanel;`
- [x] `if` — linha 4428, coluna 92 em `updateBuildPanel()` — `const panel=ensureBuildPanel(),recipe=BUILD_RECIPES[buildMode];panel.hidden=!buildMode;if(!buildMode||!recipe)return;`
- [x] `return` — linha 4428, coluna 115 em `updateBuildPanel()` — `const panel=ensureBuildPanel(),recipe=BUILD_RECIPES[buildMode];panel.hidden=!buildMode;if(!buildMode||!recipe)return;`
- [x] `if` — linha 4433, coluna 5 em `updateBuildPreview()` — `if(!buildMode)return;const candidate=buildPlacementCandidate();if(!candidate)return;`
- [x] `return` — linha 4433, coluna 19 em `updateBuildPreview()` — `if(!buildMode)return;const candidate=buildPlacementCandidate();if(!candidate)return;`
- [x] `if` — linha 4433, coluna 68 em `updateBuildPreview()` — `if(!buildMode)return;const candidate=buildPlacementCandidate();if(!candidate)return;`
- [x] `return` — linha 4433, coluna 82 em `updateBuildPreview()` — `if(!buildMode)return;const candidate=buildPlacementCandidate();if(!candidate)return;`
- [x] `if` — linha 4434, coluna 5 em `updateBuildPreview()` — `if(!buildPreview||buildPreview.userData?.previewType!==buildMode){disposeBuildPreview();buildPreview=createBuildPreviewMesh(buildMode);}`
- [x] `if` — linha 4435, coluna 30 em `updateBuildPreview()` — `buildPlacement=candidate;if(buildPreview){buildPreview.position.set(candidate.x,candidate.y+.025,candidate.z);buildPreview.rotation.y=candidate.rotation;const material=buildPreview.userData.previewMaterial;material.color.setHex(candidate.valid?0x64ef78:0xff5266);material.opacity=candidate.valid?.44:.36;}`
- [x] `if` — linha 4436, coluna 5 em `updateBuildPreview()` — `if(force||!updateBuildPreview.last||Math.hypot(candidate.x-updateBuildPreview.last.x,candidate.z-updateBuildPreview.last.z)>.01||candidate.rotation!==updateBuildPreview.last.rotation||candidate.valid!==updateBuildPreview.last.valid){updateBuildPreview.last={...candidate};updateBuildPanel();}`
- [x] `if` — linha 4439, coluna 5 em `beginBuildMode()` — `if(!BUILD_RECIPES[type])return false;if(player.vehicle||player.boating||player.transit.mode||fishingSession||currentHouse){toast('Finalize a atividade atual antes de construir.','warn');return false;}`
- [x] `return` — linha 4439, coluna 29 em `beginBuildMode()` — `if(!BUILD_RECIPES[type])return false;if(player.vehicle||player.boating||player.transit.mode||fishingSession||currentHouse){toast('Finalize a atividade atual antes de construir.','warn');return false;}`
- [x] `if` — linha 4439, coluna 42 em `beginBuildMode()` — `if(!BUILD_RECIPES[type])return false;if(player.vehicle||player.boating||player.transit.mode||fishingSession||currentHouse){toast('Finalize a atividade atual antes de construir.','warn');return false;}`
- [x] `return` — linha 4439, coluna 191 em `beginBuildMode()` — `if(!BUILD_RECIPES[type])return false;if(player.vehicle||player.boating||player.transit.mode||fishingSession||currentHouse){toast('Finalize a atividade atual antes de construir.','warn');return false;}`
- [x] `return` — linha 4440, coluna 353 em `beginBuildMode()` — `disposeBuildPreview();buildMode=type;buildRotation=Math.round(player.facing/(Math.PI/2))*(Math.PI/2);els.buildTypeLabel.textContent=BUILD_RECIPES[type].name;els.buildBadge.hidden=true;ensureBuildPanel().hidden=false;updateBuildPreview(true);updateVehicleControlsUI();updateContext(true);toast('Prévia ativa: mova-se, gire e confirme.','good',1900);return true;`
- [x] `if` — linha 4443, coluna 5 em `endBuildMode()` — `if(!buildMode&&!buildPreview)return false;disposeBuildPreview();buildMode=null;buildRotation=0;updateBuildPreview.last=null;if(buildPanel)buildPanel.hidden=true;els.buildBadge.hidden=true;updateVehicleControlsUI();updateContext(true);if(!silent&&reason==='cancelled')toast('Construção cancelada sem gastar materiais.','good',1400);return true;`
- [x] `return` — linha 4443, coluna 34 em `endBuildMode()` — `if(!buildMode&&!buildPreview)return false;disposeBuildPreview();buildMode=null;buildRotation=0;updateBuildPreview.last=null;if(buildPanel)buildPanel.hidden=true;els.buildBadge.hidden=true;updateVehicleControlsUI();updateContext(true);if(!silent&&reason==='cancelled')toast('Construção cancelada sem gastar materiais.','good',1400);return true;`
- [x] `if` — linha 4443, coluna 129 em `endBuildMode()` — `if(!buildMode&&!buildPreview)return false;disposeBuildPreview();buildMode=null;buildRotation=0;updateBuildPreview.last=null;if(buildPanel)buildPanel.hidden=true;els.buildBadge.hidden=true;updateVehicleControlsUI();updateContext(true);if(!silent&&reason==='cancelled')toast('Construção cancelada sem gastar materiais.','good',1400);return true;`
- [x] `if` — linha 4443, coluna 239 em `endBuildMode()` — `if(!buildMode&&!buildPreview)return false;disposeBuildPreview();buildMode=null;buildRotation=0;updateBuildPreview.last=null;if(buildPanel)buildPanel.hidden=true;els.buildBadge.hidden=true;updateVehicleControlsUI();updateContext(true);if(!silent&&reason==='cancelled')toast('Construção cancelada sem gastar materiais.','good',1400);return true;`
- [x] `return` — linha 4443, coluna 336 em `endBuildMode()` — `if(!buildMode&&!buildPreview)return false;disposeBuildPreview();buildMode=null;buildRotation=0;updateBuildPreview.last=null;if(buildPanel)buildPanel.hidden=true;els.buildBadge.hidden=true;updateVehicleControlsUI();updateContext(true);if(!silent&&reason==='cancelled')toast('Construção cancelada sem gastar materiais.','good',1400);return true;`
- [x] `if` — linha 4445, coluna 33 em `rotateBuildPreview()` — `function rotateBuildPreview(){if(!buildMode)return false;buildRotation=(buildRotation+Math.PI/2)%(Math.PI*2);updateBuildPreview(true);vibrate(12);return true;}`
- [x] `return` — linha 4445, coluna 47 em `rotateBuildPreview()` — `function rotateBuildPreview(){if(!buildMode)return false;buildRotation=(buildRotation+Math.PI/2)%(Math.PI*2);updateBuildPreview(true);vibrate(12);return true;}`
- [x] `return` — linha 4445, coluna 149 em `rotateBuildPreview()` — `function rotateBuildPreview(){if(!buildMode)return false;buildRotation=(buildRotation+Math.PI/2)%(Math.PI*2);updateBuildPreview(true);vibrate(12);return true;}`
- [x] `if` — linha 4447, coluna 5 em `openBuildMenu()` — `if(player.vehicle||player.boating||player.transit.mode||fishingSession){toast('Finalize o transporte ou a atividade atual antes de construir.','warn');return false;}`
- [x] `return` — linha 4447, coluna 156 em `openBuildMenu()` — `if(player.vehicle||player.boating||player.transit.mode||fishingSession){toast('Finalize o transporte ou a atividade atual antes de construir.','warn');return false;}`
- [x] `if` — linha 4449, coluna 93 em `openBuildMenu()` — `$$('[data-type]',root).forEach(btn=>btn.onclick=async()=>{const type=btn.dataset.type;if(type==='remove'){closeModal();await removeNearestBuild();return;}if(type==='extension'){openHouseExtensionMenu();return;}closeModal();beginBuildMode(type);});`
- [x] `return` — linha 4449, coluna 153 em `openBuildMenu()` — `$$('[data-type]',root).forEach(btn=>btn.onclick=async()=>{const type=btn.dataset.type;if(type==='remove'){closeModal();await removeNearestBuild();return;}if(type==='extension'){openHouseExtensionMenu();return;}closeModal();beginBuildMode(type);});`
- [x] `if` — linha 4449, coluna 161 em `openBuildMenu()` — `$$('[data-type]',root).forEach(btn=>btn.onclick=async()=>{const type=btn.dataset.type;if(type==='remove'){closeModal();await removeNearestBuild();return;}if(type==='extension'){openHouseExtensionMenu();return;}closeModal();beginBuildMode(type);});`
- [x] `return` — linha 4449, coluna 209 em `openBuildMenu()` — `$$('[data-type]',root).forEach(btn=>btn.onclick=async()=>{const type=btn.dataset.type;if(type==='remove'){closeModal();await removeNearestBuild();return;}if(type==='extension'){openHouseExtensionMenu();return;}closeModal();beginBuildMode(type);});`
- [x] `if` — linha 4454, coluna 5 em `placeBuild()` — `if(!buildMode||player.vehicle||player.boating||player.transit.mode||currentHouse){toast('Saia do transporte ou interior para construir.','warn');return false;}`
- [x] `return` — linha 4454, coluna 150 em `placeBuild()` — `if(!buildMode||player.vehicle||player.boating||player.transit.mode||currentHouse){toast('Saia do transporte ou interior para construir.','warn');return false;}`
- [x] `if` — linha 4455, coluna 61 em `placeBuild()` — `updateBuildPreview(true);const placement=buildPlacement;if(!placement?.valid){toast('Local bloqueado. Mova-se até um terreno livre e observe a prévia verde.','warn');return false;}`
- [x] `return` — linha 4455, coluna 171 em `placeBuild()` — `updateBuildPreview(true);const placement=buildPlacement;if(!placement?.valid){toast('Local bloqueado. Mova-se até um terreno livre e observe a prévia verde.','warn');return false;}`
- [x] `if` — linha 4456, coluna 43 em `placeBuild()` — `const recipe=BUILD_RECIPES[buildMode];if(!recipe)return false;if(!resourcesEnough(recipe.cost)){toast(\`Faltam materiais: ${buildCostText(recipe.cost)}.\`,'warn');updateBuildPanel();return false;}`
- [x] `return` — linha 4456, coluna 54 em `placeBuild()` — `const recipe=BUILD_RECIPES[buildMode];if(!recipe)return false;if(!resourcesEnough(recipe.cost)){toast(\`Faltam materiais: ${buildCostText(recipe.cost)}.\`,'warn');updateBuildPanel();return false;}`
- [x] `if` — linha 4456, coluna 67 em `placeBuild()` — `const recipe=BUILD_RECIPES[buildMode];if(!recipe)return false;if(!resourcesEnough(recipe.cost)){toast(\`Faltam materiais: ${buildCostText(recipe.cost)}.\`,'warn');updateBuildPanel();return false;}`
- [x] `return` — linha 4456, coluna 185 em `placeBuild()` — `const recipe=BUILD_RECIPES[buildMode];if(!recipe)return false;if(!resourcesEnough(recipe.cost)){toast(\`Faltam materiais: ${buildCostText(recipe.cost)}.\`,'warn');updateBuildPanel();return false;}`
- [x] `for` — linha 4457, coluna 21 em `placeBuild()` — `const before={};for(const[key,value]of Object.entries(recipe.cost)){before[key]=state.inventory[key];state.inventory[key]-=value;}`
- [x] `if` — linha 4460, coluna 222 em `placeBuild()` — `state.buildTombstones=normalizeBuildTombstones(state.buildTombstones).filter(item=>item.id!==data.id);state.builds=mergeBuildCollections(state.builds,[data],state.buildTombstones);const record=spawnBuild(data,true);if(!record)throw new Error('Objeto não foi criado no mundo');`
- [x] `throw` — linha 4460, coluna 233 em `placeBuild()` — `state.buildTombstones=normalizeBuildTombstones(state.buildTombstones).filter(item=>item.id!==data.id);state.builds=mergeBuildCollections(state.builds,[data],state.buildTombstones);const record=spawnBuild(data,true);if(!record)throw new Error('Objeto não foi criado no mundo');`
- [x] `return` — linha 4461, coluna 185 em `placeBuild()` — `addXP(12);evaluateMissions();checkActiveJob();saveState(true).finally(()=>syncCloudProgress(true));toast(\`${recipe.name} colocado e salvo!\`,'good',1900);updateBuildPreview(true);return true;`
- [x] `catch` — linha 4462, coluna 6 em `placeBuild()` — `}catch(error){state.builds=state.builds.filter(b=>b.id!==data.id);for(const[key,value]of Object.entries(before))state.inventory[key]=value;console.error('[OTTHOS] Falha ao construir; recursos restaurados.',error);toast('Não foi possível construir. Seus recursos foram restaurados.','bad');updateBuildPanel();return false;}`
- [x] `for` — linha 4462, coluna 71 em `placeBuild()` — `}catch(error){state.builds=state.builds.filter(b=>b.id!==data.id);for(const[key,value]of Object.entries(before))state.inventory[key]=value;console.error('[OTTHOS] Falha ao construir; recursos restaurados.',error);toast('Não foi possível construir. Seus recursos foram restaurados.','bad');updateBuildPanel();return false;}`
- [x] `return` — linha 4462, coluna 313 em `placeBuild()` — `}catch(error){state.builds=state.builds.filter(b=>b.id!==data.id);for(const[key,value]of Object.entries(before))state.inventory[key]=value;console.error('[OTTHOS] Falha ao construir; recursos restaurados.',error);toast('Não foi possível construir. Seus recursos foram restaurados.','bad');updateBuildPanel();return false;}`
- [x] `if` — linha 4465, coluna 46 em `spawnBuild()` — `const data=normalizeBuildRecord(rawData);if(!data||!worldGroup)return null;const existing=world.builds.find(item=>item.data.id===data.id);if(existing)return existing;`
- [x] `return` — linha 4465, coluna 68 em `spawnBuild()` — `const data=normalizeBuildRecord(rawData);if(!data||!worldGroup)return null;const existing=world.builds.find(item=>item.data.id===data.id);if(existing)return existing;`
- [x] `if` — linha 4465, coluna 143 em `spawnBuild()` — `const data=normalizeBuildRecord(rawData);if(!data||!worldGroup)return null;const existing=world.builds.find(item=>item.data.id===data.id);if(existing)return existing;`
- [x] `return` — linha 4465, coluna 155 em `spawnBuild()` — `const data=normalizeBuildRecord(rawData);if(!data||!worldGroup)return null;const existing=world.builds.find(item=>item.data.id===data.id);if(existing)return existing;`
- [x] `if` — linha 4467, coluna 5 em `spawnBuild()` — `if(data.type==='block'){mesh=box(1.5,1.5,1.5,materials.brick,data.x,baseY+.75,data.z);registerPlatform(data.x,data.z,1.5,1.5,baseY+1.5,{buildId:data.id});registerCollider(data.x,data.z,1.5,1.5,{buildId:data.id});}`
- [x] `else` — linha 4468, coluna 5 em `spawnBuild()` — `else if(data.type==='fence'){mesh=box(2.4,1.05,.22,materials.wood,data.x,baseY+.52,data.z);mesh.rotation.y=rotation;const size=oriented(2.4,.22);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `if` — linha 4468, coluna 10 em `spawnBuild()` — `else if(data.type==='fence'){mesh=box(2.4,1.05,.22,materials.wood,data.x,baseY+.52,data.z);mesh.rotation.y=rotation;const size=oriented(2.4,.22);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `else` — linha 4469, coluna 5 em `spawnBuild()` — `else if(data.type==='wall'){mesh=box(3.0,2.4,.32,materials.stone,data.x,baseY+1.2,data.z);mesh.rotation.y=rotation;const size=oriented(3,.32);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `if` — linha 4469, coluna 10 em `spawnBuild()` — `else if(data.type==='wall'){mesh=box(3.0,2.4,.32,materials.stone,data.x,baseY+1.2,data.z);mesh.rotation.y=rotation;const size=oriented(3,.32);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `else` — linha 4470, coluna 5 em `spawnBuild()` — `else if(data.type==='floor'){mesh=box(3.0,.28,3.0,materials.wood,data.x,baseY+.14,data.z);mesh.rotation.y=rotation;registerPlatform(data.x,data.z,3,3,baseY+.28,{buildId:data.id});}`
- [x] `if` — linha 4470, coluna 10 em `spawnBuild()` — `else if(data.type==='floor'){mesh=box(3.0,.28,3.0,materials.wood,data.x,baseY+.14,data.z);mesh.rotation.y=rotation;registerPlatform(data.x,data.z,3,3,baseY+.28,{buildId:data.id});}`
- [x] `else` — linha 4471, coluna 5 em `spawnBuild()` — `else if(data.type==='bench'){mesh=new THREE.Group();mesh.position.set(data.x,baseY,data.z);mesh.rotation.y=rotation;worldGroup.add(mesh);premiumBox(2.2,.22,.65,materials.fabric,0,.62,0,mesh);premiumBox(2.2,.74,.18,materials.fabric,0,1.02,-.27,mesh);for(const ox of [-.8,.8])premiumBox(.14,.55,.14,materials.metal,ox,.28,0,mesh);const size=oriented(2.3,.85);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `if` — linha 4471, coluna 10 em `spawnBuild()` — `else if(data.type==='bench'){mesh=new THREE.Group();mesh.position.set(data.x,baseY,data.z);mesh.rotation.y=rotation;worldGroup.add(mesh);premiumBox(2.2,.22,.65,materials.fabric,0,.62,0,mesh);premiumBox(2.2,.74,.18,materials.fabric,0,1.02,-.27,mesh);for(const ox of [-.8,.8])premiumBox(.14,.55,.14,materials.metal,ox,.28,0,mesh);const size=oriented(2.3,.85);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `for` — linha 4471, coluna 254 em `spawnBuild()` — `else if(data.type==='bench'){mesh=new THREE.Group();mesh.position.set(data.x,baseY,data.z);mesh.rotation.y=rotation;worldGroup.add(mesh);premiumBox(2.2,.22,.65,materials.fabric,0,.62,0,mesh);premiumBox(2.2,.74,.18,materials.fabric,0,1.02,-.27,mesh);for(const ox of [-.8,.8])premiumBox(.14,.55,.14,materials.metal,ox,.28,0,mesh);const size=oriented(2.3,.85);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `else` — linha 4472, coluna 5 em `spawnBuild()` — `else if(data.type==='planter'){mesh=new THREE.Group();mesh.position.set(data.x,baseY,data.z);mesh.rotation.y=rotation;worldGroup.add(mesh);premiumBox(2.2,.55,.86,materials.brick,0,.28,0,mesh);premiumBox(1.82,.18,.58,0x5f371f,0,.58,0,mesh);for(const p of [[-.65,0xff6fa8],[0,0xffd74a],[.65,0x67d965]]){premiumBox(.09,.5,.09,0x3d8c3f,p[0],.92,0,mesh);premiumBox(.46,.18,.46,p[1],p[0],1.15,0,mesh);}const size=oriented(2.3,.95);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `if` — linha 4472, coluna 10 em `spawnBuild()` — `else if(data.type==='planter'){mesh=new THREE.Group();mesh.position.set(data.x,baseY,data.z);mesh.rotation.y=rotation;worldGroup.add(mesh);premiumBox(2.2,.55,.86,materials.brick,0,.28,0,mesh);premiumBox(1.82,.18,.58,0x5f371f,0,.58,0,mesh);for(const p of [[-.65,0xff6fa8],[0,0xffd74a],[.65,0x67d965]]){premiumBox(.09,.5,.09,0x3d8c3f,p[0],.92,0,mesh);premiumBox(.46,.18,.46,p[1],p[0],1.15,0,mesh);}const size=oriented(2.3,.95);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `for` — linha 4472, coluna 244 em `spawnBuild()` — `else if(data.type==='planter'){mesh=new THREE.Group();mesh.position.set(data.x,baseY,data.z);mesh.rotation.y=rotation;worldGroup.add(mesh);premiumBox(2.2,.55,.86,materials.brick,0,.28,0,mesh);premiumBox(1.82,.18,.58,0x5f371f,0,.58,0,mesh);for(const p of [[-.65,0xff6fa8],[0,0xffd74a],[.65,0x67d965]]){premiumBox(.09,.5,.09,0x3d8c3f,p[0],.92,0,mesh);premiumBox(.46,.18,.46,p[1],p[0],1.15,0,mesh);}const size=oriented(2.3,.95);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `else` — linha 4473, coluna 5 em `spawnBuild()` — `else if(data.type==='lamp'){mesh=new THREE.Group();mesh.position.set(data.x,baseY,data.z);worldGroup.add(mesh);box(.22,2.4,.22,materials.wood,0,1.2,0,mesh);box(.65,.65,.65,0xffdc6a,0,2.65,0,mesh);extras.push(addGlow(data.x,baseY+2.65,data.z,0xffd56a,4));registerCollider(data.x,data.z,.55,.55,{buildId:data.id});}`
- [x] `if` — linha 4473, coluna 10 em `spawnBuild()` — `else if(data.type==='lamp'){mesh=new THREE.Group();mesh.position.set(data.x,baseY,data.z);worldGroup.add(mesh);box(.22,2.4,.22,materials.wood,0,1.2,0,mesh);box(.65,.65,.65,0xffdc6a,0,2.65,0,mesh);extras.push(addGlow(data.x,baseY+2.65,data.z,0xffd56a,4));registerCollider(data.x,data.z,.55,.55,{buildId:data.id});}`
- [x] `else` — linha 4474, coluna 5 em `spawnBuild()` — `else return null;`
- [x] `return` — linha 4474, coluna 10 em `spawnBuild()` — `else return null;`
- [x] `return` — linha 4475, coluna 188 em `spawnBuild()` — `mesh.userData.buildId=data.id;mesh.userData.buildType=data.type;const record={data:{...data,groundY:baseY},mesh,extras,signature:buildRecordSignature(data)};world.builds.push(record);return record;`
- [x] `return` — linha 4477, coluna 39 em `buildRecordSignature()` — `function buildRecordSignature(data){return [data.id,data.type,Number(data.x).toFixed(2),Number(data.z).toFixed(2),Number(data.rotation||0).toFixed(3),Number(data.updatedAt||data.createdAt||0)].join('|');}`
- [x] `if` — linha 4479, coluna 5 em `removeWorldBuildRecord()` — `if(!record)return;worldGroup?.remove(record.mesh);for(const extra of record.extras||[]){worldGroup?.remove(extra);world.glows=world.glows.filter(item=>item!==extra);}world.colliders=world.colliders.filter(c=>c.buildId!==record.data.id);world.platforms=world.platforms.filter(p=>p.buildId!==record.data.id);world.builds=world.builds.filter(item=>item!==record);`
- [x] `return` — linha 4479, coluna 16 em `removeWorldBuildRecord()` — `if(!record)return;worldGroup?.remove(record.mesh);for(const extra of record.extras||[]){worldGroup?.remove(extra);world.glows=world.glows.filter(item=>item!==extra);}world.colliders=world.colliders.filter(c=>c.buildId!==record.data.id);world.platforms=world.platforms.filter(p=>p.buildId!==record.data.id);world.builds=world.builds.filter(item=>item!==record);`
- [x] `for` — linha 4479, coluna 55 em `removeWorldBuildRecord()` — `if(!record)return;worldGroup?.remove(record.mesh);for(const extra of record.extras||[]){worldGroup?.remove(extra);world.glows=world.glows.filter(item=>item!==extra);}world.colliders=world.colliders.filter(c=>c.buildId!==record.data.id);world.platforms=world.platforms.filter(p=>p.buildId!==record.data.id);world.builds=world.builds.filter(item=>item!==record);`
- [x] `if` — linha 4482, coluna 5 em `reconcileWorldBuilds()` — `if(!worldGroup||!world?.builds)return false;state.buildTombstones=normalizeBuildTombstones(state.buildTombstones);state.builds=applyBuildTombstones(state.builds,state.buildTombstones);const wanted=new Map(state.builds.map(data=>[data.id,data]));`
- [x] `return` — linha 4482, coluna 36 em `reconcileWorldBuilds()` — `if(!worldGroup||!world?.builds)return false;state.buildTombstones=normalizeBuildTombstones(state.buildTombstones);state.builds=applyBuildTombstones(state.builds,state.buildTombstones);const wanted=new Map(state.builds.map(data=>[data.id,data]));`
- [x] `for` — linha 4483, coluna 5 em `reconcileWorldBuilds()` — `for(const record of [...world.builds]){const data=wanted.get(record.data.id);if(!data||record.signature!==buildRecordSignature(data)){removeWorldBuildRecord(record);}}`
- [x] `if` — linha 4483, coluna 82 em `reconcileWorldBuilds()` — `for(const record of [...world.builds]){const data=wanted.get(record.data.id);if(!data||record.signature!==buildRecordSignature(data)){removeWorldBuildRecord(record);}}`
- [x] `for` — linha 4484, coluna 5 em `reconcileWorldBuilds()` — `for(const data of state.builds)if(!world.builds.some(record=>record.data.id===data.id))spawnBuild(data,false);return true;`
- [x] `if` — linha 4484, coluna 36 em `reconcileWorldBuilds()` — `for(const data of state.builds)if(!world.builds.some(record=>record.data.id===data.id))spawnBuild(data,false);return true;`
- [x] `return` — linha 4484, coluna 115 em `reconcileWorldBuilds()` — `for(const data of state.builds)if(!world.builds.some(record=>record.data.id===data.id))spawnBuild(data,false);return true;`
- [x] `if` — linha 4487, coluna 169 em `reconcileWorldBuilds()` — `const nearest=world.builds.filter(b=>buildOwnedByPlayer(b.data)&&distance2D(player,b.data)<3.4).sort((a,b)=>distance2D(player,a.data)-distance2D(player,b.data))[0];if(!nearest){toast('Nenhuma construção sua por perto.','warn');return false;}`
- [x] `return` — linha 4487, coluna 232 em `reconcileWorldBuilds()` — `const nearest=world.builds.filter(b=>buildOwnedByPlayer(b.data)&&distance2D(player,b.data)<3.4).sort((a,b)=>distance2D(player,a.data)-distance2D(player,b.data))[0];if(!nearest){toast('Nenhuma construção sua por perto.','warn');return false;}`
- [x] `if` — linha 4488, coluna 223 em `reconcileWorldBuilds()` — `const recipe=BUILD_RECIPES[nearest.data.type],ok=await confirmModal('Remover construção',\`Remover ${recipe?.name||'esta construção'}? A ação será salva e não afetará objetos de outros jogadores.\`,'Remover','Cancelar');if(!ok)return false;`
- [x] `return` — linha 4488, coluna 230 em `reconcileWorldBuilds()` — `const recipe=BUILD_RECIPES[nearest.data.type],ok=await confirmModal('Remover construção',\`Remover ${recipe?.name||'esta construção'}? A ação será salva e não afetará objetos de outros jogadores.\`,'Remover','Cancelar');if(!ok)return false;`
- [x] `return` — linha 4489, coluna 341 em `reconcileWorldBuilds()` — `const removedAt=Date.now();state.buildTombstones=mergeBuildTombstones(state.buildTombstones,[{id:nearest.data.id,removedAt}]);state.builds=applyBuildTombstones(state.builds,state.buildTombstones);removeWorldBuildRecord(nearest);saveState(true).finally(()=>syncCloudProgress(true));toast('Construção removida e alteração salva.','good');return true;`

## `25-render-init-resize-position-collision.js`

- [x] `if` — linha 4496, coluna 5 em `initThree()` — `if(!window.THREE){openModal('Erro ao carregar 3D','<p>A biblioteca Three.js não carregou. Verifique a internet e recarregue a página.</p>');return false;}`
- [x] `return` — linha 4496, coluna 145 em `initThree()` — `if(!window.THREE){openModal('Erro ao carregar 3D','<p>A biblioteca Three.js não carregou. Verifique a internet e recarregue a página.</p>');return false;}`
- [x] `for` — linha 4502, coluna 261 em `initThree()` — `createPlayerModel();playerModel.position.y=playerModel.userData.baseY;applyAvatarCustomization();buildWorld();restoreActiveJobRuntime();reconcileCloudHouses();lockStableSceneVisibility();freezeWorldFrustumCulling();restorePosition();initLocalMultiplayer();for(const [remoteUid,data] of remotePresence)remotePlayerEvent({uid:remoteUid,...data});applyQuality();applyAdaptiveRenderSettings(true);resize(true);return true;`
- [x] `return` — linha 4502, coluna 411 em `initThree()` — `createPlayerModel();playerModel.position.y=playerModel.userData.baseY;applyAvatarCustomization();buildWorld();restoreActiveJobRuntime();reconcileCloudHouses();lockStableSceneVisibility();freezeWorldFrustumCulling();restorePosition();initLocalMultiplayer();for(const [remoteUid,data] of remotePresence)remotePlayerEvent({uid:remoteUid,...data});applyQuality();applyAdaptiveRenderSettings(true);resize(true);return true;`
- [x] `if` — linha 4504, coluna 28 em `applyQuality()` — `function applyQuality(){ if(!renderer)return;applyAdaptiveRenderSettings(); }`
- [x] `return` — linha 4504, coluna 41 em `applyQuality()` — `function applyQuality(){ if(!renderer)return;applyAdaptiveRenderSettings(); }`
- [x] `return` — linha 4516, coluna 5 em `viewportMetrics()` — `return{w:Math.max(280,w),h:Math.max(220,h),landscape,offsetTop,offsetLeft,keyboard};`
- [x] `if` — linha 4521, coluna 5 em `resize()` — `if(renderer&&camera&&(sizeChanged||force||orientationChanged)){camera.aspect=w/h;camera.updateProjectionMatrix();renderer.setPixelRatio(Math.min(devicePixelRatio||1,targetDpr()));renderer.setSize(w,h,false);perf.lastRenderW=w;perf.lastRenderH=h;}`
- [x] `if` — linha 4523, coluna 5 em `resize()` — `if(orientationChanged){clearMovementInputs();state.ui.quickOpen=false;state.ui.skillsOpen=false;state.ui.needsOpen=false;state.ui.missionOpen=false;els.game?.classList.remove('needs-expanded');els.missionCard?.classList.remove('expanded');document.body.classList.add('ui-orientation-changing');setTimeout(()=>document.body.classList.remove('ui-orientation-changing'),360);}`
- [x] `if` — linha 4535, coluna 5 em `ensureViewportCoherence()` — `if(classLandscape!==metrics.landscape||bufferMismatch){window.OTTHI_VIEWPORT?.measure?.();resize(true);return true;}return false;`
- [x] `return` — linha 4535, coluna 108 em `ensureViewportCoherence()` — `if(classLandscape!==metrics.landscape||bufferMismatch){window.OTTHI_VIEWPORT?.measure?.();resize(true);return true;}return false;`
- [x] `return` — linha 4535, coluna 121 em `ensureViewportCoherence()` — `if(classLandscape!==metrics.landscape||bufferMismatch){window.OTTHI_VIEWPORT?.measure?.();resize(true);return true;}return false;`
- [x] `if` — linha 4540, coluna 415 em `refreshOrientationLayout()` — `window.addEventListener('resize',()=>scheduleStableResize(50,true),{passive:true});window.addEventListener('orientationchange',refreshOrientationLayout,{passive:true});window.addEventListener('pageshow',refreshOrientationLayout,{passive:true});window.addEventListener('focus',()=>scheduleStableResize(30,true),{passive:true});document.addEventListener('fullscreenchange',refreshOrientationLayout,{passive:true});if(window.visualViewport){window.visualViewport.addEventListener('resize',()=>scheduleStableResize(50,true),{passive:true});window.visualViewport.addEventListener('scroll',()=>scheduleStableResize(80,false),{passive:true});}screen.orientation?.addEventListener?.('change',refreshOrientationLayout);if(window.ResizeObserver&&els.stage)new ResizeObserver(()=>scheduleStableResize(40,true)).observe(els.stage);document.addEventListener('visibilitychange',()=>{if(!document.hidden)refreshOrientationLayout();});`
- [x] `if` — linha 4540, coluna 713 em `refreshOrientationLayout()` — `window.addEventListener('resize',()=>scheduleStableResize(50,true),{passive:true});window.addEventListener('orientationchange',refreshOrientationLayout,{passive:true});window.addEventListener('pageshow',refreshOrientationLayout,{passive:true});window.addEventListener('focus',()=>scheduleStableResize(30,true),{passive:true});document.addEventListener('fullscreenchange',refreshOrientationLayout,{passive:true});if(window.visualViewport){window.visualViewport.addEventListener('resize',()=>scheduleStableResize(50,true),{passive:true});window.visualViewport.addEventListener('scroll',()=>scheduleStableResize(80,false),{passive:true});}screen.orientation?.addEventListener?.('change',refreshOrientationLayout);if(window.ResizeObserver&&els.stage)new ResizeObserver(()=>scheduleStableResize(40,true)).observe(els.stage);document.addEventListener('visibilitychange',()=>{if(!document.hidden)refreshOrientationLayout();});`
- [x] `if` — linha 4540, coluna 872 em `refreshOrientationLayout()` — `window.addEventListener('resize',()=>scheduleStableResize(50,true),{passive:true});window.addEventListener('orientationchange',refreshOrientationLayout,{passive:true});window.addEventListener('pageshow',refreshOrientationLayout,{passive:true});window.addEventListener('focus',()=>scheduleStableResize(30,true),{passive:true});document.addEventListener('fullscreenchange',refreshOrientationLayout,{passive:true});if(window.visualViewport){window.visualViewport.addEventListener('resize',()=>scheduleStableResize(50,true),{passive:true});window.visualViewport.addEventListener('scroll',()=>scheduleStableResize(80,false),{passive:true});}screen.orientation?.addEventListener?.('change',refreshOrientationLayout);if(window.ResizeObserver&&els.stage)new ResizeObserver(()=>scheduleStableResize(40,true)).observe(els.stage);document.addEventListener('visibilitychange',()=>{if(!document.hidden)refreshOrientationLayout();});`
- [x] `if` — linha 4545, coluna 5 em `restorePosition()` — `if(Math.abs(x)>110||Math.abs(z)>110||isInsideLakeNavigable(x,z)){x=0;z=8;state.boats.activeBoatId='';state.boats.passengerOf='';}`
- [x] `if` — linha 4549, coluna 5 em `returnHome()` — `if(player.boating){player.x=-24.7;player.z=52;exitBoat(true);}`
- [x] `if` — linha 4550, coluna 5 em `returnHome()` — `if(player.vehicle)exitVehicle(true);`
- [x] `if` — linha 4551, coluna 5 em `returnHome()` — `if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus&&busAtStop(bus))exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});else{player.transit.mode='';player.transit.busId='';}}`
- [x] `if` — linha 4551, coluna 96 em `returnHome()` — `if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus&&busAtStop(bus))exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});else{player.transit.mode='';player.transit.busId='';}}`
- [x] `else` — linha 4551, coluna 188 em `returnHome()` — `if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus&&busAtStop(bus))exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});else{player.transit.mode='';player.transit.busId='';}}`
- [x] `if` — linha 4552, coluna 5 em `returnHome()` — `if(player.transit.mode==='metro'){player.transit.mode='';player.transit.metroUntil=0;if(metroOverlay){metroOverlay.hidden=true;metroOverlay.classList.remove('travelling','arriving');}if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}`
- [x] `if` — linha 4552, coluna 90 em `returnHome()` — `if(player.transit.mode==='metro'){player.transit.mode='';player.transit.metroUntil=0;if(metroOverlay){metroOverlay.hidden=true;metroOverlay.classList.remove('travelling','arriving');}if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}`
- [x] `if` — linha 4552, coluna 188 em `returnHome()` — `if(player.transit.mode==='metro'){player.transit.mode='';player.transit.metroUntil=0;if(metroOverlay){metroOverlay.hidden=true;metroOverlay.classList.remove('travelling','arriving');}if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}`
- [x] `if` — linha 4552, coluna 228 em `returnHome()` — `if(player.transit.mode==='metro'){player.transit.mode='';player.transit.metroUntil=0;if(metroOverlay){metroOverlay.hidden=true;metroOverlay.classList.remove('travelling','arriving');}if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}`
- [x] `if` — linha 4552, coluna 268 em `returnHome()` — `if(player.transit.mode==='metro'){player.transit.mode='';player.transit.metroUntil=0;if(metroOverlay){metroOverlay.hidden=true;metroOverlay.classList.remove('travelling','arriving');}if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}`
- [x] `if` — linha 4553, coluna 5 em `returnHome()` — `if(currentHouse)exitHouse();const safe=safePointNear(0,23,{ignoreTraffic:true,allowWater:false});player.x=safe.x;player.z=safe.z;player.y=safe.y;player.vx=player.vz=player.vy=0;player.grounded=true;cameraYaw=Math.PI;rememberSafePlayerPosition(true);auditPlayerMode('return-home');toast('Você voltou para casa.','good');savePlayerPosition(true);`
- [x] `return` — linha 4553, coluna 271 em `returnHome()` — `if(currentHouse)exitHouse();const safe=safePointNear(0,23,{ignoreTraffic:true,allowWater:false});player.x=safe.x;player.z=safe.z;player.y=safe.y;player.vx=player.vz=player.vy=0;player.grounded=true;cameraYaw=Math.PI;rememberSafePlayerPosition(true);auditPlayerMode('return-home');toast('Você voltou para casa.','good');savePlayerPosition(true);`
- [x] `if` — linha 4556, coluna 5 em `savePlayerPosition()` — `if(player.boating&&player.boat)state.boats.lastPosition={x:+player.x.toFixed(2),z:+player.z.toFixed(2),heading:+player.boat.heading.toFixed(3)};`
- [x] `if` — linha 4559, coluna 5 em `savePlayerPosition()` — `if(Number.isFinite(x)&&Number.isFinite(z)&&!isInsideLakeNavigable(x,z))state.position={x:+x.toFixed(2),y:+Number(y||0).toFixed(2),z:+z.toFixed(2),yaw:+Number(yaw||0).toFixed(3)};`
- [x] `if` — linha 4562, coluna 43 em `savePlayerPosition()` — `const autoSaveInterval=setInterval(()=>{if(running){savePlayerPosition(true);}},8000);`
- [x] `if` — linha 4563, coluna 43 em `savePlayerPosition()` — `window.addEventListener('pagehide',()=>{if(running)savePlayerPosition(true);else commitState();});`
- [x] `else` — linha 4563, coluna 79 em `savePlayerPosition()` — `window.addEventListener('pagehide',()=>{if(running)savePlayerPosition(true);else commitState();});`
- [x] `if` — linha 4564, coluna 47 em `savePlayerPosition()` — `window.addEventListener('beforeunload',()=>{if(running)savePlayerPosition(true);else commitState();});`
- [x] `else` — linha 4564, coluna 83 em `savePlayerPosition()` — `window.addEventListener('beforeunload',()=>{if(running)savePlayerPosition(true);else commitState();});`
- [x] `if` — linha 4565, coluna 53 em `savePlayerPosition()` — `document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='hidden'){if(running)savePlayerPosition(true);else commitState();}});`
- [x] `if` — linha 4565, coluna 93 em `savePlayerPosition()` — `document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='hidden'){if(running)savePlayerPosition(true);else commitState();}});`
- [x] `else` — linha 4565, coluna 129 em `savePlayerPosition()` — `document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='hidden'){if(running)savePlayerPosition(true);else commitState();}});`
- [x] `for` — linha 4568, coluna 15 em `groundHeightAt()` — `let top=0;for(const p of world.platforms){if(p.bridgePart!==undefined&&!state.flags.bridgeFixed&&p.bridgePart%2===1)continue;if(Math.abs(x-p.x)<=p.w/2+.35&&Math.abs(z-p.z)<=p.d/2+.35&&p.top>top&&player.y>=p.top-.75)top=p.top;}return top;`
- [x] `if` — linha 4568, coluna 47 em `groundHeightAt()` — `let top=0;for(const p of world.platforms){if(p.bridgePart!==undefined&&!state.flags.bridgeFixed&&p.bridgePart%2===1)continue;if(Math.abs(x-p.x)<=p.w/2+.35&&Math.abs(z-p.z)<=p.d/2+.35&&p.top>top&&player.y>=p.top-.75)top=p.top;}return top;`
- [x] `if` — linha 4568, coluna 130 em `groundHeightAt()` — `let top=0;for(const p of world.platforms){if(p.bridgePart!==undefined&&!state.flags.bridgeFixed&&p.bridgePart%2===1)continue;if(Math.abs(x-p.x)<=p.w/2+.35&&Math.abs(z-p.z)<=p.d/2+.35&&p.top>top&&player.y>=p.top-.75)top=p.top;}return top;`
- [x] `return` — linha 4568, coluna 231 em `groundHeightAt()` — `let top=0;for(const p of world.platforms){if(p.bridgePart!==undefined&&!state.flags.bridgeFixed&&p.bridgePart%2===1)continue;if(Math.abs(x-p.x)<=p.w/2+.35&&Math.abs(z-p.z)<=p.d/2+.35&&p.top>top&&player.y>=p.top-.75)top=p.top;}return top;`
- [x] `if` — linha 4571, coluna 5 em `positionBlockedForPlayer()` — `if(!Number.isFinite(x)||!Number.isFinite(z)||Math.abs(x)>116||Math.abs(z)>116)return true;`
- [x] `return` — linha 4571, coluna 83 em `positionBlockedForPlayer()` — `if(!Number.isFinite(x)||!Number.isFinite(z)||Math.abs(x)>116||Math.abs(z)>116)return true;`
- [x] `if` — linha 4572, coluna 5 em `positionBlockedForPlayer()` — `if(!options.allowWater&&waterAt(x,z)&&groundHeightAt(x,z)<=.24)return true;`
- [x] `return` — linha 4572, coluna 68 em `positionBlockedForPlayer()` — `if(!options.allowWater&&waterAt(x,z)&&groundHeightAt(x,z)<=.24)return true;`
- [x] `for` — linha 4573, coluna 5 em `positionBlockedForPlayer()` — `for(const c of world.colliders){`
- [x] `if` — linha 4574, coluna 7 em `positionBlockedForPlayer()` — `if(options.ignoreHouseId&&c.houseId===options.ignoreHouseId)continue;`
- [x] `if` — linha 4575, coluna 7 em `positionBlockedForPlayer()` — `if(c.houseId&&currentHouse&&c.houseId===currentHouse.id)continue;`
- [x] `if` — linha 4576, coluna 7 em `positionBlockedForPlayer()` — `if(Math.abs(x-c.x)<=c.w/2+radius&&Math.abs(z-c.z)<=c.d/2+radius)return true;`
- [x] `return` — linha 4576, coluna 71 em `positionBlockedForPlayer()` — `if(Math.abs(x-c.x)<=c.w/2+radius&&Math.abs(z-c.z)<=c.d/2+radius)return true;`
- [x] `if` — linha 4578, coluna 5 em `positionBlockedForPlayer()` — `if(!options.ignoreParkedVehicles){for(const v of world.vehicles||[]){if(!v.group?.visible||v===options.ignoreActor)continue;if(Math.hypot(x-v.group.position.x,z-v.group.position.z)<1.18+radius)return true;}}`
- [x] `for` — linha 4578, coluna 39 em `positionBlockedForPlayer()` — `if(!options.ignoreParkedVehicles){for(const v of world.vehicles||[]){if(!v.group?.visible||v===options.ignoreActor)continue;if(Math.hypot(x-v.group.position.x,z-v.group.position.z)<1.18+radius)return true;}}`
- [x] `if` — linha 4578, coluna 74 em `positionBlockedForPlayer()` — `if(!options.ignoreParkedVehicles){for(const v of world.vehicles||[]){if(!v.group?.visible||v===options.ignoreActor)continue;if(Math.hypot(x-v.group.position.x,z-v.group.position.z)<1.18+radius)return true;}}`
- [x] `if` — linha 4578, coluna 129 em `positionBlockedForPlayer()` — `if(!options.ignoreParkedVehicles){for(const v of world.vehicles||[]){if(!v.group?.visible||v===options.ignoreActor)continue;if(Math.hypot(x-v.group.position.x,z-v.group.position.z)<1.18+radius)return true;}}`
- [x] `return` — linha 4578, coluna 198 em `positionBlockedForPlayer()` — `if(!options.ignoreParkedVehicles){for(const v of world.vehicles||[]){if(!v.group?.visible||v===options.ignoreActor)continue;if(Math.hypot(x-v.group.position.x,z-v.group.position.z)<1.18+radius)return true;}}`
- [x] `if` — linha 4579, coluna 5 em `positionBlockedForPlayer()` — `if(!options.ignoreTraffic){`
- [x] `for` — linha 4580, coluna 7 em `positionBlockedForPlayer()` — `for(const actor of trafficActorList()){`
- [x] `if` — linha 4581, coluna 9 em `positionBlockedForPlayer()` — `if(options.ignoreActor&&actor.ref===options.ignoreActor)continue;`
- [x] `if` — linha 4582, coluna 9 em `positionBlockedForPlayer()` — `if(Math.hypot(x-actor.group.position.x,z-actor.group.position.z)<(actor.radius||1.4)+radius)return true;`
- [x] `return` — linha 4582, coluna 101 em `positionBlockedForPlayer()` — `if(Math.hypot(x-actor.group.position.x,z-actor.group.position.z)<(actor.radius||1.4)+radius)return true;`
- [x] `return` — linha 4585, coluna 5 em `positionBlockedForPlayer()` — `return false;`
- [x] `for` — linha 4589, coluna 5 em `safePointNear()` — `for(const distance of distances)for(const offset of angles){const angle=heading+offset,cx=clamp(x+Math.sin(angle)*distance,-115,115),cz=clamp(z+Math.cos(angle)*distance,-115,115);if(!positionBlockedForPlayer(cx,cz,radius,options))return{x:cx,z:cz,y:groundHeightAt(cx,cz)};}`
- [x] `for` — linha 4589, coluna 37 em `safePointNear()` — `for(const distance of distances)for(const offset of angles){const angle=heading+offset,cx=clamp(x+Math.sin(angle)*distance,-115,115),cz=clamp(z+Math.cos(angle)*distance,-115,115);if(!positionBlockedForPlayer(cx,cz,radius,options))return{x:cx,z:cz,y:groundHeightAt(cx,cz)};}`
- [x] `if` — linha 4589, coluna 184 em `safePointNear()` — `for(const distance of distances)for(const offset of angles){const angle=heading+offset,cx=clamp(x+Math.sin(angle)*distance,-115,115),cz=clamp(z+Math.cos(angle)*distance,-115,115);if(!positionBlockedForPlayer(cx,cz,radius,options))return{x:cx,z:cz,y:groundHeightAt(cx,cz)};}`
- [x] `return` — linha 4589, coluna 235 em `safePointNear()` — `for(const distance of distances)for(const offset of angles){const angle=heading+offset,cx=clamp(x+Math.sin(angle)*distance,-115,115),cz=clamp(z+Math.cos(angle)*distance,-115,115);if(!positionBlockedForPlayer(cx,cz,radius,options))return{x:cx,z:cz,y:groundHeightAt(cx,cz)};}`
- [x] `return` — linha 4590, coluna 114 em `safePointNear()` — `const fallback={x:clamp(Number(player.lastSafeX)||0,-115,115),z:clamp(Number(player.lastSafeZ)||8,-115,115)};return{x:fallback.x,z:fallback.z,y:groundHeightAt(fallback.x,fallback.z)};`
- [x] `if` — linha 4593, coluna 33 em `rememberSafePlayerPosition()` — `const now=performance.now();if(!force&&now-Number(player.lastSafeAt||0)<450)return false;`
- [x] `return` — linha 4593, coluna 81 em `rememberSafePlayerPosition()` — `const now=performance.now();if(!force&&now-Number(player.lastSafeAt||0)<450)return false;`
- [x] `if` — linha 4594, coluna 5 em `rememberSafePlayerPosition()` — `if(!player.grounded||player.vehicle||player.boating||player.transit.mode||positionBlockedForPlayer(player.x,player.z,.38,{ignoreTraffic:true}))return false;`
- [x] `return` — linha 4594, coluna 148 em `rememberSafePlayerPosition()` — `if(!player.grounded||player.vehicle||player.boating||player.transit.mode||positionBlockedForPlayer(player.x,player.z,.38,{ignoreTraffic:true}))return false;`
- [x] `return` — linha 4595, coluna 127 em `rememberSafePlayerPosition()` — `player.lastSafeX=player.x;player.lastSafeY=player.y;player.lastSafeZ=player.z;player.lastSafeAt=now;player.invalidSince=0;return true;`
- [x] `if` — linha 4600, coluna 5 em `recoverPlayerIfInvalid()` — `if(!impossible&&!penetrated){player.invalidSince=0;rememberSafePlayerPosition();return false;}`
- [x] `return` — linha 4600, coluna 85 em `recoverPlayerIfInvalid()` — `if(!impossible&&!penetrated){player.invalidSince=0;rememberSafePlayerPosition();return false;}`
- [x] `if` — linha 4601, coluna 64 em `recoverPlayerIfInvalid()` — `player.invalidSince=player.invalidSince||performance.now();if(!impossible&&performance.now()-player.invalidSince<1100)return false;`
- [x] `return` — linha 4601, coluna 123 em `recoverPlayerIfInvalid()` — `player.invalidSince=player.invalidSince||performance.now();if(!impossible&&performance.now()-player.invalidSince<1100)return false;`
- [x] `return` — linha 4602, coluna 331 em `recoverPlayerIfInvalid()` — `const safe=safePointNear(Number(player.lastSafeX)||0,Number(player.lastSafeZ)||8,{ignoreTraffic:true,allowWater:false});player.x=safe.x;player.z=safe.z;player.y=safe.y;player.vx=player.vy=player.vz=0;player.grounded=true;player.invalidSince=0;console.warn('[OTTHOS] Posição impossível recuperada para o último ponto seguro.');return true;`
- [x] `return` — linha 4606, coluna 5 em `safeVehicleExitPoint()` — `return safePointNear(x,z,{heading,radius:.46,ignoreActor:vehicleRef,angles:[Math.PI/2,-Math.PI/2,Math.PI,0],distances:[2.25,2.9,3.6,4.4]});`
- [x] `if` — linha 4612, coluna 5 em `vehicleHitsCollider()` — `if(world.colliders.some(c=>!(c.houseId&&currentHouse&&c.houseId===currentHouse.id)&&hitBox(c)))return true;`
- [x] `return` — linha 4612, coluna 100 em `vehicleHitsCollider()` — `if(world.colliders.some(c=>!(c.houseId&&currentHouse&&c.houseId===currentHouse.id)&&hitBox(c)))return true;`
- [x] `for` — linha 4613, coluna 5 em `vehicleHitsCollider()` — `for(const v of world.vehicles||[]){if(v===active||!v.group?.visible)continue;if(Math.hypot(x-v.group.position.x,z-v.group.position.z)<2.05)return true;}`
- [x] `if` — linha 4613, coluna 40 em `vehicleHitsCollider()` — `for(const v of world.vehicles||[]){if(v===active||!v.group?.visible)continue;if(Math.hypot(x-v.group.position.x,z-v.group.position.z)<2.05)return true;}`
- [x] `if` — linha 4613, coluna 82 em `vehicleHitsCollider()` — `for(const v of world.vehicles||[]){if(v===active||!v.group?.visible)continue;if(Math.hypot(x-v.group.position.x,z-v.group.position.z)<2.05)return true;}`
- [x] `return` — linha 4613, coluna 144 em `vehicleHitsCollider()` — `for(const v of world.vehicles||[]){if(v===active||!v.group?.visible)continue;if(Math.hypot(x-v.group.position.x,z-v.group.position.z)<2.05)return true;}`
- [x] `for` — linha 4614, coluna 5 em `vehicleHitsCollider()` — `for(const actor of trafficActorList()){if(actor.ref===active)continue;if(Math.hypot(x-actor.group.position.x,z-actor.group.position.z)<(actor.radius||1.5)+1.0)return true;}`
- [x] `if` — linha 4614, coluna 44 em `vehicleHitsCollider()` — `for(const actor of trafficActorList()){if(actor.ref===active)continue;if(Math.hypot(x-actor.group.position.x,z-actor.group.position.z)<(actor.radius||1.5)+1.0)return true;}`
- [x] `if` — linha 4614, coluna 75 em `vehicleHitsCollider()` — `for(const actor of trafficActorList()){if(actor.ref===active)continue;if(Math.hypot(x-actor.group.position.x,z-actor.group.position.z)<(actor.radius||1.5)+1.0)return true;}`
- [x] `return` — linha 4614, coluna 164 em `vehicleHitsCollider()` — `for(const actor of trafficActorList()){if(actor.ref===active)continue;if(Math.hypot(x-actor.group.position.x,z-actor.group.position.z)<(actor.radius||1.5)+1.0)return true;}`
- [x] `return` — linha 4615, coluna 5 em `vehicleHitsCollider()` — `return false;`
- [x] `if` — linha 4619, coluna 31 em `registerVehicleImpact()` — `const t=performance.now();if(!registerVehicleImpact._cool||t>registerVehicleImpact._cool){registerVehicleImpact._cool=t+240;vibrate([18,35,18]);beep(135,65,'square');toast('Cuidado com a batida!','warn',900);}`
- [x] `if` — linha 4622, coluna 5 em `resolveCollisions()` — `if(player.vehicle){`
- [x] `if` — linha 4623, coluna 7 em `resolveCollisions()` — `if(vehicleHitsCollider(player.x,player.z)){player.x=prevX;player.z=prevZ;registerVehicleImpact();}`
- [x] `return` — linha 4624, coluna 7 em `resolveCollisions()` — `return;`
- [x] `for` — linha 4627, coluna 5 em `resolveCollisions()` — `for(const c of world.colliders){`
- [x] `if` — linha 4628, coluna 7 em `resolveCollisions()` — `if(c.houseId&&currentHouse&&c.houseId===currentHouse.id)continue;`
- [x] `if` — linha 4629, coluna 7 em `resolveCollisions()` — `if(Math.abs(player.x-c.x)>c.w/2+radius||Math.abs(player.z-c.z)>c.d/2+radius)continue;`
- [x] `if` — linha 4631, coluna 7 em `resolveCollisions()` — `if(fromLeft)player.x=c.x-c.w/2-radius;else if(fromRight)player.x=c.x+c.w/2+radius;else if(fromTop)player.z=c.z-c.d/2-radius;else if(fromBottom)player.z=c.z+c.d/2+radius;else{player.x=prevX;player.z=prevZ;}`
- [x] `else` — linha 4631, coluna 45 em `resolveCollisions()` — `if(fromLeft)player.x=c.x-c.w/2-radius;else if(fromRight)player.x=c.x+c.w/2+radius;else if(fromTop)player.z=c.z-c.d/2-radius;else if(fromBottom)player.z=c.z+c.d/2+radius;else{player.x=prevX;player.z=prevZ;}`
- [x] `if` — linha 4631, coluna 50 em `resolveCollisions()` — `if(fromLeft)player.x=c.x-c.w/2-radius;else if(fromRight)player.x=c.x+c.w/2+radius;else if(fromTop)player.z=c.z-c.d/2-radius;else if(fromBottom)player.z=c.z+c.d/2+radius;else{player.x=prevX;player.z=prevZ;}`
- [x] `else` — linha 4631, coluna 89 em `resolveCollisions()` — `if(fromLeft)player.x=c.x-c.w/2-radius;else if(fromRight)player.x=c.x+c.w/2+radius;else if(fromTop)player.z=c.z-c.d/2-radius;else if(fromBottom)player.z=c.z+c.d/2+radius;else{player.x=prevX;player.z=prevZ;}`
- [x] `if` — linha 4631, coluna 94 em `resolveCollisions()` — `if(fromLeft)player.x=c.x-c.w/2-radius;else if(fromRight)player.x=c.x+c.w/2+radius;else if(fromTop)player.z=c.z-c.d/2-radius;else if(fromBottom)player.z=c.z+c.d/2+radius;else{player.x=prevX;player.z=prevZ;}`
- [x] `else` — linha 4631, coluna 131 em `resolveCollisions()` — `if(fromLeft)player.x=c.x-c.w/2-radius;else if(fromRight)player.x=c.x+c.w/2+radius;else if(fromTop)player.z=c.z-c.d/2-radius;else if(fromBottom)player.z=c.z+c.d/2+radius;else{player.x=prevX;player.z=prevZ;}`
- [x] `if` — linha 4631, coluna 136 em `resolveCollisions()` — `if(fromLeft)player.x=c.x-c.w/2-radius;else if(fromRight)player.x=c.x+c.w/2+radius;else if(fromTop)player.z=c.z-c.d/2-radius;else if(fromBottom)player.z=c.z+c.d/2+radius;else{player.x=prevX;player.z=prevZ;}`
- [x] `else` — linha 4631, coluna 176 em `resolveCollisions()` — `if(fromLeft)player.x=c.x-c.w/2-radius;else if(fromRight)player.x=c.x+c.w/2+radius;else if(fromTop)player.z=c.z-c.d/2-radius;else if(fromBottom)player.z=c.z+c.d/2+radius;else{player.x=prevX;player.z=prevZ;}`
- [x] `if` — linha 4633, coluna 5 em `resolveCollisions()` — `if(currentHouse){const bounds=currentHouse.interiorBounds||{x:4.0,z:3.02};player.x=clamp(player.x,currentHouse.x-bounds.x,currentHouse.x+bounds.x);player.z=clamp(player.z,currentHouse.z-bounds.z,currentHouse.z+bounds.z);}`

## `26-input-player-physics.js`

- [x] `if` — linha 4645, coluna 5 em `resolveMovementInput()` — `if(input.virtualActive){sourceX=input.virtualX;sourceZ=input.virtualZ;}`
- [x] `else` — linha 4646, coluna 5 em `resolveMovementInput()` — `else if(Math.abs(keyboardX)+Math.abs(keyboardZ)>0){sourceX=keyboardX;sourceZ=keyboardZ;}`
- [x] `if` — linha 4646, coluna 10 em `resolveMovementInput()` — `else if(Math.abs(keyboardX)+Math.abs(keyboardZ)>0){sourceX=keyboardX;sourceZ=keyboardZ;}`
- [x] `else` — linha 4647, coluna 5 em `resolveMovementInput()` — `else if(input.joyId!==null||Math.abs(input.joyX)+Math.abs(input.joyZ)>.02){sourceX=input.joyX;sourceZ=input.joyZ;}`
- [x] `if` — linha 4647, coluna 10 em `resolveMovementInput()` — `else if(input.joyId!==null||Math.abs(input.joyX)+Math.abs(input.joyZ)>.02){sourceX=input.joyX;sourceZ=input.joyZ;}`
- [x] `else` — linha 4648, coluna 5 em `resolveMovementInput()` — `else if(input.gamepadActive){sourceX=input.gamepadX;sourceZ=input.gamepadZ;}`
- [x] `if` — linha 4648, coluna 10 em `resolveMovementInput()` — `else if(input.gamepadActive){sourceX=input.gamepadX;sourceZ=input.gamepadZ;}`
- [x] `return` — linha 4652, coluna 5 em `resolveMovementInput()` — `return {x:input.targetX,z:input.targetZ,mode:intent.mode,disabled:intent.disabled};`
- [x] `return` — linha 4654, coluna 30 em `sprintRequested()` — `function sprintRequested(){return !!(input.touchSprint||input.gamepadSprint||input.keys.has('ShiftLeft')||input.keys.has('ShiftRight'));}`
- [x] `if` — linha 4656, coluna 5 em `updateRunUI()` — `if(!els.runBtn)return;if(typeof mobilityDriverActive==='function'&&mobilityDriverActive()){updateMobilityControlLabels();return;}const active=sprintRequested();els.runBtn.classList.toggle('active',active);const icon=$('b',els.runBtn),label=$('span',els.runBtn);if(icon)icon.textContent='🏃';if(label)label.textContent='Correr';`
- [x] `return` — linha 4656, coluna 20 em `updateRunUI()` — `if(!els.runBtn)return;if(typeof mobilityDriverActive==='function'&&mobilityDriverActive()){updateMobilityControlLabels();return;}const active=sprintRequested();els.runBtn.classList.toggle('active',active);const icon=$('b',els.runBtn),label=$('span',els.runBtn);if(icon)icon.textContent='🏃';if(label)label.textContent='Correr';`
- [x] `if` — linha 4656, coluna 27 em `updateRunUI()` — `if(!els.runBtn)return;if(typeof mobilityDriverActive==='function'&&mobilityDriverActive()){updateMobilityControlLabels();return;}const active=sprintRequested();els.runBtn.classList.toggle('active',active);const icon=$('b',els.runBtn),label=$('span',els.runBtn);if(icon)icon.textContent='🏃';if(label)label.textContent='Correr';`
- [x] `return` — linha 4656, coluna 126 em `updateRunUI()` — `if(!els.runBtn)return;if(typeof mobilityDriverActive==='function'&&mobilityDriverActive()){updateMobilityControlLabels();return;}const active=sprintRequested();els.runBtn.classList.toggle('active',active);const icon=$('b',els.runBtn),label=$('span',els.runBtn);if(icon)icon.textContent='🏃';if(label)label.textContent='Correr';`
- [x] `if` — linha 4656, coluna 266 em `updateRunUI()` — `if(!els.runBtn)return;if(typeof mobilityDriverActive==='function'&&mobilityDriverActive()){updateMobilityControlLabels();return;}const active=sprintRequested();els.runBtn.classList.toggle('active',active);const icon=$('b',els.runBtn),label=$('span',els.runBtn);if(icon)icon.textContent='🏃';if(label)label.textContent='Correr';`
- [x] `if` — linha 4656, coluna 295 em `updateRunUI()` — `if(!els.runBtn)return;if(typeof mobilityDriverActive==='function'&&mobilityDriverActive()){updateMobilityControlLabels();return;}const active=sprintRequested();els.runBtn.classList.toggle('active',active);const icon=$('b',els.runBtn),label=$('span',els.runBtn);if(icon)icon.textContent='🏃';if(label)label.textContent='Correr';`
- [x] `if` — linha 4664, coluna 5 em `clearMovementInputs()` — `if(els.joystickKnob)els.joystickKnob.style.transform='translate(-50%,-50%)';`
- [x] `return` — linha 4666, coluna 22 em `canJump()` — `function canJump(){return !player.vehicle&&!player.boating&&!player.transit.mode&&(player.grounded||performance.now()-player.lastGrounded<125);}`
- [x] `if` — linha 4667, coluna 26 em `requestJump()` — `function requestJump(){if(!els.modal.hidden||paused||player.vehicle||player.boating||player.transit.mode)return;player.jumpBuffer=performance.now()+150;if(canJump())doJump();}`
- [x] `return` — linha 4667, coluna 108 em `requestJump()` — `function requestJump(){if(!els.modal.hidden||paused||player.vehicle||player.boating||player.transit.mode)return;player.jumpBuffer=performance.now()+150;if(canJump())doJump();}`
- [x] `if` — linha 4667, coluna 155 em `requestJump()` — `function requestJump(){if(!els.modal.hidden||paused||player.vehicle||player.boating||player.transit.mode)return;player.jumpBuffer=performance.now()+150;if(canJump())doJump();}`
- [x] `if` — linha 4668, coluna 21 em `doJump()` — `function doJump(){if(!canJump())return;state.stats.jumps++;trackDaily('jump',1);player.vy=10.2;player.grounded=false;player.jumpBuffer=0;beep(540);vibrate(18);}`
- [x] `return` — linha 4668, coluna 35 em `doJump()` — `function doJump(){if(!canJump())return;state.stats.jumps++;trackDaily('jump',1);player.vy=10.2;player.grounded=false;player.jumpBuffer=0;beep(540);vibrate(18);}`
- [x] `if` — linha 4675, coluna 69 em `updatePlayer()` — `const mag=Math.hypot(input.x,input.z);let ix=input.x,iz=input.z;if(mag>1){ix/=mag;iz/=mag;}if(fishingSession||fishingVisual?.active){ix=0;iz=0;input.x=input.z=input.targetX=input.targetZ=0;}`
- [x] `if` — linha 4675, coluna 96 em `updatePlayer()` — `const mag=Math.hypot(input.x,input.z);let ix=input.x,iz=input.z;if(mag>1){ix/=mag;iz/=mag;}if(fishingSession||fishingVisual?.active){ix=0;iz=0;input.x=input.z=input.targetX=input.targetZ=0;}`
- [x] `if` — linha 4676, coluna 5 em `updatePlayer()` — `if(player.transit.mode){player.vx=player.vz=0;input.x=input.z=input.targetX=input.targetZ=0;playerGroup.position.set(player.x,player.y,player.z);playerGroup.rotation.y=player.facing;contactShadow.position.set(player.x,groundHeightAt(player.x,player.z)+.025,player.z);if(player.transit.mode==='bus')animatePlayer(dt);updateContext();return;}`
- [x] `if` — linha 4676, coluna 272 em `updatePlayer()` — `if(player.transit.mode){player.vx=player.vz=0;input.x=input.z=input.targetX=input.targetZ=0;playerGroup.position.set(player.x,player.y,player.z);playerGroup.rotation.y=player.facing;contactShadow.position.set(player.x,groundHeightAt(player.x,player.z)+.025,player.z);if(player.transit.mode==='bus')animatePlayer(dt);updateContext();return;}`
- [x] `return` — linha 4676, coluna 337 em `updatePlayer()` — `if(player.transit.mode){player.vx=player.vz=0;input.x=input.z=input.targetX=input.targetZ=0;playerGroup.position.set(player.x,player.y,player.z);playerGroup.rotation.y=player.facing;contactShadow.position.set(player.x,groundHeightAt(player.x,player.z)+.025,player.z);if(player.transit.mode==='bus')animatePlayer(dt);updateContext();return;}`
- [x] `if` — linha 4678, coluna 5 em `updatePlayer()` — `if(player.boating){`
- [x] `else` — linha 4680, coluna 6 em `updatePlayer()` — `}else if(player.vehicle){`
- [x] `if` — linha 4680, coluna 11 em `updatePlayer()` — `}else if(player.vehicle){`
- [x] `else` — linha 4682, coluna 6 em `updatePlayer()` — `}else if(performance.now()<player.sitUntil){`
- [x] `if` — linha 4682, coluna 11 em `updatePlayer()` — `}else if(performance.now()<player.sitUntil){`
- [x] `else` — linha 4684, coluna 6 em `updatePlayer()` — `}else{`
- [x] `if` — linha 4691, coluna 119 em `updatePlayer()` — `player.vx=lerp(player.vx,targetVx,Math.min(1,dt*accel));player.vz=lerp(player.vz,targetVz,Math.min(1,dt*accel));if(mag<.03){player.vx*=Math.pow(.0008,dt);player.vz*=Math.pow(.0008,dt);}`
- [x] `if` — linha 4693, coluna 155 em `updatePlayer()` — `const prevX=player.x,prevZ=player.z;player.x+=player.vx*dt;player.z+=player.vz*dt;player.x=clamp(player.x,-116,116);player.z=clamp(player.z,-116,116);if(player.boating)constrainBoat(prevX,prevZ);else if(!(player.vehicle&&player.car.passengerOf)){resolveCollisions(prevX,prevZ);resolveWaterWalking(prevX,prevZ);}`
- [x] `else` — linha 4693, coluna 200 em `updatePlayer()` — `const prevX=player.x,prevZ=player.z;player.x+=player.vx*dt;player.z+=player.vz*dt;player.x=clamp(player.x,-116,116);player.z=clamp(player.z,-116,116);if(player.boating)constrainBoat(prevX,prevZ);else if(!(player.vehicle&&player.car.passengerOf)){resolveCollisions(prevX,prevZ);resolveWaterWalking(prevX,prevZ);}`
- [x] `if` — linha 4693, coluna 205 em `updatePlayer()` — `const prevX=player.x,prevZ=player.z;player.x+=player.vx*dt;player.z+=player.vz*dt;player.x=clamp(player.x,-116,116);player.z=clamp(player.z,-116,116);if(player.boating)constrainBoat(prevX,prevZ);else if(!(player.vehicle&&player.car.passengerOf)){resolveCollisions(prevX,prevZ);resolveWaterWalking(prevX,prevZ);}`
- [x] `if` — linha 4694, coluna 62 em `updatePlayer()` — `const movedNow=Math.hypot(player.x-prevX,player.z-prevZ);if(movedNow>.001){if(player.vehicle||player.boating){state.stats.driven+=movedNow;trackDaily('drive',movedNow);}else{state.stats.walked+=movedNow;trackDaily('walk',movedNow);}}`
- [x] `if` — linha 4694, coluna 80 em `updatePlayer()` — `const movedNow=Math.hypot(player.x-prevX,player.z-prevZ);if(movedNow>.001){if(player.vehicle||player.boating){state.stats.driven+=movedNow;trackDaily('drive',movedNow);}else{state.stats.walked+=movedNow;trackDaily('walk',movedNow);}}`
- [x] `else` — linha 4694, coluna 174 em `updatePlayer()` — `const movedNow=Math.hypot(player.x-prevX,player.z-prevZ);if(movedNow>.001){if(player.vehicle||player.boating){state.stats.driven+=movedNow;trackDaily('drive',movedNow);}else{state.stats.walked+=movedNow;trackDaily('walk',movedNow);}}`
- [x] `if` — linha 4695, coluna 71 em `updatePlayer()` — `const ground=player.boating?.78:groundHeightAt(player.x,player.z);if(!player.grounded)player.vy-=31*dt;player.y+=player.vy*dt;`
- [x] `if` — linha 4696, coluna 5 em `updatePlayer()` — `if(player.y<=ground&&player.vy<=0){const landed=!player.grounded&&player.vy<-4;player.y=ground;player.vy=0;player.grounded=true;player.lastGrounded=performance.now();if(landed){vibrate(20);beep(180,35,'sine');}}`
- [x] `if` — linha 4696, coluna 171 em `updatePlayer()` — `if(player.y<=ground&&player.vy<=0){const landed=!player.grounded&&player.vy<-4;player.y=ground;player.vy=0;player.grounded=true;player.lastGrounded=performance.now();if(landed){vibrate(20);beep(180,35,'sine');}}`
- [x] `else` — linha 4697, coluna 5 em `updatePlayer()` — `else if(player.y>ground+.03)player.grounded=false;`
- [x] `if` — linha 4697, coluna 10 em `updatePlayer()` — `else if(player.y>ground+.03)player.grounded=false;`
- [x] `if` — linha 4698, coluna 5 em `updatePlayer()` — `if(player.jumpBuffer&&player.jumpBuffer>performance.now()&&canJump())doJump();`
- [x] `if` — linha 4699, coluna 5 em `updatePlayer()` — `if(!player.vehicle&&!player.boating&&Math.hypot(player.vx,player.vz)>.15)player.facing=Math.atan2(player.vx,player.vz);`
- [x] `if` — linha 4700, coluna 504 em `updatePlayer()` — `playerGroup.position.set(player.x,player.y,player.z);playerGroup.rotation.y=performance.now()<player.spinUntil?player.facing+(1-(player.spinUntil-performance.now())/980)*Math.PI*4:player.facing;syncPlayerRootScale();contactShadow.position.set(player.x,ground+.025,player.z);const air=Math.max(0,player.y-ground);const ss=clamp(1-air*.08,.48,1);contactShadow.scale.setScalar(ss);contactShadow.material.opacity=clamp(.27-air*.035,.06,.27);vehicleVisual.visible=player.vehicle&&!player.car.passengerOf;if(world.boat)world.boat.group.visible=true;updateBoatPanel();`
- [x] `if` — linha 4701, coluna 5 em `updatePlayer()` — `if(!recoverPlayerIfInvalid())rememberSafePlayerPosition();`
- [x] `if` — linha 4706, coluna 5 em `mobilityThrottleIntent()` — `if(input.mobilityAccelerate)throttle=1;`
- [x] `if` — linha 4707, coluna 5 em `mobilityThrottleIntent()` — `if(input.mobilityBrake){`
- [x] `if` — linha 4708, coluna 7 em `mobilityThrottleIntent()` — `if(currentSpeed>.04)return{throttle:0,brake:true,reverse:false};`
- [x] `return` — linha 4708, coluna 27 em `mobilityThrottleIntent()` — `if(currentSpeed>.04)return{throttle:0,brake:true,reverse:false};`
- [x] `if` — linha 4709, coluna 7 em `mobilityThrottleIntent()` — `if(currentSpeed<-.04)return{throttle:-1,brake:false,reverse:true};`
- [x] `return` — linha 4709, coluna 28 em `mobilityThrottleIntent()` — `if(currentSpeed<-.04)return{throttle:-1,brake:false,reverse:true};`
- [x] `return` — linha 4710, coluna 7 em `mobilityThrottleIntent()` — `return{throttle:-1,brake:false,reverse:true};`
- [x] `if` — linha 4712, coluna 5 em `mobilityThrottleIntent()` — `if(input.mobilityAccelerate&&currentSpeed<-.04)return{throttle:0,brake:true,reverse:false};`
- [x] `return` — linha 4712, coluna 52 em `mobilityThrottleIntent()` — `if(input.mobilityAccelerate&&currentSpeed<-.04)return{throttle:0,brake:true,reverse:false};`
- [x] `if` — linha 4713, coluna 5 em `mobilityThrottleIntent()` — `if(throttle<-.05&&currentSpeed>.04)return{throttle:0,brake:true,reverse:false};`
- [x] `return` — linha 4713, coluna 40 em `mobilityThrottleIntent()` — `if(throttle<-.05&&currentSpeed>.04)return{throttle:0,brake:true,reverse:false};`
- [x] `if` — linha 4714, coluna 5 em `mobilityThrottleIntent()` — `if(throttle>.05&&currentSpeed<-.04)return{throttle:0,brake:true,reverse:false};`
- [x] `return` — linha 4714, coluna 40 em `mobilityThrottleIntent()` — `if(throttle>.05&&currentSpeed<-.04)return{throttle:0,brake:true,reverse:false};`
- [x] `return` — linha 4715, coluna 5 em `mobilityThrottleIntent()` — `return{throttle,brake:false,reverse:throttle<0};`
- [x] `if` — linha 4718, coluna 26 em `updateVehiclePhysics()` — `const car=player.car;if(performance.now()<Number(car.incidentUntil||0)){car.speed=lerp(car.speed,0,Math.min(1,dt*8));player.vx=lerp(player.vx,0,Math.min(1,dt*10));player.vz=lerp(player.vz,0,Math.min(1,dt*10));updateMobilityControlLabels();return;}`
- [x] `return` — linha 4718, coluna 244 em `updateVehiclePhysics()` — `const car=player.car;if(performance.now()<Number(car.incidentUntil||0)){car.speed=lerp(car.speed,0,Math.min(1,dt*8));player.vx=lerp(player.vx,0,Math.min(1,dt*10));player.vz=lerp(player.vz,0,Math.min(1,dt*10));updateMobilityControlLabels();return;}`
- [x] `if` — linha 4721, coluna 5 em `updateVehiclePhysics()` — `if(car.passengerOf){const ghost=world.ghosts.get(car.passengerOf),target=ghost?.userData?.target;if(!ghost||!target?.vehicle){car.hostMissingAt=car.hostMissingAt||performance.now();if(performance.now()-car.hostMissingAt>3500){toast('O motorista saiu. Você deixou o carro.','warn');exitVehicle(true);}player.vx=player.vz=0;return;}car.hostMissingAt=0;const heading=Number(target.r||ghost.rotation.y||car.heading),tx=Number(target.x??ghost.position.x)+Math.cos(heading)*.62-Math.sin(heading)*.12,tz=Number(target.z??ghost.position.z)-Math.sin(heading)*.62-Math.cos(heading)*.12;player.vx=clamp((tx-player.x)*10,-26,26);player.vz=clamp((tz-player.z)*10,-26,26);car.heading=heading;player.facing=heading;return;}`
- [x] `if` — linha 4721, coluna 102 em `updateVehiclePhysics()` — `if(car.passengerOf){const ghost=world.ghosts.get(car.passengerOf),target=ghost?.userData?.target;if(!ghost||!target?.vehicle){car.hostMissingAt=car.hostMissingAt||performance.now();if(performance.now()-car.hostMissingAt>3500){toast('O motorista saiu. Você deixou o carro.','warn');exitVehicle(true);}player.vx=player.vz=0;return;}car.hostMissingAt=0;const heading=Number(target.r||ghost.rotation.y||car.heading),tx=Number(target.x??ghost.position.x)+Math.cos(heading)*.62-Math.sin(heading)*.12,tz=Number(target.z??ghost.position.z)-Math.sin(heading)*.62-Math.cos(heading)*.12;player.vx=clamp((tx-player.x)*10,-26,26);player.vz=clamp((tz-player.z)*10,-26,26);car.heading=heading;player.facing=heading;return;}`
- [x] `if` — linha 4721, coluna 186 em `updateVehiclePhysics()` — `if(car.passengerOf){const ghost=world.ghosts.get(car.passengerOf),target=ghost?.userData?.target;if(!ghost||!target?.vehicle){car.hostMissingAt=car.hostMissingAt||performance.now();if(performance.now()-car.hostMissingAt>3500){toast('O motorista saiu. Você deixou o carro.','warn');exitVehicle(true);}player.vx=player.vz=0;return;}car.hostMissingAt=0;const heading=Number(target.r||ghost.rotation.y||car.heading),tx=Number(target.x??ghost.position.x)+Math.cos(heading)*.62-Math.sin(heading)*.12,tz=Number(target.z??ghost.position.z)-Math.sin(heading)*.62-Math.cos(heading)*.12;player.vx=clamp((tx-player.x)*10,-26,26);player.vz=clamp((tz-player.z)*10,-26,26);car.heading=heading;player.facing=heading;return;}`
- [x] `return` — linha 4721, coluna 327 em `updateVehiclePhysics()` — `if(car.passengerOf){const ghost=world.ghosts.get(car.passengerOf),target=ghost?.userData?.target;if(!ghost||!target?.vehicle){car.hostMissingAt=car.hostMissingAt||performance.now();if(performance.now()-car.hostMissingAt>3500){toast('O motorista saiu. Você deixou o carro.','warn');exitVehicle(true);}player.vx=player.vz=0;return;}car.hostMissingAt=0;const heading=Number(target.r||ghost.rotation.y||car.heading),tx=Number(target.x??ghost.position.x)+Math.cos(heading)*.62-Math.sin(heading)*.12,tz=Number(target.z??ghost.position.z)-Math.sin(heading)*.62-Math.cos(heading)*.12;player.vx=clamp((tx-player.x)*10,-26,26);player.vz=clamp((tz-player.z)*10,-26,26);car.heading=heading;player.facing=heading;return;}`
- [x] `return` — linha 4721, coluna 705 em `updateVehiclePhysics()` — `if(car.passengerOf){const ghost=world.ghosts.get(car.passengerOf),target=ghost?.userData?.target;if(!ghost||!target?.vehicle){car.hostMissingAt=car.hostMissingAt||performance.now();if(performance.now()-car.hostMissingAt>3500){toast('O motorista saiu. Você deixou o carro.','warn');exitVehicle(true);}player.vx=player.vz=0;return;}car.hostMissingAt=0;const heading=Number(target.r||ghost.rotation.y||car.heading),tx=Number(target.x??ghost.position.x)+Math.cos(heading)*.62-Math.sin(heading)*.12,tz=Number(target.z??ghost.position.z)-Math.sin(heading)*.62-Math.cos(heading)*.12;player.vx=clamp((tx-player.x)*10,-26,26);player.vz=clamp((tz-player.z)*10,-26,26);car.heading=heading;player.facing=heading;return;}`
- [x] `if` — linha 4723, coluna 5 em `updateVehiclePhysics()` — `if(command.brake){car.speed=approachNumber(car.speed,0,(18+Math.abs(car.speed)*1.25)*dt);}`
- [x] `else` — linha 4724, coluna 5 em `updateVehiclePhysics()` — `else{const accelFactor=car.speed>=0?Math.max(.22,1-car.speed/maxSpeed):1;const crossing=(car.speed>0.2&&throttle<0)||(car.speed<-.2&&throttle>0);const throttleAccel=throttle>=0?throttle*(turbo?23:17.8)*accelFactor:throttle*10.8*(crossing?2.9:1);car.speed+=throttleAccel*dt;}`
- [x] `if` — linha 4725, coluna 5 em `updateVehiclePhysics()` — `if(!throttle&&!command.brake)car.speed*=Math.pow(.05,dt);if(Math.abs(car.speed)<.025&&!throttle)car.speed=0;car.speed=clamp(car.speed,maxReverse,maxSpeed);`
- [x] `if` — linha 4725, coluna 62 em `updateVehiclePhysics()` — `if(!throttle&&!command.brake)car.speed*=Math.pow(.05,dt);if(Math.abs(car.speed)<.025&&!throttle)car.speed=0;car.speed=clamp(car.speed,maxReverse,maxSpeed);`
- [x] `if` — linha 4733, coluna 5 em `animatePlayer()` — `if (!playerModel) return;`
- [x] `return` — linha 4733, coluna 23 em `animatePlayer()` — `if (!playerModel) return;`
- [x] `if` — linha 4736, coluna 5 em `animatePlayer()` — `if(parts){`
- [x] `if` — linha 4738, coluna 7 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `if` — linha 4738, coluna 47 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `else` — linha 4738, coluna 162 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `if` — linha 4738, coluna 167 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `else` — linha 4738, coluna 307 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `if` — linha 4738, coluna 312 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `else` — linha 4738, coluna 432 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `if` — linha 4738, coluna 437 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `else` — linha 4738, coluna 503 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `if` — linha 4738, coluna 508 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `else` — linha 4738, coluna 763 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `if` — linha 4738, coluna 768 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `else` — linha 4738, coluna 921 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `if` — linha 4738, coluna 926 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `else` — linha 4738, coluna 1077 em `animatePlayer()` — `if(performance.now()<player.emoteUntil){if(player.emoteType==='wave'){parts.rightArm.rotation.x=-2.25;parts.rightArm.rotation.z=Math.sin(animTime*10)*.55;}else if(player.emoteType==='dance'){parts.leftArm.rotation.z=1.1;parts.rightArm.rotation.z=-1.1;playerModel.rotation.y=Math.sin(animTime*4)*.35;}else if(player.emoteType==='selfie'){parts.leftArm.rotation.x=-1.7;parts.rightArm.rotation.x=-.9;playerModel.rotation.z=.08;}else if(player.emoteType==='highfive'){parts.rightArm.rotation.x=-2.6;}else if(player.emoteType==='play'){parts.leftArm.rotation.x=-1.9;parts.rightArm.rotation.x=-1.9;parts.leftArm.rotation.z=.55;parts.rightArm.rotation.z=-.55;playerModel.position.y+=(Math.sin(animTime*10)+1)*.09;playerModel.rotation.y+=Math.sin(animTime*5)*.08;}else if(player.emoteType==='hug'){parts.leftArm.rotation.x=-1.45;parts.rightArm.rotation.x=-1.45;parts.leftArm.rotation.z=-.48;parts.rightArm.rotation.z=.48;}else if(player.emoteType==='tool'){parts.rightArm.rotation.x=-1.25-Math.sin(animTime*1`
- [x] `if` — linha 4739, coluna 7 em `animatePlayer()` — `if(fishingVisual?.active){const phase=fishingVisual.phase,cast=phase==='casting',pull=phase==='hooked'||phase==='pulling'||phase==='caught';parts.rightArm.rotation.x=lerp(parts.rightArm.rotation.x,pull?-2.35:cast?-1.95:-1.45,.38);parts.leftArm.rotation.x=lerp(parts.leftArm.rotation.x,pull?-1.85:cast?-1.35:-1.1,.38);parts.rightArm.rotation.z=lerp(parts.rightArm.rotation.z,-.22,.3);parts.leftArm.rotation.z=lerp(parts.leftArm.rotation.z,.28,.3);playerModel.rotation.z=lerp(playerModel.rotation.z,pull?-.08:.03,.2);}`
- [x] `if` — linha 4744, coluna 7 em `animatePlayer()` — `if(performance.now()<player.sitUntil){parts.leftLeg.rotation.x=1.25;parts.rightLeg.rotation.x=1.25;playerModel.position.y=Math.max(.12,visualBase-.10);}`
- [x] `else` — linha 4747, coluna 7 em `animatePlayer()` — `} else {`
- [x] `if` — linha 4755, coluna 5 em `animatePlayer()` — `if(avatarLayer){avatarLayer.position.y=playerModel.position.y;avatarLayer.rotation.x=playerModel.rotation.x;avatarLayer.rotation.z=playerModel.rotation.z;}`
- [x] `for` — linha 4758, coluna 5 em `checkHazards()` — `for(const h of world.hazards){if(Math.abs(player.x-h.x)<=h.w/2&&Math.abs(player.z-h.z)<=h.d/2&&player.y<.6){if(h.type==='water'){if(!player.boating){player.vx*=.9;player.vz*=.9;}}else if(performance.now()>player.damageUntil){player.damageUntil=performance.now()+1200;state.needs.energy=clamp(state.needs.energy-18,0,100);toast('Cuidado com a lava!','bad');returnHome();}}}`
- [x] `if` — linha 4758, coluna 35 em `checkHazards()` — `for(const h of world.hazards){if(Math.abs(player.x-h.x)<=h.w/2&&Math.abs(player.z-h.z)<=h.d/2&&player.y<.6){if(h.type==='water'){if(!player.boating){player.vx*=.9;player.vz*=.9;}}else if(performance.now()>player.damageUntil){player.damageUntil=performance.now()+1200;state.needs.energy=clamp(state.needs.energy-18,0,100);toast('Cuidado com a lava!','bad');returnHome();}}}`
- [x] `if` — linha 4758, coluna 113 em `checkHazards()` — `for(const h of world.hazards){if(Math.abs(player.x-h.x)<=h.w/2&&Math.abs(player.z-h.z)<=h.d/2&&player.y<.6){if(h.type==='water'){if(!player.boating){player.vx*=.9;player.vz*=.9;}}else if(performance.now()>player.damageUntil){player.damageUntil=performance.now()+1200;state.needs.energy=clamp(state.needs.energy-18,0,100);toast('Cuidado com a lava!','bad');returnHome();}}}`
- [x] `if` — linha 4758, coluna 134 em `checkHazards()` — `for(const h of world.hazards){if(Math.abs(player.x-h.x)<=h.w/2&&Math.abs(player.z-h.z)<=h.d/2&&player.y<.6){if(h.type==='water'){if(!player.boating){player.vx*=.9;player.vz*=.9;}}else if(performance.now()>player.damageUntil){player.damageUntil=performance.now()+1200;state.needs.energy=clamp(state.needs.energy-18,0,100);toast('Cuidado com a lava!','bad');returnHome();}}}`
- [x] `else` — linha 4758, coluna 184 em `checkHazards()` — `for(const h of world.hazards){if(Math.abs(player.x-h.x)<=h.w/2&&Math.abs(player.z-h.z)<=h.d/2&&player.y<.6){if(h.type==='water'){if(!player.boating){player.vx*=.9;player.vz*=.9;}}else if(performance.now()>player.damageUntil){player.damageUntil=performance.now()+1200;state.needs.energy=clamp(state.needs.energy-18,0,100);toast('Cuidado com a lava!','bad');returnHome();}}}`
- [x] `if` — linha 4758, coluna 189 em `checkHazards()` — `for(const h of world.hazards){if(Math.abs(player.x-h.x)<=h.w/2&&Math.abs(player.z-h.z)<=h.d/2&&player.y<.6){if(h.type==='water'){if(!player.boating){player.vx*=.9;player.vz*=.9;}}else if(performance.now()>player.damageUntil){player.damageUntil=performance.now()+1200;state.needs.energy=clamp(state.needs.energy-18,0,100);toast('Cuidado com a lava!','bad');returnHome();}}}`
- [x] `for` — linha 4761, coluna 5 em `collectNearbyCrystals()` — `for(const c of world.crystals){if(c.got)continue;c.mesh.rotation.y+=.035;c.mesh.position.y=c.y+Math.sin(animTime*2+c.x)*.12;if(Math.hypot(player.x-c.x,player.z-c.z)<1.25&&Math.abs(player.y-c.mesh.position.y)<2)collectCrystal(c);}`
- [x] `if` — linha 4761, coluna 36 em `collectNearbyCrystals()` — `for(const c of world.crystals){if(c.got)continue;c.mesh.rotation.y+=.035;c.mesh.position.y=c.y+Math.sin(animTime*2+c.x)*.12;if(Math.hypot(player.x-c.x,player.z-c.z)<1.25&&Math.abs(player.y-c.mesh.position.y)<2)collectCrystal(c);}`
- [x] `if` — linha 4761, coluna 129 em `collectNearbyCrystals()` — `for(const c of world.crystals){if(c.got)continue;c.mesh.rotation.y+=.035;c.mesh.position.y=c.y+Math.sin(animTime*2+c.x)*.12;if(Math.hypot(player.x-c.x,player.z-c.z)<1.25&&Math.abs(player.y-c.mesh.position.y)<2)collectCrystal(c);}`
- [x] `if` — linha 4764, coluna 44 em `npcSpeech()` — `function npcSpeech(npc,text,type='good'){if(distance2D(player,npc.group.position)<12)toast(\`${npc.name}: ${text}\`,type,2400);npc.emoteType=type==='warn'?'wave':'dance';npc.emoteUntil=performance.now()+1600;}`

## `27-npc-enemies-combat-camera-action.js`

- [x] `return` — linha 4768, coluna 5 em `nearestRideCompanion()` — `return world.npcs.filter(n=>!n.passengerMode&&(n.pendingRide||n.following)).sort((a,b)=>distance2D(player,a.group.position)-distance2D(player,b.group.position)).find(n=>distance2D(player,n.group.position)<=radius)||null;`
- [x] `return` — linha 4771, coluna 5 em `nearestBoardableNpc()` — `return world.npcs.filter(n=>!n.passengerMode).sort((a,b)=>distance2D(player,a.group.position)-distance2D(player,b.group.position)).find(n=>distance2D(player,n.group.position)<=radius)||null;`
- [x] `if` — linha 4774, coluna 5 em `boardNpcPassenger()` — `if(!npc)return false;const current=kind==='boat'?player.boat:player.car;if(current.passengerUid||current.passengerBotId)return false;current.passengerBotId=npc.id;npc.passengerMode=kind;npc.pendingRide=false;npc.following=false;if(npc.mobility?.ride)npc.mobility.ride.visible=false;toast(\`${npc.name} entrou como passageiro no ${kind==='boat'?'barco':'carro'}.\`,'good',2300);saveState();return true;`
- [x] `return` — linha 4774, coluna 13 em `boardNpcPassenger()` — `if(!npc)return false;const current=kind==='boat'?player.boat:player.car;if(current.passengerUid||current.passengerBotId)return false;current.passengerBotId=npc.id;npc.passengerMode=kind;npc.pendingRide=false;npc.following=false;if(npc.mobility?.ride)npc.mobility.ride.visible=false;toast(\`${npc.name} entrou como passageiro no ${kind==='boat'?'barco':'carro'}.\`,'good',2300);saveState();return true;`
- [x] `if` — linha 4774, coluna 77 em `boardNpcPassenger()` — `if(!npc)return false;const current=kind==='boat'?player.boat:player.car;if(current.passengerUid||current.passengerBotId)return false;current.passengerBotId=npc.id;npc.passengerMode=kind;npc.pendingRide=false;npc.following=false;if(npc.mobility?.ride)npc.mobility.ride.visible=false;toast(\`${npc.name} entrou como passageiro no ${kind==='boat'?'barco':'carro'}.\`,'good',2300);saveState();return true;`
- [x] `return` — linha 4774, coluna 125 em `boardNpcPassenger()` — `if(!npc)return false;const current=kind==='boat'?player.boat:player.car;if(current.passengerUid||current.passengerBotId)return false;current.passengerBotId=npc.id;npc.passengerMode=kind;npc.pendingRide=false;npc.following=false;if(npc.mobility?.ride)npc.mobility.ride.visible=false;toast(\`${npc.name} entrou como passageiro no ${kind==='boat'?'barco':'carro'}.\`,'good',2300);saveState();return true;`
- [x] `if` — linha 4774, coluna 233 em `boardNpcPassenger()` — `if(!npc)return false;const current=kind==='boat'?player.boat:player.car;if(current.passengerUid||current.passengerBotId)return false;current.passengerBotId=npc.id;npc.passengerMode=kind;npc.pendingRide=false;npc.following=false;if(npc.mobility?.ride)npc.mobility.ride.visible=false;toast(\`${npc.name} entrou como passageiro no ${kind==='boat'?'barco':'carro'}.\`,'good',2300);saveState();return true;`
- [x] `return` — linha 4774, coluna 392 em `boardNpcPassenger()` — `if(!npc)return false;const current=kind==='boat'?player.boat:player.car;if(current.passengerUid||current.passengerBotId)return false;current.passengerBotId=npc.id;npc.passengerMode=kind;npc.pendingRide=false;npc.following=false;if(npc.mobility?.ride)npc.mobility.ride.visible=false;toast(\`${npc.name} entrou como passageiro no ${kind==='boat'?'barco':'carro'}.\`,'good',2300);saveState();return true;`
- [x] `if` — linha 4777, coluna 82 em `releaseNpcPassenger()` — `const current=kind==='boat'?player.boat:player.car,id=current.passengerBotId;if(!id)return;const npc=world.npcs.find(n=>n.id===id);if(npc){const exitX=kind==='boat'?BOAT_DOCK.exitX+1.2:player.x+1.8,exitZ=kind==='boat'?clamp(player.z,BOAT_DOCK.minZ+.25,BOAT_DOCK.maxZ-.25):player.z;npc.passengerMode='';npc.group.position.set(exitX,groundHeightAt(exitX,exitZ),exitZ);npc.baseX=npc.group.position.x;npc.baseZ=npc.group.position.z;if(npc.mobility?.ride)npc.mobility.ride.visible=true;}current.passengerBotId='';`
- [x] `return` — linha 4777, coluna 89 em `releaseNpcPassenger()` — `const current=kind==='boat'?player.boat:player.car,id=current.passengerBotId;if(!id)return;const npc=world.npcs.find(n=>n.id===id);if(npc){const exitX=kind==='boat'?BOAT_DOCK.exitX+1.2:player.x+1.8,exitZ=kind==='boat'?clamp(player.z,BOAT_DOCK.minZ+.25,BOAT_DOCK.maxZ-.25):player.z;npc.passengerMode='';npc.group.position.set(exitX,groundHeightAt(exitX,exitZ),exitZ);npc.baseX=npc.group.position.x;npc.baseZ=npc.group.position.z;if(npc.mobility?.ride)npc.mobility.ride.visible=true;}current.passengerBotId='';`
- [x] `if` — linha 4777, coluna 136 em `releaseNpcPassenger()` — `const current=kind==='boat'?player.boat:player.car,id=current.passengerBotId;if(!id)return;const npc=world.npcs.find(n=>n.id===id);if(npc){const exitX=kind==='boat'?BOAT_DOCK.exitX+1.2:player.x+1.8,exitZ=kind==='boat'?clamp(player.z,BOAT_DOCK.minZ+.25,BOAT_DOCK.maxZ-.25):player.z;npc.passengerMode='';npc.group.position.set(exitX,groundHeightAt(exitX,exitZ),exitZ);npc.baseX=npc.group.position.x;npc.baseZ=npc.group.position.z;if(npc.mobility?.ride)npc.mobility.ride.visible=true;}current.passengerBotId='';`
- [x] `if` — linha 4777, coluna 433 em `releaseNpcPassenger()` — `const current=kind==='boat'?player.boat:player.car,id=current.passengerBotId;if(!id)return;const npc=world.npcs.find(n=>n.id===id);if(npc){const exitX=kind==='boat'?BOAT_DOCK.exitX+1.2:player.x+1.8,exitZ=kind==='boat'?clamp(player.z,BOAT_DOCK.minZ+.25,BOAT_DOCK.maxZ-.25):player.z;npc.passengerMode='';npc.group.position.set(exitX,groundHeightAt(exitX,exitZ),exitZ);npc.baseX=npc.group.position.x;npc.baseZ=npc.group.position.z;if(npc.mobility?.ride)npc.mobility.ride.visible=true;}current.passengerBotId='';`
- [x] `if` — linha 4780, coluna 55 em `updateNpcSociety()` — `updateNpcSociety.acc=(updateNpcSociety.acc||0)+dt;if(updateNpcSociety.acc<9)return;updateNpcSociety.acc=0;if(!world.npcs.length)return;`
- [x] `return` — linha 4780, coluna 81 em `updateNpcSociety()` — `updateNpcSociety.acc=(updateNpcSociety.acc||0)+dt;if(updateNpcSociety.acc<9)return;updateNpcSociety.acc=0;if(!world.npcs.length)return;`
- [x] `if` — linha 4780, coluna 111 em `updateNpcSociety()` — `updateNpcSociety.acc=(updateNpcSociety.acc||0)+dt;if(updateNpcSociety.acc<9)return;updateNpcSociety.acc=0;if(!world.npcs.length)return;`
- [x] `return` — linha 4780, coluna 133 em `updateNpcSociety()` — `updateNpcSociety.acc=(updateNpcSociety.acc||0)+dt;if(updateNpcSociety.acc<9)return;updateNpcSociety.acc=0;if(!world.npcs.length)return;`
- [x] `if` — linha 4782, coluna 5 em `updateNpcSociety()` — `if(roll<.22){const gift=Math.random()<.5?'food':'coins';if(gift==='food'){state.inventory.food=(state.inventory.food||0)+1;npcSpeech(npc,'Trouxe uma comida para você!');}else{state.profile.coins+=8;npcSpeech(npc,'Ganhei algumas moedas e dividi com você!');}saveState();updateHUD();}`
- [x] `if` — linha 4782, coluna 61 em `updateNpcSociety()` — `if(roll<.22){const gift=Math.random()<.5?'food':'coins';if(gift==='food'){state.inventory.food=(state.inventory.food||0)+1;npcSpeech(npc,'Trouxe uma comida para você!');}else{state.profile.coins+=8;npcSpeech(npc,'Ganhei algumas moedas e dividi com você!');}saveState();updateHUD();}`
- [x] `else` — linha 4782, coluna 175 em `updateNpcSociety()` — `if(roll<.22){const gift=Math.random()<.5?'food':'coins';if(gift==='food'){state.inventory.food=(state.inventory.food||0)+1;npcSpeech(npc,'Trouxe uma comida para você!');}else{state.profile.coins+=8;npcSpeech(npc,'Ganhei algumas moedas e dividi com você!');}saveState();updateHUD();}`
- [x] `else` — linha 4783, coluna 5 em `updateNpcSociety()` — `else if(roll<.44){npcSpeech(npc,'Quer apostar uma corrida comigo?');npc.userDataChallengeUntil=performance.now()+12000;}`
- [x] `if` — linha 4783, coluna 10 em `updateNpcSociety()` — `else if(roll<.44){npcSpeech(npc,'Quer apostar uma corrida comigo?');npc.userDataChallengeUntil=performance.now()+12000;}`
- [x] `else` — linha 4784, coluna 5 em `updateNpcSociety()` — `else if(roll<.66){const other=world.npcs.find(n=>n!==npc);if(other){state.npcSociety.friendships[\`${npc.id}-${other.id}\`]=(state.npcSociety.friendships[\`${npc.id}-${other.id}\`]||0)+1;npcSpeech(npc,\`Conversei com ${other.name} na praça.\`);}}`
- [x] `if` — linha 4784, coluna 10 em `updateNpcSociety()` — `else if(roll<.66){const other=world.npcs.find(n=>n!==npc);if(other){state.npcSociety.friendships[\`${npc.id}-${other.id}\`]=(state.npcSociety.friendships[\`${npc.id}-${other.id}\`]||0)+1;npcSpeech(npc,\`Conversei com ${other.name} na praça.\`);}}`
- [x] `if` — linha 4784, coluna 63 em `updateNpcSociety()` — `else if(roll<.66){const other=world.npcs.find(n=>n!==npc);if(other){state.npcSociety.friendships[\`${npc.id}-${other.id}\`]=(state.npcSociety.friendships[\`${npc.id}-${other.id}\`]||0)+1;npcSpeech(npc,\`Conversei com ${other.name} na praça.\`);}}`
- [x] `else` — linha 4785, coluna 5 em `updateNpcSociety()` — `else if(roll<.82){npcSpeech(npc,'Hoje estou chateado. Podemos conversar com calma?','warn');state.npcSociety.moods[npc.id]='chateado';}`
- [x] `if` — linha 4785, coluna 10 em `updateNpcSociety()` — `else if(roll<.82){npcSpeech(npc,'Hoje estou chateado. Podemos conversar com calma?','warn');state.npcSociety.moods[npc.id]='chateado';}`
- [x] `else` — linha 4786, coluna 5 em `updateNpcSociety()` — `else{const available=world.houses.find(h=>!h.publicBuilding&&!cloudHouseRecord(h.id)&&!state.npcSociety.houses[h.id]);if(available){state.npcSociety.houses[available.id]=npc.id;npcSpeech(npc,\`Estou juntando moedas para morar na ${available.name}.\`);saveState();}}`
- [x] `if` — linha 4786, coluna 123 em `updateNpcSociety()` — `else{const available=world.houses.find(h=>!h.publicBuilding&&!cloudHouseRecord(h.id)&&!state.npcSociety.houses[h.id]);if(available){state.npcSociety.houses[available.id]=npc.id;npcSpeech(npc,\`Estou juntando moedas para morar na ${available.name}.\`);saveState();}}`
- [x] `for` — linha 4790, coluna 5 em `updateNPCs()` — `for(const npc of world.npcs){`
- [x] `if` — linha 4793, coluna 7 em `updateNPCs()` — `if(npc.passengerMode){`
- [x] `else` — linha 4795, coluna 8 em `updateNPCs()` — `}else if(npc.fishingActivity){`
- [x] `if` — linha 4795, coluna 13 em `updateNPCs()` — `}else if(npc.fishingActivity){`
- [x] `else` — linha 4797, coluna 8 em `updateNPCs()` — `}else if(npc.coopRaceMode){`
- [x] `if` — linha 4797, coluna 13 em `updateNPCs()` — `}else if(npc.coopRaceMode){`
- [x] `else` — linha 4799, coluna 8 em `updateNPCs()` — `}else if(npc.following){`
- [x] `if` — linha 4799, coluna 13 em `updateNPCs()` — `}else if(npc.following){`
- [x] `else` — linha 4802, coluna 8 em `updateNPCs()` — `}else if(near){`
- [x] `if` — linha 4802, coluna 13 em `updateNPCs()` — `}else if(near){`
- [x] `else` — linha 4805, coluna 8 em `updateNPCs()` — `}else if(npc.mobility){`
- [x] `if` — linha 4805, coluna 13 em `updateNPCs()` — `}else if(npc.mobility){`
- [x] `if` — linha 4806, coluna 159 em `updateNPCs()` — `const route=npc.mobility.route,target=route[npc.mobility.index],dx=target.x-npc.group.position.x,dz=target.z-npc.group.position.z,d=Math.hypot(dx,dz);if(performance.now()<Number(npc.mobility.trafficHoldUntil||0)){npc.mobility.currentSpeed=0;}else if(d<.2)npc.mobility.index=(npc.mobility.index+1)%route.length;else{const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(npc.mobility,heading,6),targetSpeed=npc.mobility.speed*factor;npc.mobility.currentSpeed=lerp(Number(npc.mobility.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const step=Math.min(d,npc.mobility.currentSpeed*dt),previous={x:npc.group.position.x,z:npc.group.position.z};if(step>.0001){npc.group.position.x+=dx/d*step;npc.group.position.z+=dz/d*step;snapTrafficToRoad(npc.group,previous);npc.group.rotation.y=lerpAngle(npc.group.rotation.y,heading,Math.min(1,dt*5));for(const wheel of npc.mobility.wheels)wheel.rotation.x-=step*3;}}`
- [x] `else` — linha 4806, coluna 251 em `updateNPCs()` — `const route=npc.mobility.route,target=route[npc.mobility.index],dx=target.x-npc.group.position.x,dz=target.z-npc.group.position.z,d=Math.hypot(dx,dz);if(performance.now()<Number(npc.mobility.trafficHoldUntil||0)){npc.mobility.currentSpeed=0;}else if(d<.2)npc.mobility.index=(npc.mobility.index+1)%route.length;else{const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(npc.mobility,heading,6),targetSpeed=npc.mobility.speed*factor;npc.mobility.currentSpeed=lerp(Number(npc.mobility.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const step=Math.min(d,npc.mobility.currentSpeed*dt),previous={x:npc.group.position.x,z:npc.group.position.z};if(step>.0001){npc.group.position.x+=dx/d*step;npc.group.position.z+=dz/d*step;snapTrafficToRoad(npc.group,previous);npc.group.rotation.y=lerpAngle(npc.group.rotation.y,heading,Math.min(1,dt*5));for(const wheel of npc.mobility.wheels)wheel.rotation.x-=step*3;}}`
- [x] `if` — linha 4806, coluna 256 em `updateNPCs()` — `const route=npc.mobility.route,target=route[npc.mobility.index],dx=target.x-npc.group.position.x,dz=target.z-npc.group.position.z,d=Math.hypot(dx,dz);if(performance.now()<Number(npc.mobility.trafficHoldUntil||0)){npc.mobility.currentSpeed=0;}else if(d<.2)npc.mobility.index=(npc.mobility.index+1)%route.length;else{const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(npc.mobility,heading,6),targetSpeed=npc.mobility.speed*factor;npc.mobility.currentSpeed=lerp(Number(npc.mobility.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const step=Math.min(d,npc.mobility.currentSpeed*dt),previous={x:npc.group.position.x,z:npc.group.position.z};if(step>.0001){npc.group.position.x+=dx/d*step;npc.group.position.z+=dz/d*step;snapTrafficToRoad(npc.group,previous);npc.group.rotation.y=lerpAngle(npc.group.rotation.y,heading,Math.min(1,dt*5));for(const wheel of npc.mobility.wheels)wheel.rotation.x-=step*3;}}`
- [x] `else` — linha 4806, coluna 319 em `updateNPCs()` — `const route=npc.mobility.route,target=route[npc.mobility.index],dx=target.x-npc.group.position.x,dz=target.z-npc.group.position.z,d=Math.hypot(dx,dz);if(performance.now()<Number(npc.mobility.trafficHoldUntil||0)){npc.mobility.currentSpeed=0;}else if(d<.2)npc.mobility.index=(npc.mobility.index+1)%route.length;else{const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(npc.mobility,heading,6),targetSpeed=npc.mobility.speed*factor;npc.mobility.currentSpeed=lerp(Number(npc.mobility.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const step=Math.min(d,npc.mobility.currentSpeed*dt),previous={x:npc.group.position.x,z:npc.group.position.z};if(step>.0001){npc.group.position.x+=dx/d*step;npc.group.position.z+=dz/d*step;snapTrafficToRoad(npc.group,previous);npc.group.rotation.y=lerpAngle(npc.group.rotation.y,heading,Math.min(1,dt*5));for(const wheel of npc.mobility.wheels)wheel.rotation.x-=step*3;}}`
- [x] `if` — linha 4806, coluna 651 em `updateNPCs()` — `const route=npc.mobility.route,target=route[npc.mobility.index],dx=target.x-npc.group.position.x,dz=target.z-npc.group.position.z,d=Math.hypot(dx,dz);if(performance.now()<Number(npc.mobility.trafficHoldUntil||0)){npc.mobility.currentSpeed=0;}else if(d<.2)npc.mobility.index=(npc.mobility.index+1)%route.length;else{const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(npc.mobility,heading,6),targetSpeed=npc.mobility.speed*factor;npc.mobility.currentSpeed=lerp(Number(npc.mobility.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const step=Math.min(d,npc.mobility.currentSpeed*dt),previous={x:npc.group.position.x,z:npc.group.position.z};if(step>.0001){npc.group.position.x+=dx/d*step;npc.group.position.z+=dz/d*step;snapTrafficToRoad(npc.group,previous);npc.group.rotation.y=lerpAngle(npc.group.rotation.y,heading,Math.min(1,dt*5));for(const wheel of npc.mobility.wheels)wheel.rotation.x-=step*3;}}`
- [x] `for` — linha 4806, coluna 846 em `updateNPCs()` — `const route=npc.mobility.route,target=route[npc.mobility.index],dx=target.x-npc.group.position.x,dz=target.z-npc.group.position.z,d=Math.hypot(dx,dz);if(performance.now()<Number(npc.mobility.trafficHoldUntil||0)){npc.mobility.currentSpeed=0;}else if(d<.2)npc.mobility.index=(npc.mobility.index+1)%route.length;else{const heading=Math.atan2(dx,dz),factor=trafficSpeedFactor(npc.mobility,heading,6),targetSpeed=npc.mobility.speed*factor;npc.mobility.currentSpeed=lerp(Number(npc.mobility.currentSpeed||0),targetSpeed,Math.min(1,dt*4));const step=Math.min(d,npc.mobility.currentSpeed*dt),previous={x:npc.group.position.x,z:npc.group.position.z};if(step>.0001){npc.group.position.x+=dx/d*step;npc.group.position.z+=dz/d*step;snapTrafficToRoad(npc.group,previous);npc.group.rotation.y=lerpAngle(npc.group.rotation.y,heading,Math.min(1,dt*5));for(const wheel of npc.mobility.wheels)wheel.rotation.x-=step*3;}}`
- [x] `else` — linha 4807, coluna 8 em `updateNPCs()` — `}else{`
- [x] `if` — linha 4813, coluna 7 em `updateNPCs()` — `if(!npc.passengerMode)npc.group.position.y=lerp(npc.group.position.y,0,Math.min(1,dt*8));`
- [x] `if` — linha 4817, coluna 7 em `updateNPCs()` — `if(npc.limbs){`
- [x] `for` — linha 4827, coluna 5 em `updateEnemies()` — `for(const e of world.enemies){`
- [x] `if` — linha 4828, coluna 7 em `updateEnemies()` — `if(e.dead){if(performance.now()-e.lastHit>18000){e.dead=false;e.hp=e.type==='golem'?3:1;e.group.visible=true;e.group.position.set(e.baseX,0,e.baseZ);}continue;}`
- [x] `if` — linha 4828, coluna 18 em `updateEnemies()` — `if(e.dead){if(performance.now()-e.lastHit>18000){e.dead=false;e.hp=e.type==='golem'?3:1;e.group.visible=true;e.group.position.set(e.baseX,0,e.baseZ);}continue;}`
- [x] `if` — linha 4830, coluna 7 em `updateEnemies()` — `if(d<9&&!currentHouse){tx=player.x;tz=player.z;}`
- [x] `if` — linha 4832, coluna 7 em `updateEnemies()` — `if(d<1.45&&performance.now()>player.damageUntil){player.damageUntil=performance.now()+1100;if(performance.now()<player.shieldUntil){toast('O Escudo Furtivo bloqueou o ataque!','good',1300);beep(690,60,'sine');continue;}state.needs.energy=clamp(state.needs.energy-12,0,100);state.needs.fun=clamp(state.needs.fun-4,0,100);toast('Monstro acertou!','bad');vibrate([35,40,35]);saveState();}`
- [x] `if` — linha 4832, coluna 98 em `updateEnemies()` — `if(d<1.45&&performance.now()>player.damageUntil){player.damageUntil=performance.now()+1100;if(performance.now()<player.shieldUntil){toast('O Escudo Furtivo bloqueou o ataque!','good',1300);beep(690,60,'sine');continue;}state.needs.energy=clamp(state.needs.energy-12,0,100);state.needs.fun=clamp(state.needs.fun-4,0,100);toast('Monstro acertou!','bad');vibrate([35,40,35]);saveState();}`
- [x] `if` — linha 4837, coluna 5 em `meleeAttack()` — `if(!target||distance2D(player,target.group.position)>2.35){toast('Nada para atacar por perto.','warn');return;}`
- [x] `return` — linha 4837, coluna 108 em `meleeAttack()` — `if(!target||distance2D(player,target.group.position)>2.35){toast('Nada para atacar por perto.','warn');return;}`
- [x] `if` — linha 4841, coluna 5 em `damageEnemy()` — `if(enemy.dead)return;enemy.hp-=amount;enemy.lastHit=performance.now();enemy.group.scale.set(1.18,.82,1.18);setTimeout(()=>enemy.group&&enemy.group.scale.set(1,1,1),130);`
- [x] `return` — linha 4841, coluna 19 em `damageEnemy()` — `if(enemy.dead)return;enemy.hp-=amount;enemy.lastHit=performance.now();enemy.group.scale.set(1.18,.82,1.18);setTimeout(()=>enemy.group&&enemy.group.scale.set(1,1,1),130);`
- [x] `if` — linha 4842, coluna 5 em `damageEnemy()` — `if(enemy.hp<=0){enemy.dead=true;enemy.group.visible=false;state.defeated++;addXP(enemy.type==='golem'?45:20);addCoins(enemy.type==='golem'?35:12);toast('Monstro derrotado!','good');evaluateMissions();saveState();}`
- [x] `if` — linha 4845, coluna 5 em `firePower()` — `if(!els.modal.hidden||paused||player.transit.mode)return;`
- [x] `return` — linha 4845, coluna 55 em `firePower()` — `if(!els.modal.hidden||paused||player.transit.mode)return;`
- [x] `if` — linha 4846, coluna 5 em `firePower()` — `if(player.vehicle||player.boating){vehicleHorn();return;}`
- [x] `return` — linha 4846, coluna 54 em `firePower()` — `if(player.vehicle||player.boating){vehicleHorn();return;}`
- [x] `if` — linha 4847, coluna 5 em `firePower()` — `if(currentHouse){toast('Use o poder do lado de fora.','warn');return;}`
- [x] `return` — linha 4847, coluna 67 em `firePower()` — `if(currentHouse){toast('Use o poder do lado de fora.','warn');return;}`
- [x] `for` — linha 4851, coluna 5 em `updateFireballs()` — `for(let i=world.fireballs.length-1;i>=0;i--){const f=world.fireballs[i];f.life-=dt;f.x+=f.vx*dt;f.z+=f.vz*dt;f.mesh.position.set(f.x,f.y,f.z);f.mesh.rotation.x+=dt*7;f.mesh.rotation.y+=dt*9;let hit=false;for(const e of world.enemies){if(!e.dead&&Math.hypot(f.x-e.group.position.x,f.z-e.group.position.z)<1.1){damageEnemy(e,1);hit=true;break;}}if(hit||f.life<=0){worldGroup.remove(f.mesh);world.fireballs.splice(i,1);}}`
- [x] `for` — linha 4851, coluna 209 em `updateFireballs()` — `for(let i=world.fireballs.length-1;i>=0;i--){const f=world.fireballs[i];f.life-=dt;f.x+=f.vx*dt;f.z+=f.vz*dt;f.mesh.position.set(f.x,f.y,f.z);f.mesh.rotation.x+=dt*7;f.mesh.rotation.y+=dt*9;let hit=false;for(const e of world.enemies){if(!e.dead&&Math.hypot(f.x-e.group.position.x,f.z-e.group.position.z)<1.1){damageEnemy(e,1);hit=true;break;}}if(hit||f.life<=0){worldGroup.remove(f.mesh);world.fireballs.splice(i,1);}}`
- [x] `if` — linha 4851, coluna 239 em `updateFireballs()` — `for(let i=world.fireballs.length-1;i>=0;i--){const f=world.fireballs[i];f.life-=dt;f.x+=f.vx*dt;f.z+=f.vz*dt;f.mesh.position.set(f.x,f.y,f.z);f.mesh.rotation.x+=dt*7;f.mesh.rotation.y+=dt*9;let hit=false;for(const e of world.enemies){if(!e.dead&&Math.hypot(f.x-e.group.position.x,f.z-e.group.position.z)<1.1){damageEnemy(e,1);hit=true;break;}}if(hit||f.life<=0){worldGroup.remove(f.mesh);world.fireballs.splice(i,1);}}`
- [x] `if` — linha 4851, coluna 348 em `updateFireballs()` — `for(let i=world.fireballs.length-1;i>=0;i--){const f=world.fireballs[i];f.life-=dt;f.x+=f.vx*dt;f.z+=f.vz*dt;f.mesh.position.set(f.x,f.y,f.z);f.mesh.rotation.x+=dt*7;f.mesh.rotation.y+=dt*9;let hit=false;for(const e of world.enemies){if(!e.dead&&Math.hypot(f.x-e.group.position.x,f.z-e.group.position.z)<1.1){damageEnemy(e,1);hit=true;break;}}if(hit||f.life<=0){worldGroup.remove(f.mesh);world.fireballs.splice(i,1);}}`
- [x] `if` — linha 4856, coluna 5 em `updateCamera()` — `if(player.transit.mode==='bus'){`
- [x] `if` — linha 4858, coluna 7 em `updateCamera()` — `if(bus){`
- [x] `if` — linha 4865, coluna 5 em `updateCamera()` — `if(!desiredPos&&fishingVisual?.active){`
- [x] `if` — linha 4872, coluna 5 em `updateCamera()` — `if(!desiredPos&&currentHouse&&cameraMode==='interior'){`
- [x] `else` — linha 4875, coluna 6 em `updateCamera()` — `}else if(!desiredPos){`
- [x] `if` — linha 4875, coluna 11 em `updateCamera()` — `}else if(!desiredPos){`
- [x] `if` — linha 4877, coluna 7 em `updateCamera()` — `if((player.vehicle||player.boating)&&!input.cameraDrag){const heading=player.vehicle?player.car.heading:player.boat.heading;cameraYaw=lerpAngle(cameraYaw,Math.PI-heading,Math.min(1,dt*3.2));}`
- [x] `if` — linha 4887, coluna 5 em `nearestInteractable()` — `if(activeRace)return null;`
- [x] `return` — linha 4887, coluna 19 em `nearestInteractable()` — `if(activeRace)return null;`
- [x] `if` — linha 4888, coluna 5 em `nearestInteractable()` — `if(player.transit.mode==='metro')return null;`
- [x] `return` — linha 4888, coluna 38 em `nearestInteractable()` — `if(player.transit.mode==='metro')return null;`
- [x] `if` — linha 4889, coluna 5 em `nearestInteractable()` — `if(player.transit.mode==='bus')return{id:'request-bus-stop',type:'bus',icon:'🔔',label:player.transit.requestStop?'Parada já solicitada':'Pedir próxima parada',radius:999,priority:999,action:()=>{player.transit.requestStop=true;updateTransitPanel();toast('Parada solicitada.','good',1200);}};`
- [x] `return` — linha 4889, coluna 36 em `nearestInteractable()` — `if(player.transit.mode==='bus')return{id:'request-bus-stop',type:'bus',icon:'🔔',label:player.transit.requestStop?'Parada já solicitada':'Pedir próxima parada',radius:999,priority:999,action:()=>{player.transit.requestStop=true;updateTransitPanel();toast('Parada solicitada.','good',1200);}};`
- [x] `if` — linha 4890, coluna 5 em `nearestInteractable()` — `if(player.boating){const free=!player.boat.passengerOf&&!player.boat.passengerUid&&!player.boat.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`boat-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o barco\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`boat-invite-${npc.id}\`,type:'boat',icon:'🛶',label:\`Convidar ${npc.name} para o barco\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'boat')};return{id:'exit-boat',type:'boat',icon:'🛶',label:'Sair do barco no píer',radius:999,priority:999,action:exitBoat};}`
- [x] `if` — linha 4890, coluna 191 em `nearestInteractable()` — `if(player.boating){const free=!player.boat.passengerOf&&!player.boat.passengerUid&&!player.boat.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`boat-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o barco\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`boat-invite-${npc.id}\`,type:'boat',icon:'🛶',label:\`Convidar ${npc.name} para o barco\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'boat')};return{id:'exit-boat',type:'boat',icon:'🛶',label:'Sair do barco no píer',radius:999,priority:999,action:exitBoat};}`
- [x] `return` — linha 4890, coluna 201 em `nearestInteractable()` — `if(player.boating){const free=!player.boat.passengerOf&&!player.boat.passengerUid&&!player.boat.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`boat-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o barco\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`boat-invite-${npc.id}\`,type:'boat',icon:'🛶',label:\`Convidar ${npc.name} para o barco\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'boat')};return{id:'exit-boat',type:'boat',icon:'🛶',label:'Sair do barco no píer',radius:999,priority:999,action:exitBoat};}`
- [x] `if` — linha 4890, coluna 433 em `nearestInteractable()` — `if(player.boating){const free=!player.boat.passengerOf&&!player.boat.passengerUid&&!player.boat.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`boat-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o barco\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`boat-invite-${npc.id}\`,type:'boat',icon:'🛶',label:\`Convidar ${npc.name} para o barco\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'boat')};return{id:'exit-boat',type:'boat',icon:'🛶',label:'Sair do barco no píer',radius:999,priority:999,action:exitBoat};}`
- [x] `return` — linha 4890, coluna 440 em `nearestInteractable()` — `if(player.boating){const free=!player.boat.passengerOf&&!player.boat.passengerUid&&!player.boat.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`boat-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o barco\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`boat-invite-${npc.id}\`,type:'boat',icon:'🛶',label:\`Convidar ${npc.name} para o barco\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'boat')};return{id:'exit-boat',type:'boat',icon:'🛶',label:'Sair do barco no píer',radius:999,priority:999,action:exitBoat};}`
- [x] `return` — linha 4890, coluna 604 em `nearestInteractable()` — `if(player.boating){const free=!player.boat.passengerOf&&!player.boat.passengerUid&&!player.boat.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`boat-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o barco\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`boat-invite-${npc.id}\`,type:'boat',icon:'🛶',label:\`Convidar ${npc.name} para o barco\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'boat')};return{id:'exit-boat',type:'boat',icon:'🛶',label:'Sair do barco no píer',radius:999,priority:999,action:exitBoat};}`
- [x] `if` — linha 4891, coluna 5 em `nearestInteractable()` — `if(player.vehicle){const free=!player.car.passengerOf&&!player.car.passengerUid&&!player.car.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`car-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o carro\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`car-invite-${npc.id}\`,type:'vehicle',icon:'🚗',label:\`Convidar ${npc.name} para o carro\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'car')};return{id:'exit-vehicle',type:'vehicle',icon:'🚗',label:'Sair do carro',radius:999,priority:999,action:exitVehicle};}`
- [x] `if` — linha 4891, coluna 188 em `nearestInteractable()` — `if(player.vehicle){const free=!player.car.passengerOf&&!player.car.passengerUid&&!player.car.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`car-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o carro\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`car-invite-${npc.id}\`,type:'vehicle',icon:'🚗',label:\`Convidar ${npc.name} para o carro\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'car')};return{id:'exit-vehicle',type:'vehicle',icon:'🚗',label:'Sair do carro',radius:999,priority:999,action:exitVehicle};}`
- [x] `return` — linha 4891, coluna 198 em `nearestInteractable()` — `if(player.vehicle){const free=!player.car.passengerOf&&!player.car.passengerUid&&!player.car.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`car-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o carro\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`car-invite-${npc.id}\`,type:'vehicle',icon:'🚗',label:\`Convidar ${npc.name} para o carro\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'car')};return{id:'exit-vehicle',type:'vehicle',icon:'🚗',label:'Sair do carro',radius:999,priority:999,action:exitVehicle};}`
- [x] `if` — linha 4891, coluna 429 em `nearestInteractable()` — `if(player.vehicle){const free=!player.car.passengerOf&&!player.car.passengerUid&&!player.car.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`car-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o carro\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`car-invite-${npc.id}\`,type:'vehicle',icon:'🚗',label:\`Convidar ${npc.name} para o carro\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'car')};return{id:'exit-vehicle',type:'vehicle',icon:'🚗',label:'Sair do carro',radius:999,priority:999,action:exitVehicle};}`
- [x] `return` — linha 4891, coluna 436 em `nearestInteractable()` — `if(player.vehicle){const free=!player.car.passengerOf&&!player.car.passengerUid&&!player.car.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`car-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o carro\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`car-invite-${npc.id}\`,type:'vehicle',icon:'🚗',label:\`Convidar ${npc.name} para o carro\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'car')};return{id:'exit-vehicle',type:'vehicle',icon:'🚗',label:'Sair do carro',radius:999,priority:999,action:exitVehicle};}`
- [x] `return` — linha 4891, coluna 601 em `nearestInteractable()` — `if(player.vehicle){const free=!player.car.passengerOf&&!player.car.passengerUid&&!player.car.passengerBotId,remote=free?nearestRemotePlayer():null,npc=free?nearestBoardableNpc():null;if(remote)return{id:\`car-remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Convidar ${remote.ghost.userData.displayName||'Jogador'} para o carro\`,radius:999,priority:1001,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};if(npc)return{id:\`car-invite-${npc.id}\`,type:'vehicle',icon:'🚗',label:\`Convidar ${npc.name} para o carro\`,radius:999,priority:1000,action:()=>boardNpcPassenger(npc,'car')};return{id:'exit-vehicle',type:'vehicle',icon:'🚗',label:'Sair do carro',radius:999,priority:999,action:exitVehicle};}`
- [x] `if` — linha 4892, coluna 5 em `nearestInteractable()` — `if(buildMode)return{id:'place-build',type:'build',icon:'🧱',label:\`Confirmar ${BUILD_RECIPES[buildMode]?.name||'construção'}\`,radius:999,priority:999,action:placeBuild};`
- [x] `return` — linha 4892, coluna 18 em `nearestInteractable()` — `if(buildMode)return{id:'place-build',type:'build',icon:'🧱',label:\`Confirmar ${BUILD_RECIPES[buildMode]?.name||'construção'}\`,radius:999,priority:999,action:placeBuild};`
- [x] `if` — linha 4893, coluna 40 em `nearestInteractable()` — `const remote=nearestRemotePlayer();if(remote)return{id:\`remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Interagir: ${remote.ghost.userData.displayName||'Jogador'}\`,radius:2.8,priority:980,x:remote.ghost.position.x,z:remote.ghost.position.z,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};`
- [x] `return` — linha 4893, coluna 50 em `nearestInteractable()` — `const remote=nearestRemotePlayer();if(remote)return{id:\`remote-${remote.uid}\`,type:'remote-player',icon:'🌐',label:\`Interagir: ${remote.ghost.userData.displayName||'Jogador'}\`,radius:2.8,priority:980,x:remote.ghost.position.x,z:remote.ghost.position.z,action:()=>openRemotePlayerActions(remote.uid,remote.ghost)};`
- [x] `for` — linha 4895, coluna 5 em `nearestInteractable()` — `for(const it of world.interactables){`
- [x] `if` — linha 4896, coluna 7 em `nearestInteractable()` — `if(!isInteractionAvailable(it))continue;`
- [x] `if` — linha 4898, coluna 7 em `nearestInteractable()` — `if(d>(it.radius||2))continue;`
- [x] `if` — linha 4900, coluna 7 em `nearestInteractable()` — `if(score<best){best=score;nearest=it;}`
- [x] `return` — linha 4902, coluna 5 em `nearestInteractable()` — `return nearest;`
- [x] `if` — linha 4905, coluna 103 em `updateContext()` — `const now=performance.now(),moved=Math.hypot(player.x-lastContextScanX,player.z-lastContextScanZ);if(!force&&now-lastContextScanAt<85&&moved<.18)return;lastContextScanAt=now;lastContextScanX=player.x;lastContextScanZ=player.z;`
- [x] `return` — linha 4905, coluna 150 em `updateContext()` — `const now=performance.now(),moved=Math.hypot(player.x-lastContextScanX,player.z-lastContextScanZ);if(!force&&now-lastContextScanAt<85&&moved<.18)return;lastContextScanAt=now;lastContextScanX=player.x;lastContextScanZ=player.z;`
- [x] `if` — linha 4906, coluna 60 em `updateContext()` — `const next=nearestInteractable();const id=next?.id||'';if(!force&&id===lastContextId)return;lastContextId=id;currentContext=next;els.contextPrompt.hidden=!next;els.actionBtn.classList.toggle('pulse',!!next);els.contextIcon.textContent=next?.icon||'⚔';els.contextLabel.textContent=next?.label||'Atacar';els.contextHint.textContent=next?'Toque em AÇÃO':'Ataque próximo';const span=$('span',els.actionBtn);const icon=$('b',els.actionBtn);if(span)span.textContent=next?'Ação':'Espada';if(icon)icon.textContent=next?.icon||'⚔';`
- [x] `return` — linha 4906, coluna 90 em `updateContext()` — `const next=nearestInteractable();const id=next?.id||'';if(!force&&id===lastContextId)return;lastContextId=id;currentContext=next;els.contextPrompt.hidden=!next;els.actionBtn.classList.toggle('pulse',!!next);els.contextIcon.textContent=next?.icon||'⚔';els.contextLabel.textContent=next?.label||'Atacar';els.contextHint.textContent=next?'Toque em AÇÃO':'Ataque próximo';const span=$('span',els.actionBtn);const icon=$('b',els.actionBtn);if(span)span.textContent=next?'Ação':'Espada';if(icon)icon.textContent=next?.icon||'⚔';`
- [x] `if` — linha 4906, coluna 440 em `updateContext()` — `const next=nearestInteractable();const id=next?.id||'';if(!force&&id===lastContextId)return;lastContextId=id;currentContext=next;els.contextPrompt.hidden=!next;els.actionBtn.classList.toggle('pulse',!!next);els.contextIcon.textContent=next?.icon||'⚔';els.contextLabel.textContent=next?.label||'Atacar';els.contextHint.textContent=next?'Toque em AÇÃO':'Ataque próximo';const span=$('span',els.actionBtn);const icon=$('b',els.actionBtn);if(span)span.textContent=next?'Ação':'Espada';if(icon)icon.textContent=next?.icon||'⚔';`
- [x] `if` — linha 4906, coluna 486 em `updateContext()` — `const next=nearestInteractable();const id=next?.id||'';if(!force&&id===lastContextId)return;lastContextId=id;currentContext=next;els.contextPrompt.hidden=!next;els.actionBtn.classList.toggle('pulse',!!next);els.contextIcon.textContent=next?.icon||'⚔';els.contextLabel.textContent=next?.label||'Atacar';els.contextHint.textContent=next?'Toque em AÇÃO':'Ataque próximo';const span=$('span',els.actionBtn);const icon=$('b',els.actionBtn);if(span)span.textContent=next?'Ação':'Espada';if(icon)icon.textContent=next?.icon||'⚔';`
- [x] `if` — linha 4909, coluna 5 em `doAction()` — `if(paused||!els.modal.hidden||performance.now()<actionLockedUntil)return;`
- [x] `return` — linha 4909, coluna 71 em `doAction()` — `if(paused||!els.modal.hidden||performance.now()<actionLockedUntil)return;`
- [x] `if` — linha 4911, coluna 5 em `doAction()` — `if(state.ui.quickOpen){state.ui.quickOpen=false;syncMobilePanels();}`
- [x] `if` — linha 4913, coluna 5 em `doAction()` — `if(target&&target.radius!==999){const pos=worldPos(target);if(!isInteractionAvailable(target)||Math.hypot(player.x-pos.x,player.z-pos.z)>(target.radius||2)+.2)target=null;}`
- [x] `if` — linha 4913, coluna 64 em `doAction()` — `if(target&&target.radius!==999){const pos=worldPos(target);if(!isInteractionAvailable(target)||Math.hypot(player.x-pos.x,player.z-pos.z)>(target.radius||2)+.2)target=null;}`
- [x] `if` — linha 4914, coluna 5 em `doAction()` — `if(!target)target=nearestInteractable();`
- [x] `if` — linha 4916, coluna 5 em `doAction()` — `if(target){target.action();updateContext(true);return;}`
- [x] `return` — linha 4916, coluna 52 em `doAction()` — `if(target){target.action();updateContext(true);return;}`
- [x] `if` — linha 4921, coluna 45 em `updateNeeds()` — `updateNeeds.acc=(updateNeeds.acc||0)+dt;if(updateNeeds.acc<1)return;const sec=updateNeeds.acc;updateNeeds.acc=0;state.needs.hunger=clamp(state.needs.hunger-sec*.065,0,100);state.needs.energy=clamp(state.needs.energy-sec*((player.vehicle||player.boating)?(sprintRequested()?.085:.035):(input.isSprinting?.16:.045)),0,100);state.needs.fun=clamp(state.needs.fun-sec*.025,0,100);state.needs.hygiene=clamp(state.needs.hygiene-sec*.028,0,100);if(state.needs.hunger<8&&Math.random()<.08)toast(\`${playerDisplayName()} está com fome.\`,'warn');updateHUD();if(!updateNeeds.lastSave||performance.now()-updateNeeds.lastSave>10000){updateNeeds.lastSave=performance.now();saveState();}`
- [x] `return` — linha 4921, coluna 66 em `updateNeeds()` — `updateNeeds.acc=(updateNeeds.acc||0)+dt;if(updateNeeds.acc<1)return;const sec=updateNeeds.acc;updateNeeds.acc=0;state.needs.hunger=clamp(state.needs.hunger-sec*.065,0,100);state.needs.energy=clamp(state.needs.energy-sec*((player.vehicle||player.boating)?(sprintRequested()?.085:.035):(input.isSprinting?.16:.045)),0,100);state.needs.fun=clamp(state.needs.fun-sec*.025,0,100);state.needs.hygiene=clamp(state.needs.hygiene-sec*.028,0,100);if(state.needs.hunger<8&&Math.random()<.08)toast(\`${playerDisplayName()} está com fome.\`,'warn');updateHUD();if(!updateNeeds.lastSave||performance.now()-updateNeeds.lastSave>10000){updateNeeds.lastSave=performance.now();saveState();}`
- [x] `if` — linha 4921, coluna 442 em `updateNeeds()` — `updateNeeds.acc=(updateNeeds.acc||0)+dt;if(updateNeeds.acc<1)return;const sec=updateNeeds.acc;updateNeeds.acc=0;state.needs.hunger=clamp(state.needs.hunger-sec*.065,0,100);state.needs.energy=clamp(state.needs.energy-sec*((player.vehicle||player.boating)?(sprintRequested()?.085:.035):(input.isSprinting?.16:.045)),0,100);state.needs.fun=clamp(state.needs.fun-sec*.025,0,100);state.needs.hygiene=clamp(state.needs.hygiene-sec*.028,0,100);if(state.needs.hunger<8&&Math.random()<.08)toast(\`${playerDisplayName()} está com fome.\`,'warn');updateHUD();if(!updateNeeds.lastSave||performance.now()-updateNeeds.lastSave>10000){updateNeeds.lastSave=performance.now();saveState();}`
- [x] `if` — linha 4921, coluna 551 em `updateNeeds()` — `updateNeeds.acc=(updateNeeds.acc||0)+dt;if(updateNeeds.acc<1)return;const sec=updateNeeds.acc;updateNeeds.acc=0;state.needs.hunger=clamp(state.needs.hunger-sec*.065,0,100);state.needs.energy=clamp(state.needs.energy-sec*((player.vehicle||player.boating)?(sprintRequested()?.085:.035):(input.isSprinting?.16:.045)),0,100);state.needs.fun=clamp(state.needs.fun-sec*.025,0,100);state.needs.hygiene=clamp(state.needs.hygiene-sec*.028,0,100);if(state.needs.hunger<8&&Math.random()<.08)toast(\`${playerDisplayName()} está com fome.\`,'warn');updateHUD();if(!updateNeeds.lastSave||performance.now()-updateNeeds.lastSave>10000){updateNeeds.lastSave=performance.now();saveState();}`

## `28-multiplayer-social-online.js`

- [x] `return` — linha 4930, coluna 39 em `multiplayerGameLabel()` — `function multiplayerGameLabel(type){return type==='portuguese'?'Português Kids':type==='english'?'English Kids':'Matemática Kids';}`
- [x] `return` — linha 4931, coluna 41 em `guardianMultiplayerAllowed()` — `function guardianMultiplayerAllowed(){return true;}`
- [x] `return` — linha 4932, coluna 43 em `guardianCommunicationAllowed()` — `function guardianCommunicationAllowed(){return true;}`
- [x] `return` — linha 4935, coluna 5 em `publicPlayerName()` — `return\`Jogador ${suffix}\`;`
- [x] `return` — linha 4938, coluna 104 em `remotePlayerName()` — `const suffix=String(uid||'0000').replace(/[^a-z0-9]/gi,'').slice(-4).toUpperCase().padStart(4,'0');return\`Jogador ${suffix}\`;`
- [x] `for` — linha 4942, coluna 5 em `sanitizeRemoteSession()` — `for(const[uid,entry]of Object.entries(value.players||{}))players[uid]={...entry,name:remotePlayerName(entry?.name,uid)};`
- [x] `if` — linha 4943, coluna 27 em `sanitizeRemoteSession()` — `clean.players=players;if(value.result){const winnerUid=String(value.result.winnerUid||''),loserUid=String(value.result.loserUid||'');clean.result={...value.result,winnerName:value.result.draw?'Empate':remotePlayerName(players[winnerUid]?.name,winnerUid),loserName:value.result.draw?'':remotePlayerName(players[loserUid]?.name,loserUid)};}return clean;`
- [x] `return` — linha 4943, coluna 343 em `sanitizeRemoteSession()` — `clean.players=players;if(value.result){const winnerUid=String(value.result.winnerUid||''),loserUid=String(value.result.loserUid||'');clean.result={...value.result,winnerName:value.result.draw?'Empate':remotePlayerName(players[winnerUid]?.name,winnerUid),loserName:value.result.draw?'':remotePlayerName(players[loserUid]?.name,loserUid)};}return clean;`
- [x] `return` — linha 4945, coluna 32 em `pendingChallenges()` — `function pendingChallenges(){return [...incomingChallenges.values()].filter(c=>c.status==='pending');}`
- [x] `return` — linha 4946, coluna 66 em `readyGameSessions()` — `function readyGameSessions(){const uid=window.OTTHOS_RTDB?.uid;return [...gameSessions.values()].filter(s=>(s.fromUid===uid||s.toUid===uid)&&s.status==='active');}`
- [x] `if` — linha 4947, coluna 35 em `closeChallengePrompt()` — `function closeChallengePrompt(){if(!els.challengePrompt)return;document.body.classList.remove('social-prompt-open');els.challengePrompt.hidden=true;els.challengePrompt.classList.remove('ready','incoming','social');promptChallengeId='';promptSessionId='';promptSocialRequestId='';els.challengePromptAccept.disabled=false;els.challengePromptDecline.disabled=false;}`
- [x] `return` — linha 4947, coluna 59 em `closeChallengePrompt()` — `function closeChallengePrompt(){if(!els.challengePrompt)return;document.body.classList.remove('social-prompt-open');els.challengePrompt.hidden=true;els.challengePrompt.classList.remove('ready','incoming','social');promptChallengeId='';promptSessionId='';promptSocialRequestId='';els.challengePromptAccept.disabled=false;els.challengePromptDecline.disabled=false;}`
- [x] `if` — linha 4948, coluna 43 em `showIncomingChallengePrompt()` — `function showIncomingChallengePrompt(c){if(!guardianCommunicationAllowed()||!c||c.status!=='pending'||!els.challengePrompt)return;document.body.classList.add('social-prompt-open');promptChallengeId=c.id;promptSessionId='';els.challengePrompt.classList.add('incoming');els.challengePrompt.classList.remove('ready','social');els.challengePromptKicker.textContent='NOVO DESAFIO';els.challengePromptTitle.textContent=\`${c.fromName||'Jogador'} desafiou você\`;els.challengePromptText.textContent=\`${multiplayerGameLabel(c.type)} • toque em Aceitar e jogar\`;els.challengePromptAccept.textContent='Aceitar e jogar';els.challengePromptDecline.textContent='Recusar';els.challengePrompt.hidden=false;}`
- [x] `return` — linha 4948, coluna 126 em `showIncomingChallengePrompt()` — `function showIncomingChallengePrompt(c){if(!guardianCommunicationAllowed()||!c||c.status!=='pending'||!els.challengePrompt)return;document.body.classList.add('social-prompt-open');promptChallengeId=c.id;promptSessionId='';els.challengePrompt.classList.add('incoming');els.challengePrompt.classList.remove('ready','social');els.challengePromptKicker.textContent='NOVO DESAFIO';els.challengePromptTitle.textContent=\`${c.fromName||'Jogador'} desafiou você\`;els.challengePromptText.textContent=\`${multiplayerGameLabel(c.type)} • toque em Aceitar e jogar\`;els.challengePromptAccept.textContent='Aceitar e jogar';els.challengePromptDecline.textContent='Recusar';els.challengePrompt.hidden=false;}`
- [x] `if` — linha 4949, coluna 38 em `showReadySessionPrompt()` — `function showReadySessionPrompt(s){if(!guardianCommunicationAllowed()||!s||s.status!=='active'||!els.challengePrompt)return;document.body.classList.add('social-prompt-open');const uid=window.OTTHOS_RTDB?.uid,mine=s.players?.[uid];if(mine?.finished)return;promptSessionId=s.id;promptChallengeId='';els.challengePrompt.classList.add('ready');els.challengePrompt.classList.remove('incoming','social');els.challengePromptKicker.textContent='PARTIDA PRONTA';els.challengePromptTitle.textContent=\`Duelo de ${multiplayerGameLabel(s.type)}\`;els.challengePromptText.textContent=\`Contra ${sessionOpponentName(s)} • os dois jogarão as mesmas 5 atividades\`;els.challengePromptAccept.textContent='Jogar agora';els.challengePromptDecline.textContent='Depois';els.challengePrompt.hidden=false;}`
- [x] `return` — linha 4949, coluna 120 em `showReadySessionPrompt()` — `function showReadySessionPrompt(s){if(!guardianCommunicationAllowed()||!s||s.status!=='active'||!els.challengePrompt)return;document.body.classList.add('social-prompt-open');const uid=window.OTTHOS_RTDB?.uid,mine=s.players?.[uid];if(mine?.finished)return;promptSessionId=s.id;promptChallengeId='';els.challengePrompt.classList.add('ready');els.challengePrompt.classList.remove('incoming','social');els.challengePromptKicker.textContent='PARTIDA PRONTA';els.challengePromptTitle.textContent=\`Duelo de ${multiplayerGameLabel(s.type)}\`;els.challengePromptText.textContent=\`Contra ${sessionOpponentName(s)} • os dois jogarão as mesmas 5 atividades\`;els.challengePromptAccept.textContent='Jogar agora';els.challengePromptDecline.textContent='Depois';els.challengePrompt.hidden=false;}`
- [x] `if` — linha 4949, coluna 233 em `showReadySessionPrompt()` — `function showReadySessionPrompt(s){if(!guardianCommunicationAllowed()||!s||s.status!=='active'||!els.challengePrompt)return;document.body.classList.add('social-prompt-open');const uid=window.OTTHOS_RTDB?.uid,mine=s.players?.[uid];if(mine?.finished)return;promptSessionId=s.id;promptChallengeId='';els.challengePrompt.classList.add('ready');els.challengePrompt.classList.remove('incoming','social');els.challengePromptKicker.textContent='PARTIDA PRONTA';els.challengePromptTitle.textContent=\`Duelo de ${multiplayerGameLabel(s.type)}\`;els.challengePromptText.textContent=\`Contra ${sessionOpponentName(s)} • os dois jogarão as mesmas 5 atividades\`;els.challengePromptAccept.textContent='Jogar agora';els.challengePromptDecline.textContent='Depois';els.challengePrompt.hidden=false;}`
- [x] `return` — linha 4949, coluna 251 em `showReadySessionPrompt()` — `function showReadySessionPrompt(s){if(!guardianCommunicationAllowed()||!s||s.status!=='active'||!els.challengePrompt)return;document.body.classList.add('social-prompt-open');const uid=window.OTTHOS_RTDB?.uid,mine=s.players?.[uid];if(mine?.finished)return;promptSessionId=s.id;promptChallengeId='';els.challengePrompt.classList.add('ready');els.challengePrompt.classList.remove('incoming','social');els.challengePromptKicker.textContent='PARTIDA PRONTA';els.challengePromptTitle.textContent=\`Duelo de ${multiplayerGameLabel(s.type)}\`;els.challengePromptText.textContent=\`Contra ${sessionOpponentName(s)} • os dois jogarão as mesmas 5 atividades\`;els.challengePromptAccept.textContent='Jogar agora';els.challengePromptDecline.textContent='Depois';els.challengePrompt.hidden=false;}`
- [x] `return` — linha 4951, coluna 36 em `socialActionLabel()` — `function socialActionLabel(type){return SOCIAL_ACTION_LABELS[type]||'interagir';}`
- [x] `return` — linha 4952, coluna 35 em `socialRequestPending()` — `function socialRequestPending(){return[...incomingSocialRequests.values()].filter(r=>r.status==='pending'&&Number(r.expiresAt||0)>Date.now());}`
- [x] `if` — linha 4954, coluna 5 em `showIncomingSocialRequest()` — `if(!guardianCommunicationAllowed()||!request||request.status!=='pending'||Number(request.expiresAt||0)<=Date.now()||!els.challengePrompt)return;`
- [x] `return` — linha 4954, coluna 142 em `showIncomingSocialRequest()` — `if(!guardianCommunicationAllowed()||!request||request.status!=='pending'||Number(request.expiresAt||0)<=Date.now()||!els.challengePrompt)return;`
- [x] `if` — linha 4959, coluna 5 em `showIncomingSocialRequest()` — `if(!guardianCommunicationAllowed()){toast('A interação online está temporariamente indisponível.','warn',2800);return false;}`
- [x] `return` — linha 4959, coluna 116 em `showIncomingSocialRequest()` — `if(!guardianCommunicationAllowed()){toast('A interação online está temporariamente indisponível.','warn',2800);return false;}`
- [x] `if` — linha 4960, coluna 5 em `showIncomingSocialRequest()` — `if(actionType==='boatPassenger'&&(!player.boating||player.boat.passengerOf)){toast('Somente o motorista do barco pode convidar um passageiro.','warn',2500);return false;}`
- [x] `return` — linha 4960, coluna 161 em `showIncomingSocialRequest()` — `if(actionType==='boatPassenger'&&(!player.boating||player.boat.passengerOf)){toast('Somente o motorista do barco pode convidar um passageiro.','warn',2500);return false;}`
- [x] `if` — linha 4961, coluna 5 em `showIncomingSocialRequest()` — `if(actionType==='boatPassenger'&&(player.boat.passengerUid||player.boat.passengerBotId)){toast('Este barco já tem um passageiro.','warn',2300);return false;}`
- [x] `return` — linha 4961, coluna 148 em `showIncomingSocialRequest()` — `if(actionType==='boatPassenger'&&(player.boat.passengerUid||player.boat.passengerBotId)){toast('Este barco já tem um passageiro.','warn',2300);return false;}`
- [x] `if` — linha 4962, coluna 5 em `showIncomingSocialRequest()` — `if(actionType==='vehiclePassenger'&&(!player.vehicle||player.car.passengerOf)){toast('Somente o motorista do carro pode convidar um passageiro.','warn',2500);return false;}`
- [x] `return` — linha 4962, coluna 163 em `showIncomingSocialRequest()` — `if(actionType==='vehiclePassenger'&&(!player.vehicle||player.car.passengerOf)){toast('Somente o motorista do carro pode convidar um passageiro.','warn',2500);return false;}`
- [x] `if` — linha 4963, coluna 5 em `showIncomingSocialRequest()` — `if(actionType==='vehiclePassenger'&&(player.car.passengerUid||player.car.passengerBotId)){toast('Este carro já tem um passageiro.','warn',2300);return false;}`
- [x] `return` — linha 4963, coluna 149 em `showIncomingSocialRequest()` — `if(actionType==='vehiclePassenger'&&(player.car.passengerUid||player.car.passengerBotId)){toast('Este carro já tem um passageiro.','warn',2300);return false;}`
- [x] `if` — linha 4964, coluna 103 em `showIncomingSocialRequest()` — `const result=await window.OTTHOS_RTDB?.sendSocialRequest?.(targetUid,actionType,targetName,extra);if(result?.ok){state.multiplayerRequests.lastSentAt=Date.now();saveState();toast(\`Convite enviado para ${targetName}.\`,'good',2300);return true;}toast(result?.error||'Não foi possível enviar o convite.','warn',2600);return false;`
- [x] `return` — linha 4964, coluna 235 em `showIncomingSocialRequest()` — `const result=await window.OTTHOS_RTDB?.sendSocialRequest?.(targetUid,actionType,targetName,extra);if(result?.ok){state.multiplayerRequests.lastSentAt=Date.now();saveState();toast(\`Convite enviado para ${targetName}.\`,'good',2300);return true;}toast(result?.error||'Não foi possível enviar o convite.','warn',2600);return false;`
- [x] `return` — linha 4964, coluna 319 em `showIncomingSocialRequest()` — `const result=await window.OTTHOS_RTDB?.sendSocialRequest?.(targetUid,actionType,targetName,extra);if(result?.ok){state.multiplayerRequests.lastSentAt=Date.now();saveState();toast(\`Convite enviado para ${targetName}.\`,'good',2300);return true;}toast(result?.error||'Não foi possível enviar o convite.','warn',2600);return false;`
- [x] `if` — linha 4967, coluna 51 em `applyAcceptedSocialAction()` — `const rewardKey=String(context.requestId||'');if(rewardKey&&state.multiplayerRequests.completed.includes(rewardKey))return;`
- [x] `return` — linha 4967, coluna 121 em `applyAcceptedSocialAction()` — `const rewardKey=String(context.requestId||'');if(rewardKey&&state.multiplayerRequests.completed.includes(rewardKey))return;`
- [x] `if` — linha 4968, coluna 5 em `applyAcceptedSocialAction()` — `if(['dance','play','highfive','hug','selfie'].includes(actionType)){triggerEmote(actionType);if(actionType==='play')state.needs.fun=clamp(state.needs.fun+10,0,100);}`
- [x] `if` — linha 4968, coluna 98 em `applyAcceptedSocialAction()` — `if(['dance','play','highfive','hug','selfie'].includes(actionType)){triggerEmote(actionType);if(actionType==='play')state.needs.fun=clamp(state.needs.fun+10,0,100);}`
- [x] `else` — linha 4969, coluna 5 em `applyAcceptedSocialAction()` — `else if(actionType==='vehiclePassenger'){if(context.role==='passenger'||context.incoming)enterVehicleAsPassenger(context.fromUid||context.partnerUid,context.vehicleId||'');else{player.car.passengerUid=context.partnerUid||'';toast(\`${context.partnerName||'Seu amigo'} entrou como passageiro.\`,'good',2600);}}`
- [x] `if` — linha 4969, coluna 10 em `applyAcceptedSocialAction()` — `else if(actionType==='vehiclePassenger'){if(context.role==='passenger'||context.incoming)enterVehicleAsPassenger(context.fromUid||context.partnerUid,context.vehicleId||'');else{player.car.passengerUid=context.partnerUid||'';toast(\`${context.partnerName||'Seu amigo'} entrou como passageiro.\`,'good',2600);}}`
- [x] `if` — linha 4969, coluna 46 em `applyAcceptedSocialAction()` — `else if(actionType==='vehiclePassenger'){if(context.role==='passenger'||context.incoming)enterVehicleAsPassenger(context.fromUid||context.partnerUid,context.vehicleId||'');else{player.car.passengerUid=context.partnerUid||'';toast(\`${context.partnerName||'Seu amigo'} entrou como passageiro.\`,'good',2600);}}`
- [x] `else` — linha 4969, coluna 177 em `applyAcceptedSocialAction()` — `else if(actionType==='vehiclePassenger'){if(context.role==='passenger'||context.incoming)enterVehicleAsPassenger(context.fromUid||context.partnerUid,context.vehicleId||'');else{player.car.passengerUid=context.partnerUid||'';toast(\`${context.partnerName||'Seu amigo'} entrou como passageiro.\`,'good',2600);}}`
- [x] `else` — linha 4970, coluna 5 em `applyAcceptedSocialAction()` — `else if(actionType==='boatPassenger'){if(context.role==='passenger'||context.incoming)enterBoatAsPassenger(context.fromUid||context.partnerUid);else{player.boat.passengerUid=context.partnerUid||'';toast(\`${context.partnerName||'Seu amigo'} entrou como passageiro.\`,'good',2600);}}`
- [x] `if` — linha 4970, coluna 10 em `applyAcceptedSocialAction()` — `else if(actionType==='boatPassenger'){if(context.role==='passenger'||context.incoming)enterBoatAsPassenger(context.fromUid||context.partnerUid);else{player.boat.passengerUid=context.partnerUid||'';toast(\`${context.partnerName||'Seu amigo'} entrou como passageiro.\`,'good',2600);}}`
- [x] `if` — linha 4970, coluna 43 em `applyAcceptedSocialAction()` — `else if(actionType==='boatPassenger'){if(context.role==='passenger'||context.incoming)enterBoatAsPassenger(context.fromUid||context.partnerUid);else{player.boat.passengerUid=context.partnerUid||'';toast(\`${context.partnerName||'Seu amigo'} entrou como passageiro.\`,'good',2600);}}`
- [x] `else` — linha 4970, coluna 149 em `applyAcceptedSocialAction()` — `else if(actionType==='boatPassenger'){if(context.role==='passenger'||context.incoming)enterBoatAsPassenger(context.fromUid||context.partnerUid);else{player.boat.passengerUid=context.partnerUid||'';toast(\`${context.partnerName||'Seu amigo'} entrou como passageiro.\`,'good',2600);}}`
- [x] `else` — linha 4971, coluna 5 em `applyAcceptedSocialAction()` — `else if(actionType==='fishTogether'){startFishing(player.boating?'boat':'shore',{cooperative:true,requestId:rewardKey,partnerName:context.partnerName||context.fromName||'amigo'});}`
- [x] `if` — linha 4971, coluna 10 em `applyAcceptedSocialAction()` — `else if(actionType==='fishTogether'){startFishing(player.boating?'boat':'shore',{cooperative:true,requestId:rewardKey,partnerName:context.partnerName||context.fromName||'amigo'});}`
- [x] `else` — linha 4972, coluna 5 em `applyAcceptedSocialAction()` — `else if(actionType==='campfireJoin'){openNearestCampfire();}`
- [x] `if` — linha 4972, coluna 10 em `applyAcceptedSocialAction()` — `else if(actionType==='campfireJoin'){openNearestCampfire();}`
- [x] `else` — linha 4973, coluna 5 em `applyAcceptedSocialAction()` — `else if(actionType==='huntTogether'){startHunting({cooperative:true,requestId:rewardKey,partnerName:context.partnerName||context.fromName||'amigo'});}`
- [x] `if` — linha 4973, coluna 10 em `applyAcceptedSocialAction()` — `else if(actionType==='huntTogether'){startHunting({cooperative:true,requestId:rewardKey,partnerName:context.partnerName||context.fromName||'amigo'});}`
- [x] `if` — linha 4974, coluna 5 em `applyAcceptedSocialAction()` — `if(rewardKey){state.multiplayerRequests.completed.push(rewardKey);state.multiplayerRequests.completed=state.multiplayerRequests.completed.slice(-80);}saveState();`
- [x] `if` — linha 4977, coluna 50 em `applyAcceptedSocialAction()` — `const request=incomingSocialRequests.get(id);if(!request)return;els.challengePromptAccept.disabled=true;els.challengePromptDecline.disabled=true;const result=await window.OTTHOS_RTDB?.respondSocialRequest?.(id,'accepted');`
- [x] `return` — linha 4977, coluna 62 em `applyAcceptedSocialAction()` — `const request=incomingSocialRequests.get(id);if(!request)return;els.challengePromptAccept.disabled=true;els.challengePromptDecline.disabled=true;const result=await window.OTTHOS_RTDB?.respondSocialRequest?.(id,'accepted');`
- [x] `if` — linha 4978, coluna 5 em `applyAcceptedSocialAction()` — `if(result?.ok){closeChallengePrompt();applyAcceptedSocialAction(request.actionType,{incoming:true,role:['boatPassenger','vehiclePassenger'].includes(request.actionType)?'passenger':'participant',requestId:id,fromUid:request.fromUid,fromName:request.fromName,partnerName:request.fromName,vehicleId:request.vehicleId||request.extra?.vehicleId||''});toast(\`Convite de ${request.fromName||'Jogador'} aceito.\`,'good',2200);setTimeout(()=>window.OTTHOS_RTDB?.completeSocialRequest?.(id),1800);}else{closeChallengePrompt();toast(result?.reason||result?.error||'O convite não pôde ser aceito.','warn',2600);}`
- [x] `else` — linha 4978, coluna 493 em `applyAcceptedSocialAction()` — `if(result?.ok){closeChallengePrompt();applyAcceptedSocialAction(request.actionType,{incoming:true,role:['boatPassenger','vehiclePassenger'].includes(request.actionType)?'passenger':'participant',requestId:id,fromUid:request.fromUid,fromName:request.fromName,partnerName:request.fromName,vehicleId:request.vehicleId||request.extra?.vehicleId||''});toast(\`Convite de ${request.fromName||'Jogador'} aceito.\`,'good',2200);setTimeout(()=>window.OTTHOS_RTDB?.completeSocialRequest?.(id),1800);}else{closeChallengePrompt();toast(result?.reason||result?.error||'O convite não pôde ser aceito.','warn',2600);}`
- [x] `if` — linha 4983, coluna 54 em `applyAcceptedSocialAction()` — `async function launchSessionWithCountdown(session){if(!session)return;closeChallengePrompt();closeModal();let count=3;const draw=()=>openModal(\`Duelo: ${multiplayerGameLabel(session.type)}\`,\`<div class="duel-countdown"><small>Contra ${escapeHtml(sessionOpponentName(session))}</small><b>${count}</b><span>Prepare-se!</span></div>\`,root=>{});draw();const timer=setInterval(()=>{count--;if(count<=0){clearInterval(timer);closeModal();startMultiplayerEducationGame(session);}else draw();},620);}`
- [x] `return` — linha 4983, coluna 66 em `applyAcceptedSocialAction()` — `async function launchSessionWithCountdown(session){if(!session)return;closeChallengePrompt();closeModal();let count=3;const draw=()=>openModal(\`Duelo: ${multiplayerGameLabel(session.type)}\`,\`<div class="duel-countdown"><small>Contra ${escapeHtml(sessionOpponentName(session))}</small><b>${count}</b><span>Prepare-se!</span></div>\`,root=>{});draw();const timer=setInterval(()=>{count--;if(count<=0){clearInterval(timer);closeModal();startMultiplayerEducationGame(session);}else draw();},620);}`
- [x] `if` — linha 4983, coluna 388 em `applyAcceptedSocialAction()` — `async function launchSessionWithCountdown(session){if(!session)return;closeChallengePrompt();closeModal();let count=3;const draw=()=>openModal(\`Duelo: ${multiplayerGameLabel(session.type)}\`,\`<div class="duel-countdown"><small>Contra ${escapeHtml(sessionOpponentName(session))}</small><b>${count}</b><span>Prepare-se!</span></div>\`,root=>{});draw();const timer=setInterval(()=>{count--;if(count<=0){clearInterval(timer);closeModal();startMultiplayerEducationGame(session);}else draw();},620);}`
- [x] `else` — linha 4983, coluna 475 em `applyAcceptedSocialAction()` — `async function launchSessionWithCountdown(session){if(!session)return;closeChallengePrompt();closeModal();let count=3;const draw=()=>openModal(\`Duelo: ${multiplayerGameLabel(session.type)}\`,\`<div class="duel-countdown"><small>Contra ${escapeHtml(sessionOpponentName(session))}</small><b>${count}</b><span>Prepare-se!</span></div>\`,root=>{});draw();const timer=setInterval(()=>{count--;if(count<=0){clearInterval(timer);closeModal();startMultiplayerEducationGame(session);}else draw();},620);}`
- [x] `if` — linha 4984, coluna 277 em `updateOnlineAttention()` — `function updateOnlineAttention(){const count=guardianCommunicationAllowed()?pendingChallenges().length+socialRequestPending().length+readyGameSessions().filter(s=>!s.players?.[window.OTTHOS_RTDB?.uid]?.finished).length:0;els.onlineBtn?.classList.toggle('attention',count>0);if(els.onlineBtn){const span=$('span',els.onlineBtn);if(span)span.textContent=count?\`Online ${count}\`:'Online';}}`
- [x] `if` — linha 4984, coluna 330 em `updateOnlineAttention()` — `function updateOnlineAttention(){const count=guardianCommunicationAllowed()?pendingChallenges().length+socialRequestPending().length+readyGameSessions().filter(s=>!s.players?.[window.OTTHOS_RTDB?.uid]?.finished).length:0;els.onlineBtn?.classList.toggle('attention',count>0);if(els.onlineBtn){const span=$('span',els.onlineBtn);if(span)span.textContent=count?\`Online ${count}\`:'Online';}}`
- [x] `return` — linha 4985, coluna 64 em `challengeInboxHtml()` — `function challengeInboxHtml(){const list=pendingChallenges();return list.length?\`<div class="challenge-inbox">${list.map(c=>\`<article class="challenge-card"><div><b>⚔️ ${escapeHtml(c.fromName||'Jogador')}</b><span>Desafio: ${multiplayerGameLabel(c.type)}</span></div><div><button class="accept" data-accept-challenge="${c.id}">Aceitar e jogar</button><button data-decline-challenge="${c.id}">Recusar</button></div></article>\`).join('')}</div>\`:'<p class="empty-online">Nenhum convite educativo pendente.</p>';}`
- [x] `return` — linha 4986, coluna 71 em `socialRequestInboxHtml()` — `function socialRequestInboxHtml(){const list=socialRequestPending();return list.length?\`<div class="challenge-inbox social-inbox">${list.map(r=>\`<article class="challenge-card social"><div><b>🤝 ${escapeHtml(r.fromName||'Jogador')}</b><span>Quer ${escapeHtml(socialActionLabel(r.actionType))}</span></div><div><button class="accept" data-accept-social="${r.id}">Aceitar</button><button data-decline-social="${r.id}">Recusar</button></div></article>\`).join('')}</div>\`:'<p class="empty-online">Nenhuma solicitação social pendente.</p>';}`
- [x] `return` — linha 4987, coluna 70 em `completedGameSessions()` — `function completedGameSessions(){const uid=window.OTTHOS_RTDB?.uid;return [...gameSessions.values()].filter(s=>s.status==='completed'&&s.result&&(s.fromUid===uid||s.toUid===uid)).sort((a,b)=>Number(b.completedAt||b.result?.completedAt||0)-Number(a.completedAt||a.result?.completedAt||0)).slice(0,20);}`
- [x] `if` — linha 4988, coluna 41 em `rememberMatchResult()` — `function rememberMatchResult(session){if(!session?.result)return null;const list=state.learning.matchHistory||(state.learning.matchHistory=[]),existing=list.find(x=>x.id===session.id);if(existing)return existing;const uid=window.OTTHOS_RTDB?.uid,r=session.result,mine=session.players?.[uid]||{},other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1]||{};const entry={id:session.id,type:session.type,opponent:sessionOpponentName(session),myScore:Number(mine.score||0),otherScore:Number(other.score||0),won:!r.draw&&r.winnerUid===uid,draw:!!r.draw,winnerName:r.winnerName||'',completedAt:Number(session.completedAt||r.completedAt||Date.now()),rewarded:false};list.unshift(entry);state.learning.matchHistory=list.slice(0,20);return entry;}`
- [x] `return` — linha 4988, coluna 61 em `rememberMatchResult()` — `function rememberMatchResult(session){if(!session?.result)return null;const list=state.learning.matchHistory||(state.learning.matchHistory=[]),existing=list.find(x=>x.id===session.id);if(existing)return existing;const uid=window.OTTHOS_RTDB?.uid,r=session.result,mine=session.players?.[uid]||{},other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1]||{};const entry={id:session.id,type:session.type,opponent:sessionOpponentName(session),myScore:Number(mine.score||0),otherScore:Number(other.score||0),won:!r.draw&&r.winnerUid===uid,draw:!!r.draw,winnerName:r.winnerName||'',completedAt:Number(session.completedAt||r.completedAt||Date.now()),rewarded:false};list.unshift(entry);state.learning.matchHistory=list.slice(0,20);return entry;}`
- [x] `if` — linha 4988, coluna 187 em `rememberMatchResult()` — `function rememberMatchResult(session){if(!session?.result)return null;const list=state.learning.matchHistory||(state.learning.matchHistory=[]),existing=list.find(x=>x.id===session.id);if(existing)return existing;const uid=window.OTTHOS_RTDB?.uid,r=session.result,mine=session.players?.[uid]||{},other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1]||{};const entry={id:session.id,type:session.type,opponent:sessionOpponentName(session),myScore:Number(mine.score||0),otherScore:Number(other.score||0),won:!r.draw&&r.winnerUid===uid,draw:!!r.draw,winnerName:r.winnerName||'',completedAt:Number(session.completedAt||r.completedAt||Date.now()),rewarded:false};list.unshift(entry);state.learning.matchHistory=list.slice(0,20);return entry;}`
- [x] `return` — linha 4988, coluna 199 em `rememberMatchResult()` — `function rememberMatchResult(session){if(!session?.result)return null;const list=state.learning.matchHistory||(state.learning.matchHistory=[]),existing=list.find(x=>x.id===session.id);if(existing)return existing;const uid=window.OTTHOS_RTDB?.uid,r=session.result,mine=session.players?.[uid]||{},other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1]||{};const entry={id:session.id,type:session.type,opponent:sessionOpponentName(session),myScore:Number(mine.score||0),otherScore:Number(other.score||0),won:!r.draw&&r.winnerUid===uid,draw:!!r.draw,winnerName:r.winnerName||'',completedAt:Number(session.completedAt||r.completedAt||Date.now()),rewarded:false};list.unshift(entry);state.learning.matchHistory=list.slice(0,20);return entry;}`
- [x] `return` — linha 4988, coluna 740 em `rememberMatchResult()` — `function rememberMatchResult(session){if(!session?.result)return null;const list=state.learning.matchHistory||(state.learning.matchHistory=[]),existing=list.find(x=>x.id===session.id);if(existing)return existing;const uid=window.OTTHOS_RTDB?.uid,r=session.result,mine=session.players?.[uid]||{},other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1]||{};const entry={id:session.id,type:session.type,opponent:sessionOpponentName(session),myScore:Number(mine.score||0),otherScore:Number(other.score||0),won:!r.draw&&r.winnerUid===uid,draw:!!r.draw,winnerName:r.winnerName||'',completedAt:Number(session.completedAt||r.completedAt||Date.now()),rewarded:false};list.unshift(entry);state.learning.matchHistory=list.slice(0,20);return entry;}`
- [x] `return` — linha 4989, coluna 69 em `duelHistoryHtml()` — `function duelHistoryHtml(){const sessions=completedGameSessions();return sessions.length?\`<div class="duel-history">${sessions.map(s=>{const r=s.result,uid=window.OTTHOS_RTDB?.uid,mine=s.players?.[uid]||{},other=Object.entries(s.players||{}).find(([id])=>id!==uid)?.[1]||{},won=!r.draw&&r.winnerUid===uid;return\`<article class="duel-history-card ${won?'won':r.draw?'draw':'lost'}"><span>${r.draw?'🤝':won?'🏆':'🎮'}</span><div><b>${r.draw?'Empate':\`Vencedor: ${escapeHtml(r.winnerName||'Jogador')}\`}</b><small>${multiplayerGameLabel(s.type)} • ${Number(mine.score||0)} × ${Number(other.score||0)} contra ${escapeHtml(sessionOpponentName(s))}</small></div></article>\`}).join('')}</div>\`:'<p class="empty-online">Nenhum duelo concluído ainda.</p>';}`
- [x] `return` — linha 4989, coluna 308 em `duelHistoryHtml()` — `function duelHistoryHtml(){const sessions=completedGameSessions();return sessions.length?\`<div class="duel-history">${sessions.map(s=>{const r=s.result,uid=window.OTTHOS_RTDB?.uid,mine=s.players?.[uid]||{},other=Object.entries(s.players||{}).find(([id])=>id!==uid)?.[1]||{},won=!r.draw&&r.winnerUid===uid;return\`<article class="duel-history-card ${won?'won':r.draw?'draw':'lost'}"><span>${r.draw?'🤝':won?'🏆':'🎮'}</span><div><b>${r.draw?'Empate':\`Vencedor: ${escapeHtml(r.winnerName||'Jogador')}\`}</b><small>${multiplayerGameLabel(s.type)} • ${Number(mine.score||0)} × ${Number(other.score||0)} contra ${escapeHtml(sessionOpponentName(s))}</small></div></article>\`}).join('')}</div>\`:'<p class="empty-online">Nenhum duelo concluído ainda.</p>';}`
- [x] `return` — linha 4990, coluna 92 em `activeSessionsHtml()` — `function activeSessionsHtml(){const uid=window.OTTHOS_RTDB?.uid,list=readyGameSessions();return list.length?\`<div class="challenge-inbox">${list.map(s=>{const other=s.fromUid===uid?s.toName||remotePresence.get(s.toUid)?.name||'Adversário':s.fromName||'Adversário',mine=s.players?.[uid];return\`<article class="challenge-card ready"><div><b>🎮 ${multiplayerGameLabel(s.type)}</b><span>contra ${escapeHtml(other)}</span></div><button class="accept" data-play-session="${s.id}" ${mine?.finished?'disabled':''}>${mine?.finished?'Concluído':'Jogar agora'}</button></article>\`}).join('')}</div>\`:'';}`
- [x] `return` — linha 4990, coluna 289 em `activeSessionsHtml()` — `function activeSessionsHtml(){const uid=window.OTTHOS_RTDB?.uid,list=readyGameSessions();return list.length?\`<div class="challenge-inbox">${list.map(s=>{const other=s.fromUid===uid?s.toName||remotePresence.get(s.toUid)?.name||'Adversário':s.fromName||'Adversário',mine=s.players?.[uid];return\`<article class="challenge-card ready"><div><b>🎮 ${multiplayerGameLabel(s.type)}</b><span>contra ${escapeHtml(other)}</span></div><button class="accept" data-play-session="${s.id}" ${mine?.finished?'disabled':''}>${mine?.finished?'Concluído':'Jogar agora'}</button></article>\`}).join('')}</div>\`:'';}`
- [x] `if` — linha 4991, coluna 636 em `bindChallengeCards()` — `function bindChallengeCards(root=els.modalBody){$$('[data-accept-challenge]',root).forEach(btn=>btn.onclick=()=>acceptIncomingChallenge(btn.dataset.acceptChallenge));$$('[data-decline-challenge]',root).forEach(btn=>btn.onclick=()=>declineIncomingChallenge(btn.dataset.declineChallenge));$$('[data-accept-social]',root).forEach(btn=>btn.onclick=()=>acceptIncomingSocialRequest(btn.dataset.acceptSocial));$$('[data-decline-social]',root).forEach(btn=>btn.onclick=()=>declineIncomingSocialRequest(btn.dataset.declineSocial));$$('[data-play-session]',root).forEach(btn=>btn.onclick=()=>{const s=gameSessions.get(btn.dataset.playSession);if(s)launchSessionWithCountdown(s);});}`
- [x] `if` — linha 4992, coluna 126 em `bindChallengeCards()` — `async function acceptIncomingChallenge(id){const c=incomingChallenges.get(id)||window.OTTHOS_RTDB?.getChallenges?.()?.[id];if(!c){toast('Convite não encontrado. Abra Online e tente novamente.','warn');return;}els.challengePromptAccept.disabled=true;els.challengePromptDecline.disabled=true;els.challengePromptAccept.textContent='Conectando...';const result=await window.OTTHOS_RTDB?.respondGameChallenge?.(id,true);if(!result?.ok){els.challengePromptAccept.disabled=false;els.challengePromptDecline.disabled=false;els.challengePromptAccept.textContent='Aceitar e jogar';toast(result?.error||'Não foi possível aceitar.','warn',3000);return;}c.status='accepted';incomingChallenges.set(id,c);const session=result.session||await window.OTTHOS_RTDB?.getGameSession?.(id);if(session){gameSessions.set(id,{id,...session});refreshOpenSocialHub();toast(\`Desafio aceito: ${multiplayerGameLabel(c.type)}\`,'good');launchSessionWithCountdown({id,...session});}else{closeChallengePrompt();toast('Desafio aceito. To`
- [x] `return` — linha 4992, coluna 204 em `bindChallengeCards()` — `async function acceptIncomingChallenge(id){const c=incomingChallenges.get(id)||window.OTTHOS_RTDB?.getChallenges?.()?.[id];if(!c){toast('Convite não encontrado. Abra Online e tente novamente.','warn');return;}els.challengePromptAccept.disabled=true;els.challengePromptDecline.disabled=true;els.challengePromptAccept.textContent='Conectando...';const result=await window.OTTHOS_RTDB?.respondGameChallenge?.(id,true);if(!result?.ok){els.challengePromptAccept.disabled=false;els.challengePromptDecline.disabled=false;els.challengePromptAccept.textContent='Aceitar e jogar';toast(result?.error||'Não foi possível aceitar.','warn',3000);return;}c.status='accepted';incomingChallenges.set(id,c);const session=result.session||await window.OTTHOS_RTDB?.getGameSession?.(id);if(session){gameSessions.set(id,{id,...session});refreshOpenSocialHub();toast(\`Desafio aceito: ${multiplayerGameLabel(c.type)}\`,'good');launchSessionWithCountdown({id,...session});}else{closeChallengePrompt();toast('Desafio aceito. To`
- [x] `if` — linha 4992, coluna 418 em `bindChallengeCards()` — `async function acceptIncomingChallenge(id){const c=incomingChallenges.get(id)||window.OTTHOS_RTDB?.getChallenges?.()?.[id];if(!c){toast('Convite não encontrado. Abra Online e tente novamente.','warn');return;}els.challengePromptAccept.disabled=true;els.challengePromptDecline.disabled=true;els.challengePromptAccept.textContent='Conectando...';const result=await window.OTTHOS_RTDB?.respondGameChallenge?.(id,true);if(!result?.ok){els.challengePromptAccept.disabled=false;els.challengePromptDecline.disabled=false;els.challengePromptAccept.textContent='Aceitar e jogar';toast(result?.error||'Não foi possível aceitar.','warn',3000);return;}c.status='accepted';incomingChallenges.set(id,c);const session=result.session||await window.OTTHOS_RTDB?.getGameSession?.(id);if(session){gameSessions.set(id,{id,...session});refreshOpenSocialHub();toast(\`Desafio aceito: ${multiplayerGameLabel(c.type)}\`,'good');launchSessionWithCountdown({id,...session});}else{closeChallengePrompt();toast('Desafio aceito. To`
- [x] `return` — linha 4992, coluna 635 em `bindChallengeCards()` — `async function acceptIncomingChallenge(id){const c=incomingChallenges.get(id)||window.OTTHOS_RTDB?.getChallenges?.()?.[id];if(!c){toast('Convite não encontrado. Abra Online e tente novamente.','warn');return;}els.challengePromptAccept.disabled=true;els.challengePromptDecline.disabled=true;els.challengePromptAccept.textContent='Conectando...';const result=await window.OTTHOS_RTDB?.respondGameChallenge?.(id,true);if(!result?.ok){els.challengePromptAccept.disabled=false;els.challengePromptDecline.disabled=false;els.challengePromptAccept.textContent='Aceitar e jogar';toast(result?.error||'Não foi possível aceitar.','warn',3000);return;}c.status='accepted';incomingChallenges.set(id,c);const session=result.session||await window.OTTHOS_RTDB?.getGameSession?.(id);if(session){gameSessions.set(id,{id,...session});refreshOpenSocialHub();toast(\`Desafio aceito: ${multiplayerGameLabel(c.type)}\`,'good');launchSessionWithCountdown({id,...session});}else{closeChallengePrompt();toast('Desafio aceito. To`
- [x] `if` — linha 4992, coluna 769 em `bindChallengeCards()` — `async function acceptIncomingChallenge(id){const c=incomingChallenges.get(id)||window.OTTHOS_RTDB?.getChallenges?.()?.[id];if(!c){toast('Convite não encontrado. Abra Online e tente novamente.','warn');return;}els.challengePromptAccept.disabled=true;els.challengePromptDecline.disabled=true;els.challengePromptAccept.textContent='Conectando...';const result=await window.OTTHOS_RTDB?.respondGameChallenge?.(id,true);if(!result?.ok){els.challengePromptAccept.disabled=false;els.challengePromptDecline.disabled=false;els.challengePromptAccept.textContent='Aceitar e jogar';toast(result?.error||'Não foi possível aceitar.','warn',3000);return;}c.status='accepted';incomingChallenges.set(id,c);const session=result.session||await window.OTTHOS_RTDB?.getGameSession?.(id);if(session){gameSessions.set(id,{id,...session});refreshOpenSocialHub();toast(\`Desafio aceito: ${multiplayerGameLabel(c.type)}\`,'good');launchSessionWithCountdown({id,...session});}else{closeChallengePrompt();toast('Desafio aceito. To`
- [x] `else` — linha 4992, coluna 950 em `bindChallengeCards()` — `async function acceptIncomingChallenge(id){const c=incomingChallenges.get(id)||window.OTTHOS_RTDB?.getChallenges?.()?.[id];if(!c){toast('Convite não encontrado. Abra Online e tente novamente.','warn');return;}els.challengePromptAccept.disabled=true;els.challengePromptDecline.disabled=true;els.challengePromptAccept.textContent='Conectando...';const result=await window.OTTHOS_RTDB?.respondGameChallenge?.(id,true);if(!result?.ok){els.challengePromptAccept.disabled=false;els.challengePromptDecline.disabled=false;els.challengePromptAccept.textContent='Aceitar e jogar';toast(result?.error||'Não foi possível aceitar.','warn',3000);return;}c.status='accepted';incomingChallenges.set(id,c);const session=result.session||await window.OTTHOS_RTDB?.getGameSession?.(id);if(session){gameSessions.set(id,{id,...session});refreshOpenSocialHub();toast(\`Desafio aceito: ${multiplayerGameLabel(c.type)}\`,'good');launchSessionWithCountdown({id,...session});}else{closeChallengePrompt();toast('Desafio aceito. To`
- [x] `if` — linha 4993, coluna 82 em `bindChallengeCards()` — `async function declineIncomingChallenge(id){const c=incomingChallenges.get(id);if(!c)return;els.challengePromptAccept.disabled=true;els.challengePromptDecline.disabled=true;await window.OTTHOS_RTDB?.respondGameChallenge?.(id,false);c.status='declined';incomingChallenges.set(id,c);closeChallengePrompt();refreshOpenSocialHub();updateOnlineAttention();}`
- [x] `return` — linha 4993, coluna 88 em `bindChallengeCards()` — `async function declineIncomingChallenge(id){const c=incomingChallenges.get(id);if(!c)return;els.challengePromptAccept.disabled=true;els.challengePromptDecline.disabled=true;await window.OTTHOS_RTDB?.respondGameChallenge?.(id,false);c.status='declined';incomingChallenges.set(id,c);closeChallengePrompt();refreshOpenSocialHub();updateOnlineAttention();}`
- [x] `for` — linha 4994, coluna 54 em `highestUnlockedLevel()` — `function highestUnlockedLevel(subject){let value=1;for(let level=2;level<=6;level++){if(subjectUnlocked(subject,level))value=level;}return value;}`
- [x] `if` — linha 4994, coluna 88 em `highestUnlockedLevel()` — `function highestUnlockedLevel(subject){let value=1;for(let level=2;level<=6;level++){if(subjectUnlocked(subject,level))value=level;}return value;}`
- [x] `return` — linha 4994, coluna 135 em `highestUnlockedLevel()` — `function highestUnlockedLevel(subject){let value=1;for(let level=2;level<=6;level++){if(subjectUnlocked(subject,level))value=level;}return value;}`
- [x] `if` — linha 4995, coluna 831 em `openChallengePicker()` — `function openChallengePicker(uid,name){openModal(\`Desafiar ${name}\`,\`<p>Escolha o jogo. O outro jogador receberá um cartão na tela com <b>Aceitar e jogar</b>.</p><div class="challenge-picker"><button data-challenge-type="math"><span>🔢</span><b>Matemática Kids</b><small>Contagem, soma e lógica</small></button><button data-challenge-type="portuguese"><span>📚</span><b>Português Kids</b><small>Letras, sílabas e palavras</small></button><button data-challenge-type="english"><span>🌎</span><b>English Kids</b><small>Imagens, sons e palavras</small></button></div>\`,root=>{$$('[data-challenge-type]',root).forEach(btn=>btn.onclick=async()=>{btn.disabled=true;btn.classList.add('sending');const result=await window.OTTHOS_RTDB?.sendGameChallenge?.(uid,btn.dataset.challengeType,highestUnlockedLevel(btn.dataset.challengeType),name);if(result?.ok){toast(\`Convite enviado para ${name}. Aguardando resposta.\`,'good',2600);closeModal();}else{btn.disabled=false;btn.classList.remove('sending');toast(result?.e`
- [x] `else` — linha 4995, coluna 932 em `openChallengePicker()` — `function openChallengePicker(uid,name){openModal(\`Desafiar ${name}\`,\`<p>Escolha o jogo. O outro jogador receberá um cartão na tela com <b>Aceitar e jogar</b>.</p><div class="challenge-picker"><button data-challenge-type="math"><span>🔢</span><b>Matemática Kids</b><small>Contagem, soma e lógica</small></button><button data-challenge-type="portuguese"><span>📚</span><b>Português Kids</b><small>Letras, sílabas e palavras</small></button><button data-challenge-type="english"><span>🌎</span><b>English Kids</b><small>Imagens, sons e palavras</small></button></div>\`,root=>{$$('[data-challenge-type]',root).forEach(btn=>btn.onclick=async()=>{btn.disabled=true;btn.classList.add('sending');const result=await window.OTTHOS_RTDB?.sendGameChallenge?.(uid,btn.dataset.challengeType,highestUnlockedLevel(btn.dataset.challengeType),name);if(result?.ok){toast(\`Convite enviado para ${name}. Aguardando resposta.\`,'good',2600);closeModal();}else{btn.disabled=false;btn.classList.remove('sending');toast(result?.e`
- [x] `return` — linha 4996, coluna 239 em `sessionOpponentName()` — `function sessionOpponentName(session){const uid=window.OTTHOS_RTDB?.uid,otherUid=session.fromUid===uid?session.toUid:session.fromUid,raw=session.fromUid===uid?(remotePresence.get(session.toUid)?.name||session.toName):(session.fromName);return remotePlayerName(raw,otherUid);}`
- [x] `if` — linha 4997, coluna 51 em `startMultiplayerEducationGame()` — `function startMultiplayerEducationGame(session){if(!session||session.status!=='active'){toast('Esta partida ainda não está pronta.','warn');return;}if(activeMultiplayerGameId&&activeMultiplayerGameId!==session.id){toast('Termine a partida atual.','warn');return;}activeMultiplayerGameId=session.id;closeChallengePrompt();runEducationGame({subject:session.type,level:Number(session.level||1),seed:Number(session.seed)||Date.now(),rounds:Number(session.rounds||5),multiplayer:true,opponent:sessionOpponentName(session),onFinish:async result=>{state.learning.multiplayerPlayed=(state.learning.multiplayerPlayed||0)+1;const ok=await window.OTTHOS_RTDB?.submitGameResult?.(session.id,result);saveState(true);openModal(ok?'Resultado enviado':'Resultado salvo no aparelho',\`<div class="lesson-result"><div>${ok?'⏳':'⚠️'}</div><h3>${result.score}/${result.total}</h3><p>${ok?\`Aguardando ${escapeHtml(sessionOpponentName(session))} terminar.\`:'A conexão caiu. Abra Online para reenviar ou jogar novamente.'}</`
- [x] `return` — linha 4997, coluna 143 em `startMultiplayerEducationGame()` — `function startMultiplayerEducationGame(session){if(!session||session.status!=='active'){toast('Esta partida ainda não está pronta.','warn');return;}if(activeMultiplayerGameId&&activeMultiplayerGameId!==session.id){toast('Termine a partida atual.','warn');return;}activeMultiplayerGameId=session.id;closeChallengePrompt();runEducationGame({subject:session.type,level:Number(session.level||1),seed:Number(session.seed)||Date.now(),rounds:Number(session.rounds||5),multiplayer:true,opponent:sessionOpponentName(session),onFinish:async result=>{state.learning.multiplayerPlayed=(state.learning.multiplayerPlayed||0)+1;const ok=await window.OTTHOS_RTDB?.submitGameResult?.(session.id,result);saveState(true);openModal(ok?'Resultado enviado':'Resultado salvo no aparelho',\`<div class="lesson-result"><div>${ok?'⏳':'⚠️'}</div><h3>${result.score}/${result.total}</h3><p>${ok?\`Aguardando ${escapeHtml(sessionOpponentName(session))} terminar.\`:'A conexão caiu. Abra Online para reenviar ou jogar novamente.'}</`
- [x] `if` — linha 4997, coluna 151 em `startMultiplayerEducationGame()` — `function startMultiplayerEducationGame(session){if(!session||session.status!=='active'){toast('Esta partida ainda não está pronta.','warn');return;}if(activeMultiplayerGameId&&activeMultiplayerGameId!==session.id){toast('Termine a partida atual.','warn');return;}activeMultiplayerGameId=session.id;closeChallengePrompt();runEducationGame({subject:session.type,level:Number(session.level||1),seed:Number(session.seed)||Date.now(),rounds:Number(session.rounds||5),multiplayer:true,opponent:sessionOpponentName(session),onFinish:async result=>{state.learning.multiplayerPlayed=(state.learning.multiplayerPlayed||0)+1;const ok=await window.OTTHOS_RTDB?.submitGameResult?.(session.id,result);saveState(true);openModal(ok?'Resultado enviado':'Resultado salvo no aparelho',\`<div class="lesson-result"><div>${ok?'⏳':'⚠️'}</div><h3>${result.score}/${result.total}</h3><p>${ok?\`Aguardando ${escapeHtml(sessionOpponentName(session))} terminar.\`:'A conexão caiu. Abra Online para reenviar ou jogar novamente.'}</`
- [x] `return` — linha 4997, coluna 258 em `startMultiplayerEducationGame()` — `function startMultiplayerEducationGame(session){if(!session||session.status!=='active'){toast('Esta partida ainda não está pronta.','warn');return;}if(activeMultiplayerGameId&&activeMultiplayerGameId!==session.id){toast('Termine a partida atual.','warn');return;}activeMultiplayerGameId=session.id;closeChallengePrompt();runEducationGame({subject:session.type,level:Number(session.level||1),seed:Number(session.seed)||Date.now(),rounds:Number(session.rounds||5),multiplayer:true,opponent:sessionOpponentName(session),onFinish:async result=>{state.learning.multiplayerPlayed=(state.learning.multiplayerPlayed||0)+1;const ok=await window.OTTHOS_RTDB?.submitGameResult?.(session.id,result);saveState(true);openModal(ok?'Resultado enviado':'Resultado salvo no aparelho',\`<div class="lesson-result"><div>${ok?'⏳':'⚠️'}</div><h3>${result.score}/${result.total}</h3><p>${ok?\`Aguardando ${escapeHtml(sessionOpponentName(session))} terminar.\`:'A conexão caiu. Abra Online para reenviar ou jogar novamente.'}</`
- [x] `if` — linha 4998, coluna 125 em `maybeShowMultiplayerResult()` — `function maybeShowMultiplayerResult(session){const players=Object.values(session.players||{}),uid=window.OTTHOS_RTDB?.uid;if(players.length<2||!players.every(p=>p.finished))return;if(!session.result){window.OTTHOS_RTDB?.finalizeGameSession?.(session.id);return;}const mine=session.players?.[uid],other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1],entry=rememberMatchResult(session),won=!session.result.draw&&session.result.winnerUid===uid,draw=!!session.result.draw;if(entry&&!entry.rewarded&&won){entry.rewarded=true;state.learning.multiplayerWins=(state.learning.multiplayerWins||0)+1;addCoins(80);addXP(60);}saveState(true);if(shownGameResults.has(session.id))return;shownGameResults.add(session.id);openModal(draw?'Empate!':won?'Você venceu!':\`${session.result.winnerName||'Adversário'} venceu\`,\`<div class="duel-result"><div>${draw?'🤝':won?'🏆':'🎮'}</div><h3>${mine?.score||0} × ${other?.score||0}</h3><p><b>Vencedor registrado:</b> ${escapeHtml(draw?'Empate':session.result.wi`
- [x] `return` — linha 4998, coluna 176 em `maybeShowMultiplayerResult()` — `function maybeShowMultiplayerResult(session){const players=Object.values(session.players||{}),uid=window.OTTHOS_RTDB?.uid;if(players.length<2||!players.every(p=>p.finished))return;if(!session.result){window.OTTHOS_RTDB?.finalizeGameSession?.(session.id);return;}const mine=session.players?.[uid],other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1],entry=rememberMatchResult(session),won=!session.result.draw&&session.result.winnerUid===uid,draw=!!session.result.draw;if(entry&&!entry.rewarded&&won){entry.rewarded=true;state.learning.multiplayerWins=(state.learning.multiplayerWins||0)+1;addCoins(80);addXP(60);}saveState(true);if(shownGameResults.has(session.id))return;shownGameResults.add(session.id);openModal(draw?'Empate!':won?'Você venceu!':\`${session.result.winnerName||'Adversário'} venceu\`,\`<div class="duel-result"><div>${draw?'🤝':won?'🏆':'🎮'}</div><h3>${mine?.score||0} × ${other?.score||0}</h3><p><b>Vencedor registrado:</b> ${escapeHtml(draw?'Empate':session.result.wi`
- [x] `if` — linha 4998, coluna 183 em `maybeShowMultiplayerResult()` — `function maybeShowMultiplayerResult(session){const players=Object.values(session.players||{}),uid=window.OTTHOS_RTDB?.uid;if(players.length<2||!players.every(p=>p.finished))return;if(!session.result){window.OTTHOS_RTDB?.finalizeGameSession?.(session.id);return;}const mine=session.players?.[uid],other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1],entry=rememberMatchResult(session),won=!session.result.draw&&session.result.winnerUid===uid,draw=!!session.result.draw;if(entry&&!entry.rewarded&&won){entry.rewarded=true;state.learning.multiplayerWins=(state.learning.multiplayerWins||0)+1;addCoins(80);addXP(60);}saveState(true);if(shownGameResults.has(session.id))return;shownGameResults.add(session.id);openModal(draw?'Empate!':won?'Você venceu!':\`${session.result.winnerName||'Adversário'} venceu\`,\`<div class="duel-result"><div>${draw?'🤝':won?'🏆':'🎮'}</div><h3>${mine?.score||0} × ${other?.score||0}</h3><p><b>Vencedor registrado:</b> ${escapeHtml(draw?'Empate':session.result.wi`
- [x] `return` — linha 4998, coluna 257 em `maybeShowMultiplayerResult()` — `function maybeShowMultiplayerResult(session){const players=Object.values(session.players||{}),uid=window.OTTHOS_RTDB?.uid;if(players.length<2||!players.every(p=>p.finished))return;if(!session.result){window.OTTHOS_RTDB?.finalizeGameSession?.(session.id);return;}const mine=session.players?.[uid],other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1],entry=rememberMatchResult(session),won=!session.result.draw&&session.result.winnerUid===uid,draw=!!session.result.draw;if(entry&&!entry.rewarded&&won){entry.rewarded=true;state.learning.multiplayerWins=(state.learning.multiplayerWins||0)+1;addCoins(80);addXP(60);}saveState(true);if(shownGameResults.has(session.id))return;shownGameResults.add(session.id);openModal(draw?'Empate!':won?'Você venceu!':\`${session.result.winnerName||'Adversário'} venceu\`,\`<div class="duel-result"><div>${draw?'🤝':won?'🏆':'🎮'}</div><h3>${mine?.score||0} × ${other?.score||0}</h3><p><b>Vencedor registrado:</b> ${escapeHtml(draw?'Empate':session.result.wi`
- [x] `if` — linha 4998, coluna 488 em `maybeShowMultiplayerResult()` — `function maybeShowMultiplayerResult(session){const players=Object.values(session.players||{}),uid=window.OTTHOS_RTDB?.uid;if(players.length<2||!players.every(p=>p.finished))return;if(!session.result){window.OTTHOS_RTDB?.finalizeGameSession?.(session.id);return;}const mine=session.players?.[uid],other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1],entry=rememberMatchResult(session),won=!session.result.draw&&session.result.winnerUid===uid,draw=!!session.result.draw;if(entry&&!entry.rewarded&&won){entry.rewarded=true;state.learning.multiplayerWins=(state.learning.multiplayerWins||0)+1;addCoins(80);addXP(60);}saveState(true);if(shownGameResults.has(session.id))return;shownGameResults.add(session.id);openModal(draw?'Empate!':won?'Você venceu!':\`${session.result.winnerName||'Adversário'} venceu\`,\`<div class="duel-result"><div>${draw?'🤝':won?'🏆':'🎮'}</div><h3>${mine?.score||0} × ${other?.score||0}</h3><p><b>Vencedor registrado:</b> ${escapeHtml(draw?'Empate':session.result.wi`
- [x] `if` — linha 4998, coluna 649 em `maybeShowMultiplayerResult()` — `function maybeShowMultiplayerResult(session){const players=Object.values(session.players||{}),uid=window.OTTHOS_RTDB?.uid;if(players.length<2||!players.every(p=>p.finished))return;if(!session.result){window.OTTHOS_RTDB?.finalizeGameSession?.(session.id);return;}const mine=session.players?.[uid],other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1],entry=rememberMatchResult(session),won=!session.result.draw&&session.result.winnerUid===uid,draw=!!session.result.draw;if(entry&&!entry.rewarded&&won){entry.rewarded=true;state.learning.multiplayerWins=(state.learning.multiplayerWins||0)+1;addCoins(80);addXP(60);}saveState(true);if(shownGameResults.has(session.id))return;shownGameResults.add(session.id);openModal(draw?'Empate!':won?'Você venceu!':\`${session.result.winnerName||'Adversário'} venceu\`,\`<div class="duel-result"><div>${draw?'🤝':won?'🏆':'🎮'}</div><h3>${mine?.score||0} × ${other?.score||0}</h3><p><b>Vencedor registrado:</b> ${escapeHtml(draw?'Empate':session.result.wi`
- [x] `return` — linha 4998, coluna 685 em `maybeShowMultiplayerResult()` — `function maybeShowMultiplayerResult(session){const players=Object.values(session.players||{}),uid=window.OTTHOS_RTDB?.uid;if(players.length<2||!players.every(p=>p.finished))return;if(!session.result){window.OTTHOS_RTDB?.finalizeGameSession?.(session.id);return;}const mine=session.players?.[uid],other=Object.entries(session.players||{}).find(([id])=>id!==uid)?.[1],entry=rememberMatchResult(session),won=!session.result.draw&&session.result.winnerUid===uid,draw=!!session.result.draw;if(entry&&!entry.rewarded&&won){entry.rewarded=true;state.learning.multiplayerWins=(state.learning.multiplayerWins||0)+1;addCoins(80);addXP(60);}saveState(true);if(shownGameResults.has(session.id))return;shownGameResults.add(session.id);openModal(draw?'Empate!':won?'Você venceu!':\`${session.result.winnerName||'Adversário'} venceu\`,\`<div class="duel-result"><div>${draw?'🤝':won?'🏆':'🎮'}</div><h3>${mine?.score||0} × ${other?.score||0}</h3><p><b>Vencedor registrado:</b> ${escapeHtml(draw?'Empate':session.result.wi`
- [x] `return` — linha 4999, coluna 32 em `currentOnlineRoom()` — `function currentOnlineRoom(){return normalizeRoomId(multiplayerState.room||window.OTTHOS_RTDB?.getRoom?.()||state.multiplayer.room);}`
- [x] `if` — linha 5000, coluna 88 em `multiplayerStatusText()` — `function multiplayerStatusText(){const roomName=roomDisplayName(currentOnlineRoom());if(multiplayerState.connected)return\`${roomName} • ${multiplayerState.count||1} online\`;if(window.OTTHOS_RTDB?.configured)return multiplayerState.error?\`Offline: ${multiplayerState.error}\`:\`Conectando ao ${roomName}...\`;return'Firebase indisponível';}`
- [x] `return` — linha 5000, coluna 118 em `multiplayerStatusText()` — `function multiplayerStatusText(){const roomName=roomDisplayName(currentOnlineRoom());if(multiplayerState.connected)return\`${roomName} • ${multiplayerState.count||1} online\`;if(window.OTTHOS_RTDB?.configured)return multiplayerState.error?\`Offline: ${multiplayerState.error}\`:\`Conectando ao ${roomName}...\`;return'Firebase indisponível';}`
- [x] `if` — linha 5000, coluna 176 em `multiplayerStatusText()` — `function multiplayerStatusText(){const roomName=roomDisplayName(currentOnlineRoom());if(multiplayerState.connected)return\`${roomName} • ${multiplayerState.count||1} online\`;if(window.OTTHOS_RTDB?.configured)return multiplayerState.error?\`Offline: ${multiplayerState.error}\`:\`Conectando ao ${roomName}...\`;return'Firebase indisponível';}`
- [x] `return` — linha 5000, coluna 210 em `multiplayerStatusText()` — `function multiplayerStatusText(){const roomName=roomDisplayName(currentOnlineRoom());if(multiplayerState.connected)return\`${roomName} • ${multiplayerState.count||1} online\`;if(window.OTTHOS_RTDB?.configured)return multiplayerState.error?\`Offline: ${multiplayerState.error}\`:\`Conectando ao ${roomName}...\`;return'Firebase indisponível';}`
- [x] `return` — linha 5000, coluna 308 em `multiplayerStatusText()` — `function multiplayerStatusText(){const roomName=roomDisplayName(currentOnlineRoom());if(multiplayerState.connected)return\`${roomName} • ${multiplayerState.count||1} online\`;if(window.OTTHOS_RTDB?.configured)return multiplayerState.error?\`Offline: ${multiplayerState.error}\`:\`Conectando ao ${roomName}...\`;return'Firebase indisponível';}`
- [x] `if` — linha 5001, coluna 37 em `updateMultiplayerBadge()` — `function updateMultiplayerBadge(){if(!els.multiplayerBadge)return;els.multiplayerBadge.classList.toggle('online',multiplayerState.connected);els.multiplayerBadge.classList.toggle('offline',!multiplayerState.connected);const label=$('span',els.multiplayerBadge);if(label)label.textContent=multiplayerState.connected?\`${multiplayerState.count||1} online\`:'Offline';}`
- [x] `return` — linha 5001, coluna 62 em `updateMultiplayerBadge()` — `function updateMultiplayerBadge(){if(!els.multiplayerBadge)return;els.multiplayerBadge.classList.toggle('online',multiplayerState.connected);els.multiplayerBadge.classList.toggle('offline',!multiplayerState.connected);const label=$('span',els.multiplayerBadge);if(label)label.textContent=multiplayerState.connected?\`${multiplayerState.count||1} online\`:'Offline';}`
- [x] `if` — linha 5001, coluna 264 em `updateMultiplayerBadge()` — `function updateMultiplayerBadge(){if(!els.multiplayerBadge)return;els.multiplayerBadge.classList.toggle('online',multiplayerState.connected);els.multiplayerBadge.classList.toggle('offline',!multiplayerState.connected);const label=$('span',els.multiplayerBadge);if(label)label.textContent=multiplayerState.connected?\`${multiplayerState.count||1} online\`:'Offline';}`
- [x] `return` — linha 5002, coluna 28 em `onlinePlayers()` — `function onlinePlayers(){return [...remotePresence.entries()].map(([uid,data])=>{const ghost=world.ghosts.get(uid);return{uid,name:remotePlayerName(data.name||ghost?.userData?.displayName,uid),distance:ghost?Math.hypot(player.x-ghost.position.x,player.z-ghost.position.z):Math.hypot(player.x-(data.x||0),player.z-(data.z||0))}}).sort((a,b)=>a.distance-b.distance);}`
- [x] `return` — linha 5002, coluna 118 em `onlinePlayers()` — `function onlinePlayers(){return [...remotePresence.entries()].map(([uid,data])=>{const ghost=world.ghosts.get(uid);return{uid,name:remotePlayerName(data.name||ghost?.userData?.displayName,uid),distance:ghost?Math.hypot(player.x-ghost.position.x,player.z-ghost.position.z):Math.hypot(player.x-(data.x||0),player.z-(data.z||0))}}).sort((a,b)=>a.distance-b.distance);}`
- [x] `return` — linha 5003, coluna 58 em `onlinePlayerListHtml()` — `function onlinePlayerListHtml(players=onlinePlayers()){return players.length?players.map(p=>\`<button class="online-player-card" data-online-player="${p.uid}"><span>👤</span><b>${escapeHtml(p.name)}</b><small>${Math.round(p.distance)} m</small></button>\`).join(''):'<p>Nenhum outro jogador apareceu ainda. Abra o jogo em outro celular usando o mesmo Firebase.</p>';}`
- [x] `if` — linha 5004, coluna 179 em `bindOnlinePlayerCards()` — `function bindOnlinePlayerCards(root=els.modalBody){$$('[data-online-player]',root).forEach(btn=>btn.onclick=()=>{const uid=btn.dataset.onlinePlayer,ghost=world.ghosts.get(uid);if(ghost)openRemotePlayerActions(uid,ghost);});}`
- [x] `if` — linha 5005, coluna 59 em `refreshOpenSocialHub()` — `function refreshOpenSocialHub(){updateOnlineAttention();if(els.modal.hidden||els.modalTitle.textContent!=='Mundo Online')return;const players=onlinePlayers(),status=$('#onlineStatusText',els.modalBody),count=$('#onlineCount',els.modalBody),list=$('#onlinePlayerList',els.modalBody),chat=$('#worldChatList',els.modalBody),socialInvites=$('#socialRequestInbox',els.modalBody),invites=$('#challengeInbox',els.modalBody),sessions=$('#activeGameSessions',els.modalBody),history=$('#duelHistory',els.modalBody);if(status)status.textContent=multiplayerStatusText();if(count)count.textContent=\`${players.length} além de você\`;if(list){list.innerHTML=onlinePlayerListHtml(players);bindOnlinePlayerCards(els.modalBody);}if(socialInvites){socialInvites.innerHTML=socialRequestInboxHtml();bindChallengeCards(els.modalBody);}if(invites){invites.innerHTML=challengeInboxHtml();bindChallengeCards(els.modalBody);}if(sessions){sessions.innerHTML=activeSessionsHtml();bindChallengeCards(els.modalBody);}if(history)his`
- [x] `return` — linha 5005, coluna 124 em `refreshOpenSocialHub()` — `function refreshOpenSocialHub(){updateOnlineAttention();if(els.modal.hidden||els.modalTitle.textContent!=='Mundo Online')return;const players=onlinePlayers(),status=$('#onlineStatusText',els.modalBody),count=$('#onlineCount',els.modalBody),list=$('#onlinePlayerList',els.modalBody),chat=$('#worldChatList',els.modalBody),socialInvites=$('#socialRequestInbox',els.modalBody),invites=$('#challengeInbox',els.modalBody),sessions=$('#activeGameSessions',els.modalBody),history=$('#duelHistory',els.modalBody);if(status)status.textContent=multiplayerStatusText();if(count)count.textContent=\`${players.length} além de você\`;if(list){list.innerHTML=onlinePlayerListHtml(players);bindOnlinePlayerCards(els.modalBody);}if(socialInvites){socialInvites.innerHTML=socialRequestInboxHtml();bindChallengeCards(els.modalBody);}if(invites){invites.innerHTML=challengeInboxHtml();bindChallengeCards(els.modalBody);}if(sessions){sessions.innerHTML=activeSessionsHtml();bindChallengeCards(els.modalBody);}if(history)his`
- [x] `if` — linha 5005, coluna 508 em `refreshOpenSocialHub()` — `function refreshOpenSocialHub(){updateOnlineAttention();if(els.modal.hidden||els.modalTitle.textContent!=='Mundo Online')return;const players=onlinePlayers(),status=$('#onlineStatusText',els.modalBody),count=$('#onlineCount',els.modalBody),list=$('#onlinePlayerList',els.modalBody),chat=$('#worldChatList',els.modalBody),socialInvites=$('#socialRequestInbox',els.modalBody),invites=$('#challengeInbox',els.modalBody),sessions=$('#activeGameSessions',els.modalBody),history=$('#duelHistory',els.modalBody);if(status)status.textContent=multiplayerStatusText();if(count)count.textContent=\`${players.length} além de você\`;if(list){list.innerHTML=onlinePlayerListHtml(players);bindOnlinePlayerCards(els.modalBody);}if(socialInvites){socialInvites.innerHTML=socialRequestInboxHtml();bindChallengeCards(els.modalBody);}if(invites){invites.innerHTML=challengeInboxHtml();bindChallengeCards(els.modalBody);}if(sessions){sessions.innerHTML=activeSessionsHtml();bindChallengeCards(els.modalBody);}if(history)his`
- [x] `if` — linha 5005, coluna 561 em `refreshOpenSocialHub()` — `function refreshOpenSocialHub(){updateOnlineAttention();if(els.modal.hidden||els.modalTitle.textContent!=='Mundo Online')return;const players=onlinePlayers(),status=$('#onlineStatusText',els.modalBody),count=$('#onlineCount',els.modalBody),list=$('#onlinePlayerList',els.modalBody),chat=$('#worldChatList',els.modalBody),socialInvites=$('#socialRequestInbox',els.modalBody),invites=$('#challengeInbox',els.modalBody),sessions=$('#activeGameSessions',els.modalBody),history=$('#duelHistory',els.modalBody);if(status)status.textContent=multiplayerStatusText();if(count)count.textContent=\`${players.length} além de você\`;if(list){list.innerHTML=onlinePlayerListHtml(players);bindOnlinePlayerCards(els.modalBody);}if(socialInvites){socialInvites.innerHTML=socialRequestInboxHtml();bindChallengeCards(els.modalBody);}if(invites){invites.innerHTML=challengeInboxHtml();bindChallengeCards(els.modalBody);}if(sessions){sessions.innerHTML=activeSessionsHtml();bindChallengeCards(els.modalBody);}if(history)his`
- [x] `if` — linha 5005, coluna 621 em `refreshOpenSocialHub()` — `function refreshOpenSocialHub(){updateOnlineAttention();if(els.modal.hidden||els.modalTitle.textContent!=='Mundo Online')return;const players=onlinePlayers(),status=$('#onlineStatusText',els.modalBody),count=$('#onlineCount',els.modalBody),list=$('#onlinePlayerList',els.modalBody),chat=$('#worldChatList',els.modalBody),socialInvites=$('#socialRequestInbox',els.modalBody),invites=$('#challengeInbox',els.modalBody),sessions=$('#activeGameSessions',els.modalBody),history=$('#duelHistory',els.modalBody);if(status)status.textContent=multiplayerStatusText();if(count)count.textContent=\`${players.length} além de você\`;if(list){list.innerHTML=onlinePlayerListHtml(players);bindOnlinePlayerCards(els.modalBody);}if(socialInvites){socialInvites.innerHTML=socialRequestInboxHtml();bindChallengeCards(els.modalBody);}if(invites){invites.innerHTML=challengeInboxHtml();bindChallengeCards(els.modalBody);}if(sessions){sessions.innerHTML=activeSessionsHtml();bindChallengeCards(els.modalBody);}if(history)his`
- [x] `if` — linha 5005, coluna 713 em `refreshOpenSocialHub()` — `function refreshOpenSocialHub(){updateOnlineAttention();if(els.modal.hidden||els.modalTitle.textContent!=='Mundo Online')return;const players=onlinePlayers(),status=$('#onlineStatusText',els.modalBody),count=$('#onlineCount',els.modalBody),list=$('#onlinePlayerList',els.modalBody),chat=$('#worldChatList',els.modalBody),socialInvites=$('#socialRequestInbox',els.modalBody),invites=$('#challengeInbox',els.modalBody),sessions=$('#activeGameSessions',els.modalBody),history=$('#duelHistory',els.modalBody);if(status)status.textContent=multiplayerStatusText();if(count)count.textContent=\`${players.length} além de você\`;if(list){list.innerHTML=onlinePlayerListHtml(players);bindOnlinePlayerCards(els.modalBody);}if(socialInvites){socialInvites.innerHTML=socialRequestInboxHtml();bindChallengeCards(els.modalBody);}if(invites){invites.innerHTML=challengeInboxHtml();bindChallengeCards(els.modalBody);}if(sessions){sessions.innerHTML=activeSessionsHtml();bindChallengeCards(els.modalBody);}if(history)his`
- [x] `if` — linha 5005, coluna 815 em `refreshOpenSocialHub()` — `function refreshOpenSocialHub(){updateOnlineAttention();if(els.modal.hidden||els.modalTitle.textContent!=='Mundo Online')return;const players=onlinePlayers(),status=$('#onlineStatusText',els.modalBody),count=$('#onlineCount',els.modalBody),list=$('#onlinePlayerList',els.modalBody),chat=$('#worldChatList',els.modalBody),socialInvites=$('#socialRequestInbox',els.modalBody),invites=$('#challengeInbox',els.modalBody),sessions=$('#activeGameSessions',els.modalBody),history=$('#duelHistory',els.modalBody);if(status)status.textContent=multiplayerStatusText();if(count)count.textContent=\`${players.length} além de você\`;if(list){list.innerHTML=onlinePlayerListHtml(players);bindOnlinePlayerCards(els.modalBody);}if(socialInvites){socialInvites.innerHTML=socialRequestInboxHtml();bindChallengeCards(els.modalBody);}if(invites){invites.innerHTML=challengeInboxHtml();bindChallengeCards(els.modalBody);}if(sessions){sessions.innerHTML=activeSessionsHtml();bindChallengeCards(els.modalBody);}if(history)his`
- [x] `if` — linha 5005, coluna 901 em `refreshOpenSocialHub()` — `function refreshOpenSocialHub(){updateOnlineAttention();if(els.modal.hidden||els.modalTitle.textContent!=='Mundo Online')return;const players=onlinePlayers(),status=$('#onlineStatusText',els.modalBody),count=$('#onlineCount',els.modalBody),list=$('#onlinePlayerList',els.modalBody),chat=$('#worldChatList',els.modalBody),socialInvites=$('#socialRequestInbox',els.modalBody),invites=$('#challengeInbox',els.modalBody),sessions=$('#activeGameSessions',els.modalBody),history=$('#duelHistory',els.modalBody);if(status)status.textContent=multiplayerStatusText();if(count)count.textContent=\`${players.length} além de você\`;if(list){list.innerHTML=onlinePlayerListHtml(players);bindOnlinePlayerCards(els.modalBody);}if(socialInvites){socialInvites.innerHTML=socialRequestInboxHtml();bindChallengeCards(els.modalBody);}if(invites){invites.innerHTML=challengeInboxHtml();bindChallengeCards(els.modalBody);}if(sessions){sessions.innerHTML=activeSessionsHtml();bindChallengeCards(els.modalBody);}if(history)his`
- [x] `if` — linha 5005, coluna 989 em `refreshOpenSocialHub()` — `function refreshOpenSocialHub(){updateOnlineAttention();if(els.modal.hidden||els.modalTitle.textContent!=='Mundo Online')return;const players=onlinePlayers(),status=$('#onlineStatusText',els.modalBody),count=$('#onlineCount',els.modalBody),list=$('#onlinePlayerList',els.modalBody),chat=$('#worldChatList',els.modalBody),socialInvites=$('#socialRequestInbox',els.modalBody),invites=$('#challengeInbox',els.modalBody),sessions=$('#activeGameSessions',els.modalBody),history=$('#duelHistory',els.modalBody);if(status)status.textContent=multiplayerStatusText();if(count)count.textContent=\`${players.length} além de você\`;if(list){list.innerHTML=onlinePlayerListHtml(players);bindOnlinePlayerCards(els.modalBody);}if(socialInvites){socialInvites.innerHTML=socialRequestInboxHtml();bindChallengeCards(els.modalBody);}if(invites){invites.innerHTML=challengeInboxHtml();bindChallengeCards(els.modalBody);}if(sessions){sessions.innerHTML=activeSessionsHtml();bindChallengeCards(els.modalBody);}if(history)his`
- [x] `if` — linha 5005, coluna 1036 em `refreshOpenSocialHub()` — `function refreshOpenSocialHub(){updateOnlineAttention();if(els.modal.hidden||els.modalTitle.textContent!=='Mundo Online')return;const players=onlinePlayers(),status=$('#onlineStatusText',els.modalBody),count=$('#onlineCount',els.modalBody),list=$('#onlinePlayerList',els.modalBody),chat=$('#worldChatList',els.modalBody),socialInvites=$('#socialRequestInbox',els.modalBody),invites=$('#challengeInbox',els.modalBody),sessions=$('#activeGameSessions',els.modalBody),history=$('#duelHistory',els.modalBody);if(status)status.textContent=multiplayerStatusText();if(count)count.textContent=\`${players.length} além de você\`;if(list){list.innerHTML=onlinePlayerListHtml(players);bindOnlinePlayerCards(els.modalBody);}if(socialInvites){socialInvites.innerHTML=socialRequestInboxHtml();bindChallengeCards(els.modalBody);}if(invites){invites.innerHTML=challengeInboxHtml();bindChallengeCards(els.modalBody);}if(sessions){sessions.innerHTML=activeSessionsHtml();bindChallengeCards(els.modalBody);}if(history)his`
- [x] `return` — linha 5006, coluna 88 em `approvedChatPhrases()` — `function approvedChatPhrases(){const list=window.OTTHI_CHILD_SAFETY?.approvedPhrases;return Array.isArray(list)&&list.length?list:['Oi!','Vamos brincar?','Quer correr?','Vamos estudar juntos?','Boa jogada!','Parabéns!','Até logo!','Vamos visitar minha casa?','Vamos pescar?','Vamos construir?'];}`
- [x] `if` — linha 5009, coluna 5 em `openSocialHub()` — `if(!guardianMultiplayerAllowed()){openModal('Mundo Online','<div class="parent-gate"><span>🛡️</span><h3>Multiplayer desativado</h3><p>O modo online está temporariamente indisponível. Verifique a conexão e tente novamente.</p></div>');return;}`
- [x] `return` — linha 5009, coluna 239 em `openSocialHub()` — `if(!guardianMultiplayerAllowed()){openModal('Mundo Online','<div class="parent-gate"><span>🛡️</span><h3>Multiplayer desativado</h3><p>O modo online está temporariamente indisponível. Verifique a conexão e tente novamente.</p></div>');return;}`
- [x] `if` — linha 5010, coluna 5 em `openSocialHub()` — `if(!guardianCommunicationAllowed()){openModal('Mundo Online',\`<div class="online-status-card"><b>${multiplayerStatusText()}</b><span>A presença no bairro está ativa, mas comunicação, convites e interações foram desativados por um responsável.</span></div><div class="social-tabs"><b>Jogadores no bairro</b><small>${players.length} além de você</small></div><div class="online-player-list">${players.map(p=>\`<div class="online-player-card"><span>👤</span><b>${escapeHtml(p.name)}</b><small>${Math.round(p.distance)} m</small></div>\`).join('')||'<p>Nenhum outro jogador apareceu.</p>'}</div>\`);return;}`
- [x] `return` — linha 5010, coluna 596 em `openSocialHub()` — `if(!guardianCommunicationAllowed()){openModal('Mundo Online',\`<div class="online-status-card"><b>${multiplayerStatusText()}</b><span>A presença no bairro está ativa, mas comunicação, convites e interações foram desativados por um responsável.</span></div><div class="social-tabs"><b>Jogadores no bairro</b><small>${players.length} além de você</small></div><div class="online-player-list">${players.map(p=>\`<div class="online-player-card"><span>👤</span><b>${escapeHtml(p.name)}</b><small>${Math.round(p.distance)} m</small></div>\`).join('')||'<p>Nenhum outro jogador apareceu.</p>'}</div>\`);return;}`
- [x] `if` — linha 5014, coluna 187 em `openSocialHub()` — `$$('[data-approved-chat]',root).forEach(btn=>btn.onclick=async()=>{btn.disabled=true;const result=await window.OTTHOS_RTDB?.sendChat?.(btn.dataset.approvedChat);btn.disabled=false;if(result===false||result?.ok===false)toast(result?.error||'A frase não pôde ser enviada.','warn');});`
- [x] `return` — linha 5018, coluna 33 em `escapeHtml()` — `function escapeHtml(value=''){return String(value).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));}`
- [x] `return` — linha 5019, coluna 31 em `chatMessageHtml()` — `function chatMessageHtml(m){return\`<article class="world-chat-message"><b>${escapeHtml(m.name||'Jogador')}</b><span>${escapeHtml(m.text||'')}</span></article>\`;}`
- [x] `if` — linha 5023, coluna 176 em `openReportPlayer()` — `$$('[data-report-reason]',root).forEach(btn=>btn.onclick=async()=>{btn.disabled=true;const result=await window.OTTHOS_RTDB?.reportPlayer?.(uid,btn.dataset.reportReason);if(result?.ok===false||result===false){btn.disabled=false;toast(result?.error||'Não foi possível enviar a denúncia.','warn');return;}closeModal();toast('Denúncia registrada. Um responsável pode também bloquear esse jogador.','good',3000);});`
- [x] `return` — linha 5023, coluna 301 em `openReportPlayer()` — `$$('[data-report-reason]',root).forEach(btn=>btn.onclick=async()=>{btn.disabled=true;const result=await window.OTTHOS_RTDB?.reportPlayer?.(uid,btn.dataset.reportReason);if(result?.ok===false||result===false){btn.disabled=false;toast(result?.error||'Não foi possível enviar a denúncia.','warn');return;}closeModal();toast('Denúncia registrada. Um responsável pode também bloquear esse jogador.','good',3000);});`
- [x] `if` — linha 5028, coluna 5 em `openRemotePlayerActions()` — `if(!guardianCommunicationAllowed()){toast('As interações foram desativadas por um responsável.','warn');return;}`
- [x] `return` — linha 5028, coluna 109 em `openRemotePlayerActions()` — `if(!guardianCommunicationAllowed()){toast('As interações foram desativadas por um responsável.','warn');return;}`
- [x] `if` — linha 5032, coluna 7 em `openRemotePlayerActions()` — `if(btn.dataset.actionBusy==='1')return;btn.dataset.actionBusy='1';btn.disabled=true;const action=btn.dataset.playerAction;let completed=false;`
- [x] `return` — linha 5032, coluna 39 em `openRemotePlayerActions()` — `if(btn.dataset.actionBusy==='1')return;btn.dataset.actionBusy='1';btn.disabled=true;const action=btn.dataset.playerAction;let completed=false;`
- [x] `if` — linha 5034, coluna 9 em `openRemotePlayerActions()` — `if(action==='message'){closeModal();openSocialHub();return;}`
- [x] `return` — linha 5034, coluna 61 em `openRemotePlayerActions()` — `if(action==='message'){closeModal();openSocialHub();return;}`
- [x] `if` — linha 5035, coluna 9 em `openRemotePlayerActions()` — `if(action==='report'){openReportPlayer(uid,name);return;}`
- [x] `return` — linha 5035, coluna 58 em `openRemotePlayerActions()` — `if(action==='report'){openReportPlayer(uid,name);return;}`
- [x] `if` — linha 5036, coluna 9 em `openRemotePlayerActions()` — `if(action==='block'){if(!(await confirmModal('Bloquear jogador',\`Bloquear ${name} e ocultar seus contatos?\`,'Bloquear','Cancelar')))return;const result=await window.OTTHOS_RTDB?.blockPlayer?.(uid);if(result?.ok===false||result===false){toast(result?.error||'Não foi possível bloquear.','warn');return;}remotePresence.delete(uid);const blockedGhost=world.ghosts.get(uid);if(blockedGhost){scene?.remove(blockedGhost);world.ghosts.delete(uid);}closeModal();toast(\`${name} foi bloqueado.\`,'good');return;}`
- [x] `if` — linha 5036, coluna 30 em `openRemotePlayerActions()` — `if(action==='block'){if(!(await confirmModal('Bloquear jogador',\`Bloquear ${name} e ocultar seus contatos?\`,'Bloquear','Cancelar')))return;const result=await window.OTTHOS_RTDB?.blockPlayer?.(uid);if(result?.ok===false||result===false){toast(result?.error||'Não foi possível bloquear.','warn');return;}remotePresence.delete(uid);const blockedGhost=world.ghosts.get(uid);if(blockedGhost){scene?.remove(blockedGhost);world.ghosts.delete(uid);}closeModal();toast(\`${name} foi bloqueado.\`,'good');return;}`
- [x] `return` — linha 5036, coluna 141 em `openRemotePlayerActions()` — `if(action==='block'){if(!(await confirmModal('Bloquear jogador',\`Bloquear ${name} e ocultar seus contatos?\`,'Bloquear','Cancelar')))return;const result=await window.OTTHOS_RTDB?.blockPlayer?.(uid);if(result?.ok===false||result===false){toast(result?.error||'Não foi possível bloquear.','warn');return;}remotePresence.delete(uid);const blockedGhost=world.ghosts.get(uid);if(blockedGhost){scene?.remove(blockedGhost);world.ghosts.delete(uid);}closeModal();toast(\`${name} foi bloqueado.\`,'good');return;}`
- [x] `if` — linha 5036, coluna 206 em `openRemotePlayerActions()` — `if(action==='block'){if(!(await confirmModal('Bloquear jogador',\`Bloquear ${name} e ocultar seus contatos?\`,'Bloquear','Cancelar')))return;const result=await window.OTTHOS_RTDB?.blockPlayer?.(uid);if(result?.ok===false||result===false){toast(result?.error||'Não foi possível bloquear.','warn');return;}remotePresence.delete(uid);const blockedGhost=world.ghosts.get(uid);if(blockedGhost){scene?.remove(blockedGhost);world.ghosts.delete(uid);}closeModal();toast(\`${name} foi bloqueado.\`,'good');return;}`
- [x] `return` — linha 5036, coluna 303 em `openRemotePlayerActions()` — `if(action==='block'){if(!(await confirmModal('Bloquear jogador',\`Bloquear ${name} e ocultar seus contatos?\`,'Bloquear','Cancelar')))return;const result=await window.OTTHOS_RTDB?.blockPlayer?.(uid);if(result?.ok===false||result===false){toast(result?.error||'Não foi possível bloquear.','warn');return;}remotePresence.delete(uid);const blockedGhost=world.ghosts.get(uid);if(blockedGhost){scene?.remove(blockedGhost);world.ghosts.delete(uid);}closeModal();toast(\`${name} foi bloqueado.\`,'good');return;}`
- [x] `if` — linha 5036, coluna 379 em `openRemotePlayerActions()` — `if(action==='block'){if(!(await confirmModal('Bloquear jogador',\`Bloquear ${name} e ocultar seus contatos?\`,'Bloquear','Cancelar')))return;const result=await window.OTTHOS_RTDB?.blockPlayer?.(uid);if(result?.ok===false||result===false){toast(result?.error||'Não foi possível bloquear.','warn');return;}remotePresence.delete(uid);const blockedGhost=world.ghosts.get(uid);if(blockedGhost){scene?.remove(blockedGhost);world.ghosts.delete(uid);}closeModal();toast(\`${name} foi bloqueado.\`,'good');return;}`
- [x] `return` — linha 5036, coluna 502 em `openRemotePlayerActions()` — `if(action==='block'){if(!(await confirmModal('Bloquear jogador',\`Bloquear ${name} e ocultar seus contatos?\`,'Bloquear','Cancelar')))return;const result=await window.OTTHOS_RTDB?.blockPlayer?.(uid);if(result?.ok===false||result===false){toast(result?.error||'Não foi possível bloquear.','warn');return;}remotePresence.delete(uid);const blockedGhost=world.ghosts.get(uid);if(blockedGhost){scene?.remove(blockedGhost);world.ghosts.delete(uid);}closeModal();toast(\`${name} foi bloqueado.\`,'good');return;}`
- [x] `if` — linha 5037, coluna 9 em `openRemotePlayerActions()` — `if(action==='giftCoins'){completed=!!(await window.OTTHOS_RTDB?.sendGift?.(uid,{type:'coins',amount:10}));if(completed)toast(\`Símbolo de moedas enviado para ${name}.\`,'good');}`
- [x] `if` — linha 5037, coluna 115 em `openRemotePlayerActions()` — `if(action==='giftCoins'){completed=!!(await window.OTTHOS_RTDB?.sendGift?.(uid,{type:'coins',amount:10}));if(completed)toast(\`Símbolo de moedas enviado para ${name}.\`,'good');}`
- [x] `else` — linha 5038, coluna 9 em `openRemotePlayerActions()` — `else if(action==='giftCrystal'){completed=!!(await window.OTTHOS_RTDB?.sendGift?.(uid,{type:'crystal',amount:1}));if(completed)toast(\`Símbolo de cristal enviado para ${name}.\`,'good');}`
- [x] `if` — linha 5038, coluna 14 em `openRemotePlayerActions()` — `else if(action==='giftCrystal'){completed=!!(await window.OTTHOS_RTDB?.sendGift?.(uid,{type:'crystal',amount:1}));if(completed)toast(\`Símbolo de cristal enviado para ${name}.\`,'good');}`
- [x] `if` — linha 5038, coluna 123 em `openRemotePlayerActions()` — `else if(action==='giftCrystal'){completed=!!(await window.OTTHOS_RTDB?.sendGift?.(uid,{type:'crystal',amount:1}));if(completed)toast(\`Símbolo de cristal enviado para ${name}.\`,'good');}`
- [x] `else` — linha 5039, coluna 9 em `openRemotePlayerActions()` — `else if(action==='challenge'){openChallengePicker(uid,name);return;}`
- [x] `if` — linha 5039, coluna 14 em `openRemotePlayerActions()` — `else if(action==='challenge'){openChallengePicker(uid,name);return;}`
- [x] `return` — linha 5039, coluna 69 em `openRemotePlayerActions()` — `else if(action==='challenge'){openChallengePicker(uid,name);return;}`
- [x] `else` — linha 5040, coluna 9 em `openRemotePlayerActions()` — `else if(action==='wave'){completed=!!(await window.OTTHOS_RTDB?.sendInteraction?.(uid,{type:'wave'}));if(completed){triggerEmote('wave');toast(\`Você acenou para ${name}.\`,'good');}}`
- [x] `if` — linha 5040, coluna 14 em `openRemotePlayerActions()` — `else if(action==='wave'){completed=!!(await window.OTTHOS_RTDB?.sendInteraction?.(uid,{type:'wave'}));if(completed){triggerEmote('wave');toast(\`Você acenou para ${name}.\`,'good');}}`
- [x] `if` — linha 5040, coluna 111 em `openRemotePlayerActions()` — `else if(action==='wave'){completed=!!(await window.OTTHOS_RTDB?.sendInteraction?.(uid,{type:'wave'}));if(completed){triggerEmote('wave');toast(\`Você acenou para ${name}.\`,'good');}}`
- [x] `else` — linha 5041, coluna 9 em `openRemotePlayerActions()` — `else completed=await sendSocialActionRequest(uid,name,action,{boatId:state.boats.activeBoatId||'',vehicleId:player.car.id||'',campfireId:nearCamp?.data?.id||''});`
- [x] `if` — linha 5042, coluna 9 em `openRemotePlayerActions()` — `if(completed)closeModal();else if(action==='wave'||action.startsWith('gift'))toast('A ação não foi enviada. Confirme se as regras Firebase deste pacote foram publicadas.','warn',3200);`
- [x] `else` — linha 5042, coluna 35 em `openRemotePlayerActions()` — `if(completed)closeModal();else if(action==='wave'||action.startsWith('gift'))toast('A ação não foi enviada. Confirme se as regras Firebase deste pacote foram publicadas.','warn',3200);`
- [x] `if` — linha 5042, coluna 40 em `openRemotePlayerActions()` — `if(completed)closeModal();else if(action==='wave'||action.startsWith('gift'))toast('A ação não foi enviada. Confirme se as regras Firebase deste pacote foram publicadas.','warn',3200);`
- [x] `catch` — linha 5043, coluna 8 em `openRemotePlayerActions()` — `}catch(error){toast(error?.message||'A ação online não pôde ser concluída.','warn',3000);}`
- [x] `if` — linha 5044, coluna 15 em `openRemotePlayerActions()` — `finally{if(btn.isConnected){delete btn.dataset.actionBusy;btn.disabled=false;}}`
- [x] `for` — linha 5047, coluna 63 em `nearestRemotePlayer()` — `function nearestRemotePlayer(){let found=null,best=Infinity;for(const [uid,g] of world.ghosts){const d=Math.hypot(player.x-g.position.x,player.z-g.position.z);if(d<2.7&&d<best){best=d;found={uid,ghost:g};}}return found;}`
- [x] `if` — linha 5047, coluna 162 em `nearestRemotePlayer()` — `function nearestRemotePlayer(){let found=null,best=Infinity;for(const [uid,g] of world.ghosts){const d=Math.hypot(player.x-g.position.x,player.z-g.position.z);if(d<2.7&&d<best){best=d;found={uid,ghost:g};}}return found;}`
- [x] `return` — linha 5047, coluna 209 em `nearestRemotePlayer()` — `function nearestRemotePlayer(){let found=null,best=Infinity;for(const [uid,g] of world.ghosts){const d=Math.hypot(player.x-g.position.x,player.z-g.position.z);if(d<2.7&&d<best){best=d;found={uid,ghost:g};}}return found;}`
- [x] `if` — linha 5050, coluna 5 em `applyCloudWorldObjects()` — `if(!worldGroup)return;const me=window.OTTHOS_RTDB?.uid||'';`
- [x] `return` — linha 5050, coluna 20 em `applyCloudWorldObjects()` — `if(!worldGroup)return;const me=window.OTTHOS_RTDB?.uid||'';`
- [x] `for` — linha 5051, coluna 24 em `applyCloudWorldObjects()` — `const fireItems=[];for(const[ownerUid,items]of Object.entries(pendingCloudCampfires||{}))if(ownerUid!==me)for(const item of Object.values(items||{}))if(item&&Number(item.expiresAt||0)>Date.now())fireItems.push({...item,ownerUid});`
- [x] `if` — linha 5051, coluna 94 em `applyCloudWorldObjects()` — `const fireItems=[];for(const[ownerUid,items]of Object.entries(pendingCloudCampfires||{}))if(ownerUid!==me)for(const item of Object.values(items||{}))if(item&&Number(item.expiresAt||0)>Date.now())fireItems.push({...item,ownerUid});`
- [x] `for` — linha 5051, coluna 111 em `applyCloudWorldObjects()` — `const fireItems=[];for(const[ownerUid,items]of Object.entries(pendingCloudCampfires||{}))if(ownerUid!==me)for(const item of Object.values(items||{}))if(item&&Number(item.expiresAt||0)>Date.now())fireItems.push({...item,ownerUid});`
- [x] `if` — linha 5051, coluna 154 em `applyCloudWorldObjects()` — `const fireItems=[];for(const[ownerUid,items]of Object.entries(pendingCloudCampfires||{}))if(ownerUid!==me)for(const item of Object.values(items||{}))if(item&&Number(item.expiresAt||0)>Date.now())fireItems.push({...item,ownerUid});`
- [x] `for` — linha 5052, coluna 52 em `applyCloudWorldObjects()` — `const fireKeys=new Set(fireItems.map(x=>x.id));for(const c of [...world.campfires])if(c.data.remote&&!fireKeys.has(c.data.id)){c.interactable.disabled=true;worldGroup.remove(c.group);world.campfires=world.campfires.filter(x=>x!==c);}fireItems.forEach(item=>spawnCampfire(item,true));`
- [x] `if` — linha 5052, coluna 88 em `applyCloudWorldObjects()` — `const fireKeys=new Set(fireItems.map(x=>x.id));for(const c of [...world.campfires])if(c.data.remote&&!fireKeys.has(c.data.id)){c.interactable.disabled=true;worldGroup.remove(c.group);world.campfires=world.campfires.filter(x=>x!==c);}fireItems.forEach(item=>spawnCampfire(item,true));`
- [x] `for` — linha 5053, coluna 29 em `applyCloudWorldObjects()` — `const extensionItems=[];for(const[ownerUid,items]of Object.entries(pendingCloudExtensions||{}))if(ownerUid!==me)for(const item of Object.values(items||{}))if(item)extensionItems.push({...item,ownerUid});`
- [x] `if` — linha 5053, coluna 100 em `applyCloudWorldObjects()` — `const extensionItems=[];for(const[ownerUid,items]of Object.entries(pendingCloudExtensions||{}))if(ownerUid!==me)for(const item of Object.values(items||{}))if(item)extensionItems.push({...item,ownerUid});`
- [x] `for` — linha 5053, coluna 117 em `applyCloudWorldObjects()` — `const extensionItems=[];for(const[ownerUid,items]of Object.entries(pendingCloudExtensions||{}))if(ownerUid!==me)for(const item of Object.values(items||{}))if(item)extensionItems.push({...item,ownerUid});`
- [x] `if` — linha 5053, coluna 160 em `applyCloudWorldObjects()` — `const extensionItems=[];for(const[ownerUid,items]of Object.entries(pendingCloudExtensions||{}))if(ownerUid!==me)for(const item of Object.values(items||{}))if(item)extensionItems.push({...item,ownerUid});`
- [x] `for` — linha 5054, coluna 62 em `applyCloudWorldObjects()` — `const extensionKeys=new Set(extensionItems.map(x=>x.id));for(const e of [...world.houseExtensions])if(e.data.remote&&!extensionKeys.has(e.data.id)){worldGroup.remove(e.group);world.houseExtensions=world.houseExtensions.filter(x=>x!==e);const it=world.interactables.find(x=>x.id===\`extension-${e.data.id}\`);if(it)it.disabled=true;}extensionItems.forEach(item=>spawnHouseExtension(item,true));`
- [x] `if` — linha 5054, coluna 104 em `applyCloudWorldObjects()` — `const extensionKeys=new Set(extensionItems.map(x=>x.id));for(const e of [...world.houseExtensions])if(e.data.remote&&!extensionKeys.has(e.data.id)){worldGroup.remove(e.group);world.houseExtensions=world.houseExtensions.filter(x=>x!==e);const it=world.interactables.find(x=>x.id===\`extension-${e.data.id}\`);if(it)it.disabled=true;}extensionItems.forEach(item=>spawnHouseExtension(item,true));`
- [x] `if` — linha 5054, coluna 311 em `applyCloudWorldObjects()` — `const extensionKeys=new Set(extensionItems.map(x=>x.id));for(const e of [...world.houseExtensions])if(e.data.remote&&!extensionKeys.has(e.data.id)){worldGroup.remove(e.group);world.houseExtensions=world.houseExtensions.filter(x=>x!==e);const it=world.interactables.find(x=>x.id===\`extension-${e.data.id}\`);if(it)it.disabled=true;}extensionItems.forEach(item=>spawnHouseExtension(item,true));`
- [x] `if` — linha 5056, coluna 36 em `remotePlayerEvent()` — `function remotePlayerEvent(data){if(!guardianMultiplayerAllowed()||!data||data.uid===window.OTTHOS_RTDB?.uid||window.OTTHOS_RTDB?.isPlayerBlocked?.(data.uid))return;const safeData={...data,name:remotePlayerName(data.name,data.uid)};remotePresence.set(data.uid,safeData);let ghost=world.ghosts.get(data.uid);if(scene&&!ghost){ghost=createGhost(data.color||0x5ad8ff,safeData.name);world.ghosts.set(data.uid,ghost);}if(ghost){updateGhostName(ghost,safeData.name);ghost.userData.target=safeData;}refreshOpenSocialHub();}`
- [x] `return` — linha 5056, coluna 161 em `remotePlayerEvent()` — `function remotePlayerEvent(data){if(!guardianMultiplayerAllowed()||!data||data.uid===window.OTTHOS_RTDB?.uid||window.OTTHOS_RTDB?.isPlayerBlocked?.(data.uid))return;const safeData={...data,name:remotePlayerName(data.name,data.uid)};remotePresence.set(data.uid,safeData);let ghost=world.ghosts.get(data.uid);if(scene&&!ghost){ghost=createGhost(data.color||0x5ad8ff,safeData.name);world.ghosts.set(data.uid,ghost);}if(ghost){updateGhostName(ghost,safeData.name);ghost.userData.target=safeData;}refreshOpenSocialHub();}`
- [x] `if` — linha 5056, coluna 310 em `remotePlayerEvent()` — `function remotePlayerEvent(data){if(!guardianMultiplayerAllowed()||!data||data.uid===window.OTTHOS_RTDB?.uid||window.OTTHOS_RTDB?.isPlayerBlocked?.(data.uid))return;const safeData={...data,name:remotePlayerName(data.name,data.uid)};remotePresence.set(data.uid,safeData);let ghost=world.ghosts.get(data.uid);if(scene&&!ghost){ghost=createGhost(data.color||0x5ad8ff,safeData.name);world.ghosts.set(data.uid,ghost);}if(ghost){updateGhostName(ghost,safeData.name);ghost.userData.target=safeData;}refreshOpenSocialHub();}`
- [x] `if` — linha 5056, coluna 416 em `remotePlayerEvent()` — `function remotePlayerEvent(data){if(!guardianMultiplayerAllowed()||!data||data.uid===window.OTTHOS_RTDB?.uid||window.OTTHOS_RTDB?.isPlayerBlocked?.(data.uid))return;const safeData={...data,name:remotePlayerName(data.name,data.uid)};remotePresence.set(data.uid,safeData);let ghost=world.ghosts.get(data.uid);if(scene&&!ghost){ghost=createGhost(data.color||0x5ad8ff,safeData.name);world.ghosts.set(data.uid,ghost);}if(ghost){updateGhostName(ghost,safeData.name);ghost.userData.target=safeData;}refreshOpenSocialHub();}`
- [x] `if` — linha 5057, coluna 159 em `remotePlayerEvent()` — `window.addEventListener('otthos:mp-status',e=>{multiplayerState={...multiplayerState,...e.detail};state.multiplayer.cloudReady=!!multiplayerState.connected;if(e.detail?.uid)state.multiplayer.cloudUid=e.detail.uid;if(e.detail?.room)state.multiplayer.room=normalizeRoomId(e.detail.room);if(Array.isArray(e.detail?.players)){remotePresence.clear();e.detail.players.filter(p=>p.uid!==window.OTTHOS_RTDB?.uid&&!window.OTTHOS_RTDB?.isPlayerBlocked?.(p.uid)).forEach(p=>remotePresence.set(p.uid,{...p,name:remotePlayerName(p.name,p.uid)}));}updateMultiplayerBadge();refreshOpenSocialHub();});`
- [x] `if` — linha 5057, coluna 216 em `remotePlayerEvent()` — `window.addEventListener('otthos:mp-status',e=>{multiplayerState={...multiplayerState,...e.detail};state.multiplayer.cloudReady=!!multiplayerState.connected;if(e.detail?.uid)state.multiplayer.cloudUid=e.detail.uid;if(e.detail?.room)state.multiplayer.room=normalizeRoomId(e.detail.room);if(Array.isArray(e.detail?.players)){remotePresence.clear();e.detail.players.filter(p=>p.uid!==window.OTTHOS_RTDB?.uid&&!window.OTTHOS_RTDB?.isPlayerBlocked?.(p.uid)).forEach(p=>remotePresence.set(p.uid,{...p,name:remotePlayerName(p.name,p.uid)}));}updateMultiplayerBadge();refreshOpenSocialHub();});`
- [x] `if` — linha 5057, coluna 288 em `remotePlayerEvent()` — `window.addEventListener('otthos:mp-status',e=>{multiplayerState={...multiplayerState,...e.detail};state.multiplayer.cloudReady=!!multiplayerState.connected;if(e.detail?.uid)state.multiplayer.cloudUid=e.detail.uid;if(e.detail?.room)state.multiplayer.room=normalizeRoomId(e.detail.room);if(Array.isArray(e.detail?.players)){remotePresence.clear();e.detail.players.filter(p=>p.uid!==window.OTTHOS_RTDB?.uid&&!window.OTTHOS_RTDB?.isPlayerBlocked?.(p.uid)).forEach(p=>remotePresence.set(p.uid,{...p,name:remotePlayerName(p.name,p.uid)}));}updateMultiplayerBadge();refreshOpenSocialHub();});`
- [x] `if` — linha 5058, coluna 110 em `remotePlayerEvent()` — `window.addEventListener('otthi:guardian-settings',e=>{state.guardian={...state.guardian,...(e.detail||{})};if(!guardianCommunicationAllowed())closeChallengePrompt();if(!guardianMultiplayerAllowed()){for(const ghost of world.ghosts.values())scene?.remove(ghost);world.ghosts.clear();remotePresence.clear();}saveState();updateOnlineAttention();updateMultiplayerBadge();refreshOpenSocialHub();});`
- [x] `if` — linha 5058, coluna 168 em `remotePlayerEvent()` — `window.addEventListener('otthi:guardian-settings',e=>{state.guardian={...state.guardian,...(e.detail||{})};if(!guardianCommunicationAllowed())closeChallengePrompt();if(!guardianMultiplayerAllowed()){for(const ghost of world.ghosts.values())scene?.remove(ghost);world.ghosts.clear();remotePresence.clear();}saveState();updateOnlineAttention();updateMultiplayerBadge();refreshOpenSocialHub();});`
- [x] `for` — linha 5058, coluna 202 em `remotePlayerEvent()` — `window.addEventListener('otthi:guardian-settings',e=>{state.guardian={...state.guardian,...(e.detail||{})};if(!guardianCommunicationAllowed())closeChallengePrompt();if(!guardianMultiplayerAllowed()){for(const ghost of world.ghosts.values())scene?.remove(ghost);world.ghosts.clear();remotePresence.clear();}saveState();updateOnlineAttention();updateMultiplayerBadge();refreshOpenSocialHub();});`
- [x] `if` — linha 5060, coluna 98 em `remotePlayerEvent()` — `window.addEventListener('otthos:mp-leave',e=>{const id=e.detail?.uid;remotePresence.delete(id);if(player.boat.passengerUid===id)player.boat.passengerUid='';if(player.car.passengerUid===id)player.car.passengerUid='';const ghost=world.ghosts.get(id);if(ghost){scene?.remove(ghost);world.ghosts.delete(id);}multiplayerState.count=Math.max(1,remotePresence.size+1);updateMultiplayerBadge();refreshOpenSocialHub();});`
- [x] `if` — linha 5060, coluna 159 em `remotePlayerEvent()` — `window.addEventListener('otthos:mp-leave',e=>{const id=e.detail?.uid;remotePresence.delete(id);if(player.boat.passengerUid===id)player.boat.passengerUid='';if(player.car.passengerUid===id)player.car.passengerUid='';const ghost=world.ghosts.get(id);if(ghost){scene?.remove(ghost);world.ghosts.delete(id);}multiplayerState.count=Math.max(1,remotePresence.size+1);updateMultiplayerBadge();refreshOpenSocialHub();});`
- [x] `if` — linha 5060, coluna 251 em `remotePlayerEvent()` — `window.addEventListener('otthos:mp-leave',e=>{const id=e.detail?.uid;remotePresence.delete(id);if(player.boat.passengerUid===id)player.boat.passengerUid='';if(player.car.passengerUid===id)player.car.passengerUid='';const ghost=world.ghosts.get(id);if(ghost){scene?.remove(ghost);world.ghosts.delete(id);}multiplayerState.count=Math.max(1,remotePresence.size+1);updateMultiplayerBadge();refreshOpenSocialHub();});`
- [x] `for` — linha 5064, coluna 67 em `remotePlayerEvent()` — `window.addEventListener('otthos:houses',e=>{cloudHouses.clear();for(const [id,data] of Object.entries(e.detail||{}))cloudHouses.set(id,data);reconcileCloudHouses();});`
- [x] `if` — linha 5065, coluna 69 em `remotePlayerEvent()` — `window.addEventListener('otthos:chat',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming||window.OTTHOS_RTDB?.isPlayerBlocked?.(incoming.senderUid)||Number(incoming.createdAt||0)<=Number(state.social.chatHiddenBefore||0))return;const m={...incoming,name:remotePlayerName(incoming.name,incoming.senderUid)};const existing=cloudChat.findIndex(x=>x.id===m.id);if(existing>=0)cloudChat[existing]=m;else cloudChat.push(m);cloudChat.sort((a,b)=>Number(a.createdAt||0)-Number(b.createdAt||0));while(cloudChat.length>60)cloudChat.shift();refreshOpenSocialHub();if(m.senderUid!==window.OTTHOS_RTDB?.uid)toast(\`${m.name}: ${m.text}\`,'good',2200);});`
- [x] `return` — linha 5065, coluna 247 em `remotePlayerEvent()` — `window.addEventListener('otthos:chat',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming||window.OTTHOS_RTDB?.isPlayerBlocked?.(incoming.senderUid)||Number(incoming.createdAt||0)<=Number(state.social.chatHiddenBefore||0))return;const m={...incoming,name:remotePlayerName(incoming.name,incoming.senderUid)};const existing=cloudChat.findIndex(x=>x.id===m.id);if(existing>=0)cloudChat[existing]=m;else cloudChat.push(m);cloudChat.sort((a,b)=>Number(a.createdAt||0)-Number(b.createdAt||0));while(cloudChat.length>60)cloudChat.shift();refreshOpenSocialHub();if(m.senderUid!==window.OTTHOS_RTDB?.uid)toast(\`${m.name}: ${m.text}\`,'good',2200);});`
- [x] `if` — linha 5065, coluna 383 em `remotePlayerEvent()` — `window.addEventListener('otthos:chat',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming||window.OTTHOS_RTDB?.isPlayerBlocked?.(incoming.senderUid)||Number(incoming.createdAt||0)<=Number(state.social.chatHiddenBefore||0))return;const m={...incoming,name:remotePlayerName(incoming.name,incoming.senderUid)};const existing=cloudChat.findIndex(x=>x.id===m.id);if(existing>=0)cloudChat[existing]=m;else cloudChat.push(m);cloudChat.sort((a,b)=>Number(a.createdAt||0)-Number(b.createdAt||0));while(cloudChat.length>60)cloudChat.shift();refreshOpenSocialHub();if(m.senderUid!==window.OTTHOS_RTDB?.uid)toast(\`${m.name}: ${m.text}\`,'good',2200);});`
- [x] `else` — linha 5065, coluna 420 em `remotePlayerEvent()` — `window.addEventListener('otthos:chat',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming||window.OTTHOS_RTDB?.isPlayerBlocked?.(incoming.senderUid)||Number(incoming.createdAt||0)<=Number(state.social.chatHiddenBefore||0))return;const m={...incoming,name:remotePlayerName(incoming.name,incoming.senderUid)};const existing=cloudChat.findIndex(x=>x.id===m.id);if(existing>=0)cloudChat[existing]=m;else cloudChat.push(m);cloudChat.sort((a,b)=>Number(a.createdAt||0)-Number(b.createdAt||0));while(cloudChat.length>60)cloudChat.shift();refreshOpenSocialHub();if(m.senderUid!==window.OTTHOS_RTDB?.uid)toast(\`${m.name}: ${m.text}\`,'good',2200);});`
- [x] `while` — linha 5065, coluna 512 em `remotePlayerEvent()` — `window.addEventListener('otthos:chat',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming||window.OTTHOS_RTDB?.isPlayerBlocked?.(incoming.senderUid)||Number(incoming.createdAt||0)<=Number(state.social.chatHiddenBefore||0))return;const m={...incoming,name:remotePlayerName(incoming.name,incoming.senderUid)};const existing=cloudChat.findIndex(x=>x.id===m.id);if(existing>=0)cloudChat[existing]=m;else cloudChat.push(m);cloudChat.sort((a,b)=>Number(a.createdAt||0)-Number(b.createdAt||0));while(cloudChat.length>60)cloudChat.shift();refreshOpenSocialHub();if(m.senderUid!==window.OTTHOS_RTDB?.uid)toast(\`${m.name}: ${m.text}\`,'good',2200);});`
- [x] `if` — linha 5065, coluna 579 em `remotePlayerEvent()` — `window.addEventListener('otthos:chat',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming||window.OTTHOS_RTDB?.isPlayerBlocked?.(incoming.senderUid)||Number(incoming.createdAt||0)<=Number(state.social.chatHiddenBefore||0))return;const m={...incoming,name:remotePlayerName(incoming.name,incoming.senderUid)};const existing=cloudChat.findIndex(x=>x.id===m.id);if(existing>=0)cloudChat[existing]=m;else cloudChat.push(m);cloudChat.sort((a,b)=>Number(a.createdAt||0)-Number(b.createdAt||0));while(cloudChat.length>60)cloudChat.shift();refreshOpenSocialHub();if(m.senderUid!==window.OTTHOS_RTDB?.uid)toast(\`${m.name}: ${m.text}\`,'good',2200);});`
- [x] `if` — linha 5066, coluna 115 em `remotePlayerEvent()` — `window.addEventListener('otthos:chat-removed',e=>{const id=e.detail?.id,index=cloudChat.findIndex(m=>m.id===id);if(index>=0)cloudChat.splice(index,1);refreshOpenSocialHub();});`
- [x] `if` — linha 5067, coluna 65 em `remotePlayerEvent()` — `window.addEventListener('otthos:gift',e=>{const gift=e.detail;if(!guardianCommunicationAllowed()||!gift)return;if(!((gift.type==='coins'&&Number(gift.amount)===10)||(gift.type==='crystal'&&Number(gift.amount)===1)))return;toast('🎁 Você recebeu um presente simbólico!','good',2600);});`
- [x] `return` — linha 5067, coluna 107 em `remotePlayerEvent()` — `window.addEventListener('otthos:gift',e=>{const gift=e.detail;if(!guardianCommunicationAllowed()||!gift)return;if(!((gift.type==='coins'&&Number(gift.amount)===10)||(gift.type==='crystal'&&Number(gift.amount)===1)))return;toast('🎁 Você recebeu um presente simbólico!','good',2600);});`
- [x] `if` — linha 5067, coluna 114 em `remotePlayerEvent()` — `window.addEventListener('otthos:gift',e=>{const gift=e.detail;if(!guardianCommunicationAllowed()||!gift)return;if(!((gift.type==='coins'&&Number(gift.amount)===10)||(gift.type==='crystal'&&Number(gift.amount)===1)))return;toast('🎁 Você recebeu um presente simbólico!','good',2600);});`
- [x] `return` — linha 5067, coluna 218 em `remotePlayerEvent()` — `window.addEventListener('otthos:gift',e=>{const gift=e.detail;if(!guardianCommunicationAllowed()||!gift)return;if(!((gift.type==='coins'&&Number(gift.amount)===10)||(gift.type==='crystal'&&Number(gift.amount)===1)))return;toast('🎁 Você recebeu um presente simbólico!','good',2600);});`
- [x] `if` — linha 5068, coluna 70 em `remotePlayerEvent()` — `window.addEventListener('otthos:interaction',e=>{const it=e.detail;if(!guardianCommunicationAllowed()||!it)return;const sender='Outro jogador';`
- [x] `return` — linha 5068, coluna 110 em `remotePlayerEvent()` — `window.addEventListener('otthos:interaction',e=>{const it=e.detail;if(!guardianCommunicationAllowed()||!it)return;const sender='Outro jogador';`
- [x] `if` — linha 5069, coluna 5 em `remotePlayerEvent()` — `if(it.type==='boatPassengerLeft'){if(player.boat.passengerUid!==it.senderUid)return;player.boat.passengerUid='';toast(\`${sender} saiu do barco.\`,'warn',1900);}`
- [x] `if` — linha 5069, coluna 39 em `remotePlayerEvent()` — `if(it.type==='boatPassengerLeft'){if(player.boat.passengerUid!==it.senderUid)return;player.boat.passengerUid='';toast(\`${sender} saiu do barco.\`,'warn',1900);}`
- [x] `return` — linha 5069, coluna 82 em `remotePlayerEvent()` — `if(it.type==='boatPassengerLeft'){if(player.boat.passengerUid!==it.senderUid)return;player.boat.passengerUid='';toast(\`${sender} saiu do barco.\`,'warn',1900);}`
- [x] `else` — linha 5070, coluna 5 em `remotePlayerEvent()` — `else if(it.type==='boatEnded'){if(player.boating&&player.boat.passengerOf===it.senderUid)exitBoat(true);toast('O motorista encerrou o passeio de barco.','warn',2300);}`
- [x] `if` — linha 5070, coluna 10 em `remotePlayerEvent()` — `else if(it.type==='boatEnded'){if(player.boating&&player.boat.passengerOf===it.senderUid)exitBoat(true);toast('O motorista encerrou o passeio de barco.','warn',2300);}`
- [x] `if` — linha 5070, coluna 36 em `remotePlayerEvent()` — `else if(it.type==='boatEnded'){if(player.boating&&player.boat.passengerOf===it.senderUid)exitBoat(true);toast('O motorista encerrou o passeio de barco.','warn',2300);}`
- [x] `else` — linha 5071, coluna 5 em `remotePlayerEvent()` — `else if(it.type==='vehiclePassengerLeft'){if(player.car.passengerUid!==it.senderUid)return;player.car.passengerUid='';toast(\`${sender} saiu do carro.\`,'warn',1900);}`
- [x] `if` — linha 5071, coluna 10 em `remotePlayerEvent()` — `else if(it.type==='vehiclePassengerLeft'){if(player.car.passengerUid!==it.senderUid)return;player.car.passengerUid='';toast(\`${sender} saiu do carro.\`,'warn',1900);}`
- [x] `if` — linha 5071, coluna 47 em `remotePlayerEvent()` — `else if(it.type==='vehiclePassengerLeft'){if(player.car.passengerUid!==it.senderUid)return;player.car.passengerUid='';toast(\`${sender} saiu do carro.\`,'warn',1900);}`
- [x] `return` — linha 5071, coluna 89 em `remotePlayerEvent()` — `else if(it.type==='vehiclePassengerLeft'){if(player.car.passengerUid!==it.senderUid)return;player.car.passengerUid='';toast(\`${sender} saiu do carro.\`,'warn',1900);}`
- [x] `else` — linha 5072, coluna 5 em `remotePlayerEvent()` — `else if(it.type==='vehicleEnded'){if(player.vehicle&&player.car.passengerOf===it.senderUid)exitVehicle(true);toast('O motorista encerrou o passeio de carro.','warn',2300);}`
- [x] `if` — linha 5072, coluna 10 em `remotePlayerEvent()` — `else if(it.type==='vehicleEnded'){if(player.vehicle&&player.car.passengerOf===it.senderUid)exitVehicle(true);toast('O motorista encerrou o passeio de carro.','warn',2300);}`
- [x] `if` — linha 5072, coluna 39 em `remotePlayerEvent()` — `else if(it.type==='vehicleEnded'){if(player.vehicle&&player.car.passengerOf===it.senderUid)exitVehicle(true);toast('O motorista encerrou o passeio de carro.','warn',2300);}`
- [x] `else` — linha 5073, coluna 5 em `remotePlayerEvent()` — `else if(it.type==='challengeAccepted')toast(\`🎮 ${sender} aceitou seu desafio! Abra Online para jogar.\`,'good',3400);`
- [x] `if` — linha 5073, coluna 10 em `remotePlayerEvent()` — `else if(it.type==='challengeAccepted')toast(\`🎮 ${sender} aceitou seu desafio! Abra Online para jogar.\`,'good',3400);`
- [x] `else` — linha 5074, coluna 5 em `remotePlayerEvent()` — `else if(it.type==='challengeDeclined')toast(\`${sender} recusou o desafio.\`,'warn',2400);`
- [x] `if` — linha 5074, coluna 10 em `remotePlayerEvent()` — `else if(it.type==='challengeDeclined')toast(\`${sender} recusou o desafio.\`,'warn',2400);`
- [x] `else` — linha 5075, coluna 5 em `remotePlayerEvent()` — `else if(it.type==='socialRequestResult'){`
- [x] `if` — linha 5075, coluna 10 em `remotePlayerEvent()` — `else if(it.type==='socialRequestResult'){`
- [x] `if` — linha 5076, coluna 59 em `remotePlayerEvent()` — `const status=it.status||'',action=it.actionType||'';if(status==='accepted'){toast(\`✅ ${sender} aceitou ${socialActionLabel(action)}.\`,'good',2600);applyAcceptedSocialAction(action,{requestId:it.requestId,partnerName:sender,partnerUid:it.senderUid,role:'host'});}else if(status==='declined')toast(\`${sender} recusou o convite.\`,'warn',2300);else toast('O convite foi cancelado ou expirou.','warn',2500);`
- [x] `else` — linha 5076, coluna 269 em `remotePlayerEvent()` — `const status=it.status||'',action=it.actionType||'';if(status==='accepted'){toast(\`✅ ${sender} aceitou ${socialActionLabel(action)}.\`,'good',2600);applyAcceptedSocialAction(action,{requestId:it.requestId,partnerName:sender,partnerUid:it.senderUid,role:'host'});}else if(status==='declined')toast(\`${sender} recusou o convite.\`,'warn',2300);else toast('O convite foi cancelado ou expirou.','warn',2500);`
- [x] `if` — linha 5076, coluna 274 em `remotePlayerEvent()` — `const status=it.status||'',action=it.actionType||'';if(status==='accepted'){toast(\`✅ ${sender} aceitou ${socialActionLabel(action)}.\`,'good',2600);applyAcceptedSocialAction(action,{requestId:it.requestId,partnerName:sender,partnerUid:it.senderUid,role:'host'});}else if(status==='declined')toast(\`${sender} recusou o convite.\`,'warn',2300);else toast('O convite foi cancelado ou expirou.','warn',2500);`
- [x] `else` — linha 5076, coluna 347 em `remotePlayerEvent()` — `const status=it.status||'',action=it.actionType||'';if(status==='accepted'){toast(\`✅ ${sender} aceitou ${socialActionLabel(action)}.\`,'good',2600);applyAcceptedSocialAction(action,{requestId:it.requestId,partnerName:sender,partnerUid:it.senderUid,role:'host'});}else if(status==='declined')toast(\`${sender} recusou o convite.\`,'warn',2300);else toast('O convite foi cancelado ou expirou.','warn',2500);`
- [x] `else` — linha 5077, coluna 6 em `remotePlayerEvent()` — `}else{const social=it.type==='wave'?'wave':'wave';triggerEmote(social);toast(\`👋 ${sender} acenou para você.\`,'good',2200);}`
- [x] `if` — linha 5079, coluna 79 em `remotePlayerEvent()` — `window.addEventListener('otthos:social-request',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const r={...incoming,fromName:remotePlayerName(incoming.fromName,incoming.fromUid),toName:remotePlayerName(incoming.toName,incoming.toUid)};incomingSocialRequests.set(r.id,r);updateOnlineAttention();refreshOpenSocialHub();if(r.status==='pending'&&Number(r.expiresAt||0)>Date.now()){showIncomingSocialRequest(r);if(!shownSocialToasts.has(r.id)){shownSocialToasts.add(r.id);toast(\`🤝 ${r.fromName} quer ${socialActionLabel(r.actionType)}.\`,'good',3000);}}else if(promptSocialRequestId===r.id)closeChallengePrompt();});`
- [x] `return` — linha 5079, coluna 125 em `remotePlayerEvent()` — `window.addEventListener('otthos:social-request',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const r={...incoming,fromName:remotePlayerName(incoming.fromName,incoming.fromUid),toName:remotePlayerName(incoming.toName,incoming.toUid)};incomingSocialRequests.set(r.id,r);updateOnlineAttention();refreshOpenSocialHub();if(r.status==='pending'&&Number(r.expiresAt||0)>Date.now()){showIncomingSocialRequest(r);if(!shownSocialToasts.has(r.id)){shownSocialToasts.add(r.id);toast(\`🤝 ${r.fromName} quer ${socialActionLabel(r.actionType)}.\`,'good',3000);}}else if(promptSocialRequestId===r.id)closeChallengePrompt();});`
- [x] `if` — linha 5079, coluna 354 em `remotePlayerEvent()` — `window.addEventListener('otthos:social-request',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const r={...incoming,fromName:remotePlayerName(incoming.fromName,incoming.fromUid),toName:remotePlayerName(incoming.toName,incoming.toUid)};incomingSocialRequests.set(r.id,r);updateOnlineAttention();refreshOpenSocialHub();if(r.status==='pending'&&Number(r.expiresAt||0)>Date.now()){showIncomingSocialRequest(r);if(!shownSocialToasts.has(r.id)){shownSocialToasts.add(r.id);toast(\`🤝 ${r.fromName} quer ${socialActionLabel(r.actionType)}.\`,'good',3000);}}else if(promptSocialRequestId===r.id)closeChallengePrompt();});`
- [x] `if` — linha 5079, coluna 443 em `remotePlayerEvent()` — `window.addEventListener('otthos:social-request',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const r={...incoming,fromName:remotePlayerName(incoming.fromName,incoming.fromUid),toName:remotePlayerName(incoming.toName,incoming.toUid)};incomingSocialRequests.set(r.id,r);updateOnlineAttention();refreshOpenSocialHub();if(r.status==='pending'&&Number(r.expiresAt||0)>Date.now()){showIncomingSocialRequest(r);if(!shownSocialToasts.has(r.id)){shownSocialToasts.add(r.id);toast(\`🤝 ${r.fromName} quer ${socialActionLabel(r.actionType)}.\`,'good',3000);}}else if(promptSocialRequestId===r.id)closeChallengePrompt();});`
- [x] `else` — linha 5079, coluna 584 em `remotePlayerEvent()` — `window.addEventListener('otthos:social-request',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const r={...incoming,fromName:remotePlayerName(incoming.fromName,incoming.fromUid),toName:remotePlayerName(incoming.toName,incoming.toUid)};incomingSocialRequests.set(r.id,r);updateOnlineAttention();refreshOpenSocialHub();if(r.status==='pending'&&Number(r.expiresAt||0)>Date.now()){showIncomingSocialRequest(r);if(!shownSocialToasts.has(r.id)){shownSocialToasts.add(r.id);toast(\`🤝 ${r.fromName} quer ${socialActionLabel(r.actionType)}.\`,'good',3000);}}else if(promptSocialRequestId===r.id)closeChallengePrompt();});`
- [x] `if` — linha 5079, coluna 589 em `remotePlayerEvent()` — `window.addEventListener('otthos:social-request',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const r={...incoming,fromName:remotePlayerName(incoming.fromName,incoming.fromUid),toName:remotePlayerName(incoming.toName,incoming.toUid)};incomingSocialRequests.set(r.id,r);updateOnlineAttention();refreshOpenSocialHub();if(r.status==='pending'&&Number(r.expiresAt||0)>Date.now()){showIncomingSocialRequest(r);if(!shownSocialToasts.has(r.id)){shownSocialToasts.add(r.id);toast(\`🤝 ${r.fromName} quer ${socialActionLabel(r.actionType)}.\`,'good',3000);}}else if(promptSocialRequestId===r.id)closeChallengePrompt();});`
- [x] `if` — linha 5080, coluna 107 em `remotePlayerEvent()` — `window.addEventListener('otthos:social-request-removed',e=>{incomingSocialRequests.delete(e.detail?.id);if(promptSocialRequestId===e.detail?.id)closeChallengePrompt();updateOnlineAttention();refreshOpenSocialHub();});`
- [x] `if` — linha 5082, coluna 74 em `remotePlayerEvent()` — `window.addEventListener('otthos:challenge',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const c={...incoming,fromName:remotePlayerName(incoming.fromName,incoming.fromUid),toName:remotePlayerName(incoming.toName,incoming.toUid)};incomingChallenges.set(c.id,c);updateOnlineAttention();refreshOpenSocialHub();if(c.status==='pending'){showIncomingChallengePrompt(c);if(!shownChallengeToasts.has(c.id)){shownChallengeToasts.add(c.id);toast(\`⚔️ ${c.fromName} enviou ${multiplayerGameLabel(c.type)}!\`,'good',3400);}}});`
- [x] `return` — linha 5082, coluna 120 em `remotePlayerEvent()` — `window.addEventListener('otthos:challenge',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const c={...incoming,fromName:remotePlayerName(incoming.fromName,incoming.fromUid),toName:remotePlayerName(incoming.toName,incoming.toUid)};incomingChallenges.set(c.id,c);updateOnlineAttention();refreshOpenSocialHub();if(c.status==='pending'){showIncomingChallengePrompt(c);if(!shownChallengeToasts.has(c.id)){shownChallengeToasts.add(c.id);toast(\`⚔️ ${c.fromName} enviou ${multiplayerGameLabel(c.type)}!\`,'good',3400);}}});`
- [x] `if` — linha 5082, coluna 345 em `remotePlayerEvent()` — `window.addEventListener('otthos:challenge',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const c={...incoming,fromName:remotePlayerName(incoming.fromName,incoming.fromUid),toName:remotePlayerName(incoming.toName,incoming.toUid)};incomingChallenges.set(c.id,c);updateOnlineAttention();refreshOpenSocialHub();if(c.status==='pending'){showIncomingChallengePrompt(c);if(!shownChallengeToasts.has(c.id)){shownChallengeToasts.add(c.id);toast(\`⚔️ ${c.fromName} enviou ${multiplayerGameLabel(c.type)}!\`,'good',3400);}}});`
- [x] `if` — linha 5082, coluna 401 em `remotePlayerEvent()` — `window.addEventListener('otthos:challenge',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const c={...incoming,fromName:remotePlayerName(incoming.fromName,incoming.fromUid),toName:remotePlayerName(incoming.toName,incoming.toUid)};incomingChallenges.set(c.id,c);updateOnlineAttention();refreshOpenSocialHub();if(c.status==='pending'){showIncomingChallengePrompt(c);if(!shownChallengeToasts.has(c.id)){shownChallengeToasts.add(c.id);toast(\`⚔️ ${c.fromName} enviou ${multiplayerGameLabel(c.type)}!\`,'good',3400);}}});`
- [x] `if` — linha 5084, coluna 77 em `remotePlayerEvent()` — `window.addEventListener('otthos:game-session',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const s=sanitizeRemoteSession(incoming);gameSessions.set(s.id,s);updateOnlineAttention();refreshOpenSocialHub();if(s.status==='active'){const mine=s.players?.[window.OTTHOS_RTDB?.uid];if(!mine?.finished&&!activeMultiplayerGameId)showReadySessionPrompt(s);}if(s.status==='active'||s.status==='completed')maybeShowMultiplayerResult(s);});`
- [x] `return` — linha 5084, coluna 123 em `remotePlayerEvent()` — `window.addEventListener('otthos:game-session',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const s=sanitizeRemoteSession(incoming);gameSessions.set(s.id,s);updateOnlineAttention();refreshOpenSocialHub();if(s.status==='active'){const mine=s.players?.[window.OTTHOS_RTDB?.uid];if(!mine?.finished&&!activeMultiplayerGameId)showReadySessionPrompt(s);}if(s.status==='active'||s.status==='completed')maybeShowMultiplayerResult(s);});`
- [x] `if` — linha 5084, coluna 242 em `remotePlayerEvent()` — `window.addEventListener('otthos:game-session',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const s=sanitizeRemoteSession(incoming);gameSessions.set(s.id,s);updateOnlineAttention();refreshOpenSocialHub();if(s.status==='active'){const mine=s.players?.[window.OTTHOS_RTDB?.uid];if(!mine?.finished&&!activeMultiplayerGameId)showReadySessionPrompt(s);}if(s.status==='active'||s.status==='completed')maybeShowMultiplayerResult(s);});`
- [x] `if` — linha 5084, coluna 314 em `remotePlayerEvent()` — `window.addEventListener('otthos:game-session',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const s=sanitizeRemoteSession(incoming);gameSessions.set(s.id,s);updateOnlineAttention();refreshOpenSocialHub();if(s.status==='active'){const mine=s.players?.[window.OTTHOS_RTDB?.uid];if(!mine?.finished&&!activeMultiplayerGameId)showReadySessionPrompt(s);}if(s.status==='active'||s.status==='completed')maybeShowMultiplayerResult(s);});`
- [x] `if` — linha 5084, coluna 386 em `remotePlayerEvent()` — `window.addEventListener('otthos:game-session',e=>{const incoming=e.detail;if(!guardianCommunicationAllowed()||!incoming)return;const s=sanitizeRemoteSession(incoming);gameSessions.set(s.id,s);updateOnlineAttention();refreshOpenSocialHub();if(s.status==='active'){const mine=s.players?.[window.OTTHOS_RTDB?.uid];if(!mine?.finished&&!activeMultiplayerGameId)showReadySessionPrompt(s);}if(s.status==='active'||s.status==='completed')maybeShowMultiplayerResult(s);});`
- [x] `if` — linha 5086, coluna 126 em `remotePlayerEvent()` — `window.addEventListener('otthos:rtdb-ready',async()=>{try{const guardian=await window.OTTHOS_RTDB?.getGuardianSettings?.();if(guardian&&typeof guardian==='object')state.guardian={...state.guardian,...(guardian.settings||guardian)};}catch{}if(!guardianMultiplayerAllowed()){window.OTTHOS_RTDB?.disconnect?.();return;}if(running||hasValidPlayerName()){const ok=await window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()});if(ok){window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);window.OTTHOS_RTDB?.syncHouseExtensions?.(state.houseExtensions);}}});`
- [x] `catch` — linha 5086, coluna 235 em `remotePlayerEvent()` — `window.addEventListener('otthos:rtdb-ready',async()=>{try{const guardian=await window.OTTHOS_RTDB?.getGuardianSettings?.();if(guardian&&typeof guardian==='object')state.guardian={...state.guardian,...(guardian.settings||guardian)};}catch{}if(!guardianMultiplayerAllowed()){window.OTTHOS_RTDB?.disconnect?.();return;}if(running||hasValidPlayerName()){const ok=await window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()});if(ok){window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);window.OTTHOS_RTDB?.syncHouseExtensions?.(state.houseExtensions);}}});`
- [x] `if` — linha 5086, coluna 242 em `remotePlayerEvent()` — `window.addEventListener('otthos:rtdb-ready',async()=>{try{const guardian=await window.OTTHOS_RTDB?.getGuardianSettings?.();if(guardian&&typeof guardian==='object')state.guardian={...state.guardian,...(guardian.settings||guardian)};}catch{}if(!guardianMultiplayerAllowed()){window.OTTHOS_RTDB?.disconnect?.();return;}if(running||hasValidPlayerName()){const ok=await window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()});if(ok){window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);window.OTTHOS_RTDB?.syncHouseExtensions?.(state.houseExtensions);}}});`
- [x] `return` — linha 5086, coluna 311 em `remotePlayerEvent()` — `window.addEventListener('otthos:rtdb-ready',async()=>{try{const guardian=await window.OTTHOS_RTDB?.getGuardianSettings?.();if(guardian&&typeof guardian==='object')state.guardian={...state.guardian,...(guardian.settings||guardian)};}catch{}if(!guardianMultiplayerAllowed()){window.OTTHOS_RTDB?.disconnect?.();return;}if(running||hasValidPlayerName()){const ok=await window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()});if(ok){window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);window.OTTHOS_RTDB?.syncHouseExtensions?.(state.houseExtensions);}}});`
- [x] `if` — linha 5086, coluna 319 em `remotePlayerEvent()` — `window.addEventListener('otthos:rtdb-ready',async()=>{try{const guardian=await window.OTTHOS_RTDB?.getGuardianSettings?.();if(guardian&&typeof guardian==='object')state.guardian={...state.guardian,...(guardian.settings||guardian)};}catch{}if(!guardianMultiplayerAllowed()){window.OTTHOS_RTDB?.disconnect?.();return;}if(running||hasValidPlayerName()){const ok=await window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()});if(ok){window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);window.OTTHOS_RTDB?.syncHouseExtensions?.(state.houseExtensions);}}});`
- [x] `if` — linha 5086, coluna 425 em `remotePlayerEvent()` — `window.addEventListener('otthos:rtdb-ready',async()=>{try{const guardian=await window.OTTHOS_RTDB?.getGuardianSettings?.();if(guardian&&typeof guardian==='object')state.guardian={...state.guardian,...(guardian.settings||guardian)};}catch{}if(!guardianMultiplayerAllowed()){window.OTTHOS_RTDB?.disconnect?.();return;}if(running||hasValidPlayerName()){const ok=await window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()});if(ok){window.OTTHOS_RTDB?.syncCampfires?.(state.campfires);window.OTTHOS_RTDB?.syncHouseExtensions?.(state.houseExtensions);}}});`
- [x] `if` — linha 5089, coluna 5 em `initLocalMultiplayer()` — `if(typeof BroadcastChannel==='function'){localChannel=new BroadcastChannel('otthos-life-world-v636');localChannel.onmessage=e=>{const data=e.data;if(!data||data.id===state.profile.playerId)return;if(data.type==='leave'){const ghost=world.ghosts.get(data.id);if(ghost){scene.remove(ghost);world.ghosts.delete(data.id);}return;}remotePlayerEvent({...data,uid:data.id});};window.addEventListener('beforeunload',()=>localChannel?.postMessage({type:'leave',id:state.profile.playerId}));}`
- [x] `if` — linha 5089, coluna 151 em `initLocalMultiplayer()` — `if(typeof BroadcastChannel==='function'){localChannel=new BroadcastChannel('otthos-life-world-v636');localChannel.onmessage=e=>{const data=e.data;if(!data||data.id===state.profile.playerId)return;if(data.type==='leave'){const ghost=world.ghosts.get(data.id);if(ghost){scene.remove(ghost);world.ghosts.delete(data.id);}return;}remotePlayerEvent({...data,uid:data.id});};window.addEventListener('beforeunload',()=>localChannel?.postMessage({type:'leave',id:state.profile.playerId}));}`
- [x] `return` — linha 5089, coluna 194 em `initLocalMultiplayer()` — `if(typeof BroadcastChannel==='function'){localChannel=new BroadcastChannel('otthos-life-world-v636');localChannel.onmessage=e=>{const data=e.data;if(!data||data.id===state.profile.playerId)return;if(data.type==='leave'){const ghost=world.ghosts.get(data.id);if(ghost){scene.remove(ghost);world.ghosts.delete(data.id);}return;}remotePlayerEvent({...data,uid:data.id});};window.addEventListener('beforeunload',()=>localChannel?.postMessage({type:'leave',id:state.profile.playerId}));}`
- [x] `if` — linha 5089, coluna 201 em `initLocalMultiplayer()` — `if(typeof BroadcastChannel==='function'){localChannel=new BroadcastChannel('otthos-life-world-v636');localChannel.onmessage=e=>{const data=e.data;if(!data||data.id===state.profile.playerId)return;if(data.type==='leave'){const ghost=world.ghosts.get(data.id);if(ghost){scene.remove(ghost);world.ghosts.delete(data.id);}return;}remotePlayerEvent({...data,uid:data.id});};window.addEventListener('beforeunload',()=>localChannel?.postMessage({type:'leave',id:state.profile.playerId}));}`
- [x] `if` — linha 5089, coluna 263 em `initLocalMultiplayer()` — `if(typeof BroadcastChannel==='function'){localChannel=new BroadcastChannel('otthos-life-world-v636');localChannel.onmessage=e=>{const data=e.data;if(!data||data.id===state.profile.playerId)return;if(data.type==='leave'){const ghost=world.ghosts.get(data.id);if(ghost){scene.remove(ghost);world.ghosts.delete(data.id);}return;}remotePlayerEvent({...data,uid:data.id});};window.addEventListener('beforeunload',()=>localChannel?.postMessage({type:'leave',id:state.profile.playerId}));}`
- [x] `return` — linha 5089, coluna 323 em `initLocalMultiplayer()` — `if(typeof BroadcastChannel==='function'){localChannel=new BroadcastChannel('otthos-life-world-v636');localChannel.onmessage=e=>{const data=e.data;if(!data||data.id===state.profile.playerId)return;if(data.type==='leave'){const ghost=world.ghosts.get(data.id);if(ghost){scene.remove(ghost);world.ghosts.delete(data.id);}return;}remotePlayerEvent({...data,uid:data.id});};window.addEventListener('beforeunload',()=>localChannel?.postMessage({type:'leave',id:state.profile.playerId}));}`
- [x] `if` — linha 5090, coluna 281 em `initLocalMultiplayer()` — `window.OTTHOS_MULTIPLAYER={version:8,playerId:state.profile.playerId,mode:window.OTTHOS_RTDB?.configured?'firebase-neighborhoods':'local-preview',connect:()=>guardianMultiplayerAllowed()?(window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()})||true):false,publish:payload=>{if(!guardianMultiplayerAllowed())return false;localChannel?.postMessage(payload);window.OTTHOS_RTDB?.publish?.(payload);return true;},adapter:window.OTTHOS_RTDB?.configured?'Firebase Realtime Database':'BroadcastChannel'};updateMultiplayerBadge();if(guardianMultiplayerAllowed()&&window.OTTHOS_RTDB?.configured&&hasValidPlayerName())window.OTTHOS_RTDB.connect({name:publicPlayerName()});`
- [x] `return` — linha 5090, coluna 314 em `initLocalMultiplayer()` — `window.OTTHOS_MULTIPLAYER={version:8,playerId:state.profile.playerId,mode:window.OTTHOS_RTDB?.configured?'firebase-neighborhoods':'local-preview',connect:()=>guardianMultiplayerAllowed()?(window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()})||true):false,publish:payload=>{if(!guardianMultiplayerAllowed())return false;localChannel?.postMessage(payload);window.OTTHOS_RTDB?.publish?.(payload);return true;},adapter:window.OTTHOS_RTDB?.configured?'Firebase Realtime Database':'BroadcastChannel'};updateMultiplayerBadge();if(guardianMultiplayerAllowed()&&window.OTTHOS_RTDB?.configured&&hasValidPlayerName())window.OTTHOS_RTDB.connect({name:publicPlayerName()});`
- [x] `return` — linha 5090, coluna 401 em `initLocalMultiplayer()` — `window.OTTHOS_MULTIPLAYER={version:8,playerId:state.profile.playerId,mode:window.OTTHOS_RTDB?.configured?'firebase-neighborhoods':'local-preview',connect:()=>guardianMultiplayerAllowed()?(window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()})||true):false,publish:payload=>{if(!guardianMultiplayerAllowed())return false;localChannel?.postMessage(payload);window.OTTHOS_RTDB?.publish?.(payload);return true;},adapter:window.OTTHOS_RTDB?.configured?'Firebase Realtime Database':'BroadcastChannel'};updateMultiplayerBadge();if(guardianMultiplayerAllowed()&&window.OTTHOS_RTDB?.configured&&hasValidPlayerName())window.OTTHOS_RTDB.connect({name:publicPlayerName()});`
- [x] `if` — linha 5090, coluna 528 em `initLocalMultiplayer()` — `window.OTTHOS_MULTIPLAYER={version:8,playerId:state.profile.playerId,mode:window.OTTHOS_RTDB?.configured?'firebase-neighborhoods':'local-preview',connect:()=>guardianMultiplayerAllowed()?(window.OTTHOS_RTDB?.connect?.({name:publicPlayerName()})||true):false,publish:payload=>{if(!guardianMultiplayerAllowed())return false;localChannel?.postMessage(payload);window.OTTHOS_RTDB?.publish?.(payload);return true;},adapter:window.OTTHOS_RTDB?.configured?'Firebase Realtime Database':'BroadcastChannel'};updateMultiplayerBadge();if(guardianMultiplayerAllowed()&&window.OTTHOS_RTDB?.configured&&hasValidPlayerName())window.OTTHOS_RTDB.connect({name:publicPlayerName()});`
- [x] `if` — linha 5092, coluna 289 em `multiplayerNameTexture()` — `function multiplayerNameTexture(name){const c=document.createElement('canvas');c.width=512;c.height=128;const ctx=c.getContext('2d');ctx.clearRect(0,0,c.width,c.height);ctx.fillStyle='rgba(5,18,34,.88)';ctx.strokeStyle='rgba(255,255,255,.92)';ctx.lineWidth=8;const r=30;ctx.beginPath();if(ctx.roundRect)ctx.roundRect(8,8,c.width-16,c.height-16,r);else ctx.rect(8,8,c.width-16,c.height-16);ctx.fill();ctx.stroke();ctx.fillStyle='#fff';ctx.font='900 48px system-ui,sans-serif';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(sanitizePlayerName(name)||'Jogador',c.width/2,c.height/2+2);const tex=new THREE.CanvasTexture(c);tex.minFilter=THREE.LinearFilter;tex.magFilter=THREE.LinearFilter;tex.generateMipmaps=false;return tex;}`
- [x] `else` — linha 5092, coluna 350 em `multiplayerNameTexture()` — `function multiplayerNameTexture(name){const c=document.createElement('canvas');c.width=512;c.height=128;const ctx=c.getContext('2d');ctx.clearRect(0,0,c.width,c.height);ctx.fillStyle='rgba(5,18,34,.88)';ctx.strokeStyle='rgba(255,255,255,.92)';ctx.lineWidth=8;const r=30;ctx.beginPath();if(ctx.roundRect)ctx.roundRect(8,8,c.width-16,c.height-16,r);else ctx.rect(8,8,c.width-16,c.height-16);ctx.fill();ctx.stroke();ctx.fillStyle='#fff';ctx.font='900 48px system-ui,sans-serif';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(sanitizePlayerName(name)||'Jogador',c.width/2,c.height/2+2);const tex=new THREE.CanvasTexture(c);tex.minFilter=THREE.LinearFilter;tex.magFilter=THREE.LinearFilter;tex.generateMipmaps=false;return tex;}`
- [x] `return` — linha 5092, coluna 729 em `multiplayerNameTexture()` — `function multiplayerNameTexture(name){const c=document.createElement('canvas');c.width=512;c.height=128;const ctx=c.getContext('2d');ctx.clearRect(0,0,c.width,c.height);ctx.fillStyle='rgba(5,18,34,.88)';ctx.strokeStyle='rgba(255,255,255,.92)';ctx.lineWidth=8;const r=30;ctx.beginPath();if(ctx.roundRect)ctx.roundRect(8,8,c.width-16,c.height-16,r);else ctx.rect(8,8,c.width-16,c.height-16);ctx.fill();ctx.stroke();ctx.fillStyle='#fff';ctx.font='900 48px system-ui,sans-serif';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(sanitizePlayerName(name)||'Jogador',c.width/2,c.height/2+2);const tex=new THREE.CanvasTexture(c);tex.minFilter=THREE.LinearFilter;tex.magFilter=THREE.LinearFilter;tex.generateMipmaps=false;return tex;}`
- [x] `if` — linha 5093, coluna 41 em `updateLocalPlayerNameLabel()` — `function updateLocalPlayerNameLabel(){if(!playerGroup?.userData?.nameLabel)return;const clean=playerDisplayName();if(playerGroup.userData.displayName===clean)return;const label=playerGroup.userData.nameLabel,old=label.material.map;label.material.map=multiplayerNameTexture(clean);label.material.needsUpdate=true;old?.dispose?.();playerGroup.userData.displayName=clean;}`
- [x] `return` — linha 5093, coluna 78 em `updateLocalPlayerNameLabel()` — `function updateLocalPlayerNameLabel(){if(!playerGroup?.userData?.nameLabel)return;const clean=playerDisplayName();if(playerGroup.userData.displayName===clean)return;const label=playerGroup.userData.nameLabel,old=label.material.map;label.material.map=multiplayerNameTexture(clean);label.material.needsUpdate=true;old?.dispose?.();playerGroup.userData.displayName=clean;}`
- [x] `if` — linha 5093, coluna 117 em `updateLocalPlayerNameLabel()` — `function updateLocalPlayerNameLabel(){if(!playerGroup?.userData?.nameLabel)return;const clean=playerDisplayName();if(playerGroup.userData.displayName===clean)return;const label=playerGroup.userData.nameLabel,old=label.material.map;label.material.map=multiplayerNameTexture(clean);label.material.needsUpdate=true;old?.dispose?.();playerGroup.userData.displayName=clean;}`
- [x] `return` — linha 5093, coluna 161 em `updateLocalPlayerNameLabel()` — `function updateLocalPlayerNameLabel(){if(!playerGroup?.userData?.nameLabel)return;const clean=playerDisplayName();if(playerGroup.userData.displayName===clean)return;const label=playerGroup.userData.nameLabel,old=label.material.map;label.material.map=multiplayerNameTexture(clean);label.material.needsUpdate=true;old?.dispose?.();playerGroup.userData.displayName=clean;}`
- [x] `if` — linha 5094, coluna 88 em `updateGhostName()` — `function updateGhostName(ghost,name){const clean=sanitizePlayerName(name)||'Jogador';if(ghost.userData.displayName===clean)return;ghost.userData.displayName=clean;if(ghost.userData.nameLabel){const old=ghost.userData.nameLabel.material.map;ghost.userData.nameLabel.material.map=multiplayerNameTexture(clean);ghost.userData.nameLabel.material.needsUpdate=true;old?.dispose?.();}}`
- [x] `return` — linha 5094, coluna 126 em `updateGhostName()` — `function updateGhostName(ghost,name){const clean=sanitizePlayerName(name)||'Jogador';if(ghost.userData.displayName===clean)return;ghost.userData.displayName=clean;if(ghost.userData.nameLabel){const old=ghost.userData.nameLabel.material.map;ghost.userData.nameLabel.material.map=multiplayerNameTexture(clean);ghost.userData.nameLabel.material.needsUpdate=true;old?.dispose?.();}}`
- [x] `if` — linha 5094, coluna 166 em `updateGhostName()` — `function updateGhostName(ghost,name){const clean=sanitizePlayerName(name)||'Jogador';if(ghost.userData.displayName===clean)return;ghost.userData.displayName=clean;if(ghost.userData.nameLabel){const old=ghost.userData.nameLabel.material.map;ghost.userData.nameLabel.material.map=multiplayerNameTexture(clean);ghost.userData.nameLabel.material.needsUpdate=true;old?.dispose?.();}}`
- [x] `if` — linha 5095, coluna 88 em `ghostVehicleKind()` — `function ghostVehicleKind(vehicleId=''){const id=String(vehicleId||'').toLowerCase();if(id.includes('patrol')||id.includes('police'))return'police';if(id.includes('fire'))return'firefighter';if(id.includes('ambulance')||id.includes('rescue')||id.includes('paramedic'))return'paramedic';return'car';}`
- [x] `return` — linha 5095, coluna 136 em `ghostVehicleKind()` — `function ghostVehicleKind(vehicleId=''){const id=String(vehicleId||'').toLowerCase();if(id.includes('patrol')||id.includes('police'))return'police';if(id.includes('fire'))return'firefighter';if(id.includes('ambulance')||id.includes('rescue')||id.includes('paramedic'))return'paramedic';return'car';}`
- [x] `if` — linha 5095, coluna 151 em `ghostVehicleKind()` — `function ghostVehicleKind(vehicleId=''){const id=String(vehicleId||'').toLowerCase();if(id.includes('patrol')||id.includes('police'))return'police';if(id.includes('fire'))return'firefighter';if(id.includes('ambulance')||id.includes('rescue')||id.includes('paramedic'))return'paramedic';return'car';}`
- [x] `return` — linha 5095, coluna 174 em `ghostVehicleKind()` — `function ghostVehicleKind(vehicleId=''){const id=String(vehicleId||'').toLowerCase();if(id.includes('patrol')||id.includes('police'))return'police';if(id.includes('fire'))return'firefighter';if(id.includes('ambulance')||id.includes('rescue')||id.includes('paramedic'))return'paramedic';return'car';}`
- [x] `if` — linha 5095, coluna 194 em `ghostVehicleKind()` — `function ghostVehicleKind(vehicleId=''){const id=String(vehicleId||'').toLowerCase();if(id.includes('patrol')||id.includes('police'))return'police';if(id.includes('fire'))return'firefighter';if(id.includes('ambulance')||id.includes('rescue')||id.includes('paramedic'))return'paramedic';return'car';}`
- [x] `return` — linha 5095, coluna 271 em `ghostVehicleKind()` — `function ghostVehicleKind(vehicleId=''){const id=String(vehicleId||'').toLowerCase();if(id.includes('patrol')||id.includes('police'))return'police';if(id.includes('fire'))return'firefighter';if(id.includes('ambulance')||id.includes('rescue')||id.includes('paramedic'))return'paramedic';return'car';}`
- [x] `return` — linha 5095, coluna 289 em `ghostVehicleKind()` — `function ghostVehicleKind(vehicleId=''){const id=String(vehicleId||'').toLowerCase();if(id.includes('patrol')||id.includes('police'))return'police';if(id.includes('fire'))return'firefighter';if(id.includes('ambulance')||id.includes('rescue')||id.includes('paramedic'))return'paramedic';return'car';}`
- [x] `if` — linha 5097, coluna 44 em `styleGhostVehicle()` — `const kind=ghostVehicleKind(vehicleId);if(ghost.userData.vehicleKind===kind)return;ghost.userData.vehicleKind=kind;const style=ghost.userData.carStyle||{},layers=ghost.userData.carServiceLayers||{};for(const layer of Object.values(layers))layer.visible=false;ghost.userData.carVisual.scale.set(1,1,1);`
- [x] `return` — linha 5097, coluna 81 em `styleGhostVehicle()` — `const kind=ghostVehicleKind(vehicleId);if(ghost.userData.vehicleKind===kind)return;ghost.userData.vehicleKind=kind;const style=ghost.userData.carStyle||{},layers=ghost.userData.carServiceLayers||{};for(const layer of Object.values(layers))layer.visible=false;ghost.userData.carVisual.scale.set(1,1,1);`
- [x] `for` — linha 5097, coluna 203 em `styleGhostVehicle()` — `const kind=ghostVehicleKind(vehicleId);if(ghost.userData.vehicleKind===kind)return;ghost.userData.vehicleKind=kind;const style=ghost.userData.carStyle||{},layers=ghost.userData.carServiceLayers||{};for(const layer of Object.values(layers))layer.visible=false;ghost.userData.carVisual.scale.set(1,1,1);`
- [x] `if` — linha 5098, coluna 5 em `styleGhostVehicle()` — `if(kind==='police'){style.body?.material?.color?.setHex(0xf4f7fb);style.cabin?.material?.color?.setHex(0x183d70);layers.police.visible=true;ghost.userData.carVisual.scale.set(1.04,1.04,1.08);}`
- [x] `else` — linha 5099, coluna 5 em `styleGhostVehicle()` — `else if(kind==='firefighter'){style.body?.material?.color?.setHex(0xc93431);style.cabin?.material?.color?.setHex(0xf2d84d);layers.firefighter.visible=true;ghost.userData.carVisual.scale.set(1.18,1.2,1.42);}`
- [x] `if` — linha 5099, coluna 10 em `styleGhostVehicle()` — `else if(kind==='firefighter'){style.body?.material?.color?.setHex(0xc93431);style.cabin?.material?.color?.setHex(0xf2d84d);layers.firefighter.visible=true;ghost.userData.carVisual.scale.set(1.18,1.2,1.42);}`
- [x] `else` — linha 5100, coluna 5 em `styleGhostVehicle()` — `else if(kind==='paramedic'){style.body?.material?.color?.setHex(0xf6f8fa);style.cabin?.material?.color?.setHex(0xe34848);layers.paramedic.visible=true;ghost.userData.carVisual.scale.set(1.08,1.12,1.22);}`
- [x] `if` — linha 5100, coluna 10 em `styleGhostVehicle()` — `else if(kind==='paramedic'){style.body?.material?.color?.setHex(0xf6f8fa);style.cabin?.material?.color?.setHex(0xe34848);layers.paramedic.visible=true;ghost.userData.carVisual.scale.set(1.08,1.12,1.22);}`
- [x] `else` — linha 5101, coluna 5 em `styleGhostVehicle()` — `else{style.body?.material?.color?.setHex(Number(style.baseColor||0x5ad8ff));style.cabin?.material?.color?.setHex(0x173149);}`
- [x] `for` — linha 5105, coluna 224 em `createGhost()` — `const car=new THREE.Group();car.visible=false;g.add(car);const carBase=box(1.9,.42,2.8,mat(0x26384e),0,.35,0,car),carBody=box(1.72,.6,1.45,mat(color),0,.7,.25,car),carCabin=box(1.46,.45,.9,mat(0x173149),0,.92,-.48,car);for(const p of [[-.9,.3,-.85],[.9,.3,-.85],[-.9,.3,.85],[.9,.3,.85]]){const wheel=cylinder(.34,.24,0x10151d,p[0],p[1],p[2],car,10);wheel.rotation.z=Math.PI/2;}`
- [x] `return` — linha 5108, coluna 707 em `createGhost()` — `const label=new THREE.Sprite(new THREE.SpriteMaterial({map:multiplayerNameTexture(name),transparent:true,depthWrite:false,depthTest:false}));label.position.set(0,3.05,0);label.scale.set(2.75,.69,1);label.renderOrder=999;g.add(label);g.userData.nameLabel=label;g.userData.displayName=sanitizePlayerName(name)||'Jogador';g.userData.avatar=avatar;g.userData.carVisual=car;g.userData.carStyle={body:carBody,cabin:carCabin,base:carBase,baseColor:color};g.userData.carServiceLayers={police:policeLayer,firefighter:fireLayer,paramedic:paramedicLayer};g.userData.vehicleKind='car';g.userData.boatVisual=boat;g.userData.limbs={leftArm,rightArm,leftLeg,rightLeg};g.userData.phase=Math.random()*6.28;scene.add(g);return g;`
- [x] `if` — linha 5111, coluna 5 em `updateMultiplayer()` — `if(!guardianMultiplayerAllowed())return;`
- [x] `return` — linha 5111, coluna 38 em `updateMultiplayer()` — `if(!guardianMultiplayerAllowed())return;`
- [x] `if` — linha 5114, coluna 5 em `updateMultiplayer()` — `if((changed&&now-lastPublish>240)||now-lastPublishHeartbeat>2200){lastPublish=now;lastPublishHeartbeat=now;lastPublishSnapshot=payload;localChannel?.postMessage(payload);window.OTTHOS_RTDB?.publish?.(payload);}`
- [x] `for` — linha 5115, coluna 5 em `updateMultiplayer()` — `for(const ghost of world.ghosts.values()){`
- [x] `if` — linha 5116, coluna 37 em `updateMultiplayer()` — `const t=ghost.userData.target;if(!t)continue;const beforeX=ghost.position.x,beforeZ=ghost.position.z;ghost.position.x=lerp(ghost.position.x,Number(t.x||0),Math.min(1,dt*8));ghost.position.y=lerp(ghost.position.y,Number(t.y||0),Math.min(1,dt*8));ghost.position.z=lerp(ghost.position.z,Number(t.z||0),Math.min(1,dt*8));ghost.rotation.y=lerpAngle(ghost.rotation.y,Number(t.r||0),Math.min(1,dt*8));`
- [x] `if` — linha 5117, coluna 7 em `updateMultiplayer()` — `if(Number(t.emoteSeq||0)!==Number(ghost.userData.lastEmoteSeq||0)){ghost.userData.lastEmoteSeq=Number(t.emoteSeq||0);ghost.userData.emoteType=t.emoteType||'';ghost.userData.emoteUntil=performance.now()+2600;}`
- [x] `if` — linha 5118, coluna 249 em `updateMultiplayer()` — `const carDriver=!!t.vehicle&&t.vehicleRole!=='passenger',boatDriver=!!t.boating&&t.boatRole!=='passenger',avatar=ghost.userData.avatar;avatar.visible=!carDriver&&!boatDriver&&t.transitMode!=='metro';ghost.userData.carVisual.visible=carDriver;if(carDriver)styleGhostVehicle(ghost,t.vehicleId||'');ghost.userData.boatVisual.visible=boatDriver;avatar.scale.setScalar(t.scaleMode==='mini'?.58:t.scaleMode==='giant'?1.42:1);ghost.userData.nameLabel.position.y=carDriver?3.15:boatDriver?3.45:3.05;`
- [x] `if` — linha 5119, coluna 235 em `updateMultiplayer()` — `const moving=Math.hypot(ghost.position.x-beforeX,ghost.position.z-beforeZ)>.01,walk=moving?Math.sin(animTime*9+ghost.userData.phase)*.45:0,emote=performance.now()<Number(ghost.userData.emoteUntil||0)?ghost.userData.emoteType:'';if(ghost.userData.limbs){const l=ghost.userData.limbs;l.leftArm.rotation.x=emote==='dance'?-1.35:emote==='play'?-1.9:emote==='hug'?-1.45:walk;l.rightArm.rotation.x=emote==='wave'?-2.2:emote==='highfive'?-2.5:emote==='dance'?-1.35:emote==='play'?-1.9:emote==='hug'?-1.45:-walk;l.leftArm.rotation.z=emote==='dance'?1.0:emote==='play'?.55:emote==='hug'?-.48:0;l.rightArm.rotation.z=emote==='dance'?-1.0:emote==='play'?-.55:emote==='hug'?.48:0;l.leftLeg.rotation.x=-walk*.75;l.rightLeg.rotation.x=walk*.75;}`

## `29-game-loop-controls-gamepad.js`

- [x] `if` — linha 5127, coluna 5 em `updatePlayUsage()` — `if(!running||paused||document.hidden)return;`
- [x] `return` — linha 5127, coluna 42 em `updatePlayUsage()` — `if(!running||paused||document.hidden)return;`
- [x] `if` — linha 5132, coluna 5 em `updatePlayUsage()` — `if(now-usageLastSaveAt>=30000){usageLastSaveAt=now;saveState();}`
- [x] `if` — linha 5134, coluna 5 em `updatePlayUsage()` — `if(limit>0&&state.usage.sessionSeconds>=limit&&!sessionLimitHandled){`
- [x] `if` — linha 5146, coluna 5 em `showWorldLoading()` — `if(els.modalClose)els.modalClose.hidden=true;`
- [x] `if` — linha 5150, coluna 39 em `showWorldLoadFailure()` — `running=false;showScreen('lobby');if(els.modalClose)els.modalClose.hidden=false;`
- [x] `if` — linha 5157, coluna 5 em `gameLoop()` — `if(!running)return;raf=requestAnimationFrame(gameLoop);const dt=Math.min(.033,clock.getDelta());samplePerformance(dt);`
- [x] `return` — linha 5157, coluna 17 em `gameLoop()` — `if(!running)return;raf=requestAnimationFrame(gameLoop);const dt=Math.min(.033,clock.getDelta());samplePerformance(dt);`
- [x] `if` — linha 5159, coluna 5 em `gameLoop()` — `if(!paused){`
- [x] `if` — linha 5162, coluna 41 em `gameLoop()` — `updatePlayer(dt);updateCamera(dt);if(typeof updateOtthiWorldEnvironment==='function')updateOtthiWorldEnvironment(dt);if(typeof updateWorldHeroAdventure==='function')updateWorldHeroAdventure(dt);`
- [x] `if` — linha 5162, coluna 124 em `gameLoop()` — `updatePlayer(dt);updateCamera(dt);if(typeof updateOtthiWorldEnvironment==='function')updateOtthiWorldEnvironment(dt);if(typeof updateWorldHeroAdventure==='function')updateWorldHeroAdventure(dt);`
- [x] `if` — linha 5163, coluna 7 em `gameLoop()` — `if(buildMode)updateBuildPreview();`
- [x] `if` — linha 5164, coluna 7 em `gameLoop()` — `if(fishingSession||fishingVisual?.active)updateFishingVisual(dt);`
- [x] `if` — linha 5165, coluna 7 em `gameLoop()` — `if(player.vehicle)updateVehicleFX(dt);`
- [x] `if` — linha 5166, coluna 7 em `gameLoop()` — `if(typeof fxParticles!=='undefined'&&fxParticles.length)updateFX(dt);`
- [x] `if` — linha 5167, coluna 7 em `gameLoop()` — `if(world.fireballs?.length)updateFireballs(dt);`
- [x] `if` — linha 5168, coluna 7 em `gameLoop()` — `if(activeRace)updateRace(dt);if(typeof updateCoopVisuals==='function')updateCoopVisuals(dt);`
- [x] `if` — linha 5168, coluna 36 em `gameLoop()` — `if(activeRace)updateRace(dt);if(typeof updateCoopVisuals==='function')updateCoopVisuals(dt);`
- [x] `if` — linha 5171, coluna 7 em `gameLoop()` — `if(perf.uiAcc>=uiRate){const step=perf.uiAcc;perf.uiAcc=0;updateCareerMissions();if(typeof updateCoopMissions==='function')updateCoopMissions(step);updateNeeds(step);updateNavigation(step);}`
- [x] `if` — linha 5171, coluna 88 em `gameLoop()` — `if(perf.uiAcc>=uiRate){const step=perf.uiAcc;perf.uiAcc=0;updateCareerMissions();if(typeof updateCoopMissions==='function')updateCoopMissions(step);updateNeeds(step);updateNavigation(step);}`
- [x] `if` — linha 5174, coluna 7 em `gameLoop()` — `if(perf.trafficAcc>=trafficRate){const trafficStep=Math.min(.1,perf.trafficAcc);perf.trafficAcc=0;const trafficBefore=captureTrafficPositions();updateTransitWorld(trafficStep);perf.trafficTicks++;updatePoliceSystem(trafficStep);updateFireService(trafficStep);updateTrafficIncidents(trafficStep);resolveTrafficOverlaps(trafficBefore);}`
- [x] `if` — linha 5177, coluna 7 em `gameLoop()` — `if(perf.aiAcc>=aiRate){const step=Math.min(.11,perf.aiAcc);perf.aiAcc=0;updateNPCs(step);perf.aiTicks++;updateNpcSociety(step);updateEnemies(step);updateMultiplayer(step);updateLifeActivities(step);updateAdventure(step);}`
- [x] `if` — linha 5180, coluna 7 em `gameLoop()` — `if(perf.lodAcc>=lodRate){const step=perf.lodAcc;perf.lodAcc=0;updateVisualLOD(step);}`
- [x] `if` — linha 5181, coluna 86 em `gameLoop()` — `perf.cloudAcc+=dt;const cloudRate=tier==='high'?1/12:tier==='balanced'?1/8:1/5;if(perf.cloudAcc>=cloudRate){const step=perf.cloudAcc;perf.cloudAcc=0;updateClouds(step);}`
- [x] `if` — linha 5182, coluna 29 em `gameLoop()` — `perf.modeAuditAcc+=dt;if(perf.modeAuditAcc>=.75){perf.modeAuditAcc=0;ensureViewportCoherence();auditPlayerMode('loop');}`
- [x] `if` — linha 5183, coluna 25 em `gameLoop()` — `perf.panelAcc+=dt;if(perf.panelAcc>=1){perf.panelAcc=0;refreshTechnicalPanel();}`
- [x] `if` — linha 5191, coluna 53 em `setupControls()` — `els.joystick.addEventListener('pointermove',e=>{if(e.pointerId===input.joyId)updateJoy(e);});`
- [x] `if` — linha 5193, coluna 192 em `setupControls()` — `function updateJoy(e){const r=els.joystick.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,max=r.width*.32;let dx=e.clientX-cx,dy=e.clientY-cy;const mag=Math.hypot(dx,dy);if(mag>max){dx=dx/mag*max;dy=dy/mag*max;}input.joyX=dx/max;input.joyZ=-dy/max;els.joystickKnob.style.transform=\`translate(calc(-50% + ${dx}px),calc(-50% + ${dy}px))\`;}`
- [x] `if` — linha 5197, coluna 34 em `setupControls()` — `const setAccelerate=active=>{if(mobilityDriverActive()){input.mobilityAccelerate=!!active;input.mobilityControlSource=active?'accelerator':'';if(active)input.mobilityBrake=false;updateMobilityControlLabels();}else setTouchSprint(active);};`
- [x] `if` — linha 5197, coluna 147 em `setupControls()` — `const setAccelerate=active=>{if(mobilityDriverActive()){input.mobilityAccelerate=!!active;input.mobilityControlSource=active?'accelerator':'';if(active)input.mobilityBrake=false;updateMobilityControlLabels();}else setTouchSprint(active);};`
- [x] `else` — linha 5197, coluna 214 em `setupControls()` — `const setAccelerate=active=>{if(mobilityDriverActive()){input.mobilityAccelerate=!!active;input.mobilityControlSource=active?'accelerator':'';if(active)input.mobilityBrake=false;updateMobilityControlLabels();}else setTouchSprint(active);};`
- [x] `if` — linha 5198, coluna 29 em `setupControls()` — `const setBrake=active=>{if(mobilityDriverActive()){input.mobilityBrake=!!active;input.mobilityControlSource=active?'brake':'';if(active)input.mobilityAccelerate=false;updateMobilityControlLabels();}else if(active)requestJump();};`
- [x] `if` — linha 5198, coluna 131 em `setupControls()` — `const setBrake=active=>{if(mobilityDriverActive()){input.mobilityBrake=!!active;input.mobilityControlSource=active?'brake':'';if(active)input.mobilityAccelerate=false;updateMobilityControlLabels();}else if(active)requestJump();};`
- [x] `else` — linha 5198, coluna 203 em `setupControls()` — `const setBrake=active=>{if(mobilityDriverActive()){input.mobilityBrake=!!active;input.mobilityControlSource=active?'brake':'';if(active)input.mobilityAccelerate=false;updateMobilityControlLabels();}else if(active)requestJump();};`
- [x] `if` — linha 5198, coluna 208 em `setupControls()` — `const setBrake=active=>{if(mobilityDriverActive()){input.mobilityBrake=!!active;input.mobilityControlSource=active?'brake':'';if(active)input.mobilityAccelerate=false;updateMobilityControlLabels();}else if(active)requestJump();};`
- [x] `if` — linha 5207, coluna 66 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 202 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 308 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 454 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 513 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 543 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 574 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 608 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 650 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 694 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 737 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 769 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 820 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 861 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `return` — linha 5207, coluna 873 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 880 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `return` — linha 5207, coluna 920 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 928 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `else` — linha 5207, coluna 958 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 963 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `else` — linha 5207, coluna 997 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 1084 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5207, coluna 1162 em `setupControls()` — `window.addEventListener('keydown',e=>{input.keys.add(e.code);if(['Space','KeyE','KeyF','KeyC','Digit1','Digit2','Digit3','KeyR','KeyQ','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault();if(mobilityDriverActive()&&e.code==='ShiftLeft'){input.mobilityAccelerate=true;input.mobilityBrake=false;}if(mobilityDriverActive()&&e.code==='Space'){input.mobilityBrake=true;input.mobilityAccelerate=false;}updateRunUI();updateMobilityControlLabels();if(e.code==='Space'&&!mobilityDriverActive())requestJump();if(e.code==='KeyE')doAction();if(e.code==='KeyF')firePower();if(e.code==='KeyC')toggleCrouch();if(e.code==='Digit1')setScaleMode('mini');if(e.code==='Digit2')setScaleMode('normal');if(e.code==='Digit3')setScaleMode('giant');if(e.code==='KeyR')spinPlayer();if(e.code==='KeyQ'&&buildMode)rotateBuildPreview();if(e.code==='Escape'){e.preventDefault();if(!running)return;if(buildMode){endBuildMode('cancelled');return;}if(pauseMenuOpen)closeModal();else if(!els.modal.hidden)closeModal();else ope`
- [x] `if` — linha 5208, coluna 50 em `setupControls()` — `els.stage.addEventListener('pointerdown',e=>{if(e.target!==renderer?.domElement)return;input.cameraDrag={id:e.pointerId,x:e.clientX,y:e.clientY};safePointerCapture(els.stage,e.pointerId);});`
- [x] `return` — linha 5208, coluna 85 em `setupControls()` — `els.stage.addEventListener('pointerdown',e=>{if(e.target!==renderer?.domElement)return;input.cameraDrag={id:e.pointerId,x:e.clientX,y:e.clientY};safePointerCapture(els.stage,e.pointerId);});`
- [x] `if` — linha 5209, coluna 75 em `setupControls()` — `els.stage.addEventListener('pointermove',e=>{const d=input.cameraDrag;if(!d||d.id!==e.pointerId)return;const dx=e.clientX-d.x,dy=e.clientY-d.y;cameraYaw-=dx*.006;cameraPitch=clamp(cameraPitch+dy*.003,.05,.9);d.x=e.clientX;d.y=e.clientY;});`
- [x] `return` — linha 5209, coluna 101 em `setupControls()` — `els.stage.addEventListener('pointermove',e=>{const d=input.cameraDrag;if(!d||d.id!==e.pointerId)return;const dx=e.clientX-d.x,dy=e.clientY-d.y;cameraYaw-=dx*.006;cameraPitch=clamp(cameraPitch+dy*.003,.05,.9);d.x=e.clientX;d.y=e.clientY;});`
- [x] `if` — linha 5210, coluna 23 em `setupControls()` — `const endDrag=e=>{if(input.cameraDrag?.id===e.pointerId)input.cameraDrag=null;};els.stage.addEventListener('pointerup',endDrag);els.stage.addEventListener('pointercancel',endDrag);`
- [x] `if` — linha 5211, coluna 44 em `setupControls()` — `els.stage.addEventListener('wheel',e=>{if(!running||!els.modal.hidden)return;e.preventDefault();cameraZoom=clamp(cameraZoom+Math.sign(e.deltaY)*.9,-4.5,9);state.settings.cameraZoom=+cameraZoom.toFixed(2);},{passive:false});`
- [x] `return` — linha 5211, coluna 75 em `setupControls()` — `els.stage.addEventListener('wheel',e=>{if(!running||!els.modal.hidden)return;e.preventDefault();cameraZoom=clamp(cameraZoom+Math.sign(e.deltaY)*.9,-4.5,9);state.settings.cameraZoom=+cameraZoom.toFixed(2);},{passive:false});`
- [x] `if` — linha 5216, coluna 5 em `pollGamepad()` — `if(!gp){input.gamepadX=0;input.gamepadZ=0;input.gamepadActive=false;input.gamepadSprint=false;updateRunUI();return;}`
- [x] `return` — linha 5216, coluna 113 em `pollGamepad()` — `if(!gp){input.gamepadX=0;input.gamepadZ=0;input.gamepadActive=false;input.gamepadSprint=false;updateRunUI();return;}`
- [x] `if` — linha 5218, coluna 173 em `pollGamepad()` — `input.gamepadActive=Math.hypot(ax,az)>.16;input.gamepadX=input.gamepadActive?ax:0;input.gamepadZ=input.gamepadActive?az:0;input.gamepadSprint=!!gp.buttons[10]?.pressed;if(mobilityDriverActive()){input.mobilityAccelerate=gp.buttons[7]?.value>.18;input.mobilityBrake=gp.buttons[6]?.value>.18;}updateRunUI();updateMobilityControlLabels();`
- [x] `if` — linha 5220, coluna 5 em `pollGamepad()` — `if(jump&&!gamepadJump)requestJump();if(action&&!gamepadAction)doAction();if(power&&!gamepadPower)firePower();if(crouch&&!gamepadCrouch)toggleCrouch();if(size&&!gamepadSize)setScaleMode(player.scaleMode==='normal'?'mini':player.scaleMode==='mini'?'giant':'normal');`
- [x] `if` — linha 5220, coluna 41 em `pollGamepad()` — `if(jump&&!gamepadJump)requestJump();if(action&&!gamepadAction)doAction();if(power&&!gamepadPower)firePower();if(crouch&&!gamepadCrouch)toggleCrouch();if(size&&!gamepadSize)setScaleMode(player.scaleMode==='normal'?'mini':player.scaleMode==='mini'?'giant':'normal');`
- [x] `if` — linha 5220, coluna 78 em `pollGamepad()` — `if(jump&&!gamepadJump)requestJump();if(action&&!gamepadAction)doAction();if(power&&!gamepadPower)firePower();if(crouch&&!gamepadCrouch)toggleCrouch();if(size&&!gamepadSize)setScaleMode(player.scaleMode==='normal'?'mini':player.scaleMode==='mini'?'giant':'normal');`
- [x] `if` — linha 5220, coluna 114 em `pollGamepad()` — `if(jump&&!gamepadJump)requestJump();if(action&&!gamepadAction)doAction();if(power&&!gamepadPower)firePower();if(crouch&&!gamepadCrouch)toggleCrouch();if(size&&!gamepadSize)setScaleMode(player.scaleMode==='normal'?'mini':player.scaleMode==='mini'?'giant':'normal');`
- [x] `if` — linha 5220, coluna 155 em `pollGamepad()` — `if(jump&&!gamepadJump)requestJump();if(action&&!gamepadAction)doAction();if(power&&!gamepadPower)firePower();if(crouch&&!gamepadCrouch)toggleCrouch();if(size&&!gamepadSize)setScaleMode(player.scaleMode==='normal'?'mini':player.scaleMode==='mini'?'giant':'normal');`
- [x] `if` — linha 5222, coluna 30 em `pollGamepad()` — `const camX=gp.axes[2]||0;if(Math.abs(camX)>.18)cameraYaw-=camX*.035;const camY=gp.axes[3]||0;if(Math.abs(camY)>.18)cameraPitch=clamp(cameraPitch+camY*.018,.05,.9);`
- [x] `if` — linha 5222, coluna 98 em `pollGamepad()` — `const camX=gp.axes[2]||0;if(Math.abs(camX)>.18)cameraYaw-=camX*.035;const camY=gp.axes[3]||0;if(Math.abs(camY)>.18)cameraPitch=clamp(cameraPitch+camY*.018,.05,.9);`
- [x] `if` — linha 5227, coluna 5 em `pollGamepad()` — `if(!window.OTTHI_RELEASE_COHERENT){openModal('Atualização incompleta','<p>Os arquivos do jogo pertencem a versões diferentes. Termine o envio da V700 e atualize a página; seu progresso está preservado.</p>');return;}`
- [x] `return` — linha 5227, coluna 213 em `pollGamepad()` — `if(!window.OTTHI_RELEASE_COHERENT){openModal('Atualização incompleta','<p>Os arquivos do jogo pertencem a versões diferentes. Termine o envio da V700 e atualize a página; seu progresso está preservado.</p>');return;}`
- [x] `if` — linha 5229, coluna 5 em `pollGamepad()` — `if(sessionLimitSeconds>0&&(Number(state.usage.sessionLockedAt||0)>0||Number(state.usage.sessionSeconds||0)>=sessionLimitSeconds)){state.usage.sessionLockedAt=Number(state.usage.sessionLockedAt||Date.now());saveState(true);openModal('Tempo de jogo concluído','<div class="parent-gate"><span>⏰</span><h3>Nova sessão exige um responsável</h3><p>O limite continua bloqueado mesmo ao voltar ao menu. Um responsável pode liberar outra sessão usando a senha da conta.</p><button class="btn primary xl" data-session-parent>Área dos responsáveis</button><button class="btn" data-session-back>Ficar no menu</button></div>',root=>{$('[data-session-parent]',root).onclick=()=>openParentGate(false);$('[data-session-back]',root).onclick=closeModal;});return;}`
- [x] `return` — linha 5229, coluna 743 em `pollGamepad()` — `if(sessionLimitSeconds>0&&(Number(state.usage.sessionLockedAt||0)>0||Number(state.usage.sessionSeconds||0)>=sessionLimitSeconds)){state.usage.sessionLockedAt=Number(state.usage.sessionLockedAt||Date.now());saveState(true);openModal('Tempo de jogo concluído','<div class="parent-gate"><span>⏰</span><h3>Nova sessão exige um responsável</h3><p>O limite continua bloqueado mesmo ao voltar ao menu. Um responsável pode liberar outra sessão usando a senha da conta.</p><button class="btn primary xl" data-session-parent>Área dos responsáveis</button><button class="btn" data-session-back>Ficar no menu</button></div>',root=>{$('[data-session-parent]',root).onclick=()=>openParentGate(false);$('[data-session-back]',root).onclick=closeModal;});return;}`
- [x] `if` — linha 5230, coluna 5 em `pollGamepad()` — `if((!hasValidPlayerName()||!accountLinked())&&!(accountPromptWasHandled())){openAccountCenter(true,()=>{state.flags.accountPromptedV635=true;saveState(true);startGame(resetPosition);});return;}`
- [x] `return` — linha 5230, coluna 190 em `pollGamepad()` — `if((!hasValidPlayerName()||!accountLinked())&&!(accountPromptWasHandled())){openAccountCenter(true,()=>{state.flags.accountPromptedV635=true;saveState(true);startGame(resetPosition);});return;}`
- [x] `if` — linha 5231, coluna 5 em `pollGamepad()` — `if(!hasValidPlayerName()){openPlayerNameModal(true,()=>startGame(resetPosition));return;}`
- [x] `return` — linha 5231, coluna 86 em `pollGamepad()` — `if(!hasValidPlayerName()){openPlayerNameModal(true,()=>startGame(resetPosition));return;}`
- [x] `if` — linha 5234, coluna 9 em `pollGamepad()` — `try{if(!worldInitializationReady){if(scene||renderer)throw new Error('A inicialização 3D anterior ficou incompleta. Recarregue com segurança para tentar novamente.');showWorldLoading(42,'Construindo ruas, casas e transportes...');await nextPaint();if(!initThree())throw new Error('O navegador não conseguiu iniciar o ambiente 3D.');setupControls();worldInitializationReady=true;showWorldLoading(88,'Ativando controles e personagens...');await nextPaint();}else{applyAvatarCustomization();showWorldLoading(88,'Restaurando seu personagem...');await nextPaint();}}catch(error){worldInitializationReady=false;showWorldLoadFailure(error,resetPosition);return;}`
- [x] `if` — linha 5234, coluna 39 em `pollGamepad()` — `try{if(!worldInitializationReady){if(scene||renderer)throw new Error('A inicialização 3D anterior ficou incompleta. Recarregue com segurança para tentar novamente.');showWorldLoading(42,'Construindo ruas, casas e transportes...');await nextPaint();if(!initThree())throw new Error('O navegador não conseguiu iniciar o ambiente 3D.');setupControls();worldInitializationReady=true;showWorldLoading(88,'Ativando controles e personagens...');await nextPaint();}else{applyAvatarCustomization();showWorldLoading(88,'Restaurando seu personagem...');await nextPaint();}}catch(error){worldInitializationReady=false;showWorldLoadFailure(error,resetPosition);return;}`
- [x] `throw` — linha 5234, coluna 58 em `pollGamepad()` — `try{if(!worldInitializationReady){if(scene||renderer)throw new Error('A inicialização 3D anterior ficou incompleta. Recarregue com segurança para tentar novamente.');showWorldLoading(42,'Construindo ruas, casas e transportes...');await nextPaint();if(!initThree())throw new Error('O navegador não conseguiu iniciar o ambiente 3D.');setupControls();worldInitializationReady=true;showWorldLoading(88,'Ativando controles e personagens...');await nextPaint();}else{applyAvatarCustomization();showWorldLoading(88,'Restaurando seu personagem...');await nextPaint();}}catch(error){worldInitializationReady=false;showWorldLoadFailure(error,resetPosition);return;}`
- [x] `if` — linha 5234, coluna 253 em `pollGamepad()` — `try{if(!worldInitializationReady){if(scene||renderer)throw new Error('A inicialização 3D anterior ficou incompleta. Recarregue com segurança para tentar novamente.');showWorldLoading(42,'Construindo ruas, casas e transportes...');await nextPaint();if(!initThree())throw new Error('O navegador não conseguiu iniciar o ambiente 3D.');setupControls();worldInitializationReady=true;showWorldLoading(88,'Ativando controles e personagens...');await nextPaint();}else{applyAvatarCustomization();showWorldLoading(88,'Restaurando seu personagem...');await nextPaint();}}catch(error){worldInitializationReady=false;showWorldLoadFailure(error,resetPosition);return;}`
- [x] `throw` — linha 5234, coluna 269 em `pollGamepad()` — `try{if(!worldInitializationReady){if(scene||renderer)throw new Error('A inicialização 3D anterior ficou incompleta. Recarregue com segurança para tentar novamente.');showWorldLoading(42,'Construindo ruas, casas e transportes...');await nextPaint();if(!initThree())throw new Error('O navegador não conseguiu iniciar o ambiente 3D.');setupControls();worldInitializationReady=true;showWorldLoading(88,'Ativando controles e personagens...');await nextPaint();}else{applyAvatarCustomization();showWorldLoading(88,'Restaurando seu personagem...');await nextPaint();}}catch(error){worldInitializationReady=false;showWorldLoadFailure(error,resetPosition);return;}`
- [x] `else` — linha 5234, coluna 461 em `pollGamepad()` — `try{if(!worldInitializationReady){if(scene||renderer)throw new Error('A inicialização 3D anterior ficou incompleta. Recarregue com segurança para tentar novamente.');showWorldLoading(42,'Construindo ruas, casas e transportes...');await nextPaint();if(!initThree())throw new Error('O navegador não conseguiu iniciar o ambiente 3D.');setupControls();worldInitializationReady=true;showWorldLoading(88,'Ativando controles e personagens...');await nextPaint();}else{applyAvatarCustomization();showWorldLoading(88,'Restaurando seu personagem...');await nextPaint();}}catch(error){worldInitializationReady=false;showWorldLoadFailure(error,resetPosition);return;}`
- [x] `catch` — linha 5234, coluna 566 em `pollGamepad()` — `try{if(!worldInitializationReady){if(scene||renderer)throw new Error('A inicialização 3D anterior ficou incompleta. Recarregue com segurança para tentar novamente.');showWorldLoading(42,'Construindo ruas, casas e transportes...');await nextPaint();if(!initThree())throw new Error('O navegador não conseguiu iniciar o ambiente 3D.');setupControls();worldInitializationReady=true;showWorldLoading(88,'Ativando controles e personagens...');await nextPaint();}else{applyAvatarCustomization();showWorldLoading(88,'Restaurando seu personagem...');await nextPaint();}}catch(error){worldInitializationReady=false;showWorldLoadFailure(error,resetPosition);return;}`
- [x] `return` — linha 5234, coluna 652 em `pollGamepad()` — `try{if(!worldInitializationReady){if(scene||renderer)throw new Error('A inicialização 3D anterior ficou incompleta. Recarregue com segurança para tentar novamente.');showWorldLoading(42,'Construindo ruas, casas e transportes...');await nextPaint();if(!initThree())throw new Error('O navegador não conseguiu iniciar o ambiente 3D.');setupControls();worldInitializationReady=true;showWorldLoading(88,'Ativando controles e personagens...');await nextPaint();}else{applyAvatarCustomization();showWorldLoading(88,'Restaurando seu personagem...');await nextPaint();}}catch(error){worldInitializationReady=false;showWorldLoadFailure(error,resetPosition);return;}`
- [x] `if` — linha 5235, coluna 5 em `pollGamepad()` — `if(els.toolsBtn){els.toolsBtn.firstChild.textContent=equippedTool().icon;$('span',els.toolsBtn).textContent=equippedTool().name;}`
- [x] `if` — linha 5236, coluna 5 em `pollGamepad()` — `if(resetPosition){player.x=0;player.z=8;player.y=0;}else restorePosition();player.scaleMode=state.abilities?.scaleMode||'normal';player.crouched=!!state.abilities?.crouched;updateAbilityUI();state.usage={totalSeconds:0,sessionSeconds:0,sessionStartedAt:0,lastPlayedAt:0,sessionLockedAt:0,...(state.usage||{}),sessionStartedAt:Number(state.usage?.sessionStartedAt||Date.now()),lastPlayedAt:Date.now()};sessionLimitHandled=false;usageLastTickAt=Date.now();running=true;paused=false;window.OTTHI_ROOM_WORLD?.apply?.(window.OTTHOS_RTDB?.getRoom?.()||window.OTTHI_CONFIG?.defaultRoom||'bairro-central',{toast:false,teleport:state.multiplayer.room!==(window.OTTHOS_RTDB?.getRoom?.()||window.OTTHI_CONFIG?.defaultRoom||'bairro-central')});clock.start();evaluateMissions();updateHUD();updateContext(true);updateNavigation(0,true);resize(true);if(els.modalClose)els.modalClose.hidden=false;closeModal();cancelAnimationFrame(raf);gameLoop();toast('Bem-vindo à Vila do Sol!','good',2200);`
- [x] `else` — linha 5236, coluna 57 em `pollGamepad()` — `if(resetPosition){player.x=0;player.z=8;player.y=0;}else restorePosition();player.scaleMode=state.abilities?.scaleMode||'normal';player.crouched=!!state.abilities?.crouched;updateAbilityUI();state.usage={totalSeconds:0,sessionSeconds:0,sessionStartedAt:0,lastPlayedAt:0,sessionLockedAt:0,...(state.usage||{}),sessionStartedAt:Number(state.usage?.sessionStartedAt||Date.now()),lastPlayedAt:Date.now()};sessionLimitHandled=false;usageLastTickAt=Date.now();running=true;paused=false;window.OTTHI_ROOM_WORLD?.apply?.(window.OTTHOS_RTDB?.getRoom?.()||window.OTTHI_CONFIG?.defaultRoom||'bairro-central',{toast:false,teleport:state.multiplayer.room!==(window.OTTHOS_RTDB?.getRoom?.()||window.OTTHI_CONFIG?.defaultRoom||'bairro-central')});clock.start();evaluateMissions();updateHUD();updateContext(true);updateNavigation(0,true);resize(true);if(els.modalClose)els.modalClose.hidden=false;closeModal();cancelAnimationFrame(raf);gameLoop();toast('Bem-vindo à Vila do Sol!','good',2200);`
- [x] `if` — linha 5236, coluna 840 em `pollGamepad()` — `if(resetPosition){player.x=0;player.z=8;player.y=0;}else restorePosition();player.scaleMode=state.abilities?.scaleMode||'normal';player.crouched=!!state.abilities?.crouched;updateAbilityUI();state.usage={totalSeconds:0,sessionSeconds:0,sessionStartedAt:0,lastPlayedAt:0,sessionLockedAt:0,...(state.usage||{}),sessionStartedAt:Number(state.usage?.sessionStartedAt||Date.now()),lastPlayedAt:Date.now()};sessionLimitHandled=false;usageLastTickAt=Date.now();running=true;paused=false;window.OTTHI_ROOM_WORLD?.apply?.(window.OTTHOS_RTDB?.getRoom?.()||window.OTTHI_CONFIG?.defaultRoom||'bairro-central',{toast:false,teleport:state.multiplayer.room!==(window.OTTHOS_RTDB?.getRoom?.()||window.OTTHI_CONFIG?.defaultRoom||'bairro-central')});clock.start();evaluateMissions();updateHUD();updateContext(true);updateNavigation(0,true);resize(true);if(els.modalClose)els.modalClose.hidden=false;closeModal();cancelAnimationFrame(raf);gameLoop();toast('Bem-vindo à Vila do Sol!','good',2200);`

## `30-pause-tests-public-api-bootstrap.js`

- [x] `if` — linha 5241, coluna 5 em `stopGame()` — `if(buildMode)endBuildMode('cancelled',true);`
- [x] `if` — linha 5242, coluna 50 em `stopGame()` — `world.policeAlert=null;updateSafetyPanel('');if(player.boating){player.x=-24.7;player.z=52;exitBoat(true);}if(player.vehicle)exitVehicle(true);if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus)exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});}if(player.transit.mode==='metro'){player.transit.mode='';if(metroOverlay)metroOverlay.hidden=true;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}running=false;paused=false;pauseMenuOpen=false;cancelAnimationFrame(raf);stopEngineSound();savePlayerPosition(true);showScreen('lobby');updateLobbyStats();`
- [x] `if` — linha 5242, coluna 112 em `stopGame()` — `world.policeAlert=null;updateSafetyPanel('');if(player.boating){player.x=-24.7;player.z=52;exitBoat(true);}if(player.vehicle)exitVehicle(true);if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus)exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});}if(player.transit.mode==='metro'){player.transit.mode='';if(metroOverlay)metroOverlay.hidden=true;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}running=false;paused=false;pauseMenuOpen=false;cancelAnimationFrame(raf);stopEngineSound();savePlayerPosition(true);showScreen('lobby');updateLobbyStats();`
- [x] `if` — linha 5242, coluna 148 em `stopGame()` — `world.policeAlert=null;updateSafetyPanel('');if(player.boating){player.x=-24.7;player.z=52;exitBoat(true);}if(player.vehicle)exitVehicle(true);if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus)exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});}if(player.transit.mode==='metro'){player.transit.mode='';if(metroOverlay)metroOverlay.hidden=true;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}running=false;paused=false;pauseMenuOpen=false;cancelAnimationFrame(raf);stopEngineSound();savePlayerPosition(true);showScreen('lobby');updateLobbyStats();`
- [x] `if` — linha 5242, coluna 239 em `stopGame()` — `world.policeAlert=null;updateSafetyPanel('');if(player.boating){player.x=-24.7;player.z=52;exitBoat(true);}if(player.vehicle)exitVehicle(true);if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus)exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});}if(player.transit.mode==='metro'){player.transit.mode='';if(metroOverlay)metroOverlay.hidden=true;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}running=false;paused=false;pauseMenuOpen=false;cancelAnimationFrame(raf);stopEngineSound();savePlayerPosition(true);showScreen('lobby');updateLobbyStats();`
- [x] `if` — linha 5242, coluna 316 em `stopGame()` — `world.policeAlert=null;updateSafetyPanel('');if(player.boating){player.x=-24.7;player.z=52;exitBoat(true);}if(player.vehicle)exitVehicle(true);if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus)exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});}if(player.transit.mode==='metro'){player.transit.mode='';if(metroOverlay)metroOverlay.hidden=true;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}running=false;paused=false;pauseMenuOpen=false;cancelAnimationFrame(raf);stopEngineSound();savePlayerPosition(true);showScreen('lobby');updateLobbyStats();`
- [x] `if` — linha 5242, coluna 373 em `stopGame()` — `world.policeAlert=null;updateSafetyPanel('');if(player.boating){player.x=-24.7;player.z=52;exitBoat(true);}if(player.vehicle)exitVehicle(true);if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus)exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});}if(player.transit.mode==='metro'){player.transit.mode='';if(metroOverlay)metroOverlay.hidden=true;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}running=false;paused=false;pauseMenuOpen=false;cancelAnimationFrame(raf);stopEngineSound();savePlayerPosition(true);showScreen('lobby');updateLobbyStats();`
- [x] `if` — linha 5242, coluna 414 em `stopGame()` — `world.policeAlert=null;updateSafetyPanel('');if(player.boating){player.x=-24.7;player.z=52;exitBoat(true);}if(player.vehicle)exitVehicle(true);if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus)exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});}if(player.transit.mode==='metro'){player.transit.mode='';if(metroOverlay)metroOverlay.hidden=true;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}running=false;paused=false;pauseMenuOpen=false;cancelAnimationFrame(raf);stopEngineSound();savePlayerPosition(true);showScreen('lobby');updateLobbyStats();`
- [x] `if` — linha 5242, coluna 454 em `stopGame()` — `world.policeAlert=null;updateSafetyPanel('');if(player.boating){player.x=-24.7;player.z=52;exitBoat(true);}if(player.vehicle)exitVehicle(true);if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus)exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});}if(player.transit.mode==='metro'){player.transit.mode='';if(metroOverlay)metroOverlay.hidden=true;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}running=false;paused=false;pauseMenuOpen=false;cancelAnimationFrame(raf);stopEngineSound();savePlayerPosition(true);showScreen('lobby');updateLobbyStats();`
- [x] `if` — linha 5242, coluna 494 em `stopGame()` — `world.policeAlert=null;updateSafetyPanel('');if(player.boating){player.x=-24.7;player.z=52;exitBoat(true);}if(player.vehicle)exitVehicle(true);if(player.transit.mode==='bus'){const bus=world.buses.find(b=>b.id===player.transit.busId);if(bus)exitBusAtStop(bus,{stopId:bus.lastStopId,stopName:bus.lastStopName});}if(player.transit.mode==='metro'){player.transit.mode='';if(metroOverlay)metroOverlay.hidden=true;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;}running=false;paused=false;pauseMenuOpen=false;cancelAnimationFrame(raf);stopEngineSound();savePlayerPosition(true);showScreen('lobby');updateLobbyStats();`
- [x] `if` — linha 5247, coluna 5 em `openPauseMenu()` — `if(!running||pauseMenuOpen)return;`
- [x] `return` — linha 5247, coluna 32 em `openPauseMenu()` — `if(!running||pauseMenuOpen)return;`
- [x] `if` — linha 5248, coluna 5 em `openPauseMenu()` — `if(buildMode)endBuildMode('cancelled',true);`
- [x] `if` — linha 5249, coluna 36 em `openPauseMenu()` — `paused=true;pauseMenuOpen=true;if(engineAudio)stopEngineSound();openModal('Jogo pausado',\`<div class="choice-grid"><button class="choice" data-resume><b>▶ Continuar</b><span>Voltar ao mundo</span></button><button class="choice" data-life><b>👤 Minha vida</b><span>Carreira, amizades e visual</span></button><button class="choice" data-home><b>🏠 Casa</b><span>Voltar para minha casa</span></button><button class="choice" data-menu><b>↩ Menu inicial</b><span>Salvar e sair</span></button></div>\`,root=>{`
- [x] `if` — linha 5251, coluna 88 em `openPauseMenu()` — `$('[data-life]',root).onclick=()=>{pauseMenuOpen=false;paused=false;closeModal();if(player.vehicle)startEngineSound();openLifePanel();};`
- [x] `if` — linha 5262, coluna 5 em `prepareVehicleTestArea()` — `if(currentHouse){`
- [x] `if` — linha 5265, coluna 5 em `prepareVehicleTestArea()` — `if(player.vehicle)exitVehicle(true);`
- [x] `return` — linha 5272, coluna 5 em `prepareVehicleTestArea()` — `return {x:player.x,z:player.z,active:player.vehicle,heading:player.car.heading};`
- [x] `if` — linha 5275, coluna 5 em `stepVehicleSimulation()` — `if(!player.vehicle)prepareVehicleTestArea();`
- [x] `for` — linha 5280, coluna 5 em `stepVehicleSimulation()` — `for(let i=0;i<count;i++){`
- [x] `return` — linha 5290, coluna 5 em `stepVehicleSimulation()` — `return {frames:count,seconds:count*dt,distance:Math.hypot(player.x-startX,player.z-startZ),speed:player.car.speed,x:player.x,z:player.z,impacts:vehicleImpactCount-startImpacts};`
- [x] `if` — linha 5299, coluna 142 em `stepVehicleSimulation()` — `controlMath:(x=0,z=0,yaw=0,mode='WALKING')=>{const key=String(mode).toUpperCase(),resolved=PLAYER_MODES[key]||String(mode).toLowerCase();if([PLAYER_MODES.CAR_DRIVER,PLAYER_MODES.MOTORCYCLE_DRIVER,PLAYER_MODES.BICYCLE,PLAYER_MODES.SKATE,PLAYER_MODES.BOAT_DRIVER].includes(resolved))return{x:clamp(Number(x)||0,-1,1),z:clamp(Number(z)||0,-1,1),steer:-clamp(Number(x)||0,-1,1),throttle:clamp(Number(z)||0,-1,1),mode:resolved};const vector=cameraRelativeVector(Number(x)||0,Number(z)||0,Number(yaw)||0);return{x:vector.x,z:vector.z,steer:0,throttle:0,mode:resolved};},`
- [x] `return` — linha 5299, coluna 286 em `stepVehicleSimulation()` — `controlMath:(x=0,z=0,yaw=0,mode='WALKING')=>{const key=String(mode).toUpperCase(),resolved=PLAYER_MODES[key]||String(mode).toLowerCase();if([PLAYER_MODES.CAR_DRIVER,PLAYER_MODES.MOTORCYCLE_DRIVER,PLAYER_MODES.BICYCLE,PLAYER_MODES.SKATE,PLAYER_MODES.BOAT_DRIVER].includes(resolved))return{x:clamp(Number(x)||0,-1,1),z:clamp(Number(z)||0,-1,1),steer:-clamp(Number(x)||0,-1,1),throttle:clamp(Number(z)||0,-1,1),mode:resolved};const vector=cameraRelativeVector(Number(x)||0,Number(z)||0,Number(yaw)||0);return{x:vector.x,z:vector.z,steer:0,throttle:0,mode:resolved};},`
- [x] `return` — linha 5299, coluna 504 em `stepVehicleSimulation()` — `controlMath:(x=0,z=0,yaw=0,mode='WALKING')=>{const key=String(mode).toUpperCase(),resolved=PLAYER_MODES[key]||String(mode).toLowerCase();if([PLAYER_MODES.CAR_DRIVER,PLAYER_MODES.MOTORCYCLE_DRIVER,PLAYER_MODES.BICYCLE,PLAYER_MODES.SKATE,PLAYER_MODES.BOAT_DRIVER].includes(resolved))return{x:clamp(Number(x)||0,-1,1),z:clamp(Number(z)||0,-1,1),steer:-clamp(Number(x)||0,-1,1),throttle:clamp(Number(z)||0,-1,1),mode:resolved};const vector=cameraRelativeVector(Number(x)||0,Number(z)||0,Number(yaw)||0);return{x:vector.x,z:vector.z,steer:0,throttle:0,mode:resolved};},`
- [x] `return` — linha 5303, coluna 94 em `stepVehicleSimulation()` — `setInventory:patch=>{state.inventory={...state.inventory,...(patch||{})};saveState(true);return{...state.inventory};},`
- [x] `return` — linha 5304, coluna 173 em `stepVehicleSimulation()` — `prepareBuild:(type='block',x=0,z=35,facing=0)=>{player.x=Number(x)||0;player.z=Number(z)||35;player.y=groundHeightAt(player.x,player.z);player.facing=Number(facing)||0;return beginBuildMode(type);},`
- [x] `return` — linha 5310, coluna 43 em `stepVehicleSimulation()` — `saveNow:()=>{savePlayerPosition(true);return JSON.parse(JSON.stringify(state.position));},`
- [x] `if` — linha 5312, coluna 131 em `stepVehicleSimulation()` — `setQuality:(quality='auto')=>{const value=['auto','low','high'].includes(quality)?quality:'auto';state.settings.quality=value;if(value==='auto')perf.sessionTier=state.settings.autoTier||detectStableAutoTier();applyAdaptiveRenderSettings(true);lockStableSceneVisibility();saveState(true);return{requested:requestedQuality(),tier:qualityTier(),dpr:renderer?.getPixelRatio?.()||0};},`
- [x] `return` — linha 5312, coluna 292 em `stepVehicleSimulation()` — `setQuality:(quality='auto')=>{const value=['auto','low','high'].includes(quality)?quality:'auto';state.settings.quality=value;if(value==='auto')perf.sessionTier=state.settings.autoTier||detectStableAutoTier();applyAdaptiveRenderSettings(true);lockStableSceneVisibility();saveState(true);return{requested:requestedQuality(),tier:qualityTier(),dpr:renderer?.getPixelRatio?.()||0};},`
- [x] `return` — linha 5315, coluna 234 em `stepVehicleSimulation()` — `getVisual:()=>{const parts=playerModel?.userData?.parts||{};const modelY=playerModel?.position?.y||0;const minFootY=playerModel?.userData?.minFootY??0;const scaleY=playerGroup?.scale?.y||1;const rootY=playerGroup?.position?.y||0;return {procedural:!!playerModel?.userData?.proceduralOtthos,rendered:playerModel?.visible!==false,ownNameLabelVisible:playerGroup?.userData?.nameLabel?.visible!==false,rootY,modelY,minFootY,scaleY,visualBottom:rootY+(modelY+minFootY)*scaleY,limbs:{leftArm:parts.leftArm?.rotation?.x||0,rightArm:parts.rightArm?.rotation?.x||0,leftLeg:parts.leftLeg?.rotation?.x||0,rightLeg:parts.rightLeg?.rotation?.x||0}};},`
- [x] `return` — linha 5322, coluna 38 em `stepVehicleSimulation()` — `enterVehicle:()=>{enterVehicle();return player.vehicle;},`
- [x] `return` — linha 5324, coluna 36 em `stepVehicleSimulation()` — `exitVehicle:()=>{exitVehicle();return !player.vehicle;},`
- [x] `return` — linha 5325, coluna 231 em `stepVehicleSimulation()` — `setDriveInput:(steer=0,throttle=0)=>{input.virtualX=clamp(Number(steer)||0,-1,1);input.virtualZ=clamp(Number(throttle)||0,-1,1);input.virtualActive=Math.abs(input.virtualX)+Math.abs(input.virtualZ)>.001;resolveMovementInput();return {active:input.virtualActive,x:input.targetX,z:input.targetZ};},`
- [x] `return` — linha 5326, coluna 109 em `stepVehicleSimulation()` — `clearDriveInput:()=>{input.virtualX=0;input.virtualZ=0;input.virtualActive=false;resolveMovementInput();return {x:input.targetX,z:input.targetZ};},`
- [x] `if` — linha 5327, coluna 123 em `stepVehicleSimulation()` — `setMobilityButtons:(accelerate=false,brake=false)=>{input.mobilityAccelerate=!!accelerate;input.mobilityBrake=!!brake;if(input.mobilityAccelerate)input.mobilityBrake=false;updateMobilityControlLabels();return{accelerate:input.mobilityAccelerate,brake:input.mobilityBrake};},`
- [x] `return` — linha 5327, coluna 207 em `stepVehicleSimulation()` — `setMobilityButtons:(accelerate=false,brake=false)=>{input.mobilityAccelerate=!!accelerate;input.mobilityBrake=!!brake;if(input.mobilityAccelerate)input.mobilityBrake=false;updateMobilityControlLabels();return{accelerate:input.mobilityAccelerate,brake:input.mobilityBrake};},`
- [x] `return` — linha 5340, coluna 38 em `stepVehicleSimulation()` — `setWaypoint:id=>{setWaypoint(id);return state.waypoint;},`
- [x] `return` — linha 5341, coluna 92 em `stepVehicleSimulation()` — `clearWaypoint:()=>{state.waypoint=null;updateWaypointMarker();updateNavigation(0,true);return true;},`
- [x] `return` — linha 5343, coluna 106 em `stepVehicleSimulation()` — `setCameraZoom:value=>{cameraZoom=clamp(Number(value)||0,-4.5,9);state.settings.cameraZoom=cameraZoom;return cameraZoom;},`
- [x] `return` — linha 5344, coluna 62 em `stepVehicleSimulation()` — `sprint:active=>{input.touchSprint=!!active;updateRunUI();return sprintRequested();},`
- [x] `return` — linha 5345, coluna 82 em `stepVehicleSimulation()` — `joystickVector:(dx,dy)=>{input.joyX=clamp(dx,-1,1);input.joyZ=clamp(dy,-1,1);return resolveMovementInput();},`
- [x] `for` — linha 5346, coluna 125 em `stepVehicleSimulation()` — `stepPlayer:(frames=1,dt=1/60)=>{const n=clamp(Math.round(Number(frames)||1),1,600),step=clamp(Number(dt)||1/60,.001,.1);for(let i=0;i<n;i++)updatePlayer(step);return{x:player.x,y:player.y,z:player.z,facing:player.facing};},`
- [x] `return` — linha 5346, coluna 164 em `stepVehicleSimulation()` — `stepPlayer:(frames=1,dt=1/60)=>{const n=clamp(Math.round(Number(frames)||1),1,600),step=clamp(Number(dt)||1/60,.001,.1);for(let i=0;i<n;i++)updatePlayer(step);return{x:player.x,y:player.y,z:player.z,facing:player.facing};},`
- [x] `if` — linha 5347, coluna 67 em `stepVehicleSimulation()` — `enterHouseById:(id)=>{const h=world.houses.find(x=>x.id===id);if(!h)return false;enterHouse(h);return true;},`
- [x] `return` — linha 5347, coluna 73 em `stepVehicleSimulation()` — `enterHouseById:(id)=>{const h=world.houses.find(x=>x.id===id);if(!h)return false;enterHouse(h);return true;},`
- [x] `return` — linha 5347, coluna 100 em `stepVehicleSimulation()` — `enterHouseById:(id)=>{const h=world.houses.find(x=>x.id===id);if(!h)return false;enterHouse(h);return true;},`
- [x] `if` — linha 5373, coluna 326 em `stepVehicleSimulation()` — `sceneStability:()=>({critical:world.criticalSurfaces.length,hiddenCritical:world.criticalSurfaces.filter(m=>m&&!m.visible).length,frustumCulledCritical:world.criticalSurfaces.filter(m=>m?.frustumCulled).length,staticRenderObjects:world.staticRenderObjects||0,frustumEnabledStatic:(()=>{let n=0;worldGroup?.traverse?.(o=>{if((o.isMesh||o.isLine||o.isSprite)&&o.frustumCulled)n++;});return n;})(),mobile:perf.mobile,shadows:!!renderer?.shadowMap?.enabled}),`
- [x] `return` — linha 5373, coluna 386 em `stepVehicleSimulation()` — `sceneStability:()=>({critical:world.criticalSurfaces.length,hiddenCritical:world.criticalSurfaces.filter(m=>m&&!m.visible).length,frustumCulledCritical:world.criticalSurfaces.filter(m=>m?.frustumCulled).length,staticRenderObjects:world.staticRenderObjects||0,frustumEnabledStatic:(()=>{let n=0;worldGroup?.traverse?.(o=>{if((o.isMesh||o.isLine||o.isSprite)&&o.frustumCulled)n++;});return n;})(),mobile:perf.mobile,shadows:!!renderer?.shadowMap?.enabled}),`
- [x] `if` — linha 5380, coluna 193 em `stepVehicleSimulation()` — `rideMetro:(destinationId='village',stationId='central')=>{const destination=MAP_LOCATIONS.find(x=>x.id===destinationId),station=METRO_STATIONS.find(x=>x.id===stationId)||METRO_STATIONS[0];if(!destination)return false;rideMetroTo(station,destination);return true;},`
- [x] `return` — linha 5380, coluna 209 em `stepVehicleSimulation()` — `rideMetro:(destinationId='village',stationId='central')=>{const destination=MAP_LOCATIONS.find(x=>x.id===destinationId),station=METRO_STATIONS.find(x=>x.id===stationId)||METRO_STATIONS[0];if(!destination)return false;rideMetroTo(station,destination);return true;},`
- [x] `return` — linha 5380, coluna 255 em `stepVehicleSimulation()` — `rideMetro:(destinationId='village',stationId='central')=>{const destination=MAP_LOCATIONS.find(x=>x.id===destinationId),station=METRO_STATIONS.find(x=>x.id===stationId)||METRO_STATIONS[0];if(!destination)return false;rideMetroTo(station,destination);return true;},`
- [x] `if` — linha 5381, coluna 76 em `stepVehicleSimulation()` — `boardBus:id=>{const bus=world.buses.find(b=>b.id===id)||world.buses[0];if(!bus)return false;player.x=bus.group.position.x;player.z=bus.group.position.z;bus.stopUntil=performance.now()+2000;bus.lastStopId=bus.lastStopId||'test-stop';bus.lastStopName=bus.lastStopName||'Parada de teste';return enterBus(bus);},`
- [x] `return` — linha 5381, coluna 84 em `stepVehicleSimulation()` — `boardBus:id=>{const bus=world.buses.find(b=>b.id===id)||world.buses[0];if(!bus)return false;player.x=bus.group.position.x;player.z=bus.group.position.z;bus.stopUntil=performance.now()+2000;bus.lastStopId=bus.lastStopId||'test-stop';bus.lastStopName=bus.lastStopName||'Parada de teste';return enterBus(bus);},`
- [x] `return` — linha 5381, coluna 290 em `stepVehicleSimulation()` — `boardBus:id=>{const bus=world.buses.find(b=>b.id===id)||world.buses[0];if(!bus)return false;player.x=bus.group.position.x;player.z=bus.group.position.z;bus.stopUntil=performance.now()+2000;bus.lastStopId=bus.lastStopId||'test-stop';bus.lastStopName=bus.lastStopName||'Parada de teste';return enterBus(bus);},`
- [x] `return` — linha 5382, coluna 77 em `stepVehicleSimulation()` — `exitBus:()=>{const bus=world.buses.find(b=>b.id===player.transit.busId);return bus?exitBusAtStop(bus,{stopId:'test-stop',stopName:'Parada de teste'}):false;},`
- [x] `for` — linha 5383, coluna 129 em `stepVehicleSimulation()` — `stepTransit:(frames=60,dt=1/60)=>{const n=clamp(Math.round(Number(frames)||60),1,1200),step=clamp(Number(dt)||1/60,.001,.1);for(let i=0;i<n;i++)updateTransitWorld(step);return world.buses.map(b=>({id:b.id,x:b.group.position.x,z:b.group.position.z,stopped:busAtStop(b)}));},`
- [x] `return` — linha 5383, coluna 174 em `stepVehicleSimulation()` — `stepTransit:(frames=60,dt=1/60)=>{const n=clamp(Math.round(Number(frames)||60),1,1200),step=clamp(Number(dt)||1/60,.001,.1);for(let i=0;i<n;i++)updateTransitWorld(step);return world.buses.map(b=>({id:b.id,x:b.group.position.x,z:b.group.position.z,stopped:busAtStop(b)}));},`
- [x] `if` — linha 5386, coluna 79 em `stepVehicleSimulation()` — `setNpcRideCompanion:(id='nino')=>{const npc=world.npcs.find(n=>n.id===id);if(!npc)return false;npc.pendingRide=true;npc.following=true;npc.group.position.set(player.x+1,0,player.z);return true;},`
- [x] `return` — linha 5386, coluna 87 em `stepVehicleSimulation()` — `setNpcRideCompanion:(id='nino')=>{const npc=world.npcs.find(n=>n.id===id);if(!npc)return false;npc.pendingRide=true;npc.following=true;npc.group.position.set(player.x+1,0,player.z);return true;},`
- [x] `return` — linha 5386, coluna 186 em `stepVehicleSimulation()` — `setNpcRideCompanion:(id='nino')=>{const npc=world.npcs.find(n=>n.id===id);if(!npc)return false;npc.pendingRide=true;npc.following=true;npc.group.position.set(player.x+1,0,player.z);return true;},`
- [x] `for` — linha 5394, coluna 36 em `stepVehicleSimulation()` — `stepFire:(frames=60,dt=1/60)=>{for(let i=0;i<frames;i++)updateFireService(dt);return world.fires.map(f=>({id:f.id,active:f.active}));},`
- [x] `return` — linha 5394, coluna 83 em `stepVehicleSimulation()` — `stepFire:(frames=60,dt=1/60)=>{for(let i=0;i<frames;i++)updateFireService(dt);return world.fires.map(f=>({id:f.id,active:f.active}));},`
- [x] `return` — linha 5399, coluna 34 em `stepVehicleSimulation()` — `closeModal:()=>{closeModal();return els.modal.hidden;},`
- [x] `return` — linha 5405, coluna 42 em `stepVehicleSimulation()` — `stepAdventure:()=>{updateAdventure();return world.activeChallenge?{type:world.activeChallenge.type,progress:world.activeChallenge.progress.size}:null;},`
- [x] `if` — linha 5409, coluna 45 em `stepVehicleSimulation()` — `forceBoatState:(x=-38,z=52,heading=0)=>{if(player.vehicle)exitVehicle(true);player.boating=true;player.boat.passengerOf='';player.boat.heading=Number(heading)||0;player.boat.speed=0;player.boat.steerVisual=0;input.mobilityAccelerate=false;input.mobilityBrake=false;player.x=Number(x);player.z=Number(z);player.y=.78;state.boats.activeBoatId='lake-boat';if(world.boat){world.boat.group.position.set(player.x,.1,player.z);world.boat.heading=player.boat.heading;world.boat.group.rotation.y=player.boat.heading;}updateBoatPanel();updateVehicleControlsUI();return{canExit:validBoatExit(),distance:distanceToBoatDock()};},`
- [x] `if` — linha 5409, coluna 358 em `stepVehicleSimulation()` — `forceBoatState:(x=-38,z=52,heading=0)=>{if(player.vehicle)exitVehicle(true);player.boating=true;player.boat.passengerOf='';player.boat.heading=Number(heading)||0;player.boat.speed=0;player.boat.steerVisual=0;input.mobilityAccelerate=false;input.mobilityBrake=false;player.x=Number(x);player.z=Number(z);player.y=.78;state.boats.activeBoatId='lake-boat';if(world.boat){world.boat.group.position.set(player.x,.1,player.z);world.boat.heading=player.boat.heading;world.boat.group.rotation.y=player.boat.heading;}updateBoatPanel();updateVehicleControlsUI();return{canExit:validBoatExit(),distance:distanceToBoatDock()};},`
- [x] `return` — linha 5409, coluna 557 em `stepVehicleSimulation()` — `forceBoatState:(x=-38,z=52,heading=0)=>{if(player.vehicle)exitVehicle(true);player.boating=true;player.boat.passengerOf='';player.boat.heading=Number(heading)||0;player.boat.speed=0;player.boat.steerVisual=0;input.mobilityAccelerate=false;input.mobilityBrake=false;player.x=Number(x);player.z=Number(z);player.y=.78;state.boats.activeBoatId='lake-boat';if(world.boat){world.boat.group.position.set(player.x,.1,player.z);world.boat.heading=player.boat.heading;world.boat.group.rotation.y=player.boat.heading;}updateBoatPanel();updateVehicleControlsUI();return{canExit:validBoatExit(),distance:distanceToBoatDock()};},`
- [x] `for` — linha 5411, coluna 165 em `stepVehicleSimulation()` — `stepBoat:(steer=0,throttle=0,frames=60)=>{const n=clamp(Math.round(Number(frames)||60),1,600),dt=1/60,start={x:player.x,z:player.z,heading:player.boat.heading};for(let i=0;i<n;i++){updateBoatPhysics(dt,clamp(Number(steer)||0,-1,1),clamp(Number(throttle)||0,-1,1));const px=player.x,pz=player.z;player.x+=player.vx*dt;player.z+=player.vz*dt;constrainBoat(px,pz);}return{x:player.x,z:player.z,heading:player.boat.heading,speed:player.boat.speed,deltaHeading:player.boat.heading-start.heading,distance:Math.hypot(player.x-start.x,player.z-start.z)};},`
- [x] `return` — linha 5411, coluna 367 em `stepVehicleSimulation()` — `stepBoat:(steer=0,throttle=0,frames=60)=>{const n=clamp(Math.round(Number(frames)||60),1,600),dt=1/60,start={x:player.x,z:player.z,heading:player.boat.heading};for(let i=0;i<n;i++){updateBoatPhysics(dt,clamp(Number(steer)||0,-1,1),clamp(Number(throttle)||0,-1,1));const px=player.x,pz=player.z;player.x+=player.vx*dt;player.z+=player.vz*dt;constrainBoat(px,pz);}return{x:player.x,z:player.z,heading:player.boat.heading,speed:player.boat.speed,deltaHeading:player.boat.heading-start.heading,distance:Math.hypot(player.x-start.x,player.z-start.z)};},`
- [x] `return` — linha 5416, coluna 508 em `stepVehicleSimulation()` — `simulateRemotePresence:(data={})=>{const remote={uid:data.uid||'remote-test',name:data.name||'Jogador Teste',x:Number(data.x??player.x+2),y:Number(data.y??player.y),z:Number(data.z??player.z),r:Number(data.r??player.facing),vehicle:!!data.vehicle,vehicleId:data.vehicleId||'remote-car',vehicleRole:data.vehicleRole||'',boating:!!data.boating,boatId:data.boatId||'',boatRole:data.boatRole||'',scaleMode:data.scaleMode||'normal',color:data.color||0x5ad8ff};remotePlayerEvent(remote);updateMultiplayer(.1);return remote.uid;},`

## `31-neighborhood-world-controller.js`

- [x] `return` — linha 5426, coluna 114 em `roomWorldInfo()` — `function roomWorldInfo(id=window.OTTHOS_RTDB?.getRoom?.()||window.OTTHI_CONFIG?.defaultRoom||'bairro-central'){return ROOM_WORLD_LAYOUTS.get(id)||ROOM_WORLD_LAYOUTS.values().next().value||{id:'bairro-central',name:'Bairro Central',icon:'🏙️',entry:{x:0,z:8,yaw:0},bounds:{xMin:-48,xMax:48,zMin:-22,zMax:38},capacity:10};}`
- [x] `return` — linha 5427, coluna 73 em `roomHouseMarkers()` — `function roomHouseMarkers(){const mineUid=window.OTTHOS_RTDB?.uid||'';return [...cloudHouses.entries()].map(([id,data])=>{const local=world.houses.find(h=>h.id===id);const x=Number.isFinite(Number(data.x))&&Number(data.x)!==0?Number(data.x):Number(local?.x||0),z=Number.isFinite(Number(data.z))&&Number(data.z)!==0?Number(data.z):Number(local?.z||0);return{id,name:data.name||\`Casa de ${data.ownerName||'Jogador'}\`,ownerName:data.ownerName||'Jogador',ownerUid:data.ownerUid||'',x,z,mine:data.ownerUid===mineUid,locked:!!data.locked};}).filter(item=>Number.isFinite(item.x)&&Number.isFinite(item.z));}`
- [x] `return` — linha 5427, coluna 353 em `roomHouseMarkers()` — `function roomHouseMarkers(){const mineUid=window.OTTHOS_RTDB?.uid||'';return [...cloudHouses.entries()].map(([id,data])=>{const local=world.houses.find(h=>h.id===id);const x=Number.isFinite(Number(data.x))&&Number(data.x)!==0?Number(data.x):Number(local?.x||0),z=Number.isFinite(Number(data.z))&&Number(data.z)!==0?Number(data.z):Number(local?.z||0);return{id,name:data.name||\`Casa de ${data.ownerName||'Jogador'}\`,ownerName:data.ownerName||'Jogador',ownerUid:data.ownerUid||'',x,z,mine:data.ownerUid===mineUid,locked:!!data.locked};}).filter(item=>Number.isFinite(item.x)&&Number.isFinite(item.z));}`
- [x] `return` — linha 5428, coluna 32 em `mapHouseLocations()` — `function mapHouseLocations(){return roomHouseMarkers().map(h=>({id:\`online-house-${h.id}\`,houseId:h.id,name:h.name,icon:h.mine?'🏠':'🏡',x:h.x,z:h.z,navX:h.x,navZ:h.z,group:'Casas online',onlineHouse:true}));}`
- [x] `return` — linha 5429, coluna 49 em `mapRegionsMarkup()` — `function mapRegionsMarkup(project=worldToMap){return[...ROOM_WORLD_LAYOUTS.values()].map(room=>{const a=project(room.bounds.xMin,room.bounds.zMax),b=project(room.bounds.xMax,room.bounds.zMin),left=Math.min(a.left,b.left),top=Math.min(a.top,b.top),width=Math.abs(b.left-a.left),height=Math.abs(b.top-a.top),active=room.id===(window.OTTHOS_RTDB?.getRoom?.()||window.OTTHI_CONFIG?.defaultRoom);return\`<span class="map-neighborhood-region ${active?'active':''}" style="left:${left.toFixed(2)}%;top:${top.toFixed(2)}%;width:${width.toFixed(2)}%;height:${height.toFixed(2)}%;--room-accent:${room.accent||'#66e7ff'}" title="${escapeHtml(room.name)}"><b>${room.icon}</b><em>${room.shortName}</em></span>\`;}).join('');}`
- [x] `return` — linha 5429, coluna 394 em `mapRegionsMarkup()` — `function mapRegionsMarkup(project=worldToMap){return[...ROOM_WORLD_LAYOUTS.values()].map(room=>{const a=project(room.bounds.xMin,room.bounds.zMax),b=project(room.bounds.xMax,room.bounds.zMin),left=Math.min(a.left,b.left),top=Math.min(a.top,b.top),width=Math.abs(b.left-a.left),height=Math.abs(b.top-a.top),active=room.id===(window.OTTHOS_RTDB?.getRoom?.()||window.OTTHI_CONFIG?.defaultRoom);return\`<span class="map-neighborhood-region ${active?'active':''}" style="left:${left.toFixed(2)}%;top:${top.toFixed(2)}%;width:${width.toFixed(2)}%;height:${height.toFixed(2)}%;--room-accent:${room.accent||'#66e7ff'}" title="${escapeHtml(room.name)}"><b>${room.icon}</b><em>${room.shortName}</em></span>\`;}).join('');}`
- [x] `for` — linha 5431, coluna 5 em `clearRemoteRoomEntities()` — `for(const ghost of world.ghosts.values())scene?.remove(ghost);world.ghosts.clear();remotePresence.clear();multiplayerState.players=[];multiplayerState.count=1;`
- [x] `if` — linha 5435, coluna 5 em `resetMobilityForRoomChange()` — `if(fishingSession)cancelFishingSession();if(buildMode)endBuildMode('cancelled',true);if(currentHouse)exitHouse();if(player.vehicle)exitVehicle(true);if(player.boating)exitBoat(true);`
- [x] `if` — linha 5435, coluna 46 em `resetMobilityForRoomChange()` — `if(fishingSession)cancelFishingSession();if(buildMode)endBuildMode('cancelled',true);if(currentHouse)exitHouse();if(player.vehicle)exitVehicle(true);if(player.boating)exitBoat(true);`
- [x] `if` — linha 5435, coluna 90 em `resetMobilityForRoomChange()` — `if(fishingSession)cancelFishingSession();if(buildMode)endBuildMode('cancelled',true);if(currentHouse)exitHouse();if(player.vehicle)exitVehicle(true);if(player.boating)exitBoat(true);`
- [x] `if` — linha 5435, coluna 118 em `resetMobilityForRoomChange()` — `if(fishingSession)cancelFishingSession();if(buildMode)endBuildMode('cancelled',true);if(currentHouse)exitHouse();if(player.vehicle)exitVehicle(true);if(player.boating)exitBoat(true);`
- [x] `if` — linha 5435, coluna 154 em `resetMobilityForRoomChange()` — `if(fishingSession)cancelFishingSession();if(buildMode)endBuildMode('cancelled',true);if(currentHouse)exitHouse();if(player.vehicle)exitVehicle(true);if(player.boating)exitBoat(true);`
- [x] `if` — linha 5436, coluna 5 em `resetMobilityForRoomChange()` — `if(player.transit.mode){player.transit.mode='';player.transit.busId='';player.transit.destinationId='';player.transit.requestStop=false;player.sitUntil=0;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;document.body.classList.remove('bus-passenger');if(transitPanel)transitPanel.hidden=true;}`
- [x] `if` — linha 5436, coluna 159 em `resetMobilityForRoomChange()` — `if(player.transit.mode){player.transit.mode='';player.transit.busId='';player.transit.destinationId='';player.transit.requestStop=false;player.sitUntil=0;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;document.body.classList.remove('bus-passenger');if(transitPanel)transitPanel.hidden=true;}`
- [x] `if` — linha 5436, coluna 199 em `resetMobilityForRoomChange()` — `if(player.transit.mode){player.transit.mode='';player.transit.busId='';player.transit.destinationId='';player.transit.requestStop=false;player.sitUntil=0;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;document.body.classList.remove('bus-passenger');if(transitPanel)transitPanel.hidden=true;}`
- [x] `if` — linha 5436, coluna 239 em `resetMobilityForRoomChange()` — `if(player.transit.mode){player.transit.mode='';player.transit.busId='';player.transit.destinationId='';player.transit.requestStop=false;player.sitUntil=0;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;document.body.classList.remove('bus-passenger');if(transitPanel)transitPanel.hidden=true;}`
- [x] `if` — linha 5436, coluna 331 em `resetMobilityForRoomChange()` — `if(player.transit.mode){player.transit.mode='';player.transit.busId='';player.transit.destinationId='';player.transit.requestStop=false;player.sitUntil=0;if(playerModel)playerModel.visible=true;if(avatarLayer)avatarLayer.visible=true;if(contactShadow)contactShadow.visible=true;document.body.classList.remove('bus-passenger');if(transitPanel)transitPanel.hidden=true;}`
- [x] `return` — linha 5439, coluna 28 em `canChangeRoom()` — `function canChangeRoom(){return{ok:true};}`
- [x] `if` — linha 5442, coluna 38 em `applyRoomWorld()` — `const room=roomWorldInfo(roomId);if(!room)return false;const changed=appliedRoomId!==room.id,shouldTeleport=options.teleport!==false;`
- [x] `return` — linha 5442, coluna 47 em `applyRoomWorld()` — `const room=roomWorldInfo(roomId);if(!room)return false;const changed=appliedRoomId!==room.id,shouldTeleport=options.teleport!==false;`
- [x] `if` — linha 5443, coluna 5 em `applyRoomWorld()` — `if(shouldTeleport){const entry=safePointNear(room.entry.x,room.entry.z,{ignoreTraffic:true,allowWater:false,radius:.4,distances:[0,.7,1.2,1.8]});player.x=entry.x;player.y=entry.y||0;player.z=entry.z;player.facing=Number(room.entry.yaw||0);cameraYaw=player.facing;playerGroup?.position?.set(player.x,player.y,player.z);playerGroup&&(playerGroup.rotation.y=player.facing);state.position={x:player.x,y:player.y,z:player.z,yaw:cameraYaw};state.waypoint=null;world.routePath=[];}`
- [x] `if` — linha 5444, coluna 305 em `applyRoomWorld()` — `state.multiplayer.room=room.id;appliedRoomId=room.id;rememberSafePlayerPosition(true);updateNavigation(0,true);updateContext(true);updateHUD();saveState(true);window.dispatchEvent(new CustomEvent('otthi:room-world-ready',{detail:{room:room.id,name:room.name,entry:{x:player.x,z:player.z},changed}}));if(options.toast!==false)toast(\`${room.icon} Você entrou no ${room.name}.\`,'good',2600);return true;`
- [x] `return` — linha 5444, coluna 393 em `applyRoomWorld()` — `state.multiplayer.room=room.id;appliedRoomId=room.id;rememberSafePlayerPosition(true);updateNavigation(0,true);updateContext(true);updateHUD();saveState(true);window.dispatchEvent(new CustomEvent('otthi:room-world-ready',{detail:{room:room.id,name:room.name,entry:{x:player.x,z:player.z},changed}}));if(options.toast!==false)toast(\`${room.icon} Você entrou no ${room.name}.\`,'good',2600);return true;`
- [x] `if` — linha 5447, coluna 56 em `applyRoomWorld()` — `window.addEventListener('otthi:room-changed',event=>{if(event.detail?.connected===false)return;applyRoomWorld(event.detail?.room,{toast:event.detail?.previousRoom!==event.detail?.room,teleport:true});});`
- [x] `return` — linha 5447, coluna 91 em `applyRoomWorld()` — `window.addEventListener('otthi:room-changed',event=>{if(event.detail?.connected===false)return;applyRoomWorld(event.detail?.room,{toast:event.detail?.previousRoom!==event.detail?.room,teleport:true});});`
- [x] `if` — linha 5448, coluna 84 em `applyRoomWorld()` — `window.addEventListener('otthos:mp-status',event=>{const room=event.detail?.room;if(event.detail?.connected&&room&&!appliedRoomId&&running)applyRoomWorld(room,{toast:false,teleport:state.multiplayer.room!==room});});`
- [x] `if` — linha 5449, coluna 48 em `applyRoomWorld()` — `window.addEventListener('otthos:houses',()=>{if(!els.modal.hidden&&els.modal.classList.contains('map-modal'))refreshOpenMapAfterResize();updateNavigation(0,true);});`

## `32-cooperative-missions.js`

- [x] `return` — linha 5464, coluna 177 em `ensureCooperativeState()` — `function ensureCooperativeState(){state.cooperative={active:null,completed:[],history:[],preferredMode:'coop',soloFallback:true,lastLobbyNotice:0,...(state.cooperative||{})};return state.cooperative;}`
- [x] `return` — linha 5465, coluna 22 em `coopUid()` — `function coopUid(){return String(window.OTTHOS_RTDB?.uid||state.profile?.playerId||'local');}`
- [x] `return` — linha 5466, coluna 36 em `coopMissionTemplate()` — `function coopMissionTemplate(id){return COOP_MISSION_TEMPLATES[String(id||'')]||null;}`
- [x] `return` — linha 5467, coluna 57 em `activeCoopMission()` — `function activeCoopMission(){ensureCooperativeState();return state.cooperative.active;}`
- [x] `if` — linha 5468, coluna 64 em `activeCoopRecord()` — `function activeCoopRecord(){const active=activeCoopMission();if(!active)return null;return active.remote&&coopRemoteMissions[active.id]?coopRemoteMissions[active.id]:active.record||active;}`
- [x] `return` — linha 5468, coluna 75 em `activeCoopRecord()` — `function activeCoopRecord(){const active=activeCoopMission();if(!active)return null;return active.remote&&coopRemoteMissions[active.id]?coopRemoteMissions[active.id]:active.record||active;}`
- [x] `return` — linha 5468, coluna 87 em `activeCoopRecord()` — `function activeCoopRecord(){const active=activeCoopMission();if(!active)return null;return active.remote&&coopRemoteMissions[active.id]?coopRemoteMissions[active.id]:active.record||active;}`
- [x] `return` — linha 5469, coluna 68 em `activeCoopServiceJob()` — `function activeCoopServiceJob(){const active=activeCoopMission();return active?.serviceJob||null;}`
- [x] `return` — linha 5470, coluna 56 em `coopParticipants()` — `function coopParticipants(record=activeCoopRecord()){return Object.values(record?.participants||{}).filter(Boolean);}`
- [x] `return` — linha 5471, coluna 58 em `coopOwnParticipant()` — `function coopOwnParticipant(record=activeCoopRecord()){return record?.participants?.[coopUid()]||null;}`
- [x] `return` — linha 5472, coluna 32 em `coopRoleLabel()` — `function coopRoleLabel(role){return COOP_ROLE_LABELS[role]||'Ajudante';}`
- [x] `return` — linha 5473, coluna 52 em `coopProgress()` — `function coopProgress(record=activeCoopRecord()){return{phase:Math.max(0,Number(record?.progress?.phase||0)),counter:Math.max(0,Number(record?.progress?.counter||0)),target:Math.max(1,Number(record?.progress?.target||1)),events:record?.progress?.events||{}};}`
- [x] `return` — linha 5474, coluna 63 em `coopEventValues()` — `function coopEventValues(record=activeCoopRecord(),kind=''){return Object.values(coopProgress(record).events).filter(event=>!kind||event?.kind===kind);}`
- [x] `return` — linha 5475, coluna 43 em `coopHasEvent()` — `function coopHasEvent(kind,uidValue=''){return coopEventValues(activeCoopRecord(),kind).some(event=>!uidValue||event.uid===uidValue);}`
- [x] `return` — linha 5476, coluna 86 em `coopIndividualCount()` — `function coopIndividualCount(record=activeCoopRecord(),kind='',uidValue=coopUid()){return coopEventValues(record,kind).filter(event=>event?.uid===uidValue).reduce((sum,event)=>sum+Math.max(0,Number(event.value||0)),0);}`
- [x] `return` — linha 5477, coluna 44 em `coopRoleCapacity()` — `function coopRoleCapacity(template,role){return template?.roles?.filter(item=>item[0]===role).length||0;}`
- [x] `return` — linha 5478, coluna 59 em `coopRoleTaken()` — `function coopRoleTaken(record,role,ignoreUid=coopUid()){return coopParticipants(record).filter(item=>item.uid!==ignoreUid&&item.role===role).length;}`
- [x] `return` — linha 5479, coluna 42 em `coopEventSubjectId()` — `function coopEventSubjectId(prefix,id){return\`${prefix}-${String(id||'').replace(/[^A-Za-z0-9_-]/g,'').slice(0,56)}\`;}`
- [x] `if` — linha 5480, coluna 102 em `coopMedicalDestination()` — `function coopMedicalDestination(){const hospital=world.hospital||world.clinic||world.medicalCenter;if(hospital)return{id:hospital.id||'medical-center',name:hospital.name||'Ponto médico',icon:'🏥',x:Number(hospital.x??hospital.group?.position?.x??0),z:Number(hospital.z??hospital.group?.position?.z??0),ref:hospital};return{id:'medical-center',name:'Ponto médico da Vila',icon:'🏥',x:18,z:-24,navX:18,navZ:-24};}`
- [x] `return` — linha 5480, coluna 114 em `coopMedicalDestination()` — `function coopMedicalDestination(){const hospital=world.hospital||world.clinic||world.medicalCenter;if(hospital)return{id:hospital.id||'medical-center',name:hospital.name||'Ponto médico',icon:'🏥',x:Number(hospital.x??hospital.group?.position?.x??0),z:Number(hospital.z??hospital.group?.position?.z??0),ref:hospital};return{id:'medical-center',name:'Ponto médico da Vila',icon:'🏥',x:18,z:-24,navX:18,navZ:-24};}`
- [x] `return` — linha 5480, coluna 318 em `coopMedicalDestination()` — `function coopMedicalDestination(){const hospital=world.hospital||world.clinic||world.medicalCenter;if(hospital)return{id:hospital.id||'medical-center',name:hospital.name||'Ponto médico',icon:'🏥',x:Number(hospital.x??hospital.group?.position?.x??0),z:Number(hospital.z??hospital.group?.position?.z??0),ref:hospital};return{id:'medical-center',name:'Ponto médico da Vila',icon:'🏥',x:18,z:-24,navX:18,navZ:-24};}`
- [x] `return` — linha 5482, coluna 424 em `coopRaceStandings()` — `const kind=record?.type==='ovalRace'?'oval-lap':'street-checkpoint',started=Number(record?.startedAt||record?.createdAtClient||Date.now()),rows=coopParticipants(record).map(item=>{const events=coopEventValues(record,kind).filter(event=>event.uid===item.uid),score=events.reduce((sum,event)=>sum+Math.max(0,Number(event.value||0)),0),last=events.reduce((value,event)=>Math.max(value,Number(event.createdAtClient||0)),0);return{...item,score,elapsed:last?Math.max(0,last-started):0};});return rows.sort((a,b)=>b.score-a.score||(a.elapsed||Infinity)-(b.elapsed||Infinity)||a.name.localeCompare(b.name));`
- [x] `return` — linha 5482, coluna 489 em `coopRaceStandings()` — `const kind=record?.type==='ovalRace'?'oval-lap':'street-checkpoint',started=Number(record?.startedAt||record?.createdAtClient||Date.now()),rows=coopParticipants(record).map(item=>{const events=coopEventValues(record,kind).filter(event=>event.uid===item.uid),score=events.reduce((sum,event)=>sum+Math.max(0,Number(event.value||0)),0),last=events.reduce((value,event)=>Math.max(value,Number(event.createdAtClient||0)),0);return{...item,score,elapsed:last?Math.max(0,last-started):0};});return rows.sort((a,b)=>b.score-a.score||(a.elapsed||Infinity)-(b.elapsed||Infinity)||a.name.localeCompare(b.name));`
- [x] `return` — linha 5484, coluna 140 em `coopRaceTargetReached()` — `function coopRaceTargetReached(record=activeCoopRecord()){const target=record?.type==='ovalRace'?3:4,standings=coopRaceStandings(record);return record?.mode==='competitive'?Number(standings[0]?.score||0)>=target:coopProgress(record).counter>=target;}`
- [x] `if` — linha 5486, coluna 121 em `coopMissionMapLocations()` — `const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type||active?.type);if(!active||!template)return[];`
- [x] `return` — linha 5486, coluna 143 em `coopMissionMapLocations()` — `const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type||active?.type);if(!active||!template)return[];`
- [x] `if` — linha 5487, coluna 51 em `coopMissionMapLocations()` — `const target=coopCurrentTarget(active,record);if(!target)return[];`
- [x] `return` — linha 5487, coluna 62 em `coopMissionMapLocations()` — `const target=coopCurrentTarget(active,record);if(!target)return[];`
- [x] `return` — linha 5488, coluna 5 em `coopMissionMapLocations()` — `return[{id:\`coop-${active.id}-${target.id||'target'}\`,name:target.name||template.title,icon:target.icon||template.icon,group:'Missão cooperativa',x:target.x,z:target.z,navX:target.navX??target.x,navZ:target.navZ??target.z,missionTarget:true,coopTarget:true,description:target.description||coopCurrentInstruction(active,record),actions:[\`Função: ${coopRoleLabel(coopOwnParticipant(record)?.role||active.role)}\`,'Siga o GPS','Use AÇÃO no ponto marcado']}];`
- [x] `return` — linha 5490, coluna 34 em `coopServiceKind()` — `function coopServiceKind(type){return['firefighter','paramedic','police'].includes(type)?type:'';}`
- [x] `for` — linha 5491, coluna 58 em `coopMissionHash()` — `function coopMissionHash(value=''){let hash=2166136261;for(const char of String(value||'')){hash^=char.charCodeAt(0);hash=Math.imul(hash,16777619);}return hash>>>0;}`
- [x] `return` — linha 5491, coluna 151 em `coopMissionHash()` — `function coopMissionHash(value=''){let hash=2166136261;for(const char of String(value||'')){hash^=char.charCodeAt(0);hash=Math.imul(hash,16777619);}return hash>>>0;}`
- [x] `if` — linha 5494, coluna 5 em `coopSceneTarget()` — `if(type==='firefighter'){const site=FIRE_SITES[seed%FIRE_SITES.length],fire=world.fires.find(item=>item.id===site?.id&&item.active)||activateFireIncident(site?.id||'',true);if(fire){fire.coopMissionId=missionId;fire.coopIntensity=1;for(const truck of world.fireTrucks||[]){if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}return fire?{id:\`fire-${fire.id}\`,name:fire.name,icon:'🔥',x:fire.x,z:fire.z,navX:fire.navX,navZ:fire.navZ,ref:fire}:null;}`
- [x] `if` — linha 5494, coluna 178 em `coopSceneTarget()` — `if(type==='firefighter'){const site=FIRE_SITES[seed%FIRE_SITES.length],fire=world.fires.find(item=>item.id===site?.id&&item.active)||activateFireIncident(site?.id||'',true);if(fire){fire.coopMissionId=missionId;fire.coopIntensity=1;for(const truck of world.fireTrucks||[]){if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}return fire?{id:\`fire-${fire.id}\`,name:fire.name,icon:'🔥',x:fire.x,z:fire.z,navX:fire.navX,navZ:fire.navZ,ref:fire}:null;}`
- [x] `for` — linha 5494, coluna 237 em `coopSceneTarget()` — `if(type==='firefighter'){const site=FIRE_SITES[seed%FIRE_SITES.length],fire=world.fires.find(item=>item.id===site?.id&&item.active)||activateFireIncident(site?.id||'',true);if(fire){fire.coopMissionId=missionId;fire.coopIntensity=1;for(const truck of world.fireTrucks||[]){if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}return fire?{id:\`fire-${fire.id}\`,name:fire.name,icon:'🔥',x:fire.x,z:fire.z,navX:fire.navX,navZ:fire.navZ,ref:fire}:null;}`
- [x] `if` — linha 5494, coluna 278 em `coopSceneTarget()` — `if(type==='firefighter'){const site=FIRE_SITES[seed%FIRE_SITES.length],fire=world.fires.find(item=>item.id===site?.id&&item.active)||activateFireIncident(site?.id||'',true);if(fire){fire.coopMissionId=missionId;fire.coopIntensity=1;for(const truck of world.fireTrucks||[]){if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}return fire?{id:\`fire-${fire.id}\`,name:fire.name,icon:'🔥',x:fire.x,z:fire.z,navX:fire.navX,navZ:fire.navZ,ref:fire}:null;}`
- [x] `return` — linha 5494, coluna 384 em `coopSceneTarget()` — `if(type==='firefighter'){const site=FIRE_SITES[seed%FIRE_SITES.length],fire=world.fires.find(item=>item.id===site?.id&&item.active)||activateFireIncident(site?.id||'',true);if(fire){fire.coopMissionId=missionId;fire.coopIntensity=1;for(const truck of world.fireTrucks||[]){if(truck.targetFireId===fire.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}return fire?{id:\`fire-${fire.id}\`,name:fire.name,icon:'🔥',x:fire.x,z:fire.z,navX:fire.navX,navZ:fire.navZ,ref:fire}:null;}`
- [x] `if` — linha 5495, coluna 5 em `coopSceneTarget()` — `if(type==='paramedic'){let incident=world.activeIncident;if(!incident){const site=TRAINING_INCIDENT_SITES[seed%TRAINING_INCIDENT_SITES.length],projection=nearestRoadProjection(site)?.point||site;incident=createTrafficIncidentAt(projection.x,projection.z,'minor',[]);}if(incident){incident.coopMissionId=missionId;incident.playerAssigned={police:true,paramedic:true,firefighter:true};incident.responders={};for(const vehicle of serviceVehiclePools()){if(vehicle.incidentTargetId===incident.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}}}return incident?{id:\`incident-${incident.id}\`,name:'Área segura do resgate',icon:'⚠️',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,ref:incident}:null;}`
- [x] `if` — linha 5495, coluna 62 em `coopSceneTarget()` — `if(type==='paramedic'){let incident=world.activeIncident;if(!incident){const site=TRAINING_INCIDENT_SITES[seed%TRAINING_INCIDENT_SITES.length],projection=nearestRoadProjection(site)?.point||site;incident=createTrafficIncidentAt(projection.x,projection.z,'minor',[]);}if(incident){incident.coopMissionId=missionId;incident.playerAssigned={police:true,paramedic:true,firefighter:true};incident.responders={};for(const vehicle of serviceVehiclePools()){if(vehicle.incidentTargetId===incident.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}}}return incident?{id:\`incident-${incident.id}\`,name:'Área segura do resgate',icon:'⚠️',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,ref:incident}:null;}`
- [x] `if` — linha 5495, coluna 272 em `coopSceneTarget()` — `if(type==='paramedic'){let incident=world.activeIncident;if(!incident){const site=TRAINING_INCIDENT_SITES[seed%TRAINING_INCIDENT_SITES.length],projection=nearestRoadProjection(site)?.point||site;incident=createTrafficIncidentAt(projection.x,projection.z,'minor',[]);}if(incident){incident.coopMissionId=missionId;incident.playerAssigned={police:true,paramedic:true,firefighter:true};incident.responders={};for(const vehicle of serviceVehiclePools()){if(vehicle.incidentTargetId===incident.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}}}return incident?{id:\`incident-${incident.id}\`,name:'Área segura do resgate',icon:'⚠️',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,ref:incident}:null;}`
- [x] `for` — linha 5495, coluna 411 em `coopSceneTarget()` — `if(type==='paramedic'){let incident=world.activeIncident;if(!incident){const site=TRAINING_INCIDENT_SITES[seed%TRAINING_INCIDENT_SITES.length],projection=nearestRoadProjection(site)?.point||site;incident=createTrafficIncidentAt(projection.x,projection.z,'minor',[]);}if(incident){incident.coopMissionId=missionId;incident.playerAssigned={police:true,paramedic:true,firefighter:true};incident.responders={};for(const vehicle of serviceVehiclePools()){if(vehicle.incidentTargetId===incident.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}}}return incident?{id:\`incident-${incident.id}\`,name:'Área segura do resgate',icon:'⚠️',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,ref:incident}:null;}`
- [x] `if` — linha 5495, coluna 455 em `coopSceneTarget()` — `if(type==='paramedic'){let incident=world.activeIncident;if(!incident){const site=TRAINING_INCIDENT_SITES[seed%TRAINING_INCIDENT_SITES.length],projection=nearestRoadProjection(site)?.point||site;incident=createTrafficIncidentAt(projection.x,projection.z,'minor',[]);}if(incident){incident.coopMissionId=missionId;incident.playerAssigned={police:true,paramedic:true,firefighter:true};incident.responders={};for(const vehicle of serviceVehiclePools()){if(vehicle.incidentTargetId===incident.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}}}return incident?{id:\`incident-${incident.id}\`,name:'Área segura do resgate',icon:'⚠️',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,ref:incident}:null;}`
- [x] `return` — linha 5495, coluna 578 em `coopSceneTarget()` — `if(type==='paramedic'){let incident=world.activeIncident;if(!incident){const site=TRAINING_INCIDENT_SITES[seed%TRAINING_INCIDENT_SITES.length],projection=nearestRoadProjection(site)?.point||site;incident=createTrafficIncidentAt(projection.x,projection.z,'minor',[]);}if(incident){incident.coopMissionId=missionId;incident.playerAssigned={police:true,paramedic:true,firefighter:true};incident.responders={};for(const vehicle of serviceVehiclePools()){if(vehicle.incidentTargetId===incident.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}}}return incident?{id:\`incident-${incident.id}\`,name:'Área segura do resgate',icon:'⚠️',x:incident.x,z:incident.z,navX:incident.navX,navZ:incident.navZ,ref:incident}:null;}`
- [x] `if` — linha 5496, coluna 5 em `coopSceneTarget()` — `if(type==='police'){const candidates=(world.npcs||[]).filter(item=>!item.coopRaceBot),npc=candidates[seed%Math.max(1,candidates.length)]||world.npcs[0];return npc?{id:\`police-${npc.id}\`,name:'Abordagem educativa',icon:'👮',x:npc.group.position.x,z:npc.group.position.z,ref:npc}:null;}`
- [x] `return` — linha 5496, coluna 157 em `coopSceneTarget()` — `if(type==='police'){const candidates=(world.npcs||[]).filter(item=>!item.coopRaceBot),npc=candidates[seed%Math.max(1,candidates.length)]||world.npcs[0];return npc?{id:\`police-${npc.id}\`,name:'Abordagem educativa',icon:'👮',x:npc.group.position.x,z:npc.group.position.z,ref:npc}:null;}`
- [x] `return` — linha 5497, coluna 5 em `coopSceneTarget()` — `return null;`
- [x] `return` — linha 5499, coluna 32 em `coopNearestSchool()` — `function coopNearestSchool(){return(world.schools||[]).sort((a,b)=>Math.hypot(player.x-a.x,player.z-a.z)-Math.hypot(player.x-b.x,player.z-b.z))[0]||world.school;}`
- [x] `return` — linha 5500, coluna 101 em `coopNearestInvitableNpc()` — `function coopNearestInvitableNpc(active){const events=coopProgress(activeCoopRecord()).events||{};return(world.npcs||[]).filter(n=>!n.coopRaceBot&&!active.invitedNpcIds?.includes(n.id)&&!n.coopSchoolDelivered&&!events[coopEventSubjectId('student',n.id)]&&!events[coopEventSubjectId('student-delivered',n.id)]).sort((a,b)=>Math.hypot(player.x-a.group.position.x,player.z-a.group.position.z)-Math.hypot(player.x-b.group.position.x,player.z-b.group.position.z))[0]||null;}`
- [x] `if` — linha 5502, coluna 5 em `coopCurrentTarget()` — `if(!active||!record)return null;const type=record.type,progress=coopProgress(record),phase=progress.phase;`
- [x] `return` — linha 5502, coluna 25 em `coopCurrentTarget()` — `if(!active||!record)return null;const type=record.type,progress=coopProgress(record),phase=progress.phase;`
- [x] `if` — linha 5503, coluna 5 em `coopCurrentTarget()` — `if(coopServiceKind(type)){`
- [x] `if` — linha 5504, coluna 7 em `coopCurrentTarget()` — `if(phase<1){const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)return{id:\`service-${vehicle.id}\`,name:vehicle.label,icon:type==='police'?'🚓':type==='firefighter'?'🚒':'🚑',x:vehicle.group.position.x,z:vehicle.group.position.z,ref:vehicle,description:'Veículo reservado para esta equipe.'};}`
- [x] `if` — linha 5504, coluna 74 em `coopCurrentTarget()` — `if(phase<1){const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)return{id:\`service-${vehicle.id}\`,name:vehicle.label,icon:type==='police'?'🚓':type==='firefighter'?'🚒':'🚑',x:vehicle.group.position.x,z:vehicle.group.position.z,ref:vehicle,description:'Veículo reservado para esta equipe.'};}`
- [x] `return` — linha 5504, coluna 85 em `coopCurrentTarget()` — `if(phase<1){const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)return{id:\`service-${vehicle.id}\`,name:vehicle.label,icon:type==='police'?'🚓':type==='firefighter'?'🚒':'🚑',x:vehicle.group.position.x,z:vehicle.group.position.z,ref:vehicle,description:'Veículo reservado para esta equipe.'};}`
- [x] `if` — linha 5505, coluna 7 em `coopCurrentTarget()` — `if(type==='paramedic'&&phase>=5)return coopMedicalDestination();`
- [x] `return` — linha 5505, coluna 39 em `coopCurrentTarget()` — `if(type==='paramedic'&&phase>=5)return coopMedicalDestination();`
- [x] `if` — linha 5506, coluna 7 em `coopCurrentTarget()` — `if(type==='paramedic'&&phase>=4){const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)return{id:\`service-${vehicle.id}\`,name:'Colocar o paciente na ambulância',icon:'🚑',x:vehicle.group.position.x,z:vehicle.group.position.z,ref:vehicle};}`
- [x] `if` — linha 5506, coluna 95 em `coopCurrentTarget()` — `if(type==='paramedic'&&phase>=4){const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)return{id:\`service-${vehicle.id}\`,name:'Colocar o paciente na ambulância',icon:'🚑',x:vehicle.group.position.x,z:vehicle.group.position.z,ref:vehicle};}`
- [x] `return` — linha 5506, coluna 106 em `coopCurrentTarget()` — `if(type==='paramedic'&&phase>=4){const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)return{id:\`service-${vehicle.id}\`,name:'Colocar o paciente na ambulância',icon:'🚑',x:vehicle.group.position.x,z:vehicle.group.position.z,ref:vehicle};}`
- [x] `if` — linha 5507, coluna 7 em `coopCurrentTarget()` — `if(type==='police'&&phase>=4){const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)return{id:\`service-${vehicle.id}\`,name:'Acompanhar até a viatura',icon:'🚓',x:vehicle.group.position.x,z:vehicle.group.position.z,ref:vehicle};}`
- [x] `if` — linha 5507, coluna 92 em `coopCurrentTarget()` — `if(type==='police'&&phase>=4){const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)return{id:\`service-${vehicle.id}\`,name:'Acompanhar até a viatura',icon:'🚓',x:vehicle.group.position.x,z:vehicle.group.position.z,ref:vehicle};}`
- [x] `return` — linha 5507, coluna 103 em `coopCurrentTarget()` — `if(type==='police'&&phase>=4){const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)return{id:\`service-${vehicle.id}\`,name:'Acompanhar até a viatura',icon:'🚓',x:vehicle.group.position.x,z:vehicle.group.position.z,ref:vehicle};}`
- [x] `return` — linha 5508, coluna 7 em `coopCurrentTarget()` — `return active.sceneTarget||coopSceneTarget(type);`
- [x] `if` — linha 5510, coluna 5 em `coopCurrentTarget()` — `if(type==='fishing')return phase<2?{id:'coop-lake',name:'Ponto de pescaria do lago',icon:'🎣',x:-63,z:52,navX:-55,navZ:52}:{id:'coop-campfire',name:'Fogueira da equipe',icon:'🔥',x:-70,z:-62};`
- [x] `return` — linha 5510, coluna 25 em `coopCurrentTarget()` — `if(type==='fishing')return phase<2?{id:'coop-lake',name:'Ponto de pescaria do lago',icon:'🎣',x:-63,z:52,navX:-55,navZ:52}:{id:'coop-campfire',name:'Fogueira da equipe',icon:'🔥',x:-70,z:-62};`
- [x] `if` — linha 5511, coluna 5 em `coopCurrentTarget()` — `if(type==='school'){`
- [x] `if` — linha 5512, coluna 7 em `coopCurrentTarget()` — `if(progress.counter>=4){const school=coopNearestSchool();return school?{id:school.id,name:school.name,icon:'🏫',x:school.x,z:school.z,ref:school}:null;}`
- [x] `return` — linha 5512, coluna 64 em `coopCurrentTarget()` — `if(progress.counter>=4){const school=coopNearestSchool();return school?{id:school.id,name:school.name,icon:'🏫',x:school.x,z:school.z,ref:school}:null;}`
- [x] `if` — linha 5513, coluna 87 em `coopCurrentTarget()` — `const follower=(world.npcs||[]).find(n=>active.followingNpcIds?.includes(n.id));if(follower){const school=coopNearestSchool();return school?{id:school.id,name:\`Levar ${follower.name} à escola\`,icon:'🏫',x:school.x,z:school.z,ref:school}:null;}`
- [x] `return` — linha 5513, coluna 133 em `coopCurrentTarget()` — `const follower=(world.npcs||[]).find(n=>active.followingNpcIds?.includes(n.id));if(follower){const school=coopNearestSchool();return school?{id:school.id,name:\`Levar ${follower.name} à escola\`,icon:'🏫',x:school.x,z:school.z,ref:school}:null;}`
- [x] `return` — linha 5514, coluna 49 em `coopCurrentTarget()` — `const npc=coopNearestInvitableNpc(active);return npc?{id:\`student-${npc.id}\`,name:\`Convidar ${npc.name}\`,icon:'🧒',x:npc.group.position.x,z:npc.group.position.z,ref:npc}:null;`
- [x] `if` — linha 5516, coluna 5 em `coopCurrentTarget()` — `if(type==='streetRace')return{ id:'coop-street-race',name:'Largada da corrida de rua',icon:'🏁',x:45,z:82};`
- [x] `return` — linha 5516, coluna 28 em `coopCurrentTarget()` — `if(type==='streetRace')return{ id:'coop-street-race',name:'Largada da corrida de rua',icon:'🏁',x:45,z:82};`
- [x] `if` — linha 5517, coluna 5 em `coopCurrentTarget()` — `if(type==='ovalRace')return{ id:'coop-oval-race',name:'Pista oval do ginásio',icon:'🏟️',x:45,z:84};`
- [x] `return` — linha 5517, coluna 26 em `coopCurrentTarget()` — `if(type==='ovalRace')return{ id:'coop-oval-race',name:'Pista oval do ginásio',icon:'🏟️',x:45,z:84};`
- [x] `return` — linha 5518, coluna 5 em `coopCurrentTarget()` — `return null;`
- [x] `if` — linha 5521, coluna 5 em `coopCurrentInstruction()` — `if(!active||!record)return'Sem missão cooperativa ativa.';const template=coopMissionTemplate(record.type),p=coopProgress(record),role=coopRoleLabel(coopOwnParticipant(record)?.role||active.role),step=template?.steps[Math.min(p.phase,(template.steps?.length||1)-1)]||template?.description;`
- [x] `return` — linha 5521, coluna 25 em `coopCurrentInstruction()` — `if(!active||!record)return'Sem missão cooperativa ativa.';const template=coopMissionTemplate(record.type),p=coopProgress(record),role=coopRoleLabel(coopOwnParticipant(record)?.role||active.role),step=template?.steps[Math.min(p.phase,(template.steps?.length||1)-1)]||template?.description;`
- [x] `if` — linha 5522, coluna 5 em `coopCurrentInstruction()` — `if(record.status==='lobby')return\`Aguarde a equipe. Sua função: ${role}.\`;`
- [x] `return` — linha 5522, coluna 32 em `coopCurrentInstruction()` — `if(record.status==='lobby')return\`Aguarde a equipe. Sua função: ${role}.\`;`
- [x] `if` — linha 5523, coluna 5 em `coopCurrentInstruction()` — `if(record.type==='firefighter'&&p.phase===3)return\`Fogo controlado: ${p.counter}/4. A pessoa da mangueira deve aplicar água até zerar o risco.\`;`
- [x] `return` — linha 5523, coluna 49 em `coopCurrentInstruction()` — `if(record.type==='firefighter'&&p.phase===3)return\`Fogo controlado: ${p.counter}/4. A pessoa da mangueira deve aplicar água até zerar o risco.\`;`
- [x] `if` — linha 5524, coluna 5 em `coopCurrentInstruction()` — `if(record.type==='firefighter'&&p.phase>=4)return'A pessoa de apoio deve isolar e liberar a área.';`
- [x] `return` — linha 5524, coluna 48 em `coopCurrentInstruction()` — `if(record.type==='firefighter'&&p.phase>=4)return'A pessoa de apoio deve isolar e liberar a área.';`
- [x] `if` — linha 5525, coluna 5 em `coopCurrentInstruction()` — `if(record.type==='paramedic'&&p.phase===4)return'Leve a maca até a ambulância e use AÇÃO para acomodar o paciente.';`
- [x] `return` — linha 5525, coluna 47 em `coopCurrentInstruction()` — `if(record.type==='paramedic'&&p.phase===4)return'Leve a maca até a ambulância e use AÇÃO para acomodar o paciente.';`
- [x] `if` — linha 5526, coluna 5 em `coopCurrentInstruction()` — `if(record.type==='paramedic'&&p.phase>=5)return'O motorista deve levar a ambulância ao ponto médico marcado.';`
- [x] `return` — linha 5526, coluna 46 em `coopCurrentInstruction()` — `if(record.type==='paramedic'&&p.phase>=5)return'O motorista deve levar a ambulância ao ponto médico marcado.';`
- [x] `if` — linha 5527, coluna 5 em `coopCurrentInstruction()` — `if(record.type==='police'&&p.phase>=4)return'Acompanhe o personagem com calma até a viatura marcada.';`
- [x] `return` — linha 5527, coluna 43 em `coopCurrentInstruction()` — `if(record.type==='police'&&p.phase>=4)return'Acompanhe o personagem com calma até a viatura marcada.';`
- [x] `if` — linha 5528, coluna 5 em `coopCurrentInstruction()` — `if(record.type==='fishing'&&p.phase===1)return\`Equipe: ${p.counter}/6 peixes. Pesque junto com os outros jogadores.\`;`
- [x] `return` — linha 5528, coluna 45 em `coopCurrentInstruction()` — `if(record.type==='fishing'&&p.phase===1)return\`Equipe: ${p.counter}/6 peixes. Pesque junto com os outros jogadores.\`;`
- [x] `if` — linha 5529, coluna 5 em `coopCurrentInstruction()` — `if(record.type==='fishing'&&p.phase>=2)return\`Equipe: ${p.counter}/3 peixes assados. Vá à fogueira.\`;`
- [x] `return` — linha 5529, coluna 44 em `coopCurrentInstruction()` — `if(record.type==='fishing'&&p.phase>=2)return\`Equipe: ${p.counter}/3 peixes assados. Vá à fogueira.\`;`
- [x] `if` — linha 5530, coluna 5 em `coopCurrentInstruction()` — `if(record.type==='school')return\`Alunos levados: ${p.counter}/4. Convide um personagem e conduza-o até a escola.\`;`
- [x] `return` — linha 5530, coluna 31 em `coopCurrentInstruction()` — `if(record.type==='school')return\`Alunos levados: ${p.counter}/4. Convide um personagem e conduza-o até a escola.\`;`
- [x] `if` — linha 5531, coluna 5 em `coopCurrentInstruction()` — `if(record.type==='streetRace')return\`${record.mode==='competitive'?'Seus checkpoints':'Checkpoints da equipe'}: ${record.mode==='competitive'?coopIndividualCount(record,'street-checkpoint'):p.counter}/4. Siga a rota a pé.\`;`
- [x] `return` — linha 5531, coluna 35 em `coopCurrentInstruction()` — `if(record.type==='streetRace')return\`${record.mode==='competitive'?'Seus checkpoints':'Checkpoints da equipe'}: ${record.mode==='competitive'?coopIndividualCount(record,'street-checkpoint'):p.counter}/4. Siga a rota a pé.\`;`
- [x] `if` — linha 5532, coluna 5 em `coopCurrentInstruction()` — `if(record.type==='ovalRace')return\`${record.mode==='competitive'?'Suas voltas':'Voltas da equipe'}: ${record.mode==='competitive'?coopIndividualCount(record,'oval-lap'):p.counter}/3. Corra na pista oval.\`;`
- [x] `return` — linha 5532, coluna 33 em `coopCurrentInstruction()` — `if(record.type==='ovalRace')return\`${record.mode==='competitive'?'Suas voltas':'Voltas da equipe'}: ${record.mode==='competitive'?coopIndividualCount(record,'oval-lap'):p.counter}/3. Corra na pista oval.\`;`
- [x] `return` — linha 5533, coluna 5 em `coopCurrentInstruction()` — `return\`${step} • sua função: ${role}.\`;`
- [x] `if` — linha 5535, coluna 98 em `coopMissionProgressLabel()` — `function coopMissionProgressLabel(){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return null;const template=coopMissionTemplate(record.type),p=coopProgress(record),total=Math.max(1,template.steps.length),percent=record.status==='completed'?100:clamp((p.phase/total)*100,4,96);return{title:\`${template.icon} ${template.title}\`,label:coopCurrentInstruction(active,record),percent};}`
- [x] `return` — linha 5535, coluna 118 em `coopMissionProgressLabel()` — `function coopMissionProgressLabel(){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return null;const template=coopMissionTemplate(record.type),p=coopProgress(record),total=Math.max(1,template.steps.length),percent=record.status==='completed'?100:clamp((p.phase/total)*100,4,96);return{title:\`${template.icon} ${template.title}\`,label:coopCurrentInstruction(active,record),percent};}`
- [x] `return` — linha 5535, coluna 313 em `coopMissionProgressLabel()` — `function coopMissionProgressLabel(){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return null;const template=coopMissionTemplate(record.type),p=coopProgress(record),total=Math.max(1,template.steps.length),percent=record.status==='completed'?100:clamp((p.phase/total)*100,4,96);return{title:\`${template.icon} ${template.title}\`,label:coopCurrentInstruction(active,record),percent};}`
- [x] `if` — linha 5537, coluna 81 em `coopMissionBriefingMarkup()` — `const active=activeCoopMission(),template=coopMissionTemplate(record?.type);if(!active||!record||!template)return'';const p=coopProgress(record),participants=coopParticipants(record),own=coopOwnParticipant(record),steps=template.steps.map((text,index)=>\`<li class="${index<p.phase?'done':index===p.phase?'current':''}"><i>${index<p.phase?'✓':index+1}</i><span>${text}</span></li>\`).join(''),isRace=['streetRace','ovalRace'].includes(record.type),standings=isRace?coopRaceStandings(record):[],raceMarkup=isRace&&record.status==='active'?\`<div class="coop-race-standings"><b>CLASSIFICAÇÃO</b>${standings.map((item,index)=>\`<span><i>${index+1}º</i><strong>${item.name}</strong><em>${item.score} ${record.type==='ovalRace'?'voltas':'checkpoints'}${item.elapsed?\` • ${(item.elapsed/1000).toFixed(1)}s\`:''}</em></span>\`).join('')}</div>\`:'',fishMarkup=record.type==='fishing'?\`<div class="coop-individual-counter"><b>Seu contador</b><span>${p.phase<2?coopIndividualCount(record,'fish-caught'):coopIndividua`
- [x] `return` — linha 5537, coluna 112 em `coopMissionBriefingMarkup()` — `const active=activeCoopMission(),template=coopMissionTemplate(record?.type);if(!active||!record||!template)return'';const p=coopProgress(record),participants=coopParticipants(record),own=coopOwnParticipant(record),steps=template.steps.map((text,index)=>\`<li class="${index<p.phase?'done':index===p.phase?'current':''}"><i>${index<p.phase?'✓':index+1}</i><span>${text}</span></li>\`).join(''),isRace=['streetRace','ovalRace'].includes(record.type),standings=isRace?coopRaceStandings(record):[],raceMarkup=isRace&&record.status==='active'?\`<div class="coop-race-standings"><b>CLASSIFICAÇÃO</b>${standings.map((item,index)=>\`<span><i>${index+1}º</i><strong>${item.name}</strong><em>${item.score} ${record.type==='ovalRace'?'voltas':'checkpoints'}${item.elapsed?\` • ${(item.elapsed/1000).toFixed(1)}s\`:''}</em></span>\`).join('')}</div>\`:'',fishMarkup=record.type==='fishing'?\`<div class="coop-individual-counter"><b>Seu contador</b><span>${p.phase<2?coopIndividualCount(record,'fish-caught'):coopIndividua`
- [x] `return` — linha 5538, coluna 5 em `coopMissionBriefingMarkup()` — `return\`<section class="coop-briefing"><header><span>${template.icon}</span><div><small>MISSÃO ${record.mode==='solo'?'SOLO':record.mode==='competitive'?'COMPETITIVA':'COOPERATIVA'} • ${record.status.toUpperCase()}</small><h3>${template.title}</h3><p>${template.description}</p></div></header><div class="coop-team-strip">${participants.map(item=>\`<article class="${item.uid===coopUid()?'me':''}"><b>${item.name||'Jogador'}</b><span>${coopRoleLabel(item.role)}</span></article>\`).join('')}</div><div class="coop-role-now"><b>SUA FUNÇÃO</b><span>${coopRoleLabel(own?.role||active.role)}</span></div><div class="coop-vehicle-now"><b>VEÍCULO</b><span>${template.vehicle}</span></div><ol class="mission-step-list">${steps}</ol>${fishMarkup}${raceMarkup}<div class="mission-current-instruction">Agora: ${coopCurrentInstruction(active,record)}</div><div class="job-progress"><i style="width:${coopMissionProgressLabel()?.percent||0}%"></i></div></section>\`;`
- [x] `return` — linha 5540, coluna 43 em `coopPermissionDenied()` — `function coopPermissionDenied(error=''){return/permission|denied|permiss[aã]o/i.test(String(error||''));}`
- [x] `if` — linha 5541, coluna 84 em `coopPermissionDenied()` — `async function coopBackendCall(name,...args){const fn=window.OTTHOS_RTDB?.[name];if(typeof fn!=='function')return{ok:false,error:'O multiplayer cooperativo ainda não está pronto.'};try{return await fn(...args);}catch(error){return{ok:false,error:error?.message||String(error)}}}`
- [x] `return` — linha 5541, coluna 110 em `coopPermissionDenied()` — `async function coopBackendCall(name,...args){const fn=window.OTTHOS_RTDB?.[name];if(typeof fn!=='function')return{ok:false,error:'O multiplayer cooperativo ainda não está pronto.'};try{return await fn(...args);}catch(error){return{ok:false,error:error?.message||String(error)}}}`
- [x] `return` — linha 5541, coluna 188 em `coopPermissionDenied()` — `async function coopBackendCall(name,...args){const fn=window.OTTHOS_RTDB?.[name];if(typeof fn!=='function')return{ok:false,error:'O multiplayer cooperativo ainda não está pronto.'};try{return await fn(...args);}catch(error){return{ok:false,error:error?.message||String(error)}}}`
- [x] `catch` — linha 5541, coluna 214 em `coopPermissionDenied()` — `async function coopBackendCall(name,...args){const fn=window.OTTHOS_RTDB?.[name];if(typeof fn!=='function')return{ok:false,error:'O multiplayer cooperativo ainda não está pronto.'};try{return await fn(...args);}catch(error){return{ok:false,error:error?.message||String(error)}}}`
- [x] `return` — linha 5541, coluna 227 em `coopPermissionDenied()` — `async function coopBackendCall(name,...args){const fn=window.OTTHOS_RTDB?.[name];if(typeof fn!=='function')return{ok:false,error:'O multiplayer cooperativo ainda não está pronto.'};try{return await fn(...args);}catch(error){return{ok:false,error:error?.message||String(error)}}}`
- [x] `if` — linha 5542, coluna 49 em `bindCoopActionButton()` — `function bindCoopActionButton(button,handler){if(!button||typeof handler!=='function')return;button.addEventListener('click',async()=>{if(button.dataset.coopBusy==='1')return;button.dataset.coopBusy='1';button.disabled=true;try{await handler(button);}finally{if(button.isConnected){delete button.dataset.coopBusy;button.disabled=false;}}});}`
- [x] `return` — linha 5542, coluna 89 em `bindCoopActionButton()` — `function bindCoopActionButton(button,handler){if(!button||typeof handler!=='function')return;button.addEventListener('click',async()=>{if(button.dataset.coopBusy==='1')return;button.dataset.coopBusy='1';button.disabled=true;try{await handler(button);}finally{if(button.isConnected){delete button.dataset.coopBusy;button.disabled=false;}}});}`
- [x] `if` — linha 5542, coluna 138 em `bindCoopActionButton()` — `function bindCoopActionButton(button,handler){if(!button||typeof handler!=='function')return;button.addEventListener('click',async()=>{if(button.dataset.coopBusy==='1')return;button.dataset.coopBusy='1';button.disabled=true;try{await handler(button);}finally{if(button.isConnected){delete button.dataset.coopBusy;button.disabled=false;}}});}`
- [x] `return` — linha 5542, coluna 171 em `bindCoopActionButton()` — `function bindCoopActionButton(button,handler){if(!button||typeof handler!=='function')return;button.addEventListener('click',async()=>{if(button.dataset.coopBusy==='1')return;button.dataset.coopBusy='1';button.disabled=true;try{await handler(button);}finally{if(button.isConnected){delete button.dataset.coopBusy;button.disabled=false;}}});}`
- [x] `if` — linha 5542, coluna 262 em `bindCoopActionButton()` — `function bindCoopActionButton(button,handler){if(!button||typeof handler!=='function')return;button.addEventListener('click',async()=>{if(button.dataset.coopBusy==='1')return;button.dataset.coopBusy='1';button.disabled=true;try{await handler(button);}finally{if(button.isConnected){delete button.dataset.coopBusy;button.disabled=false;}}});}`
- [x] `return` — linha 5544, coluna 51 em `coopLocalRecord()` — `const id=\`local-${template.id}-${Date.now()}\`;return{id,type:template.id,title:template.title,icon:template.icon,hostUid:coopUid(),room:state.multiplayer?.room||'bairro-central',status:'lobby',mode,minPlayers:1,maxPlayers:template.maxPlayers,createdAtClient:Date.now(),updatedAtClient:Date.now(),participants:{[coopUid()]:{uid:coopUid(),name:publicPlayerName(),role,ready:true,joinedAtClient:Date.now(),updatedAtClient:Date.now()}},progress:{phase:0,counter:0,target:1,events:{},updatedAtClient:Date.now()}};`
- [x] `return` — linha 5546, coluna 49 em `coopActiveState()` — `function coopActiveState(record,remote=false){return{id:record.id,type:record.type,role:record.participants?.[coopUid()]?.role||'helper',remote,record,createdAt:Date.now(),baselineRawFish:Number(state.inventory.rawFish||0),baselineCookedFish:Number(state.inventory.cookedFish||0),invitedNpcIds:[],followingNpcIds:[],deliveredNpcIds:[],previousUniform:state.avatar.uniform||'none',serviceJob:null,sceneTarget:null,race:{startedAt:0,checkpoints:0,lastCheckpoint:-1,lastAngle:null,angleTravel:0,laps:0}};}`
- [x] `if` — linha 5548, coluna 30 em `coopActiveState()` — `ensureCooperativeState();if(state.cooperative.active){toast('Conclua ou saia da missão cooperativa atual.','warn');return false;}if(state.career?.activeJob){toast('Conclua ou cancele o trabalho atual antes de iniciar uma missão cooperativa.','warn',2600);return false;}const template=coopMissionTemplate(templateId);if(!template)return false;const role=template.roles[0][0];let record,remote=false;`
- [x] `return` — linha 5548, coluna 120 em `coopActiveState()` — `ensureCooperativeState();if(state.cooperative.active){toast('Conclua ou saia da missão cooperativa atual.','warn');return false;}if(state.career?.activeJob){toast('Conclua ou cancele o trabalho atual antes de iniciar uma missão cooperativa.','warn',2600);return false;}const template=coopMissionTemplate(templateId);if(!template)return false;const role=template.roles[0][0];let record,remote=false;`
- [x] `if` — linha 5548, coluna 134 em `coopActiveState()` — `ensureCooperativeState();if(state.cooperative.active){toast('Conclua ou saia da missão cooperativa atual.','warn');return false;}if(state.career?.activeJob){toast('Conclua ou cancele o trabalho atual antes de iniciar uma missão cooperativa.','warn',2600);return false;}const template=coopMissionTemplate(templateId);if(!template)return false;const role=template.roles[0][0];let record,remote=false;`
- [x] `return` — linha 5548, coluna 260 em `coopActiveState()` — `ensureCooperativeState();if(state.cooperative.active){toast('Conclua ou saia da missão cooperativa atual.','warn');return false;}if(state.career?.activeJob){toast('Conclua ou cancele o trabalho atual antes de iniciar uma missão cooperativa.','warn',2600);return false;}const template=coopMissionTemplate(templateId);if(!template)return false;const role=template.roles[0][0];let record,remote=false;`
- [x] `if` — linha 5548, coluna 321 em `coopActiveState()` — `ensureCooperativeState();if(state.cooperative.active){toast('Conclua ou saia da missão cooperativa atual.','warn');return false;}if(state.career?.activeJob){toast('Conclua ou cancele o trabalho atual antes de iniciar uma missão cooperativa.','warn',2600);return false;}const template=coopMissionTemplate(templateId);if(!template)return false;const role=template.roles[0][0];let record,remote=false;`
- [x] `return` — linha 5548, coluna 334 em `coopActiveState()` — `ensureCooperativeState();if(state.cooperative.active){toast('Conclua ou saia da missão cooperativa atual.','warn');return false;}if(state.career?.activeJob){toast('Conclua ou cancele o trabalho atual antes de iniciar uma missão cooperativa.','warn',2600);return false;}const template=coopMissionTemplate(templateId);if(!template)return false;const role=template.roles[0][0];let record,remote=false;`
- [x] `if` — linha 5549, coluna 5 em `coopActiveState()` — `if(mode!=='solo'&&window.OTTHOS_RTDB?.connected?.()){const target=template.id==='fishing'?6:template.id==='school'?4:template.id==='streetRace'?4:template.id==='ovalRace'?3:template.id==='firefighter'?4:1,result=await coopBackendCall('createCoopMission',template.id,{title:template.title,icon:template.icon,minPlayers:template.minPlayers,maxPlayers:template.maxPlayers,role,target,mode});if(!result?.ok){if(coopPermissionDenied(result?.error)){record=coopLocalRecord(template,'solo',role);toast('O servidor ainda usa regras antigas. A atividade foi aberta no modo solo para nenhum botão ficar bloqueado.','warn',4200);}else{toast(result?.error||'Não foi possível criar a equipe.','warn',2800);return false;}}else{record=result.mission;record.id=result.id;remote=true;coopRemoteMissions[result.id]=record;}}else{record=coopLocalRecord(template,'solo',role);}`
- [x] `if` — linha 5549, coluna 393 em `coopActiveState()` — `if(mode!=='solo'&&window.OTTHOS_RTDB?.connected?.()){const target=template.id==='fishing'?6:template.id==='school'?4:template.id==='streetRace'?4:template.id==='ovalRace'?3:template.id==='firefighter'?4:1,result=await coopBackendCall('createCoopMission',template.id,{title:template.title,icon:template.icon,minPlayers:template.minPlayers,maxPlayers:template.maxPlayers,role,target,mode});if(!result?.ok){if(coopPermissionDenied(result?.error)){record=coopLocalRecord(template,'solo',role);toast('O servidor ainda usa regras antigas. A atividade foi aberta no modo solo para nenhum botão ficar bloqueado.','warn',4200);}else{toast(result?.error||'Não foi possível criar a equipe.','warn',2800);return false;}}else{record=result.mission;record.id=result.id;remote=true;coopRemoteMissions[result.id]=record;}}else{record=coopLocalRecord(template,'solo',role);}`
- [x] `if` — linha 5549, coluna 409 em `coopActiveState()` — `if(mode!=='solo'&&window.OTTHOS_RTDB?.connected?.()){const target=template.id==='fishing'?6:template.id==='school'?4:template.id==='streetRace'?4:template.id==='ovalRace'?3:template.id==='firefighter'?4:1,result=await coopBackendCall('createCoopMission',template.id,{title:template.title,icon:template.icon,minPlayers:template.minPlayers,maxPlayers:template.maxPlayers,role,target,mode});if(!result?.ok){if(coopPermissionDenied(result?.error)){record=coopLocalRecord(template,'solo',role);toast('O servidor ainda usa regras antigas. A atividade foi aberta no modo solo para nenhum botão ficar bloqueado.','warn',4200);}else{toast(result?.error||'Não foi possível criar a equipe.','warn',2800);return false;}}else{record=result.mission;record.id=result.id;remote=true;coopRemoteMissions[result.id]=record;}}else{record=coopLocalRecord(template,'solo',role);}`
- [x] `else` — linha 5549, coluna 624 em `coopActiveState()` — `if(mode!=='solo'&&window.OTTHOS_RTDB?.connected?.()){const target=template.id==='fishing'?6:template.id==='school'?4:template.id==='streetRace'?4:template.id==='ovalRace'?3:template.id==='firefighter'?4:1,result=await coopBackendCall('createCoopMission',template.id,{title:template.title,icon:template.icon,minPlayers:template.minPlayers,maxPlayers:template.maxPlayers,role,target,mode});if(!result?.ok){if(coopPermissionDenied(result?.error)){record=coopLocalRecord(template,'solo',role);toast('O servidor ainda usa regras antigas. A atividade foi aberta no modo solo para nenhum botão ficar bloqueado.','warn',4200);}else{toast(result?.error||'Não foi possível criar a equipe.','warn',2800);return false;}}else{record=result.mission;record.id=result.id;remote=true;coopRemoteMissions[result.id]=record;}}else{record=coopLocalRecord(template,'solo',role);}`
- [x] `return` — linha 5549, coluna 698 em `coopActiveState()` — `if(mode!=='solo'&&window.OTTHOS_RTDB?.connected?.()){const target=template.id==='fishing'?6:template.id==='school'?4:template.id==='streetRace'?4:template.id==='ovalRace'?3:template.id==='firefighter'?4:1,result=await coopBackendCall('createCoopMission',template.id,{title:template.title,icon:template.icon,minPlayers:template.minPlayers,maxPlayers:template.maxPlayers,role,target,mode});if(!result?.ok){if(coopPermissionDenied(result?.error)){record=coopLocalRecord(template,'solo',role);toast('O servidor ainda usa regras antigas. A atividade foi aberta no modo solo para nenhum botão ficar bloqueado.','warn',4200);}else{toast(result?.error||'Não foi possível criar a equipe.','warn',2800);return false;}}else{record=result.mission;record.id=result.id;remote=true;coopRemoteMissions[result.id]=record;}}else{record=coopLocalRecord(template,'solo',role);}`
- [x] `else` — linha 5549, coluna 713 em `coopActiveState()` — `if(mode!=='solo'&&window.OTTHOS_RTDB?.connected?.()){const target=template.id==='fishing'?6:template.id==='school'?4:template.id==='streetRace'?4:template.id==='ovalRace'?3:template.id==='firefighter'?4:1,result=await coopBackendCall('createCoopMission',template.id,{title:template.title,icon:template.icon,minPlayers:template.minPlayers,maxPlayers:template.maxPlayers,role,target,mode});if(!result?.ok){if(coopPermissionDenied(result?.error)){record=coopLocalRecord(template,'solo',role);toast('O servidor ainda usa regras antigas. A atividade foi aberta no modo solo para nenhum botão ficar bloqueado.','warn',4200);}else{toast(result?.error||'Não foi possível criar a equipe.','warn',2800);return false;}}else{record=result.mission;record.id=result.id;remote=true;coopRemoteMissions[result.id]=record;}}else{record=coopLocalRecord(template,'solo',role);}`
- [x] `else` — linha 5549, coluna 811 em `coopActiveState()` — `if(mode!=='solo'&&window.OTTHOS_RTDB?.connected?.()){const target=template.id==='fishing'?6:template.id==='school'?4:template.id==='streetRace'?4:template.id==='ovalRace'?3:template.id==='firefighter'?4:1,result=await coopBackendCall('createCoopMission',template.id,{title:template.title,icon:template.icon,minPlayers:template.minPlayers,maxPlayers:template.maxPlayers,role,target,mode});if(!result?.ok){if(coopPermissionDenied(result?.error)){record=coopLocalRecord(template,'solo',role);toast('O servidor ainda usa regras antigas. A atividade foi aberta no modo solo para nenhum botão ficar bloqueado.','warn',4200);}else{toast(result?.error||'Não foi possível criar a equipe.','warn',2800);return false;}}else{record=result.mission;record.id=result.id;remote=true;coopRemoteMissions[result.id]=record;}}else{record=coopLocalRecord(template,'solo',role);}`
- [x] `return` — linha 5550, coluna 102 em `coopActiveState()` — `state.cooperative.active=coopActiveState(record,remote);saveState(true);openCoopLobby(record.id);return true;`
- [x] `if` — linha 5553, coluna 30 em `coopActiveState()` — `ensureCooperativeState();if(state.career?.activeJob||state.cooperative.active){toast('Finalize a missão atual antes de entrar em outra equipe.','warn');return false;}const result=await coopBackendCall('joinCoopMission',id,role);if(!result?.ok){if(coopPermissionDenied(result?.error)){const remote=coopRemoteMissions[id],template=coopMissionTemplate(remote?.type);if(template){toast('A entrada online foi recusada pelas regras antigas. Abrindo a versão solo equivalente.','warn',3800);return createCooperativeMission(template.id,'solo');}}toast(result?.error||'Não foi possível entrar na equipe.','warn');return false;}const record=result.mission;coopRemoteMissions[id]=record;state.cooperative.active=coopActiveState(record,true);saveState(true);openCoopLobby(id);return true;`
- [x] `return` — linha 5553, coluna 157 em `coopActiveState()` — `ensureCooperativeState();if(state.career?.activeJob||state.cooperative.active){toast('Finalize a missão atual antes de entrar em outra equipe.','warn');return false;}const result=await coopBackendCall('joinCoopMission',id,role);if(!result?.ok){if(coopPermissionDenied(result?.error)){const remote=coopRemoteMissions[id],template=coopMissionTemplate(remote?.type);if(template){toast('A entrada online foi recusada pelas regras antigas. Abrindo a versão solo equivalente.','warn',3800);return createCooperativeMission(template.id,'solo');}}toast(result?.error||'Não foi possível entrar na equipe.','warn');return false;}const record=result.mission;coopRemoteMissions[id]=record;state.cooperative.active=coopActiveState(record,true);saveState(true);openCoopLobby(id);return true;`
- [x] `if` — linha 5553, coluna 233 em `coopActiveState()` — `ensureCooperativeState();if(state.career?.activeJob||state.cooperative.active){toast('Finalize a missão atual antes de entrar em outra equipe.','warn');return false;}const result=await coopBackendCall('joinCoopMission',id,role);if(!result?.ok){if(coopPermissionDenied(result?.error)){const remote=coopRemoteMissions[id],template=coopMissionTemplate(remote?.type);if(template){toast('A entrada online foi recusada pelas regras antigas. Abrindo a versão solo equivalente.','warn',3800);return createCooperativeMission(template.id,'solo');}}toast(result?.error||'Não foi possível entrar na equipe.','warn');return false;}const record=result.mission;coopRemoteMissions[id]=record;state.cooperative.active=coopActiveState(record,true);saveState(true);openCoopLobby(id);return true;`
- [x] `if` — linha 5553, coluna 249 em `coopActiveState()` — `ensureCooperativeState();if(state.career?.activeJob||state.cooperative.active){toast('Finalize a missão atual antes de entrar em outra equipe.','warn');return false;}const result=await coopBackendCall('joinCoopMission',id,role);if(!result?.ok){if(coopPermissionDenied(result?.error)){const remote=coopRemoteMissions[id],template=coopMissionTemplate(remote?.type);if(template){toast('A entrada online foi recusada pelas regras antigas. Abrindo a versão solo equivalente.','warn',3800);return createCooperativeMission(template.id,'solo');}}toast(result?.error||'Não foi possível entrar na equipe.','warn');return false;}const record=result.mission;coopRemoteMissions[id]=record;state.cooperative.active=coopActiveState(record,true);saveState(true);openCoopLobby(id);return true;`
- [x] `if` — linha 5553, coluna 368 em `coopActiveState()` — `ensureCooperativeState();if(state.career?.activeJob||state.cooperative.active){toast('Finalize a missão atual antes de entrar em outra equipe.','warn');return false;}const result=await coopBackendCall('joinCoopMission',id,role);if(!result?.ok){if(coopPermissionDenied(result?.error)){const remote=coopRemoteMissions[id],template=coopMissionTemplate(remote?.type);if(template){toast('A entrada online foi recusada pelas regras antigas. Abrindo a versão solo equivalente.','warn',3800);return createCooperativeMission(template.id,'solo');}}toast(result?.error||'Não foi possível entrar na equipe.','warn');return false;}const record=result.mission;coopRemoteMissions[id]=record;state.cooperative.active=coopActiveState(record,true);saveState(true);openCoopLobby(id);return true;`
- [x] `return` — linha 5553, coluna 489 em `coopActiveState()` — `ensureCooperativeState();if(state.career?.activeJob||state.cooperative.active){toast('Finalize a missão atual antes de entrar em outra equipe.','warn');return false;}const result=await coopBackendCall('joinCoopMission',id,role);if(!result?.ok){if(coopPermissionDenied(result?.error)){const remote=coopRemoteMissions[id],template=coopMissionTemplate(remote?.type);if(template){toast('A entrada online foi recusada pelas regras antigas. Abrindo a versão solo equivalente.','warn',3800);return createCooperativeMission(template.id,'solo');}}toast(result?.error||'Não foi possível entrar na equipe.','warn');return false;}const record=result.mission;coopRemoteMissions[id]=record;state.cooperative.active=coopActiveState(record,true);saveState(true);openCoopLobby(id);return true;`
- [x] `return` — linha 5553, coluna 609 em `coopActiveState()` — `ensureCooperativeState();if(state.career?.activeJob||state.cooperative.active){toast('Finalize a missão atual antes de entrar em outra equipe.','warn');return false;}const result=await coopBackendCall('joinCoopMission',id,role);if(!result?.ok){if(coopPermissionDenied(result?.error)){const remote=coopRemoteMissions[id],template=coopMissionTemplate(remote?.type);if(template){toast('A entrada online foi recusada pelas regras antigas. Abrindo a versão solo equivalente.','warn',3800);return createCooperativeMission(template.id,'solo');}}toast(result?.error||'Não foi possível entrar na equipe.','warn');return false;}const record=result.mission;coopRemoteMissions[id]=record;state.cooperative.active=coopActiveState(record,true);saveState(true);openCoopLobby(id);return true;`
- [x] `return` — linha 5553, coluna 769 em `coopActiveState()` — `ensureCooperativeState();if(state.career?.activeJob||state.cooperative.active){toast('Finalize a missão atual antes de entrar em outra equipe.','warn');return false;}const result=await coopBackendCall('joinCoopMission',id,role);if(!result?.ok){if(coopPermissionDenied(result?.error)){const remote=coopRemoteMissions[id],template=coopMissionTemplate(remote?.type);if(template){toast('A entrada online foi recusada pelas regras antigas. Abrindo a versão solo equivalente.','warn',3800);return createCooperativeMission(template.id,'solo');}}toast(result?.error||'Não foi possível entrar na equipe.','warn');return false;}const record=result.mission;coopRemoteMissions[id]=record;state.cooperative.active=coopActiveState(record,true);saveState(true);openCoopLobby(id);return true;`
- [x] `if` — linha 5555, coluna 141 em `coopActiveState()` — `async function selectCoopRole(role){const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type);if(!active||!template||!template.roles.some(item=>item[0]===role))return false;if(coopRoleTaken(record,role)>=coopRoleCapacity(template,role)){toast('Essa função já está ocupada. Escolha outra função da equipe.','warn');return false;}if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{role});if(!result?.ok){toast(result?.error||'Não foi possível trocar a função.','warn');return false;}if(record.participants?.[coopUid()])record.participants[coopUid()].role=role;}else active.record.participants[coopUid()].role=role;active.role=role;saveState(true);openCoopLobby(active.id);return true;}`
- [x] `return` — linha 5555, coluna 207 em `coopActiveState()` — `async function selectCoopRole(role){const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type);if(!active||!template||!template.roles.some(item=>item[0]===role))return false;if(coopRoleTaken(record,role)>=coopRoleCapacity(template,role)){toast('Essa função já está ocupada. Escolha outra função da equipe.','warn');return false;}if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{role});if(!result?.ok){toast(result?.error||'Não foi possível trocar a função.','warn');return false;}if(record.participants?.[coopUid()])record.participants[coopUid()].role=role;}else active.record.participants[coopUid()].role=role;active.role=role;saveState(true);openCoopLobby(active.id);return true;}`
- [x] `if` — linha 5555, coluna 220 em `coopActiveState()` — `async function selectCoopRole(role){const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type);if(!active||!template||!template.roles.some(item=>item[0]===role))return false;if(coopRoleTaken(record,role)>=coopRoleCapacity(template,role)){toast('Essa função já está ocupada. Escolha outra função da equipe.','warn');return false;}if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{role});if(!result?.ok){toast(result?.error||'Não foi possível trocar a função.','warn');return false;}if(record.participants?.[coopUid()])record.participants[coopUid()].role=role;}else active.record.participants[coopUid()].role=role;active.role=role;saveState(true);openCoopLobby(active.id);return true;}`
- [x] `return` — linha 5555, coluna 361 em `coopActiveState()` — `async function selectCoopRole(role){const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type);if(!active||!template||!template.roles.some(item=>item[0]===role))return false;if(coopRoleTaken(record,role)>=coopRoleCapacity(template,role)){toast('Essa função já está ocupada. Escolha outra função da equipe.','warn');return false;}if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{role});if(!result?.ok){toast(result?.error||'Não foi possível trocar a função.','warn');return false;}if(record.participants?.[coopUid()])record.participants[coopUid()].role=role;}else active.record.participants[coopUid()].role=role;active.role=role;saveState(true);openCoopLobby(active.id);return true;}`
- [x] `if` — linha 5555, coluna 375 em `coopActiveState()` — `async function selectCoopRole(role){const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type);if(!active||!template||!template.roles.some(item=>item[0]===role))return false;if(coopRoleTaken(record,role)>=coopRoleCapacity(template,role)){toast('Essa função já está ocupada. Escolha outra função da equipe.','warn');return false;}if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{role});if(!result?.ok){toast(result?.error||'Não foi possível trocar a função.','warn');return false;}if(record.participants?.[coopUid()])record.participants[coopUid()].role=role;}else active.record.participants[coopUid()].role=role;active.role=role;saveState(true);openCoopLobby(active.id);return true;}`
- [x] `if` — linha 5555, coluna 470 em `coopActiveState()` — `async function selectCoopRole(role){const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type);if(!active||!template||!template.roles.some(item=>item[0]===role))return false;if(coopRoleTaken(record,role)>=coopRoleCapacity(template,role)){toast('Essa função já está ocupada. Escolha outra função da equipe.','warn');return false;}if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{role});if(!result?.ok){toast(result?.error||'Não foi possível trocar a função.','warn');return false;}if(record.participants?.[coopUid()])record.participants[coopUid()].role=role;}else active.record.participants[coopUid()].role=role;active.role=role;saveState(true);openCoopLobby(active.id);return true;}`
- [x] `return` — linha 5555, coluna 551 em `coopActiveState()` — `async function selectCoopRole(role){const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type);if(!active||!template||!template.roles.some(item=>item[0]===role))return false;if(coopRoleTaken(record,role)>=coopRoleCapacity(template,role)){toast('Essa função já está ocupada. Escolha outra função da equipe.','warn');return false;}if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{role});if(!result?.ok){toast(result?.error||'Não foi possível trocar a função.','warn');return false;}if(record.participants?.[coopUid()])record.participants[coopUid()].role=role;}else active.record.participants[coopUid()].role=role;active.role=role;saveState(true);openCoopLobby(active.id);return true;}`
- [x] `if` — linha 5555, coluna 565 em `coopActiveState()` — `async function selectCoopRole(role){const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type);if(!active||!template||!template.roles.some(item=>item[0]===role))return false;if(coopRoleTaken(record,role)>=coopRoleCapacity(template,role)){toast('Essa função já está ocupada. Escolha outra função da equipe.','warn');return false;}if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{role});if(!result?.ok){toast(result?.error||'Não foi possível trocar a função.','warn');return false;}if(record.participants?.[coopUid()])record.participants[coopUid()].role=role;}else active.record.participants[coopUid()].role=role;active.role=role;saveState(true);openCoopLobby(active.id);return true;}`
- [x] `else` — linha 5555, coluna 643 em `coopActiveState()` — `async function selectCoopRole(role){const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type);if(!active||!template||!template.roles.some(item=>item[0]===role))return false;if(coopRoleTaken(record,role)>=coopRoleCapacity(template,role)){toast('Essa função já está ocupada. Escolha outra função da equipe.','warn');return false;}if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{role});if(!result?.ok){toast(result?.error||'Não foi possível trocar a função.','warn');return false;}if(record.participants?.[coopUid()])record.participants[coopUid()].role=role;}else active.record.participants[coopUid()].role=role;active.role=role;saveState(true);openCoopLobby(active.id);return true;}`
- [x] `return` — linha 5555, coluna 754 em `coopActiveState()` — `async function selectCoopRole(role){const active=activeCoopMission(),record=activeCoopRecord(),template=coopMissionTemplate(record?.type);if(!active||!template||!template.roles.some(item=>item[0]===role))return false;if(coopRoleTaken(record,role)>=coopRoleCapacity(template,role)){toast('Essa função já está ocupada. Escolha outra função da equipe.','warn');return false;}if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{role});if(!result?.ok){toast(result?.error||'Não foi possível trocar a função.','warn');return false;}if(record.participants?.[coopUid()])record.participants[coopUid()].role=role;}else active.record.participants[coopUid()].role=role;active.role=role;saveState(true);openCoopLobby(active.id);return true;}`
- [x] `if` — linha 5556, coluna 126 em `coopActiveState()` — `async function toggleCoopReady(){const active=activeCoopMission(),record=activeCoopRecord(),own=coopOwnParticipant(record);if(!active||!own)return false;const ready=!own.ready;if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{ready});if(!result?.ok){toast(result?.error||'Não foi possível atualizar o estado da equipe.','warn');return false;}own.ready=ready;}else active.record.participants[coopUid()].ready=ready;openCoopLobby(active.id);return true;}`
- [x] `return` — linha 5556, coluna 143 em `coopActiveState()` — `async function toggleCoopReady(){const active=activeCoopMission(),record=activeCoopRecord(),own=coopOwnParticipant(record);if(!active||!own)return false;const ready=!own.ready;if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{ready});if(!result?.ok){toast(result?.error||'Não foi possível atualizar o estado da equipe.','warn');return false;}own.ready=ready;}else active.record.participants[coopUid()].ready=ready;openCoopLobby(active.id);return true;}`
- [x] `if` — linha 5556, coluna 179 em `coopActiveState()` — `async function toggleCoopReady(){const active=activeCoopMission(),record=activeCoopRecord(),own=coopOwnParticipant(record);if(!active||!own)return false;const ready=!own.ready;if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{ready});if(!result?.ok){toast(result?.error||'Não foi possível atualizar o estado da equipe.','warn');return false;}own.ready=ready;}else active.record.participants[coopUid()].ready=ready;openCoopLobby(active.id);return true;}`
- [x] `if` — linha 5556, coluna 275 em `coopActiveState()` — `async function toggleCoopReady(){const active=activeCoopMission(),record=activeCoopRecord(),own=coopOwnParticipant(record);if(!active||!own)return false;const ready=!own.ready;if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{ready});if(!result?.ok){toast(result?.error||'Não foi possível atualizar o estado da equipe.','warn');return false;}own.ready=ready;}else active.record.participants[coopUid()].ready=ready;openCoopLobby(active.id);return true;}`
- [x] `return` — linha 5556, coluna 369 em `coopActiveState()` — `async function toggleCoopReady(){const active=activeCoopMission(),record=activeCoopRecord(),own=coopOwnParticipant(record);if(!active||!own)return false;const ready=!own.ready;if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{ready});if(!result?.ok){toast(result?.error||'Não foi possível atualizar o estado da equipe.','warn');return false;}own.ready=ready;}else active.record.participants[coopUid()].ready=ready;openCoopLobby(active.id);return true;}`
- [x] `else` — linha 5556, coluna 400 em `coopActiveState()` — `async function toggleCoopReady(){const active=activeCoopMission(),record=activeCoopRecord(),own=coopOwnParticipant(record);if(!active||!own)return false;const ready=!own.ready;if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{ready});if(!result?.ok){toast(result?.error||'Não foi possível atualizar o estado da equipe.','warn');return false;}own.ready=ready;}else active.record.participants[coopUid()].ready=ready;openCoopLobby(active.id);return true;}`
- [x] `return` — linha 5556, coluna 480 em `coopActiveState()` — `async function toggleCoopReady(){const active=activeCoopMission(),record=activeCoopRecord(),own=coopOwnParticipant(record);if(!active||!own)return false;const ready=!own.ready;if(active.remote){const result=await coopBackendCall('updateCoopParticipant',active.id,{ready});if(!result?.ok){toast(result?.error||'Não foi possível atualizar o estado da equipe.','warn');return false;}own.ready=ready;}else active.record.participants[coopUid()].ready=ready;openCoopLobby(active.id);return true;}`
- [x] `if` — linha 5557, coluna 117 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `return` — linha 5557, coluna 137 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `if` — linha 5557, coluna 228 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `return` — linha 5557, coluna 308 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `if` — linha 5557, coluna 322 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `return` — linha 5557, coluna 525 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `if` — linha 5557, coluna 539 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `return` — linha 5557, coluna 685 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `if` — linha 5557, coluna 780 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `if` — linha 5557, coluna 892 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `if` — linha 5557, coluna 908 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `return` — linha 5557, coluna 1053 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `return` — linha 5557, coluna 1147 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `else` — linha 5557, coluna 1222 em `coopActiveState()` — `async function startActiveCoopMission(forceSolo=false){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const participants=coopParticipants(record),isHost=record.hostUid===coopUid();if(!isHost&&active.remote){toast('Aguarde quem criou a equipe iniciar.','warn');return false;}if(!forceSolo&&participants.length<Number(record.minPlayers||1)){toast(\`Esta missão foi planejada para pelo menos ${record.minPlayers} jogadores. Use “Iniciar solo” para a versão adaptada.\`,'warn',3200);return false;}if(!forceSolo&&participants.some(item=>!item.ready)){toast('Todos os participantes precisam marcar “Estou pronto” antes da largada.','warn',2800);return false;}const nextMode=forceSolo?'solo':record.mode==='competitive'?'competitive':'coop';if(active.remote){const result=await coopBackendCall('setCoopMissionStatus',active.id,'active',{mode:nextMode});if(!result?.ok){if(coopPermissionDenied(result?.error)){toast('As regras antigas impediram a largada online. Co`
- [x] `if` — linha 5558, coluna 5 em `coopActiveState()` — `if(forceSolo){record.mode='solo';if(!active.remote)active.record.mode='solo';}`
- [x] `if` — linha 5558, coluna 38 em `coopActiveState()` — `if(forceSolo){record.mode='solo';if(!active.remote)active.record.mode='solo';}`
- [x] `return` — linha 5559, coluna 145 em `coopActiveState()` — `prepareCoopMissionRuntime();closeModal();updateMissionHUD();toast(\`Missão iniciada: ${coopMissionTemplate(record.type).title}\`,'good',2800);return true;`
- [x] `if` — linha 5562, coluna 64 em `continueCoopMissionOffline()` — `const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||!active.remote)return false;`
- [x] `return` — linha 5562, coluna 100 em `continueCoopMissionOffline()` — `const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||!active.remote)return false;`
- [x] `return` — linha 5564, coluna 297 em `continueCoopMissionOffline()` — `active.id=copy.id;active.remote=false;active.record=copy;active.role=own.role||active.role;active.runtimePrepared=false;prepareCoopMissionRuntime();saveState(true);closeModal();updateMissionHUD();toast('Missão retomada no modo solo. O progresso online original não foi apagado.','good',3200);return true;`
- [x] `if` — linha 5567, coluna 64 em `prepareCoopMissionRuntime()` — `const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||record.status!=='active')return;if(!active.previousUniform)active.previousUniform=state.avatar.uniform||'none';const template=coopMissionTemplate(record.type),kind=coopServiceKind(record.type);`
- [x] `return` — linha 5567, coluna 110 em `prepareCoopMissionRuntime()` — `const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||record.status!=='active')return;if(!active.previousUniform)active.previousUniform=state.avatar.uniform||'none';const template=coopMissionTemplate(record.type),kind=coopServiceKind(record.type);`
- [x] `if` — linha 5567, coluna 117 em `prepareCoopMissionRuntime()` — `const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||record.status!=='active')return;if(!active.previousUniform)active.previousUniform=state.avatar.uniform||'none';const template=coopMissionTemplate(record.type),kind=coopServiceKind(record.type);`
- [x] `if` — linha 5568, coluna 5 em `prepareCoopMissionRuntime()` — `if(kind){active.serviceJob=active.serviceJob||{id:kind,title:template.title,icon:template.icon,instanceId:\`coop-${active.id}\`,cooperative:true,serviceVehicleId:'',serviceVehicleKind:kind,serviceVehicleBoarded:false,serviceVehicleArrived:false};reserveMissionServiceVehicle(active.serviceJob,{waypoint:true});active.sceneTarget=active.sceneTarget||coopSceneTarget(kind);equipJobUniform(kind);}`
- [x] `else` — linha 5569, coluna 5 em `prepareCoopMissionRuntime()` — `else if(record.type==='school')equipJobUniform('teacher');`
- [x] `if` — linha 5569, coluna 10 em `prepareCoopMissionRuntime()` — `else if(record.type==='school')equipJobUniform('teacher');`
- [x] `else` — linha 5570, coluna 5 em `prepareCoopMissionRuntime()` — `else if(record.type==='streetRace'||record.type==='ovalRace'){state.avatar.uniform='sport';applyAvatarCustomization();active.race=active.race||{};active.race.startedAt=active.race.startedAt||Date.now();activateCoopRaceBots(record.type);}`
- [x] `if` — linha 5570, coluna 10 em `prepareCoopMissionRuntime()` — `else if(record.type==='streetRace'||record.type==='ovalRace'){state.avatar.uniform='sport';applyAvatarCustomization();active.race=active.race||{};active.race.startedAt=active.race.startedAt||Date.now();activateCoopRaceBots(record.type);}`
- [x] `if` — linha 5571, coluna 51 em `prepareCoopMissionRuntime()` — `const target=coopCurrentTarget(active,record);if(target)coopSetWaypoint(target);saveState(true);`
- [x] `if` — linha 5573, coluna 36 em `coopSetWaypoint()` — `function coopSetWaypoint(target){if(!target)return;state.waypoint={id:\`coop-${target.id||'target'}\`,name:target.name||'Objetivo cooperativo',x:Number(target.x||0),z:Number(target.z||0),navX:Number(target.navX??target.x??0),navZ:Number(target.navZ??target.z??0),arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);}`
- [x] `return` — linha 5573, coluna 47 em `coopSetWaypoint()` — `function coopSetWaypoint(target){if(!target)return;state.waypoint={id:\`coop-${target.id||'target'}\`,name:target.name||'Objetivo cooperativo',x:Number(target.x||0),z:Number(target.z||0),navX:Number(target.navX??target.x??0),navZ:Number(target.navZ??target.z??0),arrived:false};world.routePath=buildRoutePoints(player,state.waypoint);updateWaypointMarker();updateNavigation(0,true);}`
- [x] `if` — linha 5574, coluna 112 em `onCoopServiceVehicleBoarded()` — `function onCoopServiceVehicleBoarded(vehicle,job){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||active.serviceJob!==job)return;job.serviceVehicleBoarded=true;coopRecordEvent('driver-boarded','driver',1,1);active.sceneTarget=active.sceneTarget||coopSceneTarget(record.type);if(active.sceneTarget)coopSetWaypoint(active.sceneTarget);toast('Veículo correto. Leve a equipe ao ponto marcado.','good',2500);}`
- [x] `return` — linha 5574, coluna 157 em `onCoopServiceVehicleBoarded()` — `function onCoopServiceVehicleBoarded(vehicle,job){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||active.serviceJob!==job)return;job.serviceVehicleBoarded=true;coopRecordEvent('driver-boarded','driver',1,1);active.sceneTarget=active.sceneTarget||coopSceneTarget(record.type);if(active.sceneTarget)coopSetWaypoint(active.sceneTarget);toast('Veículo correto. Leve a equipe ao ponto marcado.','good',2500);}`
- [x] `if` — linha 5574, coluna 310 em `onCoopServiceVehicleBoarded()` — `function onCoopServiceVehicleBoarded(vehicle,job){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||active.serviceJob!==job)return;job.serviceVehicleBoarded=true;coopRecordEvent('driver-boarded','driver',1,1);active.sceneTarget=active.sceneTarget||coopSceneTarget(record.type);if(active.sceneTarget)coopSetWaypoint(active.sceneTarget);toast('Veículo correto. Leve a equipe ao ponto marcado.','good',2500);}`
- [x] `if` — linha 5575, coluna 130 em `onCoopServiceVehicleBoarded()` — `async function coopRecordEvent(kind,role='',value=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const id=\`${coopUid().replace(/[^A-Za-z0-9_-]/g,'').slice(-24)}-${kind}-${Math.floor(Date.now()/1200)}\`;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:id,kind,value,phase:nextPhase});return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.events[id]={uid:coopUid(),role:role||active.role,kind,value,createdAtClient:Date.now()};if(Number.isFinite(Number(nextPhase)))progress.phase=Math.max(progress.phase,Number(nextPhase));progress.updatedAtClient=Date.now();return true;}`
- [x] `return` — linha 5575, coluna 150 em `onCoopServiceVehicleBoarded()` — `async function coopRecordEvent(kind,role='',value=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const id=\`${coopUid().replace(/[^A-Za-z0-9_-]/g,'').slice(-24)}-${kind}-${Math.floor(Date.now()/1200)}\`;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:id,kind,value,phase:nextPhase});return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.events[id]={uid:coopUid(),role:role||active.role,kind,value,createdAtClient:Date.now()};if(Number.isFinite(Number(nextPhase)))progress.phase=Math.max(progress.phase,Number(nextPhase));progress.updatedAtClient=Date.now();return true;}`
- [x] `if` — linha 5575, coluna 267 em `onCoopServiceVehicleBoarded()` — `async function coopRecordEvent(kind,role='',value=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const id=\`${coopUid().replace(/[^A-Za-z0-9_-]/g,'').slice(-24)}-${kind}-${Math.floor(Date.now()/1200)}\`;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:id,kind,value,phase:nextPhase});return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.events[id]={uid:coopUid(),role:role||active.role,kind,value,createdAtClient:Date.now()};if(Number.isFinite(Number(nextPhase)))progress.phase=Math.max(progress.phase,Number(nextPhase));progress.updatedAtClient=Date.now();return true;}`
- [x] `return` — linha 5575, coluna 399 em `onCoopServiceVehicleBoarded()` — `async function coopRecordEvent(kind,role='',value=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const id=\`${coopUid().replace(/[^A-Za-z0-9_-]/g,'').slice(-24)}-${kind}-${Math.floor(Date.now()/1200)}\`;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:id,kind,value,phase:nextPhase});return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.events[id]={uid:coopUid(),role:role||active.role,kind,value,createdAtClient:Date.now()};if(Number.isFinite(Number(nextPhase)))progress.phase=Math.max(progress.phase,Number(nextPhase));progress.updatedAtClient=Date.now();return true;}`
- [x] `if` — linha 5575, coluna 619 em `onCoopServiceVehicleBoarded()` — `async function coopRecordEvent(kind,role='',value=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const id=\`${coopUid().replace(/[^A-Za-z0-9_-]/g,'').slice(-24)}-${kind}-${Math.floor(Date.now()/1200)}\`;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:id,kind,value,phase:nextPhase});return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.events[id]={uid:coopUid(),role:role||active.role,kind,value,createdAtClient:Date.now()};if(Number.isFinite(Number(nextPhase)))progress.phase=Math.max(progress.phase,Number(nextPhase));progress.updatedAtClient=Date.now();return true;}`
- [x] `return` — linha 5575, coluna 751 em `onCoopServiceVehicleBoarded()` — `async function coopRecordEvent(kind,role='',value=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return false;const id=\`${coopUid().replace(/[^A-Za-z0-9_-]/g,'').slice(-24)}-${kind}-${Math.floor(Date.now()/1200)}\`;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:id,kind,value,phase:nextPhase});return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.events[id]={uid:coopUid(),role:role||active.role,kind,value,createdAtClient:Date.now()};if(Number.isFinite(Number(nextPhase)))progress.phase=Math.max(progress.phase,Number(nextPhase));progress.updatedAtClient=Date.now();return true;}`
- [x] `if` — linha 5576, coluna 131 em `onCoopServiceVehicleBoarded()` — `async function coopAddCounter(kind,amount=1,target=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return false;const competitive=['streetRace','ovalRace'].includes(record?.type)&&record.mode==='competitive',counterDelta=competitive?0:amount;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:\`${coopUid().slice(-12)}-${kind}-${Date.now()}\`,kind,value:amount,counterDelta,target,phase:nextPhase});return!!result?.ok;}const p=active.record.progress;p.counter=Math.min(999,Number(p.counter||0)+counterDelta);p.target=target;if(Number.isFinite(Number(nextPhase)))p.phase=Math.max(p.phase,Number(nextPhase));p.events[\`${kind}-${Date.now()}\`]={uid:coopUid(),role:active.role,kind,value:amount,createdAtClient:Date.now()};return true;}`
- [x] `return` — linha 5576, coluna 142 em `onCoopServiceVehicleBoarded()` — `async function coopAddCounter(kind,amount=1,target=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return false;const competitive=['streetRace','ovalRace'].includes(record?.type)&&record.mode==='competitive',counterDelta=competitive?0:amount;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:\`${coopUid().slice(-12)}-${kind}-${Date.now()}\`,kind,value:amount,counterDelta,target,phase:nextPhase});return!!result?.ok;}const p=active.record.progress;p.counter=Math.min(999,Number(p.counter||0)+counterDelta);p.target=target;if(Number.isFinite(Number(nextPhase)))p.phase=Math.max(p.phase,Number(nextPhase));p.events[\`${kind}-${Date.now()}\`]={uid:coopUid(),role:active.role,kind,value:amount,createdAtClient:Date.now()};return true;}`
- [x] `if` — linha 5576, coluna 285 em `onCoopServiceVehicleBoarded()` — `async function coopAddCounter(kind,amount=1,target=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return false;const competitive=['streetRace','ovalRace'].includes(record?.type)&&record.mode==='competitive',counterDelta=competitive?0:amount;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:\`${coopUid().slice(-12)}-${kind}-${Date.now()}\`,kind,value:amount,counterDelta,target,phase:nextPhase});return!!result?.ok;}const p=active.record.progress;p.counter=Math.min(999,Number(p.counter||0)+counterDelta);p.target=target;if(Number.isFinite(Number(nextPhase)))p.phase=Math.max(p.phase,Number(nextPhase));p.events[\`${kind}-${Date.now()}\`]={uid:coopUid(),role:active.role,kind,value:amount,createdAtClient:Date.now()};return true;}`
- [x] `return` — linha 5576, coluna 489 em `onCoopServiceVehicleBoarded()` — `async function coopAddCounter(kind,amount=1,target=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return false;const competitive=['streetRace','ovalRace'].includes(record?.type)&&record.mode==='competitive',counterDelta=competitive?0:amount;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:\`${coopUid().slice(-12)}-${kind}-${Date.now()}\`,kind,value:amount,counterDelta,target,phase:nextPhase});return!!result?.ok;}const p=active.record.progress;p.counter=Math.min(999,Number(p.counter||0)+counterDelta);p.target=target;if(Number.isFinite(Number(nextPhase)))p.phase=Math.max(p.phase,Number(nextPhase));p.events[\`${kind}-${Date.now()}\`]={uid:coopUid(),role:active.role,kind,value:amount,createdAtClient:Date.now()};return true;}`
- [x] `if` — linha 5576, coluna 614 em `onCoopServiceVehicleBoarded()` — `async function coopAddCounter(kind,amount=1,target=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return false;const competitive=['streetRace','ovalRace'].includes(record?.type)&&record.mode==='competitive',counterDelta=competitive?0:amount;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:\`${coopUid().slice(-12)}-${kind}-${Date.now()}\`,kind,value:amount,counterDelta,target,phase:nextPhase});return!!result?.ok;}const p=active.record.progress;p.counter=Math.min(999,Number(p.counter||0)+counterDelta);p.target=target;if(Number.isFinite(Number(nextPhase)))p.phase=Math.max(p.phase,Number(nextPhase));p.events[\`${kind}-${Date.now()}\`]={uid:coopUid(),role:active.role,kind,value:amount,createdAtClient:Date.now()};return true;}`
- [x] `return` — linha 5576, coluna 808 em `onCoopServiceVehicleBoarded()` — `async function coopAddCounter(kind,amount=1,target=1,nextPhase=null){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return false;const competitive=['streetRace','ovalRace'].includes(record?.type)&&record.mode==='competitive',counterDelta=competitive?0:amount;if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:\`${coopUid().slice(-12)}-${kind}-${Date.now()}\`,kind,value:amount,counterDelta,target,phase:nextPhase});return!!result?.ok;}const p=active.record.progress;p.counter=Math.min(999,Number(p.counter||0)+counterDelta);p.target=target;if(Number.isFinite(Number(nextPhase)))p.phase=Math.max(p.phase,Number(nextPhase));p.events[\`${kind}-${Date.now()}\`]={uid:coopUid(),role:active.role,kind,value:amount,createdAtClient:Date.now()};return true;}`
- [x] `if` — linha 5578, coluna 38 em `onCoopServiceVehicleBoarded()` — `const active=activeCoopMission();if(!active)return{ok:false,eventOwner:''};const id=String(eventId||'').replace(/[^A-Za-z0-9_-]/g,'').slice(0,80);if(!id)return{ok:false,eventOwner:''};`
- [x] `return` — linha 5578, coluna 49 em `onCoopServiceVehicleBoarded()` — `const active=activeCoopMission();if(!active)return{ok:false,eventOwner:''};const id=String(eventId||'').replace(/[^A-Za-z0-9_-]/g,'').slice(0,80);if(!id)return{ok:false,eventOwner:''};`
- [x] `if` — linha 5578, coluna 151 em `onCoopServiceVehicleBoarded()` — `const active=activeCoopMission();if(!active)return{ok:false,eventOwner:''};const id=String(eventId||'').replace(/[^A-Za-z0-9_-]/g,'').slice(0,80);if(!id)return{ok:false,eventOwner:''};`
- [x] `return` — linha 5578, coluna 158 em `onCoopServiceVehicleBoarded()` — `const active=activeCoopMission();if(!active)return{ok:false,eventOwner:''};const id=String(eventId||'').replace(/[^A-Za-z0-9_-]/g,'').slice(0,80);if(!id)return{ok:false,eventOwner:''};`
- [x] `if` — linha 5579, coluna 5 em `onCoopServiceVehicleBoarded()` — `if(active.remote)return await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:id,kind,value,reserveEvent:true,counterDelta:Number(options.counterDelta||0),target:options.target,phase:options.phase});`
- [x] `return` — linha 5579, coluna 22 em `onCoopServiceVehicleBoarded()` — `if(active.remote)return await coopBackendCall('updateCoopMissionProgress',active.id,{eventId:id,kind,value,reserveEvent:true,counterDelta:Number(options.counterDelta||0),target:options.target,phase:options.phase});`
- [x] `if` — linha 5580, coluna 137 em `onCoopServiceVehicleBoarded()` — `const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}}),existing=progress.events[id];if(existing)return{ok:existing.uid===coopUid(),eventOwner:existing.uid,progress};`
- [x] `return` — linha 5580, coluna 149 em `onCoopServiceVehicleBoarded()` — `const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}}),existing=progress.events[id];if(existing)return{ok:existing.uid===coopUid(),eventOwner:existing.uid,progress};`
- [x] `if` — linha 5581, coluna 197 em `onCoopServiceVehicleBoarded()` — `progress.events[id]={uid:coopUid(),role:active.role,kind,value,createdAtClient:Date.now()};progress.counter=Math.max(0,Number(progress.counter||0)+Math.max(0,Number(options.counterDelta||0)));if(Number.isFinite(Number(options.target)))progress.target=Math.max(1,Number(options.target));if(Number.isFinite(Number(options.phase)))progress.phase=Math.max(Number(progress.phase||0),Number(options.phase));return{ok:true,eventOwner:coopUid(),progress};`
- [x] `if` — linha 5581, coluna 291 em `onCoopServiceVehicleBoarded()` — `progress.events[id]={uid:coopUid(),role:active.role,kind,value,createdAtClient:Date.now()};progress.counter=Math.max(0,Number(progress.counter||0)+Math.max(0,Number(options.counterDelta||0)));if(Number.isFinite(Number(options.target)))progress.target=Math.max(1,Number(options.target));if(Number.isFinite(Number(options.phase)))progress.phase=Math.max(Number(progress.phase||0),Number(options.phase));return{ok:true,eventOwner:coopUid(),progress};`
- [x] `return` — linha 5581, coluna 406 em `onCoopServiceVehicleBoarded()` — `progress.events[id]={uid:coopUid(),role:active.role,kind,value,createdAtClient:Date.now()};progress.counter=Math.max(0,Number(progress.counter||0)+Math.max(0,Number(options.counterDelta||0)));if(Number.isFinite(Number(options.target)))progress.target=Math.max(1,Number(options.target));if(Number.isFinite(Number(options.phase)))progress.phase=Math.max(Number(progress.phase||0),Number(options.phase));return{ok:true,eventOwner:coopUid(),progress};`
- [x] `for` — linha 5583, coluna 142 em `coopNearbyParticipantCount()` — `function coopNearbyParticipantCount(radius=7){const record=activeCoopRecord(),presence=window.OTTHOS_RTDB?.getPresence?.()||{};let count=1;for(const participant of coopParticipants(record)){if(participant.uid===coopUid())continue;const remote=presence[participant.uid];if(remote&&Math.hypot(player.x-Number(remote.x||0),player.z-Number(remote.z||0))<=radius)count++;}return count;}`
- [x] `if` — linha 5583, coluna 193 em `coopNearbyParticipantCount()` — `function coopNearbyParticipantCount(radius=7){const record=activeCoopRecord(),presence=window.OTTHOS_RTDB?.getPresence?.()||{};let count=1;for(const participant of coopParticipants(record)){if(participant.uid===coopUid())continue;const remote=presence[participant.uid];if(remote&&Math.hypot(player.x-Number(remote.x||0),player.z-Number(remote.z||0))<=radius)count++;}return count;}`
- [x] `if` — linha 5583, coluna 272 em `coopNearbyParticipantCount()` — `function coopNearbyParticipantCount(radius=7){const record=activeCoopRecord(),presence=window.OTTHOS_RTDB?.getPresence?.()||{};let count=1;for(const participant of coopParticipants(record)){if(participant.uid===coopUid())continue;const remote=presence[participant.uid];if(remote&&Math.hypot(player.x-Number(remote.x||0),player.z-Number(remote.z||0))<=radius)count++;}return count;}`
- [x] `return` — linha 5583, coluna 370 em `coopNearbyParticipantCount()` — `function coopNearbyParticipantCount(radius=7){const record=activeCoopRecord(),presence=window.OTTHOS_RTDB?.getPresence?.()||{};let count=1;for(const participant of coopParticipants(record)){if(participant.uid===coopUid())continue;const remote=presence[participant.uid];if(remote&&Math.hypot(player.x-Number(remote.x||0),player.z-Number(remote.z||0))<=radius)count++;}return count;}`
- [x] `if` — linha 5585, coluna 64 em `coopNearbyParticipantCount()` — `const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||record.status!=='active')return false;const type=record.type,p=coopProgress(record),role=coopOwnParticipant(record)?.role||active.role,target=coopCurrentTarget(active,record);if(target&&Math.hypot(player.x-(target.navX??target.x),player.z-(target.navZ??target.z))>6){toast('Chegue ao ponto marcado no GPS.','warn');return false;}`
- [x] `return` — linha 5585, coluna 110 em `coopNearbyParticipantCount()` — `const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||record.status!=='active')return false;const type=record.type,p=coopProgress(record),role=coopOwnParticipant(record)?.role||active.role,target=coopCurrentTarget(active,record);if(target&&Math.hypot(player.x-(target.navX??target.x),player.z-(target.navZ??target.z))>6){toast('Chegue ao ponto marcado no GPS.','warn');return false;}`
- [x] `if` — linha 5585, coluna 260 em `coopNearbyParticipantCount()` — `const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||record.status!=='active')return false;const type=record.type,p=coopProgress(record),role=coopOwnParticipant(record)?.role||active.role,target=coopCurrentTarget(active,record);if(target&&Math.hypot(player.x-(target.navX??target.x),player.z-(target.navZ??target.z))>6){toast('Chegue ao ponto marcado no GPS.','warn');return false;}`
- [x] `return` — linha 5585, coluna 400 em `coopNearbyParticipantCount()` — `const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||record.status!=='active')return false;const type=record.type,p=coopProgress(record),role=coopOwnParticipant(record)?.role||active.role,target=coopCurrentTarget(active,record);if(target&&Math.hypot(player.x-(target.navX??target.x),player.z-(target.navZ??target.z))>6){toast('Chegue ao ponto marcado no GPS.','warn');return false;}`
- [x] `if` — linha 5586, coluna 5 em `coopNearbyParticipantCount()` — `if(coopServiceKind(type)){`
- [x] `if` — linha 5587, coluna 7 em `coopNearbyParticipantCount()` — `if(p.phase<1){toast(role==='driver'?'Entre no veículo reservado usando AÇÃO.':'Aguarde o motorista e entre como passageiro pelo menu do jogador online.','warn',2800);return false;}`
- [x] `return` — linha 5587, coluna 173 em `coopNearbyParticipantCount()` — `if(p.phase<1){toast(role==='driver'?'Entre no veículo reservado usando AÇÃO.':'Aguarde o motorista e entre como passageiro pelo menu do jogador online.','warn',2800);return false;}`
- [x] `if` — linha 5588, coluna 7 em `coopNearbyParticipantCount()` — `if(p.phase===1){toast('O motorista precisa estacionar no ponto marcado.','warn');return false;}`
- [x] `return` — linha 5588, coluna 88 em `coopNearbyParticipantCount()` — `if(p.phase===1){toast('O motorista precisa estacionar no ponto marcado.','warn');return false;}`
- [x] `if` — linha 5589, coluna 7 em `coopNearbyParticipantCount()` — `if(type==='paramedic'&&p.phase>=5){toast('O motorista deve levar a ambulância até o ponto médico marcado.','warn');return false;}`
- [x] `return` — linha 5589, coluna 122 em `coopNearbyParticipantCount()` — `if(type==='paramedic'&&p.phase>=5){toast('O motorista deve levar a ambulância até o ponto médico marcado.','warn');return false;}`
- [x] `if` — linha 5590, coluna 7 em `coopNearbyParticipantCount()` — `if(player.vehicle){toast('Estacione e saia do veículo para executar a tarefa.','warn');return false;}`
- [x] `return` — linha 5590, coluna 94 em `coopNearbyParticipantCount()` — `if(player.vehicle){toast('Estacione e saia do veículo para executar a tarefa.','warn');return false;}`
- [x] `if` — linha 5591, coluna 7 em `coopNearbyParticipantCount()` — `if(type==='firefighter'){`
- [x] `if` — linha 5592, coluna 9 em `coopNearbyParticipantCount()` — `if(p.counter<4){if(role!=='hose'&&record.mode!=='solo'){toast('Você está no apoio; a pessoa da mangueira deve controlar o fogo.','warn');return false;}const fire=active.sceneTarget?.ref||ensureActiveFire(true),next=Math.min(4,p.counter+1);if(fire)fire.coopIntensity=Math.max(.08,1-next/4);await coopAddCounter('water-pulse',1,4,3);if(next>=4)await coopRecordEvent('fire-controlled',role,1,4);toast(\`Água aplicada. Intensidade do fogo: ${Math.max(0,Math.round((1-next/4)*100))}%.\`,'good',2200);return true;}`
- [x] `if` — linha 5592, coluna 25 em `coopNearbyParticipantCount()` — `if(p.counter<4){if(role!=='hose'&&record.mode!=='solo'){toast('Você está no apoio; a pessoa da mangueira deve controlar o fogo.','warn');return false;}const fire=active.sceneTarget?.ref||ensureActiveFire(true),next=Math.min(4,p.counter+1);if(fire)fire.coopIntensity=Math.max(.08,1-next/4);await coopAddCounter('water-pulse',1,4,3);if(next>=4)await coopRecordEvent('fire-controlled',role,1,4);toast(\`Água aplicada. Intensidade do fogo: ${Math.max(0,Math.round((1-next/4)*100))}%.\`,'good',2200);return true;}`
- [x] `return` — linha 5592, coluna 146 em `coopNearbyParticipantCount()` — `if(p.counter<4){if(role!=='hose'&&record.mode!=='solo'){toast('Você está no apoio; a pessoa da mangueira deve controlar o fogo.','warn');return false;}const fire=active.sceneTarget?.ref||ensureActiveFire(true),next=Math.min(4,p.counter+1);if(fire)fire.coopIntensity=Math.max(.08,1-next/4);await coopAddCounter('water-pulse',1,4,3);if(next>=4)await coopRecordEvent('fire-controlled',role,1,4);toast(\`Água aplicada. Intensidade do fogo: ${Math.max(0,Math.round((1-next/4)*100))}%.\`,'good',2200);return true;}`
- [x] `if` — linha 5592, coluna 248 em `coopNearbyParticipantCount()` — `if(p.counter<4){if(role!=='hose'&&record.mode!=='solo'){toast('Você está no apoio; a pessoa da mangueira deve controlar o fogo.','warn');return false;}const fire=active.sceneTarget?.ref||ensureActiveFire(true),next=Math.min(4,p.counter+1);if(fire)fire.coopIntensity=Math.max(.08,1-next/4);await coopAddCounter('water-pulse',1,4,3);if(next>=4)await coopRecordEvent('fire-controlled',role,1,4);toast(\`Água aplicada. Intensidade do fogo: ${Math.max(0,Math.round((1-next/4)*100))}%.\`,'good',2200);return true;}`
- [x] `if` — linha 5592, coluna 340 em `coopNearbyParticipantCount()` — `if(p.counter<4){if(role!=='hose'&&record.mode!=='solo'){toast('Você está no apoio; a pessoa da mangueira deve controlar o fogo.','warn');return false;}const fire=active.sceneTarget?.ref||ensureActiveFire(true),next=Math.min(4,p.counter+1);if(fire)fire.coopIntensity=Math.max(.08,1-next/4);await coopAddCounter('water-pulse',1,4,3);if(next>=4)await coopRecordEvent('fire-controlled',role,1,4);toast(\`Água aplicada. Intensidade do fogo: ${Math.max(0,Math.round((1-next/4)*100))}%.\`,'good',2200);return true;}`
- [x] `return` — linha 5592, coluna 502 em `coopNearbyParticipantCount()` — `if(p.counter<4){if(role!=='hose'&&record.mode!=='solo'){toast('Você está no apoio; a pessoa da mangueira deve controlar o fogo.','warn');return false;}const fire=active.sceneTarget?.ref||ensureActiveFire(true),next=Math.min(4,p.counter+1);if(fire)fire.coopIntensity=Math.max(.08,1-next/4);await coopAddCounter('water-pulse',1,4,3);if(next>=4)await coopRecordEvent('fire-controlled',role,1,4);toast(\`Água aplicada. Intensidade do fogo: ${Math.max(0,Math.round((1-next/4)*100))}%.\`,'good',2200);return true;}`
- [x] `if` — linha 5593, coluna 9 em `coopNearbyParticipantCount()` — `if(role!=='rescuer'&&record.mode!=='solo'){toast('A pessoa de apoio precisa isolar e liberar a área.','warn');return false;}await coopRecordEvent('safety-release',role,1,5);toast('Área isolada, verificada e liberada com segurança.','good');return true;`
- [x] `return` — linha 5593, coluna 119 em `coopNearbyParticipantCount()` — `if(role!=='rescuer'&&record.mode!=='solo'){toast('A pessoa de apoio precisa isolar e liberar a área.','warn');return false;}await coopRecordEvent('safety-release',role,1,5);toast('Área isolada, verificada e liberada com segurança.','good');return true;`
- [x] `return` — linha 5593, coluna 249 em `coopNearbyParticipantCount()` — `if(role!=='rescuer'&&record.mode!=='solo'){toast('A pessoa de apoio precisa isolar e liberar a área.','warn');return false;}await coopRecordEvent('safety-release',role,1,5);toast('Área isolada, verificada e liberada com segurança.','good');return true;`
- [x] `if` — linha 5595, coluna 7 em `coopNearbyParticipantCount()` — `if(type==='paramedic'){`
- [x] `if` — linha 5596, coluna 9 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('first-aid')){if(role!=='medic'&&record.mode!=='solo'){toast('O socorrista precisa atender o paciente primeiro.','warn');return false;}await coopRecordEvent('first-aid',role,1,3);player.sitUntil=performance.now()+1500;toast('Primeiros socorros feitos com cuidado.','good');return true;}`
- [x] `if` — linha 5596, coluna 40 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('first-aid')){if(role!=='medic'&&record.mode!=='solo'){toast('O socorrista precisa atender o paciente primeiro.','warn');return false;}await coopRecordEvent('first-aid',role,1,3);player.sitUntil=performance.now()+1500;toast('Primeiros socorros feitos com cuidado.','good');return true;}`
- [x] `return` — linha 5596, coluna 147 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('first-aid')){if(role!=='medic'&&record.mode!=='solo'){toast('O socorrista precisa atender o paciente primeiro.','warn');return false;}await coopRecordEvent('first-aid',role,1,3);player.sitUntil=performance.now()+1500;toast('Primeiros socorros feitos com cuidado.','good');return true;}`
- [x] `return` — linha 5596, coluna 299 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('first-aid')){if(role!=='medic'&&record.mode!=='solo'){toast('O socorrista precisa atender o paciente primeiro.','warn');return false;}await coopRecordEvent('first-aid',role,1,3);player.sitUntil=performance.now()+1500;toast('Primeiros socorros feitos com cuidado.','good');return true;}`
- [x] `if` — linha 5597, coluna 9 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('stretcher')){if(record.mode!=='solo'&&coopNearbyParticipantCount(5)<2){toast('A maca precisa de duas pessoas próximas. Chame outro integrante da equipe.','warn',2800);return false;}await coopRecordEvent('stretcher',role,1,4);toast('Maca carregada por duas pessoas. Leve o paciente até a ambulância.','good',2600);const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)coopSetWaypoint({id:\`service-${vehicle.id}\`,name:'Colocar o paciente na ambulância',icon:'🚑',x:vehicle.group.position.x,z:vehicle.group.position.z});return true;}`
- [x] `if` — linha 5597, coluna 40 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('stretcher')){if(record.mode!=='solo'&&coopNearbyParticipantCount(5)<2){toast('A maca precisa de duas pessoas próximas. Chame outro integrante da equipe.','warn',2800);return false;}await coopRecordEvent('stretcher',role,1,4);toast('Maca carregada por duas pessoas. Leve o paciente até a ambulância.','good',2600);const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)coopSetWaypoint({id:\`service-${vehicle.id}\`,name:'Colocar o paciente na ambulância',icon:'🚑',x:vehicle.group.position.x,z:vehicle.group.position.z});return true;}`
- [x] `return` — linha 5597, coluna 194 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('stretcher')){if(record.mode!=='solo'&&coopNearbyParticipantCount(5)<2){toast('A maca precisa de duas pessoas próximas. Chame outro integrante da equipe.','warn',2800);return false;}await coopRecordEvent('stretcher',role,1,4);toast('Maca carregada por duas pessoas. Leve o paciente até a ambulância.','good',2600);const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)coopSetWaypoint({id:\`service-${vehicle.id}\`,name:'Colocar o paciente na ambulância',icon:'🚑',x:vehicle.group.position.x,z:vehicle.group.position.z});return true;}`
- [x] `if` — linha 5597, coluna 395 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('stretcher')){if(record.mode!=='solo'&&coopNearbyParticipantCount(5)<2){toast('A maca precisa de duas pessoas próximas. Chame outro integrante da equipe.','warn',2800);return false;}await coopRecordEvent('stretcher',role,1,4);toast('Maca carregada por duas pessoas. Leve o paciente até a ambulância.','good',2600);const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)coopSetWaypoint({id:\`service-${vehicle.id}\`,name:'Colocar o paciente na ambulância',icon:'🚑',x:vehicle.group.position.x,z:vehicle.group.position.z});return true;}`
- [x] `return` — linha 5597, coluna 555 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('stretcher')){if(record.mode!=='solo'&&coopNearbyParticipantCount(5)<2){toast('A maca precisa de duas pessoas próximas. Chame outro integrante da equipe.','warn',2800);return false;}await coopRecordEvent('stretcher',role,1,4);toast('Maca carregada por duas pessoas. Leve o paciente até a ambulância.','good',2600);const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)coopSetWaypoint({id:\`service-${vehicle.id}\`,name:'Colocar o paciente na ambulância',icon:'🚑',x:vehicle.group.position.x,z:vehicle.group.position.z});return true;}`
- [x] `if` — linha 5598, coluna 9 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('patient-loaded')){await coopRecordEvent('patient-loaded',role,1,5);toast('Paciente acomodado com segurança. Motorista: siga até o ponto médico.','good',2800);coopSetWaypoint(coopMedicalDestination());return true;}`
- [x] `return` — linha 5598, coluna 227 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('patient-loaded')){await coopRecordEvent('patient-loaded',role,1,5);toast('Paciente acomodado com segurança. Motorista: siga até o ponto médico.','good',2800);coopSetWaypoint(coopMedicalDestination());return true;}`
- [x] `return` — linha 5599, coluna 9 em `coopNearbyParticipantCount()` — `return false;`
- [x] `if` — linha 5601, coluna 7 em `coopNearbyParticipantCount()` — `if(type==='police'){`
- [x] `if` — linha 5602, coluna 9 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('approach')){if(role!=='officer'&&record.mode!=='solo'){toast('O policial deve iniciar a abordagem educativa.','warn');return false;}await coopRecordEvent('approach',role,1,3);toast('Abordagem calma: converse antes de concluir.','good');return true;}`
- [x] `if` — linha 5602, coluna 39 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('approach')){if(role!=='officer'&&record.mode!=='solo'){toast('O policial deve iniciar a abordagem educativa.','warn');return false;}await coopRecordEvent('approach',role,1,3);toast('Abordagem calma: converse antes de concluir.','good');return true;}`
- [x] `return` — linha 5602, coluna 145 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('approach')){if(role!=='officer'&&record.mode!=='solo'){toast('O policial deve iniciar a abordagem educativa.','warn');return false;}await coopRecordEvent('approach',role,1,3);toast('Abordagem calma: converse antes de concluir.','good');return true;}`
- [x] `return` — linha 5602, coluna 263 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('approach')){if(role!=='officer'&&record.mode!=='solo'){toast('O policial deve iniciar a abordagem educativa.','warn');return false;}await coopRecordEvent('approach',role,1,3);toast('Abordagem calma: converse antes de concluir.','good');return true;}`
- [x] `if` — linha 5603, coluna 9 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('you-are-arrested')){const npc=active.sceneTarget?.ref;if(npc){npc.following=true;npc.emoteType='wave';npc.emoteUntil=performance.now()+1800;}await coopRecordEvent('you-are-arrested',role,1,4);toast('“Você está preso.” Agora acompanhe o personagem, sem armas ou agressões, até a viatura.','good',3200);const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)coopSetWaypoint({id:\`service-${vehicle.id}\`,name:'Acompanhar até a viatura',icon:'🚓',x:vehicle.group.position.x,z:vehicle.group.position.z});return true;}`
- [x] `if` — linha 5603, coluna 81 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('you-are-arrested')){const npc=active.sceneTarget?.ref;if(npc){npc.following=true;npc.emoteType='wave';npc.emoteUntil=performance.now()+1800;}await coopRecordEvent('you-are-arrested',role,1,4);toast('“Você está preso.” Agora acompanhe o personagem, sem armas ou agressões, até a viatura.','good',3200);const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)coopSetWaypoint({id:\`service-${vehicle.id}\`,name:'Acompanhar até a viatura',icon:'🚓',x:vehicle.group.position.x,z:vehicle.group.position.z});return true;}`
- [x] `if` — linha 5603, coluna 383 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('you-are-arrested')){const npc=active.sceneTarget?.ref;if(npc){npc.following=true;npc.emoteType='wave';npc.emoteUntil=performance.now()+1800;}await coopRecordEvent('you-are-arrested',role,1,4);toast('“Você está preso.” Agora acompanhe o personagem, sem armas ou agressões, até a viatura.','good',3200);const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)coopSetWaypoint({id:\`service-${vehicle.id}\`,name:'Acompanhar até a viatura',icon:'🚓',x:vehicle.group.position.x,z:vehicle.group.position.z});return true;}`
- [x] `return` — linha 5603, coluna 535 em `coopNearbyParticipantCount()` — `if(!coopHasEvent('you-are-arrested')){const npc=active.sceneTarget?.ref;if(npc){npc.following=true;npc.emoteType='wave';npc.emoteUntil=performance.now()+1800;}await coopRecordEvent('you-are-arrested',role,1,4);toast('“Você está preso.” Agora acompanhe o personagem, sem armas ou agressões, até a viatura.','good',3200);const vehicle=serviceMissionVehicle(active.serviceJob);if(vehicle)coopSetWaypoint({id:\`service-${vehicle.id}\`,name:'Acompanhar até a viatura',icon:'🚓',x:vehicle.group.position.x,z:vehicle.group.position.z});return true;}`
- [x] `if` — linha 5604, coluna 43 em `coopNearbyParticipantCount()` — `const npc=active.sceneTarget?.ref;if(npc)npc.following=false;await coopRecordEvent('suspect-escorted',role,1,5);toast('Personagem acompanhado com segurança até a viatura.','good');return true;`
- [x] `return` — linha 5604, coluna 189 em `coopNearbyParticipantCount()` — `const npc=active.sceneTarget?.ref;if(npc)npc.following=false;await coopRecordEvent('suspect-escorted',role,1,5);toast('Personagem acompanhado com segurança até a viatura.','good');return true;`
- [x] `if` — linha 5607, coluna 5 em `coopNearbyParticipantCount()` — `if(type==='fishing'){`
- [x] `if` — linha 5608, coluna 7 em `coopNearbyParticipantCount()` — `if(p.phase<1){await coopRecordEvent('at-lake',role,1,1);toast('Equipe reunida no lago. Comece a pescar.','good');return true;}`
- [x] `return` — linha 5608, coluna 120 em `coopNearbyParticipantCount()` — `if(p.phase<1){await coopRecordEvent('at-lake',role,1,1);toast('Equipe reunida no lago. Comece a pescar.','good');return true;}`
- [x] `if` — linha 5609, coluna 7 em `coopNearbyParticipantCount()` — `if(p.phase===1){if(state.inventory.bait<1){toast('Você precisa de isca.','warn');return false;}startFishing('shore',{cooperative:true,requestId:active.id});return true;}`
- [x] `if` — linha 5609, coluna 23 em `coopNearbyParticipantCount()` — `if(p.phase===1){if(state.inventory.bait<1){toast('Você precisa de isca.','warn');return false;}startFishing('shore',{cooperative:true,requestId:active.id});return true;}`
- [x] `return` — linha 5609, coluna 88 em `coopNearbyParticipantCount()` — `if(p.phase===1){if(state.inventory.bait<1){toast('Você precisa de isca.','warn');return false;}startFishing('shore',{cooperative:true,requestId:active.id});return true;}`
- [x] `return` — linha 5609, coluna 163 em `coopNearbyParticipantCount()` — `if(p.phase===1){if(state.inventory.bait<1){toast('Você precisa de isca.','warn');return false;}startFishing('shore',{cooperative:true,requestId:active.id});return true;}`
- [x] `if` — linha 5610, coluna 7 em `coopNearbyParticipantCount()` — `if(Number(state.inventory.rawFish||0)<1){toast('A equipe precisa de peixe cru para assar.','warn');return false;}state.inventory.rawFish--;setTimeout(async()=>{state.inventory.cookedFish=(state.inventory.cookedFish||0)+1;saveState(true);await coopAddCounter('cooked-fish',1,3);toast('Peixe assado para a equipe!','good');},1800);toast('Peixe na grelha...','good');return true;`
- [x] `return` — linha 5610, coluna 106 em `coopNearbyParticipantCount()` — `if(Number(state.inventory.rawFish||0)<1){toast('A equipe precisa de peixe cru para assar.','warn');return false;}state.inventory.rawFish--;setTimeout(async()=>{state.inventory.cookedFish=(state.inventory.cookedFish||0)+1;saveState(true);await coopAddCounter('cooked-fish',1,3);toast('Peixe assado para a equipe!','good');},1800);toast('Peixe na grelha...','good');return true;`
- [x] `return` — linha 5610, coluna 371 em `coopNearbyParticipantCount()` — `if(Number(state.inventory.rawFish||0)<1){toast('A equipe precisa de peixe cru para assar.','warn');return false;}state.inventory.rawFish--;setTimeout(async()=>{state.inventory.cookedFish=(state.inventory.cookedFish||0)+1;saveState(true);await coopAddCounter('cooked-fish',1,3);toast('Peixe assado para a equipe!','good');},1800);toast('Peixe na grelha...','good');return true;`
- [x] `if` — linha 5612, coluna 5 em `coopNearbyParticipantCount()` — `if(type==='school'){`
- [x] `if` — linha 5614, coluna 7 em `coopNearbyParticipantCount()` — `if(follower){const school=coopNearestSchool();if(!school||Math.hypot(player.x-school.x,player.z-school.z)>8){toast('Conduza o aluno até a escola marcada.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student-delivered',follower.id),'student-delivered',1,{counterDelta:1,target:4,phase:4});if(!result?.ok||result.eventOwner!==coopUid()){active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);follower.following=false;toast('Esse aluno já foi entregue por outro integrante.','warn');return false;}follower.following=false;follower.coopSchoolDelivered=true;follower.baseX=school.x+(Math.random()-.5)*5;follower.baseZ=school.z+5+(Math.random()*2);active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);if(!active.deliveredNpcIds.includes(follower.id))active.deliveredNpcIds.push(follower.id);toast(\`${follower.name} chegou à escola. +1 aluno para a equipe.\`,'good',2300);return true;}`
- [x] `if` — linha 5614, coluna 53 em `coopNearbyParticipantCount()` — `if(follower){const school=coopNearestSchool();if(!school||Math.hypot(player.x-school.x,player.z-school.z)>8){toast('Conduza o aluno até a escola marcada.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student-delivered',follower.id),'student-delivered',1,{counterDelta:1,target:4,phase:4});if(!result?.ok||result.eventOwner!==coopUid()){active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);follower.following=false;toast('Esse aluno já foi entregue por outro integrante.','warn');return false;}follower.following=false;follower.coopSchoolDelivered=true;follower.baseX=school.x+(Math.random()-.5)*5;follower.baseZ=school.z+5+(Math.random()*2);active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);if(!active.deliveredNpcIds.includes(follower.id))active.deliveredNpcIds.push(follower.id);toast(\`${follower.name} chegou à escola. +1 aluno para a equipe.\`,'good',2300);return true;}`
- [x] `return` — linha 5614, coluna 170 em `coopNearbyParticipantCount()` — `if(follower){const school=coopNearestSchool();if(!school||Math.hypot(player.x-school.x,player.z-school.z)>8){toast('Conduza o aluno até a escola marcada.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student-delivered',follower.id),'student-delivered',1,{counterDelta:1,target:4,phase:4});if(!result?.ok||result.eventOwner!==coopUid()){active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);follower.following=false;toast('Esse aluno já foi entregue por outro integrante.','warn');return false;}follower.following=false;follower.coopSchoolDelivered=true;follower.baseX=school.x+(Math.random()-.5)*5;follower.baseZ=school.z+5+(Math.random()*2);active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);if(!active.deliveredNpcIds.includes(follower.id))active.deliveredNpcIds.push(follower.id);toast(\`${follower.name} chegou à escola. +1 aluno para a equipe.\`,'good',2300);return true;}`
- [x] `if` — linha 5614, coluna 329 em `coopNearbyParticipantCount()` — `if(follower){const school=coopNearestSchool();if(!school||Math.hypot(player.x-school.x,player.z-school.z)>8){toast('Conduza o aluno até a escola marcada.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student-delivered',follower.id),'student-delivered',1,{counterDelta:1,target:4,phase:4});if(!result?.ok||result.eventOwner!==coopUid()){active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);follower.following=false;toast('Esse aluno já foi entregue por outro integrante.','warn');return false;}follower.following=false;follower.coopSchoolDelivered=true;follower.baseX=school.x+(Math.random()-.5)*5;follower.baseZ=school.z+5+(Math.random()*2);active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);if(!active.deliveredNpcIds.includes(follower.id))active.deliveredNpcIds.push(follower.id);toast(\`${follower.name} chegou à escola. +1 aluno para a equipe.\`,'good',2300);return true;}`
- [x] `return` — linha 5614, coluna 541 em `coopNearbyParticipantCount()` — `if(follower){const school=coopNearestSchool();if(!school||Math.hypot(player.x-school.x,player.z-school.z)>8){toast('Conduza o aluno até a escola marcada.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student-delivered',follower.id),'student-delivered',1,{counterDelta:1,target:4,phase:4});if(!result?.ok||result.eventOwner!==coopUid()){active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);follower.following=false;toast('Esse aluno já foi entregue por outro integrante.','warn');return false;}follower.following=false;follower.coopSchoolDelivered=true;follower.baseX=school.x+(Math.random()-.5)*5;follower.baseZ=school.z+5+(Math.random()*2);active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);if(!active.deliveredNpcIds.includes(follower.id))active.deliveredNpcIds.push(follower.id);toast(\`${follower.name} chegou à escola. +1 aluno para a equipe.\`,'good',2300);return true;}`
- [x] `if` — linha 5614, coluna 778 em `coopNearbyParticipantCount()` — `if(follower){const school=coopNearestSchool();if(!school||Math.hypot(player.x-school.x,player.z-school.z)>8){toast('Conduza o aluno até a escola marcada.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student-delivered',follower.id),'student-delivered',1,{counterDelta:1,target:4,phase:4});if(!result?.ok||result.eventOwner!==coopUid()){active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);follower.following=false;toast('Esse aluno já foi entregue por outro integrante.','warn');return false;}follower.following=false;follower.coopSchoolDelivered=true;follower.baseX=school.x+(Math.random()-.5)*5;follower.baseZ=school.z+5+(Math.random()*2);active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);if(!active.deliveredNpcIds.includes(follower.id))active.deliveredNpcIds.push(follower.id);toast(\`${follower.name} chegou à escola. +1 aluno para a equipe.\`,'good',2300);return true;}`
- [x] `return` — linha 5614, coluna 947 em `coopNearbyParticipantCount()` — `if(follower){const school=coopNearestSchool();if(!school||Math.hypot(player.x-school.x,player.z-school.z)>8){toast('Conduza o aluno até a escola marcada.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student-delivered',follower.id),'student-delivered',1,{counterDelta:1,target:4,phase:4});if(!result?.ok||result.eventOwner!==coopUid()){active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);follower.following=false;toast('Esse aluno já foi entregue por outro integrante.','warn');return false;}follower.following=false;follower.coopSchoolDelivered=true;follower.baseX=school.x+(Math.random()-.5)*5;follower.baseZ=school.z+5+(Math.random()*2);active.followingNpcIds=active.followingNpcIds.filter(id=>id!==follower.id);if(!active.deliveredNpcIds.includes(follower.id))active.deliveredNpcIds.push(follower.id);toast(\`${follower.name} chegou à escola. +1 aluno para a equipe.\`,'good',2300);return true;}`
- [x] `if` — linha 5615, coluna 49 em `coopNearbyParticipantCount()` — `const npc=coopNearestInvitableNpc(active);if(!npc||Math.hypot(player.x-npc.group.position.x,player.z-npc.group.position.z)>4){toast('Chegue perto de um personagem para convidá-lo.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student',npc.id),'student-invited',1);if(!result?.ok||result.eventOwner!==coopUid()){toast('Outro integrante acabou de convidar esse aluno. Procure o próximo marcador.','warn',2600);return false;}if(!active.invitedNpcIds.includes(npc.id))active.invitedNpcIds.push(npc.id);if(!active.followingNpcIds.includes(npc.id))active.followingNpcIds.push(npc.id);npc.following=true;toast(\`${npc.name}: “Sim! Vou seguir você até a escola.”\`,'good',2600);const school=coopNearestSchool();if(school)coopSetWaypoint({id:school.id,name:school.name,icon:'🏫',x:school.x,z:school.z});saveState(true);return true;`
- [x] `return` — linha 5615, coluna 196 em `coopNearbyParticipantCount()` — `const npc=coopNearestInvitableNpc(active);if(!npc||Math.hypot(player.x-npc.group.position.x,player.z-npc.group.position.z)>4){toast('Chegue perto de um personagem para convidá-lo.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student',npc.id),'student-invited',1);if(!result?.ok||result.eventOwner!==coopUid()){toast('Outro integrante acabou de convidar esse aluno. Procure o próximo marcador.','warn',2600);return false;}if(!active.invitedNpcIds.includes(npc.id))active.invitedNpcIds.push(npc.id);if(!active.followingNpcIds.includes(npc.id))active.followingNpcIds.push(npc.id);npc.following=true;toast(\`${npc.name}: “Sim! Vou seguir você até a escola.”\`,'good',2600);const school=coopNearestSchool();if(school)coopSetWaypoint({id:school.id,name:school.name,icon:'🏫',x:school.x,z:school.z});saveState(true);return true;`
- [x] `if` — linha 5615, coluna 304 em `coopNearbyParticipantCount()` — `const npc=coopNearestInvitableNpc(active);if(!npc||Math.hypot(player.x-npc.group.position.x,player.z-npc.group.position.z)>4){toast('Chegue perto de um personagem para convidá-lo.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student',npc.id),'student-invited',1);if(!result?.ok||result.eventOwner!==coopUid()){toast('Outro integrante acabou de convidar esse aluno. Procure o próximo marcador.','warn',2600);return false;}if(!active.invitedNpcIds.includes(npc.id))active.invitedNpcIds.push(npc.id);if(!active.followingNpcIds.includes(npc.id))active.followingNpcIds.push(npc.id);npc.following=true;toast(\`${npc.name}: “Sim! Vou seguir você até a escola.”\`,'good',2600);const school=coopNearestSchool();if(school)coopSetWaypoint({id:school.id,name:school.name,icon:'🏫',x:school.x,z:school.z});saveState(true);return true;`
- [x] `return` — linha 5615, coluna 448 em `coopNearbyParticipantCount()` — `const npc=coopNearestInvitableNpc(active);if(!npc||Math.hypot(player.x-npc.group.position.x,player.z-npc.group.position.z)>4){toast('Chegue perto de um personagem para convidá-lo.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student',npc.id),'student-invited',1);if(!result?.ok||result.eventOwner!==coopUid()){toast('Outro integrante acabou de convidar esse aluno. Procure o próximo marcador.','warn',2600);return false;}if(!active.invitedNpcIds.includes(npc.id))active.invitedNpcIds.push(npc.id);if(!active.followingNpcIds.includes(npc.id))active.followingNpcIds.push(npc.id);npc.following=true;toast(\`${npc.name}: “Sim! Vou seguir você até a escola.”\`,'good',2600);const school=coopNearestSchool();if(school)coopSetWaypoint({id:school.id,name:school.name,icon:'🏫',x:school.x,z:school.z});saveState(true);return true;`
- [x] `if` — linha 5615, coluna 462 em `coopNearbyParticipantCount()` — `const npc=coopNearestInvitableNpc(active);if(!npc||Math.hypot(player.x-npc.group.position.x,player.z-npc.group.position.z)>4){toast('Chegue perto de um personagem para convidá-lo.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student',npc.id),'student-invited',1);if(!result?.ok||result.eventOwner!==coopUid()){toast('Outro integrante acabou de convidar esse aluno. Procure o próximo marcador.','warn',2600);return false;}if(!active.invitedNpcIds.includes(npc.id))active.invitedNpcIds.push(npc.id);if(!active.followingNpcIds.includes(npc.id))active.followingNpcIds.push(npc.id);npc.following=true;toast(\`${npc.name}: “Sim! Vou seguir você até a escola.”\`,'good',2600);const school=coopNearestSchool();if(school)coopSetWaypoint({id:school.id,name:school.name,icon:'🏫',x:school.x,z:school.z});saveState(true);return true;`
- [x] `if` — linha 5615, coluna 538 em `coopNearbyParticipantCount()` — `const npc=coopNearestInvitableNpc(active);if(!npc||Math.hypot(player.x-npc.group.position.x,player.z-npc.group.position.z)>4){toast('Chegue perto de um personagem para convidá-lo.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student',npc.id),'student-invited',1);if(!result?.ok||result.eventOwner!==coopUid()){toast('Outro integrante acabou de convidar esse aluno. Procure o próximo marcador.','warn',2600);return false;}if(!active.invitedNpcIds.includes(npc.id))active.invitedNpcIds.push(npc.id);if(!active.followingNpcIds.includes(npc.id))active.followingNpcIds.push(npc.id);npc.following=true;toast(\`${npc.name}: “Sim! Vou seguir você até a escola.”\`,'good',2600);const school=coopNearestSchool();if(school)coopSetWaypoint({id:school.id,name:school.name,icon:'🏫',x:school.x,z:school.z});saveState(true);return true;`
- [x] `if` — linha 5615, coluna 741 em `coopNearbyParticipantCount()` — `const npc=coopNearestInvitableNpc(active);if(!npc||Math.hypot(player.x-npc.group.position.x,player.z-npc.group.position.z)>4){toast('Chegue perto de um personagem para convidá-lo.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student',npc.id),'student-invited',1);if(!result?.ok||result.eventOwner!==coopUid()){toast('Outro integrante acabou de convidar esse aluno. Procure o próximo marcador.','warn',2600);return false;}if(!active.invitedNpcIds.includes(npc.id))active.invitedNpcIds.push(npc.id);if(!active.followingNpcIds.includes(npc.id))active.followingNpcIds.push(npc.id);npc.following=true;toast(\`${npc.name}: “Sim! Vou seguir você até a escola.”\`,'good',2600);const school=coopNearestSchool();if(school)coopSetWaypoint({id:school.id,name:school.name,icon:'🏫',x:school.x,z:school.z});saveState(true);return true;`
- [x] `return` — linha 5615, coluna 847 em `coopNearbyParticipantCount()` — `const npc=coopNearestInvitableNpc(active);if(!npc||Math.hypot(player.x-npc.group.position.x,player.z-npc.group.position.z)>4){toast('Chegue perto de um personagem para convidá-lo.','warn');return false;}const result=await coopReserveEvent(coopEventSubjectId('student',npc.id),'student-invited',1);if(!result?.ok||result.eventOwner!==coopUid()){toast('Outro integrante acabou de convidar esse aluno. Procure o próximo marcador.','warn',2600);return false;}if(!active.invitedNpcIds.includes(npc.id))active.invitedNpcIds.push(npc.id);if(!active.followingNpcIds.includes(npc.id))active.followingNpcIds.push(npc.id);npc.following=true;toast(\`${npc.name}: “Sim! Vou seguir você até a escola.”\`,'good',2600);const school=coopNearestSchool();if(school)coopSetWaypoint({id:school.id,name:school.name,icon:'🏫',x:school.x,z:school.z});saveState(true);return true;`
- [x] `if` — linha 5617, coluna 5 em `coopNearbyParticipantCount()` — `if(type==='streetRace'||type==='ovalRace'){toast('Corra a pé pela rota. O progresso é registrado automaticamente.','good');return true;}`
- [x] `return` — linha 5617, coluna 128 em `coopNearbyParticipantCount()` — `if(type==='streetRace'||type==='ovalRace'){toast('Corra a pé pela rota. O progresso é registrado automaticamente.','good');return true;}`
- [x] `return` — linha 5618, coluna 5 em `coopNearbyParticipantCount()` — `return false;`
- [x] `if` — linha 5620, coluna 82 em `updateCoopFishingProgress()` — `function updateCoopFishingProgress(active,record){const p=coopProgress(record);if(record.type!=='fishing'||record.status!=='active')return;const caught=Math.max(0,Number(state.inventory.rawFish||0)-Number(active.baselineRawFish||0));if(p.phase===1&&caught>Number(active.reportedFish||0)){const delta=caught-Number(active.reportedFish||0);active.reportedFish=caught;coopAddCounter('fish-caught',delta,6).catch(()=>{});}const latest=coopProgress(activeCoopRecord());if(latest.phase===1&&latest.counter>=6&&!active.fishingCookPhaseStarted){active.fishingCookPhaseStarted=true;coopAdvancePhase(2,{counterSet:0,target:3}).then(()=>{active.baselineCookedFish=Number(state.inventory.cookedFish||0);coopSetWaypoint({id:'coop-campfire',name:'Fogueira da equipe',icon:'🔥',x:-70,z:-62});updateMissionHUD();});}}`
- [x] `return` — linha 5620, coluna 135 em `updateCoopFishingProgress()` — `function updateCoopFishingProgress(active,record){const p=coopProgress(record);if(record.type!=='fishing'||record.status!=='active')return;const caught=Math.max(0,Number(state.inventory.rawFish||0)-Number(active.baselineRawFish||0));if(p.phase===1&&caught>Number(active.reportedFish||0)){const delta=caught-Number(active.reportedFish||0);active.reportedFish=caught;coopAddCounter('fish-caught',delta,6).catch(()=>{});}const latest=coopProgress(activeCoopRecord());if(latest.phase===1&&latest.counter>=6&&!active.fishingCookPhaseStarted){active.fishingCookPhaseStarted=true;coopAdvancePhase(2,{counterSet:0,target:3}).then(()=>{active.baselineCookedFish=Number(state.inventory.cookedFish||0);coopSetWaypoint({id:'coop-campfire',name:'Fogueira da equipe',icon:'🔥',x:-70,z:-62});updateMissionHUD();});}}`
- [x] `if` — linha 5620, coluna 236 em `updateCoopFishingProgress()` — `function updateCoopFishingProgress(active,record){const p=coopProgress(record);if(record.type!=='fishing'||record.status!=='active')return;const caught=Math.max(0,Number(state.inventory.rawFish||0)-Number(active.baselineRawFish||0));if(p.phase===1&&caught>Number(active.reportedFish||0)){const delta=caught-Number(active.reportedFish||0);active.reportedFish=caught;coopAddCounter('fish-caught',delta,6).catch(()=>{});}const latest=coopProgress(activeCoopRecord());if(latest.phase===1&&latest.counter>=6&&!active.fishingCookPhaseStarted){active.fishingCookPhaseStarted=true;coopAdvancePhase(2,{counterSet:0,target:3}).then(()=>{active.baselineCookedFish=Number(state.inventory.cookedFish||0);coopSetWaypoint({id:'coop-campfire',name:'Fogueira da equipe',icon:'🔥',x:-70,z:-62});updateMissionHUD();});}}`
- [x] `catch` — linha 5620, coluna 406 em `updateCoopFishingProgress()` — `function updateCoopFishingProgress(active,record){const p=coopProgress(record);if(record.type!=='fishing'||record.status!=='active')return;const caught=Math.max(0,Number(state.inventory.rawFish||0)-Number(active.baselineRawFish||0));if(p.phase===1&&caught>Number(active.reportedFish||0)){const delta=caught-Number(active.reportedFish||0);active.reportedFish=caught;coopAddCounter('fish-caught',delta,6).catch(()=>{});}const latest=coopProgress(activeCoopRecord());if(latest.phase===1&&latest.counter>=6&&!active.fishingCookPhaseStarted){active.fishingCookPhaseStarted=true;coopAdvancePhase(2,{counterSet:0,target:3}).then(()=>{active.baselineCookedFish=Number(state.inventory.cookedFish||0);coopSetWaypoint({id:'coop-campfire',name:'Fogueira da equipe',icon:'🔥',x:-70,z:-62});updateMissionHUD();});}}`
- [x] `if` — linha 5620, coluna 467 em `updateCoopFishingProgress()` — `function updateCoopFishingProgress(active,record){const p=coopProgress(record);if(record.type!=='fishing'||record.status!=='active')return;const caught=Math.max(0,Number(state.inventory.rawFish||0)-Number(active.baselineRawFish||0));if(p.phase===1&&caught>Number(active.reportedFish||0)){const delta=caught-Number(active.reportedFish||0);active.reportedFish=caught;coopAddCounter('fish-caught',delta,6).catch(()=>{});}const latest=coopProgress(activeCoopRecord());if(latest.phase===1&&latest.counter>=6&&!active.fishingCookPhaseStarted){active.fishingCookPhaseStarted=true;coopAdvancePhase(2,{counterSet:0,target:3}).then(()=>{active.baselineCookedFish=Number(state.inventory.cookedFish||0);coopSetWaypoint({id:'coop-campfire',name:'Fogueira da equipe',icon:'🔥',x:-70,z:-62});updateMissionHUD();});}}`
- [x] `for` — linha 5621, coluna 110 em `updateCoopSchoolFollowers()` — `function updateCoopSchoolFollowers(active){active.followingNpcIds=[...new Set(active.followingNpcIds||[])];for(const id of active.followingNpcIds){const npc=world.npcs.find(n=>n.id===id);if(!npc)continue;npc.following=true;const distance=Math.hypot(player.x-npc.group.position.x,player.z-npc.group.position.z);if(!Number.isFinite(distance)||distance>22){npc.group.position.set(player.x-1.4,0,player.z-1.4);npc.baseX=player.x-1.4;npc.baseZ=player.z-1.4;}}}`
- [x] `if` — linha 5621, coluna 190 em `updateCoopSchoolFollowers()` — `function updateCoopSchoolFollowers(active){active.followingNpcIds=[...new Set(active.followingNpcIds||[])];for(const id of active.followingNpcIds){const npc=world.npcs.find(n=>n.id===id);if(!npc)continue;npc.following=true;const distance=Math.hypot(player.x-npc.group.position.x,player.z-npc.group.position.z);if(!Number.isFinite(distance)||distance>22){npc.group.position.set(player.x-1.4,0,player.z-1.4);npc.baseX=player.x-1.4;npc.baseZ=player.z-1.4;}}}`
- [x] `if` — linha 5621, coluna 313 em `updateCoopSchoolFollowers()` — `function updateCoopSchoolFollowers(active){active.followingNpcIds=[...new Set(active.followingNpcIds||[])];for(const id of active.followingNpcIds){const npc=world.npcs.find(n=>n.id===id);if(!npc)continue;npc.following=true;const distance=Math.hypot(player.x-npc.group.position.x,player.z-npc.group.position.z);if(!Number.isFinite(distance)||distance>22){npc.group.position.set(player.x-1.4,0,player.z-1.4);npc.baseX=player.x-1.4;npc.baseZ=player.z-1.4;}}}`
- [x] `for` — linha 5623, coluna 37 em `deactivateCoopRaceBots()` — `function deactivateCoopRaceBots(){for(const bot of coopRaceBots){bot.coopRaceMode='';bot.group.visible=false;}}`
- [x] `if` — linha 5624, coluna 97 em `updateCoopRaceVisuals()` — `function updateCoopRaceVisuals(dt){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||!['streetRace','ovalRace'].includes(record.type)||record.status!=='active')return;for(const bot of coopRaceBots){if(!bot.coopRaceMode)continue;const nearPlayer=Math.hypot(player.x-bot.group.position.x,player.z-bot.group.position.z)<1.8,pace=nearPlayer ? .18 : 1;bot.coopRaceT=(bot.coopRaceT+dt*bot.coopRaceSpeed*pace)%1;if(record.type==='ovalRace'){const a=bot.coopRaceT*Math.PI*2,rx=23.6+Number(bot.coopRaceLane||0)*1.35,rz=6.8+Number(bot.coopRaceLane||0)*.65;bot.group.position.set(51+Math.cos(a)*rx,0,75.5+Math.sin(a)*rz);bot.group.rotation.y=Math.atan2(-Math.sin(a)*rx,Math.cos(a)*rz);}else{const lane=(Number(bot.coopRaceLane||0)-1)*.8,route=[[45+lane,82],[76+lane,78],[76+lane,0],[0,lane],[-55,lane],[-55+lane,22],[45+lane,82]],scaled=bot.coopRaceT*(route.length-1),i=Math.floor(scaled),t=scaled-i,a=route[i],b=route[Math.min(route.length-1,i+1)];bot.group.position.set(lerp(a[0]`
- [x] `return` — linha 5624, coluna 193 em `updateCoopRaceVisuals()` — `function updateCoopRaceVisuals(dt){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||!['streetRace','ovalRace'].includes(record.type)||record.status!=='active')return;for(const bot of coopRaceBots){if(!bot.coopRaceMode)continue;const nearPlayer=Math.hypot(player.x-bot.group.position.x,player.z-bot.group.position.z)<1.8,pace=nearPlayer ? .18 : 1;bot.coopRaceT=(bot.coopRaceT+dt*bot.coopRaceSpeed*pace)%1;if(record.type==='ovalRace'){const a=bot.coopRaceT*Math.PI*2,rx=23.6+Number(bot.coopRaceLane||0)*1.35,rz=6.8+Number(bot.coopRaceLane||0)*.65;bot.group.position.set(51+Math.cos(a)*rx,0,75.5+Math.sin(a)*rz);bot.group.rotation.y=Math.atan2(-Math.sin(a)*rx,Math.cos(a)*rz);}else{const lane=(Number(bot.coopRaceLane||0)-1)*.8,route=[[45+lane,82],[76+lane,78],[76+lane,0],[0,lane],[-55,lane],[-55+lane,22],[45+lane,82]],scaled=bot.coopRaceT*(route.length-1),i=Math.floor(scaled),t=scaled-i,a=route[i],b=route[Math.min(route.length-1,i+1)];bot.group.position.set(lerp(a[0]`
- [x] `for` — linha 5624, coluna 200 em `updateCoopRaceVisuals()` — `function updateCoopRaceVisuals(dt){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||!['streetRace','ovalRace'].includes(record.type)||record.status!=='active')return;for(const bot of coopRaceBots){if(!bot.coopRaceMode)continue;const nearPlayer=Math.hypot(player.x-bot.group.position.x,player.z-bot.group.position.z)<1.8,pace=nearPlayer ? .18 : 1;bot.coopRaceT=(bot.coopRaceT+dt*bot.coopRaceSpeed*pace)%1;if(record.type==='ovalRace'){const a=bot.coopRaceT*Math.PI*2,rx=23.6+Number(bot.coopRaceLane||0)*1.35,rz=6.8+Number(bot.coopRaceLane||0)*.65;bot.group.position.set(51+Math.cos(a)*rx,0,75.5+Math.sin(a)*rz);bot.group.rotation.y=Math.atan2(-Math.sin(a)*rx,Math.cos(a)*rz);}else{const lane=(Number(bot.coopRaceLane||0)-1)*.8,route=[[45+lane,82],[76+lane,78],[76+lane,0],[0,lane],[-55,lane],[-55+lane,22],[45+lane,82]],scaled=bot.coopRaceT*(route.length-1),i=Math.floor(scaled),t=scaled-i,a=route[i],b=route[Math.min(route.length-1,i+1)];bot.group.position.set(lerp(a[0]`
- [x] `if` — linha 5624, coluna 231 em `updateCoopRaceVisuals()` — `function updateCoopRaceVisuals(dt){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||!['streetRace','ovalRace'].includes(record.type)||record.status!=='active')return;for(const bot of coopRaceBots){if(!bot.coopRaceMode)continue;const nearPlayer=Math.hypot(player.x-bot.group.position.x,player.z-bot.group.position.z)<1.8,pace=nearPlayer ? .18 : 1;bot.coopRaceT=(bot.coopRaceT+dt*bot.coopRaceSpeed*pace)%1;if(record.type==='ovalRace'){const a=bot.coopRaceT*Math.PI*2,rx=23.6+Number(bot.coopRaceLane||0)*1.35,rz=6.8+Number(bot.coopRaceLane||0)*.65;bot.group.position.set(51+Math.cos(a)*rx,0,75.5+Math.sin(a)*rz);bot.group.rotation.y=Math.atan2(-Math.sin(a)*rx,Math.cos(a)*rz);}else{const lane=(Number(bot.coopRaceLane||0)-1)*.8,route=[[45+lane,82],[76+lane,78],[76+lane,0],[0,lane],[-55,lane],[-55+lane,22],[45+lane,82]],scaled=bot.coopRaceT*(route.length-1),i=Math.floor(scaled),t=scaled-i,a=route[i],b=route[Math.min(route.length-1,i+1)];bot.group.position.set(lerp(a[0]`
- [x] `if` — linha 5624, coluna 438 em `updateCoopRaceVisuals()` — `function updateCoopRaceVisuals(dt){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||!['streetRace','ovalRace'].includes(record.type)||record.status!=='active')return;for(const bot of coopRaceBots){if(!bot.coopRaceMode)continue;const nearPlayer=Math.hypot(player.x-bot.group.position.x,player.z-bot.group.position.z)<1.8,pace=nearPlayer ? .18 : 1;bot.coopRaceT=(bot.coopRaceT+dt*bot.coopRaceSpeed*pace)%1;if(record.type==='ovalRace'){const a=bot.coopRaceT*Math.PI*2,rx=23.6+Number(bot.coopRaceLane||0)*1.35,rz=6.8+Number(bot.coopRaceLane||0)*.65;bot.group.position.set(51+Math.cos(a)*rx,0,75.5+Math.sin(a)*rz);bot.group.rotation.y=Math.atan2(-Math.sin(a)*rx,Math.cos(a)*rz);}else{const lane=(Number(bot.coopRaceLane||0)-1)*.8,route=[[45+lane,82],[76+lane,78],[76+lane,0],[0,lane],[-55,lane],[-55+lane,22],[45+lane,82]],scaled=bot.coopRaceT*(route.length-1),i=Math.floor(scaled),t=scaled-i,a=route[i],b=route[Math.min(route.length-1,i+1)];bot.group.position.set(lerp(a[0]`
- [x] `else` — linha 5624, coluna 708 em `updateCoopRaceVisuals()` — `function updateCoopRaceVisuals(dt){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||!['streetRace','ovalRace'].includes(record.type)||record.status!=='active')return;for(const bot of coopRaceBots){if(!bot.coopRaceMode)continue;const nearPlayer=Math.hypot(player.x-bot.group.position.x,player.z-bot.group.position.z)<1.8,pace=nearPlayer ? .18 : 1;bot.coopRaceT=(bot.coopRaceT+dt*bot.coopRaceSpeed*pace)%1;if(record.type==='ovalRace'){const a=bot.coopRaceT*Math.PI*2,rx=23.6+Number(bot.coopRaceLane||0)*1.35,rz=6.8+Number(bot.coopRaceLane||0)*.65;bot.group.position.set(51+Math.cos(a)*rx,0,75.5+Math.sin(a)*rz);bot.group.rotation.y=Math.atan2(-Math.sin(a)*rx,Math.cos(a)*rz);}else{const lane=(Number(bot.coopRaceLane||0)-1)*.8,route=[[45+lane,82],[76+lane,78],[76+lane,0],[0,lane],[-55,lane],[-55+lane,22],[45+lane,82]],scaled=bot.coopRaceT*(route.length-1),i=Math.floor(scaled),t=scaled-i,a=route[i],b=route[Math.min(route.length-1,i+1)];bot.group.position.set(lerp(a[0]`
- [x] `if` — linha 5626, coluna 181 em `updateCoopRaceProgress()` — `function updateCoopRaceProgress(active,record){const p=coopProgress(record),race=active.race||(active.race={checkpoints:0,lastCheckpoint:-1,lastAngle:null,angleTravel:0,laps:0});if(record.type==='streetRace'){`
- [x] `if` — linha 5627, coluna 125 em `updateCoopRaceProgress()` — `const points=[[45,82],[76,45],[55,0],[0,0],[-55,0]],next=Math.min(3,Number(race.checkpoints||0)),point=points[next+1];if(point&&Math.hypot(player.x-point[0],player.z-point[1])<6&&race.lastCheckpoint!==next){race.lastCheckpoint=next;race.checkpoints=next+1;coopAddCounter('street-checkpoint',1,4).catch(()=>{});toast(\`Checkpoint ${race.checkpoints}/4 registrado para a equipe.\`,'good');if(race.checkpoints<4)coopSetWaypoint({id:\`street-${race.checkpoints}\`,name:\`Checkpoint ${race.checkpoints+1}\`,icon:'🏁',x:points[race.checkpoints+1][0],z:points[race.checkpoints+1][1]});}`
- [x] `catch` — linha 5627, coluna 303 em `updateCoopRaceProgress()` — `const points=[[45,82],[76,45],[55,0],[0,0],[-55,0]],next=Math.min(3,Number(race.checkpoints||0)),point=points[next+1];if(point&&Math.hypot(player.x-point[0],player.z-point[1])<6&&race.lastCheckpoint!==next){race.lastCheckpoint=next;race.checkpoints=next+1;coopAddCounter('street-checkpoint',1,4).catch(()=>{});toast(\`Checkpoint ${race.checkpoints}/4 registrado para a equipe.\`,'good');if(race.checkpoints<4)coopSetWaypoint({id:\`street-${race.checkpoints}\`,name:\`Checkpoint ${race.checkpoints+1}\`,icon:'🏁',x:points[race.checkpoints+1][0],z:points[race.checkpoints+1][1]});}`
- [x] `if` — linha 5627, coluna 392 em `updateCoopRaceProgress()` — `const points=[[45,82],[76,45],[55,0],[0,0],[-55,0]],next=Math.min(3,Number(race.checkpoints||0)),point=points[next+1];if(point&&Math.hypot(player.x-point[0],player.z-point[1])<6&&race.lastCheckpoint!==next){race.lastCheckpoint=next;race.checkpoints=next+1;coopAddCounter('street-checkpoint',1,4).catch(()=>{});toast(\`Checkpoint ${race.checkpoints}/4 registrado para a equipe.\`,'good');if(race.checkpoints<4)coopSetWaypoint({id:\`street-${race.checkpoints}\`,name:\`Checkpoint ${race.checkpoints+1}\`,icon:'🏁',x:points[race.checkpoints+1][0],z:points[race.checkpoints+1][1]});}`
- [x] `else` — linha 5628, coluna 6 em `updateCoopRaceProgress()` — `}else{const dx=player.x-51,dz=(player.z-75.5)*3.1,angle=Math.atan2(dz,dx);if(race.lastAngle!==null){let d=angle-race.lastAngle;if(d>Math.PI)d-=Math.PI*2;if(d<-Math.PI)d+=Math.PI*2;if(Math.abs(d)<.8)race.angleTravel+=Math.abs(d);}race.lastAngle=angle;if(race.angleTravel>=Math.PI*2){race.angleTravel-=Math.PI*2;race.laps++;coopAddCounter('oval-lap',1,3).catch(()=>{});toast(\`Volta ${race.laps} registrada para a equipe.\`,'good');}}`
- [x] `if` — linha 5628, coluna 79 em `updateCoopRaceProgress()` — `}else{const dx=player.x-51,dz=(player.z-75.5)*3.1,angle=Math.atan2(dz,dx);if(race.lastAngle!==null){let d=angle-race.lastAngle;if(d>Math.PI)d-=Math.PI*2;if(d<-Math.PI)d+=Math.PI*2;if(Math.abs(d)<.8)race.angleTravel+=Math.abs(d);}race.lastAngle=angle;if(race.angleTravel>=Math.PI*2){race.angleTravel-=Math.PI*2;race.laps++;coopAddCounter('oval-lap',1,3).catch(()=>{});toast(\`Volta ${race.laps} registrada para a equipe.\`,'good');}}`
- [x] `if` — linha 5628, coluna 132 em `updateCoopRaceProgress()` — `}else{const dx=player.x-51,dz=(player.z-75.5)*3.1,angle=Math.atan2(dz,dx);if(race.lastAngle!==null){let d=angle-race.lastAngle;if(d>Math.PI)d-=Math.PI*2;if(d<-Math.PI)d+=Math.PI*2;if(Math.abs(d)<.8)race.angleTravel+=Math.abs(d);}race.lastAngle=angle;if(race.angleTravel>=Math.PI*2){race.angleTravel-=Math.PI*2;race.laps++;coopAddCounter('oval-lap',1,3).catch(()=>{});toast(\`Volta ${race.laps} registrada para a equipe.\`,'good');}}`
- [x] `if` — linha 5628, coluna 158 em `updateCoopRaceProgress()` — `}else{const dx=player.x-51,dz=(player.z-75.5)*3.1,angle=Math.atan2(dz,dx);if(race.lastAngle!==null){let d=angle-race.lastAngle;if(d>Math.PI)d-=Math.PI*2;if(d<-Math.PI)d+=Math.PI*2;if(Math.abs(d)<.8)race.angleTravel+=Math.abs(d);}race.lastAngle=angle;if(race.angleTravel>=Math.PI*2){race.angleTravel-=Math.PI*2;race.laps++;coopAddCounter('oval-lap',1,3).catch(()=>{});toast(\`Volta ${race.laps} registrada para a equipe.\`,'good');}}`
- [x] `if` — linha 5628, coluna 185 em `updateCoopRaceProgress()` — `}else{const dx=player.x-51,dz=(player.z-75.5)*3.1,angle=Math.atan2(dz,dx);if(race.lastAngle!==null){let d=angle-race.lastAngle;if(d>Math.PI)d-=Math.PI*2;if(d<-Math.PI)d+=Math.PI*2;if(Math.abs(d)<.8)race.angleTravel+=Math.abs(d);}race.lastAngle=angle;if(race.angleTravel>=Math.PI*2){race.angleTravel-=Math.PI*2;race.laps++;coopAddCounter('oval-lap',1,3).catch(()=>{});toast(\`Volta ${race.laps} registrada para a equipe.\`,'good');}}`
- [x] `if` — linha 5628, coluna 255 em `updateCoopRaceProgress()` — `}else{const dx=player.x-51,dz=(player.z-75.5)*3.1,angle=Math.atan2(dz,dx);if(race.lastAngle!==null){let d=angle-race.lastAngle;if(d>Math.PI)d-=Math.PI*2;if(d<-Math.PI)d+=Math.PI*2;if(Math.abs(d)<.8)race.angleTravel+=Math.abs(d);}race.lastAngle=angle;if(race.angleTravel>=Math.PI*2){race.angleTravel-=Math.PI*2;race.laps++;coopAddCounter('oval-lap',1,3).catch(()=>{});toast(\`Volta ${race.laps} registrada para a equipe.\`,'good');}}`
- [x] `catch` — linha 5628, coluna 358 em `updateCoopRaceProgress()` — `}else{const dx=player.x-51,dz=(player.z-75.5)*3.1,angle=Math.atan2(dz,dx);if(race.lastAngle!==null){let d=angle-race.lastAngle;if(d>Math.PI)d-=Math.PI*2;if(d<-Math.PI)d+=Math.PI*2;if(Math.abs(d)<.8)race.angleTravel+=Math.abs(d);}race.lastAngle=angle;if(race.angleTravel>=Math.PI*2){race.angleTravel-=Math.PI*2;race.laps++;coopAddCounter('oval-lap',1,3).catch(()=>{});toast(\`Volta ${race.laps} registrada para a equipe.\`,'good');}}`
- [x] `if` — linha 5629, coluna 5 em `updateCoopRaceProgress()` — `if(coopRaceTargetReached(record))coopAdvancePhase(4);`
- [x] `if` — linha 5631, coluna 86 em `updateCoopRaceProgress()` — `async function coopAdvancePhase(phase,options={}){const active=activeCoopMission();if(!active)return false;const patch={phase,...options};if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,patch);return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.phase=Math.max(Number(progress.phase||0),Number(phase||0));if(Number.isFinite(Number(options.counterSet)))progress.counter=Math.max(0,Math.min(999,Number(options.counterSet)));if(Number.isFinite(Number(options.target)))progress.target=Math.max(1,Math.min(99,Number(options.target)));progress.updatedAtClient=Date.now();return true;}`
- [x] `return` — linha 5631, coluna 97 em `updateCoopRaceProgress()` — `async function coopAdvancePhase(phase,options={}){const active=activeCoopMission();if(!active)return false;const patch={phase,...options};if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,patch);return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.phase=Math.max(Number(progress.phase||0),Number(phase||0));if(Number.isFinite(Number(options.counterSet)))progress.counter=Math.max(0,Math.min(999,Number(options.counterSet)));if(Number.isFinite(Number(options.target)))progress.target=Math.max(1,Math.min(99,Number(options.target)));progress.updatedAtClient=Date.now();return true;}`
- [x] `if` — linha 5631, coluna 141 em `updateCoopRaceProgress()` — `async function coopAdvancePhase(phase,options={}){const active=activeCoopMission();if(!active)return false;const patch={phase,...options};if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,patch);return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.phase=Math.max(Number(progress.phase||0),Number(phase||0));if(Number.isFinite(Number(options.counterSet)))progress.counter=Math.max(0,Math.min(999,Number(options.counterSet)));if(Number.isFinite(Number(options.target)))progress.target=Math.max(1,Math.min(99,Number(options.target)));progress.updatedAtClient=Date.now();return true;}`
- [x] `return` — linha 5631, coluna 239 em `updateCoopRaceProgress()` — `async function coopAdvancePhase(phase,options={}){const active=activeCoopMission();if(!active)return false;const patch={phase,...options};if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,patch);return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.phase=Math.max(Number(progress.phase||0),Number(phase||0));if(Number.isFinite(Number(options.counterSet)))progress.counter=Math.max(0,Math.min(999,Number(options.counterSet)));if(Number.isFinite(Number(options.target)))progress.target=Math.max(1,Math.min(99,Number(options.target)));progress.updatedAtClient=Date.now();return true;}`
- [x] `if` — linha 5631, coluna 430 em `updateCoopRaceProgress()` — `async function coopAdvancePhase(phase,options={}){const active=activeCoopMission();if(!active)return false;const patch={phase,...options};if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,patch);return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.phase=Math.max(Number(progress.phase||0),Number(phase||0));if(Number.isFinite(Number(options.counterSet)))progress.counter=Math.max(0,Math.min(999,Number(options.counterSet)));if(Number.isFinite(Number(options.target)))progress.target=Math.max(1,Math.min(99,Number(options.target)));progress.updatedAtClient=Date.now();return true;}`
- [x] `if` — linha 5631, coluna 547 em `updateCoopRaceProgress()` — `async function coopAdvancePhase(phase,options={}){const active=activeCoopMission();if(!active)return false;const patch={phase,...options};if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,patch);return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.phase=Math.max(Number(progress.phase||0),Number(phase||0));if(Number.isFinite(Number(options.counterSet)))progress.counter=Math.max(0,Math.min(999,Number(options.counterSet)));if(Number.isFinite(Number(options.target)))progress.target=Math.max(1,Math.min(99,Number(options.target)));progress.updatedAtClient=Date.now();return true;}`
- [x] `return` — linha 5631, coluna 690 em `updateCoopRaceProgress()` — `async function coopAdvancePhase(phase,options={}){const active=activeCoopMission();if(!active)return false;const patch={phase,...options};if(active.remote){const result=await coopBackendCall('updateCoopMissionProgress',active.id,patch);return!!result?.ok;}const progress=active.record.progress||(active.record.progress={phase:0,counter:0,target:1,events:{}});progress.phase=Math.max(Number(progress.phase||0),Number(phase||0));if(Number.isFinite(Number(options.counterSet)))progress.counter=Math.max(0,Math.min(999,Number(options.counterSet)));if(Number.isFinite(Number(options.target)))progress.target=Math.max(1,Math.min(99,Number(options.target)));progress.updatedAtClient=Date.now();return true;}`
- [x] `if` — linha 5632, coluna 104 em `updateCoopRaceProgress()` — `async function maybeCompleteCoopMission(){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||record.status!=='active')return;const template=coopMissionTemplate(record.type),p=coopProgress(record);let done=p.phase>=template.steps.length;`
- [x] `return` — linha 5632, coluna 150 em `updateCoopRaceProgress()` — `async function maybeCompleteCoopMission(){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record||record.status!=='active')return;const template=coopMissionTemplate(record.type),p=coopProgress(record);let done=p.phase>=template.steps.length;`
- [x] `if` — linha 5633, coluna 5 em `updateCoopRaceProgress()` — `if(record.type==='firefighter')done=p.counter>=4&&coopHasEvent('fire-controlled')&&coopHasEvent('safety-release');`
- [x] `else` — linha 5634, coluna 5 em `updateCoopRaceProgress()` — `else if(record.type==='paramedic')done=coopHasEvent('first-aid')&&coopHasEvent('stretcher')&&coopHasEvent('patient-loaded')&&coopHasEvent('patient-delivered');`
- [x] `if` — linha 5634, coluna 10 em `updateCoopRaceProgress()` — `else if(record.type==='paramedic')done=coopHasEvent('first-aid')&&coopHasEvent('stretcher')&&coopHasEvent('patient-loaded')&&coopHasEvent('patient-delivered');`
- [x] `else` — linha 5635, coluna 5 em `updateCoopRaceProgress()` — `else if(record.type==='police')done=coopHasEvent('approach')&&coopHasEvent('you-are-arrested')&&coopHasEvent('suspect-escorted');`
- [x] `if` — linha 5635, coluna 10 em `updateCoopRaceProgress()` — `else if(record.type==='police')done=coopHasEvent('approach')&&coopHasEvent('you-are-arrested')&&coopHasEvent('suspect-escorted');`
- [x] `else` — linha 5636, coluna 5 em `updateCoopRaceProgress()` — `else if(record.type==='fishing')done=p.phase>=2&&p.counter>=3;`
- [x] `if` — linha 5636, coluna 10 em `updateCoopRaceProgress()` — `else if(record.type==='fishing')done=p.phase>=2&&p.counter>=3;`
- [x] `else` — linha 5637, coluna 5 em `updateCoopRaceProgress()` — `else if(record.type==='school')done=p.counter>=4;`
- [x] `if` — linha 5637, coluna 10 em `updateCoopRaceProgress()` — `else if(record.type==='school')done=p.counter>=4;`
- [x] `else` — linha 5638, coluna 5 em `updateCoopRaceProgress()` — `else if(record.type==='streetRace'||record.type==='ovalRace')done=coopRaceTargetReached(record);`
- [x] `if` — linha 5638, coluna 10 em `updateCoopRaceProgress()` — `else if(record.type==='streetRace'||record.type==='ovalRace')done=coopRaceTargetReached(record);`
- [x] `if` — linha 5639, coluna 5 em `updateCoopRaceProgress()` — `if(!done||active.completionPending)return;active.completionPending=true;if(active.remote){if(record.hostUid===coopUid()){const result=await coopBackendCall('setCoopMissionStatus',active.id,'completed');if(!result?.ok)active.completionPending=false;}else active.completionPending=false;}else{active.record.status='completed';completeCoopMissionLocal(active.record);}}`
- [x] `return` — linha 5639, coluna 40 em `updateCoopRaceProgress()` — `if(!done||active.completionPending)return;active.completionPending=true;if(active.remote){if(record.hostUid===coopUid()){const result=await coopBackendCall('setCoopMissionStatus',active.id,'completed');if(!result?.ok)active.completionPending=false;}else active.completionPending=false;}else{active.record.status='completed';completeCoopMissionLocal(active.record);}}`
- [x] `if` — linha 5639, coluna 77 em `updateCoopRaceProgress()` — `if(!done||active.completionPending)return;active.completionPending=true;if(active.remote){if(record.hostUid===coopUid()){const result=await coopBackendCall('setCoopMissionStatus',active.id,'completed');if(!result?.ok)active.completionPending=false;}else active.completionPending=false;}else{active.record.status='completed';completeCoopMissionLocal(active.record);}}`
- [x] `if` — linha 5639, coluna 95 em `updateCoopRaceProgress()` — `if(!done||active.completionPending)return;active.completionPending=true;if(active.remote){if(record.hostUid===coopUid()){const result=await coopBackendCall('setCoopMissionStatus',active.id,'completed');if(!result?.ok)active.completionPending=false;}else active.completionPending=false;}else{active.record.status='completed';completeCoopMissionLocal(active.record);}}`
- [x] `if` — linha 5639, coluna 207 em `updateCoopRaceProgress()` — `if(!done||active.completionPending)return;active.completionPending=true;if(active.remote){if(record.hostUid===coopUid()){const result=await coopBackendCall('setCoopMissionStatus',active.id,'completed');if(!result?.ok)active.completionPending=false;}else active.completionPending=false;}else{active.record.status='completed';completeCoopMissionLocal(active.record);}}`
- [x] `else` — linha 5639, coluna 254 em `updateCoopRaceProgress()` — `if(!done||active.completionPending)return;active.completionPending=true;if(active.remote){if(record.hostUid===coopUid()){const result=await coopBackendCall('setCoopMissionStatus',active.id,'completed');if(!result?.ok)active.completionPending=false;}else active.completionPending=false;}else{active.record.status='completed';completeCoopMissionLocal(active.record);}}`
- [x] `else` — linha 5639, coluna 291 em `updateCoopRaceProgress()` — `if(!done||active.completionPending)return;active.completionPending=true;if(active.remote){if(record.hostUid===coopUid()){const result=await coopBackendCall('setCoopMissionStatus',active.id,'completed');if(!result?.ok)active.completionPending=false;}else active.completionPending=false;}else{active.record.status='completed';completeCoopMissionLocal(active.record);}}`
- [x] `if` — linha 5640, coluna 142 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `if` — linha 5640, coluna 213 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `else` — linha 5640, coluna 261 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `for` — linha 5640, coluna 362 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `if` — linha 5640, coluna 402 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `else` — linha 5640, coluna 509 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `if` — linha 5640, coluna 514 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `if` — linha 5640, coluna 592 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `else` — linha 5640, coluna 755 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `for` — linha 5640, coluna 840 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `if` — linha 5640, coluna 883 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `if` — linha 5640, coluna 1001 em `resolveCoopMissionScene()` — `function resolveCoopMissionScene(active=activeCoopMission(),record=activeCoopRecord(),completed=true){const scene=active?.sceneTarget?.ref;if(record?.type==='firefighter'&&scene?.active){scene.coopMissionId='';if(completed)extinguishFireIncident(scene,true);else{scene.active=false;scene.group.visible=false;scene.playerHelping=false;scene.truckHelping=false;for(const truck of world.fireTrucks||[])if(truck.targetFireId===scene.id){truck.targetFireId='';truck.serviceRoute=[];truck.serviceRouteIndex=0;}}}else if(record?.type==='paramedic'&&scene&&!scene.resolved){scene.coopMissionId='';if(completed){scene.arrived={police:true,paramedic:true,firefighter:true};scene.tasks={police:true,paramedic:true,firefighter:true};resolveTrafficIncident(scene);}else{scene.resolved=true;scene.group.visible=false;releaseIncidentActorStates(scene);for(const vehicle of serviceVehiclePools())if(vehicle.incidentTargetId===scene.id){vehicle.incidentTargetId='';vehicle.responseRoute=[];vehicle.responseIndex=0;}if`
- [x] `for` — linha 5641, coluna 61 em `resetCoopMissionNpcs()` — `function resetCoopMissionNpcs(active=activeCoopMission()){for(const npc of world.npcs||[]){if(!npc)continue;if(active?.followingNpcIds?.includes(npc.id)||active?.invitedNpcIds?.includes(npc.id)||active?.deliveredNpcIds?.includes(npc.id)||npc.coopSchoolDelivered){npc.following=false;npc.coopSchoolDelivered=false;if(npc.group)npc.group.visible=npc.coopRaceBot?false:true;}}}`
- [x] `if` — linha 5641, coluna 94 em `resetCoopMissionNpcs()` — `function resetCoopMissionNpcs(active=activeCoopMission()){for(const npc of world.npcs||[]){if(!npc)continue;if(active?.followingNpcIds?.includes(npc.id)||active?.invitedNpcIds?.includes(npc.id)||active?.deliveredNpcIds?.includes(npc.id)||npc.coopSchoolDelivered){npc.following=false;npc.coopSchoolDelivered=false;if(npc.group)npc.group.visible=npc.coopRaceBot?false:true;}}}`
- [x] `if` — linha 5641, coluna 111 em `resetCoopMissionNpcs()` — `function resetCoopMissionNpcs(active=activeCoopMission()){for(const npc of world.npcs||[]){if(!npc)continue;if(active?.followingNpcIds?.includes(npc.id)||active?.invitedNpcIds?.includes(npc.id)||active?.deliveredNpcIds?.includes(npc.id)||npc.coopSchoolDelivered){npc.following=false;npc.coopSchoolDelivered=false;if(npc.group)npc.group.visible=npc.coopRaceBot?false:true;}}}`
- [x] `if` — linha 5641, coluna 316 em `resetCoopMissionNpcs()` — `function resetCoopMissionNpcs(active=activeCoopMission()){for(const npc of world.npcs||[]){if(!npc)continue;if(active?.followingNpcIds?.includes(npc.id)||active?.invitedNpcIds?.includes(npc.id)||active?.deliveredNpcIds?.includes(npc.id)||npc.coopSchoolDelivered){npc.following=false;npc.coopSchoolDelivered=false;if(npc.group)npc.group.visible=npc.coopRaceBot?false:true;}}}`
- [x] `if` — linha 5643, coluna 81 em `completeCoopMissionLocal()` — `const active=activeCoopMission(),template=coopMissionTemplate(record?.type);if(!active||!template)return false;const token=\`coop-${active.id}\`;if(state.cooperative.completed.includes(token)){state.cooperative.active=null;return false;}state.cooperative.completed.push(token);state.cooperative.completed=state.cooperative.completed.slice(-100);state.cooperative.history.push({id:token,type:record.type,title:template.title,completedAt:Date.now(),participants:coopParticipants(record).length});state.cooperative.history=state.cooperative.history.slice(-80);addCoins(template.reward);addReputation(template.rep);addXP(120);awardMedal(\`Equipe: ${template.title}\`);resolveCoopMissionScene(active,record);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';applyAvatarCustomization();state.cooperative.active=null;state.waypoint=null;world.routePath=[];updateWaypointMarker();updateN`
- [x] `return` — linha 5643, coluna 103 em `completeCoopMissionLocal()` — `const active=activeCoopMission(),template=coopMissionTemplate(record?.type);if(!active||!template)return false;const token=\`coop-${active.id}\`;if(state.cooperative.completed.includes(token)){state.cooperative.active=null;return false;}state.cooperative.completed.push(token);state.cooperative.completed=state.cooperative.completed.slice(-100);state.cooperative.history.push({id:token,type:record.type,title:template.title,completedAt:Date.now(),participants:coopParticipants(record).length});state.cooperative.history=state.cooperative.history.slice(-80);addCoins(template.reward);addReputation(template.rep);addXP(120);awardMedal(\`Equipe: ${template.title}\`);resolveCoopMissionScene(active,record);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';applyAvatarCustomization();state.cooperative.active=null;state.waypoint=null;world.routePath=[];updateWaypointMarker();updateN`
- [x] `if` — linha 5643, coluna 148 em `completeCoopMissionLocal()` — `const active=activeCoopMission(),template=coopMissionTemplate(record?.type);if(!active||!template)return false;const token=\`coop-${active.id}\`;if(state.cooperative.completed.includes(token)){state.cooperative.active=null;return false;}state.cooperative.completed.push(token);state.cooperative.completed=state.cooperative.completed.slice(-100);state.cooperative.history.push({id:token,type:record.type,title:template.title,completedAt:Date.now(),participants:coopParticipants(record).length});state.cooperative.history=state.cooperative.history.slice(-80);addCoins(template.reward);addReputation(template.rep);addXP(120);awardMedal(\`Equipe: ${template.title}\`);resolveCoopMissionScene(active,record);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';applyAvatarCustomization();state.cooperative.active=null;state.waypoint=null;world.routePath=[];updateWaypointMarker();updateN`
- [x] `return` — linha 5643, coluna 226 em `completeCoopMissionLocal()` — `const active=activeCoopMission(),template=coopMissionTemplate(record?.type);if(!active||!template)return false;const token=\`coop-${active.id}\`;if(state.cooperative.completed.includes(token)){state.cooperative.active=null;return false;}state.cooperative.completed.push(token);state.cooperative.completed=state.cooperative.completed.slice(-100);state.cooperative.history.push({id:token,type:record.type,title:template.title,completedAt:Date.now(),participants:coopParticipants(record).length});state.cooperative.history=state.cooperative.history.slice(-80);addCoins(template.reward);addReputation(template.rep);addXP(120);awardMedal(\`Equipe: ${template.title}\`);resolveCoopMissionScene(active,record);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';applyAvatarCustomization();state.cooperative.active=null;state.waypoint=null;world.routePath=[];updateWaypointMarker();updateN`
- [x] `if` — linha 5643, coluna 704 em `completeCoopMissionLocal()` — `const active=activeCoopMission(),template=coopMissionTemplate(record?.type);if(!active||!template)return false;const token=\`coop-${active.id}\`;if(state.cooperative.completed.includes(token)){state.cooperative.active=null;return false;}state.cooperative.completed.push(token);state.cooperative.completed=state.cooperative.completed.slice(-100);state.cooperative.history.push({id:token,type:record.type,title:template.title,completedAt:Date.now(),participants:coopParticipants(record).length});state.cooperative.history=state.cooperative.history.slice(-80);addCoins(template.reward);addReputation(template.rep);addXP(120);awardMedal(\`Equipe: ${template.title}\`);resolveCoopMissionScene(active,record);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';applyAvatarCustomization();state.cooperative.active=null;state.waypoint=null;world.routePath=[];updateWaypointMarker();updateN`
- [x] `return` — linha 5643, coluna 1136 em `completeCoopMissionLocal()` — `const active=activeCoopMission(),template=coopMissionTemplate(record?.type);if(!active||!template)return false;const token=\`coop-${active.id}\`;if(state.cooperative.completed.includes(token)){state.cooperative.active=null;return false;}state.cooperative.completed.push(token);state.cooperative.completed=state.cooperative.completed.slice(-100);state.cooperative.history.push({id:token,type:record.type,title:template.title,completedAt:Date.now(),participants:coopParticipants(record).length});state.cooperative.history=state.cooperative.history.slice(-80);addCoins(template.reward);addReputation(template.rep);addXP(120);awardMedal(\`Equipe: ${template.title}\`);resolveCoopMissionScene(active,record);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';applyAvatarCustomization();state.cooperative.active=null;state.waypoint=null;world.routePath=[];updateWaypointMarker();updateN`
- [x] `if` — linha 5645, coluna 102 em `completeCoopMissionLocal()` — `async function leaveActiveCoopMission(){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return;if(!(await confirmModal('Sair da missão','A missão será encerrada somente para você; as etapas sincronizadas não serão corrompidas.','Sair','Continuar')))return;if(active.remote){if(record?.hostUid===coopUid())await coopBackendCall('setCoopMissionStatus',active.id,'cancelled');await coopBackendCall('leaveCoopMission',active.id);}resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);deactivateCoopRaceBots();resetCoopMissionNpcs(active);state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);closeModal();updateMissionHUD();toast('Você saiu da missão cooperativa.','good');}`
- [x] `return` — linha 5645, coluna 113 em `completeCoopMissionLocal()` — `async function leaveActiveCoopMission(){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return;if(!(await confirmModal('Sair da missão','A missão será encerrada somente para você; as etapas sincronizadas não serão corrompidas.','Sair','Continuar')))return;if(active.remote){if(record?.hostUid===coopUid())await coopBackendCall('setCoopMissionStatus',active.id,'cancelled');await coopBackendCall('leaveCoopMission',active.id);}resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);deactivateCoopRaceBots();resetCoopMissionNpcs(active);state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);closeModal();updateMissionHUD();toast('Você saiu da missão cooperativa.','good');}`
- [x] `if` — linha 5645, coluna 120 em `completeCoopMissionLocal()` — `async function leaveActiveCoopMission(){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return;if(!(await confirmModal('Sair da missão','A missão será encerrada somente para você; as etapas sincronizadas não serão corrompidas.','Sair','Continuar')))return;if(active.remote){if(record?.hostUid===coopUid())await coopBackendCall('setCoopMissionStatus',active.id,'cancelled');await coopBackendCall('leaveCoopMission',active.id);}resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);deactivateCoopRaceBots();resetCoopMissionNpcs(active);state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);closeModal();updateMissionHUD();toast('Você saiu da missão cooperativa.','good');}`
- [x] `return` — linha 5645, coluna 274 em `completeCoopMissionLocal()` — `async function leaveActiveCoopMission(){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return;if(!(await confirmModal('Sair da missão','A missão será encerrada somente para você; as etapas sincronizadas não serão corrompidas.','Sair','Continuar')))return;if(active.remote){if(record?.hostUid===coopUid())await coopBackendCall('setCoopMissionStatus',active.id,'cancelled');await coopBackendCall('leaveCoopMission',active.id);}resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);deactivateCoopRaceBots();resetCoopMissionNpcs(active);state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);closeModal();updateMissionHUD();toast('Você saiu da missão cooperativa.','good');}`
- [x] `if` — linha 5645, coluna 281 em `completeCoopMissionLocal()` — `async function leaveActiveCoopMission(){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return;if(!(await confirmModal('Sair da missão','A missão será encerrada somente para você; as etapas sincronizadas não serão corrompidas.','Sair','Continuar')))return;if(active.remote){if(record?.hostUid===coopUid())await coopBackendCall('setCoopMissionStatus',active.id,'cancelled');await coopBackendCall('leaveCoopMission',active.id);}resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);deactivateCoopRaceBots();resetCoopMissionNpcs(active);state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);closeModal();updateMissionHUD();toast('Você saiu da missão cooperativa.','good');}`
- [x] `if` — linha 5645, coluna 299 em `completeCoopMissionLocal()` — `async function leaveActiveCoopMission(){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return;if(!(await confirmModal('Sair da missão','A missão será encerrada somente para você; as etapas sincronizadas não serão corrompidas.','Sair','Continuar')))return;if(active.remote){if(record?.hostUid===coopUid())await coopBackendCall('setCoopMissionStatus',active.id,'cancelled');await coopBackendCall('leaveCoopMission',active.id);}resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);deactivateCoopRaceBots();resetCoopMissionNpcs(active);state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);closeModal();updateMissionHUD();toast('Você saiu da missão cooperativa.','good');}`
- [x] `if` — linha 5645, coluna 496 em `completeCoopMissionLocal()` — `async function leaveActiveCoopMission(){const active=activeCoopMission(),record=activeCoopRecord();if(!active)return;if(!(await confirmModal('Sair da missão','A missão será encerrada somente para você; as etapas sincronizadas não serão corrompidas.','Sair','Continuar')))return;if(active.remote){if(record?.hostUid===coopUid())await coopBackendCall('setCoopMissionStatus',active.id,'cancelled');await coopBackendCall('leaveCoopMission',active.id);}resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);deactivateCoopRaceBots();resetCoopMissionNpcs(active);state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);closeModal();updateMissionHUD();toast('Você saiu da missão cooperativa.','good');}`
- [x] `if` — linha 5647, coluna 103 em `openCoopLobby()` — `const active=activeCoopMission(),record=active?.id===id?activeCoopRecord():coopRemoteMissions[id];if(!record)return openCoopMissionCenter();`
- [x] `return` — linha 5647, coluna 114 em `openCoopLobby()` — `const active=activeCoopMission(),record=active?.id===id?activeCoopRecord():coopRemoteMissions[id];if(!record)return openCoopMissionCenter();`
- [x] `return` — linha 5649, coluna 142 em `openCoopLobby()` — `const roles=template.roles.map(([role,label])=>{const full=coopRoleTaken(record,role)>=coopRoleCapacity(template,role)&&own?.role!==role;return\`<button class="choice ${own?.role===role?'selected':''}" data-coop-role="${role}" ${full||running?'disabled':''}><b>${label}</b><span>${full?'Função ocupada':role==='driver'?'Conduz o veículo reservado':'Executa uma etapa definida'}</span></button>\`}).join('');`
- [x] `if` — linha 5651, coluna 1321 em `openCoopLobby()` — `openModal('Equipe da missão',\`<div class="coop-lobby"><header><span>${template.icon}</span><div><small>${modeLabel}</small><h3>${template.title}</h3><p>${template.description}</p></div></header>${offline?'<p class="coop-offline-alert">Conexão perdida. O progresso sincronizado foi preservado; você pode aguardar a reconexão ou continuar uma cópia solo.</p>':''}<section class="coop-lobby-members"><b>Equipe ${participants.length}/${record.maxPlayers}</b>${participants.map(item=>\`<article><span>${item.uid===record.hostUid?'⭐':'👤'}</span><div><b>${item.name||'Jogador'}</b><small>${coopRoleLabel(item.role)}${item.ready?' • pronto':' • aguardando'}</small></div></article>\`).join('')}</section>${running?coopMissionBriefingMarkup(record):\`<section><b>Escolha sua função</b><div class="coop-role-grid">${roles}</div></section>\`}<div class="modal-actions">${actions}</div></div>\`,root=>{$$('[data-coop-role]',root).forEach(btn=>bindCoopActionButton(btn,()=>selectCoopRole(btn.dataset.coopRole)));bindCo`
- [x] `return` — linha 5655, coluna 119 em `openCoopMissionCenter()` — `const templates=Object.values(COOP_MISSION_TEMPLATES).map(t=>{const race=['streetRace','ovalRace'].includes(t.id);return\`<article class="coop-template"><header><span>${t.icon}</span><div><b>${t.title}</b><small>${t.minPlayers}-${t.maxPlayers} jogadores • solo adaptado</small></div></header><p>${t.description}</p><div class="coop-template-roles">${t.roles.map(r=>\`<i>${r[1]}</i>\`).join('')}</div><small class="coop-required-vehicle">Veículo: ${t.vehicle}</small><footer><button class="btn primary" data-create-coop="${t.id}">Criar equipe</button>${race?\`<button class="btn good" data-create-competitive="${t.id}">Competição online</button>\`:''}<button class="btn" data-create-solo="${t.id}">Jogar solo</button></footer></article>\`}).join('');`
- [x] `return` — linha 5656, coluna 90 em `openCoopMissionCenter()` — `const lobbyMarkup=lobbies.length?lobbies.map(m=>{const t=coopMissionTemplate(m.type);return\`<article class="coop-open-lobby"><span>${t.icon}</span><div><b>${t.title}</b><small>${coopParticipants(m).length}/${m.maxPlayers} jogadores • ${m.mode==='competitive'?'competição':'equipe'}</small></div><button class="btn good" data-join-coop="${m.id}">Entrar voluntariamente</button></article>\`}).join(''):'<p class="empty-online">Nenhuma equipe aguardando neste bairro. Você pode criar uma ou jogar a versão solo.</p>';`
- [x] `if` — linha 5657, coluna 1414 em `openCoopMissionCenter()` — `openModal('Missões cooperativas',\`${active?\`${coopMissionBriefingMarkup()}<div class="modal-actions"><button class="btn primary" data-open-active-coop>Abrir missão</button><button class="btn" data-coop-gps>Marcar próximo passo no GPS</button></div>\`:''}<section class="coop-center-intro"><span>🤝</span><div><h3>Objetivos para jogar junto</h3><p>Papéis definidos, progresso sincronizado, entrada voluntária e alternativa solo quando faltarem parceiros.</p></div></section><h3 class="panel-subtitle">Equipes aguardando neste bairro</h3><div class="coop-open-lobbies">${lobbyMarkup}</div><h3 class="panel-subtitle">Criar uma missão</h3><div class="coop-template-grid">${templates}</div>\`,root=>{$$('[data-create-coop]',root).forEach(btn=>bindCoopActionButton(btn,()=>createCooperativeMission(btn.dataset.createCoop,'coop')));$$('[data-create-competitive]',root).forEach(btn=>bindCoopActionButton(btn,()=>createCooperativeMission(btn.dataset.createCompetitive,'competitive')));$$('[data-create-solo]',roo`
- [x] `for` — linha 5662, coluna 5 em `createCoopOvalTrackWorld()` — `for(const factor of[0,1,2,3]){const rx=22.6+factor*2.05,rz=5.9+factor*.95,points=[];for(let i=0;i<96;i++){const angle=i/96*Math.PI*2;points.push(new THREE.Vector3(51+Math.cos(angle)*rx,.085,75.5+Math.sin(angle)*rz));}const edge=factor===0||factor===3,line=new THREE.LineLoop(new THREE.BufferGeometry().setFromPoints(points),new THREE.LineBasicMaterial({color:edge?0xf4f7fb:0xd7dde6,transparent:true,opacity:edge ? .92 : .62}));line.name=\`coop-oval-lane-${factor}\`;worldGroup.add(line);}`
- [x] `for` — linha 5662, coluna 89 em `createCoopOvalTrackWorld()` — `for(const factor of[0,1,2,3]){const rx=22.6+factor*2.05,rz=5.9+factor*.95,points=[];for(let i=0;i<96;i++){const angle=i/96*Math.PI*2;points.push(new THREE.Vector3(51+Math.cos(angle)*rx,.085,75.5+Math.sin(angle)*rz));}const edge=factor===0||factor===3,line=new THREE.LineLoop(new THREE.BufferGeometry().setFromPoints(points),new THREE.LineBasicMaterial({color:edge?0xf4f7fb:0xd7dde6,transparent:true,opacity:edge ? .92 : .62}));line.name=\`coop-oval-lane-${factor}\`;worldGroup.add(line);}`
- [x] `return` — linha 5665, coluna 424 em `createCooperativeMissionWorld()` — `ensureCooperativeState();createCoopOvalTrackWorld();registerInteractable({id:'coop-mission-board',type:'coopMission',icon:'🤝',label:'Missões cooperativas',x:45,z:85.5,radius:3,priority:246,action:openCoopMissionCenter});coopActionInteractable=registerInteractable({id:'coop-dynamic-action',type:'coopAction',icon:'🤝',label:'Executar etapa da equipe',radius:4.8,priority:260,getPos:()=>{const target=coopCurrentTarget();return{x:Number(target?.x||9999),z:Number(target?.z||9999)}},available:()=>!!activeCoopMission()&&activeCoopRecord()?.status==='active'&&!player.vehicle,action:performCoopMissionAction});`
- [x] `for` — linha 5666, coluna 5 em `createCooperativeMissionWorld()` — `for(let i=0;i<5;i++){const bot=createNPC(\`track-bot-${i}\`,\`Atleta ${i+1}\`,45+i,82,0x55a8ff,0);bot.coopRaceBot=true;bot.coopRaceMode='';bot.group.visible=false;const interaction=(world.interactables||[]).find(item=>item.id===\`npc-track-bot-${i}\`);if(interaction)interaction.available=()=>bot.group.visible;coopRaceBots.push(bot);}`
- [x] `if` — linha 5666, coluna 251 em `createCooperativeMissionWorld()` — `for(let i=0;i<5;i++){const bot=createNPC(\`track-bot-${i}\`,\`Atleta ${i+1}\`,45+i,82,0x55a8ff,0);bot.coopRaceBot=true;bot.coopRaceMode='';bot.group.visible=false;const interaction=(world.interactables||[]).find(item=>item.id===\`npc-track-bot-${i}\`);if(interaction)interaction.available=()=>bot.group.visible;coopRaceBots.push(bot);}`
- [x] `if` — linha 5667, coluna 138 em `createCooperativeMissionWorld()` — `addEventListener('otthi:coop-missions',event=>{coopRemoteMissions={...(event.detail?.missions||{})};const active=activeCoopMission();if(active?.remote&&coopRemoteMissions[active.id]){active.record=coopRemoteMissions[active.id];if(active.record.status==='active'&&!active.runtimePrepared){active.runtimePrepared=true;prepareCoopMissionRuntime();}if(active.record.status==='completed')completeCoopMissionLocal(active.record);updateMissionHUD();}else if(!active){const waiting=Object.values(coopRemoteMissions).filter(m=>m.status==='lobby').length;if(waiting&&Date.now()-Number(state.cooperative.lastLobbyNotice||0)>30000){state.cooperative.lastLobbyNotice=Date.now();toast(\`${waiting} equipe(s) cooperativa(s) aguardando neste bairro.\`,'good',2600);}}});`
- [x] `if` — linha 5667, coluna 232 em `createCooperativeMissionWorld()` — `addEventListener('otthi:coop-missions',event=>{coopRemoteMissions={...(event.detail?.missions||{})};const active=activeCoopMission();if(active?.remote&&coopRemoteMissions[active.id]){active.record=coopRemoteMissions[active.id];if(active.record.status==='active'&&!active.runtimePrepared){active.runtimePrepared=true;prepareCoopMissionRuntime();}if(active.record.status==='completed')completeCoopMissionLocal(active.record);updateMissionHUD();}else if(!active){const waiting=Object.values(coopRemoteMissions).filter(m=>m.status==='lobby').length;if(waiting&&Date.now()-Number(state.cooperative.lastLobbyNotice||0)>30000){state.cooperative.lastLobbyNotice=Date.now();toast(\`${waiting} equipe(s) cooperativa(s) aguardando neste bairro.\`,'good',2600);}}});`
- [x] `if` — linha 5667, coluna 350 em `createCooperativeMissionWorld()` — `addEventListener('otthi:coop-missions',event=>{coopRemoteMissions={...(event.detail?.missions||{})};const active=activeCoopMission();if(active?.remote&&coopRemoteMissions[active.id]){active.record=coopRemoteMissions[active.id];if(active.record.status==='active'&&!active.runtimePrepared){active.runtimePrepared=true;prepareCoopMissionRuntime();}if(active.record.status==='completed')completeCoopMissionLocal(active.record);updateMissionHUD();}else if(!active){const waiting=Object.values(coopRemoteMissions).filter(m=>m.status==='lobby').length;if(waiting&&Date.now()-Number(state.cooperative.lastLobbyNotice||0)>30000){state.cooperative.lastLobbyNotice=Date.now();toast(\`${waiting} equipe(s) cooperativa(s) aguardando neste bairro.\`,'good',2600);}}});`
- [x] `else` — linha 5667, coluna 448 em `createCooperativeMissionWorld()` — `addEventListener('otthi:coop-missions',event=>{coopRemoteMissions={...(event.detail?.missions||{})};const active=activeCoopMission();if(active?.remote&&coopRemoteMissions[active.id]){active.record=coopRemoteMissions[active.id];if(active.record.status==='active'&&!active.runtimePrepared){active.runtimePrepared=true;prepareCoopMissionRuntime();}if(active.record.status==='completed')completeCoopMissionLocal(active.record);updateMissionHUD();}else if(!active){const waiting=Object.values(coopRemoteMissions).filter(m=>m.status==='lobby').length;if(waiting&&Date.now()-Number(state.cooperative.lastLobbyNotice||0)>30000){state.cooperative.lastLobbyNotice=Date.now();toast(\`${waiting} equipe(s) cooperativa(s) aguardando neste bairro.\`,'good',2600);}}});`
- [x] `if` — linha 5667, coluna 453 em `createCooperativeMissionWorld()` — `addEventListener('otthi:coop-missions',event=>{coopRemoteMissions={...(event.detail?.missions||{})};const active=activeCoopMission();if(active?.remote&&coopRemoteMissions[active.id]){active.record=coopRemoteMissions[active.id];if(active.record.status==='active'&&!active.runtimePrepared){active.runtimePrepared=true;prepareCoopMissionRuntime();}if(active.record.status==='completed')completeCoopMissionLocal(active.record);updateMissionHUD();}else if(!active){const waiting=Object.values(coopRemoteMissions).filter(m=>m.status==='lobby').length;if(waiting&&Date.now()-Number(state.cooperative.lastLobbyNotice||0)>30000){state.cooperative.lastLobbyNotice=Date.now();toast(\`${waiting} equipe(s) cooperativa(s) aguardando neste bairro.\`,'good',2600);}}});`
- [x] `if` — linha 5667, coluna 550 em `createCooperativeMissionWorld()` — `addEventListener('otthi:coop-missions',event=>{coopRemoteMissions={...(event.detail?.missions||{})};const active=activeCoopMission();if(active?.remote&&coopRemoteMissions[active.id]){active.record=coopRemoteMissions[active.id];if(active.record.status==='active'&&!active.runtimePrepared){active.runtimePrepared=true;prepareCoopMissionRuntime();}if(active.record.status==='completed')completeCoopMissionLocal(active.record);updateMissionHUD();}else if(!active){const waiting=Object.values(coopRemoteMissions).filter(m=>m.status==='lobby').length;if(waiting&&Date.now()-Number(state.cooperative.lastLobbyNotice||0)>30000){state.cooperative.lastLobbyNotice=Date.now();toast(\`${waiting} equipe(s) cooperativa(s) aguardando neste bairro.\`,'good',2600);}}});`
- [x] `if` — linha 5669, coluna 96 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `return` — linha 5669, coluna 116 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `if` — linha 5669, coluna 123 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `return` — linha 5669, coluna 150 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `if` — linha 5669, coluna 157 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `return` — linha 5669, coluna 222 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `if` — linha 5669, coluna 230 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `if` — linha 5669, coluna 307 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `return` — linha 5669, coluna 674 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `if` — linha 5669, coluna 682 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `return` — linha 5669, coluna 710 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `if` — linha 5669, coluna 717 em `updateCoopMissions()` — `function updateCoopMissions(dt=0){const active=activeCoopMission(),record=activeCoopRecord();if(!active||!record)return;if(record.status==='lobby')return;if(record.status==='completed'){completeCoopMissionLocal(record);return;}if(record.status==='cancelled'){resolveCoopMissionScene(active,record,false);if(active.serviceJob)releaseMissionServiceVehicle(active.serviceJob);resetCoopMissionNpcs(active);deactivateCoopRaceBots();state.avatar.uniform=active.previousUniform||'none';state.cooperative.active=null;state.waypoint=null;updateWaypointMarker();saveState(true);updateMissionHUD();toast('A missão cooperativa foi cancelada sem apagar o progresso anterior.','warn');return;}if(record.status!=='active')return;if(!active.runtimePrepared){active.runtimePrepared=true;active.previousUniform=state.avatar.uniform||'none';prepareCoopMissionRuntime();}`
- [x] `if` — linha 5670, coluna 80 em `updateCoopMissions()` — `updateCoopSchoolFollowers(active);updateCoopFishingProgress(active,record);if(record.type==='streetRace'||record.type==='ovalRace')updateCoopRaceProgress(active,record);`
- [x] `if` — linha 5671, coluna 5 em `updateCoopMissions()` — `if(coopServiceKind(record.type)&&active.serviceJob?.serviceVehicleBoarded&&!active.serviceJob.serviceVehicleArrived&&active.sceneTarget){const d=Math.hypot(player.x-(active.sceneTarget.navX??active.sceneTarget.x),player.z-(active.sceneTarget.navZ??active.sceneTarget.z));if(isDrivingServiceVehicle(record.type)&&d<6){active.serviceJob.serviceVehicleArrived=true;coopRecordEvent('vehicle-arrived','driver',1,2).catch(()=>{});toast('Veículo posicionado. Estacione, saia e cumpra sua função.','good',2800);}}`
- [x] `if` — linha 5671, coluna 276 em `updateCoopMissions()` — `if(coopServiceKind(record.type)&&active.serviceJob?.serviceVehicleBoarded&&!active.serviceJob.serviceVehicleArrived&&active.sceneTarget){const d=Math.hypot(player.x-(active.sceneTarget.navX??active.sceneTarget.x),player.z-(active.sceneTarget.navZ??active.sceneTarget.z));if(isDrivingServiceVehicle(record.type)&&d<6){active.serviceJob.serviceVehicleArrived=true;coopRecordEvent('vehicle-arrived','driver',1,2).catch(()=>{});toast('Veículo posicionado. Estacione, saia e cumpra sua função.','good',2800);}}`
- [x] `catch` — linha 5671, coluna 415 em `updateCoopMissions()` — `if(coopServiceKind(record.type)&&active.serviceJob?.serviceVehicleBoarded&&!active.serviceJob.serviceVehicleArrived&&active.sceneTarget){const d=Math.hypot(player.x-(active.sceneTarget.navX??active.sceneTarget.x),player.z-(active.sceneTarget.navZ??active.sceneTarget.z));if(isDrivingServiceVehicle(record.type)&&d<6){active.serviceJob.serviceVehicleArrived=true;coopRecordEvent('vehicle-arrived','driver',1,2).catch(()=>{});toast('Veículo posicionado. Estacione, saia e cumpra sua função.','good',2800);}}`
- [x] `if` — linha 5672, coluna 34 em `updateCoopMissions()` — `const p=coopProgress(record);if(record.type==='firefighter'&&active.sceneTarget?.ref)active.sceneTarget.ref.coopIntensity=Math.max(.08,1-Math.min(4,p.counter)/4);`
- [x] `if` — linha 5673, coluna 5 em `updateCoopMissions()` — `if(record.type==='paramedic'&&p.phase>=5&&!coopHasEvent('patient-delivered')&&!active.patientDeliveryPending){const destination=coopMedicalDestination(),d=Math.hypot(player.x-(destination.navX??destination.x),player.z-(destination.navZ??destination.z));if(isDrivingServiceVehicle('paramedic')&&d<7){active.patientDeliveryPending=true;coopRecordEvent('patient-delivered','driver',1,6).then(()=>{toast('Paciente entregue com segurança no ponto médico.','good',2800);}).finally(()=>{active.patientDeliveryPending=false;});}}`
- [x] `if` — linha 5673, coluna 258 em `updateCoopMissions()` — `if(record.type==='paramedic'&&p.phase>=5&&!coopHasEvent('patient-delivered')&&!active.patientDeliveryPending){const destination=coopMedicalDestination(),d=Math.hypot(player.x-(destination.navX??destination.x),player.z-(destination.navZ??destination.z));if(isDrivingServiceVehicle('paramedic')&&d<7){active.patientDeliveryPending=true;coopRecordEvent('patient-delivered','driver',1,6).then(()=>{toast('Paciente entregue com segurança no ponto médico.','good',2800);}).finally(()=>{active.patientDeliveryPending=false;});}}`
- [x] `catch` — linha 5674, coluna 32 em `updateCoopMissions()` — `maybeCompleteCoopMission().catch(()=>{});const label=coopMissionProgressLabel();if(label&&updateCoopMissions.lastLabel!==label.label){updateCoopMissions.lastLabel=label.label;updateMissionHUD();}const now=Date.now();if(now-Number(updateCoopMissions.lastSavedAt||0)>2500){updateCoopMissions.lastSavedAt=now;saveState();}`
- [x] `if` — linha 5674, coluna 85 em `updateCoopMissions()` — `maybeCompleteCoopMission().catch(()=>{});const label=coopMissionProgressLabel();if(label&&updateCoopMissions.lastLabel!==label.label){updateCoopMissions.lastLabel=label.label;updateMissionHUD();}const now=Date.now();if(now-Number(updateCoopMissions.lastSavedAt||0)>2500){updateCoopMissions.lastSavedAt=now;saveState();}`
- [x] `if` — linha 5674, coluna 221 em `updateCoopMissions()` — `maybeCompleteCoopMission().catch(()=>{});const label=coopMissionProgressLabel();if(label&&updateCoopMissions.lastLabel!==label.label){updateCoopMissions.lastLabel=label.label;updateMissionHUD();}const now=Date.now();if(now-Number(updateCoopMissions.lastSavedAt||0)>2500){updateCoopMissions.lastSavedAt=now;saveState();}`

## `33-otthi-world-professional-core.js`

- [x] `return` — linha 5711, coluna 5 em `ensureOtthiWorldState()` — `return state;`
- [x] `return` — linha 5714, coluna 39 em `worldPbrPath()` — `function worldPbrPath(pack,channel){return \`./assets/world/textures/${pack}-${channel}.png\`;}`
- [x] `if` — linha 5716, coluna 5 em `configureWorldTexture()` — `if(!texture)return texture;`
- [x] `return` — linha 5716, coluna 17 em `configureWorldTexture()` — `if(!texture)return texture;`
- [x] `if` — linha 5722, coluna 5 em `configureWorldTexture()` — `if(options.color){if('colorSpace'in texture&&THREE.SRGBColorSpace)texture.colorSpace=THREE.SRGBColorSpace;else texture.encoding=THREE.sRGBEncoding;}`
- [x] `if` — linha 5722, coluna 23 em `configureWorldTexture()` — `if(options.color){if('colorSpace'in texture&&THREE.SRGBColorSpace)texture.colorSpace=THREE.SRGBColorSpace;else texture.encoding=THREE.sRGBEncoding;}`
- [x] `else` — linha 5722, coluna 111 em `configureWorldTexture()` — `if(options.color){if('colorSpace'in texture&&THREE.SRGBColorSpace)texture.colorSpace=THREE.SRGBColorSpace;else texture.encoding=THREE.sRGBEncoding;}`
- [x] `return` — linha 5723, coluna 5 em `configureWorldTexture()` — `return texture;`
- [x] `if` — linha 5727, coluna 5 em `loadWorldTexture()` — `if(otthiWorldRuntime.textures.has(key))return otthiWorldRuntime.textures.get(key);`
- [x] `return` — linha 5727, coluna 44 em `loadWorldTexture()` — `if(otthiWorldRuntime.textures.has(key))return otthiWorldRuntime.textures.get(key);`
- [x] `return` — linha 5733, coluna 233 em `loadWorldTexture()` — `configureWorldTexture(texture,options);texture.name=\`OTTHI_WORLD_${pack}_${channel}\`;otthiWorldRuntime.textures.set(key,texture);registerVisualAsset(\`world-texture:${key}\`,texture,{type:'texture',status:'loading',pack,channel});return texture;`
- [x] `if` — linha 5736, coluna 35 em `ensureUv2()` — `const geometry=mesh?.geometry;if(!geometry?.attributes?.uv||geometry.attributes.uv2)return false;`
- [x] `return` — linha 5736, coluna 89 em `ensureUv2()` — `const geometry=mesh?.geometry;if(!geometry?.attributes?.uv||geometry.attributes.uv2)return false;`
- [x] `return` — linha 5737, coluna 126 em `ensureUv2()` — `const source=geometry.attributes.uv;geometry.setAttribute('uv2',new THREE.BufferAttribute(source.array,source.itemSize));return true;`
- [x] `if` — linha 5740, coluna 5 em `applyPbrPack()` — `if(!material||!packName)return material;`
- [x] `return` — linha 5740, coluna 29 em `applyPbrPack()` — `if(!material||!packName)return material;`
- [x] `if` — linha 5745, coluna 5 em `applyPbrPack()` — `if(options.replaceBase!==false)material.map=loadWorldTexture(packName,'basecolor',{repeat,color:true,nearest:!!options.nearest});`
- [x] `if` — linha 5746, coluna 5 em `applyPbrPack()` — `if(options.emissive){material.emissiveMap=loadWorldTexture(packName,'emissive',{repeat});material.emissive=material.emissive||new THREE.Color(options.emissiveColor||0xffffff);material.emissiveIntensity=Number(options.emissiveIntensity||.18);}`
- [x] `if` — linha 5748, coluna 5 em `applyPbrPack()` — `if(Number.isFinite(options.roughness))material.roughness=Number(options.roughness);`
- [x] `return` — linha 5749, coluna 198 em `applyPbrPack()` — `material.envMapIntensity=Number(options.envMapIntensity||.42);material.needsUpdate=true;material.userData={...(material.userData||{}),otthiWorldPbr:packName};otthiWorldRuntime.stats.pbrMaps+=4;return material;`
- [x] `if` — linha 5752, coluna 5 em `upgradeCoreMaterialsToWorldPbr()` — `if(!materials||otthiWorldRuntime.pbrReady)return false;`
- [x] `return` — linha 5752, coluna 47 em `upgradeCoreMaterialsToWorldPbr()` — `if(!materials||otthiWorldRuntime.pbrReady)return false;`
- [x] `for` — linha 5756, coluna 5 em `upgradeCoreMaterialsToWorldPbr()` — `for(const [key,pack,repeat,roughness]of configs)if(materials[key])applyPbrPack(materials[key],pack,{repeat,roughness,emissive:['water','city-glass','emergency-metal','gold-ore'].includes(pack),emissiveIntensity:pack==='gold-ore'?.24:.10});`
- [x] `if` — linha 5756, coluna 53 em `upgradeCoreMaterialsToWorldPbr()` — `for(const [key,pack,repeat,roughness]of configs)if(materials[key])applyPbrPack(materials[key],pack,{repeat,roughness,emissive:['water','city-glass','emergency-metal','gold-ore'].includes(pack),emissiveIntensity:pack==='gold-ore'?.24:.10});`
- [x] `if` — linha 5757, coluna 5 em `upgradeCoreMaterialsToWorldPbr()` — `if(materials.water)applyPbrPack(materials.water,'water',{repeat:[7,7],roughness:.22,emissive:true,emissiveColor:0x1b7ca7,emissiveIntensity:.12,normalScale:.34});`
- [x] `return` — linha 5758, coluna 37 em `upgradeCoreMaterialsToWorldPbr()` — `otthiWorldRuntime.pbrReady=true;return true;`
- [x] `if` — linha 5761, coluna 5 em `improveSceneMeshMaterials()` — `if(!root)return 0;let count=0;`
- [x] `return` — linha 5761, coluna 14 em `improveSceneMeshMaterials()` — `if(!root)return 0;let count=0;`
- [x] `if` — linha 5763, coluna 7 em `improveSceneMeshMaterials()` — `if(!object?.isMesh)return;ensureUv2(object);`
- [x] `return` — linha 5763, coluna 26 em `improveSceneMeshMaterials()` — `if(!object?.isMesh)return;ensureUv2(object);`
- [x] `for` — linha 5766, coluna 83 em `improveSceneMeshMaterials()` — `const list=Array.isArray(object.material)?object.material:[object.material];for(const material of list){if(!material)continue;material.dithering=true;if(material.map)configureWorldTexture(material.map,{repeat:material.map.repeat?.toArray?.()||[1,1],color:true});}`
- [x] `if` — linha 5766, coluna 111 em `improveSceneMeshMaterials()` — `const list=Array.isArray(object.material)?object.material:[object.material];for(const material of list){if(!material)continue;material.dithering=true;if(material.map)configureWorldTexture(material.map,{repeat:material.map.repeat?.toArray?.()||[1,1],color:true});}`
- [x] `if` — linha 5766, coluna 157 em `improveSceneMeshMaterials()` — `const list=Array.isArray(object.material)?object.material:[object.material];for(const material of list){if(!material)continue;material.dithering=true;if(material.map)configureWorldTexture(material.map,{repeat:material.map.repeat?.toArray?.()||[1,1],color:true});}`
- [x] `return` — linha 5769, coluna 50 em `improveSceneMeshMaterials()` — `otthiWorldRuntime.stats.enhancedMeshes=count;return count;`
- [x] `return` — linha 5772, coluna 5 em `otthiWorldStageMarkup()` — `return Object.values(OTTHI_WORLD_STAGES).map(stage=>\`<article class="otthi-world-stage implemented"><span>${stage.number}</span><div><b>${stage.title}</b><small>Implementada com fallback e preservação da base</small></div><i>✓</i></article>\`).join('');`
- [x] `for` — linha 5781, coluna 7 em `openOtthiWorldCenter()` — `for(const [selector,key]of [['[data-world-render]','worldRender'],['[data-world-details]','worldDetails'],['[data-world-day]','worldDayCycle']])$(selector,root).onchange=event=>{state.settings[key]=!!event.currentTarget.checked;saveState(true);applyOtthiWorldRuntimeSettings();};`
- [x] `if` — linha 5785, coluna 5 em `injectOtthiWorldButtons()` — `if(!document.getElementById('otthiWorldBtn')){`
- [x] `if` — linha 5789, coluna 5 em `injectOtthiWorldButtons()` — `if(!document.getElementById('otthiWorldQuickBtn')){`
- [x] `if` — linha 5795, coluna 5 em `applyOtthiWorldRuntimeSettings()` — `if(worldGroup)worldGroup.traverse(object=>{if(object.userData?.otthiWorldDetail)object.visible=state.settings?.worldDetails!==false;});`
- [x] `if` — linha 5795, coluna 48 em `applyOtthiWorldRuntimeSettings()` — `if(worldGroup)worldGroup.traverse(object=>{if(object.userData?.otthiWorldDetail)object.visible=state.settings?.worldDetails!==false;});`
- [x] `if` — linha 5796, coluna 5 em `applyOtthiWorldRuntimeSettings()` — `if(renderer){renderer.toneMappingExposure=enabled?1.02:.94;renderer.shadowMap.enabled=enabled&&qualityTier()==='high'&&!perf.mobile;}`
- [x] `return` — linha 5799, coluna 36 em `otthiWorldDiagnostics()` — `function otthiWorldDiagnostics(){return{version:OTTHI_WORLD_VERSION,build:OTTHI_WORLD_BUILD,stages:OTTHI_WORLD_STAGES,runtime:{...otthiWorldRuntime,assets:undefined,textures:otthiWorldRuntime.textures.size,materials:otthiWorldRuntime.materials.size},state:{avatar:{...state.avatar},vehicles:{modularParts:Object.keys(state.vehicles?.modularParts||{}).length},hero:{...state.adventures?.hero}},firebaseConfigPreserved:true};}`

## `34-avatar-studio-professional-v3.js`

- [x] `return` — linha 5815, coluna 57 em `worldAvatarSafeChoice()` — `const options=OTTHI_WORLD_AVATAR_CATALOG[field]||[];return options.some(item=>item[0]===value)?value:(options[0]?.[0]||'none');`
- [x] `if` — linha 5818, coluna 32 em `disposeWorldAvatarObject()` — `object?.traverse?.(child=>{if(child.isMesh){child.geometry?.dispose?.();const list=Array.isArray(child.material)?child.material:[child.material];for(const material of list){if(material?.userData?.otthiWorldAvatarMaterial)material.dispose?.();}}});`
- [x] `for` — linha 5818, coluna 150 em `disposeWorldAvatarObject()` — `object?.traverse?.(child=>{if(child.isMesh){child.geometry?.dispose?.();const list=Array.isArray(child.material)?child.material:[child.material];for(const material of list){if(material?.userData?.otthiWorldAvatarMaterial)material.dispose?.();}}});`
- [x] `if` — linha 5818, coluna 178 em `disposeWorldAvatarObject()` — `object?.traverse?.(child=>{if(child.isMesh){child.geometry?.dispose?.();const list=Array.isArray(child.material)?child.material:[child.material];for(const material of list){if(material?.userData?.otthiWorldAvatarMaterial)material.dispose?.();}}});`
- [x] `for` — linha 5822, coluna 5 em `clearWorldAvatarV3()` — `for(const attachment of attachments){attachment.parent?.remove(attachment);disposeWorldAvatarObject(attachment);}`
- [x] `if` — linha 5823, coluna 5 em `clearWorldAvatarV3()` — `if(playerModel)playerModel.userData.worldAvatarV3Attachments=[];`
- [x] `return` — linha 5826, coluna 390 em `worldAvatarMaterial()` — `const material=new THREE.MeshStandardMaterial({color:new THREE.Color(color),roughness:Number(options.roughness??.48),metalness:Number(options.metalness??.04),transparent:!!options.transparent,opacity:Number(options.opacity??1),emissive:new THREE.Color(options.emissive??0x000000),emissiveIntensity:Number(options.emissiveIntensity??0)});material.userData.otthiWorldAvatarMaterial=true;return material;`
- [x] `return` — linha 5829, coluna 251 em `worldAvatarLayer()` — `const group=new THREE.Group();group.name=\`OTTHI_WORLD_AVATAR_V3_${name}\`;parent?.add(group);playerModel.userData.worldAvatarV3Attachments=playerModel.userData.worldAvatarV3Attachments||[];playerModel.userData.worldAvatarV3Attachments.push(group);return group;`
- [x] `if` — linha 5831, coluna 211 em `avatarV3Box()` — `function avatarV3Box(parent,w,h,d,material,x=0,y=0,z=0){const mesh=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),material);mesh.position.set(x,y,z);mesh.castShadow=true;mesh.receiveShadow=true;parent.add(mesh);if(state.settings?.worldOutlines!==false)addVoxelOutline(mesh,0x102238,.22);return mesh;}`
- [x] `return` — linha 5831, coluna 287 em `avatarV3Box()` — `function avatarV3Box(parent,w,h,d,material,x=0,y=0,z=0){const mesh=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),material);mesh.position.set(x,y,z);mesh.castShadow=true;mesh.receiveShadow=true;parent.add(mesh);if(state.settings?.worldOutlines!==false)addVoxelOutline(mesh,0x102238,.22);return mesh;}`
- [x] `return` — linha 5832, coluna 254 em `avatarV3Sphere()` — `function avatarV3Sphere(parent,r,material,x=0,y=0,z=0,sx=1,sy=1,sz=1){const mesh=new THREE.Mesh(new THREE.SphereGeometry(r,12,8),material);mesh.position.set(x,y,z);mesh.scale.set(sx,sy,sz);mesh.castShadow=true;mesh.receiveShadow=true;parent.add(mesh);return mesh;}`
- [x] `return` — linha 5833, coluna 33 em `avatarPatternColor()` — `function avatarPatternColor(){return state.avatar.pattern==='world-star'?0xffd84d:state.avatar.pattern==='world-pixels'?0x5ad7ff:state.avatar.pattern==='world-stripe'?0xffffff:state.avatar.secondaryColor;}`
- [x] `if` — linha 5835, coluna 5 em `applyWorldAvatarV3()` — `if(!playerModel?.userData?.parts)return false;ensureOtthiWorldState();clearWorldAvatarV3();`
- [x] `return` — linha 5835, coluna 38 em `applyWorldAvatarV3()` — `if(!playerModel?.userData?.parts)return false;ensureOtthiWorldState();clearWorldAvatarV3();`
- [x] `if` — linha 5840, coluna 5 em `applyWorldAvatarV3()` — `if(avatar.bodyStyle==='toy'){`
- [x] `else` — linha 5842, coluna 6 em `applyWorldAvatarV3()` — `}else if(avatar.bodyStyle==='hero'){`
- [x] `if` — linha 5842, coluna 11 em `applyWorldAvatarV3()` — `}else if(avatar.bodyStyle==='hero'){`
- [x] `if` — linha 5845, coluna 5 em `applyWorldAvatarV3()` — `if(avatar.torso==='world-jacket-01'){`
- [x] `else` — linha 5847, coluna 6 em `applyWorldAvatarV3()` — `}else if(avatar.torso==='world-hoodie-01'){`
- [x] `if` — linha 5847, coluna 11 em `applyWorldAvatarV3()` — `}else if(avatar.torso==='world-hoodie-01'){`
- [x] `else` — linha 5849, coluna 6 em `applyWorldAvatarV3()` — `}else if(avatar.torso==='world-explorer-01'){`
- [x] `if` — linha 5849, coluna 11 em `applyWorldAvatarV3()` — `}else if(avatar.torso==='world-explorer-01'){`
- [x] `else` — linha 5851, coluna 6 em `applyWorldAvatarV3()` — `}else{`
- [x] `if` — linha 5854, coluna 5 em `applyWorldAvatarV3()` — `if(avatar.pattern==='world-stripe'){avatarV3Box(bodyLayer,1.13,.15,.11,accent,0,.23,.48);avatarV3Box(bodyLayer,1.13,.15,.11,accent,0,-.04,.48);}`
- [x] `if` — linha 5855, coluna 5 em `applyWorldAvatarV3()` — `if(avatar.pattern==='world-pixels')for(const [x,y]of[[-.3,.26],[0,.04],[.3,.26],[-.3,-.18],[.3,-.18]])avatarV3Box(bodyLayer,.16,.16,.11,accent,x,y,.48);`
- [x] `for` — linha 5855, coluna 40 em `applyWorldAvatarV3()` — `if(avatar.pattern==='world-pixels')for(const [x,y]of[[-.3,.26],[0,.04],[.3,.26],[-.3,-.18],[.3,-.18]])avatarV3Box(bodyLayer,.16,.16,.11,accent,x,y,.48);`
- [x] `if` — linha 5856, coluna 5 em `applyWorldAvatarV3()` — `if(avatar.pattern==='world-star'){const star=new THREE.Mesh(new THREE.CircleGeometry(.23,5),accent);star.position.set(0,.1,.49);star.rotation.z=Math.PI/10;bodyLayer.add(star);}`
- [x] `if` — linha 5857, coluna 5 em `applyWorldAvatarV3()` — `if(avatar.legs==='world-shorts-01'){`
- [x] `else` — linha 5859, coluna 6 em `applyWorldAvatarV3()` — `}else if(avatar.legs==='world-armor-01'){`
- [x] `if` — linha 5859, coluna 11 em `applyWorldAvatarV3()` — `}else if(avatar.legs==='world-armor-01'){`
- [x] `else` — linha 5861, coluna 6 em `applyWorldAvatarV3()` — `}else{`
- [x] `if` — linha 5866, coluna 5 em `applyWorldAvatarV3()` — `if(avatar.shoes==='world-energy-01'){avatarV3Box(leftLeg,.34,.08,.63,energy,0,-1.16,.11);avatarV3Box(rightLeg,.34,.08,.63,energy,0,-1.16,.11);}`
- [x] `if` — linha 5867, coluna 5 em `applyWorldAvatarV3()` — `if(avatar.hair==='hair-short-01'){avatarV3Box(headLayer,1.13,.24,1.13,hairMat,0,.49,0);avatarV3Box(headLayer,.24,.42,1.08,hairMat,-.48,.29,-.02);avatarV3Box(headLayer,.24,.42,1.08,hairMat,.48,.29,-.02);}`
- [x] `else` — linha 5868, coluna 5 em `applyWorldAvatarV3()` — `else if(avatar.hair==='hair-spikes-01'){avatarV3Box(headLayer,1.13,.18,1.10,hairMat,0,.48,0);for(const x of[-.38,-.12,.14,.4]){const spike=new THREE.Mesh(new THREE.ConeGeometry(.16,.48,4),hairMat);spike.position.set(x,.76,-.03);spike.rotation.y=Math.PI/4;headLayer.add(spike);}}`
- [x] `if` — linha 5868, coluna 10 em `applyWorldAvatarV3()` — `else if(avatar.hair==='hair-spikes-01'){avatarV3Box(headLayer,1.13,.18,1.10,hairMat,0,.48,0);for(const x of[-.38,-.12,.14,.4]){const spike=new THREE.Mesh(new THREE.ConeGeometry(.16,.48,4),hairMat);spike.position.set(x,.76,-.03);spike.rotation.y=Math.PI/4;headLayer.add(spike);}}`
- [x] `for` — linha 5868, coluna 98 em `applyWorldAvatarV3()` — `else if(avatar.hair==='hair-spikes-01'){avatarV3Box(headLayer,1.13,.18,1.10,hairMat,0,.48,0);for(const x of[-.38,-.12,.14,.4]){const spike=new THREE.Mesh(new THREE.ConeGeometry(.16,.48,4),hairMat);spike.position.set(x,.76,-.03);spike.rotation.y=Math.PI/4;headLayer.add(spike);}}`
- [x] `else` — linha 5869, coluna 5 em `applyWorldAvatarV3()` — `else if(avatar.hair==='hair-curls-01'){for(const [x,y,z]of[[-.4,.48,0],[-.14,.57,.04],[.14,.57,.04],[.4,.48,0],[-.48,.28,-.05],[.48,.28,-.05]])avatarV3Sphere(headLayer,.22,hairMat,x,y,z);}`
- [x] `if` — linha 5869, coluna 10 em `applyWorldAvatarV3()` — `else if(avatar.hair==='hair-curls-01'){for(const [x,y,z]of[[-.4,.48,0],[-.14,.57,.04],[.14,.57,.04],[.4,.48,0],[-.48,.28,-.05],[.48,.28,-.05]])avatarV3Sphere(headLayer,.22,hairMat,x,y,z);}`
- [x] `for` — linha 5869, coluna 44 em `applyWorldAvatarV3()` — `else if(avatar.hair==='hair-curls-01'){for(const [x,y,z]of[[-.4,.48,0],[-.14,.57,.04],[.14,.57,.04],[.4,.48,0],[-.48,.28,-.05],[.48,.28,-.05]])avatarV3Sphere(headLayer,.22,hairMat,x,y,z);}`
- [x] `if` — linha 5873, coluna 5 em `applyWorldAvatarV3()` — `if(expression==='face-brave-01'){leftBrow.rotation.z=-.18;rightBrow.rotation.z=.18;}`
- [x] `if` — linha 5874, coluna 5 em `applyWorldAvatarV3()` — `if(expression==='face-curious-01'){leftBrow.position.y=.25;rightBrow.rotation.z=.14;}`
- [x] `if` — linha 5875, coluna 142 em `applyWorldAvatarV3()` — `const mouth=avatarV3Box(headLayer,expression==='face-focus-01'?.26:.34,.05,.04,worldAvatarMaterial(0xff6f81,{roughness:.45}),0,-.22,.54);if(expression==='face-happy-01')mouth.rotation.z=.02;`
- [x] `if` — linha 5876, coluna 5 em `applyWorldAvatarV3()` — `if(avatar.back==='world-backpack-01'){const pack=worldAvatarLayer(parts.body,'BACKPACK');avatarV3Box(pack,.82,.92,.38,secondary,0,.02,-.54);avatarV3Box(pack,.55,.24,.15,accent,0,-.22,-.76);}`
- [x] `else` — linha 5877, coluna 5 em `applyWorldAvatarV3()` — `else if(avatar.back==='world-cape-01'){const cape=worldAvatarLayer(parts.body,'CAPE');avatarV3Box(cape,.94,1.34,.06,primary,0,-.18,-.49).rotation.x=-.08;avatarV3Box(cape,.52,.14,.08,accent,0,.42,-.5);}`
- [x] `if` — linha 5877, coluna 10 em `applyWorldAvatarV3()` — `else if(avatar.back==='world-cape-01'){const cape=worldAvatarLayer(parts.body,'CAPE');avatarV3Box(cape,.94,1.34,.06,primary,0,-.18,-.49).rotation.x=-.08;avatarV3Box(cape,.52,.14,.08,accent,0,.42,-.5);}`
- [x] `else` — linha 5878, coluna 5 em `applyWorldAvatarV3()` — `else if(avatar.back==='world-jetpack-01'){const jet=worldAvatarLayer(parts.body,'JETPACK');avatarV3Box(jet,.34,.82,.36,secondary,-.28,.02,-.54);avatarV3Box(jet,.34,.82,.36,secondary,.28,.02,-.54);avatarV3Box(jet,.22,.22,.3,energy,-.28,-.48,-.56);avatarV3Box(jet,.22,.22,.3,energy,.28,-.48,-.56);}`
- [x] `if` — linha 5878, coluna 10 em `applyWorldAvatarV3()` — `else if(avatar.back==='world-jetpack-01'){const jet=worldAvatarLayer(parts.body,'JETPACK');avatarV3Box(jet,.34,.82,.36,secondary,-.28,.02,-.54);avatarV3Box(jet,.34,.82,.36,secondary,.28,.02,-.54);avatarV3Box(jet,.22,.22,.3,energy,-.28,-.48,-.56);avatarV3Box(jet,.22,.22,.3,energy,.28,-.48,-.56);}`
- [x] `return` — linha 5879, coluna 40 em `applyWorldAvatarV3()` — `otthiWorldRuntime.avatarReady=true;return true;`
- [x] `return` — linha 5882, coluna 40 em `worldAvatarOptions()` — `const selected=state.avatar[field];return \`<section class="avatar-section world-avatar-section"><h3>${title}</h3><div class="avatar-grid">${OTTHI_WORLD_AVATAR_CATALOG[field].map(([id,name,icon])=>\`<button class="avatar-option ${selected===id?'selected':''}" data-world-avatar-field="${field}" data-world-avatar-value="${id}"><b>${icon}</b><span>${name}</span></button>\`).join('')}</div></section>\`;`

## `35-world-render-pbr-environment.js`

- [x] `return` — linha 5899, coluna 59 em `otthiSeededRandom()` — `function otthiSeededRandom(seed=700){let value=seed>>>0;return()=>{value=(value*1664525+1013904223)>>>0;return value/4294967296;};}`
- [x] `return` — linha 5899, coluna 107 em `otthiSeededRandom()` — `function otthiSeededRandom(seed=700){let value=seed>>>0;return()=>{value=(value*1664525+1013904223)>>>0;return value/4294967296;};}`
- [x] `if` — linha 5901, coluna 5 em `worldDetailAllowed()` — `if(Math.abs(x)<12||Math.abs(z)<12)return false;`
- [x] `return` — linha 5901, coluna 39 em `worldDetailAllowed()` — `if(Math.abs(x)<12||Math.abs(z)<12)return false;`
- [x] `if` — linha 5902, coluna 5 em `worldDetailAllowed()` — `if(waterAt(x,z))return false;`
- [x] `return` — linha 5902, coluna 21 em `worldDetailAllowed()` — `if(waterAt(x,z))return false;`
- [x] `if` — linha 5903, coluna 5 em `worldDetailAllowed()` — `if(world.colliders?.some(c=>Math.abs(x-c.x)<c.w/2+1.1&&Math.abs(z-c.z)<c.d/2+1.1))return false;`
- [x] `return` — linha 5903, coluna 87 em `worldDetailAllowed()` — `if(world.colliders?.some(c=>Math.abs(x-c.x)<c.w/2+1.1&&Math.abs(z-c.z)<c.d/2+1.1))return false;`
- [x] `if` — linha 5904, coluna 5 em `worldDetailAllowed()` — `if(world.houses?.some(h=>Math.abs(x-h.x)<7&&Math.abs(z-h.z)<6))return false;`
- [x] `return` — linha 5904, coluna 68 em `worldDetailAllowed()` — `if(world.houses?.some(h=>Math.abs(x-h.x)<7&&Math.abs(z-h.z)<6))return false;`
- [x] `return` — linha 5905, coluna 5 em `worldDetailAllowed()` — `return Math.abs(x)<108&&Math.abs(z)<108;`
- [x] `if` — linha 5908, coluna 5 em `createWorldInstancedDetails()` — `if(!worldGroup||world.worldProfessional?.details)return false;const random=otthiSeededRandom(700031),quality=qualityTier(),grassCount=quality==='low'?100:quality==='high'?360:220,flowerCount=quality==='low'?30:quality==='high'?120:72;`
- [x] `return` — linha 5908, coluna 54 em `createWorldInstancedDetails()` — `if(!worldGroup||world.worldProfessional?.details)return false;const random=otthiSeededRandom(700031),quality=qualityTier(),grassCount=quality==='low'?100:quality==='high'?360:220,flowerCount=quality==='low'?30:quality==='high'?120:72;`
- [x] `while` — linha 5911, coluna 5 em `createWorldInstancedDetails()` — `while(placed<grassCount&&attempts<grassCount*16){attempts++;const x=(random()-.5)*208,z=(random()-.5)*208;if(!worldDetailAllowed(x,z))continue;dummy.position.set(x,.025,z);dummy.rotation.set(0,random()*Math.PI*2,0);const scale=.55+random()*.8;dummy.scale.set(scale,scale,scale);dummy.updateMatrix();grass.setMatrixAt(placed++,dummy.matrix);}`
- [x] `if` — linha 5911, coluna 111 em `createWorldInstancedDetails()` — `while(placed<grassCount&&attempts<grassCount*16){attempts++;const x=(random()-.5)*208,z=(random()-.5)*208;if(!worldDetailAllowed(x,z))continue;dummy.position.set(x,.025,z);dummy.rotation.set(0,random()*Math.PI*2,0);const scale=.55+random()*.8;dummy.scale.set(scale,scale,scale);dummy.updateMatrix();grass.setMatrixAt(placed++,dummy.matrix);}`
- [x] `while` — linha 5914, coluna 5 em `createWorldInstancedDetails()` — `while(placed<flowerCount&&attempts<flowerCount*18){attempts++;const x=(random()-.5)*198,z=(random()-.5)*198;if(!worldDetailAllowed(x,z))continue;dummy.position.set(x,.18,z);dummy.rotation.set(random()*Math.PI,random()*Math.PI,random()*Math.PI);const scale=.7+random()*.8;dummy.scale.set(scale,scale,scale);dummy.updateMatrix();flowers.setMatrixAt(placed++,dummy.matrix);}`
- [x] `if` — linha 5914, coluna 113 em `createWorldInstancedDetails()` — `while(placed<flowerCount&&attempts<flowerCount*18){attempts++;const x=(random()-.5)*198,z=(random()-.5)*198;if(!worldDetailAllowed(x,z))continue;dummy.position.set(x,.18,z);dummy.rotation.set(random()*Math.PI,random()*Math.PI,random()*Math.PI);const scale=.7+random()*.8;dummy.scale.set(scale,scale,scale);dummy.updateMatrix();flowers.setMatrixAt(placed++,dummy.matrix);}`
- [x] `return` — linha 5915, coluna 240 em `createWorldInstancedDetails()` — `flowers.count=placed;flowers.instanceMatrix.needsUpdate=true;worldGroup.add(flowers);world.worldProfessional={...(world.worldProfessional||{}),details:{grass,flowers}};otthiWorldRuntime.stats.instancedDetails=grass.count+flowers.count;return true;`
- [x] `return` — linha 5920, coluna 294 em `createWorldMushroomCluster()` — `const body=new THREE.Mesh(new THREE.CylinderGeometry(.18,.27,.8,8),stem);body.position.y=.4;body.castShadow=true;group.add(body);const top=new THREE.Mesh(new THREE.SphereGeometry(.5,12,8,0,Math.PI*2,0,Math.PI*.55),cap);top.position.y=.92;top.scale.y=.65;top.castShadow=true;group.add(top);return group;`
- [x] `if` — linha 5923, coluna 5 em `enhanceWorldHouses()` — `if(!world?.houses||world.worldProfessional?.housesEnhanced)return 0;let count=0;`
- [x] `return` — linha 5923, coluna 64 em `enhanceWorldHouses()` — `if(!world?.houses||world.worldProfessional?.housesEnhanced)return 0;let count=0;`
- [x] `for` — linha 5924, coluna 5 em `enhanceWorldHouses()` — `for(const house of world.houses){`
- [x] `for` — linha 5926, coluna 7 em `enhanceWorldHouses()` — `for(const side of[-1,1]){premiumBox(.20,3.05,.24,trim,house.x+side*4.16,1.55,house.z+3.42,house.front,0x142033);premiumBox(.38,.18,7.2,dark,house.x+side*4.48,2.92,house.z,house.roof,0x142033);}`
- [x] `if` — linha 5928, coluna 7 em `enhanceWorldHouses()` — `if(!house.publicBuilding){for(const ox of[-2.55,2.55]){const planter=new THREE.Group();planter.userData.otthiWorldDetail=true;worldGroup.add(planter);premiumBox(1.25,.32,.45,materials.wood,house.x+ox,.82,house.z+3.76,planter,0x142033);for(const dx of[-.36,0,.36]){premiumBox(.07,.38,.07,0x3f8f47,house.x+ox+dx,1.12,house.z+3.76,planter,0x14341c);premiumBox(.20,.14,.20,dx===0?0xffdf55:0xff6f9e,house.x+ox+dx,1.36,house.z+3.76,planter,0x4a1b2c);}}}`
- [x] `for` — linha 5928, coluna 33 em `enhanceWorldHouses()` — `if(!house.publicBuilding){for(const ox of[-2.55,2.55]){const planter=new THREE.Group();planter.userData.otthiWorldDetail=true;worldGroup.add(planter);premiumBox(1.25,.32,.45,materials.wood,house.x+ox,.82,house.z+3.76,planter,0x142033);for(const dx of[-.36,0,.36]){premiumBox(.07,.38,.07,0x3f8f47,house.x+ox+dx,1.12,house.z+3.76,planter,0x14341c);premiumBox(.20,.14,.20,dx===0?0xffdf55:0xff6f9e,house.x+ox+dx,1.36,house.z+3.76,planter,0x4a1b2c);}}}`
- [x] `for` — linha 5928, coluna 242 em `enhanceWorldHouses()` — `if(!house.publicBuilding){for(const ox of[-2.55,2.55]){const planter=new THREE.Group();planter.userData.otthiWorldDetail=true;worldGroup.add(planter);premiumBox(1.25,.32,.45,materials.wood,house.x+ox,.82,house.z+3.76,planter,0x142033);for(const dx of[-.36,0,.36]){premiumBox(.07,.38,.07,0x3f8f47,house.x+ox+dx,1.12,house.z+3.76,planter,0x14341c);premiumBox(.20,.14,.20,dx===0?0xffdf55:0xff6f9e,house.x+ox+dx,1.36,house.z+3.76,planter,0x4a1b2c);}}}`
- [x] `return` — linha 5931, coluna 84 em `enhanceWorldHouses()` — `world.worldProfessional={...(world.worldProfessional||{}),housesEnhanced:true};return count;`
- [x] `if` — linha 5934, coluna 5 em `createWorldAtmosphericDetails()` — `if(world.worldProfessional?.atmosphere)return false;const group=new THREE.Group();group.name='OTTHI_WORLD_ATMOSPHERE';group.userData.otthiWorldDetail=true;scene.add(group);`
- [x] `return` — linha 5934, coluna 44 em `createWorldAtmosphericDetails()` — `if(world.worldProfessional?.atmosphere)return false;const group=new THREE.Group();group.name='OTTHI_WORLD_ATMOSPHERE';group.userData.otthiWorldDetail=true;scene.add(group);`
- [x] `for` — linha 5935, coluna 194 em `createWorldAtmosphericDetails()` — `const particleMaterial=new THREE.PointsMaterial({color:0xffef9c,size:.16,transparent:true,opacity:.62,depthWrite:false,sizeAttenuation:true});const points=[],random=otthiSeededRandom(7721);for(let i=0;i<(qualityTier()==='low'?24:64);i++)points.push(new THREE.Vector3(-92+random()*58,.8+random()*7,-78+random()*48));const geometry=new THREE.BufferGeometry().setFromPoints(points),particles=new THREE.Points(geometry,particleMaterial);group.add(particles);`
- [x] `return` — linha 5937, coluna 103 em `createWorldAtmosphericDetails()` — `world.worldProfessional={...(world.worldProfessional||{}),atmosphere:{group,particles,portalMat}};return true;`
- [x] `if` — linha 5940, coluna 5 em `createWorldLandmarks()` — `if(world.worldProfessional?.landmarks)return false;const items=[];`
- [x] `return` — linha 5940, coluna 43 em `createWorldLandmarks()` — `if(world.worldProfessional?.landmarks)return false;const items=[];`
- [x] `for` — linha 5941, coluna 5 em `createWorldLandmarks()` — `for(const [x,z,s]of[[-98,-18,.75],[-85,-28,.5],[-75,-62,.62],[-52,68,.58],[36,66,.5],[92,10,.68]])items.push(createWorldMushroomCluster(x,z,s));`
- [x] `return` — linha 5943, coluna 80 em `createWorldLandmarks()` — `world.worldProfessional={...(world.worldProfessional||{}),landmarks:items};return true;`
- [x] `if` — linha 5946, coluna 5 em `createOtthiWorldProfessionalLayer()` — `if(!worldGroup||otthiWorldRuntime.worldLayerReady)return false;ensureOtthiWorldState();upgradeCoreMaterialsToWorldPbr();improveSceneMeshMaterials(worldGroup);enhanceWorldHouses();createWorldInstancedDetails();createWorldAtmosphericDetails();createWorldLandmarks();`
- [x] `return` — linha 5946, coluna 55 em `createOtthiWorldProfessionalLayer()` — `if(!worldGroup||otthiWorldRuntime.worldLayerReady)return false;ensureOtthiWorldState();upgradeCoreMaterialsToWorldPbr();improveSceneMeshMaterials(worldGroup);enhanceWorldHouses();createWorldInstancedDetails();createWorldAtmosphericDetails();createWorldLandmarks();`
- [x] `if` — linha 5947, coluna 5 em `createOtthiWorldProfessionalLayer()` — `if(renderer){renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.02;renderer.outputEncoding=THREE.sRGBEncoding;}`
- [x] `return` — linha 5948, coluna 162 em `createOtthiWorldProfessionalLayer()` — `scene.background=new THREE.Color(0x70c8ef);scene.fog=new THREE.Fog(0xaadcf1,210,520);applyOtthiWorldRuntimeSettings();otthiWorldRuntime.worldLayerReady=true;return true;`
- [x] `if` — linha 5951, coluna 5 em `updateOtthiWorldEnvironment()` — `if(!otthiWorldRuntime.worldLayerReady)return;otthiWorldRuntime.updateTime+=dt;const time=otthiWorldRuntime.updateTime;`
- [x] `return` — linha 5951, coluna 43 em `updateOtthiWorldEnvironment()` — `if(!otthiWorldRuntime.worldLayerReady)return;otthiWorldRuntime.updateTime+=dt;const time=otthiWorldRuntime.updateTime;`
- [x] `if` — linha 5952, coluna 41 em `updateOtthiWorldEnvironment()` — `const waterMaterial=materials.water;if(waterMaterial?.normalMap){waterMaterial.normalMap.offset.x=(waterMaterial.normalMap.offset.x+dt*.008)%1;waterMaterial.normalMap.offset.y=(waterMaterial.normalMap.offset.y+dt*.005)%1;}`
- [x] `if` — linha 5953, coluna 58 em `updateOtthiWorldEnvironment()` — `const atmosphere=world.worldProfessional?.atmosphere;if(atmosphere?.particles){atmosphere.particles.rotation.y=time*.018;atmosphere.particles.position.y=Math.sin(time*.35)*.14;}`
- [x] `if` — linha 5954, coluna 5 em `updateOtthiWorldEnvironment()` — `if(state.settings?.worldDayCycle&&sunLight){const cycle=(Math.sin(time*.018)+1)*.5,angle=time*.012;sunLight.position.set(Math.cos(angle)*52,30+cycle*26,Math.sin(angle)*48);sunLight.intensity=.88+cycle*.55;scene.background.setHSL(.55,.65,.58+cycle*.12);if(scene.fog)scene.fog.color.copy(scene.background).lerp(new THREE.Color(0xd9eeff),.45);}`
- [x] `if` — linha 5954, coluna 257 em `updateOtthiWorldEnvironment()` — `if(state.settings?.worldDayCycle&&sunLight){const cycle=(Math.sin(time*.018)+1)*.5,angle=time*.012;sunLight.position.set(Math.cos(angle)*52,30+cycle*26,Math.sin(angle)*48);sunLight.intensity=.88+cycle*.55;scene.background.setHSL(.55,.65,.58+cycle*.12);if(scene.fog)scene.fog.color.copy(scene.background).lerp(new THREE.Color(0xd9eeff),.45);}`

## `36-modular-build-machines.js`

- [x] `if` — linha 5972, coluna 105 em `updateOtthiWorldEnvironment()` — `buildFootprint=function buildFootprintWorld(type,rotation=0){const base=OTTHI_WORLD_BUILD_SIZES[type];if(!base)return legacyBuildFootprint(type,rotation);return Math.abs(Math.sin(Number(rotation||0)))>.7?{w:base.d,d:base.w}:{...base};};`
- [x] `return` — linha 5972, coluna 114 em `updateOtthiWorldEnvironment()` — `buildFootprint=function buildFootprintWorld(type,rotation=0){const base=OTTHI_WORLD_BUILD_SIZES[type];if(!base)return legacyBuildFootprint(type,rotation);return Math.abs(Math.sin(Number(rotation||0)))>.7?{w:base.d,d:base.w}:{...base};};`
- [x] `return` — linha 5972, coluna 157 em `updateOtthiWorldEnvironment()` — `buildFootprint=function buildFootprintWorld(type,rotation=0){const base=OTTHI_WORLD_BUILD_SIZES[type];if(!base)return legacyBuildFootprint(type,rotation);return Math.abs(Math.sin(Number(rotation||0)))>.7?{w:base.d,d:base.w}:{...base};};`
- [x] `if` — linha 5975, coluna 5 em `updateOtthiWorldEnvironment()` — `if(!OTTHI_WORLD_BUILD_SIZES[type])return legacyCreateBuildPreviewMesh(type);if(!window.THREE||!worldGroup)return null;const group=new THREE.Group(),material=new THREE.MeshBasicMaterial({color:0x64ef78,transparent:true,opacity:.42,depthWrite:false,depthTest:true});`
- [x] `return` — linha 5975, coluna 39 em `updateOtthiWorldEnvironment()` — `if(!OTTHI_WORLD_BUILD_SIZES[type])return legacyCreateBuildPreviewMesh(type);if(!window.THREE||!worldGroup)return null;const group=new THREE.Group(),material=new THREE.MeshBasicMaterial({color:0x64ef78,transparent:true,opacity:.42,depthWrite:false,depthTest:true});`
- [x] `if` — linha 5975, coluna 81 em `updateOtthiWorldEnvironment()` — `if(!OTTHI_WORLD_BUILD_SIZES[type])return legacyCreateBuildPreviewMesh(type);if(!window.THREE||!worldGroup)return null;const group=new THREE.Group(),material=new THREE.MeshBasicMaterial({color:0x64ef78,transparent:true,opacity:.42,depthWrite:false,depthTest:true});`
- [x] `return` — linha 5975, coluna 111 em `updateOtthiWorldEnvironment()` — `if(!OTTHI_WORLD_BUILD_SIZES[type])return legacyCreateBuildPreviewMesh(type);if(!window.THREE||!worldGroup)return null;const group=new THREE.Group(),material=new THREE.MeshBasicMaterial({color:0x64ef78,transparent:true,opacity:.42,depthWrite:false,depthTest:true});`
- [x] `return` — linha 5976, coluna 180 em `updateOtthiWorldEnvironment()` — `const part=(w,h,d,x,y,z)=>{const mesh=new THREE.Mesh(sharedBoxGeometry(w,h,d),material);mesh.position.set(x,y,z);mesh.renderOrder=990;mesh.frustumCulled=false;group.add(mesh);return mesh;};`
- [x] `if` — linha 5977, coluna 5 em `updateOtthiWorldEnvironment()` — `if(type==='roof'){part(3,.35,3,0,.18,0);part(2.3,.35,2.5,0,.48,0);part(1.5,.35,2,0,.78,0);}`
- [x] `else` — linha 5978, coluna 5 em `updateOtthiWorldEnvironment()` — `else if(type==='door'){part(1.7,2.5,.35,0,1.25,0);part(1.2,2.15,.38,0,1.08,.02);}`
- [x] `if` — linha 5978, coluna 10 em `updateOtthiWorldEnvironment()` — `else if(type==='door'){part(1.7,2.5,.35,0,1.25,0);part(1.2,2.15,.38,0,1.08,.02);}`
- [x] `else` — linha 5979, coluna 5 em `updateOtthiWorldEnvironment()` — `else if(type==='window'){part(2,1.55,.32,0,.78,0);part(1.55,1.15,.36,0,.78,.02);}`
- [x] `if` — linha 5979, coluna 10 em `updateOtthiWorldEnvironment()` — `else if(type==='window'){part(2,1.55,.32,0,.78,0);part(1.55,1.15,.36,0,.78,.02);}`
- [x] `else` — linha 5980, coluna 5 em `updateOtthiWorldEnvironment()` — `else if(type==='stairs')for(let i=0;i<4;i++)part(2.5,.28+i*.28,.72,0,(.28+i*.28)/2,-1.05+i*.7);`
- [x] `if` — linha 5980, coluna 10 em `updateOtthiWorldEnvironment()` — `else if(type==='stairs')for(let i=0;i<4;i++)part(2.5,.28+i*.28,.72,0,(.28+i*.28)/2,-1.05+i*.7);`
- [x] `for` — linha 5980, coluna 29 em `updateOtthiWorldEnvironment()` — `else if(type==='stairs')for(let i=0;i<4;i++)part(2.5,.28+i*.28,.72,0,(.28+i*.28)/2,-1.05+i*.7);`
- [x] `else` — linha 5981, coluna 5 em `updateOtthiWorldEnvironment()` — `else if(type==='table'){part(2.2,.18,1.1,0,1,0);for(const x of[-.85,.85])for(const z of[-.38,.38])part(.15,.92,.15,x,.46,z);}`
- [x] `if` — linha 5981, coluna 10 em `updateOtthiWorldEnvironment()` — `else if(type==='table'){part(2.2,.18,1.1,0,1,0);for(const x of[-.85,.85])for(const z of[-.38,.38])part(.15,.92,.15,x,.46,z);}`
- [x] `for` — linha 5981, coluna 53 em `updateOtthiWorldEnvironment()` — `else if(type==='table'){part(2.2,.18,1.1,0,1,0);for(const x of[-.85,.85])for(const z of[-.38,.38])part(.15,.92,.15,x,.46,z);}`
- [x] `for` — linha 5981, coluna 78 em `updateOtthiWorldEnvironment()` — `else if(type==='table'){part(2.2,.18,1.1,0,1,0);for(const x of[-.85,.85])for(const z of[-.38,.38])part(.15,.92,.15,x,.46,z);}`
- [x] `else` — linha 5982, coluna 5 em `updateOtthiWorldEnvironment()` — `else if(type==='chair'){part(.9,.16,.86,0,.62,0);part(.9,.82,.16,0,1.02,-.34);for(const x of[-.32,.32])part(.12,.58,.12,x,.29,0);}`
- [x] `if` — linha 5982, coluna 10 em `updateOtthiWorldEnvironment()` — `else if(type==='chair'){part(.9,.16,.86,0,.62,0);part(.9,.82,.16,0,1.02,-.34);for(const x of[-.32,.32])part(.12,.58,.12,x,.29,0);}`
- [x] `for` — linha 5982, coluna 83 em `updateOtthiWorldEnvironment()` — `else if(type==='chair'){part(.9,.16,.86,0,.62,0);part(.9,.82,.16,0,1.02,-.34);for(const x of[-.32,.32])part(.12,.58,.12,x,.29,0);}`
- [x] `else` — linha 5983, coluna 5 em `updateOtthiWorldEnvironment()` — `else if(type==='crate'){part(1.2,1.2,1.2,0,.6,0);}`
- [x] `if` — linha 5983, coluna 10 em `updateOtthiWorldEnvironment()` — `else if(type==='crate'){part(1.2,1.2,1.2,0,.6,0);}`
- [x] `else` — linha 5984, coluna 5 em `updateOtthiWorldEnvironment()` — `else if(type==='mushroom'){part(.42,.9,.42,0,.45,0);part(1.35,.48,1.35,0,1.05,0);}`
- [x] `if` — linha 5984, coluna 10 em `updateOtthiWorldEnvironment()` — `else if(type==='mushroom'){part(.42,.9,.42,0,.45,0);part(1.35,.48,1.35,0,1.05,0);}`
- [x] `else` — linha 5985, coluna 5 em `updateOtthiWorldEnvironment()` — `else if(type==='heroPad'){part(2.8,.28,2.8,0,.14,0);part(1.9,.18,1.9,0,.32,0);}`
- [x] `if` — linha 5985, coluna 10 em `updateOtthiWorldEnvironment()` — `else if(type==='heroPad'){part(2.8,.28,2.8,0,.14,0);part(1.9,.18,1.9,0,.32,0);}`
- [x] `else` — linha 5986, coluna 5 em `updateOtthiWorldEnvironment()` — `else{part(3,.92,1.1,0,.46,0);part(2.7,.18,1.25,0,1,0);}`
- [x] `return` — linha 5987, coluna 99 em `updateOtthiWorldEnvironment()` — `group.userData.previewMaterial=material;group.userData.previewType=type;worldGroup.add(group);return group;`
- [x] `return` — linha 5989, coluna 249 em `registerWorldBuildRecord()` — `function registerWorldBuildRecord(data,mesh,extras=[],baseY=0){mesh.userData.buildId=data.id;mesh.userData.buildType=data.type;const record={data:{...data,groundY:baseY},mesh,extras,signature:buildRecordSignature(data)};world.builds.push(record);return record;}`
- [x] `if` — linha 5992, coluna 53 em `registerWorldBuildRecord()` — `const preliminary=normalizeBuildRecord(rawData);if(!preliminary||!OTTHI_WORLD_BUILD_SIZES[preliminary.type])return legacySpawnBuild(rawData,persist);if(!worldGroup)return null;const existing=world.builds.find(item=>item.data.id===preliminary.id);if(existing)return existing;`
- [x] `return` — linha 5992, coluna 113 em `registerWorldBuildRecord()` — `const preliminary=normalizeBuildRecord(rawData);if(!preliminary||!OTTHI_WORLD_BUILD_SIZES[preliminary.type])return legacySpawnBuild(rawData,persist);if(!worldGroup)return null;const existing=world.builds.find(item=>item.data.id===preliminary.id);if(existing)return existing;`
- [x] `if` — linha 5992, coluna 154 em `registerWorldBuildRecord()` — `const preliminary=normalizeBuildRecord(rawData);if(!preliminary||!OTTHI_WORLD_BUILD_SIZES[preliminary.type])return legacySpawnBuild(rawData,persist);if(!worldGroup)return null;const existing=world.builds.find(item=>item.data.id===preliminary.id);if(existing)return existing;`
- [x] `return` — linha 5992, coluna 169 em `registerWorldBuildRecord()` — `const preliminary=normalizeBuildRecord(rawData);if(!preliminary||!OTTHI_WORLD_BUILD_SIZES[preliminary.type])return legacySpawnBuild(rawData,persist);if(!worldGroup)return null;const existing=world.builds.find(item=>item.data.id===preliminary.id);if(existing)return existing;`
- [x] `if` — linha 5992, coluna 251 em `registerWorldBuildRecord()` — `const preliminary=normalizeBuildRecord(rawData);if(!preliminary||!OTTHI_WORLD_BUILD_SIZES[preliminary.type])return legacySpawnBuild(rawData,persist);if(!worldGroup)return null;const existing=world.builds.find(item=>item.data.id===preliminary.id);if(existing)return existing;`
- [x] `return` — linha 5992, coluna 263 em `registerWorldBuildRecord()` — `const preliminary=normalizeBuildRecord(rawData);if(!preliminary||!OTTHI_WORLD_BUILD_SIZES[preliminary.type])return legacySpawnBuild(rawData,persist);if(!worldGroup)return null;const existing=world.builds.find(item=>item.data.id===preliminary.id);if(existing)return existing;`
- [x] `if` — linha 5994, coluna 5 em `registerWorldBuildRecord()` — `if(data.type==='roof'){premiumBox(3,.34,3,materials.roof,0,.17,0,group);premiumBox(2.35,.34,2.55,materials.roof,0,.49,0,group);premiumBox(1.65,.34,2.05,materials.roof,0,.81,0,group);registerPlatform(data.x,data.z,3,3,baseY+1,{buildId:data.id});}`
- [x] `else` — linha 5995, coluna 5 em `registerWorldBuildRecord()` — `else if(data.type==='door'){premiumBox(1.7,2.5,.35,materials.stone,0,1.25,0,group);premiumBox(1.22,2.15,.39,materials.wood,0,1.08,.03,group);premiumCylinder(.07,.08,0xffd84d,.4,1.08,.24,group,10);const size=oriented(1.7,.38);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `if` — linha 5995, coluna 10 em `registerWorldBuildRecord()` — `else if(data.type==='door'){premiumBox(1.7,2.5,.35,materials.stone,0,1.25,0,group);premiumBox(1.22,2.15,.39,materials.wood,0,1.08,.03,group);premiumCylinder(.07,.08,0xffd84d,.4,1.08,.24,group,10);const size=oriented(1.7,.38);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `else` — linha 5996, coluna 5 em `registerWorldBuildRecord()` — `else if(data.type==='window'){premiumBox(2,1.55,.32,materials.wood,0,.78,0,group);const glass=new THREE.MeshStandardMaterial({map:loadWorldTexture('city-glass','basecolor',{repeat:[1,1],color:true}),normalMap:loadWorldTexture('city-glass','normal',{repeat:[1,1]}),roughnessMap:loadWorldTexture('city-glass','roughness',{repeat:[1,1]}),color:0x8fdfff,transparent:true,opacity:.72,roughness:.18,metalness:.16});premiumBox(1.55,1.12,.36,glass,0,.78,.03,group,0x204b69);premiumBox(.08,1.12,.4,materials.wood,0,.78,.05,group);premiumBox(1.55,.08,.4,materials.wood,0,.78,.05,group);}`
- [x] `if` — linha 5996, coluna 10 em `registerWorldBuildRecord()` — `else if(data.type==='window'){premiumBox(2,1.55,.32,materials.wood,0,.78,0,group);const glass=new THREE.MeshStandardMaterial({map:loadWorldTexture('city-glass','basecolor',{repeat:[1,1],color:true}),normalMap:loadWorldTexture('city-glass','normal',{repeat:[1,1]}),roughnessMap:loadWorldTexture('city-glass','roughness',{repeat:[1,1]}),color:0x8fdfff,transparent:true,opacity:.72,roughness:.18,metalness:.16});premiumBox(1.55,1.12,.36,glass,0,.78,.03,group,0x204b69);premiumBox(.08,1.12,.4,materials.wood,0,.78,.05,group);premiumBox(1.55,.08,.4,materials.wood,0,.78,.05,group);}`
- [x] `else` — linha 5997, coluna 5 em `registerWorldBuildRecord()` — `else if(data.type==='stairs'){for(let i=0;i<4;i++){const height=.28+i*.28,z=-1.05+i*.7;premiumBox(2.5,height,.72,materials.wood,0,height/2,z,group);const worldZ=data.z+Math.cos(rotation)*z,worldX=data.x+Math.sin(rotation)*z,size=oriented(2.5,.72);registerPlatform(worldX,worldZ,size.w,size.d,baseY+height,{buildId:data.id});}}`
- [x] `if` — linha 5997, coluna 10 em `registerWorldBuildRecord()` — `else if(data.type==='stairs'){for(let i=0;i<4;i++){const height=.28+i*.28,z=-1.05+i*.7;premiumBox(2.5,height,.72,materials.wood,0,height/2,z,group);const worldZ=data.z+Math.cos(rotation)*z,worldX=data.x+Math.sin(rotation)*z,size=oriented(2.5,.72);registerPlatform(worldX,worldZ,size.w,size.d,baseY+height,{buildId:data.id});}}`
- [x] `for` — linha 5997, coluna 35 em `registerWorldBuildRecord()` — `else if(data.type==='stairs'){for(let i=0;i<4;i++){const height=.28+i*.28,z=-1.05+i*.7;premiumBox(2.5,height,.72,materials.wood,0,height/2,z,group);const worldZ=data.z+Math.cos(rotation)*z,worldX=data.x+Math.sin(rotation)*z,size=oriented(2.5,.72);registerPlatform(worldX,worldZ,size.w,size.d,baseY+height,{buildId:data.id});}}`
- [x] `else` — linha 5998, coluna 5 em `registerWorldBuildRecord()` — `else if(data.type==='table'){premiumBox(2.2,.18,1.1,materials.wood,0,1,0,group);for(const x of[-.85,.85])for(const z of[-.38,.38])premiumBox(.15,.92,.15,materials.wood,x,.46,z,group);const size=oriented(2.2,1.2);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `if` — linha 5998, coluna 10 em `registerWorldBuildRecord()` — `else if(data.type==='table'){premiumBox(2.2,.18,1.1,materials.wood,0,1,0,group);for(const x of[-.85,.85])for(const z of[-.38,.38])premiumBox(.15,.92,.15,materials.wood,x,.46,z,group);const size=oriented(2.2,1.2);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `for` — linha 5998, coluna 85 em `registerWorldBuildRecord()` — `else if(data.type==='table'){premiumBox(2.2,.18,1.1,materials.wood,0,1,0,group);for(const x of[-.85,.85])for(const z of[-.38,.38])premiumBox(.15,.92,.15,materials.wood,x,.46,z,group);const size=oriented(2.2,1.2);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `for` — linha 5998, coluna 110 em `registerWorldBuildRecord()` — `else if(data.type==='table'){premiumBox(2.2,.18,1.1,materials.wood,0,1,0,group);for(const x of[-.85,.85])for(const z of[-.38,.38])premiumBox(.15,.92,.15,materials.wood,x,.46,z,group);const size=oriented(2.2,1.2);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `else` — linha 5999, coluna 5 em `registerWorldBuildRecord()` — `else if(data.type==='chair'){premiumBox(.9,.16,.86,materials.wood,0,.62,0,group);premiumBox(.9,.82,.16,materials.wood,0,1.02,-.34,group);for(const x of[-.32,.32])premiumBox(.12,.58,.12,materials.wood,x,.29,0,group);const size=oriented(1,1);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `if` — linha 5999, coluna 10 em `registerWorldBuildRecord()` — `else if(data.type==='chair'){premiumBox(.9,.16,.86,materials.wood,0,.62,0,group);premiumBox(.9,.82,.16,materials.wood,0,1.02,-.34,group);for(const x of[-.32,.32])premiumBox(.12,.58,.12,materials.wood,x,.29,0,group);const size=oriented(1,1);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `for` — linha 5999, coluna 142 em `registerWorldBuildRecord()` — `else if(data.type==='chair'){premiumBox(.9,.16,.86,materials.wood,0,.62,0,group);premiumBox(.9,.82,.16,materials.wood,0,1.02,-.34,group);for(const x of[-.32,.32])premiumBox(.12,.58,.12,materials.wood,x,.29,0,group);const size=oriented(1,1);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `else` — linha 6000, coluna 5 em `registerWorldBuildRecord()` — `else if(data.type==='crate'){premiumBox(1.2,1.2,1.2,materials.wood,0,.6,0,group);for(const r of[-.52,.52]){premiumBox(.12,1.22,.12,0x5b341c,r,.61,.52,group);premiumBox(.12,1.22,.12,0x5b341c,r,.61,-.52,group);}registerPlatform(data.x,data.z,1.2,1.2,baseY+1.2,{buildId:data.id});registerCollider(data.x,data.z,1.2,1.2,{buildId:data.id});}`
- [x] `if` — linha 6000, coluna 10 em `registerWorldBuildRecord()` — `else if(data.type==='crate'){premiumBox(1.2,1.2,1.2,materials.wood,0,.6,0,group);for(const r of[-.52,.52]){premiumBox(.12,1.22,.12,0x5b341c,r,.61,.52,group);premiumBox(.12,1.22,.12,0x5b341c,r,.61,-.52,group);}registerPlatform(data.x,data.z,1.2,1.2,baseY+1.2,{buildId:data.id});registerCollider(data.x,data.z,1.2,1.2,{buildId:data.id});}`
- [x] `for` — linha 6000, coluna 86 em `registerWorldBuildRecord()` — `else if(data.type==='crate'){premiumBox(1.2,1.2,1.2,materials.wood,0,.6,0,group);for(const r of[-.52,.52]){premiumBox(.12,1.22,.12,0x5b341c,r,.61,.52,group);premiumBox(.12,1.22,.12,0x5b341c,r,.61,-.52,group);}registerPlatform(data.x,data.z,1.2,1.2,baseY+1.2,{buildId:data.id});registerCollider(data.x,data.z,1.2,1.2,{buildId:data.id});}`
- [x] `else` — linha 6001, coluna 5 em `registerWorldBuildRecord()` — `else if(data.type==='mushroom'){const stem=worldAvatarMaterial(0xe7d0aa,{roughness:.88});const cap=new THREE.MeshStandardMaterial({map:loadWorldTexture('mushroom','basecolor',{repeat:[1,1],color:true,nearest:true}),normalMap:loadWorldTexture('mushroom','normal',{repeat:[1,1]}),roughnessMap:loadWorldTexture('mushroom','roughness',{repeat:[1,1]}),roughness:.74});const trunk=new THREE.Mesh(new THREE.CylinderGeometry(.2,.3,.9,8),stem);trunk.position.y=.45;group.add(trunk);const top=new THREE.Mesh(new THREE.SphereGeometry(.7,12,8,0,Math.PI*2,0,Math.PI*.58),cap);top.position.y=1.12;top.scale.y=.65;group.add(top);registerCollider(data.x,data.z,1.1,1.1,{buildId:data.id});}`
- [x] `if` — linha 6001, coluna 10 em `registerWorldBuildRecord()` — `else if(data.type==='mushroom'){const stem=worldAvatarMaterial(0xe7d0aa,{roughness:.88});const cap=new THREE.MeshStandardMaterial({map:loadWorldTexture('mushroom','basecolor',{repeat:[1,1],color:true,nearest:true}),normalMap:loadWorldTexture('mushroom','normal',{repeat:[1,1]}),roughnessMap:loadWorldTexture('mushroom','roughness',{repeat:[1,1]}),roughness:.74});const trunk=new THREE.Mesh(new THREE.CylinderGeometry(.2,.3,.9,8),stem);trunk.position.y=.45;group.add(trunk);const top=new THREE.Mesh(new THREE.SphereGeometry(.7,12,8,0,Math.PI*2,0,Math.PI*.58),cap);top.position.y=1.12;top.scale.y=.65;group.add(top);registerCollider(data.x,data.z,1.1,1.1,{buildId:data.id});}`
- [x] `else` — linha 6002, coluna 5 em `registerWorldBuildRecord()` — `else if(data.type==='heroPad'){const energy=otthiWorldRuntime.materials.get('heroEnergy')||new THREE.MeshStandardMaterial({color:0x4fddff,emissive:0x147ca2,emissiveIntensity:.65,roughness:.2});premiumBox(2.8,.28,2.8,materials.stone,0,.14,0,group);premiumBox(1.95,.18,1.95,energy,0,.34,0,group,0x0b4b66);extras.push(addGlow(data.x,baseY+.55,data.z,0x45ddff,4));registerPlatform(data.x,data.z,2.8,2.8,baseY+.43,{buildId:data.id});}`
- [x] `if` — linha 6002, coluna 10 em `registerWorldBuildRecord()` — `else if(data.type==='heroPad'){const energy=otthiWorldRuntime.materials.get('heroEnergy')||new THREE.MeshStandardMaterial({color:0x4fddff,emissive:0x147ca2,emissiveIntensity:.65,roughness:.2});premiumBox(2.8,.28,2.8,materials.stone,0,.14,0,group);premiumBox(1.95,.18,1.95,energy,0,.34,0,group,0x0b4b66);extras.push(addGlow(data.x,baseY+.55,data.z,0x45ddff,4));registerPlatform(data.x,data.z,2.8,2.8,baseY+.43,{buildId:data.id});}`
- [x] `else` — linha 6003, coluna 5 em `registerWorldBuildRecord()` — `else if(data.type==='garageRack'){premiumBox(3,.92,1.1,materials.wood,0,.46,0,group);premiumBox(2.75,.18,1.25,materials.emergencyMetal,0,1,0,group);for(const x of[-1.15,1.15])premiumBox(.18,1.85,.18,materials.emergencyMetal,x,.93,0,group);for(const [x,c]of[[-.72,0xf2c94c],[0,0x4fc3f7],[.72,0xef5350]])premiumBox(.42,.28,.34,c,x,1.24,0,group);const size=oriented(3,1.2);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `if` — linha 6003, coluna 10 em `registerWorldBuildRecord()` — `else if(data.type==='garageRack'){premiumBox(3,.92,1.1,materials.wood,0,.46,0,group);premiumBox(2.75,.18,1.25,materials.emergencyMetal,0,1,0,group);for(const x of[-1.15,1.15])premiumBox(.18,1.85,.18,materials.emergencyMetal,x,.93,0,group);for(const [x,c]of[[-.72,0xf2c94c],[0,0x4fc3f7],[.72,0xef5350]])premiumBox(.42,.28,.34,c,x,1.24,0,group);const size=oriented(3,1.2);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `for` — linha 6003, coluna 153 em `registerWorldBuildRecord()` — `else if(data.type==='garageRack'){premiumBox(3,.92,1.1,materials.wood,0,.46,0,group);premiumBox(2.75,.18,1.25,materials.emergencyMetal,0,1,0,group);for(const x of[-1.15,1.15])premiumBox(.18,1.85,.18,materials.emergencyMetal,x,.93,0,group);for(const [x,c]of[[-.72,0xf2c94c],[0,0x4fc3f7],[.72,0xef5350]])premiumBox(.42,.28,.34,c,x,1.24,0,group);const size=oriented(3,1.2);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `for` — linha 6003, coluna 244 em `registerWorldBuildRecord()` — `else if(data.type==='garageRack'){premiumBox(3,.92,1.1,materials.wood,0,.46,0,group);premiumBox(2.75,.18,1.25,materials.emergencyMetal,0,1,0,group);for(const x of[-1.15,1.15])premiumBox(.18,1.85,.18,materials.emergencyMetal,x,.93,0,group);for(const [x,c]of[[-.72,0xf2c94c],[0,0x4fc3f7],[.72,0xef5350]])premiumBox(.42,.28,.34,c,x,1.24,0,group);const size=oriented(3,1.2);registerCollider(data.x,data.z,size.w,size.d,{buildId:data.id});}`
- [x] `if` — linha 6004, coluna 29 em `registerWorldBuildRecord()` — `group.traverse(object=>{if(object.isMesh){object.castShadow=true;object.receiveShadow=true;ensureUv2(object);}});return registerWorldBuildRecord(data,group,extras,baseY);`
- [x] `return` — linha 6004, coluna 118 em `registerWorldBuildRecord()` — `group.traverse(object=>{if(object.isMesh){object.castShadow=true;object.receiveShadow=true;ensureUv2(object);}});return registerWorldBuildRecord(data,group,extras,baseY);`
- [x] `return` — linha 6015, coluna 39 em `defaultWorldVehicleParts()` — `function defaultWorldVehicleParts(){return{body:'classic',hood:'flat',roof:'standard',wheels:'city',rear:'none',lights:'warm',primary:'#f28a22',secondary:'#0aa7b8',accent:'#f5d84d'};}`
- [x] `return` — linha 6016, coluna 377 em `worldVehicleParts()` — `function worldVehicleParts(vehicleId){ensureOtthiWorldState();const current=state.vehicles.modularParts[vehicleId]||{};const next={...defaultWorldVehicleParts(),...current};state.vehicles.modularParts[vehicleId]=next;state.vehicles.partDurability[vehicleId]=Number.isFinite(state.vehicles.partDurability[vehicleId])?clamp(state.vehicles.partDurability[vehicleId],0,100):100;return next;}`
- [x] `if` — linha 6017, coluna 102 em `removeWorldVehicleModules()` — `function removeWorldVehicleModules(group){const previous=group?.userData?.otthiWorldVehicleModules;if(previous){group.remove(previous);disposeWorldAvatarObject(previous);}if(group?.userData)group.userData.otthiWorldVehicleModules=null;}`
- [x] `if` — linha 6017, coluna 174 em `removeWorldVehicleModules()` — `function removeWorldVehicleModules(group){const previous=group?.userData?.otthiWorldVehicleModules;if(previous){group.remove(previous);disposeWorldAvatarObject(previous);}if(group?.userData)group.userData.otthiWorldVehicleModules=null;}`
- [x] `return` — linha 6018, coluna 432 em `worldVehicleModuleMaterial()` — `function worldVehicleModuleMaterial(color,metalness=.18,roughness=.42){const material=new THREE.MeshStandardMaterial({map:loadWorldTexture('toy-plastic','basecolor',{repeat:[1,1],color:true}),normalMap:loadWorldTexture('toy-plastic','normal',{repeat:[1,1]}),roughnessMap:loadWorldTexture('toy-plastic','roughness',{repeat:[1,1]}),color:new THREE.Color(color),metalness,roughness});material.userData.otthiWorldAvatarMaterial=true;return material;}`
- [x] `if` — linha 6020, coluna 5 em `applyWorldVehicleModulesToGroup()` — `if(!group||!vehicleId)return false;removeWorldVehicleModules(group);const parts=worldVehicleParts(vehicleId),durability=Number(state.vehicles.partDurability[vehicleId]||100),modules=new THREE.Group();modules.name=\`OTTHI_WORLD_VEHICLE_${vehicleId}\`;group.add(modules);group.userData.otthiWorldVehicleModules=modules;`
- [x] `return` — linha 6020, coluna 27 em `applyWorldVehicleModulesToGroup()` — `if(!group||!vehicleId)return false;removeWorldVehicleModules(group);const parts=worldVehicleParts(vehicleId),durability=Number(state.vehicles.partDurability[vehicleId]||100),modules=new THREE.Group();modules.name=\`OTTHI_WORLD_VEHICLE_${vehicleId}\`;group.add(modules);group.userData.otthiWorldVehicleModules=modules;`
- [x] `if` — linha 6022, coluna 194 em `applyWorldVehicleModulesToGroup()` — `const part=(w,h,d,material,x,y,z)=>{const mesh=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),material);mesh.position.set(x,y,z);mesh.castShadow=true;mesh.receiveShadow=true;modules.add(mesh);if(state.settings?.worldOutlines!==false)addVoxelOutline(mesh,0x102238,.20);return mesh;};`
- [x] `return` — linha 6022, coluna 270 em `applyWorldVehicleModulesToGroup()` — `const part=(w,h,d,material,x,y,z)=>{const mesh=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),material);mesh.position.set(x,y,z);mesh.castShadow=true;mesh.receiveShadow=true;modules.add(mesh);if(state.settings?.worldOutlines!==false)addVoxelOutline(mesh,0x102238,.20);return mesh;};`
- [x] `if` — linha 6023, coluna 5 em `applyWorldVehicleModulesToGroup()` — `if(parts.body==='sport'){part(2.02,.16,.34,primary,0,.22,1.42);part(2.05,.14,.30,primary,0,.22,-1.42);part(.24,.28,2.45,secondary,-1.0,.38,0);part(.24,.28,2.45,secondary,1.0,.38,0);}`
- [x] `else` — linha 6024, coluna 5 em `applyWorldVehicleModulesToGroup()` — `else if(parts.body==='offroad'){part(2.12,.24,.42,dark,0,.32,1.42);part(2.12,.24,.42,dark,0,.32,-1.42);for(const x of[-1.02,1.02])part(.20,.42,2.42,dark,x,.44,0);}`
- [x] `if` — linha 6024, coluna 10 em `applyWorldVehicleModulesToGroup()` — `else if(parts.body==='offroad'){part(2.12,.24,.42,dark,0,.32,1.42);part(2.12,.24,.42,dark,0,.32,-1.42);for(const x of[-1.02,1.02])part(.20,.42,2.42,dark,x,.44,0);}`
- [x] `for` — linha 6024, coluna 108 em `applyWorldVehicleModulesToGroup()` — `else if(parts.body==='offroad'){part(2.12,.24,.42,dark,0,.32,1.42);part(2.12,.24,.42,dark,0,.32,-1.42);for(const x of[-1.02,1.02])part(.20,.42,2.42,dark,x,.44,0);}`
- [x] `else` — linha 6025, coluna 5 em `applyWorldVehicleModulesToGroup()` — `else if(parts.body==='service'){part(1.92,.20,.36,accent,0,.34,1.39);part(1.92,.20,.36,accent,0,.34,-1.39);part(.12,.34,2.3,accent,-.97,.48,0);part(.12,.34,2.3,accent,.97,.48,0);}`
- [x] `if` — linha 6025, coluna 10 em `applyWorldVehicleModulesToGroup()` — `else if(parts.body==='service'){part(1.92,.20,.36,accent,0,.34,1.39);part(1.92,.20,.36,accent,0,.34,-1.39);part(.12,.34,2.3,accent,-.97,.48,0);part(.12,.34,2.3,accent,.97,.48,0);}`
- [x] `if` — linha 6026, coluna 5 em `applyWorldVehicleModulesToGroup()` — `if(parts.hood==='intake')part(.72,.24,.64,secondary,0,.98,.73);else if(parts.hood==='power'){part(.74,.18,.66,energy,0,.99,.73);part(.12,.08,.72,accent,0,1.10,.73);}`
- [x] `else` — linha 6026, coluna 68 em `applyWorldVehicleModulesToGroup()` — `if(parts.hood==='intake')part(.72,.24,.64,secondary,0,.98,.73);else if(parts.hood==='power'){part(.74,.18,.66,energy,0,.99,.73);part(.12,.08,.72,accent,0,1.10,.73);}`
- [x] `if` — linha 6026, coluna 73 em `applyWorldVehicleModulesToGroup()` — `if(parts.hood==='intake')part(.72,.24,.64,secondary,0,.98,.73);else if(parts.hood==='power'){part(.74,.18,.66,energy,0,.99,.73);part(.12,.08,.72,accent,0,1.10,.73);}`
- [x] `if` — linha 6027, coluna 5 em `applyWorldVehicleModulesToGroup()` — `if(durability>20&&parts.roof==='rack'){for(const x of[-.62,.62])part(.10,.36,1.18,dark,x,1.36,-.42);for(const z of[-.92,.02])part(1.36,.10,.10,dark,0,1.55,z);}`
- [x] `for` — linha 6027, coluna 44 em `applyWorldVehicleModulesToGroup()` — `if(durability>20&&parts.roof==='rack'){for(const x of[-.62,.62])part(.10,.36,1.18,dark,x,1.36,-.42);for(const z of[-.92,.02])part(1.36,.10,.10,dark,0,1.55,z);}`
- [x] `for` — linha 6027, coluna 105 em `applyWorldVehicleModulesToGroup()` — `if(durability>20&&parts.roof==='rack'){for(const x of[-.62,.62])part(.10,.36,1.18,dark,x,1.36,-.42);for(const z of[-.92,.02])part(1.36,.10,.10,dark,0,1.55,z);}`
- [x] `else` — linha 6028, coluna 5 em `applyWorldVehicleModulesToGroup()` — `else if(durability>20&&parts.roof==='beacon'){part(1.18,.12,.22,dark,0,1.31,-.42);part(.34,.22,.24,energy,-.32,1.47,-.42);part(.34,.22,.24,accent,.32,1.47,-.42);}`
- [x] `if` — linha 6028, coluna 10 em `applyWorldVehicleModulesToGroup()` — `else if(durability>20&&parts.roof==='beacon'){part(1.18,.12,.22,dark,0,1.31,-.42);part(.34,.22,.24,energy,-.32,1.47,-.42);part(.34,.22,.24,accent,.32,1.47,-.42);}`
- [x] `if` — linha 6029, coluna 5 em `applyWorldVehicleModulesToGroup()` — `if(durability>10&&parts.rear==='spoiler'){part(1.46,.12,.34,secondary,0,1.12,-1.32);for(const x of[-.5,.5])part(.10,.44,.10,dark,x,.91,-1.23);}`
- [x] `for` — linha 6029, coluna 89 em `applyWorldVehicleModulesToGroup()` — `if(durability>10&&parts.rear==='spoiler'){part(1.46,.12,.34,secondary,0,1.12,-1.32);for(const x of[-.5,.5])part(.10,.44,.10,dark,x,.91,-1.23);}`
- [x] `else` — linha 6030, coluna 5 em `applyWorldVehicleModulesToGroup()` — `else if(durability>10&&parts.rear==='box')part(1.32,.66,.82,secondary,0,.93,-1.12);`
- [x] `if` — linha 6030, coluna 10 em `applyWorldVehicleModulesToGroup()` — `else if(durability>10&&parts.rear==='box')part(1.32,.66,.82,secondary,0,.93,-1.12);`
- [x] `for` — linha 6032, coluna 5 em `applyWorldVehicleModulesToGroup()` — `for(const [x,z]of[[-.86,-.8],[.86,-.8],[-.86,.8],[.86,.8]]){const wheel=new THREE.Mesh(new THREE.CylinderGeometry(wheelRadius,wheelRadius,wheelWidth,14),wheelMaterial);wheel.position.set(x,.24,z);wheel.rotation.z=Math.PI/2;wheel.castShadow=true;modules.add(wheel);const hub=new THREE.Mesh(new THREE.CylinderGeometry(.13,.13,wheelWidth+.03,10),parts.wheels==='sport'?accent:secondary);hub.position.copy(wheel.position);hub.rotation.z=Math.PI/2;modules.add(hub);}`
- [x] `for` — linha 6033, coluna 277 em `applyWorldVehicleModulesToGroup()` — `const lightMaterial=parts.lights==='energy'?energy:worldVehicleModuleMaterial(parts.lights==='white'?0xffffff:0xffe7a1,.1,.2);lightMaterial.emissive=new THREE.Color(parts.lights==='energy'?0x28c7ef:0xffc95a);lightMaterial.emissiveIntensity=parts.lights==='energy'?1.1:.72;for(const x of[-.58,.58])part(.34,.18,.08,lightMaterial,x,.52,1.37);`
- [x] `return` — linha 6034, coluna 122 em `applyWorldVehicleModulesToGroup()` — `modules.userData.vehicleId=vehicleId;modules.userData.durability=durability;otthiWorldRuntime.stats.vehicleModules++;return true;`
- [x] `return` — linha 6037, coluna 165 em `applyWorldVehicleModulesToGroup()` — `createToyCar=function createToyCarWorld(x,z,options={}){const vehicle=legacyCreateToyCar(x,z,options);applyWorldVehicleModulesToGroup(vehicle?.group,vehicle?.id);return vehicle;};`
- [x] `if` — linha 6039, coluna 110 em `applyWorldVehicleModulesToGroup()` — `applyVehicleAppearance=function applyVehicleAppearanceWorld(vehicle){legacyApplyVehicleAppearance(vehicle);if(vehicleVisual&&vehicle?.id)applyWorldVehicleModulesToGroup(vehicleVisual,vehicle.id);};`
- [x] `if` — linha 6041, coluna 157 em `applyWorldVehicleModulesToGroup()` — `registerVehicleImpact=function registerVehicleImpactWorld(){const vehicle=currentVehicleRef(),id=vehicle?.id||player.car.id;legacyRegisterVehicleImpact();if(!id)return;const before=Number(state.vehicles.partDurability[id]??100),after=clamp(before-5,0,100);state.vehicles.partDurability[id]=after;if(vehicle?.group)applyWorldVehicleModulesToGroup(vehicle.group,id);if(player.vehicle&&vehicleVisual)applyWorldVehicleModulesToGroup(vehicleVisual,id);if(before>20&&after<=20)toast('Uma peça externa ficou solta. Visite a oficina para reparar.','warn',2400);saveState();};`
- [x] `return` — linha 6041, coluna 164 em `applyWorldVehicleModulesToGroup()` — `registerVehicleImpact=function registerVehicleImpactWorld(){const vehicle=currentVehicleRef(),id=vehicle?.id||player.car.id;legacyRegisterVehicleImpact();if(!id)return;const before=Number(state.vehicles.partDurability[id]??100),after=clamp(before-5,0,100);state.vehicles.partDurability[id]=after;if(vehicle?.group)applyWorldVehicleModulesToGroup(vehicle.group,id);if(player.vehicle&&vehicleVisual)applyWorldVehicleModulesToGroup(vehicleVisual,id);if(before>20&&after<=20)toast('Uma peça externa ficou solta. Visite a oficina para reparar.','warn',2400);saveState();};`
- [x] `if` — linha 6041, coluna 299 em `applyWorldVehicleModulesToGroup()` — `registerVehicleImpact=function registerVehicleImpactWorld(){const vehicle=currentVehicleRef(),id=vehicle?.id||player.car.id;legacyRegisterVehicleImpact();if(!id)return;const before=Number(state.vehicles.partDurability[id]??100),after=clamp(before-5,0,100);state.vehicles.partDurability[id]=after;if(vehicle?.group)applyWorldVehicleModulesToGroup(vehicle.group,id);if(player.vehicle&&vehicleVisual)applyWorldVehicleModulesToGroup(vehicleVisual,id);if(before>20&&after<=20)toast('Uma peça externa ficou solta. Visite a oficina para reparar.','warn',2400);saveState();};`
- [x] `if` — linha 6041, coluna 367 em `applyWorldVehicleModulesToGroup()` — `registerVehicleImpact=function registerVehicleImpactWorld(){const vehicle=currentVehicleRef(),id=vehicle?.id||player.car.id;legacyRegisterVehicleImpact();if(!id)return;const before=Number(state.vehicles.partDurability[id]??100),after=clamp(before-5,0,100);state.vehicles.partDurability[id]=after;if(vehicle?.group)applyWorldVehicleModulesToGroup(vehicle.group,id);if(player.vehicle&&vehicleVisual)applyWorldVehicleModulesToGroup(vehicleVisual,id);if(before>20&&after<=20)toast('Uma peça externa ficou solta. Visite a oficina para reparar.','warn',2400);saveState();};`
- [x] `if` — linha 6041, coluna 450 em `applyWorldVehicleModulesToGroup()` — `registerVehicleImpact=function registerVehicleImpactWorld(){const vehicle=currentVehicleRef(),id=vehicle?.id||player.car.id;legacyRegisterVehicleImpact();if(!id)return;const before=Number(state.vehicles.partDurability[id]??100),after=clamp(before-5,0,100);state.vehicles.partDurability[id]=after;if(vehicle?.group)applyWorldVehicleModulesToGroup(vehicle.group,id);if(player.vehicle&&vehicleVisual)applyWorldVehicleModulesToGroup(vehicleVisual,id);if(before>20&&after<=20)toast('Uma peça externa ficou solta. Visite a oficina para reparar.','warn',2400);saveState();};`
- [x] `return` — linha 6042, coluna 50 em `vehicleCatalogOptions()` — `function vehicleCatalogOptions(field,selected){return OTTHI_WORLD_VEHICLE_CATALOG[field].map(([id,name])=>\`<button class="avatar-option ${selected===id?'selected':''}" data-world-vehicle-field="${field}" data-world-vehicle-value="${id}"><b>${field==='wheels'?'◉':field==='roof'?'▰':field==='lights'?'✦':'▣'}</b><span>${name}</span></button>\`).join('');}`
- [x] `if` — linha 6047, coluna 365 em `openWorldModularGarage()` — `$$('[data-world-vehicle-field]',root).forEach(button=>button.onclick=()=>{const target=worldVehicleParts(id),field=button.dataset.worldVehicleField,value=button.dataset.worldVehicleValue;target[field]=value;state.vehicles.modularParts[id]=target;$$(\`[data-world-vehicle-field="${field}"]\`,root).forEach(item=>item.classList.toggle('selected',item===button));if(vehicle?.group)applyWorldVehicleModulesToGroup(vehicle.group,id);if(player.vehicle&&player.car.id===id)applyWorldVehicleModulesToGroup(vehicleVisual,id);});`
- [x] `if` — linha 6047, coluna 433 em `openWorldModularGarage()` — `$$('[data-world-vehicle-field]',root).forEach(button=>button.onclick=()=>{const target=worldVehicleParts(id),field=button.dataset.worldVehicleField,value=button.dataset.worldVehicleValue;target[field]=value;state.vehicles.modularParts[id]=target;$$(\`[data-world-vehicle-field="${field}"]\`,root).forEach(item=>item.classList.toggle('selected',item===button));if(vehicle?.group)applyWorldVehicleModulesToGroup(vehicle.group,id);if(player.vehicle&&player.car.id===id)applyWorldVehicleModulesToGroup(vehicleVisual,id);});`
- [x] `if` — linha 6048, coluna 262 em `openWorldModularGarage()` — `$$('[data-world-vehicle-color]',root).forEach(input=>input.oninput=()=>{const target=worldVehicleParts(id);target[input.dataset.worldVehicleColor]=safeAvatarColor(input.value,target[input.dataset.worldVehicleColor]);state.vehicles.modularParts[id]=target;if(vehicle?.group)applyWorldVehicleModulesToGroup(vehicle.group,id);if(player.vehicle&&player.car.id===id)applyWorldVehicleModulesToGroup(vehicleVisual,id);});`
- [x] `if` — linha 6048, coluna 330 em `openWorldModularGarage()` — `$$('[data-world-vehicle-color]',root).forEach(input=>input.oninput=()=>{const target=worldVehicleParts(id);target[input.dataset.worldVehicleColor]=safeAvatarColor(input.value,target[input.dataset.worldVehicleColor]);state.vehicles.modularParts[id]=target;if(vehicle?.group)applyWorldVehicleModulesToGroup(vehicle.group,id);if(player.vehicle&&player.car.id===id)applyWorldVehicleModulesToGroup(vehicleVisual,id);});`
- [x] `if` — linha 6050, coluna 96 em `openWorldModularGarage()` — `$('[data-world-vehicle-repair]',root).onclick=()=>{state.vehicles.partDurability[id]=100;if(vehicle?.group)applyWorldVehicleModulesToGroup(vehicle.group,id);if(player.vehicle&&player.car.id===id)applyWorldVehicleModulesToGroup(vehicleVisual,id);saveState(true);openWorldModularGarage(id);toast('Peças reparadas.','good',1600);};`
- [x] `if` — linha 6050, coluna 164 em `openWorldModularGarage()` — `$('[data-world-vehicle-repair]',root).onclick=()=>{state.vehicles.partDurability[id]=100;if(vehicle?.group)applyWorldVehicleModulesToGroup(vehicle.group,id);if(player.vehicle&&player.car.id===id)applyWorldVehicleModulesToGroup(vehicleVisual,id);saveState(true);openWorldModularGarage(id);toast('Peças reparadas.','good',1600);};`
- [x] `if` — linha 6054, coluna 5 em `createWorldModularGarageInteractable()` — `if(world.interactables?.some(item=>item.id==='otthi-world-garage'))return false;registerInteractable({id:'otthi-world-garage',type:'workshop',icon:'🛠',label:'Abrir oficina modular OTTHI',x:22,z:-13.5,radius:3.2,priority:244,action:()=>openWorldModularGarage()});otthiWorldRuntime.modularVehiclesReady=true;return true;`
- [x] `return` — linha 6054, coluna 72 em `createWorldModularGarageInteractable()` — `if(world.interactables?.some(item=>item.id==='otthi-world-garage'))return false;registerInteractable({id:'otthi-world-garage',type:'workshop',icon:'🛠',label:'Abrir oficina modular OTTHI',x:22,z:-13.5,radius:3.2,priority:244,action:()=>openWorldModularGarage()});otthiWorldRuntime.modularVehiclesReady=true;return true;`
- [x] `return` — linha 6054, coluna 311 em `createWorldModularGarageInteractable()` — `if(world.interactables?.some(item=>item.id==='otthi-world-garage'))return false;registerInteractable({id:'otthi-world-garage',type:'workshop',icon:'🛠',label:'Abrir oficina modular OTTHI',x:22,z:-13.5,radius:3.2,priority:244,action:()=>openWorldModularGarage()});otthiWorldRuntime.modularVehiclesReady=true;return true;`

## `37-hero-platform-gameplay.js`

- [x] `return` — linha 6065, coluna 53 em `worldHeroState()` — `function worldHeroState(){ensureOtthiWorldState();return state.adventures.hero;}`
- [x] `return` — linha 6066, coluna 34 em `heroPowerUnlocked()` — `function heroPowerUnlocked(id){return worldHeroState().unlocked.includes(id);}`
- [x] `return` — linha 6067, coluna 62 em `heroPowerReady()` — `function heroPowerReady(power){const hero=worldHeroState();return performance.now()-Number(hero.lastUse||0)>=power.cooldown&&hero.energy>=power.cost;}`
- [x] `if` — linha 6069, coluna 5 em `createHeroPulseVisual()` — `if(!worldGroup)return;const material=new THREE.MeshBasicMaterial({color,transparent:true,opacity:.8,depthWrite:false,side:THREE.DoubleSide}),ring=new THREE.Mesh(new THREE.RingGeometry(.3,.52,32),material);ring.rotation.x=-Math.PI/2;ring.position.set(player.x,player.y+.08,player.z);worldGroup.add(ring);const start=performance.now();const animate=()=>{const t=clamp((performance.now()-start)/480,0,1);ring.scale.setScalar(1+t*radius*2.2);material.opacity=.8*(1-t);if(t<1)requestAnimationFrame(animate);else{worldGroup?.remove(ring);material.dispose();ring.geometry.dispose();}};animate();`
- [x] `return` — linha 6069, coluna 20 em `createHeroPulseVisual()` — `if(!worldGroup)return;const material=new THREE.MeshBasicMaterial({color,transparent:true,opacity:.8,depthWrite:false,side:THREE.DoubleSide}),ring=new THREE.Mesh(new THREE.RingGeometry(.3,.52,32),material);ring.rotation.x=-Math.PI/2;ring.position.set(player.x,player.y+.08,player.z);worldGroup.add(ring);const start=performance.now();const animate=()=>{const t=clamp((performance.now()-start)/480,0,1);ring.scale.setScalar(1+t*radius*2.2);material.opacity=.8*(1-t);if(t<1)requestAnimationFrame(animate);else{worldGroup?.remove(ring);material.dispose();ring.geometry.dispose();}};animate();`
- [x] `if` — linha 6069, coluna 469 em `createHeroPulseVisual()` — `if(!worldGroup)return;const material=new THREE.MeshBasicMaterial({color,transparent:true,opacity:.8,depthWrite:false,side:THREE.DoubleSide}),ring=new THREE.Mesh(new THREE.RingGeometry(.3,.52,32),material);ring.rotation.x=-Math.PI/2;ring.position.set(player.x,player.y+.08,player.z);worldGroup.add(ring);const start=performance.now();const animate=()=>{const t=clamp((performance.now()-start)/480,0,1);ring.scale.setScalar(1+t*radius*2.2);material.opacity=.8*(1-t);if(t<1)requestAnimationFrame(animate);else{worldGroup?.remove(ring);material.dispose();ring.geometry.dispose();}};animate();`
- [x] `else` — linha 6069, coluna 507 em `createHeroPulseVisual()` — `if(!worldGroup)return;const material=new THREE.MeshBasicMaterial({color,transparent:true,opacity:.8,depthWrite:false,side:THREE.DoubleSide}),ring=new THREE.Mesh(new THREE.RingGeometry(.3,.52,32),material);ring.rotation.x=-Math.PI/2;ring.position.set(player.x,player.y+.08,player.z);worldGroup.add(ring);const start=performance.now();const animate=()=>{const t=clamp((performance.now()-start)/480,0,1);ring.scale.setScalar(1+t*radius*2.2);material.opacity=.8*(1-t);if(t<1)requestAnimationFrame(animate);else{worldGroup?.remove(ring);material.dispose();ring.geometry.dispose();}};animate();`
- [x] `if` — linha 6072, coluna 5 em `activateWorldHeroPower()` — `if(!running||paused||!els.modal.hidden||player.vehicle||player.boating||player.transit.mode||currentHouse){toast('Use os poderes do lado de fora e sem transporte.','warn',1800);return false;}const power=OTTHI_WORLD_HERO_POWERS[id];if(!power||!heroPowerUnlocked(id)){toast('Este poder ainda precisa ser desbloqueado no desafio OTTHI.','warn');return false;}const hero=worldHeroState();if(!heroPowerReady(power)){const wait=Math.max(0,Math.ceil((power.cooldown-(performance.now()-hero.lastUse))/1000));toast(hero.energy<power.cost?'Energia OTTHI recarregando.':\`Aguarde ${wait}s para usar novamente.\`,'warn',1300);return false;}`
- [x] `return` — linha 6072, coluna 182 em `activateWorldHeroPower()` — `if(!running||paused||!els.modal.hidden||player.vehicle||player.boating||player.transit.mode||currentHouse){toast('Use os poderes do lado de fora e sem transporte.','warn',1800);return false;}const power=OTTHI_WORLD_HERO_POWERS[id];if(!power||!heroPowerUnlocked(id)){toast('Este poder ainda precisa ser desbloqueado no desafio OTTHI.','warn');return false;}const hero=worldHeroState();if(!heroPowerReady(power)){const wait=Math.max(0,Math.ceil((power.cooldown-(performance.now()-hero.lastUse))/1000));toast(hero.energy<power.cost?'Energia OTTHI recarregando.':\`Aguarde ${wait}s para usar novamente.\`,'warn',1300);return false;}`
- [x] `if` — linha 6072, coluna 236 em `activateWorldHeroPower()` — `if(!running||paused||!els.modal.hidden||player.vehicle||player.boating||player.transit.mode||currentHouse){toast('Use os poderes do lado de fora e sem transporte.','warn',1800);return false;}const power=OTTHI_WORLD_HERO_POWERS[id];if(!power||!heroPowerUnlocked(id)){toast('Este poder ainda precisa ser desbloqueado no desafio OTTHI.','warn');return false;}const hero=worldHeroState();if(!heroPowerReady(power)){const wait=Math.max(0,Math.ceil((power.cooldown-(performance.now()-hero.lastUse))/1000));toast(hero.energy<power.cost?'Energia OTTHI recarregando.':\`Aguarde ${wait}s para usar novamente.\`,'warn',1300);return false;}`
- [x] `return` — linha 6072, coluna 347 em `activateWorldHeroPower()` — `if(!running||paused||!els.modal.hidden||player.vehicle||player.boating||player.transit.mode||currentHouse){toast('Use os poderes do lado de fora e sem transporte.','warn',1800);return false;}const power=OTTHI_WORLD_HERO_POWERS[id];if(!power||!heroPowerUnlocked(id)){toast('Este poder ainda precisa ser desbloqueado no desafio OTTHI.','warn');return false;}const hero=worldHeroState();if(!heroPowerReady(power)){const wait=Math.max(0,Math.ceil((power.cooldown-(performance.now()-hero.lastUse))/1000));toast(hero.energy<power.cost?'Energia OTTHI recarregando.':\`Aguarde ${wait}s para usar novamente.\`,'warn',1300);return false;}`
- [x] `if` — linha 6072, coluna 389 em `activateWorldHeroPower()` — `if(!running||paused||!els.modal.hidden||player.vehicle||player.boating||player.transit.mode||currentHouse){toast('Use os poderes do lado de fora e sem transporte.','warn',1800);return false;}const power=OTTHI_WORLD_HERO_POWERS[id];if(!power||!heroPowerUnlocked(id)){toast('Este poder ainda precisa ser desbloqueado no desafio OTTHI.','warn');return false;}const hero=worldHeroState();if(!heroPowerReady(power)){const wait=Math.max(0,Math.ceil((power.cooldown-(performance.now()-hero.lastUse))/1000));toast(hero.energy<power.cost?'Energia OTTHI recarregando.':\`Aguarde ${wait}s para usar novamente.\`,'warn',1300);return false;}`
- [x] `return` — linha 6072, coluna 617 em `activateWorldHeroPower()` — `if(!running||paused||!els.modal.hidden||player.vehicle||player.boating||player.transit.mode||currentHouse){toast('Use os poderes do lado de fora e sem transporte.','warn',1800);return false;}const power=OTTHI_WORLD_HERO_POWERS[id];if(!power||!heroPowerUnlocked(id)){toast('Este poder ainda precisa ser desbloqueado no desafio OTTHI.','warn');return false;}const hero=worldHeroState();if(!heroPowerReady(power)){const wait=Math.max(0,Math.ceil((power.cooldown-(performance.now()-hero.lastUse))/1000));toast(hero.energy<power.cost?'Energia OTTHI recarregando.':\`Aguarde ${wait}s para usar novamente.\`,'warn',1300);return false;}`
- [x] `if` — linha 6074, coluna 5 em `activateWorldHeroPower()` — `if(id==='energyPulse'){`
- [x] `for` — linha 6075, coluna 43 em `activateWorldHeroPower()` — `createHeroPulseVisual(0x5de6ff,5.4);for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<5.6){damageEnemy(enemy,1);const angle=Math.atan2(enemy.group.position.x-player.x,enemy.group.position.z-player.z);enemy.group.position.x+=Math.sin(angle)*1.8;enemy.group.position.z+=Math.cos(angle)*1.8;}beep(520,110,'sine');`
- [x] `if` — linha 6075, coluna 76 em `activateWorldHeroPower()` — `createHeroPulseVisual(0x5de6ff,5.4);for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<5.6){damageEnemy(enemy,1);const angle=Math.atan2(enemy.group.position.x-player.x,enemy.group.position.z-player.z);enemy.group.position.x+=Math.sin(angle)*1.8;enemy.group.position.z+=Math.cos(angle)*1.8;}beep(520,110,'sine');`
- [x] `else` — linha 6076, coluna 6 em `activateWorldHeroPower()` — `}else if(id==='velocityDash'){`
- [x] `if` — linha 6076, coluna 11 em `activateWorldHeroPower()` — `}else if(id==='velocityDash'){`
- [x] `for` — linha 6077, coluna 143 em `activateWorldHeroPower()` — `const speed=15;player.vx=Math.sin(player.facing)*speed;player.vz=Math.cos(player.facing)*speed;player.damageUntil=performance.now()+720;for(let i=0;i<7;i++)spawnDust(player.x-Math.sin(player.facing)*i*.24,player.z-Math.cos(player.facing)*i*.24);beep(760,80,'square');`
- [x] `else` — linha 6078, coluna 6 em `activateWorldHeroPower()` — `}else if(id==='guardianShield'){`
- [x] `if` — linha 6078, coluna 11 em `activateWorldHeroPower()` — `}else if(id==='guardianShield'){`
- [x] `else` — linha 6080, coluna 6 em `activateWorldHeroPower()` — `}else if(id==='skyLeap'){`
- [x] `if` — linha 6080, coluna 11 em `activateWorldHeroPower()` — `}else if(id==='skyLeap'){`
- [x] `else` — linha 6082, coluna 6 em `activateWorldHeroPower()` — `}else if(id==='magnetWave'){`
- [x] `if` — linha 6082, coluna 11 em `activateWorldHeroPower()` — `}else if(id==='magnetWave'){`
- [x] `for` — linha 6083, coluna 41 em `activateWorldHeroPower()` — `createHeroPulseVisual(0xb77bff,8);for(const crystal of world.crystals)if(!crystal.got&&Math.hypot(player.x-crystal.x,player.z-crystal.z)<9)collectCrystal(crystal);for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<7.5)damageEnemy(enemy,1);beep(410,160,'triangle');`
- [x] `if` — linha 6083, coluna 77 em `activateWorldHeroPower()` — `createHeroPulseVisual(0xb77bff,8);for(const crystal of world.crystals)if(!crystal.got&&Math.hypot(player.x-crystal.x,player.z-crystal.z)<9)collectCrystal(crystal);for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<7.5)damageEnemy(enemy,1);beep(410,160,'triangle');`
- [x] `for` — linha 6083, coluna 170 em `activateWorldHeroPower()` — `createHeroPulseVisual(0xb77bff,8);for(const crystal of world.crystals)if(!crystal.got&&Math.hypot(player.x-crystal.x,player.z-crystal.z)<9)collectCrystal(crystal);for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<7.5)damageEnemy(enemy,1);beep(410,160,'triangle');`
- [x] `if` — linha 6083, coluna 203 em `activateWorldHeroPower()` — `createHeroPulseVisual(0xb77bff,8);for(const crystal of world.crystals)if(!crystal.got&&Math.hypot(player.x-crystal.x,player.z-crystal.z)<9)collectCrystal(crystal);for(const enemy of world.enemies)if(!enemy.dead&&distance2D(player,enemy.group.position)<7.5)damageEnemy(enemy,1);beep(410,160,'triangle');`
- [x] `return` — linha 6085, coluna 58 em `activateWorldHeroPower()` — `vibrate([24,28,34]);saveState();updateWorldHeroHud();return true;`
- [x] `return` — linha 6087, coluna 62 em `worldHeroPowerCards()` — `function worldHeroPowerCards(){const hero=worldHeroState();return Object.values(OTTHI_WORLD_HERO_POWERS).map(power=>{const unlocked=heroPowerUnlocked(power.id),selected=hero.selectedPower===power.id;return\`<button class="hero-power-card ${selected?'selected':''} ${unlocked?'':'locked'}" data-world-hero-power="${power.id}" ${unlocked?'':'disabled'}><span>${power.icon}</span><div><b>${power.name}</b><small>${power.description}</small><i>${unlocked?\`${power.cost} energia\`:'Bloqueado pelo desafio'}</i></div></button>\`;}).join('');}`
- [x] `return` — linha 6087, coluna 202 em `worldHeroPowerCards()` — `function worldHeroPowerCards(){const hero=worldHeroState();return Object.values(OTTHI_WORLD_HERO_POWERS).map(power=>{const unlocked=heroPowerUnlocked(power.id),selected=hero.selectedPower===power.id;return\`<button class="hero-power-card ${selected?'selected':''} ${unlocked?'':'locked'}" data-world-hero-power="${power.id}" ${unlocked?'':'disabled'}><span>${power.icon}</span><div><b>${power.name}</b><small>${power.description}</small><i>${unlocked?\`${power.cost} energia\`:'Bloqueado pelo desafio'}</i></div></button>\`;}).join('');}`
- [x] `return` — linha 6096, coluna 344 em `createHeroPlatform()` — `function createHeroPlatform(x,y,z,w=3,d=3,color=0x365f92){const group=new THREE.Group();group.position.set(x,0,z);worldGroup.add(group);premiumBox(w,.42,d,color,0,y-.21,0,group,0x102238);premiumBox(w*.72,.12,d*.72,otthiWorldRuntime.materials.get('heroEnergy')||0x5de6ff,0,y+.05,0,group,0x0a4056);registerPlatform(x,z,w,d,y,{otthiHero:true});return group;}`
- [x] `return` — linha 6097, coluna 484 em `createHeroToken()` — `function createHeroToken(x,y,z,index){const material=new THREE.MeshStandardMaterial({map:loadWorldTexture('hero-energy','basecolor',{repeat:[1,1],color:true}),emissiveMap:loadWorldTexture('hero-energy','emissive',{repeat:[1,1]}),color:index%2?0xffd75e:0x5de6ff,emissive:index%2?0xa15c00:0x147fae,emissiveIntensity:.82,roughness:.18,metalness:.22}),mesh=new THREE.Mesh(new THREE.OctahedronGeometry(.44,0),material);mesh.position.set(x,y,z);mesh.castShadow=true;worldGroup.add(mesh);return{id:\`hero-token-${index}\`,x,y,z,mesh,index,collected:false};}`
- [x] `if` — linha 6099, coluna 5 em `createWorldHeroAdventure()` — `if(world.worldProfessional?.heroAdventure)return false;const root=new THREE.Group();root.name='OTTHI_WORLD_HERO_ADVENTURE';worldGroup.add(root);const start={x:-41,z:88,y:2.2},platforms=[[-41,2.2,88,5,5],[-34,3.5,91,3.4,3.4],[-27,4.8,87,3.2,3.2],[-20,6.2,92,3.2,3.2],[-12,7.6,88,3.4,3.4],[-4,9.1,93,3.6,3.6],[5,10.5,89,5.2,5.2]];for(const [x,y,z,w,d]of platforms)createHeroPlatform(x,y,z,w,d,y>8?0x7252aa:0x365f92);`
- [x] `return` — linha 6099, coluna 47 em `createWorldHeroAdventure()` — `if(world.worldProfessional?.heroAdventure)return false;const root=new THREE.Group();root.name='OTTHI_WORLD_HERO_ADVENTURE';worldGroup.add(root);const start={x:-41,z:88,y:2.2},platforms=[[-41,2.2,88,5,5],[-34,3.5,91,3.4,3.4],[-27,4.8,87,3.2,3.2],[-20,6.2,92,3.2,3.2],[-12,7.6,88,3.4,3.4],[-4,9.1,93,3.6,3.6],[5,10.5,89,5.2,5.2]];for(const [x,y,z,w,d]of platforms)createHeroPlatform(x,y,z,w,d,y>8?0x7252aa:0x365f92);`
- [x] `for` — linha 6099, coluna 333 em `createWorldHeroAdventure()` — `if(world.worldProfessional?.heroAdventure)return false;const root=new THREE.Group();root.name='OTTHI_WORLD_HERO_ADVENTURE';worldGroup.add(root);const start={x:-41,z:88,y:2.2},platforms=[[-41,2.2,88,5,5],[-34,3.5,91,3.4,3.4],[-27,4.8,87,3.2,3.2],[-20,6.2,92,3.2,3.2],[-12,7.6,88,3.4,3.4],[-4,9.1,93,3.6,3.6],[5,10.5,89,5.2,5.2]];for(const [x,y,z,w,d]of platforms)createHeroPlatform(x,y,z,w,d,y>8?0x7252aa:0x365f92);`
- [x] `for` — linha 6102, coluna 91 em `createWorldHeroAdventure()` — `const finish=new THREE.Group();finish.position.set(5,10.55,89);worldGroup.add(finish);for(const x of[-1.55,1.55])premiumBox(.22,3.2,.22,0xf5d75a,x,1.6,0,finish,0x3c2d0a);premiumBox(3.5,.3,.32,0xf5d75a,0,3.05,0,finish,0x3c2d0a);const banner=new THREE.Mesh(new THREE.PlaneGeometry(3.1,.72),new THREE.MeshStandardMaterial({map:signTexture('OTTHI HERO','#163b62','#ffffff'),side:THREE.DoubleSide,roughness:.5}));banner.position.set(0,2.75,.18);finish.add(banner);`
- [x] `return` — linha 6104, coluna 292 em `createWorldHeroAdventure()` — `world.worldProfessional={...(world.worldProfessional||{}),heroAdventure:{root,portal,platforms,tokens,start,finish,active:false,startedAt:0,collected:0,lastCheckpoint:start}};otthiWorldRuntime.stats.adventureObjects=platforms.length+tokens.length+2;otthiWorldRuntime.adventureReady=true;return true;`
- [x] `if` — linha 6106, coluna 90 em `resetWorldHeroTokens()` — `function resetWorldHeroTokens(){const challenge=world.worldProfessional?.heroAdventure;if(!challenge)return;challenge.collected=0;for(const token of challenge.tokens){token.collected=false;token.mesh.visible=true;}}`
- [x] `return` — linha 6106, coluna 104 em `resetWorldHeroTokens()` — `function resetWorldHeroTokens(){const challenge=world.worldProfessional?.heroAdventure;if(!challenge)return;challenge.collected=0;for(const token of challenge.tokens){token.collected=false;token.mesh.visible=true;}}`
- [x] `for` — linha 6106, coluna 133 em `resetWorldHeroTokens()` — `function resetWorldHeroTokens(){const challenge=world.worldProfessional?.heroAdventure;if(!challenge)return;challenge.collected=0;for(const token of challenge.tokens){token.collected=false;token.mesh.visible=true;}}`
- [x] `if` — linha 6108, coluna 60 em `startWorldHeroChallenge()` — `const challenge=world.worldProfessional?.heroAdventure;if(!challenge){toast('O circuito ainda está sendo preparado.','warn');return false;}if(player.vehicle)exitVehicle(true);if(player.boating)exitBoat(true);if(currentHouse)exitHouse();resetWorldHeroTokens();challenge.active=true;challenge.startedAt=performance.now();challenge.lastCheckpoint=challenge.start;player.x=challenge.start.x;player.z=challenge.start.z;player.y=challenge.start.y+.2;player.vx=player.vz=player.vy=0;player.grounded=true;cameraYaw=Math.PI/2;worldHeroState().energy=100;state.waypoint={id:'otthi-hero-finish',name:'Final do Circuito das Nuvens',x:5,z:89,navX:5,navZ:89,arrived:false};updateWaypointMarker();toast('Circuito iniciado: colete os 6 cristais e alcance o portal final.','good',3000);return true;`
- [x] `return` — linha 6108, coluna 130 em `startWorldHeroChallenge()` — `const challenge=world.worldProfessional?.heroAdventure;if(!challenge){toast('O circuito ainda está sendo preparado.','warn');return false;}if(player.vehicle)exitVehicle(true);if(player.boating)exitBoat(true);if(currentHouse)exitHouse();resetWorldHeroTokens();challenge.active=true;challenge.startedAt=performance.now();challenge.lastCheckpoint=challenge.start;player.x=challenge.start.x;player.z=challenge.start.z;player.y=challenge.start.y+.2;player.vx=player.vz=player.vy=0;player.grounded=true;cameraYaw=Math.PI/2;worldHeroState().energy=100;state.waypoint={id:'otthi-hero-finish',name:'Final do Circuito das Nuvens',x:5,z:89,navX:5,navZ:89,arrived:false};updateWaypointMarker();toast('Circuito iniciado: colete os 6 cristais e alcance o portal final.','good',3000);return true;`
- [x] `if` — linha 6108, coluna 144 em `startWorldHeroChallenge()` — `const challenge=world.worldProfessional?.heroAdventure;if(!challenge){toast('O circuito ainda está sendo preparado.','warn');return false;}if(player.vehicle)exitVehicle(true);if(player.boating)exitBoat(true);if(currentHouse)exitHouse();resetWorldHeroTokens();challenge.active=true;challenge.startedAt=performance.now();challenge.lastCheckpoint=challenge.start;player.x=challenge.start.x;player.z=challenge.start.z;player.y=challenge.start.y+.2;player.vx=player.vz=player.vy=0;player.grounded=true;cameraYaw=Math.PI/2;worldHeroState().energy=100;state.waypoint={id:'otthi-hero-finish',name:'Final do Circuito das Nuvens',x:5,z:89,navX:5,navZ:89,arrived:false};updateWaypointMarker();toast('Circuito iniciado: colete os 6 cristais e alcance o portal final.','good',3000);return true;`
- [x] `if` — linha 6108, coluna 180 em `startWorldHeroChallenge()` — `const challenge=world.worldProfessional?.heroAdventure;if(!challenge){toast('O circuito ainda está sendo preparado.','warn');return false;}if(player.vehicle)exitVehicle(true);if(player.boating)exitBoat(true);if(currentHouse)exitHouse();resetWorldHeroTokens();challenge.active=true;challenge.startedAt=performance.now();challenge.lastCheckpoint=challenge.start;player.x=challenge.start.x;player.z=challenge.start.z;player.y=challenge.start.y+.2;player.vx=player.vz=player.vy=0;player.grounded=true;cameraYaw=Math.PI/2;worldHeroState().energy=100;state.waypoint={id:'otthi-hero-finish',name:'Final do Circuito das Nuvens',x:5,z:89,navX:5,navZ:89,arrived:false};updateWaypointMarker();toast('Circuito iniciado: colete os 6 cristais e alcance o portal final.','good',3000);return true;`
- [x] `if` — linha 6108, coluna 213 em `startWorldHeroChallenge()` — `const challenge=world.worldProfessional?.heroAdventure;if(!challenge){toast('O circuito ainda está sendo preparado.','warn');return false;}if(player.vehicle)exitVehicle(true);if(player.boating)exitBoat(true);if(currentHouse)exitHouse();resetWorldHeroTokens();challenge.active=true;challenge.startedAt=performance.now();challenge.lastCheckpoint=challenge.start;player.x=challenge.start.x;player.z=challenge.start.z;player.y=challenge.start.y+.2;player.vx=player.vz=player.vy=0;player.grounded=true;cameraYaw=Math.PI/2;worldHeroState().energy=100;state.waypoint={id:'otthi-hero-finish',name:'Final do Circuito das Nuvens',x:5,z:89,navX:5,navZ:89,arrived:false};updateWaypointMarker();toast('Circuito iniciado: colete os 6 cristais e alcance o portal final.','good',3000);return true;`
- [x] `return` — linha 6108, coluna 774 em `startWorldHeroChallenge()` — `const challenge=world.worldProfessional?.heroAdventure;if(!challenge){toast('O circuito ainda está sendo preparado.','warn');return false;}if(player.vehicle)exitVehicle(true);if(player.boating)exitBoat(true);if(currentHouse)exitHouse();resetWorldHeroTokens();challenge.active=true;challenge.startedAt=performance.now();challenge.lastCheckpoint=challenge.start;player.x=challenge.start.x;player.z=challenge.start.z;player.y=challenge.start.y+.2;player.vx=player.vz=player.vy=0;player.grounded=true;cameraYaw=Math.PI/2;worldHeroState().energy=100;state.waypoint={id:'otthi-hero-finish',name:'Final do Circuito das Nuvens',x:5,z:89,navX:5,navZ:89,arrived:false};updateWaypointMarker();toast('Circuito iniciado: colete os 6 cristais e alcance o portal final.','good',3000);return true;`
- [x] `if` — linha 6110, coluna 96 em `completeWorldHeroChallenge()` — `function completeWorldHeroChallenge(){const challenge=world.worldProfessional?.heroAdventure;if(!challenge?.active)return;challenge.active=false;const seconds=(performance.now()-challenge.startedAt)/1000,hero=worldHeroState();hero.challengeWins++;hero.challengeBest=hero.challengeBest?Math.min(hero.challengeBest,seconds):seconds;for(const id of['skyLeap','magnetWave'])if(!hero.unlocked.includes(id))hero.unlocked.push(id);hero.energy=100;addXP(180);addCoins(140);awardMedal('Herói do Circuito OTTHI');setFlag('otthiWorldHeroChallenge');state.waypoint=null;updateWaypointMarker();saveState(true).finally(()=>syncCloudProgress(true));toast(\`Circuito concluído em ${seconds.toFixed(1)}s! Novos poderes desbloqueados.\`, 'good',4200);updateWorldHeroHud();}`
- [x] `return` — linha 6110, coluna 118 em `completeWorldHeroChallenge()` — `function completeWorldHeroChallenge(){const challenge=world.worldProfessional?.heroAdventure;if(!challenge?.active)return;challenge.active=false;const seconds=(performance.now()-challenge.startedAt)/1000,hero=worldHeroState();hero.challengeWins++;hero.challengeBest=hero.challengeBest?Math.min(hero.challengeBest,seconds):seconds;for(const id of['skyLeap','magnetWave'])if(!hero.unlocked.includes(id))hero.unlocked.push(id);hero.energy=100;addXP(180);addCoins(140);awardMedal('Herói do Circuito OTTHI');setFlag('otthiWorldHeroChallenge');state.waypoint=null;updateWaypointMarker();saveState(true).finally(()=>syncCloudProgress(true));toast(\`Circuito concluído em ${seconds.toFixed(1)}s! Novos poderes desbloqueados.\`, 'good',4200);updateWorldHeroHud();}`
- [x] `for` — linha 6110, coluna 333 em `completeWorldHeroChallenge()` — `function completeWorldHeroChallenge(){const challenge=world.worldProfessional?.heroAdventure;if(!challenge?.active)return;challenge.active=false;const seconds=(performance.now()-challenge.startedAt)/1000,hero=worldHeroState();hero.challengeWins++;hero.challengeBest=hero.challengeBest?Math.min(hero.challengeBest,seconds):seconds;for(const id of['skyLeap','magnetWave'])if(!hero.unlocked.includes(id))hero.unlocked.push(id);hero.energy=100;addXP(180);addCoins(140);awardMedal('Herói do Circuito OTTHI');setFlag('otthiWorldHeroChallenge');state.waypoint=null;updateWaypointMarker();saveState(true).finally(()=>syncCloudProgress(true));toast(\`Circuito concluído em ${seconds.toFixed(1)}s! Novos poderes desbloqueados.\`, 'good',4200);updateWorldHeroHud();}`
- [x] `if` — linha 6110, coluna 373 em `completeWorldHeroChallenge()` — `function completeWorldHeroChallenge(){const challenge=world.worldProfessional?.heroAdventure;if(!challenge?.active)return;challenge.active=false;const seconds=(performance.now()-challenge.startedAt)/1000,hero=worldHeroState();hero.challengeWins++;hero.challengeBest=hero.challengeBest?Math.min(hero.challengeBest,seconds):seconds;for(const id of['skyLeap','magnetWave'])if(!hero.unlocked.includes(id))hero.unlocked.push(id);hero.energy=100;addXP(180);addCoins(140);awardMedal('Herói do Circuito OTTHI');setFlag('otthiWorldHeroChallenge');state.waypoint=null;updateWaypointMarker();saveState(true).finally(()=>syncCloudProgress(true));toast(\`Circuito concluído em ${seconds.toFixed(1)}s! Novos poderes desbloqueados.\`, 'good',4200);updateWorldHeroHud();}`
- [x] `if` — linha 6112, coluna 150 em `updateWorldHeroAdventure()` — `const hero=worldHeroState(),challenge=world.worldProfessional?.heroAdventure;hero.energy=clamp(hero.energy+dt*(challenge?.active?5.2:8.5),0,100);if(challenge){challenge.portal.rotation.z+=dt*.65;for(const token of challenge.tokens){if(token.collected)continue;token.mesh.rotation.y+=dt*2.2;token.mesh.position.y=token.y+Math.sin(animTime*2.4+token.index)*.16;if(challenge.active&&Math.hypot(player.x-token.x,player.z-token.z)<1.15&&Math.abs(player.y-token.mesh.position.y)<2){token.collected=true;token.mesh.visible=false;challenge.collected++;challenge.lastCheckpoint={x:token.x,y:Math.max(groundHeightAt(token.x,token.z),token.y-1),z:token.z};hero.energy=clamp(hero.energy+18,0,100);addXP(15);beep(920,70,'sine');toast(\`Cristal de energia ${challenge.collected}/${challenge.tokens.length}\`,'good',1100);}}`
- [x] `for` — linha 6112, coluna 200 em `updateWorldHeroAdventure()` — `const hero=worldHeroState(),challenge=world.worldProfessional?.heroAdventure;hero.energy=clamp(hero.energy+dt*(challenge?.active?5.2:8.5),0,100);if(challenge){challenge.portal.rotation.z+=dt*.65;for(const token of challenge.tokens){if(token.collected)continue;token.mesh.rotation.y+=dt*2.2;token.mesh.position.y=token.y+Math.sin(animTime*2.4+token.index)*.16;if(challenge.active&&Math.hypot(player.x-token.x,player.z-token.z)<1.15&&Math.abs(player.y-token.mesh.position.y)<2){token.collected=true;token.mesh.visible=false;challenge.collected++;challenge.lastCheckpoint={x:token.x,y:Math.max(groundHeightAt(token.x,token.z),token.y-1),z:token.z};hero.energy=clamp(hero.energy+18,0,100);addXP(15);beep(920,70,'sine');toast(\`Cristal de energia ${challenge.collected}/${challenge.tokens.length}\`,'good',1100);}}`
- [x] `if` — linha 6112, coluna 237 em `updateWorldHeroAdventure()` — `const hero=worldHeroState(),challenge=world.worldProfessional?.heroAdventure;hero.energy=clamp(hero.energy+dt*(challenge?.active?5.2:8.5),0,100);if(challenge){challenge.portal.rotation.z+=dt*.65;for(const token of challenge.tokens){if(token.collected)continue;token.mesh.rotation.y+=dt*2.2;token.mesh.position.y=token.y+Math.sin(animTime*2.4+token.index)*.16;if(challenge.active&&Math.hypot(player.x-token.x,player.z-token.z)<1.15&&Math.abs(player.y-token.mesh.position.y)<2){token.collected=true;token.mesh.visible=false;challenge.collected++;challenge.lastCheckpoint={x:token.x,y:Math.max(groundHeightAt(token.x,token.z),token.y-1),z:token.z};hero.energy=clamp(hero.energy+18,0,100);addXP(15);beep(920,70,'sine');toast(\`Cristal de energia ${challenge.collected}/${challenge.tokens.length}\`,'good',1100);}}`
- [x] `if` — linha 6112, coluna 364 em `updateWorldHeroAdventure()` — `const hero=worldHeroState(),challenge=world.worldProfessional?.heroAdventure;hero.energy=clamp(hero.energy+dt*(challenge?.active?5.2:8.5),0,100);if(challenge){challenge.portal.rotation.z+=dt*.65;for(const token of challenge.tokens){if(token.collected)continue;token.mesh.rotation.y+=dt*2.2;token.mesh.position.y=token.y+Math.sin(animTime*2.4+token.index)*.16;if(challenge.active&&Math.hypot(player.x-token.x,player.z-token.z)<1.15&&Math.abs(player.y-token.mesh.position.y)<2){token.collected=true;token.mesh.visible=false;challenge.collected++;challenge.lastCheckpoint={x:token.x,y:Math.max(groundHeightAt(token.x,token.z),token.y-1),z:token.z};hero.energy=clamp(hero.energy+18,0,100);addXP(15);beep(920,70,'sine');toast(\`Cristal de energia ${challenge.collected}/${challenge.tokens.length}\`,'good',1100);}}`
- [x] `if` — linha 6113, coluna 7 em `updateWorldHeroAdventure()` — `if(challenge.active&&challenge.collected===challenge.tokens.length&&Math.hypot(player.x-5,player.z-89)<2.4&&player.y>8)completeWorldHeroChallenge();`
- [x] `if` — linha 6114, coluna 7 em `updateWorldHeroAdventure()` — `if(challenge.active&&player.y<-.5){const safe=challenge.lastCheckpoint||challenge.start;player.x=safe.x;player.z=safe.z;player.y=safe.y+.35;player.vx=player.vz=player.vy=0;player.grounded=true;toast('Retorno ao último cristal do circuito.','warn',1300);}`
- [x] `if` — linha 6119, coluna 5 em `ensureWorldHeroHud()` — `if(document.getElementById('worldHeroBtn'))return;const button=document.createElement('button');button.id='worldHeroBtn';button.className='world-hero-action';button.type='button';button.setAttribute('aria-label','Usar poder OTTHI');button.innerHTML='<b data-world-hero-icon>⚡</b><span data-world-hero-energy>100</span>';button.onclick=()=>activateWorldHeroPower();document.getElementById('game')?.appendChild(button);`
- [x] `return` — linha 6119, coluna 48 em `ensureWorldHeroHud()` — `if(document.getElementById('worldHeroBtn'))return;const button=document.createElement('button');button.id='worldHeroBtn';button.className='world-hero-action';button.type='button';button.setAttribute('aria-label','Usar poder OTTHI');button.innerHTML='<b data-world-hero-icon>⚡</b><span data-world-hero-energy>100</span>';button.onclick=()=>activateWorldHeroPower();document.getElementById('game')?.appendChild(button);`
- [x] `if` — linha 6121, coluna 86 em `updateWorldHeroHud()` — `function updateWorldHeroHud(){const button=document.getElementById('worldHeroBtn');if(!button)return;const hero=worldHeroState(),power=OTTHI_WORLD_HERO_POWERS[hero.selectedPower]||OTTHI_WORLD_HERO_POWERS.energyPulse;button.querySelector('[data-world-hero-icon]').textContent=power.icon;button.querySelector('[data-world-hero-energy]').textContent=Math.round(hero.energy);button.disabled=hero.energy<power.cost;button.title=\`${power.name} • ${Math.round(hero.energy)}%\`;}`
- [x] `return` — linha 6121, coluna 97 em `updateWorldHeroHud()` — `function updateWorldHeroHud(){const button=document.getElementById('worldHeroBtn');if(!button)return;const hero=worldHeroState(),power=OTTHI_WORLD_HERO_POWERS[hero.selectedPower]||OTTHI_WORLD_HERO_POWERS.energyPulse;button.querySelector('[data-world-hero-icon]').textContent=power.icon;button.querySelector('[data-world-hero-energy]').textContent=Math.round(hero.energy);button.disabled=hero.energy<power.cost;button.title=\`${power.name} • ${Math.round(hero.energy)}%\`;}`

## `38-otthi-world-integration-bootstrap.js`

- [x] `if` — linha 6125, coluna 79 em `updateWorldHeroHud()` — `initMaterials=function initMaterialsOtthiWorld(){legacyWorldInitMaterials();if(state.settings?.worldRender!==false)upgradeCoreMaterialsToWorldPbr();};`
- [x] `if` — linha 6128, coluna 37 em `updateWorldHeroHud()` — `const ok=legacyWorldInitThree();if(!ok)return false;`
- [x] `return` — linha 6128, coluna 44 em `updateWorldHeroHud()` — `const ok=legacyWorldInitThree();if(!ok)return false;`
- [x] `catch` — linha 6130, coluna 5 em `updateWorldHeroHud()` — `catch(error){otthiWorldRuntime.errors.push(String(error?.stack||error));console.error('[OTTHI WORLD] Camada profissional em fallback:',error);toast('A base do jogo foi preservada; um detalhe profissional iniciou em fallback.','warn',3000);}`
- [x] `return` — linha 6131, coluna 5 em `updateWorldHeroHud()` — `return true;`
- [x] `if` — linha 6134, coluna 76 em `bootstrapOtthiWorldShell()` — `ensureOtthiWorldState();injectOtthiWorldButtons();ensureWorldHeroHud();if(els.avatarBtn)els.avatarBtn.onclick=openAvatarStudio;`
- [x] `catch` — linha 6138, coluna 96 em `bootstrapOtthiWorldShell()` — `dbReady.then(()=>{ensureOtthiWorldState();bootstrapOtthiWorldShell();updateWorldHeroHud();}).catch(()=>{});`
- [x] `if` — linha 6139, coluna 86 em `bootstrapOtthiWorldShell()` — `window.addEventListener('otthi:cloud-progress-merged',()=>{ensureOtthiWorldState();if(playerModel)applyAvatarCustomization();});`
- [x] `return` — linha 6145, coluna 69 em `bootstrapOtthiWorldShell()` — `vehicle:id=>{const vehicle=vehicleById(id)||world.vehicles?.[0];return vehicle?applyWorldVehicleModulesToGroup(vehicle.group,vehicle.id):false;},`
- [x] `if` — linha 6150, coluna 3 em `bootstrapOtthiWorldShell()` — `if(window.OTTHI_TEST_API)window.OTTHI_TEST_API.world=window.OTTHI_WORLD_TESTS;`
